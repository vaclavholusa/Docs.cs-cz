---
uid: mvc/overview/getting-started/getting-started-with-ef-using-mvc/implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application
title: Implementace základních funkcí CRUD s rozhraním Entity Framework v aplikaci ASP.NET MVC | Dokumentace Microsoftu
author: tdykstra
description: Contoso University ukázkovou webovou aplikaci ukazuje, jak vytvářet aplikace ASP.NET MVC 5 pomocí sady Visual Studio a Entity Framework 6 Code First...
ms.author: riande
ms.date: 10/05/2015
ms.assetid: a2f70ba4-83d1-4002-9255-24732726c4f2
msc.legacyurl: /mvc/overview/getting-started/getting-started-with-ef-using-mvc/implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application
msc.type: authoredcontent
ms.openlocfilehash: 08b5d38b38d3323e347f0f849ccc0c25fe49efb9
ms.sourcegitcommit: a4dcca4f1cb81227c5ed3c92dc0e28be6e99447b
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/10/2018
ms.locfileid: "48912653"
---
<a name="implementing-basic-crud-functionality-with-the-entity-framework-in-aspnet-mvc-application"></a><span data-ttu-id="b4008-103">Implementace základních funkcí CRUD s rozhraním Entity Framework v aplikaci ASP.NET MVC</span><span class="sxs-lookup"><span data-stu-id="b4008-103">Implementing Basic CRUD Functionality with the Entity Framework in ASP.NET MVC Application</span></span>
====================
<span data-ttu-id="b4008-104">podle [Petr Dykstra](https://github.com/tdykstra)</span><span class="sxs-lookup"><span data-stu-id="b4008-104">by [Tom Dykstra](https://github.com/tdykstra)</span></span>

[<span data-ttu-id="b4008-105">Stáhnout dokončený projekt</span><span class="sxs-lookup"><span data-stu-id="b4008-105">Download Completed Project</span></span>](http://code.msdn.microsoft.com/ASPNET-MVC-Application-b01a9fe8)

> <span data-ttu-id="b4008-106">Ukázková webová aplikace Contoso University ukazuje, jak vytvářet aplikace ASP.NET MVC 5 pomocí Entity Framework 6 kód první a Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="b4008-106">The Contoso University sample web application demonstrates how to create ASP.NET MVC 5 applications using the Entity Framework 6 Code First and Visual Studio.</span></span> <span data-ttu-id="b4008-107">Informace o této sérii kurzů, naleznete v tématu [z prvního kurzu této série](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md).</span><span class="sxs-lookup"><span data-stu-id="b4008-107">For information about the tutorial series, see [the first tutorial in the series](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md).</span></span>

<span data-ttu-id="b4008-108">V předchozím kurzu jste vytvořili aplikaci MVC, která ukládá a zobrazuje data pomocí rozhraní Entity Framework a SQL Server LocalDB.</span><span class="sxs-lookup"><span data-stu-id="b4008-108">In the previous tutorial, you created an MVC application that stores and displays data using the Entity Framework and SQL Server LocalDB.</span></span> <span data-ttu-id="b4008-109">V tomto kurzu budete zkontrolovat a upravit vytvořit, číst, aktualizovat, odstranění (CRUD) kód, který generování uživatelského rozhraní MVC se automaticky vytvoří za vás do kontrolerů a zobrazení.</span><span class="sxs-lookup"><span data-stu-id="b4008-109">In this tutorial, you'll review and customize the create, read, update, delete (CRUD) code that the MVC scaffolding automatically creates for you in controllers and views.</span></span>

> [!NOTE]
> <span data-ttu-id="b4008-110">Je běžnou praxí pro implementaci vzoru úložiště chcete-li vytvořit vrstvu HAL mezi řadiči a vrstva přístupu k datům.</span><span class="sxs-lookup"><span data-stu-id="b4008-110">It's a common practice to implement the repository pattern in order to create an abstraction layer between your controller and the data access layer.</span></span> <span data-ttu-id="b4008-111">Na tyto kurzy byly jednoduché a zaměřují se na vyučují způsob použití rozhraní Entity Framework samotné, nepoužívejte úložišť.</span><span class="sxs-lookup"><span data-stu-id="b4008-111">To keep these tutorials simple and focused on teaching how to use the Entity Framework itself, they don't use repositories.</span></span> <span data-ttu-id="b4008-112">Informace o tom, jak implementovat úložiště, najdete v článku [mapa obsahu přístupu k Data technologie ASP.NET](../../../../whitepapers/aspnet-data-access-content-map.md).</span><span class="sxs-lookup"><span data-stu-id="b4008-112">For information about how to implement repositories, see the [ASP.NET Data Access Content Map](../../../../whitepapers/aspnet-data-access-content-map.md).</span></span>

<span data-ttu-id="b4008-113">V tomto kurzu vytvoříte následujících webových stránek:</span><span class="sxs-lookup"><span data-stu-id="b4008-113">In this tutorial, you'll create the following web pages:</span></span>

![Student_Details_page](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image1.png)

![Student_Edit_page](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image2.png)

![Student_delete_page](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image3.png)

## <a name="create-a-details-page"></a><span data-ttu-id="b4008-117">Vytvoření stránky podrobností</span><span class="sxs-lookup"><span data-stu-id="b4008-117">Create a Details page</span></span>

<span data-ttu-id="b4008-118">Automaticky generovaný kód pro studenty `Index` vynechali stránky `Enrollments` vlastnost, protože tato vlastnost obsahuje kolekci.</span><span class="sxs-lookup"><span data-stu-id="b4008-118">The scaffolded code for the Students `Index` page left out the `Enrollments` property, because that property holds a collection.</span></span> <span data-ttu-id="b4008-119">V `Details` stránce obsah kolekce bude zobrazen jako tabulku HTML.</span><span class="sxs-lookup"><span data-stu-id="b4008-119">In the `Details` page, you'll display the contents of the collection in an HTML table.</span></span>

<span data-ttu-id="b4008-120">V *Controllers\StudentController.cs*, metoda akce pro `Details` zobrazení používá [najít](https://msdn.microsoft.com/library/gg696418(v=VS.103).aspx) metodu pro načtení jedné `Student` entity.</span><span class="sxs-lookup"><span data-stu-id="b4008-120">In *Controllers\StudentController.cs*, the action method for the `Details` view uses the [Find](https://msdn.microsoft.com/library/gg696418(v=VS.103).aspx) method to retrieve a single `Student` entity.</span></span>

[!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample1.cs)]

<span data-ttu-id="b4008-121">Hodnota klíče, která se předá metodě jako `id` parametr a pochází z *směrování dat* v **podrobnosti** hypertextový odkaz na indexovou stránku.</span><span class="sxs-lookup"><span data-stu-id="b4008-121">The key value is passed to the method as the `id` parameter and comes from *route data* in the **Details** hyperlink on the Index page.</span></span>

### <a name="tip-route-data"></a><span data-ttu-id="b4008-122">Tip: **směrování dat**</span><span class="sxs-lookup"><span data-stu-id="b4008-122">Tip: **Route data**</span></span>

<span data-ttu-id="b4008-123">Data trasy, která jsou data, která v segment adresy URL zadané ve směrovací tabulce nalezen vazač modelu.</span><span class="sxs-lookup"><span data-stu-id="b4008-123">Route data is data that the model binder found in a URL segment specified in the routing table.</span></span> <span data-ttu-id="b4008-124">Například výchozí trasa určuje `controller`, `action`, a `id` segmenty:</span><span class="sxs-lookup"><span data-stu-id="b4008-124">For example, the default route specifies `controller`, `action`, and `id` segments:</span></span>

[!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample2.cs?highlight=3)]

<span data-ttu-id="b4008-125">V následující adrese URL, výchozí trasu mapuje `Instructor` jako `controller`, `Index` jako `action` a 1 stejně jako `id`; jde o hodnot dat trasy.</span><span class="sxs-lookup"><span data-stu-id="b4008-125">In the following URL, the default route maps `Instructor` as the `controller`, `Index` as the `action` and 1 as the `id`; these are route data values.</span></span>

`http://localhost:1230/Instructor/Index/1?courseID=2021`

<span data-ttu-id="b4008-126">`?courseID=2021` je hodnota řetězce dotazu.</span><span class="sxs-lookup"><span data-stu-id="b4008-126">`?courseID=2021` is a query string value.</span></span> <span data-ttu-id="b4008-127">Vazač modelu budou fungovat i Pokud předáte `id` jako hodnotu řetězce dotazu:</span><span class="sxs-lookup"><span data-stu-id="b4008-127">The model binder will also work if you pass the `id` as a query string value:</span></span>

`http://localhost:1230/Instructor/Index?id=1&CourseID=2021`

<span data-ttu-id="b4008-128">Adresy URL jsou vytvořeny pomocí `ActionLink` příkazy v zobrazení Razor.</span><span class="sxs-lookup"><span data-stu-id="b4008-128">The URLs are created by `ActionLink` statements in the Razor view.</span></span> <span data-ttu-id="b4008-129">V následujícím kódu `id` parametr odpovídá výchozí trasu, takže `id` se přidá do data trasy.</span><span class="sxs-lookup"><span data-stu-id="b4008-129">In the following code, the `id` parameter matches the default route, so `id` is added to the route data.</span></span>

[!code-cshtml[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample3.cshtml)]

<span data-ttu-id="b4008-130">V následujícím kódu `courseID` neodpovídá parametru v této výchozí trase, tak se přidá jako řetězec dotazu.</span><span class="sxs-lookup"><span data-stu-id="b4008-130">In the following code, `courseID` doesn't match a parameter in the default route, so it's added as a query string.</span></span>

[!code-cshtml[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample4.cshtml)]

### <a name="to-create-the-details-page"></a><span data-ttu-id="b4008-131">Chcete-li vytvořit na stránce podrobností</span><span class="sxs-lookup"><span data-stu-id="b4008-131">To create the Details page</span></span>

1. <span data-ttu-id="b4008-132">Otevřít *Views\Student\Details.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b4008-132">Open *Views\Student\Details.cshtml*.</span></span>

   <span data-ttu-id="b4008-133">Každé pole je zobrazeno pomocí `DisplayFor` pomocné rutiny, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="b4008-133">Each field is displayed using a `DisplayFor` helper, as shown in the following example:</span></span>

    [!code-cshtml[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample5.cshtml)]

2. <span data-ttu-id="b4008-134">Po `EnrollmentDate` pole a bezprostředně před uzavírací `</dl>` značky, přidejte zvýrazněný kód k zobrazení seznamu registrací, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="b4008-134">After the `EnrollmentDate` field and immediately before the closing `</dl>` tag, add the highlighted code to display a list of enrollments, as shown in the following example:</span></span>

    [!code-cshtml[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample6.cshtml?highlight=8-29)]

    <span data-ttu-id="b4008-135">Pokud po vložení kódu je nesprávné odsazení kódu, stiskněte **Ctrl**+**K**, **Ctrl**+**D** k formátování ho.</span><span class="sxs-lookup"><span data-stu-id="b4008-135">If code indentation is wrong after you paste the code, press **Ctrl**+**K**, **Ctrl**+**D** to format it.</span></span>

    <span data-ttu-id="b4008-136">Tento kód projde entity v `Enrollments` navigační vlastnost.</span><span class="sxs-lookup"><span data-stu-id="b4008-136">This code loops through the entities in the `Enrollments` navigation property.</span></span> <span data-ttu-id="b4008-137">Pro každou `Enrollment` entity ve vlastnosti, zobrazí název kurzu a na podnikové úrovni.</span><span class="sxs-lookup"><span data-stu-id="b4008-137">For each `Enrollment` entity in the property, it displays the course title and the grade.</span></span> <span data-ttu-id="b4008-138">Název kurzu je načten z `Course` entita, která je uložena v `Course` vlastnost navigace `Enrollments` entity.</span><span class="sxs-lookup"><span data-stu-id="b4008-138">The course title is retrieved from the `Course` entity that's stored in the `Course` navigation property of the `Enrollments` entity.</span></span> <span data-ttu-id="b4008-139">Všechna tato data se načtou z databáze automaticky když ho nepotřebují.</span><span class="sxs-lookup"><span data-stu-id="b4008-139">All of this data is retrieved from the database automatically when it's needed.</span></span> <span data-ttu-id="b4008-140">Jinými slovy že používáte opožděné načtení tady.</span><span class="sxs-lookup"><span data-stu-id="b4008-140">In other words, you are using lazy loading here.</span></span> <span data-ttu-id="b4008-141">Nezadali jste *předběžné načítání* pro `Courses` vlastnost navigace, takže registrací nebyly načteny ve stejném dotazu, které získali studenty.</span><span class="sxs-lookup"><span data-stu-id="b4008-141">You did not specify *eager loading* for the `Courses` navigation property, so the enrollments were not retrieved in the same query that got the students.</span></span> <span data-ttu-id="b4008-142">Místo toho při prvním pokusu o přístup k `Enrollments` navigační vlastnost, nový dotaz se odesílají do databáze načíst data.</span><span class="sxs-lookup"><span data-stu-id="b4008-142">Instead, the first time you try to access the `Enrollments` navigation property, a new query is sent to the database to retrieve the data.</span></span> <span data-ttu-id="b4008-143">Další informace o opožděné načtení a nemůžou dočkat, až načítání v [čtení souvisejících dat](reading-related-data-with-the-entity-framework-in-an-asp-net-mvc-application.md) později v této sérii kurzů.</span><span class="sxs-lookup"><span data-stu-id="b4008-143">You can read more about lazy loading and eager loading in the [Reading Related Data](reading-related-data-with-the-entity-framework-in-an-asp-net-mvc-application.md) tutorial later in this series.</span></span>

3. <span data-ttu-id="b4008-144">Otevřete stránku podrobností spuštěním programu (**Ctrl**+**F5**), vyberete **studenty** kartu a pak levým na **podrobnosti** odkaz Alexander Carsonovy.</span><span class="sxs-lookup"><span data-stu-id="b4008-144">Open the Details page by starting the program (**Ctrl**+**F5**), selecting the **Students** tab, and then clicking the **Details** link for Alexander Carson.</span></span> <span data-ttu-id="b4008-145">(Pokud stisknete **Ctrl**+**F5** zatímco *Details.cshtml* je soubor otevřen, dojde k chybě HTTP 400.</span><span class="sxs-lookup"><span data-stu-id="b4008-145">(If you press **Ctrl**+**F5** while the *Details.cshtml* file is open, you get an HTTP 400 error.</span></span> <span data-ttu-id="b4008-146">Je to proto, že Visual Studio se pokusí spustit na stránce podrobností, ale nebyl dosažen z odkazu, který určuje student k zobrazení.</span><span class="sxs-lookup"><span data-stu-id="b4008-146">This is because Visual Studio tries to run the Details page, but it wasn't reached from a link that specifies the student to display.</span></span> <span data-ttu-id="b4008-147">Pokud k tomu dojde, odeberte "Student/Details" z adresy URL a zkuste to znovu, nebo, zavřete prohlížeč, klikněte pravým tlačítkem na projekt a klikněte na tlačítko **zobrazení** > **zobrazit v prohlížeči**.)</span><span class="sxs-lookup"><span data-stu-id="b4008-147">If that happens, remove "Student/Details" from the URL and try again, or, close the browser, right-click the project, and click **View** > **View in Browser**.)</span></span>

    <span data-ttu-id="b4008-148">Zobrazí seznam kurzů a známek pro vybrané studenta:</span><span class="sxs-lookup"><span data-stu-id="b4008-148">You see the list of courses and grades for the selected student:</span></span>

    ![Student_Details_page](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image4.png)

4. <span data-ttu-id="b4008-150">Zavřete prohlížeč.</span><span class="sxs-lookup"><span data-stu-id="b4008-150">Close the browser.</span></span>

## <a name="update-the-create-page"></a><span data-ttu-id="b4008-151">Aktualizovat stránku vytvořit</span><span class="sxs-lookup"><span data-stu-id="b4008-151">Update the Create page</span></span>

1. <span data-ttu-id="b4008-152">V *Controllers\StudentController.cs*, nahraďte <xref:System.Web.Mvc.HttpPostAttribute> `Create` metody akce s následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="b4008-152">In *Controllers\StudentController.cs*, replace the <xref:System.Web.Mvc.HttpPostAttribute> `Create` action method with the following code.</span></span> <span data-ttu-id="b4008-153">Tento kód přidá `try-catch` bloku a odebere `ID` z <xref:System.Web.Mvc.BindAttribute> atributů pro metodu vygenerované:</span><span class="sxs-lookup"><span data-stu-id="b4008-153">This code adds a `try-catch` block and removes `ID` from the <xref:System.Web.Mvc.BindAttribute> attribute for the scaffolded method:</span></span>

    [!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample7.cs?highlight=3,5-6,13-18)]

    <span data-ttu-id="b4008-154">Tento kód přidá `Student` entita vytvořená pomocí vazače modelu ASP.NET MVC do `Students` entity nastavení a pak uloží změny do databáze.</span><span class="sxs-lookup"><span data-stu-id="b4008-154">This code adds the `Student` entity created by the ASP.NET MVC model binder to the `Students` entity set and then saves the changes to the database.</span></span> <span data-ttu-id="b4008-155">*Vazač modelu* odkazuje na funkci ASP.NET MVC, která provádí, což usnadňuje práci s data odeslaná formuláře; vazač modelu převádí publikování formuláře hodnoty pro typy CLR a předává je na metodu akce v parametrech.</span><span class="sxs-lookup"><span data-stu-id="b4008-155">*Model binder* refers to the ASP.NET MVC functionality that makes it easier for you to work with data submitted by a form; a model binder converts posted form values to CLR types and passes them to the action method in parameters.</span></span> <span data-ttu-id="b4008-156">V takovém případě vytvoří instanci vazače modelu `Student` entity pomocí vlastnosti hodnoty z `Form` kolekce.</span><span class="sxs-lookup"><span data-stu-id="b4008-156">In this case, the model binder instantiates a `Student` entity for you using property values from the `Form` collection.</span></span>

    <span data-ttu-id="b4008-157">Můžete odebrat `ID` z vazba atributu, protože `ID` je hodnota primárního klíče, které SQL Server se nastaví automaticky při vložení řádku.</span><span class="sxs-lookup"><span data-stu-id="b4008-157">You removed `ID` from the Bind attribute because `ID` is the primary key value which SQL Server will set automatically when the row is inserted.</span></span> <span data-ttu-id="b4008-158">Vstup od uživatele nenastaví `ID` hodnotu.</span><span class="sxs-lookup"><span data-stu-id="b4008-158">Input from the user does not set the `ID` value.</span></span>

    <a id="overpost"></a>

    ### <a name="security-warning---the-validateantiforgerytoken-attribute-helps-prevent-cross-site-request-forgerysecurityxsrfcsrf-prevention-in-aspnet-mvc-and-web-pagesmd-attacks-it-requires-a-corresponding-htmlantiforgerytoken-statement-in-the-view-which-youll-see-later"></a><span data-ttu-id="b4008-159">Upozornění zabezpečení – `ValidateAntiForgeryToken` atribut pomáhá zabránit [padělání žádosti více webů](../../security/xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages.md) útoky.</span><span class="sxs-lookup"><span data-stu-id="b4008-159">Security warning - The `ValidateAntiForgeryToken` attribute helps prevent [cross-site request forgery](../../security/xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages.md) attacks.</span></span> <span data-ttu-id="b4008-160">Vyžaduje odpovídající `Html.AntiForgeryToken()` příkaz v zobrazení uvidíte později.</span><span class="sxs-lookup"><span data-stu-id="b4008-160">It requires a corresponding `Html.AntiForgeryToken()` statement in the view, which you'll see later.</span></span>

    <span data-ttu-id="b4008-161">`Bind` Atribut je jedním ze způsobů pro ochranu před *over-pass-the účtování* v vytvářet scénáře.</span><span class="sxs-lookup"><span data-stu-id="b4008-161">The `Bind` attribute is one way to protect against *over-posting* in create scenarios.</span></span> <span data-ttu-id="b4008-162">Předpokládejme například, že `Student` zahrnuje entity `Secret` vlastnost, která nechcete tuto webovou stránku nastavení.</span><span class="sxs-lookup"><span data-stu-id="b4008-162">For example, suppose the `Student` entity includes a `Secret` property that you don't want this web page to set.</span></span>

    [!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample8.cs?highlight=7)]

    <span data-ttu-id="b4008-163">I v případě, že nemáte `Secret` pole na webové stránce, se hacker použít nástroj, jako [fiddler](http://fiddler2.com/home), nebo napište určitého kódu JavaScript, k publikování `Secret` tvoří hodnotu.</span><span class="sxs-lookup"><span data-stu-id="b4008-163">Even if you don't have a `Secret` field on the web page, a hacker could use a tool such as [fiddler](http://fiddler2.com/home), or write some JavaScript, to post a `Secret` form value.</span></span> <span data-ttu-id="b4008-164">Bez <xref:System.Web.Mvc.BindAttribute> atribut pole, která používá vazač modelu při vytváření omezení `Student` instance<em>,</em> vazače modelu bude pokračovat, který `Secret` formu hodnotu a použijte ji k vytvoření `Student` entity instance.</span><span class="sxs-lookup"><span data-stu-id="b4008-164">Without the <xref:System.Web.Mvc.BindAttribute> attribute limiting the fields that the model binder uses when it creates a `Student` instance<em>,</em> the model binder would pick up that `Secret` form value and use it to create the `Student` entity instance.</span></span> <span data-ttu-id="b4008-165">Pak cokoli, co hodnota kyberzločinci zadaný pro `Secret` pole formuláře se aktualizují v databázi.</span><span class="sxs-lookup"><span data-stu-id="b4008-165">Then whatever value the hacker specified for the `Secret` form field would be updated in your database.</span></span> <span data-ttu-id="b4008-166">Následující obrázek ukazuje, fiddleru nástroj pro přidání `Secret` pole (hodnotu "OverPost") na hodnoty odeslaného formuláře.</span><span class="sxs-lookup"><span data-stu-id="b4008-166">The following image shows the fiddler tool adding the `Secret` field (with the value "OverPost") to the posted form values.</span></span>

    ![](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image5.png)

    <span data-ttu-id="b4008-167">Hodnota "OverPost" by poté úspěšně přidány do `Secret` vlastností vloženého řádku, i když jste zamýšleli nikdy, že je pro webové stránky moct nastavit tuto vlastnost.</span><span class="sxs-lookup"><span data-stu-id="b4008-167">The value "OverPost" would then be successfully added to the `Secret` property of the inserted row, although you never intended that the web page be able to set that property.</span></span>

    <span data-ttu-id="b4008-168">Je nejvhodnější použít `Include` parametr `Bind` atribut *seznamu povolených IP adres* pole.</span><span class="sxs-lookup"><span data-stu-id="b4008-168">It's best to use the `Include` parameter with the `Bind` attribute to *whitelist* fields.</span></span> <span data-ttu-id="b4008-169">Je také možné použít `Exclude` parametr *blacklist* pole, které chcete vyloučit.</span><span class="sxs-lookup"><span data-stu-id="b4008-169">It's also possible to use the `Exclude` parameter to *blacklist* fields you want to exclude.</span></span> <span data-ttu-id="b4008-170">Z důvodu `Include` je bezpečnější je, že při přidání nové vlastnosti do entity nové pole nejsou chráněny automaticky `Exclude` seznamu.</span><span class="sxs-lookup"><span data-stu-id="b4008-170">The reason `Include` is more secure is that when you add a new property to the entity, the new field is not automatically protected by an `Exclude` list.</span></span>

    <span data-ttu-id="b4008-171">Je možné zabránit overposting ve scénářích upravit, je nejprve čtení entit z databáze a následným voláním `TryUpdateModel`, předejte v seznamu povolených explicitní vlastností.</span><span class="sxs-lookup"><span data-stu-id="b4008-171">You can prevent overposting in edit scenarios is by reading the entity from the database first and then calling `TryUpdateModel`, passing in an explicit allowed properties list.</span></span> <span data-ttu-id="b4008-172">To je metoda použitá v těchto kurzech.</span><span class="sxs-lookup"><span data-stu-id="b4008-172">That is the method used in these tutorials.</span></span>

    <span data-ttu-id="b4008-173">Alternativní způsob zabránění overposting, který mnoho vývojářů preferuje se má zobrazit modely, nikoli tříd entit pomocí vazby modelu.</span><span class="sxs-lookup"><span data-stu-id="b4008-173">An alternative way to prevent overposting that is preferred by many developers is to use view models rather than entity classes with model binding.</span></span> <span data-ttu-id="b4008-174">Zahrnout pouze vlastnosti, které chcete aktualizovat v modelu zobrazení.</span><span class="sxs-lookup"><span data-stu-id="b4008-174">Include only the properties you want to update in the view model.</span></span> <span data-ttu-id="b4008-175">Po dokončení vazač modelu MVC zkopírovat vlastnosti zobrazení modelu do instance entity, případně můžete použít nástroj, jako [AutoMapper](http://automapper.org/).</span><span class="sxs-lookup"><span data-stu-id="b4008-175">Once the MVC model binder has finished, copy the view model properties to the entity instance, optionally using a tool such as [AutoMapper](http://automapper.org/).</span></span> <span data-ttu-id="b4008-176">Použití databáze. Položka na zapisovanou instanci entity k nastavení stavu Unchanged a potom nastavte Property("PropertyName"). IsModified na hodnotu true na každé vlastnosti entity, která je součástí modelu zobrazení.</span><span class="sxs-lookup"><span data-stu-id="b4008-176">Use db.Entry on the entity instance to set its state to Unchanged, and then set Property("PropertyName").IsModified to true on each entity property that is included in the view model.</span></span> <span data-ttu-id="b4008-177">Tato metoda funguje v obou upravovat a vytvářet scénáře.</span><span class="sxs-lookup"><span data-stu-id="b4008-177">This method works in both edit and create scenarios.</span></span>

    <span data-ttu-id="b4008-178">Jiné než `Bind` atribut, `try-catch` blok je pouze změny provedené na automaticky generovaný kód.</span><span class="sxs-lookup"><span data-stu-id="b4008-178">Other than the `Bind` attribute, the `try-catch` block is the only change you've made to the scaffolded code.</span></span> <span data-ttu-id="b4008-179">Pokud výjimka, která je odvozena z <xref:System.Data.DataException> je zachycena, zatímco se ukládají se změny, zobrazí se obecné chybové zprávy.</span><span class="sxs-lookup"><span data-stu-id="b4008-179">If an exception that derives from <xref:System.Data.DataException> is caught while the changes are being saved, a generic error message is displayed.</span></span> <span data-ttu-id="b4008-180"><xref:System.Data.DataException> výjimky jsou někdy způsobeny něco mimo aplikací, nikoli programovací chyba, takže uživatele se doporučuje to chcete zkusit znovu.</span><span class="sxs-lookup"><span data-stu-id="b4008-180"><xref:System.Data.DataException> exceptions are sometimes caused by something external to the application rather than a programming error, so the user is advised to try again.</span></span> <span data-ttu-id="b4008-181">I když není implementovaná v této ukázce, typicky zaznamenávají produkční kvality aplikace výjimku.</span><span class="sxs-lookup"><span data-stu-id="b4008-181">Although not implemented in this sample, a production quality application would log the exception.</span></span> <span data-ttu-id="b4008-182">Další informace najdete v tématu **protokolu insight** tématu [monitorování a Telemetrie (vytváření skutečných cloudových aplikací s Azure)](../../../../aspnet/overview/developing-apps-with-windows-azure/building-real-world-cloud-apps-with-windows-azure/monitoring-and-telemetry.md#log).</span><span class="sxs-lookup"><span data-stu-id="b4008-182">For more information, see the **Log for insight** section in [Monitoring and Telemetry (Building Real-World Cloud Apps with Azure)](../../../../aspnet/overview/developing-apps-with-windows-azure/building-real-world-cloud-apps-with-windows-azure/monitoring-and-telemetry.md#log).</span></span>

    <span data-ttu-id="b4008-183">Kód v *Views\Student\Create.cshtml* je podobný viděli v *Details.cshtml*, s tím rozdílem, že `EditorFor` a `ValidationMessageFor` pomocné rutiny, které se používají pro každé pole namísto `DisplayFor`.</span><span class="sxs-lookup"><span data-stu-id="b4008-183">The code in *Views\Student\Create.cshtml* is similar to what you saw in *Details.cshtml*, except that `EditorFor` and `ValidationMessageFor` helpers are used for each field instead of `DisplayFor`.</span></span> <span data-ttu-id="b4008-184">Tady je příslušný kód:</span><span class="sxs-lookup"><span data-stu-id="b4008-184">Here is the relevant code:</span></span>

    [!code-cshtml[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample9.cshtml)]

    <span data-ttu-id="b4008-185">*Create.chstml* také zahrnuje `@Html.AntiForgeryToken()`, který pracuje s `ValidateAntiForgeryToken` atribut v kontroleru, aby se zabránilo [padělání žádosti více webů](../../security/xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages.md) útoky.</span><span class="sxs-lookup"><span data-stu-id="b4008-185">*Create.chstml* also includes `@Html.AntiForgeryToken()`, which works with the `ValidateAntiForgeryToken` attribute in the controller to help prevent [cross-site request forgery](../../security/xsrfcsrf-prevention-in-aspnet-mvc-and-web-pages.md) attacks.</span></span>

    <span data-ttu-id="b4008-186">Nevyžaduje žádné změny v *Create.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="b4008-186">No changes are required in *Create.cshtml*.</span></span>

2. <span data-ttu-id="b4008-187">Spuštěním programu spustit na stránce Výběr **studenty** kartu a pak levým na **vytvořit nový**.</span><span class="sxs-lookup"><span data-stu-id="b4008-187">Run the page by starting the program, selecting the **Students** tab, and then clicking **Create New**.</span></span>

3. <span data-ttu-id="b4008-188">Zadejte názvy a neplatné datum a klikněte na tlačítko **vytvořit** zobrazíte chybovou zprávu.</span><span class="sxs-lookup"><span data-stu-id="b4008-188">Enter names and an invalid date and click **Create** to see the error message.</span></span>

    ![Students_Create_page_error_message](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image6.png)

    <span data-ttu-id="b4008-190">Toto je ověřování na straně serveru, který získáte ve výchozím nastavení.</span><span class="sxs-lookup"><span data-stu-id="b4008-190">This is server-side validation that you get by default.</span></span> <span data-ttu-id="b4008-191">V pozdějších kurzech uvidíte, jak přidat atributy, které generují kód pro ověřování na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="b4008-191">In a later tutorial, you'll see how to add attributes that generate code for client-side validation.</span></span> <span data-ttu-id="b4008-192">Následující zvýrazněný kód ukazuje model ověřování vrácení se změnami **vytvořit** metody.</span><span class="sxs-lookup"><span data-stu-id="b4008-192">The following highlighted code shows the model validation check in the **Create** method.</span></span>

    [!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample10.cs?highlight=1)]

4. <span data-ttu-id="b4008-193">Změňte na platnou hodnotu data a klikněte na tlačítko **vytvořit** zobrazíte nového objektu student joinkind **Index** stránky.</span><span class="sxs-lookup"><span data-stu-id="b4008-193">Change the date to a valid value and click **Create** to see the new student appear in the **Index** page.</span></span>

    ![Students_Index_page_with_new_student](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image7.png)

5. <span data-ttu-id="b4008-195">Zavřete prohlížeč.</span><span class="sxs-lookup"><span data-stu-id="b4008-195">Close the browser.</span></span>

## <a name="update-the-edit-httppost-method"></a><span data-ttu-id="b4008-196">Metoda HttpPost upravit aktualizace</span><span class="sxs-lookup"><span data-stu-id="b4008-196">Update the Edit HttpPost method</span></span>

1. <span data-ttu-id="b4008-197">Nahradit <xref:System.Web.Mvc.HttpPostAttribute> `Edit` metody akce s následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="b4008-197">Replace the <xref:System.Web.Mvc.HttpPostAttribute> `Edit` action method with the following code:</span></span>

   [!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample11.cs)]

   > [!NOTE]
   > <span data-ttu-id="b4008-198">V *Controllers\StudentController.cs*, `HttpGet Edit` – metoda (jeden bez `HttpPost` atribut) používá `Find` metodu pro načtení vybraných `Student` entity, jako jste viděli v `Details`metody.</span><span class="sxs-lookup"><span data-stu-id="b4008-198">In *Controllers\StudentController.cs*, the `HttpGet Edit` method (the one without the `HttpPost` attribute) uses the `Find` method to retrieve the selected `Student` entity, as you saw in the `Details` method.</span></span> <span data-ttu-id="b4008-199">Nemusíte změnit tuto metodu.</span><span class="sxs-lookup"><span data-stu-id="b4008-199">You don't need to change this method.</span></span>

   <span data-ttu-id="b4008-200">Tyto změny implementovat osvědčeným postupem zabezpečení, aby se zabránilo [overposting](#overpost), generátor vygeneruje `Bind` atribut a přidání entity vytvořené v entitě nastavení pomocí příznaku změněné vazače modelu.</span><span class="sxs-lookup"><span data-stu-id="b4008-200">These changes implement a security best practice to prevent [overposting](#overpost), The scaffolder generated a `Bind` attribute and added the entity created by the model binder to the entity set with a Modified flag.</span></span> <span data-ttu-id="b4008-201">Že kód už nedoporučuje, protože `Bind` atribut vymaže všechny už existující data v polích, které nejsou uvedené v `Include` parametru.</span><span class="sxs-lookup"><span data-stu-id="b4008-201">That code is no longer recommended because the `Bind` attribute clears out any pre-existing data in fields not listed in the `Include` parameter.</span></span> <span data-ttu-id="b4008-202">V budoucnu, generátor řadiče MVC aktualizují tak, aby negeneroval `Bind` atributy pro úpravy metody.</span><span class="sxs-lookup"><span data-stu-id="b4008-202">In the future, the MVC controller scaffolder will be updated so that it doesn't generate `Bind` attributes for Edit methods.</span></span>

   <span data-ttu-id="b4008-203">Nový kód čte existující entity a volání <xref:System.Web.Mvc.Controller.TryUpdateModel%2A> aktualizace polí ze vstupu uživatele v datech odeslaného formuláře.</span><span class="sxs-lookup"><span data-stu-id="b4008-203">The new code reads the existing entity and calls <xref:System.Web.Mvc.Controller.TryUpdateModel%2A> to update fields from user input in the posted form data.</span></span> <span data-ttu-id="b4008-204">Entity Framework automatické sledování sad změn [EntityState.Modified](<xref:System.Data.EntityState.Modified>) příznak v entitě.</span><span class="sxs-lookup"><span data-stu-id="b4008-204">The Entity Framework's automatic change tracking sets the [EntityState.Modified](<xref:System.Data.EntityState.Modified>) flag on the entity.</span></span> <span data-ttu-id="b4008-205">Když [SaveChanges](https://msdn.microsoft.com/library/system.data.entity.dbcontext.savechanges(v=VS.103).aspx) metoda je volána, <xref:System.Data.EntityState.Modified> příznak způsobí, že rozhraní Entity Framework pro vytváření příkazů SQL aktualizujte řádek databáze.</span><span class="sxs-lookup"><span data-stu-id="b4008-205">When the [SaveChanges](https://msdn.microsoft.com/library/system.data.entity.dbcontext.savechanges(v=VS.103).aspx) method is called, the <xref:System.Data.EntityState.Modified> flag causes the Entity Framework to create SQL statements to update the database row.</span></span> <span data-ttu-id="b4008-206">[Konflikty souběžnosti](handling-concurrency-with-the-entity-framework-in-an-asp-net-mvc-application.md) jsou ignorovány, a jsou aktualizovány všechny sloupce řádek databáze, včetně těch, které uživatel nezměnil.</span><span class="sxs-lookup"><span data-stu-id="b4008-206">[Concurrency conflicts](handling-concurrency-with-the-entity-framework-in-an-asp-net-mvc-application.md) are ignored, and all columns of the database row are updated, including those that the user didn't change.</span></span> <span data-ttu-id="b4008-207">(Vyšší kurz ukazuje postupy při zpracování konfliktů souběžnosti, a pokud chcete jenom jednotlivá pole v databázi, můžete nastavit entity na [EntityState.Unchanged](<xref:System.Data.EntityState.Unchanged>) a nastavte jednotlivá pole na [ EntityState.Modified](<xref:System.Data.EntityState.Modified>).)</span><span class="sxs-lookup"><span data-stu-id="b4008-207">(A later tutorial shows how to handle concurrency conflicts, and if you only want individual fields to be updated in the database, you can set the entity to [EntityState.Unchanged](<xref:System.Data.EntityState.Unchanged>) and set individual fields to [EntityState.Modified](<xref:System.Data.EntityState.Modified>).)</span></span>

   <span data-ttu-id="b4008-208">Pokud chcete zabránit overposting, pole, která mají být možné aktualizovat pomocí stránky pro úpravu jsou povolené v `TryUpdateModel` parametry.</span><span class="sxs-lookup"><span data-stu-id="b4008-208">To prevent overposting, the fields that you want to be updateable by the Edit page are whitelisted in the `TryUpdateModel` parameters.</span></span> <span data-ttu-id="b4008-209">Aktuálně nejsou žádné další pole, které chcete chránit, ale seznam polí, která má vazač modelu pro vazbu zajistí, že pokud přidáte pole do datového modelu v budoucnu, že jsou automaticky chráněné dokud explicitně přidáte je tady.</span><span class="sxs-lookup"><span data-stu-id="b4008-209">Currently there are no extra fields that you're protecting, but listing the fields that you want the model binder to bind ensures that if you add fields to the data model in the future, they're automatically protected until you explicitly add them here.</span></span>

   <span data-ttu-id="b4008-210">V důsledku těchto změn podpis metody HttpPost úpravy metody je stejná jako metoda úprav HttpGet; Proto když jste přejmenovali metoda EditPost.</span><span class="sxs-lookup"><span data-stu-id="b4008-210">As a result of these changes, the method signature of the HttpPost Edit method is the same as the HttpGet edit method; therefore you've renamed the method EditPost.</span></span>

   > [!TIP]
   >
   > <span data-ttu-id="b4008-211">**Stavy entity a připojit a SaveChanges metody**</span><span class="sxs-lookup"><span data-stu-id="b4008-211">**Entity States and the Attach and SaveChanges Methods**</span></span>
   >
   > <span data-ttu-id="b4008-212">Uchovává informace o kontextu databáze, jestli jsou synchronizované s jejich odpovídajících řádků v databázi entity v paměti a těchto informací Určuje, co se stane při volání `SaveChanges` metody.</span><span class="sxs-lookup"><span data-stu-id="b4008-212">The database context keeps track of whether entities in memory are in sync with their corresponding rows in the database, and this information determines what happens when you call the `SaveChanges` method.</span></span> <span data-ttu-id="b4008-213">Například při předání do nové entity [přidat](https://msdn.microsoft.com/library/system.data.entity.dbset.add(v=vs.103).aspx) metoda, která stav entity je nastavený na `Added`.</span><span class="sxs-lookup"><span data-stu-id="b4008-213">For example, when you pass a new entity to the [Add](https://msdn.microsoft.com/library/system.data.entity.dbset.add(v=vs.103).aspx) method, that entity's state is set to `Added`.</span></span> <span data-ttu-id="b4008-214">Potom při volání [SaveChanges](https://msdn.microsoft.com/library/system.data.entity.dbcontext.savechanges(v=VS.103).aspx) metoda, vydá kontext databáze SQL `INSERT` příkazu.</span><span class="sxs-lookup"><span data-stu-id="b4008-214">Then when you call the [SaveChanges](https://msdn.microsoft.com/library/system.data.entity.dbcontext.savechanges(v=VS.103).aspx) method, the database context issues a SQL `INSERT` command.</span></span>
   >
   > <span data-ttu-id="b4008-215">Entita může být v jednom z následujících [stavy](xref:System.Data.EntityState):</span><span class="sxs-lookup"><span data-stu-id="b4008-215">An entity may be in one of the following [states](xref:System.Data.EntityState):</span></span>
   >
   > - <span data-ttu-id="b4008-216">`Added`.</span><span class="sxs-lookup"><span data-stu-id="b4008-216">`Added`.</span></span> <span data-ttu-id="b4008-217">Entita ještě neexistuje v databázi.</span><span class="sxs-lookup"><span data-stu-id="b4008-217">The entity does not yet exist in the database.</span></span> <span data-ttu-id="b4008-218">`SaveChanges` Metody musíte vydat `INSERT` příkazu.</span><span class="sxs-lookup"><span data-stu-id="b4008-218">The `SaveChanges` method must issue an `INSERT` statement.</span></span>
   > - <span data-ttu-id="b4008-219">`Unchanged`.</span><span class="sxs-lookup"><span data-stu-id="b4008-219">`Unchanged`.</span></span> <span data-ttu-id="b4008-220">Co je potřeba udělat s touto entitou podle `SaveChanges` metody.</span><span class="sxs-lookup"><span data-stu-id="b4008-220">Nothing needs to be done with this entity by the `SaveChanges` method.</span></span> <span data-ttu-id="b4008-221">Při čtení entity z databáze entity je na začátku s tímto stavem.</span><span class="sxs-lookup"><span data-stu-id="b4008-221">When you read an entity from the database, the entity starts out with this status.</span></span>
   > - <span data-ttu-id="b4008-222">`Modified`.</span><span class="sxs-lookup"><span data-stu-id="b4008-222">`Modified`.</span></span> <span data-ttu-id="b4008-223">Některé nebo všechny hodnoty vlastností entity byly změněny.</span><span class="sxs-lookup"><span data-stu-id="b4008-223">Some or all of the entity's property values have been modified.</span></span> <span data-ttu-id="b4008-224">`SaveChanges` Metody musíte vydat `UPDATE` příkazu.</span><span class="sxs-lookup"><span data-stu-id="b4008-224">The `SaveChanges` method must issue an `UPDATE` statement.</span></span>
   > - <span data-ttu-id="b4008-225">`Deleted`.</span><span class="sxs-lookup"><span data-stu-id="b4008-225">`Deleted`.</span></span> <span data-ttu-id="b4008-226">Entita byla označena k odstranění.</span><span class="sxs-lookup"><span data-stu-id="b4008-226">The entity has been marked for deletion.</span></span> <span data-ttu-id="b4008-227">`SaveChanges` Metody musíte vydat `DELETE` příkazu.</span><span class="sxs-lookup"><span data-stu-id="b4008-227">The `SaveChanges` method must issue a `DELETE` statement.</span></span>
   > - <span data-ttu-id="b4008-228">`Detached`.</span><span class="sxs-lookup"><span data-stu-id="b4008-228">`Detached`.</span></span> <span data-ttu-id="b4008-229">Entita není sledován správou kontext databáze.</span><span class="sxs-lookup"><span data-stu-id="b4008-229">The entity isn't being tracked by the database context.</span></span>
   >
   > <span data-ttu-id="b4008-230">V desktopové aplikaci změny stavu se obvykle nastaví automaticky.</span><span class="sxs-lookup"><span data-stu-id="b4008-230">In a desktop application, state changes are typically set automatically.</span></span> <span data-ttu-id="b4008-231">V typu desktopové aplikace čtení entity a provést změny na některé z jeho hodnot vlastností.</span><span class="sxs-lookup"><span data-stu-id="b4008-231">In a desktop type of application, you read an entity and make changes to some of its property values.</span></span> <span data-ttu-id="b4008-232">To způsobí, že jeho stav entity automaticky změnil na `Modified`.</span><span class="sxs-lookup"><span data-stu-id="b4008-232">This causes its entity state to automatically be changed to `Modified`.</span></span> <span data-ttu-id="b4008-233">Potom při volání `SaveChanges`, Entity Framework generuje SQL `UPDATE` příkaz, který aktualizuje pouze skutečné vlastnosti, které jste změnili.</span><span class="sxs-lookup"><span data-stu-id="b4008-233">Then when you call `SaveChanges`, the Entity Framework generates a SQL `UPDATE` statement that updates only the actual properties that you changed.</span></span>
   >
   > <span data-ttu-id="b4008-234">Toto pořadí průběžné neumožňuje odpojené povaha služby web apps.</span><span class="sxs-lookup"><span data-stu-id="b4008-234">The disconnected nature of web apps doesn't allow for this continuous sequence.</span></span> <span data-ttu-id="b4008-235">[DbContext](https://msdn.microsoft.com/library/system.data.entity.dbcontext(v=VS.103).aspx) , která načte entity je uvolněn po vykreslení stránky.</span><span class="sxs-lookup"><span data-stu-id="b4008-235">The [DbContext](https://msdn.microsoft.com/library/system.data.entity.dbcontext(v=VS.103).aspx) that reads an entity is disposed after a page is rendered.</span></span> <span data-ttu-id="b4008-236">Když `HttpPost` `Edit` volání metody, vytvoří se nový požadavek a mít novou instanci třídy [DbContext](https://msdn.microsoft.com/library/system.data.entity.dbcontext(v=VS.103).aspx), takže je nutné ručně nastavit stavu entity `Modified.` pak při volání `SaveChanges`, Entity Framework aktualizuje všechny sloupce řádek databáze, protože kontext nemá žádný způsob, jak zjistit vlastnosti, které jste změnili.</span><span class="sxs-lookup"><span data-stu-id="b4008-236">When the `HttpPost` `Edit` action method is called, a new request is made and you have a new instance of the [DbContext](https://msdn.microsoft.com/library/system.data.entity.dbcontext(v=VS.103).aspx), so you have to manually set the entity state to `Modified.` Then when you call `SaveChanges`, the Entity Framework updates all columns of the database row, because the context has no way to know which properties you changed.</span></span>
   >
   > <span data-ttu-id="b4008-237">Pokud chcete, aby SQL `Update` . doje k aktualizaci pouze pole, která uživatel ve skutečnosti změní, můžete uložit původní hodnoty nějakým způsobem (třeba skrytá pole) tak, aby byly k dispozici při `HttpPost` `Edit` metoda je volána.</span><span class="sxs-lookup"><span data-stu-id="b4008-237">If you want the SQL `Update` statement to update only the fields that the user actually changed, you can save the original values in some way (such as hidden fields) so that they are available when the `HttpPost` `Edit` method is called.</span></span> <span data-ttu-id="b4008-238">Můžete vytvořit `Student` entity pomocí původní hodnoty, volání `Attach` metodu s původní verzi entitou, aktualizujte hodnoty entity na nové hodnoty a následně zavolat `SaveChanges.` Další informace najdete v tématu [ Stavy entity a SaveChanges](/ef/ef6/saving/change-tracking/entity-state) a [místních dat](/ef/ef6/querying/local-data).</span><span class="sxs-lookup"><span data-stu-id="b4008-238">Then you can create a `Student` entity using the original values, call the `Attach` method with that original version of the entity, update the entity's values to the new values, and then call `SaveChanges.` For more information, see [Entity states and SaveChanges](/ef/ef6/saving/change-tracking/entity-state) and [Local Data](/ef/ef6/querying/local-data).</span></span>

   <span data-ttu-id="b4008-239">Kód HTML a Razor v *Views\Student\Edit.cshtml* je podobný viděli v *Create.cshtml*, a nejsou potřeba žádné změny.</span><span class="sxs-lookup"><span data-stu-id="b4008-239">The HTML and Razor code in *Views\Student\Edit.cshtml* is similar to what you saw in *Create.cshtml*, and no changes are required.</span></span>

2. <span data-ttu-id="b4008-240">Spuštěním programu spustit na stránce Výběr **studenty** kartu a pak levým na **upravit** hypertextový odkaz.</span><span class="sxs-lookup"><span data-stu-id="b4008-240">Run the page by starting the program, selecting the **Students** tab, and then clicking an **Edit** hyperlink.</span></span>

   ![Student_Edit_page](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image8.png)

3. <span data-ttu-id="b4008-242">Některá data a klikněte na tlačítko Změnit **Uložit**.</span><span class="sxs-lookup"><span data-stu-id="b4008-242">Change some of the data and click **Save**.</span></span> <span data-ttu-id="b4008-243">Zobrazí změněných dat v indexovou stránku.</span><span class="sxs-lookup"><span data-stu-id="b4008-243">You see the changed data in the Index page.</span></span>

   ![Students_Index_page_after_edit](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image9.png)

4. <span data-ttu-id="b4008-245">Zavřete prohlížeč.</span><span class="sxs-lookup"><span data-stu-id="b4008-245">Close the browser.</span></span>

## <a name="update-the-delete-page"></a><span data-ttu-id="b4008-246">Aktualizovat stránku Delete</span><span class="sxs-lookup"><span data-stu-id="b4008-246">Update the Delete page</span></span>

<span data-ttu-id="b4008-247">V *Controllers\StudentController.cs*, kód šablony pro <xref:System.Web.Mvc.HttpGetAttribute> `Delete` metoda používá `Find` metodu pro načtení vybraných `Student` entity, jako jste viděli v `Details` a `Edit` metody.</span><span class="sxs-lookup"><span data-stu-id="b4008-247">In *Controllers\StudentController.cs*, the template code for the <xref:System.Web.Mvc.HttpGetAttribute> `Delete` method uses the `Find` method to retrieve the selected `Student` entity, as you saw in the `Details` and `Edit` methods.</span></span> <span data-ttu-id="b4008-248">Však implementovat vlastní chybová zpráva při volání `SaveChanges` selže, přidáte některé funkce pro tuto metodu a jeho odpovídající zobrazení.</span><span class="sxs-lookup"><span data-stu-id="b4008-248">However, to implement a custom error message when the call to `SaveChanges` fails, you'll add some functionality to this method and its corresponding view.</span></span>

<span data-ttu-id="b4008-249">Už znáte aktualizace a operace vytvoření, odstranění operace vyžadují dvě metody akce.</span><span class="sxs-lookup"><span data-stu-id="b4008-249">As you saw for update and create operations, delete operations require two action methods.</span></span> <span data-ttu-id="b4008-250">Metoda, která je volána v reakci na požadavek GET zobrazí zobrazení, které umožňuje uživateli možnost schválit nebo zrušit operaci odstranění zopakovat.</span><span class="sxs-lookup"><span data-stu-id="b4008-250">The method that is called in response to a GET request displays a view that gives the user a chance to approve or cancel the delete operation.</span></span> <span data-ttu-id="b4008-251">Pokud uživatel ho buď schválí, vytvoří se požadavek POST.</span><span class="sxs-lookup"><span data-stu-id="b4008-251">If the user approves it, a POST request is created.</span></span> <span data-ttu-id="b4008-252">Pokud k tomu dojde, `HttpPost` `Delete` volání metody a pak tato metoda ve skutečnosti provádí operaci odstranění zopakovat.</span><span class="sxs-lookup"><span data-stu-id="b4008-252">When that happens, the `HttpPost` `Delete` method is called and then that method actually performs the delete operation.</span></span>

<span data-ttu-id="b4008-253">Přidáte `try-catch` bloku <xref:System.Web.Mvc.HttpPostAttribute> `Delete` metodu ke zpracování všechny chyby, které mohou nastat při aktualizaci databáze.</span><span class="sxs-lookup"><span data-stu-id="b4008-253">You'll add a `try-catch` block to the <xref:System.Web.Mvc.HttpPostAttribute> `Delete` method to handle any errors that might occur when the database is updated.</span></span> <span data-ttu-id="b4008-254">Pokud dojde k chybě <xref:System.Web.Mvc.HttpPostAttribute> `Delete` volání metod <xref:System.Web.Mvc.HttpGetAttribute> `Delete` metodu předáním parametru, která označuje, že došlo k chybě.</span><span class="sxs-lookup"><span data-stu-id="b4008-254">If an error occurs, the <xref:System.Web.Mvc.HttpPostAttribute> `Delete` method calls the <xref:System.Web.Mvc.HttpGetAttribute> `Delete` method, passing it a parameter that indicates that an error has occurred.</span></span> <span data-ttu-id="b4008-255"><xref:System.Web.Mvc.HttpGetAttribute> `Delete` Metoda pak znovu zobrazí na stránce potvrzení spolu s chybovou zprávu, poskytnou tak uživateli možnost zrušit, nebo to zkuste znovu.</span><span class="sxs-lookup"><span data-stu-id="b4008-255">The <xref:System.Web.Mvc.HttpGetAttribute> `Delete` method then redisplays the confirmation page along with the error message, giving the user an opportunity to cancel or try again.</span></span>

1. <span data-ttu-id="b4008-256">Nahradit <xref:System.Web.Mvc.HttpGetAttribute> `Delete` metody akce, která spravuje zpráv o chybách následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="b4008-256">Replace the <xref:System.Web.Mvc.HttpGetAttribute> `Delete` action method with the following code, which manages error reporting:</span></span>

    [!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample12.cs?highlight=1,7-10)]

    <span data-ttu-id="b4008-257">Tento kód přijímá [volitelný parametr](https://msdn.microsoft.com/library/dd264739.aspx) , která označuje, zda byla volána metoda po selhání, uložte změny.</span><span class="sxs-lookup"><span data-stu-id="b4008-257">This code accepts an [optional parameter](https://msdn.microsoft.com/library/dd264739.aspx) that indicates whether the method was called after a failure to save changes.</span></span> <span data-ttu-id="b4008-258">Tento parametr je `false` při `HttpGet` `Delete` metoda je volána bez předchozí chyby.</span><span class="sxs-lookup"><span data-stu-id="b4008-258">This parameter is `false` when the `HttpGet` `Delete` method is called without a previous failure.</span></span> <span data-ttu-id="b4008-259">Pokud je volán `HttpPost` `Delete` je parametr metody v reakci na chybě aktualizace databáze `true` a chybová zpráva je předána do zobrazení.</span><span class="sxs-lookup"><span data-stu-id="b4008-259">When it is called by the `HttpPost` `Delete` method in response to a database update error, the parameter is `true` and an error message is passed to the view.</span></span>

2. <span data-ttu-id="b4008-260">Nahradit <xref:System.Web.Mvc.HttpPostAttribute> `Delete` metody akce (s názvem `DeleteConfirmed`) následujícím kódem, který provádí operace odstranění skutečné a zachytí všechny chyby aktualizace databáze.</span><span class="sxs-lookup"><span data-stu-id="b4008-260">Replace the <xref:System.Web.Mvc.HttpPostAttribute> `Delete` action method (named `DeleteConfirmed`) with the following code, which performs the actual delete operation and catches any database update errors.</span></span>

    [!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample13.cs)]

    <span data-ttu-id="b4008-261">Tento kód načte vybranou entitu, zavolá [odebrat](https://msdn.microsoft.com/library/system.data.entity.dbset.remove(v=vs.103).aspx) metodu pro nastavení stavu entity `Deleted`.</span><span class="sxs-lookup"><span data-stu-id="b4008-261">This code retrieves the selected entity, then calls the [Remove](https://msdn.microsoft.com/library/system.data.entity.dbset.remove(v=vs.103).aspx) method to set the entity's status to `Deleted`.</span></span> <span data-ttu-id="b4008-262">Když `SaveChanges` nazývá SQL `DELETE` příkaz je generován.</span><span class="sxs-lookup"><span data-stu-id="b4008-262">When `SaveChanges` is called, a SQL `DELETE` command is generated.</span></span> <span data-ttu-id="b4008-263">Také jste změnili název metody akce z `DeleteConfirmed` k `Delete`.</span><span class="sxs-lookup"><span data-stu-id="b4008-263">You have also changed the action method name from `DeleteConfirmed` to `Delete`.</span></span> <span data-ttu-id="b4008-264">Automaticky generovaný kód s názvem `HttpPost` `Delete` metoda `DeleteConfirmed` poskytnout `HttpPost` metoda jedinečnou signaturu.</span><span class="sxs-lookup"><span data-stu-id="b4008-264">The scaffolded code named the `HttpPost` `Delete` method `DeleteConfirmed` to give the `HttpPost` method a unique signature.</span></span> <span data-ttu-id="b4008-265">(CLR vyžaduje přetížené metody s parametry jinou metodu.) Teď, když podpisy jsou jedinečné, můžete zůstat u konvence MVC a použijte stejný název pro `HttpPost` a `HttpGet` metody pro odstranění.</span><span class="sxs-lookup"><span data-stu-id="b4008-265">(The CLR requires overloaded methods to have different method parameters.) Now that the signatures are unique, you can stick with the MVC convention and use the same name for the `HttpPost` and `HttpGet` delete methods.</span></span>

    <span data-ttu-id="b4008-266">Pokud je vylepšení výkonu v aplikaci s velkým objemem prioritu, se můžete vyhnout zbytečným jazyka SQL k načtení na řádku tak, že nahradíte řádků kódu, které volají `Find` a `Remove` metody s následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="b4008-266">If improving performance in a high-volume application is a priority, you could avoid an unnecessary SQL query to retrieve the row by replacing the lines of code that call the `Find` and `Remove` methods with the following code:</span></span>

    [!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample14.cs)]

    <span data-ttu-id="b4008-267">Tento kód vytvoří instanci `Student` entity pomocí pouze hodnotu primárního klíče a pak nastaví stav entity `Deleted`.</span><span class="sxs-lookup"><span data-stu-id="b4008-267">This code instantiates a `Student` entity using only the primary key value and then sets the entity state to `Deleted`.</span></span> <span data-ttu-id="b4008-268">To je vše, Entity Framework potřebuje, aby odstranit entitu.</span><span class="sxs-lookup"><span data-stu-id="b4008-268">That's all that the Entity Framework needs in order to delete the entity.</span></span>

    <span data-ttu-id="b4008-269">Jak je uvedeno, `HttpGet` `Delete` metoda neodstraní data.</span><span class="sxs-lookup"><span data-stu-id="b4008-269">As noted, the `HttpGet` `Delete` method doesn't delete the data.</span></span> <span data-ttu-id="b4008-270">Operace delete v reakci na příkaz GET žádosti (nebo pro tento účel, provedení libovolné operace úprav vytvořte operace nebo jiné operace, která se mění data) vytvoří ohrožení zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="b4008-270">Performing a delete operation in response to a GET request (or for that matter, performing any edit operation, create operation, or any other operation that changes data) creates a security risk.</span></span> <span data-ttu-id="b4008-271">Další informace najdete v tématu [46 Tip # aplikace ASP.NET MVC – nepoužívejte odstranit odkazy, protože uživatel vytvořit bezpečnostní díry](http://stephenwalther.com/blog/archive/2009/01/21/asp.net-mvc-tip-46-ndash-donrsquot-use-delete-links-because.aspx) na blogu Stephen Walther.</span><span class="sxs-lookup"><span data-stu-id="b4008-271">For more information, see [ASP.NET MVC Tip #46 — Don't use Delete Links because they create Security Holes](http://stephenwalther.com/blog/archive/2009/01/21/asp.net-mvc-tip-46-ndash-donrsquot-use-delete-links-because.aspx) on Stephen Walther's blog.</span></span>

3. <span data-ttu-id="b4008-272">V *Views\Student\Delete.cshtml*, přidejte mezi chybová zpráva `h2` záhlaví a `h3` záhlaví, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="b4008-272">In *Views\Student\Delete.cshtml*, add an error message between the `h2` heading and the `h3` heading, as shown in the following example:</span></span>

    [!code-cshtml[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample15.cshtml?highlight=2)]

4. <span data-ttu-id="b4008-273">Spuštěním programu spustit na stránce Výběr **studenty** kartu a pak levým na **odstranit** hypertextový odkaz:</span><span class="sxs-lookup"><span data-stu-id="b4008-273">Run the page by starting the program, selecting the **Students** tab, and then clicking a **Delete** hyperlink:</span></span>

    ![Student_Delete_page](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/_static/image10.png)

5. <span data-ttu-id="b4008-275">Zvolte **odstranit** na stránce, která uvádí, že **Opravdu chcete odstranit tento?**.</span><span class="sxs-lookup"><span data-stu-id="b4008-275">Choose **Delete** on the page that says **Are you sure you want to delete this?**.</span></span>

    <span data-ttu-id="b4008-276">Indexovou stránku zobrazuje informace bez odstraněné studentů.</span><span class="sxs-lookup"><span data-stu-id="b4008-276">The Index page displays without the deleted student.</span></span> <span data-ttu-id="b4008-277">(Zobrazí se vám příklad kód v akci pro zpracování chyb [souběžnosti kurzu](handling-concurrency-with-the-entity-framework-in-an-asp-net-mvc-application.md).)</span><span class="sxs-lookup"><span data-stu-id="b4008-277">(You'll see an example of the error handling code in action in the [concurrency tutorial](handling-concurrency-with-the-entity-framework-in-an-asp-net-mvc-application.md).)</span></span>

## <a name="close-database-connections"></a><span data-ttu-id="b4008-278">Připojení k databázi zavřít</span><span class="sxs-lookup"><span data-stu-id="b4008-278">Close database connections</span></span>

<span data-ttu-id="b4008-279">Zavřete připojení k databázi a uvolnit tak prostředky, které drží co nejdříve, uvolnění instance kontextu, až budete hotovi s ním.</span><span class="sxs-lookup"><span data-stu-id="b4008-279">To close database connections and free up the resources they hold as soon as possible, dispose the context instance when you are done with it.</span></span> <span data-ttu-id="b4008-280">To znamená, proč obsahuje automaticky generovaný kód [Dispose](https://msdn.microsoft.com/library/system.idisposable.dispose(v=vs.110).aspx) metoda na konci `StudentController` třídy v *StudentController.cs*, jak je znázorněno v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="b4008-280">That is why the scaffolded code provides a [Dispose](https://msdn.microsoft.com/library/system.idisposable.dispose(v=vs.110).aspx) method at the end of the `StudentController` class in *StudentController.cs*, as shown in the following example:</span></span>

[!code-csharp[Main](implementing-basic-crud-functionality-with-the-entity-framework-in-asp-net-mvc-application/samples/sample16.cs)]

<span data-ttu-id="b4008-281">Základní `Controller` implementuje třída již `IDisposable` rozhraní, takže tento kód jednoduše přidá přepsání `Dispose(bool)` metoda explicitně uvolnit instance kontextu.</span><span class="sxs-lookup"><span data-stu-id="b4008-281">The base `Controller` class already implements the `IDisposable` interface, so this code simply adds an override to the `Dispose(bool)` method to explicitly dispose the context instance.</span></span>

## <a name="handle-transactions"></a><span data-ttu-id="b4008-282">Zpracování transakcí</span><span class="sxs-lookup"><span data-stu-id="b4008-282">Handle transactions</span></span>

<span data-ttu-id="b4008-283">Ve výchozím nastavení rozhraní Entity Framework implementuje implicitně transakce.</span><span class="sxs-lookup"><span data-stu-id="b4008-283">By default the Entity Framework implicitly implements transactions.</span></span> <span data-ttu-id="b4008-284">V situacích, kdy provést změny na více řádcích nebo tabulky a poté zavolejte `SaveChanges`, Entity Framework automaticky zajišťuje, že všechny změny úspěšné nebo se všechny nezdaří.</span><span class="sxs-lookup"><span data-stu-id="b4008-284">In scenarios where you make changes to multiple rows or tables and then call `SaveChanges`, the Entity Framework automatically makes sure that either all of your changes succeed or all fail.</span></span> <span data-ttu-id="b4008-285">Pokud některé změny nejprve dokončení a potom se stane chyba, tyto změny se automaticky vrátí zpět.</span><span class="sxs-lookup"><span data-stu-id="b4008-285">If some changes are done first and then an error happens, those changes are automatically rolled back.</span></span> <span data-ttu-id="b4008-286">Pro scénáře, kde je nutné mít lepší kontrolu&mdash;například, pokud chcete zahrnout operací provést v transakci mimo rozhraní Entity Framework&mdash;naleznete v tématu [práce s transakcí](/ef/ef6/saving/transactions).</span><span class="sxs-lookup"><span data-stu-id="b4008-286">For scenarios where you need more control&mdash;for example, if you want to include operations done outside of Entity Framework in a transaction&mdash;see [Working with Transactions](/ef/ef6/saving/transactions).</span></span>

## <a name="summary"></a><span data-ttu-id="b4008-287">Souhrn</span><span class="sxs-lookup"><span data-stu-id="b4008-287">Summary</span></span>

<span data-ttu-id="b4008-288">Teď máte úplnou sadu stránek, které provádějí jednoduché operace CRUD pro `Student` entity.</span><span class="sxs-lookup"><span data-stu-id="b4008-288">You now have a complete set of pages that perform simple CRUD operations for `Student` entities.</span></span> <span data-ttu-id="b4008-289">Pomocné rutiny MVC jste použili k vygenerování prvky uživatelského rozhraní pro datová pole.</span><span class="sxs-lookup"><span data-stu-id="b4008-289">You used MVC helpers to generate UI elements for data fields.</span></span> <span data-ttu-id="b4008-290">Další informace o pomocné rutiny MVC najdete v tématu [vykreslení formuláři pomocí pomocných rutin HTML](/previous-versions/aspnet/dd410596(v=vs.98)) (Tento článek je pro MVC 3, ale je stále relevantní pro MVC 5).</span><span class="sxs-lookup"><span data-stu-id="b4008-290">For more information about MVC helpers, see [Rendering a Form Using HTML Helpers](/previous-versions/aspnet/dd410596(v=vs.98)) (the article is for MVC 3 but is still relevant for MVC 5).</span></span>

<span data-ttu-id="b4008-291">V dalším kurzu rozšiřujete funkce indexovou stránku tak, že přidáte řazení a stránkování.</span><span class="sxs-lookup"><span data-stu-id="b4008-291">In the next tutorial, you'll expand the functionality of the Index page by adding sorting and paging.</span></span>

<span data-ttu-id="b4008-292">Jak vám v tomto kurzu líbilo a co můžeme zlepšit nám prosím zpětnou vazbu.</span><span class="sxs-lookup"><span data-stu-id="b4008-292">Please leave feedback on how you liked this tutorial and what we could improve.</span></span>

<span data-ttu-id="b4008-293">Odkazy na další zdroje Entity Framework lze nalézt v [přístup k datům ASP.NET – doporučené zdroje informací](../../../../whitepapers/aspnet-data-access-content-map.md).</span><span class="sxs-lookup"><span data-stu-id="b4008-293">Links to other Entity Framework resources can be found in [ASP.NET Data Access - Recommended Resources](../../../../whitepapers/aspnet-data-access-content-map.md).</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="b4008-294">[Předchozí](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md)
> [další](sorting-filtering-and-paging-with-the-entity-framework-in-an-asp-net-mvc-application.md)</span><span class="sxs-lookup"><span data-stu-id="b4008-294">[Previous](creating-an-entity-framework-data-model-for-an-asp-net-mvc-application.md)
[Next](sorting-filtering-and-paging-with-the-entity-framework-in-an-asp-net-mvc-application.md)</span></span>
