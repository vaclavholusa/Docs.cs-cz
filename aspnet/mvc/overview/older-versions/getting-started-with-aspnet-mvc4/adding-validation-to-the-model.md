---
uid: mvc/overview/older-versions/getting-started-with-aspnet-mvc4/adding-validation-to-the-model
title: Přidání ověření do modelu | Dokumentace Microsoftu
author: Rick-Anderson
description: 'Poznámka: Aktualizovanou verzi tohoto kurzu je k dispozici tady, která používá ASP.NET MVC 5 a Visual Studio 2013. Je bezpečnější, sledovat a ukázka mnohem jednodušší...'
ms.author: aspnetcontent
ms.date: 08/28/2012
ms.assetid: 5d9a2999-fcc4-4c45-a018-271fddf74a3b
msc.legacyurl: /mvc/overview/older-versions/getting-started-with-aspnet-mvc4/adding-validation-to-the-model
msc.type: authoredcontent
ms.openlocfilehash: 50911920ed80f09033cfc53a137a6bbba3fc62f3
ms.sourcegitcommit: b28cd0313af316c051c2ff8549865bff67f2fbb4
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/05/2018
ms.locfileid: "37834488"
---
<a name="adding-validation-to-the-model"></a><span data-ttu-id="f3ef4-104">Přidání ověření do modelu</span><span class="sxs-lookup"><span data-stu-id="f3ef4-104">Adding Validation to the Model</span></span>
====================
<span data-ttu-id="f3ef4-105">Podle [Rick Anderson](https://github.com/Rick-Anderson)</span><span class="sxs-lookup"><span data-stu-id="f3ef4-105">by [Rick Anderson](https://github.com/Rick-Anderson)</span></span>

> > [!NOTE]
> > <span data-ttu-id="f3ef4-106">Je k dispozici aktualizovaná verze tohoto kurzu [tady](../../getting-started/introduction/getting-started.md) , která používá ASP.NET MVC 5 a Visual Studio 2013.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-106">An updated version of this tutorial is available [here](../../getting-started/introduction/getting-started.md) that uses ASP.NET MVC 5 and Visual Studio 2013.</span></span> <span data-ttu-id="f3ef4-107">Je bezpečnější, postupujte podle mnohem jednodušší a ukazuje další funkce.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-107">It's more secure, much simpler to follow and demonstrates more features.</span></span>


<span data-ttu-id="f3ef4-108">V této části přidáte logiku ověřování k `Movie` model a zajistíte, že ověřovacích pravidel se vynucují kdykoli se uživatel pokusí o vytvoření nebo úprava videa pomocí aplikace.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-108">In this section you'll add validation logic to the `Movie` model, and you'll ensure that the validation rules are enforced any time a user attempts to create or edit a movie using the application.</span></span>

## <a name="keeping-things-dry"></a><span data-ttu-id="f3ef4-109">Udržování suchého věcí</span><span class="sxs-lookup"><span data-stu-id="f3ef4-109">Keeping Things DRY</span></span>

<span data-ttu-id="f3ef4-110">Jedním ze základních principů návrhu rozhraní ASP.NET MVC je zkušební (&quot;není opakujte sami&quot;).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-110">One of the core design tenets of ASP.NET MVC is DRY (&quot;Don't Repeat Yourself&quot;).</span></span> <span data-ttu-id="f3ef4-111">ASP.NET MVC umožňuje zadat funkci nebo chování pouze jednou a potom jste ho všude, kde se projeví v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-111">ASP.NET MVC encourages you to specify functionality or behavior only once, and then have it be reflected everywhere in an application.</span></span> <span data-ttu-id="f3ef4-112">To snižuje množství kódu, které potřebujete k zápisu a provede kód, který napíšete náchylné k chybám a snadněji udržovat méně chyb.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-112">This reduces the amount of code you need to write and makes the code you do write less error prone and easier to maintain.</span></span>

<span data-ttu-id="f3ef4-113">Podpora ověřování poskytované rozhraní ASP.NET MVC a Entity Framework Code First je skvělé příkladem zásada SUCHÝCH v akci.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-113">The validation support provided by ASP.NET MVC and Entity Framework Code First is a great example of the DRY principle in action.</span></span> <span data-ttu-id="f3ef4-114">Ověřovací pravidla můžete zadat pomocí deklarace na jednom místě (ve třídě modelu) a pravidel se vynucují kdekoli v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-114">You can declaratively specify validation rules in one place (in the model class) and the rules are enforced everywhere in the application.</span></span>

<span data-ttu-id="f3ef4-115">Podívejme se na tom, jak můžete využít této podpory ověřování v aplikace movie.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-115">Let's look at how you can take advantage of this validation support in the movie application.</span></span>

## <a name="adding-validation-rules-to-the-movie-model"></a><span data-ttu-id="f3ef4-116">Přidání pravidel ověřování do modelu Movie</span><span class="sxs-lookup"><span data-stu-id="f3ef4-116">Adding Validation Rules to the Movie Model</span></span>

<span data-ttu-id="f3ef4-117">Zobrazí za přibližně tak, že přidáte některé logiku ověřování k `Movie` třídy.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-117">You'll begin by adding some validation logic to the `Movie` class.</span></span>

<span data-ttu-id="f3ef4-118">Otevřít *Movie.cs* souboru.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-118">Open the *Movie.cs* file.</span></span> <span data-ttu-id="f3ef4-119">Přidat `using` příkazu v horní části souboru, který odkazuje [ `System.ComponentModel.DataAnnotations` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx) obor názvů:</span><span class="sxs-lookup"><span data-stu-id="f3ef4-119">Add a `using` statement at the top of the file that references the [`System.ComponentModel.DataAnnotations`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx) namespace:</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample1.cs)]

<span data-ttu-id="f3ef4-120">Všimněte si, že obor názvů neobsahuje `System.Web`.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-120">Notice the namespace does not contain `System.Web`.</span></span> <span data-ttu-id="f3ef4-121">DataAnnotations poskytuje integrovanou sadu atributů ověření, které můžete provést pomocí deklarace tříd nebo vlastností.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-121">DataAnnotations provides a built-in set of validation attributes that you can apply declaratively to any class or property.</span></span>

<span data-ttu-id="f3ef4-122">Teď aktualizovat `Movie` třídy, které chcete využít výhod integrovaného [ `Required` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx), [ `StringLength` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx), a [ `Range` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.rangeattribute.aspx) atributů ověření .</span><span class="sxs-lookup"><span data-stu-id="f3ef4-122">Now update the `Movie` class to take advantage of the built-in [`Required`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.requiredattribute.aspx), [`StringLength`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.stringlengthattribute.aspx), and [`Range`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.rangeattribute.aspx) validation attributes.</span></span> <span data-ttu-id="f3ef4-123">Použijte následující kód s ukázkovým kde použít atributy.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-123">Use the following code as an example of where to apply the attributes.</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample2.cs?highlight=4,10,13,17)]

<span data-ttu-id="f3ef4-124">Spusťte aplikaci a znovu se zobrazí následující chyba běhu:</span><span class="sxs-lookup"><span data-stu-id="f3ef4-124">Run the application and you will again get the following run time error:</span></span>

<span data-ttu-id="f3ef4-125">***Model zálohování kontextu 'MovieDBContext' byl změněn, protože byla vytvořena databáze. Zvažte použití migrace Code First k aktualizaci databáze ([https://go.microsoft.com/fwlink/?LinkId=238269](https://go.microsoft.com/fwlink/?LinkId=238269)).***</span><span class="sxs-lookup"><span data-stu-id="f3ef4-125">***The model backing the 'MovieDBContext' context has changed since the database was created. Consider using Code First Migrations to update the database ([https://go.microsoft.com/fwlink/?LinkId=238269](https://go.microsoft.com/fwlink/?LinkId=238269)).***</span></span>

<span data-ttu-id="f3ef4-126">Migraci budeme používat k aktualizaci schématu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-126">We will use migrations to update the schema.</span></span> <span data-ttu-id="f3ef4-127">Sestavte řešení a pak otevřete **Konzola správce balíčků** okna a zadejte následující příkazy:</span><span class="sxs-lookup"><span data-stu-id="f3ef4-127">Build the solution, and then open the **Package Manager Console** window and enter the following commands:</span></span>

[!code-console[Main](adding-validation-to-the-model/samples/sample3.cmd)]

<span data-ttu-id="f3ef4-128">Po dokončení tohoto příkazu, Visual Studio otevře soubor třídy, která definuje nový `DbMIgration` odvozené třídy se zadaným názvem (*AddDataAnnotationsMig*) a `Up` metoda se zobrazí kód, který aktualizuje omezení schématu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-128">When this command finishes, Visual Studio opens the class file that defines the new `DbMIgration` derived class with the name specified (*AddDataAnnotationsMig*), and in the `Up` method you can see the code that updates the schema constraints.</span></span> <span data-ttu-id="f3ef4-129">`Title` a `Genre` polí už nejsou s možnou hodnotou Null (to znamená, je nutné zadat hodnotu) a `Rating` pole má maximální délku 5.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-129">The `Title` and `Genre` fields are no longer nullable (that is, you must enter a value) and the `Rating` field has a maximum length of 5.</span></span>

<span data-ttu-id="f3ef4-130">Ověření atributy určují chování, které chcete vynutit na, které se použijí pro vlastnosti modelu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-130">The validation attributes specify behavior that you want to enforce on the model properties they are applied to.</span></span> <span data-ttu-id="f3ef4-131">`Required` Atribut označuje, že vlastnost musí mít hodnotu; v této ukázce, musí mít hodnoty pro video `Title`, `ReleaseDate`, `Genre`, a `Price` vlastnosti, aby byla platná.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-131">The `Required` attribute indicates that a property must have a value; in this sample, a movie has to have values for the `Title`, `ReleaseDate`, `Genre`, and `Price` properties in order to be valid.</span></span> <span data-ttu-id="f3ef4-132">`Range` Atribut omezuje hodnotu do zadaného rozsahu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-132">The `Range` attribute constrains a value to within a specified range.</span></span> <span data-ttu-id="f3ef4-133">`StringLength` Atribut umožňuje nastavit maximální délka vlastnosti typu string a volitelně jeho minimální délka.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-133">The `StringLength` attribute lets you set the maximum length of a string property, and optionally its minimum length.</span></span> <span data-ttu-id="f3ef4-134">Vnitřní typy (například `decimal, int, float, DateTime`) se vyžadují ve výchozím nastavení a nemusí `Required` atribut.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-134">Intrinsic types (such as `decimal, int, float, DateTime`) are required by default and don't need the `Required` attribute.</span></span>

<span data-ttu-id="f3ef4-135">Kód nejprve zajistí, že se vynucují ověřovacích pravidel, které jste zadali na třídu modelu, předtím, než se aplikace uloží změny do databáze.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-135">Code First ensures that the validation rules you specify on a model class are enforced before the application saves changes in the database.</span></span> <span data-ttu-id="f3ef4-136">Následující kód například vyvolá výjimku při `SaveChanges` metoda je volána, protože některé požadované `Movie` nebyly nalezeny hodnoty vlastností a cena je nula (což je mimo platný rozsah).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-136">For example, the code below will throw an exception when the `SaveChanges` method is called, because several required `Movie` property values are missing and the price is zero (which is out of the valid range).</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample4.cs?highlight=7-8)]

<span data-ttu-id="f3ef4-137">S ověřovací pravidla automaticky vynucuje sada .NET Framework pomáhá vytvářet vaše aplikace odolnější.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-137">Having validation rules automatically enforced by the .NET Framework helps make your application more robust.</span></span> <span data-ttu-id="f3ef4-138">Také zajistí, že nelze zapomenete něco ověření a neúmyslně nechat chybná data do databáze.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-138">It also ensures that you can't forget to validate something and inadvertently let bad data into the database.</span></span>

<span data-ttu-id="f3ef4-139">Tady je úplný výpis pro aktualizaci kódu *Movie.cs* souboru:</span><span class="sxs-lookup"><span data-stu-id="f3ef4-139">Here's a complete code listing for the updated *Movie.cs* file:</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample5.cs)]

## <a name="validation-error-ui-in-aspnet-mvc"></a><span data-ttu-id="f3ef4-140">Chyba ověření uživatelského rozhraní v architektuře ASP.NET MVC</span><span class="sxs-lookup"><span data-stu-id="f3ef4-140">Validation Error UI in ASP.NET MVC</span></span>

<span data-ttu-id="f3ef4-141">Znovu spusťte aplikaci a přejděte */Movies* adresy URL.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-141">Re-run the application and navigate to the */Movies* URL.</span></span>

<span data-ttu-id="f3ef4-142">Klikněte na tlačítko **vytvořit nový** odkaz na přidání nového videa.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-142">Click the **Create New** link to add a new movie.</span></span> <span data-ttu-id="f3ef4-143">Vyplňte formulář pomocí některé neplatné hodnoty a pak klikněte na tlačítko **vytvořit** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-143">Fill out the form with some invalid values and then click the **Create** button.</span></span>

![8_validationErrors](adding-validation-to-the-model/_static/image1.png)

> [!NOTE]
> <span data-ttu-id="f3ef4-145">pro podporu ověřování jQuery pro neanglická národní prostředí, které používají čárku (&quot;,&quot;) desetinné čárky, je třeba zahrnout *globalize.js* a konkrétní *cultures/globalize.cultures.js* souboru (z [ https://github.com/jquery/globalize ](https://github.com/jquery/globalize) ) a JavaScript, který chcete použít `Globalize.parseFloat`.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-145">to support jQuery validation for non-English locales that use a comma (&quot;,&quot;) for a decimal point, you must include *globalize.js* and your specific *cultures/globalize.cultures.js* file(from [https://github.com/jquery/globalize](https://github.com/jquery/globalize) ) and JavaScript to use `Globalize.parseFloat`.</span></span> <span data-ttu-id="f3ef4-146">Následující kód ukazuje změny souboru Views\Movies\Edit.cshtml pracovat &quot;fr-FR&quot; jazykové verze:</span><span class="sxs-lookup"><span data-stu-id="f3ef4-146">The following code shows the modifications to the Views\Movies\Edit.cshtml file to work with the &quot;fr-FR&quot; culture:</span></span>


[!code-cshtml[Main](adding-validation-to-the-model/samples/sample6.cshtml)]

<span data-ttu-id="f3ef4-147">Všimněte si, jak formulář automaticky použila červené ohraničení zvýrazněte textová pole, které obsahují neplatná data a je vyzářeno chybovou zprávu ověření odpovídající vedle každé z nich.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-147">Notice how the form has automatically used a red border color to highlight the text boxes that contain invalid data and has emitted an appropriate validation error message next to each one.</span></span> <span data-ttu-id="f3ef4-148">Chyby se vynucují straně klienta (pomocí jazyků JavaScript a jQuery) a na straně serveru (v případě má zakázaný JavaScript).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-148">The errors are enforced both client-side (using JavaScript and jQuery) and server-side (in case a user has JavaScript disabled).</span></span>

<span data-ttu-id="f3ef4-149">Skutečné výhodou je, že nemusíte změnit jediný řádek kódu v `MoviesController` třídy nebo *Create.cshtml* zobrazení, chcete-li povolit toto ověření uživatelského rozhraní.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-149">A real benefit is that you didn't need to change a single line of code in the `MoviesController` class or in the *Create.cshtml* view in order to enable this validation UI.</span></span> <span data-ttu-id="f3ef4-150">Kontroler a zobrazení, které jste vytvořili dříve v tomto kurzu automaticky vybere nahoru ověřovací pravidla, které jste zadali pomocí atributů ověření na vlastnosti `Movie` třída modelu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-150">The controller and views you created earlier in this tutorial automatically picked up the validation rules that you specified by using validation attributes on the properties of the `Movie` model class.</span></span>

<span data-ttu-id="f3ef4-151">Možná jste si všimli vlastností `Title` a `Genre`, povinný atribut není vynucena, dokud se odesláním formuláře (přístupů **vytvořit** tlačítko), nebo zadejte text do vstupního pole a odebrat ho.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-151">You might have noticed for the properties `Title` and `Genre`, the required attribute is not enforced until you submit the form (hit the **Create** button), or enter text into the input field and removed it.</span></span> <span data-ttu-id="f3ef4-152">Pro pole který je původně prázdné (například pole v zobrazení pro vytváření) a který má pouze povinný atribut a žádné jiné atributy ověření můžete provést následující aktivovat ověřování:</span><span class="sxs-lookup"><span data-stu-id="f3ef4-152">For a field which is initially empty (such as the fields on the Create view) and which has only the required attribute and no other validation attributes, you can do the following to trigger validation:</span></span>

1. <span data-ttu-id="f3ef4-153">Kartu do pole.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-153">Tab into the field.</span></span>
2. <span data-ttu-id="f3ef4-154">Zadejte nějaký text.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-154">Enter some text.</span></span>
3. <span data-ttu-id="f3ef4-155">Karta navýšení kapacity.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-155">Tab out.</span></span>
4. <span data-ttu-id="f3ef4-156">Karta zpět do pole.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-156">Tab back into the field.</span></span>
5. <span data-ttu-id="f3ef4-157">Odeberte text.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-157">Remove the text.</span></span>
6. <span data-ttu-id="f3ef4-158">Karta navýšení kapacity.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-158">Tab out.</span></span>

<span data-ttu-id="f3ef4-159">Výše uvedené pořadí aktivují požadované ověřovací bez klepnutím na tlačítko Odeslat.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-159">The above sequence will trigger the required validation without hitting the submit button.</span></span> <span data-ttu-id="f3ef4-160">Jednoduše klepnutím na tlačítko Odeslat bez nutnosti zadávat žádné z polí se aktivuje ověřování na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-160">Simply hitting the submit button without entering any of the fields will trigger client side validation.</span></span> <span data-ttu-id="f3ef4-161">Data formuláře neodešle na server, dokud nedojde k žádným chybám ověření na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-161">The form data is not sent to the server until there are no client side validation errors.</span></span> <span data-ttu-id="f3ef4-162">Můžete ho otestovat uvedením přerušení metody Post protokolu HTTP nebo pomocí [nástroj fiddler](http://fiddler2.com/fiddler2/) nebo aplikace Internet Explorer 9 [vývojářské nástroje F12 pomáhají](https://msdn.microsoft.com/ie/aa740478).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-162">You can test this by putting a break point in the HTTP Post method or using the [fiddler tool](http://fiddler2.com/fiddler2/) or the IE 9 [F12 developer tools](https://msdn.microsoft.com/ie/aa740478).</span></span>

![](adding-validation-to-the-model/_static/image2.png)

## <a name="how-validation-occurs-in-the-create-view-and-create-action-method"></a><span data-ttu-id="f3ef4-163">Jak probíhá ověřování vytvořením zobrazit a vytvořit metody akce</span><span class="sxs-lookup"><span data-stu-id="f3ef4-163">How Validation Occurs in the Create View and Create Action Method</span></span>

<span data-ttu-id="f3ef4-164">Může vás zajímat, jak byl vygenerován ověření uživatelského rozhraní bez jakýchkoli aktualizací kódu v kontroleru nebo zobrazení.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-164">You might wonder how the validation UI was generated without any updates to the code in the controller or views.</span></span> <span data-ttu-id="f3ef4-165">Následující výpis ukazuje, co `Create` metody v `MovieController` vypadají třídy.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-165">The next listing shows what the `Create` methods in the `MovieController` class look like.</span></span> <span data-ttu-id="f3ef4-166">Jsou to neliší od jak jste je vytvořili dříve v tomto kurzu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-166">They're unchanged from how you created them earlier in this tutorial.</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample7.cs?highlight=12,15)]

<span data-ttu-id="f3ef4-167">První (HTTP GET) `Create` zobrazí úvodní formulář vytvořit metody akce.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-167">The first (HTTP GET) `Create` action method displays the initial Create form.</span></span> <span data-ttu-id="f3ef4-168">Druhý (`[HttpPost]`) verze zpracovává odeslaného formuláře.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-168">The second (`[HttpPost]`) version handles the form post.</span></span> <span data-ttu-id="f3ef4-169">Druhá `Create` – metoda ( `HttpPost` verze) volání `ModelState.IsValid` ke kontrole, jestli má tento film všechny chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-169">The second `Create` method (The `HttpPost` version) calls `ModelState.IsValid` to check whether the movie has any validation errors.</span></span> <span data-ttu-id="f3ef4-170">Voláním této metody vyhodnotí všechny atributy ověření, které se použily k objektu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-170">Calling this method evaluates any validation attributes that have been applied to the object.</span></span> <span data-ttu-id="f3ef4-171">Pokud objekt má chyby ověření, `Create` metoda znovu zobrazí formulář.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-171">If the object has validation errors, the `Create` method re-displays the form.</span></span> <span data-ttu-id="f3ef4-172">Pokud nejsou žádné chyby, metoda tento nový film uloží v databázi.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-172">If there are no errors, the method saves the new movie in the database.</span></span> <span data-ttu-id="f3ef4-173">V našem příkladu film používáme **formuláři není odeslána na server, pokud nejsou zjištěny na straně klienta; chyby ověřování druhého** `Create` **nikdy volána metoda**.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-173">In our movie example we are using, **the form is not posted to the server when their are validation errors detected on the client side; the second** `Create`**method is never called**.</span></span> <span data-ttu-id="f3ef4-174">Pokud zakážete jazyka JavaScript v prohlížeči, je zakázáno ověřování na straně klienta a HTTP POST `Create` volání metody `ModelState.IsValid` ke kontrole, jestli má tento film všechny chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-174">If you disable JavaScript in your browser, client validation is disabled and the HTTP POST `Create` method calls `ModelState.IsValid` to check whether the movie has any validation errors.</span></span>

<span data-ttu-id="f3ef4-175">Lze nastavit bod přerušení v `HttpPost Create` metoda a ověřit se nikdy nevolá metodu, ověřování na straně klienta nebude odeslat data formuláře, když se zjistí chyby ověření.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-175">You can set a break point in the `HttpPost Create` method and verify the method is never called, client side validation will not submit the form data when validation errors are detected.</span></span> <span data-ttu-id="f3ef4-176">Pokud zakážete jazyka JavaScript v prohlížeči a pak odesláním formuláře s chybami, se setkají bodu přerušení.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-176">If you disable JavaScript in your browser, then submit the form with errors, the break point will be hit.</span></span> <span data-ttu-id="f3ef4-177">Vy i přesto úplného ověření bez jazyka JavaScript.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-177">You still get full validation without JavaScript.</span></span> <span data-ttu-id="f3ef4-178">Následující obrázek ukazuje, jak zakázat jazyka JavaScript v aplikaci Internet Explorer.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-178">The following image shows how to disable JavaScript in Internet Explorer.</span></span>

![](adding-validation-to-the-model/_static/image3.png)

![](adding-validation-to-the-model/_static/image4.png)

<span data-ttu-id="f3ef4-179">Následující obrázek ukazuje, jak zakázat jazyka JavaScript v prohlížeči FireFox.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-179">The following image shows how to disable JavaScript in the FireFox browser.</span></span>

![](adding-validation-to-the-model/_static/image5.png)

<span data-ttu-id="f3ef4-180">Následující obrázek ukazuje, jak zakázat JavaScript s prohlížečem Chrome.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-180">The following image shows how to disable JavaScript with the Chrome browser.</span></span>

![](adding-validation-to-the-model/_static/image6.png)

<span data-ttu-id="f3ef4-181">Níže je uvedená *Create.cshtml* zobrazit šablonu, která automaticky dříve v tomto kurzu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-181">Below is the *Create.cshtml* view template that you scaffolded earlier in the tutorial.</span></span> <span data-ttu-id="f3ef4-182">Používá se pomocí metody akce, které jsou uvedené výše obě počáteční formulář pro zobrazení a znovu ho zobrazit v případě chyby.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-182">It's used by the action methods shown above both to display the initial form and to redisplay it in the event of an error.</span></span>

[!code-cshtml[Main](adding-validation-to-the-model/samples/sample8.cshtml?highlight=22-23,30-31,38-39,46-47)]

<span data-ttu-id="f3ef4-183">Všimněte si, jak tento kód použije `Html.EditorFor` pomocné rutiny pro výstup `<input>` – element pro každé `Movie` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-183">Notice how the code uses an `Html.EditorFor` helper to output the `<input>` element for each `Movie` property.</span></span> <span data-ttu-id="f3ef4-184">Vedle tohoto pomocníka je volání `Html.ValidationMessageFor` Pomocná metoda.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-184">Next to this helper is a call to the `Html.ValidationMessageFor` helper method.</span></span> <span data-ttu-id="f3ef4-185">Tyto dvě metody helper pracovat s modelu objektu, který je předán adaptérem k zobrazení (v tomto případě `Movie` objekt).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-185">These two helper methods work with the model object that's passed by the controller to the view (in this case, a `Movie` object).</span></span> <span data-ttu-id="f3ef4-186">Zobrazují se automaticky pro ověření atributy určené v modelu a zobrazí chybové zprávy podle potřeby.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-186">They automatically look for validation attributes specified on the model and display error messages as appropriate.</span></span>

<span data-ttu-id="f3ef4-187">Co je hodně příjemné o tento přístup je, že kontroler ani zobrazit šablonu vytvořit cokoli o skutečné ověřovacích pravidel se vynucují nebo zná o určité chybové zprávy zobrazeny.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-187">What's really nice about this approach is that neither the controller nor the Create view template knows anything about the actual validation rules being enforced or about the specific error messages displayed.</span></span> <span data-ttu-id="f3ef4-188">Ověřovací pravidla a chybové řetězce se zadávají pouze v `Movie` třídy.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-188">The validation rules and the error strings are specified only in the `Movie` class.</span></span> <span data-ttu-id="f3ef4-189">Tyto stejné ověřovací pravidla se automaticky použijí k zobrazení pro úpravy a žádné jiné zobrazení šablony, které vytvoříte, které upravit model.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-189">These same validation rules are automatically applied to the Edit view and any other views templates you might create that edit your model.</span></span>

<span data-ttu-id="f3ef4-190">Pokud chcete později změnit logiku ověřování, můžete k tomu v přesně jedno místo přidávání atributů ověření do modelu (v tomto příkladu `movie` třídy).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-190">If you want to change the validation logic later, you can do so in exactly one place by adding validation attributes to the model (in this example, the `movie` class).</span></span> <span data-ttu-id="f3ef4-191">Není nutné se starat o různé části aplikace, s jakým způsobem se vynucují pravidla nekonzistentní – veškerou logiku ověřování definované na jednom místě, který se použít kdekoli.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-191">You won't have to worry about different parts of the application being inconsistent with how the rules are enforced — all validation logic will be defined in one place and used everywhere.</span></span> <span data-ttu-id="f3ef4-192">To zajišťuje velmi čistým kódem a umožňuje snadno vyvíjet a udržovat.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-192">This keeps the code very clean, and makes it easy to maintain and evolve.</span></span> <span data-ttu-id="f3ef4-193">A to znamená, že je budete mít plně dodržením zásada SUCHÝCH.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-193">And it means that you'll be fully honoring the DRY principle.</span></span>

## <a name="adding-formatting-to-the-movie-model"></a><span data-ttu-id="f3ef4-194">Přidání formátování ke modelu Movie</span><span class="sxs-lookup"><span data-stu-id="f3ef4-194">Adding Formatting to the Movie Model</span></span>

<span data-ttu-id="f3ef4-195">Otevřít *Movie.cs* souboru a zkoumat `Movie` třídy.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-195">Open the *Movie.cs* file and examine the `Movie` class.</span></span> <span data-ttu-id="f3ef4-196">[ `System.ComponentModel.DataAnnotations` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx) Obor názvů obsahuje atributy formátování kromě integrovaná sada atributů ověření.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-196">The [`System.ComponentModel.DataAnnotations`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.aspx) namespace provides formatting attributes in addition to the built-in set of validation attributes.</span></span> <span data-ttu-id="f3ef4-197">Už jsme použili [ `DataType` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) hodnota výčtu datum vydání a na pole price.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-197">We've already applied a [`DataType`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) enumeration value to the release date and to the price fields.</span></span> <span data-ttu-id="f3ef4-198">Následující kód ukazuje `ReleaseDate` a `Price` vlastnosti příslušnou [ `DisplayFormat` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) atribut.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-198">The following code shows the `ReleaseDate` and `Price` properties with the appropriate [`DisplayFormat`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.displayformatattribute.aspx) attribute.</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample9.cs)]

<span data-ttu-id="f3ef4-199">[ `DataType` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) Atributy nejsou atributů ověření, používají se k informování zobrazovací modul, jak vykreslení kódu HTML.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-199">The [`DataType`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) attributes are not validation attributes, they are used to tell the view engine how to render the HTML.</span></span> <span data-ttu-id="f3ef4-200">V příkladu výše `DataType.Date` atribut zobrazuje data film jako pouze data bez času.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-200">In the example above, the `DataType.Date` attribute displays the movie dates as dates only, without time.</span></span> <span data-ttu-id="f3ef4-201">Například následující [ `DataType` ](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) atributy není ověření formátu dat:</span><span class="sxs-lookup"><span data-stu-id="f3ef4-201">For example, the following [`DataType`](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.datatype.aspx) attributes don't validate the format of the data:</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample10.cs)]

<span data-ttu-id="f3ef4-202">Atributy uvedené výše pouze poskytnout nápovědu pro modul zobrazení pro zobrazení dat (a zadat atributy například &lt;&gt; pro adresy URL a &lt;href =&quot;mailto:EmailAddress.com&quot; &gt; e-mailu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-202">The attributes listed above only provide hints for the view engine to format the data (and supply attributes such as &lt;a&gt; for URL's and &lt;a href=&quot;mailto:EmailAddress.com&quot;&gt; for email.</span></span> <span data-ttu-id="f3ef4-203">Můžete použít [regulární výraz](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.regularexpressionattribute.aspx) atribut pro ověření formátu data.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-203">You can use the [RegularExpression](https://msdn.microsoft.com/library/system.componentmodel.dataannotations.regularexpressionattribute.aspx) attribute to validate the format of the data.</span></span>

<span data-ttu-id="f3ef4-204">Alternativním přístupem k použití `DataType` atributy, můžete explicitně nastavit [ `DataFormatString` ](https://msdn.microsoft.com/library/system.string.format.aspx) hodnotu.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-204">An alternative approach to using the `DataType` attributes, you could explicitly set a [`DataFormatString`](https://msdn.microsoft.com/library/system.string.format.aspx) value.</span></span> <span data-ttu-id="f3ef4-205">Následující kód ukazuje vlastnost datum vydání verze s řetězec formátu data (konkrétně &quot;d&quot;).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-205">The following code shows the release date property with a date format string (namely, &quot;d&quot;).</span></span> <span data-ttu-id="f3ef4-206">Můžete využít k určení, že nechcete čas jako součást datum vydání.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-206">You'd use this to specify that you don't want to time as part of the release date.</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample11.cs)]

<span data-ttu-id="f3ef4-207">Kompletní `Movie` třídy je uveden níže.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-207">The complete `Movie` class is shown below.</span></span>

[!code-csharp[Main](adding-validation-to-the-model/samples/sample12.cs)]

<span data-ttu-id="f3ef4-208">Spusťte aplikaci a přejděte `Movies` kontroleru.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-208">Run the application and browse to the `Movies` controller.</span></span> <span data-ttu-id="f3ef4-209">Datum vydání a cena přehledně naformátovaná.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-209">The release date and price are nicely formatted.</span></span> <span data-ttu-id="f3ef4-210">Následující obrázek ukazuje datum vydání verze a pomocí cena &quot;fr-FR&quot; jako jazykovou verzi.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-210">The image below shows the release date and price using &quot;fr-FR&quot; as the culture.</span></span>

![8_format_SM](adding-validation-to-the-model/_static/image7.png)

<span data-ttu-id="f3ef4-212">Následující obrázek ukazuje stejná data, zobrazí se výchozí jazykovou verzi (English US).</span><span class="sxs-lookup"><span data-stu-id="f3ef4-212">The image below shows the same data displayed with the default culture (English US).</span></span>

![](adding-validation-to-the-model/_static/image8.png)

<span data-ttu-id="f3ef4-213">V další části série, přidáme zkontrolujte, zda aplikace a vylepšování některé automaticky generované `Details` a `Delete` metody.</span><span class="sxs-lookup"><span data-stu-id="f3ef4-213">In the next part of the series, we'll review the application and make some improvements to the automatically generated `Details` and `Delete` methods.</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="f3ef4-214">[Předchozí](adding-a-new-field-to-the-movie-model-and-table.md)
> [další](examining-the-details-and-delete-methods.md)</span><span class="sxs-lookup"><span data-stu-id="f3ef4-214">[Previous](adding-a-new-field-to-the-movie-model-and-table.md)
[Next](examining-the-details-and-delete-methods.md)</span></span>
