---
uid: mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
title: Použití asynchronních metod v architektuře ASP.NET MVC 4 | Microsoft Docs
author: Rick-Anderson
description: V tomto kurzu naučit se základy vytváření asynchronní ASP.NET MVC webovou aplikaci pomocí Visual Studio Express 2012 pro Web, který je bezplatná sunout...
ms.author: aspnetcontent
manager: wpickett
ms.date: 06/06/2012
ms.topic: article
ms.assetid: a56572ba-81c3-47af-826d-941e9c4775ec
ms.technology: dotnet-mvc
ms.prod: .net-framework
msc.legacyurl: /mvc/overview/performance/using-asynchronous-methods-in-aspnet-mvc-4
msc.type: authoredcontent
ms.openlocfilehash: 65ba01dd1b0ed5a43ca5c868608f2858f86b4b59
ms.sourcegitcommit: 726ffab258070b4fe6cf950bf030ce10c0c07bb4
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/04/2018
ms.locfileid: "34734533"
---
<a name="using-asynchronous-methods-in-aspnet-mvc-4"></a><span data-ttu-id="c4ea2-103">Použití asynchronních metod v architektuře ASP.NET MVC 4</span><span class="sxs-lookup"><span data-stu-id="c4ea2-103">Using Asynchronous Methods in ASP.NET MVC 4</span></span>
====================
<span data-ttu-id="c4ea2-104">podle [Rick Anderson](https://github.com/Rick-Anderson)</span><span class="sxs-lookup"><span data-stu-id="c4ea2-104">by [Rick Anderson](https://github.com/Rick-Anderson)</span></span>

> <span data-ttu-id="c4ea2-105">V tomto kurzu naučit, základní informace o vytváření, asynchronní aplikace technologie ASP.NET MVC pomocí [Visual Studio Express 2012 pro Web](https://www.microsoft.com/visualstudio/11), což je bezplatnou verzi sady Microsoft Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-105">This tutorial will teach you the basics of building an asynchronous ASP.NET MVC Web application using [Visual Studio Express 2012 for Web](https://www.microsoft.com/visualstudio/11), which is a free version of Microsoft Visual Studio.</span></span> <span data-ttu-id="c4ea2-106">Můžete také použít [Visual Studio 2012](https://www.microsoft.com/visualstudio/11).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-106">You can also use [Visual Studio 2012](https://www.microsoft.com/visualstudio/11).</span></span>
> 
> <span data-ttu-id="c4ea2-107">Poskytuje kompletní příklad v tomto kurzu na githubu  [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span><span class="sxs-lookup"><span data-stu-id="c4ea2-107">A complete sample is provided for this tutorial on github  [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET/)</span></span>


<span data-ttu-id="c4ea2-108">ASP.NET MVC 4 [řadič](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx) – třída v kombinaci [.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx) umožňuje psát metody asynchronní akce, které vracejí objekt typu [úloh&lt;ActionResult&gt; ](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-108">The ASP.NET MVC 4 [Controller](https://msdn.microsoft.com/library/system.web.mvc.controller(VS.108).aspx) class in combination [.NET 4.5](https://msdn.microsoft.com/library/w0x726c2(VS.110).aspx) enables you to write asynchronous action methods that return an object of type [Task&lt;ActionResult&gt;](https://msdn.microsoft.com/library/dd321424(VS.110).aspx).</span></span> <span data-ttu-id="c4ea2-109">Asynchronní programování koncept, označuje jako zavedená rozhraní .NET Framework 4 [úloh](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) a podporuje rozhraní ASP.NET MVC 4 [úloh](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-109">The .NET Framework 4 introduced an asynchronous programming concept referred to as a [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) and ASP.NET MVC 4 supports [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx).</span></span> <span data-ttu-id="c4ea2-110">Úlohy jsou reprezentované pomocí **úloh** typu a souvisejících typů v [System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx) oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-110">Tasks are represented by the **Task** type and related types in the [System.Threading.Tasks](https://msdn.microsoft.com/library/system.threading.tasks.aspx) namespace.</span></span> <span data-ttu-id="c4ea2-111">Rozhraní .NET Framework 4.5 je založený na tato asynchronní podpora s [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) a [asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) klíčová slova, která usnadnění práce s [úloh](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) objekty mnohem méně složitější než předchozí asynchronní přístupy.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-111">The .NET Framework 4.5 builds on this asynchronous support with the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords that make working with [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) objects much less complex than previous asynchronous approaches.</span></span> <span data-ttu-id="c4ea2-112">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) – klíčové slovo je syntaktické sdružená označující, kterou část kódu by měla asynchronně čekat na další část kódu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-112">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword is syntactical shorthand for indicating that a piece of code should asynchronously wait on some other piece of code.</span></span> <span data-ttu-id="c4ea2-113">[Asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) – klíčové slovo představuje pomocný parametr, který slouží k označení metody jako asynchronní metody založené na úlohách.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-113">The [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword represents a hint that you can use to mark methods as task-based asynchronous methods.</span></span> <span data-ttu-id="c4ea2-114">Kombinace **await**, **asynchronní**a **úloh** objektu je mnohem jednodušší pro psaní asynchronní kódu v rozhraní .NET 4.5.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-114">The combination of **await**, **async**, and the **Task** object makes it much easier for you to write asynchronous code in .NET 4.5.</span></span> <span data-ttu-id="c4ea2-115">Nový model pro asynchronní metody je volána *Task-based Asynchronous Pattern* (**klepněte na**).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-115">The new model for asynchronous methods is called the *Task-based Asynchronous Pattern* (**TAP**).</span></span> <span data-ttu-id="c4ea2-116">V tomto kurzu se předpokládá, máte určitou znalost asynchronní pomocí programing [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) a [asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) klíčová slova a [úloh](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-116">This tutorial assumes you have some familiarity with asynchronous programing using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace.</span></span>

<span data-ttu-id="c4ea2-117">Další informace o použití [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) a [asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) klíčová slova a [úloh](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) obor názvů, naleznete v následujících odkazech.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-117">For more information on the using [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the following references.</span></span>

- [<span data-ttu-id="c4ea2-118">Dokument White Paper: Asynchrony v rozhraní .NET</span><span class="sxs-lookup"><span data-stu-id="c4ea2-118">Whitepaper: Asynchrony in .NET</span></span>](https://go.microsoft.com/fwlink/?LinkId=204844)
- [<span data-ttu-id="c4ea2-119">Asynchronní/Await – nejčastější dotazy</span><span class="sxs-lookup"><span data-stu-id="c4ea2-119">Async/Await FAQ</span></span>](https://blogs.msdn.com/b/pfxteam/archive/2012/04/12/10293335.aspx)
- [<span data-ttu-id="c4ea2-120">Asynchronní programování v sadě Visual Studio</span><span class="sxs-lookup"><span data-stu-id="c4ea2-120">Visual Studio Asynchronous Programming</span></span>](https://msdn.microsoft.com/vstudio/gg316360)

## <a id="HowRequestsProcessedByTP"></a>  <span data-ttu-id="c4ea2-121">Zpracování požadavků ve fondu vláken</span><span class="sxs-lookup"><span data-stu-id="c4ea2-121">How Requests Are Processed by the Thread Pool</span></span>

<span data-ttu-id="c4ea2-122">Rozhraní .NET Framework na webový server udržuje fond vláken, které se používají ke zpracování požadavků ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-122">On the web server, the .NET Framework maintains a pool of threads that are used to service ASP.NET requests.</span></span> <span data-ttu-id="c4ea2-123">Pokud dorazí požadavek, je odeslána vlákno z fondu ke zpracování tohoto požadavku.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-123">When a request arrives, a thread from the pool is dispatched to process that request.</span></span> <span data-ttu-id="c4ea2-124">Pokud je požadavek zpracován synchronně, vláken, který zpracuje žádost je zaneprázdněn při žádost je zpracovávána a že vlákno nemůže obsloužit další požadavek.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-124">If the request is processed synchronously, the thread that processes the request is busy while the request is being processed, and that thread cannot service another request.</span></span>   
  
<span data-ttu-id="c4ea2-125">Toto nemusí být problém, protože fond vláken můžete provedeny dostatečně velké na to, aby dokázala pojmout mnoho zaneprázdněn vláken.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-125">This might not be a problem, because the thread pool can be made large enough to accommodate many busy threads.</span></span> <span data-ttu-id="c4ea2-126">Počet vláken ve fondu vláken je však omezená (pro .NET 4.5 maximální výchozí hodnota je 5 000).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-126">However, the number of threads in the thread pool is limited (the default maximum for .NET 4.5 is 5,000).</span></span> <span data-ttu-id="c4ea2-127">V velké aplikace s vysokou souběžnosti dlouho běžící požadavků může být zaneprázdněn všech dostupných vláken.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-127">In large applications with high concurrency of long-running requests, all available threads might be busy.</span></span> <span data-ttu-id="c4ea2-128">Tento stav se označuje jako přístup z více vláken nedostatku prostředků.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-128">This condition is known as thread starvation.</span></span> <span data-ttu-id="c4ea2-129">Když je dosaženo této podmínky, webový server fronty požadavků.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-129">When this condition is reached, the web server queues requests.</span></span> <span data-ttu-id="c4ea2-130">Pokud zaplnění fronty požadavků, webový server odmítá požadavky se stavem HTTP 503 (Server je přetížen).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-130">If the request queue becomes full, the web server rejects requests with an HTTP 503 status (Server Too Busy).</span></span> <span data-ttu-id="c4ea2-131">Fond vláken CLR má omezení na nové vlákno injekce.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-131">The CLR thread pool has limitations on new thread injections.</span></span> <span data-ttu-id="c4ea2-132">Pokud je shlukovým přenosem souběžnosti (to znamená, že váš web z umístění můžete najednou získat velkého množství požadavků) a všechna vlákna požadavek k dispozici jsou zaneprázdněn z důvodu back-end volání s vysokou latencí, míra vkládání omezený přístup z více vláken můžete provést velmi špatně reakce aplikace.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-132">If concurrency is bursty (that is, your web site can suddenly get a large number of requests) and all available request threads are busy because of backend calls with high latency, the limited thread injection rate can make your application respond very poorly.</span></span> <span data-ttu-id="c4ea2-133">Kromě toho má každý nové vlákno přidané do fondu vláken režijní náklady na (například 1 MB paměti zásobníku).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-133">Additionally, each new thread added to the thread pool has overhead (such as 1 MB of stack memory).</span></span> <span data-ttu-id="c4ea2-134">Webové aplikace pomocí synchronních metod k volání služby vysokou latencí, kde fondu vláken zvětšuje, aby výchozí rozhraní .NET 4.5 maximálně 5 000 vláken by využívat přibližně 5 GB více paměti, než je možné aplikaci na stejné žádosti o službu pomocí asynchronní metody a jenom 50 vláken.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-134">A web application using synchronous methods to service high latency calls where the thread pool grows to the .NET 4.5 default maximum of 5, 000 threads would consume approximately 5 GB more memory than an application able the service the same requests using asynchronous methods and only 50 threads.</span></span> <span data-ttu-id="c4ea2-135">Pokud jste to asynchronní pracovní, ne vždy používáte vlákna.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-135">When you're doing asynchronous work, you're not always using a thread.</span></span> <span data-ttu-id="c4ea2-136">Například při provádění požadavku asynchronní webové služby ASP.NET nebudete používat žádné podprocesy mezi **asynchronní** volání metody a **await**.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-136">For example, when you make an asynchronous web service request, ASP.NET will not be using any threads between the **async** method call and the **await**.</span></span> <span data-ttu-id="c4ea2-137">Použití fondu vláken pro žádosti o služby s vysokou latencí může vést k velké paměti nároky a nízký využití hardwaru serveru.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-137">Using the thread pool to service requests with high latency can lead to a large memory footprint and poor utilization of the server hardware.</span></span>

## <a name="processing-asynchronous-requests"></a><span data-ttu-id="c4ea2-138">Asynchronní zpracování požadavků</span><span class="sxs-lookup"><span data-stu-id="c4ea2-138">Processing Asynchronous Requests</span></span>

<span data-ttu-id="c4ea2-139">Ve webové aplikaci, která zobrazí velký počet souběžných požadavků na spuštění nebo má shlukovým přenosem zatížení (kde souběžnosti najednou zvyšuje) provedení asynchronní volání webové služby zvyšuje rychlost reakce aplikace.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-139">In a web app that sees a large number of concurrent requests at start-up or has a bursty load (where concurrency increases suddenly), making web service calls asynchronous increases the responsiveness of the app.</span></span> <span data-ttu-id="c4ea2-140">Asynchronní požadavek trvá stejné množství času na zpracování jako synchronní požadavek.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-140">An asynchronous request takes the same amount of time to process as a synchronous request.</span></span> <span data-ttu-id="c4ea2-141">Pokud požadavek díky webové službě volání, které vyžaduje dvě sekundy k dokončení, požadavku trvá dvou sekund, zda je prováděno synchronně nebo asynchronně.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-141">If a request makes a web service call that requires two seconds to complete, the request takes two seconds whether it's performed synchronously or asynchronously.</span></span> <span data-ttu-id="c4ea2-142">Během asynchronního volání, ale vlákno nezablokuje z reagovat na požadavky na jiné během čekání na dokončení první žádosti.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-142">However during an asynchronous call, a thread isn't blocked from responding to other requests while it waits for the first request to complete.</span></span> <span data-ttu-id="c4ea2-143">Proto asynchronní požadavky zabránit růstu žádosti o služby Řízení front a vlákno fondu při mnoho souběžných požadavků, které vyvolají dlouhotrvající operace.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-143">Therefore, asynchronous requests prevent request queuing and thread pool growth when there are many concurrent requests that invoke long-running operations.</span></span>

## <a id="ChoosingSyncVasync"></a>  <span data-ttu-id="c4ea2-144">Výběr metody synchronní nebo asynchronní akce</span><span class="sxs-lookup"><span data-stu-id="c4ea2-144">Choosing Synchronous or Asynchronous Action Methods</span></span>

<span data-ttu-id="c4ea2-145">Tato část obsahuje pokyny pro použití metody synchronní nebo asynchronní akce.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-145">This section lists guidelines for when to use synchronous or asynchronous action methods.</span></span> <span data-ttu-id="c4ea2-146">Jsou to jenom pokyny; Zkontrolujte každou aplikaci zvlášť k určení, zda asynchronní metody pomoci s výkonem.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-146">These are just guidelines; examine each application individually to determine whether asynchronous methods help with performance.</span></span>

<span data-ttu-id="c4ea2-147">Obecně platí používejte synchronní metody byly splněny následující podmínky:</span><span class="sxs-lookup"><span data-stu-id="c4ea2-147">In general, use synchronous methods for the following conditions:</span></span>

- <span data-ttu-id="c4ea2-148">Operace jsou jednoduché nebo krátké spuštěná.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-148">The operations are simple or short-running.</span></span>
- <span data-ttu-id="c4ea2-149">Jednoduchost je důležitější než účinnost.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-149">Simplicity is more important than efficiency.</span></span>
- <span data-ttu-id="c4ea2-150">Operace, které jsou primárně procesoru operace místo operace, které zahrnují rozsáhlé disk nebo nároky na síť.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-150">The operations are primarily CPU operations instead of operations that involve extensive disk or network overhead.</span></span> <span data-ttu-id="c4ea2-151">Pomocí metody asynchronní akce na operace vázané na procesor poskytuje žádné výhody a další režie výsledkem.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-151">Using asynchronous action methods on CPU-bound operations provides no benefits and results in more overhead.</span></span>

  <span data-ttu-id="c4ea2-152">Obecně platí používejte asynchronní metody byly splněny následující podmínky:</span><span class="sxs-lookup"><span data-stu-id="c4ea2-152">In general, use asynchronous methods for the following conditions:</span></span>

- <span data-ttu-id="c4ea2-153">Jste volání služby, které mohou být využívány prostřednictvím asynchronních metod a používáte rozhraní .NET 4.5 nebo vyšší.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-153">You're calling services that can be consumed through asynchronous methods, and you're using .NET 4.5 or higher.</span></span>
- <span data-ttu-id="c4ea2-154">Operace, které jsou vázané na síti nebo I/čítači místo vázané na procesor.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-154">The operations are network-bound or I/O-bound instead of CPU-bound.</span></span>
- <span data-ttu-id="c4ea2-155">Paralelismus je důležitější než jednoduchost kódu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-155">Parallelism is more important than simplicity of code.</span></span>
- <span data-ttu-id="c4ea2-156">Chcete zadat mechanismus, který umožňuje uživatelům zrušit žádost o časově náročné.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-156">You want to provide a mechanism that lets users cancel a long-running request.</span></span>
- <span data-ttu-id="c4ea2-157">Když výhodou přepínání vláken převáží náklady na kontext přepínače.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-157">When the benefit of switching threads outweighs the cost of the context switch.</span></span> <span data-ttu-id="c4ea2-158">Obecně platí měl by metodu asynchronní Pokud synchronní metoda čeká na vlákno žádost ASP.NET přitom žádné kroky.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-158">In general, you should make a method asynchronous if the synchronous method waits on the ASP.NET request thread while doing no work.</span></span> <span data-ttu-id="c4ea2-159">Tím, že volání asynchronní, není vlákno žádost ASP.NET zastaveno, provádění žádné pracovní, kdy čeká k dokončení žádosti webové služby.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-159">By making the call asynchronous, the ASP.NET request thread is not stalled doing no work while it waits for the web service request to complete.</span></span>
- <span data-ttu-id="c4ea2-160">Testování ukazuje, že blokování operace jsou úzkým místem v výkonu webu a že služby IIS můžete další žádosti o služby s použitím pro tyto blokování volání asynchronních metod.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-160">Testing shows that the blocking operations are a bottleneck in site performance and that IIS can service more requests by using asynchronous methods for these blocking calls.</span></span>

  <span data-ttu-id="c4ea2-161">Ke stažení ukázkové ukazuje, jak efektivně používat metody asynchronní akce.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-161">The downloadable sample shows how to use asynchronous action methods effectively.</span></span> <span data-ttu-id="c4ea2-162">Ukázka poskytuje byla určená k poskytnutí jednoduché ukázka asynchronního programování v architektuře ASP.NET MVC 4 pomocí rozhraní .NET 4.5.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-162">The sample provided was designed to provide a simple demonstration of asynchronous programming in ASP.NET MVC 4 using .NET 4.5.</span></span> <span data-ttu-id="c4ea2-163">Ukázka neměla být referenční architektura pro asynchronní programování v architektuře ASP.NET MVC.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-163">The sample is not intended to be a reference architecture for asynchronous programming in ASP.NET MVC.</span></span> <span data-ttu-id="c4ea2-164">Ukázka programu volání [rozhraní ASP.NET Web API](../../../web-api/index.md) metody, které pak volání [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) k simulaci volání dlouho běžící webové služby.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-164">The sample program calls [ASP.NET Web API](../../../web-api/index.md) methods which in turn call [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate long-running web service calls.</span></span> <span data-ttu-id="c4ea2-165">Většina aplikací produkční nezobrazí takové zřejmé výhody metody asynchronní akce.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-165">Most production applications will not show such obvious benefits to using asynchronous action methods.</span></span>   
  
<span data-ttu-id="c4ea2-166">Několik aplikace vyžadují všechny metody akce jako asynchronní.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-166">Few applications require all action methods to be asynchronous.</span></span> <span data-ttu-id="c4ea2-167">Převádění několik metod synchronní akce pro asynchronní metody často poskytuje nejlepší zvýšení efektivity pro množství práce potřebné.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-167">Often, converting a few synchronous action methods to asynchronous methods provides the best efficiency increase for the amount of work required.</span></span>

## <a id="SampleApp"></a>  <span data-ttu-id="c4ea2-168">Ukázkové aplikace</span><span class="sxs-lookup"><span data-stu-id="c4ea2-168">The Sample Application</span></span>

<span data-ttu-id="c4ea2-169">Si můžete stáhnout ukázkovou aplikaci z [ https://github.com/RickAndMSFT/Async-ASP.NET/ ](https://github.com/RickAndMSFT/Async-ASP.NET) na [Githubu](https://github.com/) lokality.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-169">You can download the sample application from [https://github.com/RickAndMSFT/Async-ASP.NET/](https://github.com/RickAndMSFT/Async-ASP.NET) on the [GitHub](https://github.com/) site.</span></span> <span data-ttu-id="c4ea2-170">Úložiště se skládá ze tří projektů:</span><span class="sxs-lookup"><span data-stu-id="c4ea2-170">The repository consists of three projects:</span></span>

- <span data-ttu-id="c4ea2-171">*Mvc4Async*: ASP.NET MVC 4 projekt, který obsahuje kód použitý v tomto kurzu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-171">*Mvc4Async*: The ASP.NET MVC 4 project that contains the code used in this tutorial.</span></span> <span data-ttu-id="c4ea2-172">Umožňuje volání webového rozhraní API **WebAPIpgw** služby.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-172">It makes Web API calls to the **WebAPIpgw** service.</span></span>
- <span data-ttu-id="c4ea2-173">*WebAPIpgw*: projekt ASP.NET MVC 4 Web API, který implementuje `Products, Gizmos and Widgets` řadiče.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-173">*WebAPIpgw*: The ASP.NET MVC 4 Web API project that implements the `Products, Gizmos and Widgets` controllers.</span></span> <span data-ttu-id="c4ea2-174">Poskytuje data pro *WebAppAsync* projektu a *Mvc4Async* projektu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-174">It provides the data for the *WebAppAsync* project and the *Mvc4Async* project.</span></span>
- <span data-ttu-id="c4ea2-175">*WebAppAsync*: webových formulářů ASP.NET projektu používá v jiné kurzu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-175">*WebAppAsync*: The ASP.NET Web Forms project used in another tutorial.</span></span>

## <a id="GizmosSynch"></a>  <span data-ttu-id="c4ea2-176">Metoda si synchronní akce</span><span class="sxs-lookup"><span data-stu-id="c4ea2-176">The Gizmos Synchronous Action Method</span></span>

 <span data-ttu-id="c4ea2-177">Následující kód ukazuje `Gizmos` metodu synchronní akce, který slouží k zobrazení seznamu si.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-177">The following code shows the `Gizmos` synchronous action method that is used to display a list of gizmos.</span></span> <span data-ttu-id="c4ea2-178">(V tomto článku gizmo je fiktivních mechanických zařízení.)</span><span class="sxs-lookup"><span data-stu-id="c4ea2-178">(For this article, a gizmo is a fictional mechanical device.)</span></span> 

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample1.cs)]

<span data-ttu-id="c4ea2-179">Následující kód ukazuje `GetGizmos` metoda gizmo služby.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-179">The following code shows the `GetGizmos` method of the gizmo service.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample2.cs)]

<span data-ttu-id="c4ea2-180">`GizmoService GetGizmos` Metoda předá identifikátoru URI služby ASP.NET Web API HTTP, který vrátí seznam hodnot si data.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-180">The `GizmoService GetGizmos` method passes a URI to an ASP.NET Web API HTTP service which returns a list of gizmos data.</span></span> <span data-ttu-id="c4ea2-181">*WebAPIpgw* projekt obsahuje implementace rozhraní Web API `gizmos, widget` a `product` řadiče.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-181">The *WebAPIpgw* project contains the implementation of the Web API `gizmos, widget` and `product` controllers.</span></span>  
<span data-ttu-id="c4ea2-182">Následující obrázek znázorňuje zobrazení si z ukázkového projektu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-182">The following image shows the gizmos view from the sample project.</span></span>

![Si](using-asynchronous-methods-in-aspnet-mvc-4/_static/image1.png)

## <a id="CreatingAsynchGizmos"></a>  <span data-ttu-id="c4ea2-184">Vytváření si asynchronní metody akce</span><span class="sxs-lookup"><span data-stu-id="c4ea2-184">Creating an Asynchronous Gizmos Action Method</span></span>

<span data-ttu-id="c4ea2-185">Ukázka používá nový [asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) a [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) klíčová slova (k dispozici v rozhraní .NET 4.5 a Visual Studio 2012) chcete, aby služba je zodpovědná za údržbu složité transformace potřebné pro kompilátor asynchronní programování.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-185">The sample uses the new [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) and [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keywords (available in .NET 4.5 and Visual Studio 2012) to let the compiler be responsible for maintaining the complicated transformations necessary for asynchronous programming.</span></span> <span data-ttu-id="c4ea2-186">Kompilátor umožňuje psát kód, který vytvoří jazyka C# na synchronní řízení toku pomocí a kompilátor automaticky použije transformace nezbytných k používání zpětných volání k zabránění blokování vláken.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-186">The compiler lets you write code using the C#'s synchronous control flow constructs and the compiler automatically applies the transformations necessary to use callbacks in order to avoid blocking threads.</span></span>

<span data-ttu-id="c4ea2-187">Následující kód ukazuje `Gizmos` synchronní metoda a `GizmosAsync` asynchronní metody.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-187">The following code shows the `Gizmos` synchronous method and the `GizmosAsync` asynchronous method.</span></span> <span data-ttu-id="c4ea2-188">Pokud váš prohlížeč podporuje [standardu HTML 5 `<mark>` element](http://www.w3.org/wiki/HTML/Elements/mark), uvidíte změny v `GizmosAsync` v žlutý zvýraznění.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-188">If your browser supports the [HTML 5 `<mark>` element](http://www.w3.org/wiki/HTML/Elements/mark), you'll see the changes in `GizmosAsync` in yellow highlight.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample3.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample4.cs?highlight=1,3,5)]

 <span data-ttu-id="c4ea2-189">Aby byly použity následující změny `GizmosAsync` jako asynchronní.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-189">The following changes were applied to allow the `GizmosAsync` to be asynchronous.</span></span>

- <span data-ttu-id="c4ea2-190">Metoda je označena [asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) – klíčové slovo, které sděluje kompilátoru generování zpětných volání pro části textu a automaticky vytvářet `Task<ActionResult>` , je vrácena.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-190">The method is marked with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, which tells the compiler to generate callbacks for parts of the body and to automatically create a `Task<ActionResult>` that is returned.</span></span>
- <span data-ttu-id="c4ea2-191">&quot;Asynchronní&quot; se připojí k název metody.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-191">&quot;Async&quot; was appended to the method name.</span></span> <span data-ttu-id="c4ea2-192">Připojování "Asynchronní" není povinný, ale je konvence při psaní asynchronních metod.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-192">Appending "Async" is not required but is the convention when writing asynchronous methods.</span></span>
- <span data-ttu-id="c4ea2-193">Návratový typ byl změněn z `ActionResult` k `Task<ActionResult>`.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-193">The return type was changed from `ActionResult` to `Task<ActionResult>`.</span></span> <span data-ttu-id="c4ea2-194">Návratový typ `Task<ActionResult>` reprezentuje probíhající práce a poskytuje volající metody s popisovačem, pomocí kterého je možné čekání na dokončení asynchronní operace.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-194">The return type of `Task<ActionResult>` represents ongoing work and provides callers of the method with a handle through which to wait for the asynchronous operation's completion.</span></span> <span data-ttu-id="c4ea2-195">V takovém případě volající je webová služba.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-195">In this case, the caller is the web service.</span></span> <span data-ttu-id="c4ea2-196">`Task<ActionResult>` představuje probíhající práce s výsledkem `ActionResult.`</span><span class="sxs-lookup"><span data-stu-id="c4ea2-196">`Task<ActionResult>` represents ongoing work with a result of `ActionResult.`</span></span>
- <span data-ttu-id="c4ea2-197">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) – klíčové slovo, které bylo použito pro volání webové služby.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-197">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the web service call.</span></span>
- <span data-ttu-id="c4ea2-198">Volala se asynchronní webového rozhraní API služby (`GetGizmosAsync`).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-198">The asynchronous web service API was called (`GetGizmosAsync`).</span></span>

<span data-ttu-id="c4ea2-199">Uvnitř `GetGizmosAsync` metoda body jinou asynchronní metodu `GetGizmosAsync` je volána.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-199">Inside of the `GetGizmosAsync` method body another asynchronous method, `GetGizmosAsync` is called.</span></span> <span data-ttu-id="c4ea2-200">`GetGizmosAsync` Vrátí okamžitě `Task<List<Gizmo>>` , nakonec dokončí když jsou data k dispozici.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-200">`GetGizmosAsync` immediately returns a `Task<List<Gizmo>>` that will eventually complete when the data is available.</span></span> <span data-ttu-id="c4ea2-201">Vzhledem k tomu, že nechcete dělat žádné další kroky, dokud se gizmo data, kód čeká úlohy (pomocí **await** – klíčové slovo).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-201">Because you don't want to do anything else until you have the gizmo data, the code awaits the task (using the **await** keyword).</span></span> <span data-ttu-id="c4ea2-202">Můžete použít **await** – klíčové slovo pouze v metodách opatřen poznámkou **asynchronní** – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-202">You can use the **await** keyword only in methods annotated with the **async** keyword.</span></span>

<span data-ttu-id="c4ea2-203">**Await** – klíčové slovo neblokuje vlákno až do dokončení úlohy.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-203">The **await** keyword does not block the thread until the task is complete.</span></span> <span data-ttu-id="c4ea2-204">Zaregistruje zbytek metodu jako zpětné volání v úloze a vrátí okamžitě.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-204">It signs up the rest of the method as a callback on the task, and immediately returns.</span></span> <span data-ttu-id="c4ea2-205">Pokud úlohu awaited nakonec dokončí, bude vyvolání že zpětné volání a proto pokračovat v provádění právo metoda, kde bylo přerušeno.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-205">When the awaited task eventually completes, it will invoke that callback and thus resume the execution of the method right where it left off.</span></span> <span data-ttu-id="c4ea2-206">Další informace o používání [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) a [asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) klíčová slova a [úloh](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) obor názvů, najdete v článku [asynchronní odkazy](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async).</span><span class="sxs-lookup"><span data-stu-id="c4ea2-206">For more information on using the [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) and [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keywords and the [Task](https://msdn.microsoft.com/library/system.threading.tasks.task.aspx) namespace, see the [async references](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/async).</span></span>

<span data-ttu-id="c4ea2-207">Následující kód ukazuje `GetGizmos` a `GetGizmosAsync` metody.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-207">The following code shows the `GetGizmos` and `GetGizmosAsync` methods.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample5.cs)]

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample6.cs?highlight=1,4-8)]

 <span data-ttu-id="c4ea2-208">Asynchronní změny jsou podobné těm, které jsou provedeny **GizmosAsync** výše.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-208">The asynchronous changes are similar to those made to the **GizmosAsync** above.</span></span> 

- <span data-ttu-id="c4ea2-209">Podpis metody byl opatřen poznámkou [asynchronní](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) – klíčové slovo, návratový typ byl změněn na `Task<List<Gizmo>>`, a *asynchronní* se připojí k název metody.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-209">The method signature was annotated with the [async](https://msdn.microsoft.com/library/hh156513(VS.110).aspx) keyword, the return type was changed to `Task<List<Gizmo>>`, and *Async* was appended to the method name.</span></span>
- <span data-ttu-id="c4ea2-210">Asynchronní [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) třída se používá namísto [WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx) třídy.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-210">The asynchronous [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) class is used instead of the [WebClient](https://msdn.microsoft.com/library/system.net.webclient.aspx) class.</span></span>
- <span data-ttu-id="c4ea2-211">[Await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) – klíčové slovo, které bylo použito pro [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) asynchronních metod.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-211">The [await](https://msdn.microsoft.com/library/hh156528(VS.110).aspx) keyword was applied to the [HttpClient](https://msdn.microsoft.com/library/system.net.http.httpclient(VS.110).aspx) asynchronous methods.</span></span>

<span data-ttu-id="c4ea2-212">Následující obrázek znázorňuje asynchronní gizmo zobrazení.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-212">The following image shows the asynchronous gizmo view.</span></span>

![async](using-asynchronous-methods-in-aspnet-mvc-4/_static/image2.png)

<span data-ttu-id="c4ea2-214">Prezentace prohlížeče si dat je stejný jako zobrazení vytvořené synchronní volání.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-214">The browsers presentation of the gizmos data is identical to the view created by the synchronous call.</span></span> <span data-ttu-id="c4ea2-215">Jediným rozdílem je, že asynchronní verzi může být více původce přetížena.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-215">The only difference is the asynchronous version may be more performant under heavy loads.</span></span>

## <a id="Parallel"></a>  <span data-ttu-id="c4ea2-216">Paralelní provádění více operací</span><span class="sxs-lookup"><span data-stu-id="c4ea2-216">Performing Multiple Operations in Parallel</span></span>

<span data-ttu-id="c4ea2-217">Asynchronní akce metody mít významné výhody přes synchronních metod, při akci musíte provést několik nezávislých operací.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-217">Asynchronous action methods have a significant advantage over synchronous methods when an action must perform several independent operations.</span></span> <span data-ttu-id="c4ea2-218">V ukázce k dispozici, synchronní metoda `PWG`(pro produkty, pomůcek a si) se zobrazí výsledky získáte seznam produktů, pomůcek a si tři volání webové služby.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-218">In the sample provided, the synchronous method `PWG`(for Products, Widgets and Gizmos) displays the results of three web service calls to get a list of products, widgets, and gizmos.</span></span> <span data-ttu-id="c4ea2-219">[Rozhraní ASP.NET Web API](../../../web-api/index.md) projekt, který poskytuje tyto služby používá [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) k simulaci latenci nebo pomalé síťové volání.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-219">The [ASP.NET Web API](../../../web-api/index.md) project that provides these services uses [Task.Delay](https://msdn.microsoft.com/library/hh139096(VS.110).aspx) to simulate latency or slow network calls.</span></span> <span data-ttu-id="c4ea2-220">Když je zpoždění nastavená na 500 milisekund asynchronní `PWGasync` metoda přebírá trochu delší než 500 ms do dokončení při synchronní `PWG` verze převezme 1 500 milisekundách.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-220">When the delay is set to 500 milliseconds, the asynchronous `PWGasync` method takes a little over 500 milliseconds to complete while the synchronous `PWG` version takes over 1,500 milliseconds.</span></span> <span data-ttu-id="c4ea2-221">Synchronní `PWG` metoda je znázorněno v následujícím kódu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-221">The synchronous `PWG` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample7.cs)]

<span data-ttu-id="c4ea2-222">Asynchronní `PWGasync` metoda je znázorněno v následujícím kódu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-222">The asynchronous `PWGasync` method is shown in the following code.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample8.cs?highlight=1,3,12)]

<span data-ttu-id="c4ea2-223">Následující obrázek znázorňuje zobrazení vrácených **PWGasync** metoda.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-223">The following image shows the view returned from the **PWGasync** method.</span></span>

![pwgAsync](using-asynchronous-methods-in-aspnet-mvc-4/_static/image3.png)

## <a id="CancelToken"></a>  <span data-ttu-id="c4ea2-225">Pomocí Token zrušení</span><span class="sxs-lookup"><span data-stu-id="c4ea2-225">Using a Cancellation Token</span></span>

<span data-ttu-id="c4ea2-226">Metody asynchronní akce vrácení `Task<ActionResult>`jsou možné zrušit, která je jejich trvat [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parametr, pokud je k dispozici s [hodnota vlastnosti AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx) atribut.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-226">Asynchronous action methods returning `Task<ActionResult>`are cancelable, that is they take a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter when one is provided with the [AsyncTimeout](https://msdn.microsoft.com/library/system.web.mvc.asynctimeoutattribute(VS.108).aspx) attribute.</span></span> <span data-ttu-id="c4ea2-227">Následující kód ukazuje `GizmosCancelAsync` metoda s časovým limitem 150 milisekund.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-227">The following code shows the `GizmosCancelAsync` method with a timeout of 150 milliseconds.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample9.cs?highlight=1-3,5,10)]

<span data-ttu-id="c4ea2-228">Následující kód ukazuje GetGizmosAsync přetížení, která přebírá [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parametr.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-228">The following code shows the GetGizmosAsync overload, which takes a [CancellationToken](https://msdn.microsoft.com/library/system.threading.cancellationtoken(VS.110).aspx) parameter.</span></span>

[!code-csharp[Main](using-asynchronous-methods-in-aspnet-mvc-4/samples/sample10.cs)]

<span data-ttu-id="c4ea2-229">V ukázkové aplikaci zadané, výběr *ukázkový Token zrušení* odkaz volání `GizmosCancelAsync` metoda a předvádí zrušení asynchronního volání.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-229">In the sample application provided, selecting the *Cancellation Token Demo* link calls the `GizmosCancelAsync` method and demonstrates the cancelation of the asynchronous call.</span></span>

## <a id="ServerConfig"></a>  <span data-ttu-id="c4ea2-230">Konfigurace serveru pro volání vysoké souběžnosti vysoký latence webové služby</span><span class="sxs-lookup"><span data-stu-id="c4ea2-230">Server Configuration for High Concurrency/High Latency Web Service Calls</span></span>

<span data-ttu-id="c4ea2-231">Pochopit výhody asynchronní webové aplikace, může být nutné provést některé změny konfigurace serveru výchozí.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-231">To realize the benefits of an asynchronous web application, you might need to make some changes to the default server configuration.</span></span> <span data-ttu-id="c4ea2-232">Mějte na paměti při konfiguraci a zátěžové testování asynchronní webové aplikace.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-232">Keep the following in mind when configuring and stress testing your asynchronous web application.</span></span>

- <span data-ttu-id="c4ea2-233">Windows 7, Windows Vista a všechny klientské operační systémy Windows mít maximálně 10 souběžných požadavků.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-233">Windows 7, Windows Vista and all Windows client operating systems have a maximum of 10 concurrent requests.</span></span> <span data-ttu-id="c4ea2-234">Budete potřebovat operační systém Windows Server zobrazíte výhod asynchronních metod vysoké zatížení.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-234">You'll need a Windows Server operating system to see the benefits of asynchronous methods under high load.</span></span>
- <span data-ttu-id="c4ea2-235">Službu IIS zaregistrujte .NET 4.5 z příkazového řádku se zvýšenými oprávněními:</span><span class="sxs-lookup"><span data-stu-id="c4ea2-235">Register .NET 4.5 with IIS from an elevated command prompt:</span></span>  
  <span data-ttu-id="c4ea2-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis -i</span><span class="sxs-lookup"><span data-stu-id="c4ea2-236">%windir%\Microsoft.NET\Framework64\v4.0.30319\aspnet\_regiis -i</span></span>  
  <span data-ttu-id="c4ea2-237">V tématu [ASP.NET IIS Registration Tool (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span><span class="sxs-lookup"><span data-stu-id="c4ea2-237">See    [ASP.NET IIS Registration Tool (Aspnet\_regiis.exe)](https://msdn.microsoft.com/library/k6h9cz8h.aspx)</span></span>
- <span data-ttu-id="c4ea2-238">Možná budete muset zvýšit [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) limit fronty z výchozí hodnotu 1000 až 5 000.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-238">You might need to increase the [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) queue limit from the default value of 1,000 to 5,000.</span></span> <span data-ttu-id="c4ea2-239">Pokud toto nastavení je příliš nízké, mohou se zobrazit [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) zamítal požadavky, se stavem HTTP 503.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-239">If the setting is too low, you may see [HTTP.sys](https://www.iis.net/learn/get-started/introduction-to-iis/introduction-to-iis-architecture) reject requests with a HTTP 503 status.</span></span> <span data-ttu-id="c4ea2-240">Chcete-li změnit limit fronty HTTP.sys:</span><span class="sxs-lookup"><span data-stu-id="c4ea2-240">To change the HTTP.sys queue limit:</span></span>

    - <span data-ttu-id="c4ea2-241">Otevřete Správce služby IIS a přejděte do podokna fondů aplikací.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-241">Open IIS manager and navigate to the Application Pools pane.</span></span>
    - <span data-ttu-id="c4ea2-242">Klikněte pravým tlačítkem na cílový fond aplikací a vyberte **Upřesnit nastavení**.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-242">Right click on the target application pool and select **Advanced Settings**.</span></span>  
        <span data-ttu-id="c4ea2-243">![Upřesnit](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="c4ea2-243">![advanced](using-asynchronous-methods-in-aspnet-mvc-4/_static/image4.png)</span></span>
    - <span data-ttu-id="c4ea2-244">V **Upřesnit nastavení** dialogové okno, změna *délka fronty* od 1 do 5 000 000.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-244">In the **Advanced Settings** dialog box, change *Queue Length* from 1,000 to 5,000.</span></span>  
        <span data-ttu-id="c4ea2-245">![Délka fronty](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span><span class="sxs-lookup"><span data-stu-id="c4ea2-245">![Queue length](using-asynchronous-methods-in-aspnet-mvc-4/_static/image5.png)</span></span>  
  
  <span data-ttu-id="c4ea2-246">Poznámka: v obrázcích výše, rozhraní .NET framework je uveden jako v4.0, i když je fond aplikací pomocí rozhraní .NET 4.5.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-246">Note in the images above, the .NET framework is listed as v4.0, even though the application pool is using .NET 4.5.</span></span> <span data-ttu-id="c4ea2-247">Tato nesrovnalost pochopit, naleznete v následujících tématech:</span><span class="sxs-lookup"><span data-stu-id="c4ea2-247">To understand this discrepancy, see the following:</span></span>

    - [<span data-ttu-id="c4ea2-248">Správa verzí rozhraní .NET a cílení na více – rozhraní .NET 4.5 je místní upgrade .NET 4.0</span><span class="sxs-lookup"><span data-stu-id="c4ea2-248">.NET Versioning and Multi-Targeting - .NET 4.5 is an in-place upgrade to .NET 4.0</span></span>](http://www.hanselman.com/blog/NETVersioningAndMultiTargetingNET45IsAnInplaceUpgradeToNET40.aspx)
    - [<span data-ttu-id="c4ea2-249">Postup nastavení aplikace služby IIS nebo fondu aplikací používá technologii ASP.NET 3.5 namísto 2.0</span><span class="sxs-lookup"><span data-stu-id="c4ea2-249">How to set an IIS Application or AppPool to use ASP.NET 3.5 rather than 2.0</span></span>](http://www.hanselman.com/blog/HowToSetAnIISApplicationOrAppPoolToUseASPNET35RatherThan20.aspx)
    - <span data-ttu-id="c4ea2-250">[Rozhraní .NET framework verze a závislosti](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span><span class="sxs-lookup"><span data-stu-id="c4ea2-250">[.NET Framework Versions and Dependencies](https://msdn.microsoft.com/library/bb822049(VS.110).aspx)</span></span>
- <span data-ttu-id="c4ea2-251">Pokud vaše aplikace používá webové služby nebo System.NET ke komunikaci s back-end pomocí protokolu HTTP budete možná muset zvýšit [connectionManagement – / maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx) elementu.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-251">If your application is using web services or System.NET to communicate with a backend over HTTP you may need to increase the [connectionManagement/maxconnection](https://msdn.microsoft.com/library/fb6y0fyc(VS.110).aspx) element.</span></span> <span data-ttu-id="c4ea2-252">Pro aplikace ASP.NET to je omezena funkci automatické konfigurace 12krát většího počtu procesorů.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-252">For ASP.NET applications, this is limited by the autoConfig feature to 12 times the number of CPUs.</span></span> <span data-ttu-id="c4ea2-253">To znamená, že na quad-proc, můžete mít maximálně 12 \* 4 = 48 souběžných připojení ke koncovému bodu IP.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-253">That means that on a quad-proc, you can have at most 12 \* 4 = 48 concurrent connections to an IP end point.</span></span> <span data-ttu-id="c4ea2-254">Protože to je vázaný na [automatické konfigurace](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx), nejjednodušší způsob, jak zvýšit `maxconnection` v technologie ASP.NET je aplikace nastavit [System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx) v prostřednictvím kódu programu z `Application_Start` metoda v *global.asax* souboru.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-254">Because this is tied to [autoConfig](https://msdn.microsoft.com/library/7w2sway1(VS.110).aspx), the easiest way to increase `maxconnection` in an ASP.NET application is to set [System.Net.ServicePointManager.DefaultConnectionLimit](https://msdn.microsoft.com/library/system.net.servicepointmanager.defaultconnectionlimit(VS.110).aspx) programmatically in the from `Application_Start` method in the *global.asax* file.</span></span> <span data-ttu-id="c4ea2-255">Viz ukázka stáhnout příklad.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-255">See the sample download for an example.</span></span>
- <span data-ttu-id="c4ea2-256">V rozhraní .NET 4.5, výchozí hodnotu 5000 pro [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx) by byl v pořádku.</span><span class="sxs-lookup"><span data-stu-id="c4ea2-256">In .NET 4.5, the default of 5000 for [MaxConcurrentRequestsPerCPU](https://blogs.msdn.com/tmarq/archive/2007/07/21/asp-net-thread-usage-on-iis-7-0-and-6-0.aspx) should be fine.</span></span>
