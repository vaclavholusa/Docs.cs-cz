---
uid: signalr/overview/getting-started/introduction-to-signalr
title: "Úvod do SignalR | Microsoft Docs"
author: pfletcher
description: "Tento článek popisuje, co je SignalR a některé řešení, které je určen k vytvoření."
ms.author: aspnetcontent
manager: wpickett
ms.date: 06/10/2014
ms.topic: article
ms.assetid: 0fab5e35-8c1f-43d4-8635-b8aba8766a71
ms.technology: dotnet-signalr
ms.prod: .net-framework
msc.legacyurl: /signalr/overview/getting-started/introduction-to-signalr
msc.type: authoredcontent
ms.openlocfilehash: 27150d314b6861f1098e6ef4a7de94e7b371a78e
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 11/10/2017
---
<a name="introduction-to-signalr"></a><span data-ttu-id="160b1-103">Úvod do SignalR</span><span class="sxs-lookup"><span data-stu-id="160b1-103">Introduction to SignalR</span></span>
====================
<span data-ttu-id="160b1-104">podle [Patrik Fletcher](https://github.com/pfletcher)</span><span class="sxs-lookup"><span data-stu-id="160b1-104">by [Patrick Fletcher](https://github.com/pfletcher)</span></span>

> <span data-ttu-id="160b1-105">Tento článek popisuje, co je SignalR a některé řešení, které je určen k vytvoření.</span><span class="sxs-lookup"><span data-stu-id="160b1-105">This article describes what SignalR is, and some of the solutions it was designed to create.</span></span> 
> 
> ## <a name="questions-and-comments"></a><span data-ttu-id="160b1-106">Dotazy a připomínky</span><span class="sxs-lookup"><span data-stu-id="160b1-106">Questions and comments</span></span>
> 
> <span data-ttu-id="160b1-107">Prosím sdělit svůj názor na tom, jak líbilo tohoto kurzu a co jsme může zlepšit v komentářích v dolní části stránky.</span><span class="sxs-lookup"><span data-stu-id="160b1-107">Please leave feedback on how you liked this tutorial and what we could improve in the comments at the bottom of the page.</span></span> <span data-ttu-id="160b1-108">Pokud máte otázky, které přímo nesouvisejí s kurz, můžete je do příspěvku [fórum pro ASP.NET SignalR](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) nebo [StackOverflow.com](http://stackoverflow.com/).</span><span class="sxs-lookup"><span data-stu-id="160b1-108">If you have questions that are not directly related to the tutorial, you can post them to the [ASP.NET SignalR forum](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) or [StackOverflow.com](http://stackoverflow.com/).</span></span>


## <a name="what-is-signalr"></a><span data-ttu-id="160b1-109">Co je SignalR?</span><span class="sxs-lookup"><span data-stu-id="160b1-109">What is SignalR?</span></span>

<span data-ttu-id="160b1-110">Funkce SignalR technologie ASP.NET je knihovna pro vývojáře využívající technologii ASP.NET, který zjednodušuje proces přidávání funkce webu v reálném čase do aplikací.</span><span class="sxs-lookup"><span data-stu-id="160b1-110">ASP.NET SignalR is a library for ASP.NET developers that simplifies the process of adding real-time web functionality to applications.</span></span> <span data-ttu-id="160b1-111">Funkce webu v reálném čase je schopnost serveru kód nabízené obsah připojeným klientům okamžitě, jakmile je k dispozici, místo aby se server čekat na klienta k žádosti o nová data.</span><span class="sxs-lookup"><span data-stu-id="160b1-111">Real-time web functionality is the ability to have server code push content to connected clients instantly as it becomes available, rather than having the server wait for a client to request new data.</span></span>

<span data-ttu-id="160b1-112">SignalR slouží k přidání žádné řazení funkce "v reálném čase" webu do aplikace ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="160b1-112">SignalR can be used to add any sort of "real-time" web functionality to your ASP.NET application.</span></span> <span data-ttu-id="160b1-113">Při chat se často používá jako příklad, můžete provést mnoho více.</span><span class="sxs-lookup"><span data-stu-id="160b1-113">While chat is often used as an example, you can do a whole lot more.</span></span> <span data-ttu-id="160b1-114">Kdykoli uživatel aktualizuje na webové stránce zobrazíte nová data nebo stránce implementuje [dlouhé dotazování](http://en.wikipedia.org/wiki/Push_technology#Long_polling) načte nová data, je kandidátem pro použití funkce SignalR.</span><span class="sxs-lookup"><span data-stu-id="160b1-114">Any time a user refreshes a web page to see new data, or the page implements [long polling](http://en.wikipedia.org/wiki/Push_technology#Long_polling) to retrieve new data, it is a candidate for using SignalR.</span></span> <span data-ttu-id="160b1-115">Mezi příklady patří řídicí panely a monitorování aplikací, spolupráce aplikace (např. souběžných úpravy dokumentů), úlohy, Probíhá aktualizace a v reálném čase formuláře.</span><span class="sxs-lookup"><span data-stu-id="160b1-115">Examples include dashboards and monitoring applications, collaborative applications (such as simultaneous editing of documents), job progress updates, and real-time forms.</span></span>

<span data-ttu-id="160b1-116">Funkce SignalR také umožňuje zcela nové typy webových aplikací, které vyžadují vysoká frekvence aktualizace ze serveru, například v reálném čase herní.</span><span class="sxs-lookup"><span data-stu-id="160b1-116">SignalR also enables completely new types of web applications that require high frequency updates from the server, for example, real-time gaming.</span></span> <span data-ttu-id="160b1-117">Skvělé příklad tohoto najdete v tématu [ShootR hra.](http://shootr.signalr.net/)</span><span class="sxs-lookup"><span data-stu-id="160b1-117">For a great example of this, see the [ShootR game.](http://shootr.signalr.net/)</span></span>

<span data-ttu-id="160b1-118">Funkce SignalR poskytuje jednoduché rozhraní API pro vytvoření klienta a serveru vzdálených volání procedur (RPC) volají funkce JavaScript v klientovi prohlížeče (a jiné platformy klienta) z kódu .NET na straně serveru.</span><span class="sxs-lookup"><span data-stu-id="160b1-118">SignalR provides a simple API for creating server-to-client remote procedure calls (RPC) that call JavaScript functions in client browsers (and other client platforms) from server-side .NET code.</span></span> <span data-ttu-id="160b1-119">Funkce SignalR také zahrnuje rozhraní API pro správu připojení (například připojit a odpojit událostí) a seskupování připojení.</span><span class="sxs-lookup"><span data-stu-id="160b1-119">SignalR also includes API for connection management (for instance, connect and disconnect events), and grouping connections.</span></span>

![Volání metody s SignalR](introduction-to-signalr/_static/image1.png)

<span data-ttu-id="160b1-121">SignalR zpracovává správu připojení automaticky a umožňuje vám zprávy všesměrového vysílání pro všechny připojené klienty současně, jako je chatovací místnosti.</span><span class="sxs-lookup"><span data-stu-id="160b1-121">SignalR handles connection management automatically, and lets you broadcast messages to all connected clients simultaneously, like a chat room.</span></span> <span data-ttu-id="160b1-122">Také mohou zasílat zprávy do konkrétní klientů.</span><span class="sxs-lookup"><span data-stu-id="160b1-122">You can also send messages to specific clients.</span></span> <span data-ttu-id="160b1-123">Připojení mezi klientem a serverem je trvalé, na rozdíl od classic připojení protokolu HTTP, které je obnoveno pro každé komunikace směrem.</span><span class="sxs-lookup"><span data-stu-id="160b1-123">The connection between the client and server is persistent, unlike a classic HTTP connection, which is re-established for each communication.</span></span>

<span data-ttu-id="160b1-124">SignalR podporuje funkce "serveru push", ve kterém můžete volat serverový kód kód klienta v prohlížeči Dnes pomocí vzdáleného volání procedur (RPC), nikoli běžné modelu požadavků a odpovědí na webu.</span><span class="sxs-lookup"><span data-stu-id="160b1-124">SignalR supports "server push" functionality, in which server code can call out to client code in the browser using Remote Procedure Calls (RPC), rather than the request-response model common on the web today.</span></span>

<span data-ttu-id="160b1-125">Můžete škálování aplikací SignalR k tisícům klientů pomocí služby Service Bus, SQL Server nebo [Redis](http://redis.io).</span><span class="sxs-lookup"><span data-stu-id="160b1-125">SignalR applications can scale out to thousands of clients using Service Bus, SQL Server or [Redis](http://redis.io).</span></span>

<span data-ttu-id="160b1-126">SignalR je open source, přístupný prostřednictvím [Githubu](https://github.com/signalr).</span><span class="sxs-lookup"><span data-stu-id="160b1-126">SignalR is open-source, accessible through [GitHub](https://github.com/signalr).</span></span>

## <a name="signalr-and-websocket"></a><span data-ttu-id="160b1-127">SignalR a protokolu WebSocket</span><span class="sxs-lookup"><span data-stu-id="160b1-127">SignalR and WebSocket</span></span>

<span data-ttu-id="160b1-128">SignalR používá nový přenos protokolu WebSocket, kde je k dispozici a spadne zpět na starší přenosy potřeby.</span><span class="sxs-lookup"><span data-stu-id="160b1-128">SignalR uses the new WebSocket transport where available, and falls back to older transports where necessary.</span></span> <span data-ttu-id="160b1-129">Zatímco můžete napsat určitě vaší aplikace pomocí protokolu WebSocket přímo, pomocí funkce SignalR znamená, že mnoho další funkce, které by bylo nutné implementovat bude mít neudělali za vás.</span><span class="sxs-lookup"><span data-stu-id="160b1-129">While you could certainly write your application using WebSocket directly, using SignalR means that a lot of the extra functionality you would need to implement will already have been done for you.</span></span> <span data-ttu-id="160b1-130">Co je nejdůležitější – to znamená, že může kód aplikace využívat výhod protokolu WebSocket bez nutnosti starat o vytváření samostatných kódové cestě pro starší klienty.</span><span class="sxs-lookup"><span data-stu-id="160b1-130">Most importantly, this means that you can code your application to take advantage of WebSocket without having to worry about creating a separate code path for older clients.</span></span> <span data-ttu-id="160b1-131">Funkce SignalR také chrání před starosti o aktualizace protokolu WebSocket, protože SignalR budou nadále být aktualizované kvůli podpoře změny v základní přenos poskytuje jednotné rozhraní aplikace mezi verzemi protokolu WebSocket.</span><span class="sxs-lookup"><span data-stu-id="160b1-131">SignalR also shields you from having to worry about updates to WebSocket, since SignalR will continue to be updated to support changes in the underlying transport, providing your application a consistent interface across versions of WebSocket.</span></span>

<span data-ttu-id="160b1-132">Určitě může vytvořit řešení pomocí protokolu WebSocket samostatně, funkce SignalR poskytuje všechny funkce, budete muset napsat sami, jako je například přechod na další přenosy a úprava aplikace aktualizací implementace protokolu WebSocket.</span><span class="sxs-lookup"><span data-stu-id="160b1-132">While you could certainly create a solution using WebSocket alone, SignalR provides all of the functionality you would need to write yourself, such as fallback to other transports and revising your application for updates to WebSocket implementations.</span></span>

<a id="transports"></a>

## <a name="transports-and-fallbacks"></a><span data-ttu-id="160b1-133">Přenosy a případech přejít</span><span class="sxs-lookup"><span data-stu-id="160b1-133">Transports and fallbacks</span></span>

<span data-ttu-id="160b1-134">SignalR je abstrakci přes některé přenosů, které jsou požadovány pro práci v reálném čase mezi klientem a serverem.</span><span class="sxs-lookup"><span data-stu-id="160b1-134">SignalR is an abstraction over some of the transports that are required to do real-time work between client and server.</span></span> <span data-ttu-id="160b1-135">Připojení SignalR se spustí jako HTTP a se poté vyzval připojení protokolu WebSocket, pokud je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="160b1-135">A SignalR connection starts as HTTP, and is then promoted to a WebSocket connection if it is available.</span></span> <span data-ttu-id="160b1-136">Protokol WebSocket je ideální přenos pro funkci SignalR, protože umožňuje využívat s maximální efektivitou paměti serveru, má nejnižší latenci a má nejvíce základní funkce (například plně duplexní komunikace mezi klientem a serverem), ale má také nejpřísnější požadavky: protokolu WebSocket vyžaduje server musí používat Windows Server 2012 nebo Windows 8 a rozhraní .NET Framework 4.5.</span><span class="sxs-lookup"><span data-stu-id="160b1-136">WebSocket is the ideal transport for SignalR, since it makes the most efficient use of server memory, has the lowest latency, and has the most underlying features (such as full duplex communication between client and server), but it also has the most stringent requirements: WebSocket requires the server to be using Windows Server 2012 or Windows 8, and .NET Framework 4.5.</span></span> <span data-ttu-id="160b1-137">Pokud tyto požadavky nejsou splněny, se pokusí používat ostatní přenosy k vytvoření připojení SignalR.</span><span class="sxs-lookup"><span data-stu-id="160b1-137">If these requirements are not met, SignalR will attempt to use other transports to make its connections.</span></span>

### <a name="html-5-transports"></a><span data-ttu-id="160b1-138">Přenosy, HTML 5</span><span class="sxs-lookup"><span data-stu-id="160b1-138">HTML 5 transports</span></span>

<span data-ttu-id="160b1-139">Tyto přenosy závisí na podporu pro [standardu HTML 5](http://en.wikipedia.org/wiki/HTML5).</span><span class="sxs-lookup"><span data-stu-id="160b1-139">These transports depend on support for [HTML 5](http://en.wikipedia.org/wiki/HTML5).</span></span> <span data-ttu-id="160b1-140">Pokud prohlížeč klienta nepodporuje standardu HTML 5, použije se starší přenosy.</span><span class="sxs-lookup"><span data-stu-id="160b1-140">If the client browser does not support the HTML 5 standard, older transports will be used.</span></span>

- <span data-ttu-id="160b1-141">**Protokol WebSocket** (Pokud server i prohlížeč znamenat mohou podporovat protokolu Websocket).</span><span class="sxs-lookup"><span data-stu-id="160b1-141">**WebSocket** (if the both the server and browser indicate they can support Websocket).</span></span> <span data-ttu-id="160b1-142">Protokol WebSocket je pouze přenos, který stanoví true trvalého obousměrné připojení mezi klientem a serverem.</span><span class="sxs-lookup"><span data-stu-id="160b1-142">WebSocket is the only transport that establishes a true persistent, two-way connection between client and server.</span></span> <span data-ttu-id="160b1-143">Má-však také protokolu WebSocket nejpřísnější požadavky; je plně podporovaný jenom v nejnovější verzi aplikace Microsoft Internet Explorer a Google Chrome, Mozilla Firefox a má jenom částečnou implementaci v dalších prohlížečích například Opera a Safari.</span><span class="sxs-lookup"><span data-stu-id="160b1-143">However, WebSocket also has the most stringent requirements; it is fully supported only in the latest versions of Microsoft Internet Explorer, Google Chrome, and Mozilla Firefox, and only has a partial implementation in other browsers such as Opera and Safari.</span></span>
- <span data-ttu-id="160b1-144">**Události odeslané serverem**, označované také jako EventSource (Pokud je prohlížeč podporuje odeslané události serveru, který je v podstatě všechny prohlížeče s výjimkou aplikace Internet Explorer).</span><span class="sxs-lookup"><span data-stu-id="160b1-144">**Server Sent Events**, also known as EventSource (if the browser supports Server Sent Events, which is basically all browsers except Internet Explorer.)</span></span>

### <a name="comet-transports"></a><span data-ttu-id="160b1-145">Přenosy Comet</span><span class="sxs-lookup"><span data-stu-id="160b1-145">Comet transports</span></span>

<span data-ttu-id="160b1-146">Následující přenosy jsou založené na [Comet](http://en.wikipedia.org/wiki/Comet_(programming)) model webové aplikace, ve kterém prohlížeče nebo jiné klienta udržuje uchovávat dlouho požadavek HTTP, který server můžete použít tak, aby nabízel data konkrétně klientovi bez klienta nástroje o to požádá.</span><span class="sxs-lookup"><span data-stu-id="160b1-146">The following transports are based on the [Comet](http://en.wikipedia.org/wiki/Comet_(programming)) web application model, in which a browser or other client maintains a long-held HTTP request, which the server can use to push data to the client without the client specifically requesting it.</span></span>

- <span data-ttu-id="160b1-147">**Navždy rámce** (pro Internet Explorer pouze).</span><span class="sxs-lookup"><span data-stu-id="160b1-147">**Forever Frame** (for Internet Explorer only).</span></span> <span data-ttu-id="160b1-148">Navždy rámce vytvoří skrytá IFrame, který vytváří požadavek na koncový bod na serveru, který není dokončena.</span><span class="sxs-lookup"><span data-stu-id="160b1-148">Forever Frame creates a hidden IFrame which makes a request to an endpoint on the server that does not complete.</span></span> <span data-ttu-id="160b1-149">Server pak průběžně odešle skriptu klientovi, který se spustí okamžitě, poskytuje jednosměrný v reálném čase připojení ze serveru do klienta.</span><span class="sxs-lookup"><span data-stu-id="160b1-149">The server then continually sends script to the client which is immediately executed, providing a one-way realtime connection from server to client.</span></span> <span data-ttu-id="160b1-150">Připojení z klienta na server používá samostatné připojení ze serveru pro připojení klienta, a jako standardní požadavku HTTP, se vytvoří nové připojení pro jednotlivá data, která potřebují k odeslání.</span><span class="sxs-lookup"><span data-stu-id="160b1-150">The connection from client to server uses a separate connection from the server to client connection, and like a standard HTTP request, a new connection is created for each piece of data that needs to be sent.</span></span>
- <span data-ttu-id="160b1-151">**AJAX dlouhé dotazování**.</span><span class="sxs-lookup"><span data-stu-id="160b1-151">**Ajax long polling**.</span></span> <span data-ttu-id="160b1-152">Dlouhé dotazování nevytvoří trvalé připojení, ale místo toho dotazuje server s požadavek, který zůstane otevřená, dokud server odpoví, okamžiku zavře připojení a okamžitě vyžádání nového připojení.</span><span class="sxs-lookup"><span data-stu-id="160b1-152">Long polling does not create a persistent connection, but instead polls the server with a request that stays open until the server responds, at which point the connection closes, and a new connection is requested immediately.</span></span> <span data-ttu-id="160b1-153">To může způsobit určité zpoždění při resetuje připojení.</span><span class="sxs-lookup"><span data-stu-id="160b1-153">This may introduce some latency while the connection resets.</span></span>

<span data-ttu-id="160b1-154">Další informace o jaké přenosy jsou podporovány v rámci kterých konfiguracích najdete v tématu [podporované platformy](supported-platforms.md).</span><span class="sxs-lookup"><span data-stu-id="160b1-154">For more information on what transports are supported under which configurations, see [Supported Platforms](supported-platforms.md).</span></span>

### <a name="transport-selection-process"></a><span data-ttu-id="160b1-155">Výběr proces přenosu</span><span class="sxs-lookup"><span data-stu-id="160b1-155">Transport selection process</span></span>

<span data-ttu-id="160b1-156">Následující seznam obsahuje kroky, které používá SignalR rozhodnout, který typ přenosu.</span><span class="sxs-lookup"><span data-stu-id="160b1-156">The following list shows the steps that SignalR uses to decide which transport to use.</span></span>

1. <span data-ttu-id="160b1-157">Pokud v prohlížeči Internet Explorer 8 nebo dřívější verzí, dlouhé dotazování se používá.</span><span class="sxs-lookup"><span data-stu-id="160b1-157">If the browser is Internet Explorer 8 or earlier, Long Polling is used.</span></span>
2. <span data-ttu-id="160b1-158">Pokud je nakonfigurovaný JSONP (tedy `jsonp` parametr je nastaven na `true` při spuštění připojení), dlouhé dotazování se používá.</span><span class="sxs-lookup"><span data-stu-id="160b1-158">If JSONP is configured (that is, the `jsonp` parameter is set to `true` when the connection is started), Long Polling is used.</span></span>
3. <span data-ttu-id="160b1-159">Pokud mezi doménami právě připojení (Pokud SignalR koncový bod není ve stejné doméně jako hostování stránka), bude WebSocket použit, pokud se splní následující kritéria:</span><span class="sxs-lookup"><span data-stu-id="160b1-159">If a cross-domain connection is being made (that is, if the SignalR endpoint is not in the same domain as the hosting page), then WebSocket will be used if the following criteria are met:</span></span>

    - <span data-ttu-id="160b1-160">Klient podporuje CORS (sdílení prostředků různého původu).</span><span class="sxs-lookup"><span data-stu-id="160b1-160">The client supports CORS (Cross-Origin Resource Sharing).</span></span> <span data-ttu-id="160b1-161">Informace, na kterých klienti podporují CORS najdete v tématu [CORS v caniuse.com](http://www.caniuse.com/CORS).</span><span class="sxs-lookup"><span data-stu-id="160b1-161">For details on which clients support CORS, see [CORS at caniuse.com](http://www.caniuse.com/CORS).</span></span>
    - <span data-ttu-id="160b1-162">Klient podporuje protokol WebSocket</span><span class="sxs-lookup"><span data-stu-id="160b1-162">The client supports WebSocket</span></span>
    - <span data-ttu-id="160b1-163">Server podporuje protokol WebSocket</span><span class="sxs-lookup"><span data-stu-id="160b1-163">The server supports WebSocket</span></span>

    <span data-ttu-id="160b1-164">Pokud nejsou splněny některé z těchto kritérií, dlouhé dotazování se použije.</span><span class="sxs-lookup"><span data-stu-id="160b1-164">If any of these criteria are not met, Long Polling will be used.</span></span> <span data-ttu-id="160b1-165">Další informace o připojení mezi doménami, najdete v části [postup připojení mezi doménami](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span><span class="sxs-lookup"><span data-stu-id="160b1-165">For more information on cross-domain connections, see [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span></span>
4. <span data-ttu-id="160b1-166">Pokud není nakonfigurovaný JSONP a připojení není mezi doménami, WebSocket se použijí, pokud klient i server podporovat.</span><span class="sxs-lookup"><span data-stu-id="160b1-166">If JSONP is not configured and the connection is not cross-domain, WebSocket will be used if both the client and server support it.</span></span>
5. <span data-ttu-id="160b1-167">Pokud klient nebo server nepodporují protokolu WebSocket, odeslané události serveru se používá, pokud je k dispozici.</span><span class="sxs-lookup"><span data-stu-id="160b1-167">If either the client or server do not support WebSocket, Server Sent Events is used if it is available.</span></span>
6. <span data-ttu-id="160b1-168">Pokud události odeslané serveru není k dispozici, dojde k pokusu o navždy rámce.</span><span class="sxs-lookup"><span data-stu-id="160b1-168">If Server Sent Events is not available, Forever Frame is attempted.</span></span>
7. <span data-ttu-id="160b1-169">Dlouhé dotazování se používá v případě selhání navždy rámce.</span><span class="sxs-lookup"><span data-stu-id="160b1-169">If Forever Frame fails, Long Polling is used.</span></span>

<a id="MonitoringTransports"></a>
### <a name="monitoring-transports"></a><span data-ttu-id="160b1-170">Monitorování přenosů</span><span class="sxs-lookup"><span data-stu-id="160b1-170">Monitoring transports</span></span>

<span data-ttu-id="160b1-171">Můžete určit, jaký přenos vaše aplikace používá povolením protokolování na rozbočovače a otevřete v okně konzoly v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="160b1-171">You can determine what transport your application is using by enabling logging on your hub, and opening the console window in your browser.</span></span>

<span data-ttu-id="160b1-172">Povolení protokolování pro vaše Centrum událostí v prohlížeči, přidejte do klientské aplikace následující příkaz:</span><span class="sxs-lookup"><span data-stu-id="160b1-172">To enable logging for your hub's events in a browser, add the following command to your client application:</span></span>

`$.connection.hub.logging = true;`

- <span data-ttu-id="160b1-173">V aplikaci Internet Explorer otevřete stisknutím klávesy F12 nástrojů pro vývojáře a klikněte na kartu konzoly.</span><span class="sxs-lookup"><span data-stu-id="160b1-173">In Internet Explorer, open the developer tools by pressing F12, and click the Console tab.</span></span>

    ![V Microsoft Internet Explorer](introduction-to-signalr/_static/image2.png)
- <span data-ttu-id="160b1-175">V prohlížeči Chrome otevřete stisknutím kláves Ctrl + Shift + J konzolu.</span><span class="sxs-lookup"><span data-stu-id="160b1-175">In Chrome, open the console by pressing Ctrl+Shift+J.</span></span>

    ![Konzoly Google Chrome](introduction-to-signalr/_static/image3.png)

<span data-ttu-id="160b1-177">Otevřete konzolu a protokolování budete moci zobrazit, které přenosu je stále používán SignalR.</span><span class="sxs-lookup"><span data-stu-id="160b1-177">With the console open and logging enabled, you'll be able to see which transport is being used by SignalR.</span></span>

![Konzole v Internet Exploreru zobrazující přenos protokolu WebSocket](introduction-to-signalr/_static/image4.png)

### <a name="specifying-a-transport"></a><span data-ttu-id="160b1-179">Určení přenos</span><span class="sxs-lookup"><span data-stu-id="160b1-179">Specifying a transport</span></span>

<span data-ttu-id="160b1-180">Vyjednávání přenos trvá prostředky určité množství času a klient server.</span><span class="sxs-lookup"><span data-stu-id="160b1-180">Negotiating a transport takes a certain amount of time and client/server resources.</span></span> <span data-ttu-id="160b1-181">Pokud se ví, že možnosti klienta, může přenos zadán, při spuštění připojení klienta.</span><span class="sxs-lookup"><span data-stu-id="160b1-181">If the client capabilities are known, then a transport can be specified when the client connection is started.</span></span> <span data-ttu-id="160b1-182">Následující fragment kódu ukazuje spuštění pomocí přenosu Ajax dlouhé dotazování, jak se použije, pokud byl označuje, že klient nepodporoval žádný jiný protokol pro připojení:</span><span class="sxs-lookup"><span data-stu-id="160b1-182">The following code snippet demonstrates starting a connection using the Ajax Long Polling transport, as would be used if it was known that the client did not support any other protocol:</span></span>

`connection.start({ transport: 'longPolling' });`

<span data-ttu-id="160b1-183">Pokud chcete, aby klient pokusit určité přenosy v pořadí, můžete zadat záložní pořadí.</span><span class="sxs-lookup"><span data-stu-id="160b1-183">You can specify a fallback order if you want a client to try specific transports in order.</span></span> <span data-ttu-id="160b1-184">Následující fragment kódu ukazuje snažíme WebSocket a pokud v něm, přejdete přímo na dlouhé dotazování.</span><span class="sxs-lookup"><span data-stu-id="160b1-184">The following code snippet demonstrates trying WebSocket, and failing that, going directly to Long Polling.</span></span>

`connection.start({ transport: ['webSockets','longPolling'] });`

<span data-ttu-id="160b1-185">Řetězcové konstanty pro zadání přenosy jsou definovány takto:</span><span class="sxs-lookup"><span data-stu-id="160b1-185">The string constants for specifying transports are defined as follows:</span></span>

- `webSockets`
- `foreverFrame`
- `serverSentEvents`
- `longPolling`

## <a name="connections-and-hubs"></a><span data-ttu-id="160b1-186">Připojeními a rozbočovači</span><span class="sxs-lookup"><span data-stu-id="160b1-186">Connections and Hubs</span></span>

<span data-ttu-id="160b1-187">Rozhraní API SignalR obsahuje dva modely pro komunikaci mezi klienty a servery: trvalé připojeními a rozbočovači.</span><span class="sxs-lookup"><span data-stu-id="160b1-187">The SignalR API contains two models for communicating between clients and servers: Persistent Connections and Hubs.</span></span>

<span data-ttu-id="160b1-188">Připojení představuje jednoduchý koncový bod pro odesílání zpráv jednoho příjemce, seskupené nebo všesměrového vysílání.</span><span class="sxs-lookup"><span data-stu-id="160b1-188">A Connection represents a simple endpoint for sending single-recipient, grouped, or broadcast messages.</span></span> <span data-ttu-id="160b1-189">Trvalé připojení API (v třídě připojení PersistentConnection znázorněná kód .NET) poskytuje, vývojář přímý přístup k nízké úrovně komunikační protokol, který zveřejňuje funkce SignalR.</span><span class="sxs-lookup"><span data-stu-id="160b1-189">The Persistent Connection API (represented in .NET code by the PersistentConnection class) gives the developer direct access to the low-level communication protocol that SignalR exposes.</span></span> <span data-ttu-id="160b1-190">Pomocí připojení komunikační model bude pro vývojáře, kteří použili založeného na připojení rozhraní API jako je Windows Communication Foundation.</span><span class="sxs-lookup"><span data-stu-id="160b1-190">Using the Connections communication model will be familiar to developers who have used connection-based APIs such as Windows Communication Foundation.</span></span>

<span data-ttu-id="160b1-191">Rozbočovač je založena na rozhraní API připojení, která umožňuje klientovi i serveru volat metody na sobě navzájem přímo více vysoké úrovně kanálu.</span><span class="sxs-lookup"><span data-stu-id="160b1-191">A Hub is a more high-level pipeline built upon the Connection API that allows your client and server to call methods on each other directly.</span></span> <span data-ttu-id="160b1-192">SignalR zpracovává odeslání mezi různými počítači jako podle magic, které klientům umožňuje volat metody na serveru jako snadno jako místní metody a naopak.</span><span class="sxs-lookup"><span data-stu-id="160b1-192">SignalR handles the dispatching across machine boundaries as if by magic, allowing clients to call methods on the server as easily as local methods, and vice versa.</span></span> <span data-ttu-id="160b1-193">Pomocí centra komunikační model bude pro vývojáře, kteří použili vzdáleného volání rozhraní API, jako je například .NET Remoting.</span><span class="sxs-lookup"><span data-stu-id="160b1-193">Using the Hubs communication model will be familiar to developers who have used remote invocation APIs such as .NET Remoting.</span></span> <span data-ttu-id="160b1-194">Použití rozbočovače také umožňuje předat silného typu parametry metody, povolení vazby modelu.</span><span class="sxs-lookup"><span data-stu-id="160b1-194">Using a Hub also allows you to pass strongly typed parameters to methods, enabling model binding.</span></span>

### <a name="architecture-diagram"></a><span data-ttu-id="160b1-195">diagram architektury</span><span class="sxs-lookup"><span data-stu-id="160b1-195">Architecture diagram</span></span>

<span data-ttu-id="160b1-196">Následující diagram znázorňuje vztah mezi rozbočovačů, trvalé připojení a základní technologií používaných pro přenosy.</span><span class="sxs-lookup"><span data-stu-id="160b1-196">The following diagram shows the relationship between Hubs, Persistent Connections, and the underlying technologies used for transports.</span></span>

![Diagram architektury SignalR znázorňující rozhraní API, přenosy a klienty](introduction-to-signalr/_static/image5.png)

### <a name="how-hubs-work"></a><span data-ttu-id="160b1-198">Jak fungují rozbočovače</span><span class="sxs-lookup"><span data-stu-id="160b1-198">How Hubs work</span></span>

<span data-ttu-id="160b1-199">Když kódu na straně serveru volá metody na straně klienta, paket se odesílají přes aktivní přenos, který obsahuje název a parametry metody, která se má volat (když je objekt jako parametru metody, je serializován pomocí JSON).</span><span class="sxs-lookup"><span data-stu-id="160b1-199">When server-side code calls a method on the client, a packet is sent across the active transport that contains the name and parameters of the method to be called (when an object is sent as a method parameter, it is serialized using JSON).</span></span> <span data-ttu-id="160b1-200">Klient pak odpovídá názvu metoda pro metody definované v kódu na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="160b1-200">The client then matches the method name to methods defined in client-side code.</span></span> <span data-ttu-id="160b1-201">Pokud je nalezen, metoda klienta provést pomocí dat deserializovat parametru.</span><span class="sxs-lookup"><span data-stu-id="160b1-201">If there is a match, the client method will be executed using the deserialized parameter data.</span></span>

<span data-ttu-id="160b1-202">Volání metody, které je možné monitorovat pomocí nástroje, například [aplikaci Fiddler.](http://fiddler2.com/)</span><span class="sxs-lookup"><span data-stu-id="160b1-202">The method call can be monitored using tools like [Fiddler.](http://fiddler2.com/)</span></span> <span data-ttu-id="160b1-203">Následující obrázek ukazuje volání metody odeslaných ze serveru SignalR do webového prohlížeče klienta v podokně protokoly aplikaci Fiddler.</span><span class="sxs-lookup"><span data-stu-id="160b1-203">The following image shows a method call sent from a SignalR server to a web browser client in the Logs pane of Fiddler.</span></span> <span data-ttu-id="160b1-204">Volání metody, které je odeslané z rozbočovače názvem `MoveShapeHub`, a ke kterému volaná metoda je volána `updateShape`.</span><span class="sxs-lookup"><span data-stu-id="160b1-204">The method call is being sent from a hub called `MoveShapeHub`, and the method being invoked is called `updateShape`.</span></span>

![Zobrazení protokolu Fiddler zobrazující provoz SignalR](introduction-to-signalr/_static/image6.png)

<span data-ttu-id="160b1-206">V tomto příkladu je název centra označeny `H` parametr; metodu přiřazen název `M` parametr a data odesílána metodu je identifikován s `A` parametr.</span><span class="sxs-lookup"><span data-stu-id="160b1-206">In this example, the hub name is identified with the `H` parameter; the method name is identified with the `M` parameter, and the data being sent to the method is identified with the `A` parameter.</span></span> <span data-ttu-id="160b1-207">Vytvoření aplikace, která vygenerovala tuto zprávu v [v reálném čase vysoká frekvence](tutorial-high-frequency-realtime-with-signalr.md) kurzu.</span><span class="sxs-lookup"><span data-stu-id="160b1-207">The application that generated this message is created in the [High-Frequency Realtime](tutorial-high-frequency-realtime-with-signalr.md) tutorial.</span></span>

### <a name="choosing-a-communication-model"></a><span data-ttu-id="160b1-208">Výběr modelu komunikace</span><span class="sxs-lookup"><span data-stu-id="160b1-208">Choosing a communication model</span></span>

<span data-ttu-id="160b1-209">Většina aplikací by měl použít rozhraní API rozbočovače.</span><span class="sxs-lookup"><span data-stu-id="160b1-209">Most applications should use the Hubs API.</span></span> <span data-ttu-id="160b1-210">Rozhraní API připojení může v následujících případech:</span><span class="sxs-lookup"><span data-stu-id="160b1-210">The Connections API could be used in the following circumstances:</span></span>

- <span data-ttu-id="160b1-211">Formát je nutné zadat skutečné zpráva byla odeslána.</span><span class="sxs-lookup"><span data-stu-id="160b1-211">The format of the actual message sent needs to be specified.</span></span>
- <span data-ttu-id="160b1-212">Vývojář upřednostní pro práci s modelem zasílání zpráv a odesílající spíše než model vzdálené volání.</span><span class="sxs-lookup"><span data-stu-id="160b1-212">The developer prefers to work with a messaging and dispatching model rather than a remote invocation model.</span></span>
- <span data-ttu-id="160b1-213">Existující aplikace, která používá model zasílání zpráv je právě přesně do použít SignalR.</span><span class="sxs-lookup"><span data-stu-id="160b1-213">An existing application that uses a messaging model is being ported to use SignalR.</span></span>
