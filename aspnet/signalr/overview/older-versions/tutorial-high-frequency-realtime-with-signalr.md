---
uid: signalr/overview/older-versions/tutorial-high-frequency-realtime-with-signalr
title: Vysokofrekvenční Reálný čas s knihovnou SignalR 1.x | Dokumentace Microsoftu
author: pfletcher
description: Tento kurz ukazuje, jak vytvořit webovou aplikaci, která používá knihovnu ASP.NET SignalR k zajištění funkce zasílání zpráv vysokou frekvencí. Vysoká frekvence zasílání zpráv v...
ms.author: aspnetcontent
manager: wpickett
ms.date: 04/16/2013
ms.topic: article
ms.assetid: ad2a5da5-2e79-40ea-bc84-028d327f5982
ms.technology: dotnet-signalr
msc.legacyurl: /signalr/overview/older-versions/tutorial-high-frequency-realtime-with-signalr
msc.type: authoredcontent
ms.openlocfilehash: d9a5539d79d0b6396282af8036f2c6417f44ce23
ms.sourcegitcommit: 953ff9ea4369f154d6fd0239599279ddd3280009
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/03/2018
ms.locfileid: "37401131"
---
<a name="high-frequency-realtime-with-signalr-1x"></a><span data-ttu-id="c28a6-104">Vysokofrekvenční Reálný čas s knihovnou SignalR 1.x</span><span class="sxs-lookup"><span data-stu-id="c28a6-104">High-Frequency Realtime with SignalR 1.x</span></span>
====================
<span data-ttu-id="c28a6-105">podle [Patrick Fletcher](https://github.com/pfletcher)</span><span class="sxs-lookup"><span data-stu-id="c28a6-105">by [Patrick Fletcher](https://github.com/pfletcher)</span></span>

> <span data-ttu-id="c28a6-106">Tento kurz ukazuje, jak vytvořit webovou aplikaci, která používá knihovnu ASP.NET SignalR k zajištění funkce zasílání zpráv vysokou frekvencí.</span><span class="sxs-lookup"><span data-stu-id="c28a6-106">This tutorial shows how to create a web application that uses ASP.NET SignalR to provide high-frequency messaging functionality.</span></span> <span data-ttu-id="c28a6-107">Vysoká frekvence zasílání zpráv v tomto případě znamená, že aktualizace, které se odesílají s pevnou sazbou; v případě této aplikace, až 10 zpráv za sekundu.</span><span class="sxs-lookup"><span data-stu-id="c28a6-107">High-frequency messaging in this case means updates that are sent at a fixed rate; in the case of this application, up to 10 messages a second.</span></span>
> 
> <span data-ttu-id="c28a6-108">Aplikace, kterou vytvoříte v tomto kurzu zobrazí obrazec, který můžete přetáhnout uživatelů.</span><span class="sxs-lookup"><span data-stu-id="c28a6-108">The application you'll create in this tutorial displays a shape that users can drag.</span></span> <span data-ttu-id="c28a6-109">Umístění obrazce ve všech propojených prohlížečů pak aktualizuje tak, aby odpovídala pozici Přetahované tvaru použitím vypršel časový limit aktualizace.</span><span class="sxs-lookup"><span data-stu-id="c28a6-109">The position of the shape in all other connected browsers will then be updated to match the position of the dragged shape using timed updates.</span></span>
> 
> <span data-ttu-id="c28a6-110">Koncepty představenými v tomto kurzu jste aplikací v reálném čase hry a další aplikace simulace.</span><span class="sxs-lookup"><span data-stu-id="c28a6-110">Concepts introduced in this tutorial have applications in real-time gaming and other simulation applications.</span></span>
> 
> <span data-ttu-id="c28a6-111">Si mohou komentáře u tohoto kurzu.</span><span class="sxs-lookup"><span data-stu-id="c28a6-111">Comments on the tutorial are welcome.</span></span> <span data-ttu-id="c28a6-112">Pokud máte nějaké otázky, které přímo nesouvisejí, najdete v tomto kurzu, můžete je publikovat [fórum ASP.NET SignalR](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) nebo [StackOverflow.com](http://stackoverflow.com).</span><span class="sxs-lookup"><span data-stu-id="c28a6-112">If you have questions that are not directly related to the tutorial, you can post them to the [ASP.NET SignalR forum](https://forums.asp.net/1254.aspx/1?ASP+NET+SignalR) or [StackOverflow.com](http://stackoverflow.com).</span></span>


## <a name="overview"></a><span data-ttu-id="c28a6-113">Přehled</span><span class="sxs-lookup"><span data-stu-id="c28a6-113">Overview</span></span>

<span data-ttu-id="c28a6-114">Tento kurz ukazuje, jak vytvořit aplikaci, která sdílí stav objektu se ostatní prohlížeče v reálném čase.</span><span class="sxs-lookup"><span data-stu-id="c28a6-114">This tutorial demonstrates how to create an application that shares the state of an object with other browsers in real time.</span></span> <span data-ttu-id="c28a6-115">Aplikace, kterou vytvoříme, se nazývá MoveShape.</span><span class="sxs-lookup"><span data-stu-id="c28a6-115">The application we'll create is called MoveShape.</span></span> <span data-ttu-id="c28a6-116">Na stránce MoveShape se zobrazí element HTML Div, který uživatel můžete přetáhnout; Když uživatel přetáhne divu, pošle se na server, který vám potom sdělí, všechny ostatní připojené klienty se aktualizovat umístění obrazce tak, aby odpovídaly jeho nové pozice.</span><span class="sxs-lookup"><span data-stu-id="c28a6-116">The MoveShape page will display an HTML Div element that the user can drag; when the user drags the Div, its new position will be sent to the server, which will then tell all other connected clients to update the shape's position to match.</span></span>

![Okno aplikace](tutorial-high-frequency-realtime-with-signalr/_static/image1.png)

<span data-ttu-id="c28a6-118">Aplikace vytvořené v tomto kurzu je založena na ukázku Damianem Edwardsem.</span><span class="sxs-lookup"><span data-stu-id="c28a6-118">The application created in this tutorial is based on a demo by Damian Edwards.</span></span> <span data-ttu-id="c28a6-119">Můžou zobrazit videa obsahující tuto ukázku [tady](https://channel9.msdn.com/Series/Building-Web-Apps-with-ASP-NET-Jump-Start/Building-Web-Apps-with-ASPNET-Jump-Start-08-Real-time-Communication-with-SignalR).</span><span class="sxs-lookup"><span data-stu-id="c28a6-119">A video containing this demo can be seen [here](https://channel9.msdn.com/Series/Building-Web-Apps-with-ASP-NET-Jump-Start/Building-Web-Apps-with-ASPNET-Jump-Start-08-Real-time-Communication-with-SignalR).</span></span>

<span data-ttu-id="c28a6-120">Tento kurz se spustí prokazováním zasílání zpráv SignalR z každé události, který se aktivuje přetáhnout na obrazec.</span><span class="sxs-lookup"><span data-stu-id="c28a6-120">The tutorial will start by demonstrating how to send SignalR messages from each event that fires as the shape is dragged.</span></span> <span data-ttu-id="c28a6-121">Každý připojený klient aktualizuje umístění v místní verzi tvar pak pokaždé, když je přijata zpráva.</span><span class="sxs-lookup"><span data-stu-id="c28a6-121">Each connected client will then update the position of the local version of the shape each time a message is received.</span></span>

<span data-ttu-id="c28a6-122">I aplikace bude fungovat s touto metodou, není doporučenou programovací model, protože by existovat žádné horní omezení počtu zpráv, získávání odeslat, aby klienti a server může získat zahlcen zprávy a by dojít ke snížení výkonu .</span><span class="sxs-lookup"><span data-stu-id="c28a6-122">While the application will function using this method, this is not a recommended programming model, since there would be no upper limit to the number of messages getting sent, so the clients and server could get overwhelmed with messages and performance would degrade.</span></span> <span data-ttu-id="c28a6-123">Zobrazené animací na straně klienta by také odděleném jako tvar by okamžitě přesunout jednotlivé metody spíše než přesouvání plynule do každého nového umístění.</span><span class="sxs-lookup"><span data-stu-id="c28a6-123">The displayed animation on the client would also be disjointed, as the shape would be moved instantly by each method, rather than moving smoothly to each new location.</span></span> <span data-ttu-id="c28a6-124">V dalších částech tohoto kurzu vám ukáže, jak vytvořit funkci časovače, který omezí maximální rychlost přenosu, ve kterém jsou zprávy odesílány klienta nebo serveru a postup přesunutí tvar plynule mezi umístěními.</span><span class="sxs-lookup"><span data-stu-id="c28a6-124">Later sections of the tutorial will demonstrate how to create a timer function that restricts the maximum rate at which messages are sent by either the client or server, and how to move the shape smoothly between locations.</span></span> <span data-ttu-id="c28a6-125">Konečná verze aplikace vytvořené v tomto kurzu si můžete stáhnout z [Galerie kódu na](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span><span class="sxs-lookup"><span data-stu-id="c28a6-125">The final version of the application created in this tutorial can be downloaded from [Code Gallery](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span></span>

<span data-ttu-id="c28a6-126">Tento kurz obsahuje následující části:</span><span class="sxs-lookup"><span data-stu-id="c28a6-126">This tutorial contains the following sections:</span></span>

- [<span data-ttu-id="c28a6-127">Požadované součásti</span><span class="sxs-lookup"><span data-stu-id="c28a6-127">Prerequisites</span></span>](#prerequisites)
- [<span data-ttu-id="c28a6-128">Vytvoření projektu</span><span class="sxs-lookup"><span data-stu-id="c28a6-128">Create the project</span></span>](#createtheproject)
- [<span data-ttu-id="c28a6-129">Přidání balíčků funkce SignalR technologie ASP.NET a JQuery.UI NuGet</span><span class="sxs-lookup"><span data-stu-id="c28a6-129">Add the ASP.NET SignalR and JQuery.UI NuGet packages</span></span>](#nugetpackages)
- [<span data-ttu-id="c28a6-130">Vytvoření základní aplikace</span><span class="sxs-lookup"><span data-stu-id="c28a6-130">Create the base application</span></span>](#baseapp)
- [<span data-ttu-id="c28a6-131">Přidat cyklus klienta</span><span class="sxs-lookup"><span data-stu-id="c28a6-131">Add the client loop</span></span>](#clientloop)
- [<span data-ttu-id="c28a6-132">Přidat cyklus serveru</span><span class="sxs-lookup"><span data-stu-id="c28a6-132">Add the server loop</span></span>](#serverloop)
- [<span data-ttu-id="c28a6-133">Přidat plynulou animaci na straně klienta</span><span class="sxs-lookup"><span data-stu-id="c28a6-133">Add smooth animation on the client</span></span>](#animation)
- [<span data-ttu-id="c28a6-134">Další kroky</span><span class="sxs-lookup"><span data-stu-id="c28a6-134">Further Steps</span></span>](#furthersteps)

<a id="prerequisites"></a>

## <a name="prerequisites"></a><span data-ttu-id="c28a6-135">Požadavky</span><span class="sxs-lookup"><span data-stu-id="c28a6-135">Prerequisites</span></span>

<span data-ttu-id="c28a6-136">Tento kurz vyžaduje Visual Studio 2012 nebo Visual Studio 2010.</span><span class="sxs-lookup"><span data-stu-id="c28a6-136">This tutorial requires Visual Studio 2012 or Visual Studio 2010.</span></span> <span data-ttu-id="c28a6-137">Pokud se používá Visual Studio 2010, že projekt bude používat rozhraní .NET Framework 4 namísto rozhraní .NET Framework 4.5.</span><span class="sxs-lookup"><span data-stu-id="c28a6-137">If Visual Studio 2010 is used, the project will use .NET Framework 4 rather than .NET Framework 4.5.</span></span>

<span data-ttu-id="c28a6-138">Pokud používáte sadu Visual Studio 2012, doporučuje se, že nainstalujete [ASP.NET and Web Tools 2012.2 aktualizace](https://go.microsoft.com/fwlink/?LinkId=282650).</span><span class="sxs-lookup"><span data-stu-id="c28a6-138">If you are using Visual Studio 2012, it's recommended that you install the [ASP.NET and Web Tools 2012.2 update](https://go.microsoft.com/fwlink/?LinkId=282650).</span></span> <span data-ttu-id="c28a6-139">Tato aktualizace obsahuje nové funkce, jako je vylepšení publikování nové funkce a nové šablony.</span><span class="sxs-lookup"><span data-stu-id="c28a6-139">This update contains new features such as enhancements to publishing, new functionality, and new templates.</span></span>

<span data-ttu-id="c28a6-140">Pokud máte Visual Studio 2010, ujistěte se, že [NuGet](https://visualstudiogallery.msdn.microsoft.com/27077b70-9dad-4c64-adcf-c7cf6bc9970c) je nainstalována.</span><span class="sxs-lookup"><span data-stu-id="c28a6-140">If you have Visual Studio 2010, make sure that [NuGet](https://visualstudiogallery.msdn.microsoft.com/27077b70-9dad-4c64-adcf-c7cf6bc9970c) is installed.</span></span>

<a id="createtheproject"></a>

## <a name="create-the-project"></a><span data-ttu-id="c28a6-141">Vytvoření projektu</span><span class="sxs-lookup"><span data-stu-id="c28a6-141">Create the project</span></span>

<span data-ttu-id="c28a6-142">V této části vytvoříme projektu v sadě Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="c28a6-142">In this section, we'll create the project in Visual Studio.</span></span>

1. <span data-ttu-id="c28a6-143">Z **souboru** klikněte na nabídku **nový projekt**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-143">From the **File** menu click **New Project**.</span></span>
2. <span data-ttu-id="c28a6-144">V **nový projekt** dialogového okna rozbalte **jazyka C#** pod **šablony** a vyberte **webové**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-144">In the **New Project** dialog box, expand **C#** under **Templates** and select **Web**.</span></span>
3. <span data-ttu-id="c28a6-145">Vyberte **prázdná webová aplikace ASP.NET** šablony, pojmenujte projekt *MoveShapeDemo*a klikněte na tlačítko **OK**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-145">Select the **ASP.NET Empty Web Application** template, name the project *MoveShapeDemo*, and click **OK**.</span></span>

    ![Vytvoření nového projektu](tutorial-high-frequency-realtime-with-signalr/_static/image2.png)

<a id="nugetpackages"></a>

## <a name="add-the-signalr-and-jqueryui-nuget-packages"></a><span data-ttu-id="c28a6-147">Přidat funkci SignalR a balíčky JQuery.UI NuGet</span><span class="sxs-lookup"><span data-stu-id="c28a6-147">Add the SignalR and JQuery.UI NuGet Packages</span></span>

<span data-ttu-id="c28a6-148">Funkce SignalR můžete přidat do projektu po instalaci balíčku NuGet.</span><span class="sxs-lookup"><span data-stu-id="c28a6-148">You can add SignalR functionality to a project by installing a NuGet package.</span></span> <span data-ttu-id="c28a6-149">V tomto kurzu se také použít balíček JQuery.UI umožňující tvar, který má být kvůli usnadnění použití vypsány a animovat.</span><span class="sxs-lookup"><span data-stu-id="c28a6-149">This tutorial will also use the JQuery.UI package for allowing the shape to be dragged and animated.</span></span>

1. <span data-ttu-id="c28a6-150">Klikněte na tlačítko **nástroje | Správce balíčků knihoven | Konzola správce balíčků**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-150">Click **Tools | Library Package Manager | Package Manager Console**.</span></span>
2. <span data-ttu-id="c28a6-151">Správce balíčků zadejte následující příkaz.</span><span class="sxs-lookup"><span data-stu-id="c28a6-151">Enter the following command in the package manager.</span></span>

    [!code-powershell[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample1.ps1)]

    <span data-ttu-id="c28a6-152">Funkce SignalR balíček nainstaluje celou řadou dalších balíčcích NuGet jako závislosti.</span><span class="sxs-lookup"><span data-stu-id="c28a6-152">The SignalR package installs a number of other NuGet packages as dependencies.</span></span> <span data-ttu-id="c28a6-153">Po dokončení instalace budete mít vše serveru a klientské komponenty potřebné k použití v aplikaci ASP.NET SignalR.</span><span class="sxs-lookup"><span data-stu-id="c28a6-153">When the installation is finished you have all of the server and client components required to use SignalR in an ASP.NET application.</span></span>
3. <span data-ttu-id="c28a6-154">Zadáním následujícího příkazu do konzoly Správce balíčků knihovny JQuery a JQuery.UI balíčky nainstalovat.</span><span class="sxs-lookup"><span data-stu-id="c28a6-154">Enter the following command into the package manager console to install the JQuery and JQuery.UI packages.</span></span>

    [!code-powershell[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample2.ps1)]

<a id="baseapp"></a>

## <a name="create-the-base-application"></a><span data-ttu-id="c28a6-155">Vytvoření základní aplikace</span><span class="sxs-lookup"><span data-stu-id="c28a6-155">Create the base application</span></span>

<span data-ttu-id="c28a6-156">V této části vytvoříme aplikaci prohlížeče, která odešle umístění obrazce na server během každou událost pohybu myší.</span><span class="sxs-lookup"><span data-stu-id="c28a6-156">In this section, we'll create a browser application that sends the location of the shape to the server during each mouse move event.</span></span> <span data-ttu-id="c28a6-157">Po přijetí, server pak vyšle tyto informace do všech ostatních připojených klientů.</span><span class="sxs-lookup"><span data-stu-id="c28a6-157">The server then broadcasts this information to all other connected clients as it is received.</span></span> <span data-ttu-id="c28a6-158">Jsme budete rozbalit na tuto aplikaci v předchozích částech.</span><span class="sxs-lookup"><span data-stu-id="c28a6-158">We'll expand on this application in later sections.</span></span>

1. <span data-ttu-id="c28a6-159">V **Průzkumníka řešení**, klikněte pravým tlačítkem na projekt a vyberte **přidat**, **třídy...** . Název třídy **MoveShapeHub** a klikněte na tlačítko **přidat**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-159">In **Solution Explorer**, right-click on the project and select **Add**, **Class...**. Name the class **MoveShapeHub** and click **Add**.</span></span>
2. <span data-ttu-id="c28a6-160">Nahraďte kód v novém **MoveShapeHub** třídy následujícím kódem.</span><span class="sxs-lookup"><span data-stu-id="c28a6-160">Replace the code in the new **MoveShapeHub** class with the following code.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample3.cs)]

    <span data-ttu-id="c28a6-161">`MoveShapeHub` Třídy výše je implementace rozbočovače SignalR.</span><span class="sxs-lookup"><span data-stu-id="c28a6-161">The `MoveShapeHub` class above is an implementation of a SignalR hub.</span></span> <span data-ttu-id="c28a6-162">Stejně jako [Začínáme s knihovnou SignalR](index.md) kurzu centra má metodu, která klientům se volat přímo.</span><span class="sxs-lookup"><span data-stu-id="c28a6-162">As in the [Getting Started with SignalR](index.md) tutorial, the hub has a method that the clients will call directly.</span></span> <span data-ttu-id="c28a6-163">V tomto případě klient odešle objekt, který obsahuje novou souřadnice X a Y obrazce na server, který pak získá vysílali pro všechny ostatní připojených klientů.</span><span class="sxs-lookup"><span data-stu-id="c28a6-163">In this case, the client will send an object containing the new X and Y coordinates of the shape to the server, which then gets broadcasted to all other connected clients.</span></span> <span data-ttu-id="c28a6-164">Funkce SignalR bude automaticky serializovat tento objekt pomocí formátu JSON.</span><span class="sxs-lookup"><span data-stu-id="c28a6-164">SignalR will automatically serialize this object using JSON.</span></span>

    <span data-ttu-id="c28a6-165">Objekt, který se pošle na klientovi (`ShapeModel`) obsahuje členy k uložení umístění obrazce.</span><span class="sxs-lookup"><span data-stu-id="c28a6-165">The object that will be sent to the client (`ShapeModel`) contains members to store the position of the shape.</span></span> <span data-ttu-id="c28a6-166">Verze objektu na serveru také obsahuje členy, aby mohli sledovat data klientů, které ukládají, tak, aby daného klienta se neodešlou svá vlastní data.</span><span class="sxs-lookup"><span data-stu-id="c28a6-166">The version of the object on the server also contains a member to track which client's data is being stored, so that a given client won't be sent their own data.</span></span> <span data-ttu-id="c28a6-167">Používá tento člen `JsonIgnore` atribut zabránit serializována a může odeslat klientovi.</span><span class="sxs-lookup"><span data-stu-id="c28a6-167">This member uses the `JsonIgnore` attribute to keep it from being serialized and sent to the client.</span></span>
3. <span data-ttu-id="c28a6-168">V dalším kroku nastavíme centra při spuštění aplikace.</span><span class="sxs-lookup"><span data-stu-id="c28a6-168">Next, we'll set up the hub when the application starts.</span></span> <span data-ttu-id="c28a6-169">V **Průzkumníka řešení**, klikněte pravým tlačítkem na projekt a pak klikněte na tlačítko **přidat | Globální třída aplikace**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-169">In **Solution Explorer**, right-click the project, then click **Add | Global Application Class**.</span></span> <span data-ttu-id="c28a6-170">Přijměte výchozí název *globální* a klikněte na tlačítko **OK**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-170">Accept the default name of *Global* and click **OK**.</span></span>

    ![Přidání globální třída aplikace](tutorial-high-frequency-realtime-with-signalr/_static/image3.png)
4. <span data-ttu-id="c28a6-172">Přidejte následující `using` příkazem za zadaných **pomocí** příkazy ve třídě Global.asax.cs.</span><span class="sxs-lookup"><span data-stu-id="c28a6-172">Add the following `using` statement after the provided **using** statements in the Global.asax.cs class.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample4.cs)]
5. <span data-ttu-id="c28a6-173">Přidejte následující řádek kódu `Application_Start` metoda globální třídy k registraci výchozí trasu pro funkci SignalR.</span><span class="sxs-lookup"><span data-stu-id="c28a6-173">Add the following line of code in the `Application_Start` method of the Global class to register the default route for SignalR.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample5.cs)]

    <span data-ttu-id="c28a6-174">Váš soubor global.asax by měl vypadat nějak takto:</span><span class="sxs-lookup"><span data-stu-id="c28a6-174">Your global.asax file should look like the following:</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample6.cs)]
6. <span data-ttu-id="c28a6-175">V dalším kroku přidáme klienta.</span><span class="sxs-lookup"><span data-stu-id="c28a6-175">Next, we'll add the client.</span></span> <span data-ttu-id="c28a6-176">V **Průzkumníka řešení**, klikněte pravým tlačítkem na projekt a pak klikněte na tlačítko **přidat | Nová položka**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-176">In **Solution Explorer**, right-click the project, then click **Add | New Item**.</span></span> <span data-ttu-id="c28a6-177">V **přidat novou položku** dialogového okna, vyberte **stránku Html**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-177">In the **Add New Item** dialog, select **Html Page**.</span></span> <span data-ttu-id="c28a6-178">Na stránce zadejte vhodný název (jako je **Default.html**) a klikněte na tlačítko **přidat**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-178">Give the page an appropriate name (like **Default.html**) and click **Add**.</span></span>
7. <span data-ttu-id="c28a6-179">V **Průzkumníka řešení**, klikněte pravým tlačítkem na stránku, který jste právě vytvořili a klikněte na tlačítko **nastavit jako úvodní stránku**.</span><span class="sxs-lookup"><span data-stu-id="c28a6-179">In **Solution Explorer**, right-click the page you just created and click **Set as Start Page**.</span></span>
8. <span data-ttu-id="c28a6-180">Nahraďte kód výchozí stránku HTML následující fragment kódu.</span><span class="sxs-lookup"><span data-stu-id="c28a6-180">Replace the default code in the HTML page with the following code snippet.</span></span>

    > [!NOTE]
    > <span data-ttu-id="c28a6-181">Ověřte, že skript odkazuje pod shoda balíčky se přidaly do projektu ve složce Scripts.</span><span class="sxs-lookup"><span data-stu-id="c28a6-181">Verify that the script references below match the packages added to your project in the Scripts folder.</span></span> <span data-ttu-id="c28a6-182">V sadě Visual Studio 2010 nemusí odpovídat verzi knihovny JQuery a SignalR přidány do projektu následující čísla verze.</span><span class="sxs-lookup"><span data-stu-id="c28a6-182">In Visual Studio 2010, the version of JQuery and SignalR added to the project may not match the version numbers below.</span></span>

    [!code-html[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample7.html)]

    <span data-ttu-id="c28a6-183">Výše uvedený kód HTML a JavaScript vytvoří red Div volá tvar, umožňuje přetahování chování tvaru pomocí knihovny jQuery a používá obrazce `drag` události a umístění obrazce odeslat na server.</span><span class="sxs-lookup"><span data-stu-id="c28a6-183">The above HTML and JavaScript code creates a red Div called Shape, enables the shape's dragging behavior using the jQuery library, and uses the shape's `drag` event to send the shape's position to the server.</span></span>
9. <span data-ttu-id="c28a6-184">Stisknutím klávesy F5 spusťte aplikaci.</span><span class="sxs-lookup"><span data-stu-id="c28a6-184">Start the application by pressing F5.</span></span> <span data-ttu-id="c28a6-185">Zkopírujte adresu URL na stránku a vložte ho do druhé okno prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="c28a6-185">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="c28a6-186">Přetáhněte obrazec v jednom z okna prohlížeče; tvar v druhém okně prohlížeče by se měla přesunout.</span><span class="sxs-lookup"><span data-stu-id="c28a6-186">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span>

    ![Okno aplikace](tutorial-high-frequency-realtime-with-signalr/_static/image4.png)

<a id="clientloop"></a>

## <a name="add-the-client-loop"></a><span data-ttu-id="c28a6-188">Přidat cyklus klienta</span><span class="sxs-lookup"><span data-stu-id="c28a6-188">Add the client loop</span></span>

<span data-ttu-id="c28a6-189">Protože odesílání umístění tvar na každou událost pohybu myší vytvořit nepotřebných objemu síťových přenosů, zpráv od klienta je potřeba omezit.</span><span class="sxs-lookup"><span data-stu-id="c28a6-189">Since sending the location of the shape on every mouse move event will create an unneccesary amount of network traffic, the messages from the client need to be throttled.</span></span> <span data-ttu-id="c28a6-190">Použijeme javascript `setInterval` funkce nastavit smyčku, která odešle nové informace o umístění na server s pevnou sazbou.</span><span class="sxs-lookup"><span data-stu-id="c28a6-190">We'll use the javascript `setInterval` function to set up a loop that sends new position information to the server at a fixed rate.</span></span> <span data-ttu-id="c28a6-191">Smyčka je velmi základní reprezentace "herní cyklus" opakovaně volaná funkce, která řídí všechny funkce hru nebo jiných simulace.</span><span class="sxs-lookup"><span data-stu-id="c28a6-191">This loop is a very basic representation of a "game loop", a repeatedly called function that drives all of the functionality of a game or other simulation.</span></span>

1. <span data-ttu-id="c28a6-192">Aktualizace kódu klienta na stránce HTML tak, aby odpovídala následující fragment kódu.</span><span class="sxs-lookup"><span data-stu-id="c28a6-192">Update the client code in the HTML page to match the following code snippet.</span></span>

    [!code-html[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample8.html)]

    <span data-ttu-id="c28a6-193">Výše uvedené aktualizace přidá `updateServerModel` funkce, který je volán na pevnou frekvencí.</span><span class="sxs-lookup"><span data-stu-id="c28a6-193">The above update adds the `updateServerModel` function, which gets called on a fixed frequency.</span></span> <span data-ttu-id="c28a6-194">Tato funkce odesílá umístění dat na server pokaždé, když `moved` příznak určuje, že je nová umístění dat k odeslání.</span><span class="sxs-lookup"><span data-stu-id="c28a6-194">This function sends the position data to the server whenever the `moved` flag indicates that there is new position data to send.</span></span>
2. <span data-ttu-id="c28a6-195">Stisknutím klávesy F5 spusťte aplikaci.</span><span class="sxs-lookup"><span data-stu-id="c28a6-195">Start the application by pressing F5.</span></span> <span data-ttu-id="c28a6-196">Zkopírujte adresu URL na stránku a vložte ho do druhé okno prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="c28a6-196">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="c28a6-197">Přetáhněte obrazec v jednom z okna prohlížeče; tvar v druhém okně prohlížeče by se měla přesunout.</span><span class="sxs-lookup"><span data-stu-id="c28a6-197">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span> <span data-ttu-id="c28a6-198">Protože se omezí počet zpráv, které odesílání na server, se nezobrazí animace pohodlné stejně jako v předchozí části.</span><span class="sxs-lookup"><span data-stu-id="c28a6-198">Since the number of messages that get sent to the server will be throttled, the animation will not appear as smooth as in the previous section.</span></span>

    ![Okno aplikace](tutorial-high-frequency-realtime-with-signalr/_static/image5.png)

<a id="serverloop"></a>

## <a name="add-the-server-loop"></a><span data-ttu-id="c28a6-200">Přidat cyklus serveru</span><span class="sxs-lookup"><span data-stu-id="c28a6-200">Add the server loop</span></span>

<span data-ttu-id="c28a6-201">V aktuální aplikaci zprávy odeslané ze serveru klientovi chodit přímo tak často, jak jejich přijetí.</span><span class="sxs-lookup"><span data-stu-id="c28a6-201">In the current application, messages sent from the server to the client go out as often as they are received.</span></span> <span data-ttu-id="c28a6-202">To představuje podobný problém, jak je vidět na straně klienta; častěji, než jsou potřeba, a připojení může být zahlcenou díky tomu lze odesílat zprávy.</span><span class="sxs-lookup"><span data-stu-id="c28a6-202">This presents a similar problem as was seen on the client; messages can be sent more often than they are needed, and the connection could become flooded as a result.</span></span> <span data-ttu-id="c28a6-203">Tato část popisuje postup aktualizace serveru pro implementaci časovač, který omezuje počet odchozích zpráv.</span><span class="sxs-lookup"><span data-stu-id="c28a6-203">This section describes how to update the server to implement a timer that throttles the rate of the outgoing messages.</span></span>

1. <span data-ttu-id="c28a6-204">Nahraďte obsah `MoveShapeHub.cs` následujícím fragmentem kódu.</span><span class="sxs-lookup"><span data-stu-id="c28a6-204">Replace the contents of `MoveShapeHub.cs` with the following code snippet.</span></span>

    [!code-csharp[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample9.cs)]

    <span data-ttu-id="c28a6-205">Výše uvedený kód rozšiřuje klienta k přidání `Broadcaster` třídu, která omezuje odchozí zprávy pomocí `Timer` třídy z rozhraní .NET framework.</span><span class="sxs-lookup"><span data-stu-id="c28a6-205">The above code expands the client to add the `Broadcaster` class, which throttles the outgoing messages using the `Timer` class from the .NET framework.</span></span>

    <span data-ttu-id="c28a6-206">Protože je Centrum samotné přechodné (ho vytvoří pokaždé, když je to potřeba), `Broadcaster` se vytvoří jako typ singleton.</span><span class="sxs-lookup"><span data-stu-id="c28a6-206">Since the hub itself is transitory (it is created every time it is needed), the `Broadcaster` will be created as a singleton.</span></span> <span data-ttu-id="c28a6-207">Opožděná inicializace (představíme v rozhraní .NET 4) slouží k jeho vytvoření odložit, dokud není potřeba, zajištění, že první instanci rozbočovače zcela vytvořil před spuštěním časovač.</span><span class="sxs-lookup"><span data-stu-id="c28a6-207">Lazy initialization (introduced in .NET 4) is used to defer its creation until it is needed, ensuring that the first hub instance is completely created before the timer is started.</span></span>

    <span data-ttu-id="c28a6-208">Volání klientů `UpdateShape` funkce je poté přesunut mimo centra `UpdateModel` metodu tak, že je volána již okamžitě pokaždé, když se přijme příchozí zprávy.</span><span class="sxs-lookup"><span data-stu-id="c28a6-208">The call to the clients' `UpdateShape` function is then moved out of the hub's `UpdateModel` method, so that it is no longer called immediately whenever incoming messages are received.</span></span> <span data-ttu-id="c28a6-209">Místo toho se pošle zprávy, které mají klienti ve výši 25 volání za sekundu, spravuje `_broadcastLoop` časovač v rámci `Broadcaster` třídy.</span><span class="sxs-lookup"><span data-stu-id="c28a6-209">Instead, the messages to the clients will be sent at a rate of 25 calls per second, managed by the `_broadcastLoop` timer from within the `Broadcaster` class.</span></span>

    <span data-ttu-id="c28a6-210">A konečně, namísto volání metody klienta z centra přímo, `Broadcaster` třídy je potřeba získat odkaz na aktuálně operačního centra (`_hubContext`) pomocí `GlobalHost`.</span><span class="sxs-lookup"><span data-stu-id="c28a6-210">Lastly, instead of calling the client method from the hub directly, the `Broadcaster` class needs to obtain a reference to the currently operating hub (`_hubContext`) using the `GlobalHost`.</span></span>
2. <span data-ttu-id="c28a6-211">Stisknutím klávesy F5 spusťte aplikaci.</span><span class="sxs-lookup"><span data-stu-id="c28a6-211">Start the application by pressing F5.</span></span> <span data-ttu-id="c28a6-212">Zkopírujte adresu URL na stránku a vložte ho do druhé okno prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="c28a6-212">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="c28a6-213">Přetáhněte obrazec v jednom z okna prohlížeče; tvar v druhém okně prohlížeče by se měla přesunout.</span><span class="sxs-lookup"><span data-stu-id="c28a6-213">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span> <span data-ttu-id="c28a6-214">Nesmí být vidět rozdíl v prohlížeči z předchozí části, ale se omezí počet zpráv, které odesílání do klienta.</span><span class="sxs-lookup"><span data-stu-id="c28a6-214">There will not be a visible difference in the browser from the previous section, but the number of messages that get sent to the client will be throttled.</span></span>

    ![Okno aplikace](tutorial-high-frequency-realtime-with-signalr/_static/image6.png)

<a id="animation"></a>

## <a name="add-smooth-animation-on-the-client"></a><span data-ttu-id="c28a6-216">Přidat plynulou animaci na straně klienta</span><span class="sxs-lookup"><span data-stu-id="c28a6-216">Add smooth animation on the client</span></span>

<span data-ttu-id="c28a6-217">Aplikace je skoro hotová, ale jsme dokonce vytvářet jeden další vylepšení v pohybu tvar na straně klienta jako je přesunut v reakci na zprávu serveru.</span><span class="sxs-lookup"><span data-stu-id="c28a6-217">The application is almost complete, but we could make one more improvement, in the motion of the shape on the client as it is moved in response to server messages.</span></span> <span data-ttu-id="c28a6-218">Namísto nastavení pozice tvar do nového umístění uvedena v každém serveru, použijeme knihovna uživatelského rozhraní JQuery `animate` funkce přesunout tvar plynule mezi jeho aktuální a nové pozice.</span><span class="sxs-lookup"><span data-stu-id="c28a6-218">Rather than setting the position of the shape to the new location given by the server, we'll use the JQuery UI library's `animate` function to move the shape smoothly between its current and new position.</span></span>

1. <span data-ttu-id="c28a6-219">Také aktualizovat klienta sady `updateShape` metoda vypadat jako následující zvýrazněný kód:</span><span class="sxs-lookup"><span data-stu-id="c28a6-219">Update the client's `updateShape` method to look like the highlighted code below:</span></span>

    [!code-html[Main](tutorial-high-frequency-realtime-with-signalr/samples/sample10.html?highlight=35-42)]

    <span data-ttu-id="c28a6-220">Výše uvedený kód přesune tvaru z původního umístění do nové Dal serveru v průběhu intervalu animace (v tomto případě 100 milisekund).</span><span class="sxs-lookup"><span data-stu-id="c28a6-220">The above code moves the shape from the old location to the new one given by the server over the course of the animation interval (in this case, 100 milliseconds).</span></span> <span data-ttu-id="c28a6-221">Všechny předchozí animace spuštěna s tvarem není zaškrtnuto, před zahájením nové animace.</span><span class="sxs-lookup"><span data-stu-id="c28a6-221">Any previous animation running on the shape is cleared before the new animation starts.</span></span>
2. <span data-ttu-id="c28a6-222">Stisknutím klávesy F5 spusťte aplikaci.</span><span class="sxs-lookup"><span data-stu-id="c28a6-222">Start the application by pressing F5.</span></span> <span data-ttu-id="c28a6-223">Zkopírujte adresu URL na stránku a vložte ho do druhé okno prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="c28a6-223">Copy the page's URL, and paste it into a second browser window.</span></span> <span data-ttu-id="c28a6-224">Přetáhněte obrazec v jednom z okna prohlížeče; tvar v druhém okně prohlížeče by se měla přesunout.</span><span class="sxs-lookup"><span data-stu-id="c28a6-224">Drag the shape in one of the browser windows; the shape in the other browser window should move.</span></span> <span data-ttu-id="c28a6-225">Pohyb tvaru v druhém okně by se zobrazit méně přehrávat nepravidelně, jako je pohyb interpolovaných přes čas Místo nastavování jednou za příchozí zprávy.</span><span class="sxs-lookup"><span data-stu-id="c28a6-225">The movement of the shape in the other window should appear less jerky as its movement is interpolated over time rather than being set once per incoming message.</span></span>

    ![Okno aplikace](tutorial-high-frequency-realtime-with-signalr/_static/image7.png)

<a id="furthersteps"></a>

## <a name="further-steps"></a><span data-ttu-id="c28a6-227">Další kroky</span><span class="sxs-lookup"><span data-stu-id="c28a6-227">Further Steps</span></span>

<span data-ttu-id="c28a6-228">V tomto kurzu jste zjistili, jak programovat aplikace SignalR, která odesílá zprávy s vysokou frekvencí mezi klienty a servery.</span><span class="sxs-lookup"><span data-stu-id="c28a6-228">In this tutorial, you've learned how to program a SignalR application that sends high-frequency messages between clients and servers.</span></span> <span data-ttu-id="c28a6-229">Toto paradigma komunikace je užitečné pro vývoj online hry a další simulace, jako například [ShootR hru vytvořili s knihovnou SignalR](http://shootr.signalr.net).</span><span class="sxs-lookup"><span data-stu-id="c28a6-229">This communication paradigm is useful for developing online games and other simulations, such as [the ShootR game created with SignalR](http://shootr.signalr.net).</span></span>

<span data-ttu-id="c28a6-230">Hotové aplikace vytvořené v tomto kurzu si můžete stáhnout z [Galerie kódu na](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span><span class="sxs-lookup"><span data-stu-id="c28a6-230">The complete application created in this tutorial can be downloaded from [Code Gallery](https://code.msdn.microsoft.com/SignalR-MoveShape-demo-3366dac6).</span></span>

<span data-ttu-id="c28a6-231">Další informace o konceptech vývoj SignalR, najdete na následujících webech pro funkci SignalR zdrojový kód a prostředky:</span><span class="sxs-lookup"><span data-stu-id="c28a6-231">To learn more about SignalR development concepts, visit the following sites for SignalR source code and resources:</span></span>

- [<span data-ttu-id="c28a6-232">Projekt SignalR</span><span class="sxs-lookup"><span data-stu-id="c28a6-232">SignalR Project</span></span>](http://signalr.net)
- [<span data-ttu-id="c28a6-233">Funkce SignalR Githubu a ukázky</span><span class="sxs-lookup"><span data-stu-id="c28a6-233">SignalR Github and Samples</span></span>](https://github.com/SignalR/SignalR)
- [<span data-ttu-id="c28a6-234">SignalR Wiki</span><span class="sxs-lookup"><span data-stu-id="c28a6-234">SignalR Wiki</span></span>](https://github.com/SignalR/SignalR/wiki)
