---
uid: web-forms/overview/older-versions-security/roles/role-based-authorization-cs
title: Ověření na základě role (C#) | Microsoft Docs
author: rick-anderson
description: V tomto kurzu začíná podívejte se na tom, jak rozhraní role přidruží role uživatele jeho kontext zabezpečení. Pak prozkoumá jak se má použít na základě rolí URL...
ms.author: aspnetcontent
manager: wpickett
ms.date: 03/24/2008
ms.topic: article
ms.assetid: 4d9b63fa-c3d4-4e85-82b1-26ae3ba3ca1c
ms.technology: dotnet-webforms
ms.prod: .net-framework
msc.legacyurl: /web-forms/overview/older-versions-security/roles/role-based-authorization-cs
msc.type: authoredcontent
ms.openlocfilehash: 0a494e697eba44fcbf373c979e119572a8e37565
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 04/06/2018
---
<a name="role-based-authorization-c"></a><span data-ttu-id="0387a-104">Ověření na základě role (C#)</span><span class="sxs-lookup"><span data-stu-id="0387a-104">Role-Based Authorization (C#)</span></span>
====================
<span data-ttu-id="0387a-105">podle [Scott Meisnerová](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="0387a-105">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="0387a-106">[Stáhněte si kód](http://download.microsoft.com/download/6/0/3/6032582f-360d-4739-b935-38721fdb86ea/CS.11.zip) nebo [stáhnout PDF](http://download.microsoft.com/download/6/0/3/6032582f-360d-4739-b935-38721fdb86ea/aspnet_tutorial11_RoleAuth_cs.pdf)</span><span class="sxs-lookup"><span data-stu-id="0387a-106">[Download Code](http://download.microsoft.com/download/6/0/3/6032582f-360d-4739-b935-38721fdb86ea/CS.11.zip) or [Download PDF](http://download.microsoft.com/download/6/0/3/6032582f-360d-4739-b935-38721fdb86ea/aspnet_tutorial11_RoleAuth_cs.pdf)</span></span>

> <span data-ttu-id="0387a-107">V tomto kurzu začíná podívejte se na tom, jak rozhraní role přidruží role uživatele jeho kontext zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="0387a-107">This tutorial starts with a look at how the Roles framework associates a user's roles with his security context.</span></span> <span data-ttu-id="0387a-108">Pak zkontroluje jak se má použít na základě rolí autorizačních pravidel adres URL.</span><span class="sxs-lookup"><span data-stu-id="0387a-108">It then examines how to apply role-based URL authorization rules.</span></span> <span data-ttu-id="0387a-109">Následující, že se podíváme na pomocí deklarativní a programové znamená pro změnu zobrazená data a funkce nabízené stránky ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0387a-109">Following that, we will look at using declarative and programmatic means for altering the data displayed and the functionality offered by an ASP.NET page.</span></span>


## <a name="introduction"></a><span data-ttu-id="0387a-110">Úvod</span><span class="sxs-lookup"><span data-stu-id="0387a-110">Introduction</span></span>

<span data-ttu-id="0387a-111">V <a id="_msoanchor_1"> </a> [ *autorizace na základě uživatele* ](../membership/user-based-authorization-cs.md) kurzu jsme viděli postup použijte k určení, kterými se uživatelé mohou najdete konkrétní sadu stránek autorizace adres URL.</span><span class="sxs-lookup"><span data-stu-id="0387a-111">In the <a id="_msoanchor_1"></a>[*User-Based Authorization*](../membership/user-based-authorization-cs.md) tutorial we saw how to use URL authorization to specify what users could visit a particular set of pages.</span></span> <span data-ttu-id="0387a-112">S stačí jenom trocha značek v `Web.config`, jsme může vyzvat technologie ASP.NET najdete stránce pouze ověřeným uživatelům umožnit.</span><span class="sxs-lookup"><span data-stu-id="0387a-112">With just a little bit of markup in `Web.config`, we could instruct ASP.NET to allow only authenticated users to visit a page.</span></span> <span data-ttu-id="0387a-113">Nebo jsme může stanovují, že byly mohou pouze uživatelé, Tito a Bob nebo znamenat, že byly povoleny všechny ověřené uživatele s výjimkou Sam.</span><span class="sxs-lookup"><span data-stu-id="0387a-113">Or we could dictate that only users Tito and Bob were allowed, or indicate that all authenticated users except for Sam were permitted.</span></span>

<span data-ttu-id="0387a-114">Kromě autorizace adres URL jsme také prohlédli deklarativní a programové techniky pro řízení dat, které zobrazuje a funkce nabízené stránky v závislosti na uživatele, kteří navštěvují.</span><span class="sxs-lookup"><span data-stu-id="0387a-114">In addition to URL authorization, we also looked at declarative and programmatic techniques for controlling the data displayed and the functionality offered by a page based on the user visiting.</span></span> <span data-ttu-id="0387a-115">Konkrétně jsme vytvořili stránky, který je uvedený obsah aktuální adresář.</span><span class="sxs-lookup"><span data-stu-id="0387a-115">In particular, we created a page that listed the contents of the current directory.</span></span> <span data-ttu-id="0387a-116">Každý, kdo může najdete na této stránce, ale pouze ověření uživatelé mohou zobrazit tyto soubory obsahu a pouze Tito může odstranit soubory.</span><span class="sxs-lookup"><span data-stu-id="0387a-116">Anyone could visit this page, but only authenticated users could view the files' contents and only Tito could delete the files.</span></span>

<span data-ttu-id="0387a-117">Použití autorizační pravidla na základě uživatele uživatelů můžou růst do při důvodů účetnictví.</span><span class="sxs-lookup"><span data-stu-id="0387a-117">Applying authorization rules on a user-by-user basis can grow into a bookkeeping nightmare.</span></span> <span data-ttu-id="0387a-118">Více udržovatelný možných přístupů je použít ověřování založené na rolích.</span><span class="sxs-lookup"><span data-stu-id="0387a-118">A more maintainable approach is to use role-based authorization.</span></span> <span data-ttu-id="0387a-119">Dobrá zpráva je, že nástroje naše k dispozici pro použití autorizačních pravidel fungovat stejně jako u uživatelských účtů spolu s rolemi.</span><span class="sxs-lookup"><span data-stu-id="0387a-119">The good news is that the tools at our disposal for applying authorization rules work equally well with roles as they do for user accounts.</span></span> <span data-ttu-id="0387a-120">Autorizačních pravidel adres URL můžete zadat role místo uživatelů.</span><span class="sxs-lookup"><span data-stu-id="0387a-120">URL authorization rules can specify roles instead of users.</span></span> <span data-ttu-id="0387a-121">Ovládací prvek LoginView, který vykreslí různých výstupu pro ověřený a anonymním uživatelům, můžete nakonfigurovat k zobrazení různých obsahu na základě rolí přihlášeného uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-121">The LoginView control, which renders different output for authenticated and anonymous users, can be configured to display different content based on the logged in user's roles.</span></span> <span data-ttu-id="0387a-122">A rozhraní API rolí obsahuje metody pro určení rolí přihlášeného uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-122">And the Roles API includes methods for determining the logged in user's roles.</span></span>

<span data-ttu-id="0387a-123">V tomto kurzu začíná podívejte se na tom, jak rozhraní role přidruží role uživatele jeho kontext zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="0387a-123">This tutorial starts with a look at how the Roles framework associates a user's roles with his security context.</span></span> <span data-ttu-id="0387a-124">Pak zkontroluje jak se má použít na základě rolí autorizačních pravidel adres URL.</span><span class="sxs-lookup"><span data-stu-id="0387a-124">It then examines how to apply role-based URL authorization rules.</span></span> <span data-ttu-id="0387a-125">Následující, že se podíváme na pomocí deklarativní a programové znamená pro změnu zobrazená data a funkce nabízené stránky ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0387a-125">Following that, we will look at using declarative and programmatic means for altering the data displayed and the functionality offered by an ASP.NET page.</span></span> <span data-ttu-id="0387a-126">Můžeme začít!</span><span class="sxs-lookup"><span data-stu-id="0387a-126">Let's get started!</span></span>

## <a name="understanding-how-roles-are-associated-with-a-users-security-context"></a><span data-ttu-id="0387a-127">Pochopení jak role jsou přidružené kontextu zabezpečení uživatele</span><span class="sxs-lookup"><span data-stu-id="0387a-127">Understanding How Roles are Associated with a User's Security Context</span></span>

<span data-ttu-id="0387a-128">Vždy, když požadavek vstoupí kanálu ASP.NET je přidružen kontext zabezpečení, který obsahuje informace o identifikaci žadatel.</span><span class="sxs-lookup"><span data-stu-id="0387a-128">Whenever a request enters the ASP.NET pipeline it is associated with a security context, which includes information identifying the requestor.</span></span> <span data-ttu-id="0387a-129">Pokud používáte ověřování pomocí formulářů, na ověřovací lístek slouží jako identity token.</span><span class="sxs-lookup"><span data-stu-id="0387a-129">When using forms authentication, an authentication ticket is used as an identity token.</span></span> <span data-ttu-id="0387a-130">Jak již bylo zmíněno <a id="_msoanchor_2"> </a> [ *Přehled ověřování založené na formulářích* ](../introduction/an-overview-of-forms-authentication-cs.md) a <a id="_msoanchor_3"> </a> [ *formulářů Konfigurace ověřování a rozšířené témata* ](../introduction/forms-authentication-configuration-and-advanced-topics-cs.md) kurzy, `FormsAuthenticationModule` je zodpovědná za určení identitu žadatele, který provede během [ `AuthenticateRequest` událostí](https://msdn.microsoft.com/library/system.web.httpapplication.authenticaterequest.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-130">As we discussed in the <a id="_msoanchor_2"></a>[*An Overview of Forms Authentication*](../introduction/an-overview-of-forms-authentication-cs.md) and <a id="_msoanchor_3"></a>[*Forms Authentication Configuration and Advanced Topics*](../introduction/forms-authentication-configuration-and-advanced-topics-cs.md) tutorials, the `FormsAuthenticationModule` is responsible for determining the identity of the requestor, which it does during the [`AuthenticateRequest` event](https://msdn.microsoft.com/library/system.web.httpapplication.authenticaterequest.aspx).</span></span>

<span data-ttu-id="0387a-131">Pokud se najde platný, – platnost ověřovací lístek, `FormsAuthenticationModule` dekóduje, aby zjistily identitu žadatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-131">If a valid, non-expired authentication ticket is found, the `FormsAuthenticationModule` decodes it to ascertain the requestor's identity.</span></span> <span data-ttu-id="0387a-132">Vytvoří novou `GenericPrincipal` objektu a přiřadí k tomu `HttpContext.User` objektu.</span><span class="sxs-lookup"><span data-stu-id="0387a-132">It creates a new `GenericPrincipal` object and assigns this to the `HttpContext.User` object.</span></span> <span data-ttu-id="0387a-133">Účelem objekt zabezpečení, jako například `GenericPrincipal`, je identifikace jméno ověřeného uživatele a co uživatel patří do role.</span><span class="sxs-lookup"><span data-stu-id="0387a-133">The purpose of a principal, like `GenericPrincipal`, is to identify the authenticated user's name and what roles she belong to.</span></span> <span data-ttu-id="0387a-134">Je zřejmé ve skutečnosti, že všechny hlavní objekty mají tento účel `Identity` vlastnost a `IsInRole(roleName)` metoda.</span><span class="sxs-lookup"><span data-stu-id="0387a-134">This purpose is evident by the fact that all principal objects have an `Identity` property and an `IsInRole(roleName)` method.</span></span> <span data-ttu-id="0387a-135">`FormsAuthenticationModule`, Ale není zájem o zaznamenávání informací o rolích a `GenericPrincipal` objekt vytvoří neurčuje žádné role.</span><span class="sxs-lookup"><span data-stu-id="0387a-135">The `FormsAuthenticationModule`, however, is not interested in recording role information and the `GenericPrincipal` object it creates does not specify any roles.</span></span>

<span data-ttu-id="0387a-136">Pokud je povoleno rozhraní rolí, [ `RoleManagerModule` ](https://msdn.microsoft.com/library/system.web.security.rolemanagermodule.aspx) modulu HTTP kroky po `FormsAuthenticationModule` a identifikuje role ověřeného uživatele při [ `PostAuthenticateRequest` událostí](https://msdn.microsoft.com/library/system.web.httpapplication.postauthenticaterequest.aspx), které Aktivuje se po `AuthenticateRequest` událostí.</span><span class="sxs-lookup"><span data-stu-id="0387a-136">If the Roles framework is enabled, the [`RoleManagerModule`](https://msdn.microsoft.com/library/system.web.security.rolemanagermodule.aspx) HTTP Module steps in after the `FormsAuthenticationModule` and identifies the authenticated user's roles during the [`PostAuthenticateRequest` event](https://msdn.microsoft.com/library/system.web.httpapplication.postauthenticaterequest.aspx), which fires after the `AuthenticateRequest` event.</span></span> <span data-ttu-id="0387a-137">Pokud se jedná o žádost z ověřeného uživatele, `RoleManagerModule` přepíše `GenericPrincipal` objekt vytvořený `FormsAuthenticationModule` a nahradí ji s [ `RolePrincipal` objekt](https://msdn.microsoft.com/library/system.web.security.roleprincipal.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-137">If the request is from an authenticated user, the `RoleManagerModule` overwrites the `GenericPrincipal` object created by the `FormsAuthenticationModule` and replaces it with a [`RolePrincipal` object](https://msdn.microsoft.com/library/system.web.security.roleprincipal.aspx).</span></span> <span data-ttu-id="0387a-138">`RolePrincipal` Třída používá k určení rolí, které uživatel patří do rozhraní API rolí.</span><span class="sxs-lookup"><span data-stu-id="0387a-138">The `RolePrincipal` class uses the Roles API to determine what roles the user belongs to.</span></span>

<span data-ttu-id="0387a-139">Obrázek 1 znázorňuje pracovním postupem kanálu ASP.NET při použití ověřování pomocí formulářů a rozhraní role.</span><span class="sxs-lookup"><span data-stu-id="0387a-139">Figure 1 depicts the ASP.NET pipeline workflow when using forms authentication and the Roles framework.</span></span> <span data-ttu-id="0387a-140">`FormsAuthenticationModule` Provede první, identifikuje uživatele na základě jeho lístek ověřování a vytvoří novou `GenericPrincipal` objektu.</span><span class="sxs-lookup"><span data-stu-id="0387a-140">The `FormsAuthenticationModule` executes first, identifies the user via her authentication ticket, and creates a new `GenericPrincipal` object.</span></span> <span data-ttu-id="0387a-141">Dále `RoleManagerModule` kroky a přepíše `GenericPrincipal` objektu s `RolePrincipal` objektu.</span><span class="sxs-lookup"><span data-stu-id="0387a-141">Next, the `RoleManagerModule` steps in and overwrites the `GenericPrincipal` object with a `RolePrincipal` object.</span></span>

<span data-ttu-id="0387a-142">Pokud anonymní uživatel navštíví web, ani `FormsAuthenticationModule` ani `RoleManagerModule` vytvoří objekt zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="0387a-142">If an anonymous user visits the site, neither the `FormsAuthenticationModule` nor the `RoleManagerModule` creates a principal object.</span></span>


<span data-ttu-id="0387a-143">[![Události kanálu ASP.NET pro ověřeného uživatele při použití ověřování pomocí formulářů a rozhraní role](role-based-authorization-cs/_static/image2.png)](role-based-authorization-cs/_static/image1.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-143">[![The ASP.NET Pipeline Events for an Authenticated User When Using Forms Authentication and the Roles Framework](role-based-authorization-cs/_static/image2.png)](role-based-authorization-cs/_static/image1.png)</span></span>

<span data-ttu-id="0387a-144">**Obrázek 1**: události kanálu ASP.NET pro k ověření uživatele při použití ověřování pomocí formulářů a rozhraní role ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image3.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-144">**Figure 1**: The ASP.NET Pipeline Events for an Authenticated User When Using Forms Authentication and the Roles Framework  ([Click to view full-size image](role-based-authorization-cs/_static/image3.png))</span></span>


### <a name="caching-role-information-in-a-cookie"></a><span data-ttu-id="0387a-145">Ukládání do mezipaměti informace o rolích do souboru cookie</span><span class="sxs-lookup"><span data-stu-id="0387a-145">Caching Role Information in a Cookie</span></span>

<span data-ttu-id="0387a-146">`RolePrincipal` Objektu `IsInRole(roleName)` volání metod `Roles.GetRolesForUser` získat role pro uživatele, aby bylo možné zjistit, zda uživatel je členem *roleName*.</span><span class="sxs-lookup"><span data-stu-id="0387a-146">The `RolePrincipal` object's `IsInRole(roleName)` method calls `Roles.GetRolesForUser` to get the roles for the user in order to determine whether the user is a member of *roleName*.</span></span> <span data-ttu-id="0387a-147">Při použití `SqlRoleProvider`, výsledkem dotazu do databáze úložiště rolí.</span><span class="sxs-lookup"><span data-stu-id="0387a-147">When using the `SqlRoleProvider`, this results in a query to the role store database.</span></span> <span data-ttu-id="0387a-148">Při použití autorizačních pravidel adres URL na základě rolí `RolePrincipal`na `IsInRole` bude volána metoda u každého požadavku na stránku, který je chráněný pomocí autorizačních pravidel adres URL na základě rolí.</span><span class="sxs-lookup"><span data-stu-id="0387a-148">When using role-based URL authorization rules the `RolePrincipal`'s `IsInRole` method will be called on every request to a page that is protected by the role-based URL authorization rules.</span></span> <span data-ttu-id="0387a-149">Nemusí používat k vyhledávání informací role v databázi na každou žádost, rozhraní role zahrnuje možnost pro ukládání do mezipaměti role uživatele v souboru cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-149">Rather than have to lookup the role information in the database on every request, the Roles framework includes an option to cache the user's roles in a cookie.</span></span>

<span data-ttu-id="0387a-150">Pokud rozhraní role je nakonfigurovaný pro ukládání do mezipaměti role uživatele do souboru cookie `RoleManagerModule` během kanálu ASP.NET vytvoří soubor cookie [ `EndRequest` událostí](https://msdn.microsoft.com/library/system.web.httpapplication.endrequest.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-150">If the Roles framework is configured to cache the user's roles in a cookie, the `RoleManagerModule` creates the cookie during the ASP.NET pipeline's [`EndRequest` event](https://msdn.microsoft.com/library/system.web.httpapplication.endrequest.aspx).</span></span> <span data-ttu-id="0387a-151">Tento soubor cookie se používá v následných žádostí v `PostAuthenticateRequest`, který je při `RolePrincipal` je vytvořen objekt.</span><span class="sxs-lookup"><span data-stu-id="0387a-151">This cookie is used in subsequent requests in the `PostAuthenticateRequest`, which is when the `RolePrincipal` object is created.</span></span> <span data-ttu-id="0387a-152">Pokud soubor cookie platný a jestli nevypršela platnost, je analyzovat a používaných k naplnění role uživatele, a tím ukládání dat v souboru cookie `RolePrincipal` z nutnosti provádět volání `Roles` třídu k určení role uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-152">If the cookie is valid and has not expired, the data in the cookie is parsed and used to populate the user's roles, thereby saving the `RolePrincipal` from having to make a call to the `Roles` class to determine the user's roles.</span></span> <span data-ttu-id="0387a-153">Obrázek 2 znázorňuje tento pracovní postup.</span><span class="sxs-lookup"><span data-stu-id="0387a-153">Figure 2 depicts this workflow.</span></span>


<span data-ttu-id="0387a-154">[![Informace o rolích uživatele mohou být uloženy do souboru cookie pro zlepšení výkonu](role-based-authorization-cs/_static/image5.png)](role-based-authorization-cs/_static/image4.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-154">[![The User's Role Information Can Be Stored in a Cookie to Improve Performance](role-based-authorization-cs/_static/image5.png)](role-based-authorization-cs/_static/image4.png)</span></span>

<span data-ttu-id="0387a-155">**Obrázek 2**: pole uživatelské Role může být uložena v souboru Cookie ke zlepšení výkonu ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image6.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-155">**Figure 2**: The User's Role Information Can Be Stored in a Cookie to Improve Performance  ([Click to view full-size image](role-based-authorization-cs/_static/image6.png))</span></span>


<span data-ttu-id="0387a-156">Mechanismus role mezipaměti souborů cookie je ve výchozím nastavení zakázaná.</span><span class="sxs-lookup"><span data-stu-id="0387a-156">By default, the role cache cookie mechanism is disabled.</span></span> <span data-ttu-id="0387a-157">Může být povoleno prostřednictvím `<roleManager>` značek konfigurace v `Web.config`.</span><span class="sxs-lookup"><span data-stu-id="0387a-157">It can be enabled through the `<roleManager>` configuration markup in `Web.config`.</span></span> <span data-ttu-id="0387a-158">Jsme probrali pomocí [ `<roleManager>` element](https://msdn.microsoft.com/library/ms164660.aspx) chcete zadat zprostředkovatele rolí v <a id="_msoanchor_4"> </a> [ *vytváření a Správa rolí* ](creating-and-managing-roles-cs.md) kurzu Proto byste již měli mít tento element ve vaší aplikaci `Web.config` souboru.</span><span class="sxs-lookup"><span data-stu-id="0387a-158">We discussed using the [`<roleManager>` element](https://msdn.microsoft.com/library/ms164660.aspx) to specify Role providers in the <a id="_msoanchor_4"></a>[*Creating and Managing Roles*](creating-and-managing-roles-cs.md) tutorial, so you should already have this element in your application's `Web.config` file.</span></span> <span data-ttu-id="0387a-159">Nastavení ukládání do mezipaměti souborů cookie role jsou zadané jako atributy `<roleManager>` elementu a jsou shrnuty v tabulce 1.</span><span class="sxs-lookup"><span data-stu-id="0387a-159">The role cache cookie settings are specified as attributes of the `<roleManager>` element, and are summarized in Table 1.</span></span>

> [!NOTE]
> <span data-ttu-id="0387a-160">Nastavení konfigurace, které jsou uvedené v tabulce 1 zadejte vlastnosti výsledný soubor cookie mezipaměti role.</span><span class="sxs-lookup"><span data-stu-id="0387a-160">The configuration settings listed in Table 1 specify the properties of the resulting role cache cookie.</span></span> <span data-ttu-id="0387a-161">Další informace o soubory cookie, funkce a jejich různé vlastnosti čtení [tohoto kurzu soubory cookie](http://www.quirksmode.org/js/cookies.html).</span><span class="sxs-lookup"><span data-stu-id="0387a-161">For more information on cookies, how they work, and their various properties, read [this Cookies tutorial](http://www.quirksmode.org/js/cookies.html).</span></span>


| <span data-ttu-id="0387a-162"><strong>Vlastnost</strong></span><span class="sxs-lookup"><span data-stu-id="0387a-162"><strong>Property</strong></span></span> |                                                                                                                                                                                                                                                                                                                                                         <span data-ttu-id="0387a-163"><strong>Popis</strong></span><span class="sxs-lookup"><span data-stu-id="0387a-163"><strong>Description</strong></span></span>                                                                                                                                                                                                                                                                                                                                                          |
|---------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|   `cacheRolesInCookie`    |                                                                                                                                                                                                                                                                                                                              <span data-ttu-id="0387a-164">Logická hodnota, která označuje, zda se používá ukládání do mezipaměti souborů cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-164">A Boolean value that indicates whether cookie caching is used.</span></span> <span data-ttu-id="0387a-165">Použije se výchozí hodnota `false`.</span><span class="sxs-lookup"><span data-stu-id="0387a-165">Defaults to `false`.</span></span>                                                                                                                                                                                                                                                                                                                              |
|       `cookieName`        |                                                                                                                                                                                                                                                                                                                                     <span data-ttu-id="0387a-166">Název souboru cookie mezipaměti role.</span><span class="sxs-lookup"><span data-stu-id="0387a-166">The name of the role cache cookie.</span></span> <span data-ttu-id="0387a-167">Výchozí hodnota je ". ASPXROLES".</span><span class="sxs-lookup"><span data-stu-id="0387a-167">The default value is ".ASPXROLES".</span></span>                                                                                                                                                                                                                                                                                                                                     |
|       `cookiePath`        |                                                                                                                                                                                                                                <span data-ttu-id="0387a-168">Cesta k souboru cookie název role.</span><span class="sxs-lookup"><span data-stu-id="0387a-168">The path for the roles name cookie.</span></span> <span data-ttu-id="0387a-169">Atribut path umožňuje vývojáři k omezení oboru soubor cookie pro konkrétní adresář hierarchie.</span><span class="sxs-lookup"><span data-stu-id="0387a-169">The path attribute enables a developer to limit the scope of a cookie to a particular directory hierarchy.</span></span> <span data-ttu-id="0387a-170">Výchozí hodnota je "/", která informuje prohlížeč odeslat lístek ověřovacího souboru cookie na všechny žádosti do domény.</span><span class="sxs-lookup"><span data-stu-id="0387a-170">The default value is "/", which informs the browser to send the authentication ticket cookie to any request made to the domain.</span></span>                                                                                                                                                                                                                                 |
|    `cookieProtection`     |                                                                                                                                                               <span data-ttu-id="0387a-171">Určuje, jaké postupy se používají k ochraně souborů cookie mezipaměti role.</span><span class="sxs-lookup"><span data-stu-id="0387a-171">Indicates what techniques are used to protect the role cache cookie.</span></span> <span data-ttu-id="0387a-172">Povolené hodnoty jsou: `All` (výchozí); `Encryption`; `None`; a `Validation`.</span><span class="sxs-lookup"><span data-stu-id="0387a-172">The allowable values are: `All` (the default); `Encryption`; `None`; and `Validation`.</span></span> <span data-ttu-id="0387a-173">Odkazovat zpět do kroku 3 <a id="_anchor_5"> </a> [ *konfiguraci ověřování formulářů a rozšířené témata* ](../introduction/forms-authentication-configuration-and-advanced-topics-cs.md) kurz další informace o těchto úrovních ochrany.</span><span class="sxs-lookup"><span data-stu-id="0387a-173">Refer back to Step 3 of the <a id="_anchor_5"></a>[*Forms Authentication Configuration and Advanced Topics*](../introduction/forms-authentication-configuration-and-advanced-topics-cs.md) tutorial for more information on these protection levels.</span></span>                                                                                                                                                                |
|    `cookieRequireSSL`     |                                                                                                                                                                                                                                                                                                   <span data-ttu-id="0387a-174">Logická hodnota, která určuje, jestli přenos ověřovacího souboru cookie vyžaduje připojení SSL.</span><span class="sxs-lookup"><span data-stu-id="0387a-174">A Boolean value that indicates whether an SSL connection is required to transmit the authentication cookie.</span></span> <span data-ttu-id="0387a-175">Výchozí hodnota je `false`.</span><span class="sxs-lookup"><span data-stu-id="0387a-175">The default value is `false`.</span></span>                                                                                                                                                                                                                                                                                                   |
| `cookieSlidingExpiration` |                                                                                                                                                                                                                                                  <span data-ttu-id="0387a-176">Logická hodnota, která určuje, že zda časový limit souboru cookie se vynuluje pokaždé, když uživatel navštíví web během jedné relace.</span><span class="sxs-lookup"><span data-stu-id="0387a-176">A Boolean value that indicates whether the cookie's timeout is reset each time the user visits the site during a single session.</span></span> <span data-ttu-id="0387a-177">Výchozí hodnota je `false`.</span><span class="sxs-lookup"><span data-stu-id="0387a-177">The default value is `false`.</span></span> <span data-ttu-id="0387a-178">Tato hodnota je pouze v příslušném případě `createPersistentCookie` je nastaven na `true`.</span><span class="sxs-lookup"><span data-stu-id="0387a-178">This value is only pertinent when `createPersistentCookie` is set to `true`.</span></span>                                                                                                                                                                                                                                                  |
|      `cookieTimeout`      |                                                                                                                                                                                                                                                                         <span data-ttu-id="0387a-179">Určuje dobu v minutách, po kterém vyprší platnost ověřovacího lístku souboru cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-179">Specifies the time, in minutes, after which the authentication ticket cookie expires.</span></span> <span data-ttu-id="0387a-180">Výchozí hodnota je `30`.</span><span class="sxs-lookup"><span data-stu-id="0387a-180">The default value is `30`.</span></span> <span data-ttu-id="0387a-181">Tato hodnota je pouze v příslušném případě `createPersistentCookie` je nastaven na `true`.</span><span class="sxs-lookup"><span data-stu-id="0387a-181">This value is only pertinent when `createPersistentCookie` is set to `true`.</span></span>                                                                                                                                                                                                                                                                         |
| `createPersistentCookie`  |                                                                                                                                                                   <span data-ttu-id="0387a-182">Logická hodnota, která určuje, zda role mezipaměti souborů cookie je soubor cookie relace nebo trvalého souboru cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-182">A Boolean value that specifies whether the role cache cookie is a session cookie or persistent cookie.</span></span> <span data-ttu-id="0387a-183">Pokud `false` (výchozí), se používá soubor cookie relace, které se odstraní při zavření prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="0387a-183">If `false` (the default), a session cookie is used, which is deleted when the browser is closed.</span></span> <span data-ttu-id="0387a-184">Pokud `true`, je použita trvalého souboru cookie; jeho platnost vyprší `cookieTimeout` počet minut po jeho vytvoření nebo po předchozí návštěvě, v závislosti na hodnotě `cookieSlidingExpiration`.</span><span class="sxs-lookup"><span data-stu-id="0387a-184">If `true`, a persistent cookie is used; it expires `cookieTimeout` number of minutes after it has been created or after the previous visit, depending on the value of `cookieSlidingExpiration`.</span></span>                                                                                                                                                                    |
|         `domain`          |                                                                                                                                                 <span data-ttu-id="0387a-185">Určuje hodnotu domény souboru cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-185">Specifies the cookie's domain value.</span></span> <span data-ttu-id="0387a-186">Výchozí hodnota je řetězec prázdný, což způsobí, že prohlížeči domény, ze kterého byl vydán (například www.yourdomain.com).</span><span class="sxs-lookup"><span data-stu-id="0387a-186">The default value is an empty string, which causes the browser to use the domain from which it was issued (such as www.yourdomain.com).</span></span> <span data-ttu-id="0387a-187">V takovém případě bude soubor cookie <strong>není</strong> zasílá po provedení žádosti subdomény, jako je například admin.yourdomain.com. Pokud chcete soubor cookie, které mají být předány všechny subdomény, budete muset přizpůsobit `domain` atribut, jeho nastavení na hodnotu "vase_domena.com".</span><span class="sxs-lookup"><span data-stu-id="0387a-187">In this case, the cookie will <strong>not</strong> be sent when making requests to subdomains, such as admin.yourdomain.com. If you want the cookie to be passed to all subdomains you need to customize the `domain` attribute, setting it to "yourdomain.com".</span></span>                                                                                                                                                 |
|    `maxCachedResults`     | <span data-ttu-id="0387a-188">Určuje maximální počet názvů rolí, které jsou uložené v mezipaměti v souboru cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-188">Specifies the maximum number of role names that are cached in the cookie.</span></span> <span data-ttu-id="0387a-189">Výchozí hodnota je 25.</span><span class="sxs-lookup"><span data-stu-id="0387a-189">The default is 25.</span></span> <span data-ttu-id="0387a-190">`RoleManagerModule` Nevytváří soubor cookie pro uživatele, kteří patří do více než `maxCachedResults` role.</span><span class="sxs-lookup"><span data-stu-id="0387a-190">The `RoleManagerModule` does not create a cookie for users that belong to more than `maxCachedResults` roles.</span></span> <span data-ttu-id="0387a-191">V důsledku toho `RolePrincipal` objektu `IsInRole` metoda použije `Roles` třídu k určení role uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-191">Consequently, the `RolePrincipal` object's `IsInRole` method will use the `Roles` class to determine the user's roles.</span></span> <span data-ttu-id="0387a-192">Z důvodu `maxCachedResults` existuje totiž mnoho Uživatelští agenti nepovoluje soubory cookie, které jsou větší než 4 096 bajtů.</span><span class="sxs-lookup"><span data-stu-id="0387a-192">The reason `maxCachedResults` exists is because many user agents do not permit cookies larger than 4,096 bytes.</span></span> <span data-ttu-id="0387a-193">Proto tento cap rozumí se sníží pravděpodobnost, což představuje překročení toto omezení velikosti.</span><span class="sxs-lookup"><span data-stu-id="0387a-193">So this cap is meant to reduce the likelihood of exceeding this size limitation.</span></span> <span data-ttu-id="0387a-194">Pokud máte role extrémně dlouhé názvy, můžete chtít zvažte zadání menší `maxCachedResults` hodnoty; contrariwise, pokud máte role velmi krátké názvy, můžete pravděpodobně zvýšit tuto hodnotu.</span><span class="sxs-lookup"><span data-stu-id="0387a-194">If you have extremely long role names, you may want to consider specifying a smaller `maxCachedResults` value; contrariwise, if you have extremely short role names, you can probably increase this value.</span></span> |

<span data-ttu-id="0387a-195">**Tabulka 1:** možnosti konfigurace Role mezipaměti souborů Cookie</span><span class="sxs-lookup"><span data-stu-id="0387a-195">**Table 1:** The Role Cache Cookie Configuration Options</span></span>

<span data-ttu-id="0387a-196">Umožňuje nakonfigurovat naší aplikaci používat soubory cookie mezipaměti dočasnou role.</span><span class="sxs-lookup"><span data-stu-id="0387a-196">Let's configure our application to use non-persistent role cache cookies.</span></span> <span data-ttu-id="0387a-197">K tomu, aktualizovat `<roleManager>` element v `Web.config` zahrnout následující atributy souboru cookie související:</span><span class="sxs-lookup"><span data-stu-id="0387a-197">To accomplish this, update the `<roleManager>` element in `Web.config` to include the following cookie-related attributes:</span></span>

[!code-xml[Main](role-based-authorization-cs/samples/sample1.xml)]

<span data-ttu-id="0387a-198">Můžu aktualizovat `<roleManager>` element tak, že přidáte tři atributy: `cacheRolesInCookie`, `createPersistentCookie`, a `cookieProtection`.</span><span class="sxs-lookup"><span data-stu-id="0387a-198">I updated the `<roleManager>` element by adding three attributes: `cacheRolesInCookie`, `createPersistentCookie`, and `cookieProtection`.</span></span> <span data-ttu-id="0387a-199">Nastavením `cacheRolesInCookie` k `true`, `RoleManagerModule` bude nyní automaticky ukládat do mezipaměti role uživatele v souboru cookie, namísto nutnosti vyhledat informace o rolích uživatele na každý požadavek.</span><span class="sxs-lookup"><span data-stu-id="0387a-199">By setting `cacheRolesInCookie` to `true`, the `RoleManagerModule` will now automatically cache the user's roles in a cookie rather than having to lookup the user's role information on each request.</span></span> <span data-ttu-id="0387a-200">Explicitně nastavit `createPersistentCookie` a `cookieProtection` atributy pro `false` a `All`, v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="0387a-200">I explicitly set the `createPersistentCookie` and `cookieProtection` attributes to `false` and `All`, respectively.</span></span> <span data-ttu-id="0387a-201">Technicky nebylo nutné zadat hodnoty pro tyto atributy, protože je právě přiděleny na výchozí hodnoty, ale I je zde zadejte aby explicitně vymazat nepoužívám trvalé soubory cookie a zda je soubor cookie obě zašifrované a ověřit.</span><span class="sxs-lookup"><span data-stu-id="0387a-201">Technically, I didn't need to specify values for these attributes since I just assigned them to their default values, but I put them here to make it explicitly clear that I am not using persistent cookies and that the cookie is both encrypted and validated.</span></span>

<span data-ttu-id="0387a-202">To je všechno je k němu!</span><span class="sxs-lookup"><span data-stu-id="0387a-202">That's all there is to it!</span></span> <span data-ttu-id="0387a-203">Role uživatelů v souborech cookie v mezipaměti od nynějška, rozhraní role.</span><span class="sxs-lookup"><span data-stu-id="0387a-203">Henceforth, the Roles framework will cache the users' roles in cookies.</span></span> <span data-ttu-id="0387a-204">Pokud uživatele prohlížeč nepodporuje soubory cookie, nebo pokud své soubory cookie jsou odstraněna nebo ke ztrátě, nějakým způsobem, je žádné big pozornosti – `RolePrincipal` objektu bude jednoduše používat `Roles` – třída v případě, že je k dispozici žádný soubor cookie (nebo některého neplatná nebo vypršela její platnost).</span><span class="sxs-lookup"><span data-stu-id="0387a-204">If the user's browser does not support cookies, or if their cookies are deleted or lost, somehow, it's no big deal – the `RolePrincipal` object will simply use the `Roles` class in the case that no cookie (or an invalid or expired one) is available.</span></span>

> [!NOTE]
> <span data-ttu-id="0387a-205">Společnosti Microsoft vzory &amp; postupy skupiny nedoporučuje pomocí role trvalé mezipaměti souborů cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-205">Microsoft's Patterns &amp; Practices group discourages using persistent role cache cookies.</span></span> <span data-ttu-id="0387a-206">Vzhledem k tomu, že vlastnictví souboru cookie mezipaměti role je dostačující k prokázání členství role, pokud se hacker nějakým způsobem můžete získat přístup k souboru cookie platné uživatelské mu zosobnit uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-206">Since possession of the role cache cookie is sufficient to prove role membership, if a hacker can somehow gain access to a valid user's cookie he can impersonate that user.</span></span> <span data-ttu-id="0387a-207">Pravděpodobnost této situaci se zvyšuje, pokud je soubor cookie trvalý na prohlížeče uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-207">The likelihood of this happening increases if the cookie is persisted on the user's browser.</span></span> <span data-ttu-id="0387a-208">Další informace o toto doporučení zabezpečení, jakož i další otázky zabezpečení, najdete v části [seznamu otázky zabezpečení pro technologii ASP.NET 2.0](https://msdn.microsoft.com/library/ms998375.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-208">For more information on this security recommendation, as well as other security concerns, refer to the [Security Question List for ASP.NET 2.0](https://msdn.microsoft.com/library/ms998375.aspx).</span></span>


## <a name="step-1-defining-role-based-url-authorization-rules"></a><span data-ttu-id="0387a-209">Krok 1: Definování autorizačních pravidel adres URL na základě rolí</span><span class="sxs-lookup"><span data-stu-id="0387a-209">Step 1: Defining Role-Based URL Authorization Rules</span></span>

<span data-ttu-id="0387a-210">Jak je popsáno v <a id="_msoanchor_6"> </a> [ *autorizace na základě uživatele* ](../membership/user-based-authorization-cs.md) kurzu autorizace adres URL nabízí způsob omezení přístupu k sadě stránek na uživatele podle uživatele nebo roli role základ.</span><span class="sxs-lookup"><span data-stu-id="0387a-210">As discussed in the <a id="_msoanchor_6"></a>[*User-Based Authorization*](../membership/user-based-authorization-cs.md) tutorial, URL authorization offers a means to restrict access to a set of pages on a user-by-user or role-by-role basis.</span></span> <span data-ttu-id="0387a-211">Autorizačních pravidel adres URL jsou vypsány v `Web.config` pomocí [ `<authorization>` element](https://msdn.microsoft.com/library/8d82143t.aspx) s `<allow>` a `<deny>` podřízené elementy.</span><span class="sxs-lookup"><span data-stu-id="0387a-211">The URL authorization rules are spelled out in `Web.config` using the [`<authorization>` element](https://msdn.microsoft.com/library/8d82143t.aspx) with `<allow>` and `<deny>` child elements.</span></span> <span data-ttu-id="0387a-212">Kromě související s uživateli autorizačních pravidel popsaných v předchozí kurzy každý `<allow>` a `<deny>` podřízený element může také zahrnovat:</span><span class="sxs-lookup"><span data-stu-id="0387a-212">In addition to the user-related authorization rules discussed in previous tutorials, each `<allow>` and `<deny>` child element can also include:</span></span>

- <span data-ttu-id="0387a-213">Konkrétní roli</span><span class="sxs-lookup"><span data-stu-id="0387a-213">A particular role</span></span>
- <span data-ttu-id="0387a-214">Čárkami oddělený seznam rolí</span><span class="sxs-lookup"><span data-stu-id="0387a-214">A comma-delimited list of roles</span></span>

<span data-ttu-id="0387a-215">Například autorizačních pravidel adres URL udělení přístupu k tito uživatelé v rolích správce a vedoucí, ale odepření přístupu pro všechny ostatní:</span><span class="sxs-lookup"><span data-stu-id="0387a-215">For example, the URL authorization rules grant access to those users in the Administrators and Supervisors roles, but deny access to all others:</span></span>

[!code-xml[Main](role-based-authorization-cs/samples/sample2.xml)]

<span data-ttu-id="0387a-216">`<allow>` Element ve výše uvedených značek stavy, že jsou povolené role správců a vedoucí; `<deny>` dá pokyn element, který *všechny* uživatelé mají odepřený.</span><span class="sxs-lookup"><span data-stu-id="0387a-216">The `<allow>` element in the above markup states that the Administrators and Supervisors roles are allowed; the `<deny>` element instructs that *all* users are denied.</span></span>

<span data-ttu-id="0387a-217">Umožňuje nakonfigurovat aplikaci tak, aby `ManageRoles.aspx`, `UsersAndRoles.aspx`, a `CreateUserWizardWithRoles.aspx` stránky jsou pouze uživatelé, v roli správce při `RoleBasedAuthorization.aspx` nadále dostupná pro všechny návštěvníky.</span><span class="sxs-lookup"><span data-stu-id="0387a-217">Let's configure our application so that the `ManageRoles.aspx`, `UsersAndRoles.aspx`, and `CreateUserWizardWithRoles.aspx` pages are only accessible to those users in the Administrators role, while the `RoleBasedAuthorization.aspx` page remains accessible to all visitors.</span></span>

<span data-ttu-id="0387a-218">K tomu, začněte přidáním `Web.config` do souboru `Roles` složky.</span><span class="sxs-lookup"><span data-stu-id="0387a-218">To accomplish this, start by adding a `Web.config` file to the `Roles` folder.</span></span>


<span data-ttu-id="0387a-219">[![Přidá soubor Web.config k adresáři role](role-based-authorization-cs/_static/image8.png)](role-based-authorization-cs/_static/image7.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-219">[![Add a Web.config File to the Roles directory](role-based-authorization-cs/_static/image8.png)](role-based-authorization-cs/_static/image7.png)</span></span>

<span data-ttu-id="0387a-220">**Obrázek 3**: přidání `Web.config` do souboru `Roles` adresáře ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image9.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-220">**Figure 3**: Add a `Web.config` File to the `Roles` directory  ([Click to view full-size image](role-based-authorization-cs/_static/image9.png))</span></span>


<span data-ttu-id="0387a-221">Dál přidejte následující kód konfigurace k `Web.config`:</span><span class="sxs-lookup"><span data-stu-id="0387a-221">Next, add the following configuration markup to `Web.config`:</span></span>

[!code-xml[Main](role-based-authorization-cs/samples/sample3.xml)]

<span data-ttu-id="0387a-222">`<authorization>` Element v `<system.web>` části označuje, že jenom uživatelé v roli správce může přístup k prostředkům technologie ASP.NET v `Roles` adresáře.</span><span class="sxs-lookup"><span data-stu-id="0387a-222">The `<authorization>` element in the `<system.web>` section indicates that only users in the Administrators role may access the ASP.NET resources in the `Roles` directory.</span></span> <span data-ttu-id="0387a-223">`<location>` Element definuje alternativní sadu autorizačních pravidel adres URL pro `RoleBasedAuthorization.aspx` stránky, umožňuje všem uživatelům najdete na stránce.</span><span class="sxs-lookup"><span data-stu-id="0387a-223">The `<location>` element defines an alternate set of URL authorization rules for the `RoleBasedAuthorization.aspx` page, allowing all users to visit the page.</span></span>

<span data-ttu-id="0387a-224">Po uložení změn do `Web.config`, přihlášení jako uživatel, který se nenachází v roli správce a potom se pokusíte navštíví některý z chráněných stránky.</span><span class="sxs-lookup"><span data-stu-id="0387a-224">After saving your changes to `Web.config`, log in as a user that is not in the Administrators role and then try to visit one of the protected pages.</span></span> <span data-ttu-id="0387a-225">`UrlAuthorizationModule` Zjistí, že nemáte oprávnění k navštívení požadovaný prostředek; v důsledku toho `FormsAuthenticationModule` vás přesměruje na přihlašovací stránku.</span><span class="sxs-lookup"><span data-stu-id="0387a-225">The `UrlAuthorizationModule` will detect that you do not have permission to visit the requested resource; consequently, the `FormsAuthenticationModule` will redirect you to the login page.</span></span> <span data-ttu-id="0387a-226">Na přihlašovací stránku vás pak přesměruje na `UnauthorizedAccess.aspx` stránce (viz obrázek 4).</span><span class="sxs-lookup"><span data-stu-id="0387a-226">The login page will then redirect you to the `UnauthorizedAccess.aspx` page (see Figure 4).</span></span> <span data-ttu-id="0387a-227">Tento poslední přesměrování z přihlašovací stránky k `UnauthorizedAccess.aspx` k tomu dochází kvůli kód jsme přidali na přihlašovací stránku v kroku 2 <a id="_msoanchor_7"> </a> [ *autorizace na základě uživatele* ](../membership/user-based-authorization-cs.md) kurzu.</span><span class="sxs-lookup"><span data-stu-id="0387a-227">This final redirect from the login page to `UnauthorizedAccess.aspx` occurs because of code we added to the login page in Step 2 of the <a id="_msoanchor_7"></a>[*User-Based Authorization*](../membership/user-based-authorization-cs.md) tutorial.</span></span> <span data-ttu-id="0387a-228">Na konkrétní stránku pro přihlášení automaticky přesměruje všem ověřeným uživatelům `UnauthorizedAccess.aspx` Pokud řetězec dotazu obsahuje `ReturnUrl` parametru, jako tento parametr určuje, že uživatel na přihlašovací stránku po pokusu o zobrazení stránky mu nebyla dostali oprávnění k zobrazení.</span><span class="sxs-lookup"><span data-stu-id="0387a-228">In particular, the login page automatically redirects any authenticated user to `UnauthorizedAccess.aspx` if the querystring contains a `ReturnUrl` parameter, as this parameter indicates that the user arrived at the login page after attempting to view a page he was not authorized to view.</span></span>


<span data-ttu-id="0387a-229">[![Chráněné stránky lze zobrazit pouze uživatele v roli správce](role-based-authorization-cs/_static/image11.png)](role-based-authorization-cs/_static/image10.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-229">[![Only Users in the Administrators Role Can View the Protected Pages](role-based-authorization-cs/_static/image11.png)](role-based-authorization-cs/_static/image10.png)</span></span>

<span data-ttu-id="0387a-230">**Obrázek 4**: pouze uživatelé v roli správce můžete zobrazit na stránkách chráněné ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image12.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-230">**Figure 4**: Only Users in the Administrators Role Can View the Protected Pages  ([Click to view full-size image](role-based-authorization-cs/_static/image12.png))</span></span>


<span data-ttu-id="0387a-231">Odhlásit a potom se přihlaste jako uživatel, který je v roli správce.</span><span class="sxs-lookup"><span data-stu-id="0387a-231">Log off and then log in as a user that is in the Administrators role.</span></span> <span data-ttu-id="0387a-232">Nyní měli být schopní zobrazit tři chráněné stránky.</span><span class="sxs-lookup"><span data-stu-id="0387a-232">Now you should be able to view the three protected pages.</span></span>


<span data-ttu-id="0387a-233">[![Tito najdete UsersAndRoles.aspx stránky protože se v roli správce](role-based-authorization-cs/_static/image14.png)](role-based-authorization-cs/_static/image13.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-233">[![Tito Can Visit the UsersAndRoles.aspx Page Because He is in the Administrators Role](role-based-authorization-cs/_static/image14.png)](role-based-authorization-cs/_static/image13.png)</span></span>

<span data-ttu-id="0387a-234">**Obrázek 5**: Tito najdete `UsersAndRoles.aspx` stránce vzhledem k tomu, že je v roli správce ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image15.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-234">**Figure 5**: Tito Can Visit the `UsersAndRoles.aspx` Page Because He is in the Administrators Role  ([Click to view full-size image](role-based-authorization-cs/_static/image15.png))</span></span>


> [!NOTE]
> <span data-ttu-id="0387a-235">Při zadávání autorizačních pravidel adres URL – rolí nebo uživatelů – je důležité mít na paměti, že pravidla jsou analyzovaných jeden po druhém, shora dolů.</span><span class="sxs-lookup"><span data-stu-id="0387a-235">When specifying URL authorization rules – for roles or users – it is important to keep in mind that the rules are analyzed one at a time, from the top down.</span></span> <span data-ttu-id="0387a-236">Jakmile je nalezena shoda, je uživatel povolen nebo odepřen přístup, podle toho, pokud v byla nalezena shoda `<allow>` nebo `<deny>` elementu.</span><span class="sxs-lookup"><span data-stu-id="0387a-236">As soon as a match is found, the user is granted or denied access, depending on if the match was found in an `<allow>` or `<deny>` element.</span></span> <span data-ttu-id="0387a-237">**Pokud není nalezena žádná shoda, uživateli je udělen přístup.**</span><span class="sxs-lookup"><span data-stu-id="0387a-237">**If no match is found, the user is granted access.**</span></span> <span data-ttu-id="0387a-238">V důsledku toho, pokud chcete omezit přístup k jedné nebo víc uživatelských účtů, je nezbytné použít `<deny>` element jako posledním prvkem v konfiguraci ověřování adresy URL.</span><span class="sxs-lookup"><span data-stu-id="0387a-238">Consequently, if you want to restrict access to one or more user accounts, it is imperative that you use a `<deny>` element as the last element in the URL authorization configuration.</span></span> <span data-ttu-id="0387a-239">**Pokud nebudou obsahovat vaše autorizačních pravidel adres URL**`<deny>`**elementu, všichni uživatelé bude udělen přístup.**</span><span class="sxs-lookup"><span data-stu-id="0387a-239">**If your URL authorization rules do not include a**`<deny>`**element, all users will be granted access.**</span></span> <span data-ttu-id="0387a-240">Podrobnější informace o tom, jak se analyzují autorizačních pravidel adres URL, najdete v části zpět "podívejte se na to, jak `UrlAuthorizationModule` používá pravidla autorizace udělit nebo odepřít přístup" oddílu <a id="_msoanchor_8"> </a> [  *Autorizace uživatele na základě* ](../membership/user-based-authorization-cs.md) kurzu.</span><span class="sxs-lookup"><span data-stu-id="0387a-240">For a more thorough discussion on how the URL authorization rules are analyzed, refer back to the "A Look at How the `UrlAuthorizationModule` Uses the Authorization Rules to Grant or Deny Access" section of the <a id="_msoanchor_8"></a>[*User-Based Authorization*](../membership/user-based-authorization-cs.md) tutorial.</span></span>


## <a name="step-2-limiting-functionality-based-on-the-currently-logged-in-users-roles"></a><span data-ttu-id="0387a-241">Krok 2: Omezení funkce na základě rolí aktuálně přihlášeného uživatele</span><span class="sxs-lookup"><span data-stu-id="0387a-241">Step 2: Limiting Functionality Based On the Currently Logged In User's Roles</span></span>

<span data-ttu-id="0387a-242">Adresa URL autorizace díky usnadňují zadejte hrubým autorizační pravidla tímto stavem jaké identity jsou povoleny a které z nich je odepřeno zobrazení konkrétní stránky (nebo všechny stránky ve složce a jejích podsložkách).</span><span class="sxs-lookup"><span data-stu-id="0387a-242">URL authorization makes it easy to specify coarse authorization rules that state what identities are permitted and which ones are denied from viewing a particular page (or all pages in a folder and its subfolders).</span></span> <span data-ttu-id="0387a-243">Ale v některých případech může Chceme všem uživatelům najdete stránce, ale omezit tak jeho funkčnost stránky na základě rolí hostujícími uživatelů.</span><span class="sxs-lookup"><span data-stu-id="0387a-243">However, in certain cases we may want to allow all users to visit a page, but limit the page's functionality based on the visiting user's roles.</span></span> <span data-ttu-id="0387a-244">To může mít za následek zobrazení nebo skrytí dat na základě role uživatele, nebo nabídky další funkce pro uživatele, kteří patří do určité role.</span><span class="sxs-lookup"><span data-stu-id="0387a-244">This may entail showing or hiding data based on the user's role, or offering additional functionality to users that belong to a particular role.</span></span>

<span data-ttu-id="0387a-245">Tato pravidla autorizace na základě rolí spočívá v jemné dá implementovat buď deklarativně nebo prostřednictvím kódu programu (nebo prostřednictvím některé kombinaci obou).</span><span class="sxs-lookup"><span data-stu-id="0387a-245">Such fine grain role-based authorization rules can be implemented either declaratively or programmatically (or through some combination of the two).</span></span> <span data-ttu-id="0387a-246">V další části jsme se zobrazí jak implementovat deklarativní spočívá v jemné autorizace prostřednictvím ovládacího prvku LoginView.</span><span class="sxs-lookup"><span data-stu-id="0387a-246">In the next section we will see how to implement declarative fine grain authorization via the LoginView control.</span></span> <span data-ttu-id="0387a-247">Následující, se podíváme na programový techniky.</span><span class="sxs-lookup"><span data-stu-id="0387a-247">Following that, we will explore programmatic techniques.</span></span> <span data-ttu-id="0387a-248">Podíváme můžete na použití spočívá v jemné autorizační pravidla, ale nejprve musíme vytvořit stránku, jejichž funkce závisí na roli uživatele návštěvou ho.</span><span class="sxs-lookup"><span data-stu-id="0387a-248">Before we can look at applying fine grain authorization rules, however, we first need to create a page whose functionality depends on the role of the user visiting it.</span></span>

<span data-ttu-id="0387a-249">Umožňuje vytvořit stránku, která obsahuje seznam všech uživatelských účtů v systému v GridView.</span><span class="sxs-lookup"><span data-stu-id="0387a-249">Let's create a page that lists all of the user accounts in the system in a GridView.</span></span> <span data-ttu-id="0387a-250">GridView bude zahrnovat uživatelské jméno, e-mailovou adresu, datum posledního přihlášení a komentáře o uživateli jednotlivých uživatelů.</span><span class="sxs-lookup"><span data-stu-id="0387a-250">The GridView will include each user's username, email address, last login date, and comments about the user.</span></span> <span data-ttu-id="0387a-251">Kromě zobrazení informací o jednotlivých uživatelů, budou GridView zahrnují upravit a odstranit možnosti.</span><span class="sxs-lookup"><span data-stu-id="0387a-251">In addition to displaying each user's information, the GridView will include edit and delete capabilities.</span></span> <span data-ttu-id="0387a-252">Původně jsme vytvoříte tuto stránku s upravit a odstranit funkce je k dispozici pro všechny uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-252">We will initially create this page with the edit and delete functionality available to all users.</span></span> <span data-ttu-id="0387a-253">V části "Použití LoginView řídicí" a "Prostřednictvím kódu programu omezení funkce" jsme se zobrazí postup povolení nebo zakázání tyto funkce na základě role uživatele, který web navštíví.</span><span class="sxs-lookup"><span data-stu-id="0387a-253">In the "Using the LoginView Control" and "Programmatically Limiting Functionality" sections we will see how to enable or disable these features based on the visiting user's role.</span></span>

> [!NOTE]
> <span data-ttu-id="0387a-254">Stránka ASP.NET, které jsme se chystáte vytvořit pomocí ovládacího prvku GridView zobrazí uživatelské účty.</span><span class="sxs-lookup"><span data-stu-id="0387a-254">The ASP.NET page we are about to build uses a GridView control to display the user accounts.</span></span> <span data-ttu-id="0387a-255">Od tohoto kurzu, které řady se zaměřuje na ověřování pomocí formulářů, ověřování, uživatelské účty a rolí nechci se příliš mnoho času hovoříte o vnitřního chodu ovládacího prvku GridView.</span><span class="sxs-lookup"><span data-stu-id="0387a-255">Since this tutorial series focuses on forms authentication, authorization, user accounts, and roles, I do not want to spend too much time discussing the inner workings of the GridView control.</span></span> <span data-ttu-id="0387a-256">Při tomto kurzu obsahuje konkrétní podrobné pokyny pro nastavení této stránky, není pustíte do podrobnosti proč byly provedeny některé možnosti, nebo konkrétní vlastnosti vliv mají na vykresleném výstupu.</span><span class="sxs-lookup"><span data-stu-id="0387a-256">While this tutorial provides specific step-by-step instructions for setting up this page, it does not delve into the details of why certain choices were made, or what effect particular properties have on the rendered output.</span></span> <span data-ttu-id="0387a-257">Pro důkladné posouzení ovládacího prvku GridView, podívejte se na Moje *[práci s daty v technologii ASP.NET 2.0](../../data-access/index.md)* kurz řady.</span><span class="sxs-lookup"><span data-stu-id="0387a-257">For a thorough examination of the GridView control, check out my *[Working with Data in ASP.NET 2.0](../../data-access/index.md)* tutorial series.</span></span>


<span data-ttu-id="0387a-258">Začněte otevřením `RoleBasedAuthorization.aspx` stránku `Roles` složky.</span><span class="sxs-lookup"><span data-stu-id="0387a-258">Start by opening the `RoleBasedAuthorization.aspx` page in the `Roles` folder.</span></span> <span data-ttu-id="0387a-259">Přetáhněte GridView ze stránky na Designer a sadu jeho `ID` k `UserGrid`.</span><span class="sxs-lookup"><span data-stu-id="0387a-259">Drag a GridView from the page onto the Designer and set its `ID` to `UserGrid`.</span></span> <span data-ttu-id="0387a-260">Za chvíli jsme psát kód, který volá `Membership.GetAllUsers` metoda a sváže výsledná `MembershipUserCollection` objekt, který chcete GridView.</span><span class="sxs-lookup"><span data-stu-id="0387a-260">In a moment we will write code that calls the `Membership.GetAllUsers` method and binds the resulting `MembershipUserCollection` object to the GridView.</span></span> <span data-ttu-id="0387a-261">`MembershipUserCollection` Obsahuje `MembershipUser` objekt pro každý uživatelský účet v systému. `MembershipUser` objekty mají vlastnosti, například `UserName`, `Email`, `LastLoginDate`, a tak dále.</span><span class="sxs-lookup"><span data-stu-id="0387a-261">The `MembershipUserCollection` contains a `MembershipUser` object for each user account in the system; `MembershipUser` objects have properties like `UserName`, `Email`, `LastLoginDate`, and so forth.</span></span>

<span data-ttu-id="0387a-262">Před jsme napsat kód, který váže uživatelské účty do mřížky, nejdříve definujme prvku GridView pole.</span><span class="sxs-lookup"><span data-stu-id="0387a-262">Before we write the code that binds the user accounts to the grid, let's first define the GridView's fields.</span></span> <span data-ttu-id="0387a-263">Z prvku GridView inteligentních značek, klikněte na odkaz "Upravit sloupce" Spustit dialogové okno pole (viz obrázek 6).</span><span class="sxs-lookup"><span data-stu-id="0387a-263">From the GridView's Smart Tag, click the "Edit Columns" link to launch the Fields dialog box (see Figure 6).</span></span> <span data-ttu-id="0387a-264">Z tohoto místa zrušte zaškrtnutí políčka "automaticky generovat pole" v levém dolním rohu.</span><span class="sxs-lookup"><span data-stu-id="0387a-264">From here, uncheck the "Auto-generate fields" checkbox in the lower left corner.</span></span> <span data-ttu-id="0387a-265">Vzhledem k tomu, že má být tato rutina GridView zahrnují úpravy a odstraňování možnosti, přidání CommandField a nastavení jeho `ShowEditButton` a `ShowDeleteButton` vlastnosti na hodnotu True.</span><span class="sxs-lookup"><span data-stu-id="0387a-265">Since we want this GridView to include editing and deleting capabilities, add a CommandField and set its `ShowEditButton` and `ShowDeleteButton` properties to True.</span></span> <span data-ttu-id="0387a-266">V dalším kroku přidejte čtyři pole pro zobrazení `UserName`, `Email`, `LastLoginDate`, a `Comment` vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="0387a-266">Next, add four fields for displaying the `UserName`, `Email`, `LastLoginDate`, and `Comment` properties.</span></span> <span data-ttu-id="0387a-267">Použít BoundField pro dvě vlastnosti jen pro čtení (`UserName` a `LastLoginDate`) a TemplateFields pro dvě upravitelné pole (`Email` a `Comment`).</span><span class="sxs-lookup"><span data-stu-id="0387a-267">Use a BoundField for the two read-only properties (`UserName` and `LastLoginDate`) and TemplateFields for the two editable fields (`Email` and `Comment`).</span></span>

<span data-ttu-id="0387a-268">Mít první zobrazení BoundField `UserName` vlastnost; nastavte její `HeaderText` a `DataField` vlastnosti, které chcete "UserName".</span><span class="sxs-lookup"><span data-stu-id="0387a-268">Have the first BoundField display the `UserName` property; set its `HeaderText` and `DataField` properties to "UserName".</span></span> <span data-ttu-id="0387a-269">Toto pole nebude možné upravovat, takže nastavit jeho `ReadOnly` vlastnost na hodnotu True.</span><span class="sxs-lookup"><span data-stu-id="0387a-269">This field will not be editable, so set its `ReadOnly` property to True.</span></span> <span data-ttu-id="0387a-270">Konfigurace `LastLoginDate` BoundField nastavením jeho `HeaderText` "Poslední přihlášení" a jeho `DataField` na "LastLoginDate".</span><span class="sxs-lookup"><span data-stu-id="0387a-270">Configure the `LastLoginDate` BoundField by setting its `HeaderText` to "Last Login" and its `DataField` to "LastLoginDate".</span></span> <span data-ttu-id="0387a-271">Pojďme formátu výstup této BoundField tak, aby se zobrazí jenom datum (namísto datum a čas).</span><span class="sxs-lookup"><span data-stu-id="0387a-271">Let's format the output of this BoundField so that just the date is displayed (instead of the date and time).</span></span> <span data-ttu-id="0387a-272">Chcete-li dosáhnout, nastavte tento BoundField `HtmlEncode` vlastnost na hodnotu False a jeho `DataFormatString` vlastnost "{0: d}".</span><span class="sxs-lookup"><span data-stu-id="0387a-272">To accomplish this, set this BoundField's `HtmlEncode` property to False and its `DataFormatString` property to "{0:d}".</span></span> <span data-ttu-id="0387a-273">Také nastavit `ReadOnly` vlastnost na hodnotu True.</span><span class="sxs-lookup"><span data-stu-id="0387a-273">Also set the `ReadOnly` property to True.</span></span>

<span data-ttu-id="0387a-274">Nastavte `HeaderText` vlastnosti dvě TemplateFields "E-mailu" a "Komentář".</span><span class="sxs-lookup"><span data-stu-id="0387a-274">Set the `HeaderText` properties of the two TemplateFields to "Email" and "Comment".</span></span>


<span data-ttu-id="0387a-275">[![Rutina GridView na pole můžete nakonfigurovat přes dialogové okno pole](role-based-authorization-cs/_static/image17.png)](role-based-authorization-cs/_static/image16.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-275">[![The GridView's Fields Can Be Configured Through the Fields Dialog Box](role-based-authorization-cs/_static/image17.png)](role-based-authorization-cs/_static/image16.png)</span></span>

<span data-ttu-id="0387a-276">**Obrázek 6**: The GridView pole může být nakonfigurovaný prostřednictvím dialogové okno pole ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image18.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-276">**Figure 6**: The GridView's Fields Can Be Configured Through the Fields Dialog Box  ([Click to view full-size image](role-based-authorization-cs/_static/image18.png))</span></span>


<span data-ttu-id="0387a-277">Nyní potřebujeme k definování `ItemTemplate` a `EditItemTemplate` "E-mailu" a "Komentář" TemplateFields.</span><span class="sxs-lookup"><span data-stu-id="0387a-277">We now need to define the `ItemTemplate` and `EditItemTemplate` for the "Email" and "Comment" TemplateFields.</span></span> <span data-ttu-id="0387a-278">Přidání ovládacího prvku popisek pro každý z `ItemTemplate` s a vazby jejich `Text` vlastnosti, které chcete `Email` a `Comment` vlastnosti, v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="0387a-278">Add a Label Web control to each of the `ItemTemplate` s and bind their `Text` properties to the `Email` and `Comment` properties, respectively.</span></span>

<span data-ttu-id="0387a-279">Pro "E-mailu" TemplateField, přidejte textové pole s názvem `Email` k jeho `EditItemTemplate` a vytvořte vazbu jeho `Text` vlastnost, která má `Email` vlastnost pomocí obousměrné vazby dat.</span><span class="sxs-lookup"><span data-stu-id="0387a-279">For the "Email" TemplateField, add a TextBox named `Email` to its `EditItemTemplate` and bind its `Text` property to the `Email` property using two-way databinding.</span></span> <span data-ttu-id="0387a-280">Přidání RequiredFieldValidator a RegularExpressionValidator k `EditItemTemplate` zajistit, že návštěvník úpravy vlastnost e-mailu zadal platný e-mailovou adresu.</span><span class="sxs-lookup"><span data-stu-id="0387a-280">Add a RequiredFieldValidator and RegularExpressionValidator to the `EditItemTemplate` to ensure that a visitor editing the Email property has entered a valid email address.</span></span> <span data-ttu-id="0387a-281">Pro "Komentář" TemplateField, přidejte Víceřádkový textové pole s názvem `Comment` k jeho `EditItemTemplate`.</span><span class="sxs-lookup"><span data-stu-id="0387a-281">For the "Comment" TemplateField, add a multi-line TextBox named `Comment` to its `EditItemTemplate`.</span></span> <span data-ttu-id="0387a-282">Nastavit textového pole `Columns` a `Rows` vlastností 40 a 4, v uvedeném pořadí a pak vytvořte vazbu jeho `Text` vlastnost, která má `Comment` vlastnost pomocí obousměrné vazby dat.</span><span class="sxs-lookup"><span data-stu-id="0387a-282">Set the TextBox's `Columns` and `Rows` properties to 40 and 4, respectively, and then bind its `Text` property to the `Comment` property using two-way databinding.</span></span>

<span data-ttu-id="0387a-283">Po dokončení konfigurace těchto TemplateFields, jejich deklarativní značek by měl vypadat takto:</span><span class="sxs-lookup"><span data-stu-id="0387a-283">After configuring these TemplateFields, their declarative markup should look similar to the following:</span></span>

[!code-aspx[Main](role-based-authorization-cs/samples/sample4.aspx)]

<span data-ttu-id="0387a-284">Při úpravě nebo odstranění uživatelského účtu, potřebujeme vědět, že uživatel `UserName` hodnotu vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="0387a-284">When editing or deleting a user account we will need to know that user's `UserName` property value.</span></span> <span data-ttu-id="0387a-285">Nastavte GridView `DataKeyNames` vlastnost "UserName" tak, aby tyto informace jsou k dispozici prostřednictvím prvku GridView `DataKeys` kolekce.</span><span class="sxs-lookup"><span data-stu-id="0387a-285">Set the GridView's `DataKeyNames` property to "UserName" so that this information is available through the GridView's `DataKeys` collection.</span></span>

<span data-ttu-id="0387a-286">Nakonec přidejte ValidationSummary ovládacího prvku na stránku a nastavit jeho `ShowMessageBox` vlastnost na hodnotu True a jeho `ShowSummary` vlastnost na hodnotu False.</span><span class="sxs-lookup"><span data-stu-id="0387a-286">Finally, add a ValidationSummary control to the page and set its `ShowMessageBox` property to True and its `ShowSummary` property to False.</span></span> <span data-ttu-id="0387a-287">S těmito nastaveními ValidationSummary zobrazí upozornění na straně klienta, pokud se uživatel pokusí upravit uživatelský účet s e-mailovou adresu chybí nebo je neplatný.</span><span class="sxs-lookup"><span data-stu-id="0387a-287">With these settings, the ValidationSummary will display a client-side alert if the user attempts to edit a user account with a missing or invalid email address.</span></span>

[!code-aspx[Main](role-based-authorization-cs/samples/sample5.aspx)]

<span data-ttu-id="0387a-288">Nyní jsme dokončili deklarativní tuto stránku.</span><span class="sxs-lookup"><span data-stu-id="0387a-288">We have now completed this page's declarative markup.</span></span> <span data-ttu-id="0387a-289">Naše dalším krokem je vytvoření vazby sadu uživatelské účty na GridView.</span><span class="sxs-lookup"><span data-stu-id="0387a-289">Our next task is to bind the set of user accounts to the GridView.</span></span> <span data-ttu-id="0387a-290">Přidejte metodu s názvem `BindUserGrid` k `RoleBasedAuthorization.aspx` třídy kódu stránky, která se váže `MembershipUserCollection` vrácený `Membership.GetAllUsers` k `UserGrid` GridView.</span><span class="sxs-lookup"><span data-stu-id="0387a-290">Add a method named `BindUserGrid` to the `RoleBasedAuthorization.aspx` page's code-behind class that binds the `MembershipUserCollection` returned by `Membership.GetAllUsers` to the `UserGrid` GridView.</span></span> <span data-ttu-id="0387a-291">Volat tuto metodu z `Page_Load` obslužné rutiny události při první návštěvě stránky.</span><span class="sxs-lookup"><span data-stu-id="0387a-291">Call this method from the `Page_Load` event handler on the first page visit.</span></span>

[!code-csharp[Main](role-based-authorization-cs/samples/sample6.cs)]

<span data-ttu-id="0387a-292">S tímto kódem na místě najdete na stránce prostřednictvím prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="0387a-292">With this code in place, visit the page through a browser.</span></span> <span data-ttu-id="0387a-293">Jak je vidět na obrázku 7, měli byste vidět GridView výpis informace o všechny uživatelské účty v systému.</span><span class="sxs-lookup"><span data-stu-id="0387a-293">As Figure 7 shows, you should see a GridView listing information about each user account in the system.</span></span>


<span data-ttu-id="0387a-294">[![Rutina UserGrid GridView uvádí informace o jednotlivých uživatelů v systému](role-based-authorization-cs/_static/image20.png)](role-based-authorization-cs/_static/image19.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-294">[![The UserGrid GridView Lists Information About Each User in the System](role-based-authorization-cs/_static/image20.png)](role-based-authorization-cs/_static/image19.png)</span></span>

<span data-ttu-id="0387a-295">**Obrázek 7**: `UserGrid` GridView uvádí informace o každý uživatel v systému ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image21.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-295">**Figure 7**: The `UserGrid` GridView Lists Information About Each User in the System  ([Click to view full-size image](role-based-authorization-cs/_static/image21.png))</span></span>


> [!NOTE]
> <span data-ttu-id="0387a-296">`UserGrid` GridView obsahuje seznam všech uživatelů v nestránkovaného rozhraní.</span><span class="sxs-lookup"><span data-stu-id="0387a-296">The `UserGrid` GridView lists all of the users in a non-paged interface.</span></span> <span data-ttu-id="0387a-297">Toto rozhraní jednoduché mřížky není vhodný pro scénáře, kde je několik desítek nebo více uživatelů.</span><span class="sxs-lookup"><span data-stu-id="0387a-297">This simple grid interface is not suitable for scenarios where there are several dozen or more users.</span></span> <span data-ttu-id="0387a-298">Jednou z možností je konfigurovat GridView pro povolení stránkování.</span><span class="sxs-lookup"><span data-stu-id="0387a-298">One option is to configure the GridView to enable paging.</span></span> <span data-ttu-id="0387a-299">`Membership.GetAllUsers` Metoda má dva přetížení: ten, který přijímá žádné vstupní parametry a vrátí všechny uživatele a ten, který přebírá celočíselné hodnoty pro index stránky a velikost stránky a vrátí pouze zadané podmnožině uživatelů.</span><span class="sxs-lookup"><span data-stu-id="0387a-299">The `Membership.GetAllUsers` method has two overloads: one that accepts no input parameters and returns all of the users and one that takes in integer values for the page index and page size, and returns only the specified subset of the users.</span></span> <span data-ttu-id="0387a-300">Druhý přetížení umožňuje efektivněji stránky do uživatele vzhledem k tomu, že se vrátí jenom přesné podmnožině uživatelské účty místo *všechny* z nich.</span><span class="sxs-lookup"><span data-stu-id="0387a-300">The second overload can be used to more efficiently page through the users since it returns just the precise subset of user accounts rather than *all* of them.</span></span> <span data-ttu-id="0387a-301">Pokud máte tisíce uživatelské účty, můžete zvážit rozhraní založené na filtr, který se zobrazí pouze uživatelům, jejichž uživatelské jméno začíná znakem vybrané pro instanci.</span><span class="sxs-lookup"><span data-stu-id="0387a-301">If you have thousands of user accounts, you might want to consider a filter-based interface, one that only shows those users whose UserName begins with a selected character, for instance.</span></span> <span data-ttu-id="0387a-302">[ `Membership.FindUsersByName method` ](https://msdn.microsoft.com/library/system.web.security.membership.findusersbyname.aspx) Je ideální pro vytvoření filtru na základě uživatelské rozhraní.</span><span class="sxs-lookup"><span data-stu-id="0387a-302">The [`Membership.FindUsersByName method`](https://msdn.microsoft.com/library/system.web.security.membership.findusersbyname.aspx) is ideal for building a filter-based user interface.</span></span> <span data-ttu-id="0387a-303">Podíváme se na vytvoření takového rozhraní v budoucnu kurzu.</span><span class="sxs-lookup"><span data-stu-id="0387a-303">We will look at building such an interface in a future tutorial.</span></span>


<span data-ttu-id="0387a-304">Ovládací prvek GridView nabízí integrovanou úpravy a odstraňování podpory, když je ovládací prvek vázán na ovládací prvek zdroje dat správně nakonfigurované, například SqlDataSource nebo ObjectDataSource.</span><span class="sxs-lookup"><span data-stu-id="0387a-304">The GridView control offers built-in editing and deleting support when the control is bound to a properly configured data source control, such as the SqlDataSource or ObjectDataSource.</span></span> <span data-ttu-id="0387a-305">`UserGrid` GridView, má však jeho data prostřednictvím kódu programu vázaná; proto jsme musíte napsat kód pro provést tyto dvě úlohy.</span><span class="sxs-lookup"><span data-stu-id="0387a-305">The `UserGrid` GridView, however, has its data programmatically bound; therefore, we must write code to perform these two tasks.</span></span> <span data-ttu-id="0387a-306">Konkrétně budeme muset vytváření obslužných rutin událostí pro prvku GridView `RowEditing`, `RowCancelingEdit`, `RowUpdating`, a `RowDeleting` události, které jsou aktivována, jestliže návštěvník klepne GridView úpravy, zrušit, aktualizace nebo odstranění tlačítka.</span><span class="sxs-lookup"><span data-stu-id="0387a-306">In particular, we will need to create event handlers for the GridView's `RowEditing`, `RowCancelingEdit`, `RowUpdating`, and `RowDeleting` events, which are fired when a visitor clicks the GridView's Edit, Cancel, Update, or Delete buttons.</span></span>

<span data-ttu-id="0387a-307">Začněte tím, že vytváření obslužných rutin událostí pro prvku GridView `RowEditing`, `RowCancelingEdit`, a `RowUpdating` události a poté přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="0387a-307">Start by creating the event handlers for the GridView's `RowEditing`, `RowCancelingEdit`, and `RowUpdating` events and then add the following code:</span></span>

[!code-csharp[Main](role-based-authorization-cs/samples/sample7.cs)]

<span data-ttu-id="0387a-308">`RowEditing` a `RowCancelingEdit` obslužné rutiny událostí jednoduše nastavit prvku GridView `EditIndex` vlastnost a potom obnovení vazby seznam uživatelské účty k mřížce.</span><span class="sxs-lookup"><span data-stu-id="0387a-308">The `RowEditing` and `RowCancelingEdit` event handlers simply set the GridView's `EditIndex` property and then rebind the list of user accounts to the grid.</span></span> <span data-ttu-id="0387a-309">Probíhá zajímavé věcem `RowUpdating` obslužné rutiny události.</span><span class="sxs-lookup"><span data-stu-id="0387a-309">The interesting stuff happens in the `RowUpdating` event handler.</span></span> <span data-ttu-id="0387a-310">Tuto obslužnou rutinu události spustí tím zajistí, že data platná a pak získá `UserName` hodnotu upravená uživatelského účtu z `DataKeys` kolekce.</span><span class="sxs-lookup"><span data-stu-id="0387a-310">This event handler starts by ensuring that the data is valid and then grabs the `UserName` value of the edited user account from the `DataKeys` collection.</span></span> <span data-ttu-id="0387a-311">`Email` a `Comment` textových polí ve dvou TemplateFields `EditItemTemplate` s jsou pak prostřednictvím kódu programu odkazuje.</span><span class="sxs-lookup"><span data-stu-id="0387a-311">The `Email` and `Comment` TextBoxes in the two TemplateFields' `EditItemTemplate` s are then programmatically referenced.</span></span> <span data-ttu-id="0387a-312">Jejich `Text` vlastnosti obsahovat upravená e-mailovou adresu a komentář.</span><span class="sxs-lookup"><span data-stu-id="0387a-312">Their `Text` properties contain the edited email address and comment.</span></span>

<span data-ttu-id="0387a-313">Aby bylo možné aktualizovat k uživatelskému účtu pomocí rozhraní API členství musíme nejdřív získat informace o uživateli, který provedeme prostřednictvím volání `Membership.GetUser(userName)`.</span><span class="sxs-lookup"><span data-stu-id="0387a-313">In order to update a user account through the Membership API we need to first get the user's information, which we do via a call to `Membership.GetUser(userName)`.</span></span> <span data-ttu-id="0387a-314">Vrácený `MembershipUser` objektu `Email` a `Comment` vlastnosti jsou potom aktualizovány hodnot zadaných do dvou textových polí z rozhraní úpravy.</span><span class="sxs-lookup"><span data-stu-id="0387a-314">The returned `MembershipUser` object's `Email` and `Comment` properties are then updated with the values entered into the two TextBoxes from the editing interface.</span></span> <span data-ttu-id="0387a-315">Nakonec se tyto změny se ukládají s volání [ `Membership.UpdateUser` ](https://msdn.microsoft.com/library/system.web.security.membership.updateuser.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-315">Finally, these modifications are saved with a call to [`Membership.UpdateUser`](https://msdn.microsoft.com/library/system.web.security.membership.updateuser.aspx).</span></span> <span data-ttu-id="0387a-316">`RowUpdating` Obslužné rutiny události dokončí vrácení rutina GridView k jeho předem úpravy rozhraní.</span><span class="sxs-lookup"><span data-stu-id="0387a-316">The `RowUpdating` event handler completes by reverting the GridView to its pre-editing interface.</span></span>

<span data-ttu-id="0387a-317">Dále vytvořte `RowDeleting` obslužné rutiny události a poté přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="0387a-317">Next, create the `RowDeleting` event handler and then add the following code:</span></span>

[!code-csharp[Main](role-based-authorization-cs/samples/sample8.cs)]

<span data-ttu-id="0387a-318">Výše uvedené obslužné rutiny události začne metodou `UserName` hodnotu z prvku GridView `DataKeys` kolekce; tato `UserName` hodnota je předána do třídy členství [ `DeleteUser` metoda](https://msdn.microsoft.com/library/system.web.security.membership.deleteuser.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-318">The above event handler starts by grabbing the `UserName` value from the GridView's `DataKeys` collection; this `UserName` value is then passed into the Membership class's [`DeleteUser` method](https://msdn.microsoft.com/library/system.web.security.membership.deleteuser.aspx).</span></span> <span data-ttu-id="0387a-319">`DeleteUser` Metoda odstraní účet uživatele ze systému, včetně členství v souvisejících dat (například jaké role patří tento uživatel).</span><span class="sxs-lookup"><span data-stu-id="0387a-319">The `DeleteUser` method deletes the user account from the system, including related membership data (such as what roles this user belongs to).</span></span> <span data-ttu-id="0387a-320">Po odstranění uživatele, mřížky `EditIndex` je nastaven na hodnotu -1 (v případě, že uživatel klepl odstranit při další řádek v režimu úprav) a `BindUserGrid` metoda je volána.</span><span class="sxs-lookup"><span data-stu-id="0387a-320">After deleting the user, the grid's `EditIndex` is set to -1 (in case the user clicked Delete while another row was in edit mode) and the `BindUserGrid` method is called.</span></span>

> [!NOTE]
> <span data-ttu-id="0387a-321">Tlačítko Odstranit nevyžaduje žádné řazení potvrzení od uživatele před odstraněním účtu uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-321">The Delete button does not require any sort of confirmation from the user before deleting the user account.</span></span> <span data-ttu-id="0387a-322">I doporučujeme přidat určitou formu potvrzení uživatele k omezit možnost náhodnému odstranění účtu.</span><span class="sxs-lookup"><span data-stu-id="0387a-322">I encourage you to add some form of user confirmation to lessen the chance of an account being accidentally deleted.</span></span> <span data-ttu-id="0387a-323">Jedním z nejjednodušších způsobů, jak akci potvrďte je pomocí dialogového okna potvrdit na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="0387a-323">One of the easiest ways to confirm an action is through a client-side confirm dialog box.</span></span> <span data-ttu-id="0387a-324">Další informace o této technice najdete v tématu [přidání Client-Side potvrzení při odstranění](https://asp.net/learn/data-access/tutorial-42-cs.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-324">For more information on this technique, see [Adding Client-Side Confirmation When Deleting](https://asp.net/learn/data-access/tutorial-42-cs.aspx).</span></span>


<span data-ttu-id="0387a-325">Ověřte, zda tato stránka fungují podle očekávání.</span><span class="sxs-lookup"><span data-stu-id="0387a-325">Verify that this page functions as expected.</span></span> <span data-ttu-id="0387a-326">Nyní byste měli mít upravit kteréhokoli uživatele e-mailovou adresu a komentáře a také odstranit libovolný uživatelský účet.</span><span class="sxs-lookup"><span data-stu-id="0387a-326">You should be able to edit any user's email address and comment, as well as delete any user account.</span></span> <span data-ttu-id="0387a-327">Vzhledem k tomu `RoleBasedAuthorization.aspx` stránka je přístupné všem uživatelům, každý uživatel – i anonymní návštěvníky – najdete na této stránce a upravit a odstranit uživatelské účty!</span><span class="sxs-lookup"><span data-stu-id="0387a-327">Since the `RoleBasedAuthorization.aspx` page is accessible to all users, any user – even anonymous visitors – can visit this page and edit and delete user accounts!</span></span> <span data-ttu-id="0387a-328">Umožňuje aktualizovat tuto stránku tak, že jenom uživatelé v rolích dohledu a správci můžete upravit e-mailovou adresu uživatele a komentářů a pouze správci mohou odstranit uživatelský účet.</span><span class="sxs-lookup"><span data-stu-id="0387a-328">Let's update this page so that only users in the Supervisors and Administrators roles can edit a user's email address and comment, and only Administrators can delete a user account.</span></span>

<span data-ttu-id="0387a-329">V části "Použití LoginView řídicí" zjistí pomocí ovládacího prvku LoginView zobrazíte pokyny k roli uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-329">The "Using the LoginView Control" section looks at using the LoginView control to show instructions specific to the user's role.</span></span> <span data-ttu-id="0387a-330">Pokud uživatel v roli správce navštíví tuto stránku, jsme se zobrazí pokyny o tom, jak upravit a odstranit uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-330">If a person in the Administrators role visits this page, we will show instructions on how to edit and delete users.</span></span> <span data-ttu-id="0387a-331">Pokud uživatel v roli dohledu dosáhne této stránce, jsme se zobrazí pokyny k úpravám uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-331">If a user in the Supervisors role reaches this page, we will show instructions on editing users.</span></span> <span data-ttu-id="0387a-332">A pokud návštěvníka je anonymní, nebo se nenachází v roli dohledu nebo správci, jsme se zobrazí zpráva, která vysvětluje, že se nedají upravit ani odstranit informace o uživatelském účtu.</span><span class="sxs-lookup"><span data-stu-id="0387a-332">And if the visitor is anonymous or is not in either the Supervisors or Administrators role, we will display a message explaining that they cannot edit or delete user account information.</span></span> <span data-ttu-id="0387a-333">V části "Prostřednictvím kódu programu omezení funkce" jsme psát kód, který prostřednictvím kódu programu zobrazí nebo skryje tlačítka Upravit a odstranit, na základě role uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-333">In the "Programmatically Limiting Functionality" section we will write code that programmatically shows or hides the Edit and Delete buttons based on the user's role.</span></span>

### <a name="using-the-loginview-control"></a><span data-ttu-id="0387a-334">Použití ovládacího prvku LoginView</span><span class="sxs-lookup"><span data-stu-id="0387a-334">Using the LoginView Control</span></span>

<span data-ttu-id="0387a-335">Jak jsme viděli v posledních kurzy, ovládacího prvku LoginView je užitečná pro zobrazení různých rozhraní pro ověřený a anonymní uživatele, ale ovládací prvek LoginView lze také zobrazit různých značek na základě rolí uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-335">As we've seen in past tutorials, the LoginView control is useful for displaying different interfaces for authenticated and anonymous users, but the LoginView control can also be used to display different markup based on the user's roles.</span></span> <span data-ttu-id="0387a-336">K zobrazení různých pokynů na základě role uživatele, který web navštíví použijeme LoginView ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="0387a-336">Let's use a LoginView control to display different instructions based on the visiting user's role.</span></span>

<span data-ttu-id="0387a-337">Spuštění přidáním LoginView výše `UserGrid` GridView.</span><span class="sxs-lookup"><span data-stu-id="0387a-337">Start by adding a LoginView above the `UserGrid` GridView.</span></span> <span data-ttu-id="0387a-338">Jak jsme probrali dříve, ovládacího prvku LoginView má dvě předdefinované šablony: `AnonymousTemplate` a `LoggedInTemplate`.</span><span class="sxs-lookup"><span data-stu-id="0387a-338">As we've discussed earlier, the LoginView control has two built-in templates: `AnonymousTemplate` and `LoggedInTemplate`.</span></span> <span data-ttu-id="0387a-339">Zadejte stručný zprávu v obou těchto šablon, které informuje uživatele, že se nedají upravit ani odstranit nějaké informace o uživateli.</span><span class="sxs-lookup"><span data-stu-id="0387a-339">Enter a brief message in both of these templates that informs the user that they cannot edit or delete any user information.</span></span>

[!code-aspx[Main](role-based-authorization-cs/samples/sample9.aspx)]

<span data-ttu-id="0387a-340">Kromě `AnonymousTemplate` a `LoggedInTemplate`, může zahrnovat ovládacího prvku LoginView *kolekci RoleGroups*, které jsou specifické pro roli šablony.</span><span class="sxs-lookup"><span data-stu-id="0387a-340">In addition to the `AnonymousTemplate` and `LoggedInTemplate`, the LoginView control can include *RoleGroups*, which are role-specific templates.</span></span> <span data-ttu-id="0387a-341">Každý RoleGroup obsahuje jednu vlastnost `Roles`, která určuje, jaké role RoleGroup platí pro.</span><span class="sxs-lookup"><span data-stu-id="0387a-341">Each RoleGroup contains a single property, `Roles`, which specifies what roles the RoleGroup applies to.</span></span> <span data-ttu-id="0387a-342">`Roles` Vlastnost lze nastavit do jedné role (např. "Administrators") nebo do čárkami oddělený seznam rolí (např. "Administrators, vedoucí").</span><span class="sxs-lookup"><span data-stu-id="0387a-342">The `Roles` property can be set to a single role (like "Administrators") or to a comma-delimited list of roles (like "Administrators, Supervisors").</span></span>

<span data-ttu-id="0387a-343">Abyste mohli spravovat kolekci RoleGroups, klikněte na odkaz "Upravit kolekci RoleGroups" ze inteligentní značky ovládacího prvku, aby si Editor kolekce RoleGroup.</span><span class="sxs-lookup"><span data-stu-id="0387a-343">To manage the RoleGroups, click the "Edit RoleGroups" link from the control's Smart Tag to bring up the RoleGroup Collection Editor.</span></span> <span data-ttu-id="0387a-344">Přidejte dva nové kolekci RoleGroups.</span><span class="sxs-lookup"><span data-stu-id="0387a-344">Add two new RoleGroups.</span></span> <span data-ttu-id="0387a-345">Nastavit první RoleGroup `Roles` vlastnost a "Administrators" na "Vedoucí" sekundu.</span><span class="sxs-lookup"><span data-stu-id="0387a-345">Set the first RoleGroup's `Roles` property to "Administrators" and the second's to "Supervisors".</span></span>


<span data-ttu-id="0387a-346">[![Správa šablon specifické Role LoginView prostřednictvím Editor kolekce RoleGroup.](role-based-authorization-cs/_static/image23.png)](role-based-authorization-cs/_static/image22.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-346">[![Manage the LoginView's Role-Specific Templates Through the RoleGroup Collection Editor](role-based-authorization-cs/_static/image23.png)](role-based-authorization-cs/_static/image22.png)</span></span>

<span data-ttu-id="0387a-347">**Obrázek 8**: Spravovat LoginView specifickou rolí šablony prostřednictvím Editor kolekce RoleGroup ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image24.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-347">**Figure 8**: Manage the LoginView's Role-Specific Templates Through the RoleGroup Collection Editor  ([Click to view full-size image](role-based-authorization-cs/_static/image24.png))</span></span>


<span data-ttu-id="0387a-348">Kliknutím na OK zavřete Editor kolekce RoleGroup; Tím se aktualizuje LoginView deklarativní zahrnout `<RoleGroups>` část s `<asp:RoleGroup>` podřízený element pro každou RoleGroup definované v editoru kolekce RoleGroup.</span><span class="sxs-lookup"><span data-stu-id="0387a-348">Click OK to close the RoleGroup Collection Editor; this updates the LoginView's declarative markup to include a `<RoleGroups>` section with an `<asp:RoleGroup>` child element for each RoleGroup defined in the RoleGroup Collection Editor.</span></span> <span data-ttu-id="0387a-349">Kromě toho, z rozevírací nabídky "Zobrazení" seznamu v inteligentních značek LoginView – které původně uvedené jenom na `AnonymousTemplate` a `LoggedInTemplate` – nyní zahrnuje přidání kolekci RoleGroups také.</span><span class="sxs-lookup"><span data-stu-id="0387a-349">Furthermore, the "Views" drop-down list in the LoginView's Smart Tag - which initially listed just the `AnonymousTemplate` and `LoggedInTemplate` – now includes the added RoleGroups as well.</span></span>

<span data-ttu-id="0387a-350">Upravte kolekci RoleGroups tak, aby byly zobrazené pokyny o tom, jak upravit uživatelské účty, zatímco uživatelé v roli správce jsou uvedeny pokyny pro úpravy a odstraňování uživatelů v roli dohledu.</span><span class="sxs-lookup"><span data-stu-id="0387a-350">Edit the RoleGroups so that users in the Supervisors role are displayed instructions on how to edit user accounts, while users in the Administrators role are shown instructions for editing and deleting.</span></span> <span data-ttu-id="0387a-351">Po provedení těchto změn, vaše LoginView deklarativní by měl vypadat takto.</span><span class="sxs-lookup"><span data-stu-id="0387a-351">After making these changes, your LoginView's declarative markup should look similar to the following.</span></span>

[!code-aspx[Main](role-based-authorization-cs/samples/sample10.aspx)]

<span data-ttu-id="0387a-352">Po provedení těchto změn, uložte na stránku a potom navštivte prostřednictvím prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="0387a-352">After making these changes, save the page and then visit it through a browser.</span></span> <span data-ttu-id="0387a-353">Nejprve najdete na stránce jako anonymní uživatel.</span><span class="sxs-lookup"><span data-stu-id="0387a-353">First visit the page as an anonymous user.</span></span> <span data-ttu-id="0387a-354">By měla zobrazit zpráva, "nejste přihlášeni k systému.</span><span class="sxs-lookup"><span data-stu-id="0387a-354">You should be shown the message, "You are not logged into the system.</span></span> <span data-ttu-id="0387a-355">Proto se nedají upravit ani odstranit nějaké informace o uživateli."</span><span class="sxs-lookup"><span data-stu-id="0387a-355">Therefore you cannot edit or delete any user information."</span></span> <span data-ttu-id="0387a-356">Pak se přihlaste jako ověřený uživatel, ale ten, který je ani v roli dohledu ani správci.</span><span class="sxs-lookup"><span data-stu-id="0387a-356">Then log in as an authenticated user, but one that is neither in the Supervisors nor Administrators role.</span></span> <span data-ttu-id="0387a-357">Tentokrát měla zobrazit zpráva, "nejste členem role vedoucí nebo správci.</span><span class="sxs-lookup"><span data-stu-id="0387a-357">This time you should see the message, "You are not a member of the Supervisors or Administrators roles.</span></span> <span data-ttu-id="0387a-358">Proto se nedají upravit ani odstranit nějaké informace o uživateli."</span><span class="sxs-lookup"><span data-stu-id="0387a-358">Therefore you cannot edit or delete any user information."</span></span>

<span data-ttu-id="0387a-359">V dalším kroku přihlaste jako uživatel, který je členem role orgánů dohledu.</span><span class="sxs-lookup"><span data-stu-id="0387a-359">Next, log in as a user who is a member of the Supervisors role.</span></span> <span data-ttu-id="0387a-360">Nyní byste měli vidět vedoucí specifickou rolí zprávy (viz obrázek 9).</span><span class="sxs-lookup"><span data-stu-id="0387a-360">This time you should see the Supervisors role-specific message (see Figure 9).</span></span> <span data-ttu-id="0387a-361">A pokud se přihlásit jako uživatel v roli, měli byste vidět správci specifickou rolí zprávy (viz obrázek 10) správců.</span><span class="sxs-lookup"><span data-stu-id="0387a-361">And if you log in as a user in the Administrators role you should see the Administrators role-specific message (see Figure 10).</span></span>


<span data-ttu-id="0387a-362">[![Bruce se zobrazí zprávy specifické pro roli dohledu](role-based-authorization-cs/_static/image26.png)](role-based-authorization-cs/_static/image25.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-362">[![Bruce is Shown the Supervisors Role-Specific Message](role-based-authorization-cs/_static/image26.png)](role-based-authorization-cs/_static/image25.png)</span></span>

<span data-ttu-id="0387a-363">**Obrázek 9**: Bruce se zobrazí zprávy specifické pro roli dohledu ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image27.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-363">**Figure 9**: Bruce is Shown the Supervisors Role-Specific Message  ([Click to view full-size image](role-based-authorization-cs/_static/image27.png))</span></span>


<span data-ttu-id="0387a-364">[![Tito se zobrazí zprávy specifické pro roli správce](role-based-authorization-cs/_static/image29.png)](role-based-authorization-cs/_static/image28.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-364">[![Tito is Shown the Administrators Role-Specific Message](role-based-authorization-cs/_static/image29.png)](role-based-authorization-cs/_static/image28.png)</span></span>

<span data-ttu-id="0387a-365">**Obrázek 10**: Tito se zobrazí zprávy specifické pro roli správce ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image30.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-365">**Figure 10**: Tito is Shown the Administrators Role-Specific Message  ([Click to view full-size image](role-based-authorization-cs/_static/image30.png))</span></span>


<span data-ttu-id="0387a-366">Jako snímky obrazovky v útvarů 9 a 10 zobrazit vykreslí LoginView jenom jedna šablona i v případě, že platí více šablon.</span><span class="sxs-lookup"><span data-stu-id="0387a-366">As the screen shots in Figures 9 and 10 show, the LoginView only renders one template, even if multiple templates apply.</span></span> <span data-ttu-id="0387a-367">Bruce a Tito i přihlášení uživatele, ale LoginView vykreslí pouze odpovídající RoleGroup a ne `LoggedInTemplate`.</span><span class="sxs-lookup"><span data-stu-id="0387a-367">Bruce and Tito are both logged in users, yet the LoginView renders only the matching RoleGroup and not the `LoggedInTemplate`.</span></span> <span data-ttu-id="0387a-368">Kromě toho Tito patří do role správců a vedoucí, ale ovládací prvek LoginView vykreslí šablony specifické role Správci místo vedoucí jeden.</span><span class="sxs-lookup"><span data-stu-id="0387a-368">Moreover, Tito belongs to both the Administrators and Supervisors roles, yet the LoginView control renders the Administrators role-specific template instead of the Supervisors one.</span></span>

<span data-ttu-id="0387a-369">Obrázek 11 znázorňuje používají k určení jakou šablonu k vykreslení ovládacího prvku LoginView pracovního postupu.</span><span class="sxs-lookup"><span data-stu-id="0387a-369">Figure 11 illustrates the workflow used by the LoginView control to determine what template to render.</span></span> <span data-ttu-id="0387a-370">Všimněte si, že pokud je více než jeden zadaný RoleGroup šabloně LoginView vykreslí *první* RoleGroup, který odpovídá.</span><span class="sxs-lookup"><span data-stu-id="0387a-370">Note that if there is more than one RoleGroup specified, the LoginView template renders the *first* RoleGroup that matches.</span></span> <span data-ttu-id="0387a-371">Jinými slovy Pokud jsme měl umístit RoleGroup dohledu jako první RoleGroup a správci jako druhý, pak když Tito navštívili tuto stránku mohl by najdete v části vedoucí zprávy.</span><span class="sxs-lookup"><span data-stu-id="0387a-371">In other words, if we had placed the Supervisors RoleGroup as the first RoleGroup and the Administrators as the second, then when Tito visited this page he would see the Supervisors message.</span></span>


<span data-ttu-id="0387a-372">[![Ovládací prvek LoginView pracovního postupu pro určení, jakou šablonu vykreslování](role-based-authorization-cs/_static/image32.png)](role-based-authorization-cs/_static/image31.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-372">[![The LoginView Control's Workflow for Determining What Template to Render](role-based-authorization-cs/_static/image32.png)](role-based-authorization-cs/_static/image31.png)</span></span>

<span data-ttu-id="0387a-373">**Obrázek 11**: LoginView řízení pracovního postupu pro určení, co šablonu k vykreslení ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image33.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-373">**Figure 11**: The LoginView Control's Workflow for Determining What Template to Render  ([Click to view full-size image](role-based-authorization-cs/_static/image33.png))</span></span>


### <a name="programmatically-limiting-functionality"></a><span data-ttu-id="0387a-374">Prostřednictvím kódu programu omezení funkce</span><span class="sxs-lookup"><span data-stu-id="0387a-374">Programmatically Limiting Functionality</span></span>

<span data-ttu-id="0387a-375">Když tento ovládací prvek LoginView zobrazí různé pokyny na základě role uživatele na stránce, úpravy a tlačítka Storno zůstanou viditelné pro všechny.</span><span class="sxs-lookup"><span data-stu-id="0387a-375">While the LoginView control displays different instructions based on the role of the user visiting the page, the Edit and Cancel buttons remain visible to all.</span></span> <span data-ttu-id="0387a-376">Musíme prostřednictvím kódu programu skrýt tlačítka Upravit a odstranit pro anonymní návštěvníky a uživatelé, kteří jsou v roli dohledu ani správci.</span><span class="sxs-lookup"><span data-stu-id="0387a-376">We need to programmatically hide the Edit and Delete buttons for anonymous visitors and users who are in neither the Supervisors nor Administrators role.</span></span> <span data-ttu-id="0387a-377">Musíme skrýt tlačítko Odstranit pro každého, kdo není správce.</span><span class="sxs-lookup"><span data-stu-id="0387a-377">We need to hide the Delete button for everyone who is not an Administrator.</span></span> <span data-ttu-id="0387a-378">Aby to bylo možné jsme zapíše bit kódu, který prostřednictvím kódu programu odkazuje CommandField upravit a odstranit LinkButtons a nastaví jejich `Visible` vlastnosti, které chcete `false`, v případě potřeby.</span><span class="sxs-lookup"><span data-stu-id="0387a-378">In order to accomplish this we will write a bit of code that programmatically references the CommandField's Edit and Delete LinkButtons and sets their `Visible` properties to `false`, if necessary.</span></span>

<span data-ttu-id="0387a-379">Nejjednodušší způsob, jak programově odkazovat ovládacích prvků v CommandField je nejdřív ho převést na šablonu.</span><span class="sxs-lookup"><span data-stu-id="0387a-379">The easiest way to programmatically reference controls in a CommandField is to first convert it to a template.</span></span> <span data-ttu-id="0387a-380">K tomu, klikněte na odkaz "Upravit sloupce" z prvku GridView inteligentních značek, vyberte CommandField ze seznamu aktuální pole a klikněte na odkaz "Převést toto pole TemplateField".</span><span class="sxs-lookup"><span data-stu-id="0387a-380">To accomplish this, click the "Edit Columns" link from the GridView's Smart Tag, select the CommandField from the list of current fields, and click the "Convert this field into a TemplateField" link.</span></span> <span data-ttu-id="0387a-381">To se změní CommandField TemplateField s `ItemTemplate` a `EditItemTemplate`.</span><span class="sxs-lookup"><span data-stu-id="0387a-381">This turns the CommandField into a TemplateField with an `ItemTemplate` and `EditItemTemplate`.</span></span> <span data-ttu-id="0387a-382">`ItemTemplate` Obsahuje upravit a odstranit LinkButtons při `EditItemTemplate` ve uložený aktualizace a LinkButtons zrušit.</span><span class="sxs-lookup"><span data-stu-id="0387a-382">The `ItemTemplate` contains the Edit and Delete LinkButtons while the `EditItemTemplate` houses the Update and Cancel LinkButtons.</span></span>


<span data-ttu-id="0387a-383">[![Převést CommandField TemplateField](role-based-authorization-cs/_static/image35.png)](role-based-authorization-cs/_static/image34.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-383">[![Convert the CommandField Into a TemplateField](role-based-authorization-cs/_static/image35.png)](role-based-authorization-cs/_static/image34.png)</span></span>

<span data-ttu-id="0387a-384">**Obrázek 12**: převést CommandField do TemplateField ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image36.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-384">**Figure 12**: Convert the CommandField Into a TemplateField  ([Click to view full-size image](role-based-authorization-cs/_static/image36.png))</span></span>


<span data-ttu-id="0387a-385">Aktualizace upravit a odstranit LinkButtons v `ItemTemplate`, nastavení jejich `ID` vlastnosti hodnoty `EditButton` a `DeleteButton`, v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="0387a-385">Update the Edit and Delete LinkButtons in the `ItemTemplate`, setting their `ID` properties to values of `EditButton` and `DeleteButton`, respectively.</span></span>

[!code-aspx[Main](role-based-authorization-cs/samples/sample11.aspx)]

<span data-ttu-id="0387a-386">Vždy, když data vázán na GridView, GridView zobrazí záznamy v jeho `DataSource` vlastnost a vygeneruje odpovídající `GridViewRow` objektu.</span><span class="sxs-lookup"><span data-stu-id="0387a-386">Whenever data is bound to the GridView, the GridView enumerates the records in its `DataSource` property and generates a corresponding `GridViewRow` object.</span></span> <span data-ttu-id="0387a-387">Protože každému `GridViewRow` je vytvořen objekt, `RowCreated` událost je aktivována.</span><span class="sxs-lookup"><span data-stu-id="0387a-387">As each `GridViewRow` object is created, the `RowCreated` event is fired.</span></span> <span data-ttu-id="0387a-388">Chcete-li skrýt tlačítka Upravit a odstranit neoprávnění uživatelé, je potřeba vytvořit obslužnou rutinu události pro tuto událost a programově odkazovat upravit a odstranit LinkButtons nastavení jejich `Visible` vlastnosti odpovídajícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="0387a-388">In order to hide the Edit and Delete buttons for unauthorized users, we need to create an event handler for this event and programmatically reference the Edit and Delete LinkButtons, setting their `Visible` properties accordingly.</span></span>

<span data-ttu-id="0387a-389">Vytvoření obslužné rutiny události `RowCreated` události a poté přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="0387a-389">Create an event handler the `RowCreated` event and then add the following code:</span></span>

[!code-csharp[Main](role-based-authorization-cs/samples/sample12.cs)]

<span data-ttu-id="0387a-390">Mějte na paměti, že `RowCreated` událost aktivuje pro *všechny* GridView řádků, včetně záhlaví, zápatí, pager rozhraní a tak dále.</span><span class="sxs-lookup"><span data-stu-id="0387a-390">Keep in mind that the `RowCreated` event fires for *all* of the GridView rows, including the header, the footer, the pager interface, and so forth.</span></span> <span data-ttu-id="0387a-391">Chceme prostřednictvím kódu programu odkazu upravit a odstranit LinkButtons, pokud jsme se zabývají řádek dat není v režimu úprav, (protože řádek v režimu úprav má aktualizace a zrušit tlačítka místo upravit a odstranit).</span><span class="sxs-lookup"><span data-stu-id="0387a-391">We only want to programmatically reference the Edit and Delete LinkButtons if we are dealing with a data row not in edit mode (since the row in edit mode has Update and Cancel buttons instead of Edit and Delete).</span></span> <span data-ttu-id="0387a-392">Tato kontrola se provádí `if` příkaz.</span><span class="sxs-lookup"><span data-stu-id="0387a-392">This check is handled by the `if` statement.</span></span>

<span data-ttu-id="0387a-393">Pokud jsme se zabývají na řádek dat, který není v režimu úprav, upravit a odstranit LinkButtons odkazují a jejich `Visible` vlastnosti jsou nastaveny na základě na logické hodnoty vrácené `User` objektu `IsInRole(roleName)` metoda.</span><span class="sxs-lookup"><span data-stu-id="0387a-393">If we are dealing with a data row that is not in edit mode, the Edit and Delete LinkButtons are referenced and their `Visible` properties are set based on the Boolean values returned by the `User` object's `IsInRole(roleName)` method.</span></span> <span data-ttu-id="0387a-394">Objekt uživatele odkazuje na objekt zabezpečení vytvořené `RoleManagerModule`; v důsledku toho `IsInRole(roleName)` metoda používá rozhraní API rolí k určení, zda aktuální návštěvníka patří do *roleName*.</span><span class="sxs-lookup"><span data-stu-id="0387a-394">The User object references the principal created by the `RoleManagerModule`; consequently, the `IsInRole(roleName)` method uses the Roles API to determine whether the current visitor belongs to *roleName*.</span></span>

> [!NOTE]
> <span data-ttu-id="0387a-395">Může mít použili jsme třída role přímo, nahrazení volání `User.IsInRole(roleName)` pomocí volání [ `Roles.IsUserInRole(roleName)` metoda](https://msdn.microsoft.com/library/system.web.security.roles.isuserinrole.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-395">We could have used the Roles class directly, replacing the call to `User.IsInRole(roleName)` with a call to the [`Roles.IsUserInRole(roleName)` method](https://msdn.microsoft.com/library/system.web.security.roles.isuserinrole.aspx).</span></span> <span data-ttu-id="0387a-396">Rozhodli používat objekt zabezpečení `IsInRole(roleName)` metoda v tomto příkladu vzhledem k tomu, že je efektivnější než přímo pomocí rozhraní API rolí.</span><span class="sxs-lookup"><span data-stu-id="0387a-396">I decided to use the principal object's `IsInRole(roleName)` method in this example because it is more efficient than using the Roles API directly.</span></span> <span data-ttu-id="0387a-397">V tomto kurzu jsme nakonfigurovali Správce rolí pro ukládání do mezipaměti role uživatele v souboru cookie.</span><span class="sxs-lookup"><span data-stu-id="0387a-397">Earlier in this tutorial we configured the role manager to cache the user's roles in a cookie.</span></span> <span data-ttu-id="0387a-398">Tento soubor cookie data jako do mezipaměti je využít pouze při objektu zabezpečení `IsInRole(roleName)` metoda je volána; přímé volání rozhraní API rolí vždy zahrnovat cestu k úložišti role.</span><span class="sxs-lookup"><span data-stu-id="0387a-398">This cached cookie data is only utilized when the principal's `IsInRole(roleName)` method is called; direct calls to the Roles API always involve a trip to the role store.</span></span> <span data-ttu-id="0387a-399">I v případě, že role se neukládají do mezipaměti v souboru cookie, volání objekt zabezpečení `IsInRole(roleName)` metoda je obvykle je efektivnější, protože když je volána pro poprvé během požadavku se ukládá do mezipaměti výsledky.</span><span class="sxs-lookup"><span data-stu-id="0387a-399">Even if roles are not cached in a cookie, calling the principal object's `IsInRole(roleName)` method is usually more efficient because when it is called for the first time during a request it caches the results.</span></span> <span data-ttu-id="0387a-400">Rozhraní API rolí na druhé straně neprovede žádné ukládání do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="0387a-400">The Roles API, on the other hand, does not perform any caching.</span></span> <span data-ttu-id="0387a-401">Protože `RowCreated` událost je aktivována, jednou pro každý řádek v GridView, pomocí `User.IsInRole(roleName)` zahrnuje pouze jediná cesta k úložišti role, zatímco `Roles.IsUserInRole(roleName)` vyžaduje *N* služebních cest, kde *N* je Počet uživatelských účtů zobrazené v mřížce.</span><span class="sxs-lookup"><span data-stu-id="0387a-401">Because the `RowCreated` event is fired once for every row in the GridView, using `User.IsInRole(roleName)` involves just one trip to the role store whereas `Roles.IsUserInRole(roleName)` requires *N* trips, where *N* is the number of user accounts displayed in the grid.</span></span>


<span data-ttu-id="0387a-402">Tlačítko Upravit `Visible` je nastavena na `true` Pokud uživatele, kteří navštěvují Tato stránka je v roli správce nebo vedoucí; v opačném případě je nastavený na `false`.</span><span class="sxs-lookup"><span data-stu-id="0387a-402">The Edit button's `Visible` property is set to `true` if the user visiting this page is in the Administrators or Supervisors role; otherwise it is set to `false`.</span></span> <span data-ttu-id="0387a-403">Tlačítko Odstranit `Visible` je nastavena na `true` pouze pokud je uživatel v roli správce.</span><span class="sxs-lookup"><span data-stu-id="0387a-403">The Delete button's `Visible` property is set to `true` only if the user is in the Administrators role.</span></span>

<span data-ttu-id="0387a-404">Otestujte tuto stránku prostřednictvím prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="0387a-404">Test this page through a browser.</span></span> <span data-ttu-id="0387a-405">Pokud jako anonymní návštěvníka nebo jako uživatel, který není nadřízeným ani správce, najdete na stránce, CommandField je prázdná. stále existuje, ale jako dynamické následující bez úpravy nebo odstranění tlačítka.</span><span class="sxs-lookup"><span data-stu-id="0387a-405">If you visit the page as an anonymous visitor or as a user that is neither a Supervisor nor an Administrator, the CommandField is empty; it still exists, but as a thin sliver without the Edit or Delete buttons.</span></span>

> [!NOTE]
> <span data-ttu-id="0387a-406">Je možné skrýt CommandField zcela při bez nadřízeného a bez oprávnění správce je na stránce.</span><span class="sxs-lookup"><span data-stu-id="0387a-406">It is possible to hide the CommandField altogether when a non-Supervisor and non-Administrator is visiting the page.</span></span> <span data-ttu-id="0387a-407">Nechat to jako cvičení pro čtečku.</span><span class="sxs-lookup"><span data-stu-id="0387a-407">I leave this as an exercise for the reader.</span></span>


<span data-ttu-id="0387a-408">[![Upravit a odstranit tlačítka jsou skryto bez dohledu a všichni uživatelé](role-based-authorization-cs/_static/image38.png)](role-based-authorization-cs/_static/image37.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-408">[![The Edit and Delete Buttons are Hidden for Non-Supervisors and Non-Administrators](role-based-authorization-cs/_static/image38.png)](role-based-authorization-cs/_static/image37.png)</span></span>

<span data-ttu-id="0387a-409">**Obrázek 13**: Upravit a odstranit tlačítka jsou skryto bez dohledu a všichni uživatelé ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image39.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-409">**Figure 13**: The Edit and Delete Buttons are Hidden for Non-Supervisors and Non-Administrators  ([Click to view full-size image](role-based-authorization-cs/_static/image39.png))</span></span>


<span data-ttu-id="0387a-410">Pokud uživatel, který patří k roli dohledu (ale ne do role Správci) navštíví, zjistí, jenom na tlačítko Upravit.</span><span class="sxs-lookup"><span data-stu-id="0387a-410">If a user that belongs to the Supervisors role (but not to the Administrators role) visits, he sees only the Edit button.</span></span>


<span data-ttu-id="0387a-411">[![Tlačítko Upravit je k dispozici pro vedoucí, je skrytý na tlačítko Odstranit](role-based-authorization-cs/_static/image41.png)](role-based-authorization-cs/_static/image40.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-411">[![While the Edit Button is Available for Supervisors, the Delete Button is Hidden](role-based-authorization-cs/_static/image41.png)](role-based-authorization-cs/_static/image40.png)</span></span>

<span data-ttu-id="0387a-412">**Obrázek 14**: při the tlačítka Upravit je k dispozici pro vedoucí, je skrytý na tlačítko Odstranit ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image42.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-412">**Figure 14**: While the Edit Button is Available for Supervisors, the Delete Button is Hidden  ([Click to view full-size image](role-based-authorization-cs/_static/image42.png))</span></span>


<span data-ttu-id="0387a-413">A pokud správce navštíví, která má přístup k tlačítka Upravit a odstranit.</span><span class="sxs-lookup"><span data-stu-id="0387a-413">And if an Administrator visits, she has access to both the Edit and Delete buttons.</span></span>


<span data-ttu-id="0387a-414">[![Upravit a odstranit tlačítka jsou k dispozici pouze pro správce](role-based-authorization-cs/_static/image44.png)](role-based-authorization-cs/_static/image43.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-414">[![The Edit and Delete Buttons are Available Only for Administrators](role-based-authorization-cs/_static/image44.png)](role-based-authorization-cs/_static/image43.png)</span></span>

<span data-ttu-id="0387a-415">**Obrázek 15**: Upravit a odstranit tlačítka jsou k dispozici pouze pro správce ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image45.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-415">**Figure 15**: The Edit and Delete Buttons are Available Only for Administrators  ([Click to view full-size image](role-based-authorization-cs/_static/image45.png))</span></span>


## <a name="step-3-applying-role-based-authorization-rules-to-classes-and-methods"></a><span data-ttu-id="0387a-416">Krok 3: Použití třídy a metody na základě rolí autorizační pravidla</span><span class="sxs-lookup"><span data-stu-id="0387a-416">Step 3: Applying Role-Based Authorization Rules to Classes and Methods</span></span>

<span data-ttu-id="0387a-417">V kroku 2, které jsme omezené upravit funkce, které uživatelé v rolích dohledu a správci a odstraňte možnosti pouze správcům.</span><span class="sxs-lookup"><span data-stu-id="0387a-417">In Step 2 we limited edit capabilities to users in the Supervisors and Administrators roles and delete capabilities to Administrators only.</span></span> <span data-ttu-id="0387a-418">Toho dosáhlo tím skrytí prvky přidružené uživatelského rozhraní pro neoprávněné uživatele prostřednictvím programový techniky.</span><span class="sxs-lookup"><span data-stu-id="0387a-418">This was accomplished by hiding the associated user interface elements for unauthorized users through programmatic techniques.</span></span> <span data-ttu-id="0387a-419">Tato opatření není zaručeno, že neoprávněný uživatel nebude možné provádět privilegované akce.</span><span class="sxs-lookup"><span data-stu-id="0387a-419">Such measures do not guarantee that an unauthorized user will be unable to perform a privileged action.</span></span> <span data-ttu-id="0387a-420">Může být prvky uživatelského rozhraní, které jsou přidány později nebo že jsme zapomněli skrýt neoprávnění uživatelé.</span><span class="sxs-lookup"><span data-stu-id="0387a-420">There may be user interface elements that are added later or that we forgot to hide for unauthorized users.</span></span> <span data-ttu-id="0387a-421">Nebo se hacker může zjistit nějakým způsobem získat stránku ASP.NET a provést požadovanou metodu.</span><span class="sxs-lookup"><span data-stu-id="0387a-421">Or a hacker may discover some other way to get the ASP.NET page to execute the desired method.</span></span>

<span data-ttu-id="0387a-422">Snadný způsob, jak Ujistěte se, že konkrétní funkce nelze získat přístup, neoprávněný uživatel je pro uspořádání třídy nebo metoda s [ `PrincipalPermission` atribut](https://msdn.microsoft.com/library/system.security.permissions.principalpermissionattribute.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-422">An easy way to ensure that a particular piece of functionality cannot be accessed by an unauthorized user is to decorate that class or method with the [`PrincipalPermission` attribute](https://msdn.microsoft.com/library/system.security.permissions.principalpermissionattribute.aspx).</span></span> <span data-ttu-id="0387a-423">Pokud modul runtime rozhraní .NET používá třídu nebo spustí jeden z jeho metody, zkontroluje, ujistěte se, zda aktuální kontext zabezpečení má oprávnění.</span><span class="sxs-lookup"><span data-stu-id="0387a-423">When the .NET runtime uses a class or executes one of its methods, it checks to ensure that the current security context has permission.</span></span> <span data-ttu-id="0387a-424">`PrincipalPermission` Atribut poskytuje mechanismus, pomocí kterého definujeme tato pravidla.</span><span class="sxs-lookup"><span data-stu-id="0387a-424">The `PrincipalPermission` attribute provides a mechanism through which we can define these rules.</span></span>

<span data-ttu-id="0387a-425">Jsme se podívali na použití `PrincipalPermission` atribut zpět <a id="_msoanchor_9"> </a> [ *autorizace na základě uživatele* ](../membership/user-based-authorization-cs.md) kurzu.</span><span class="sxs-lookup"><span data-stu-id="0387a-425">We looked at using the `PrincipalPermission` attribute back in the <a id="_msoanchor_9"></a>[*User-Based Authorization*](../membership/user-based-authorization-cs.md) tutorial.</span></span> <span data-ttu-id="0387a-426">Konkrétně jsme viděli postupy pro uspořádání prvku GridView `SelectedIndexChanged` a `RowDeleting` obslužné rutiny události tak, aby se může spustit pouze pomocí ověřeným uživatelům a Tito, v uvedeném pořadí.</span><span class="sxs-lookup"><span data-stu-id="0387a-426">Specifically, we saw how to decorate the GridView's `SelectedIndexChanged` and `RowDeleting` event handler so that they could only be executed by authenticated users and Tito, respectively.</span></span> <span data-ttu-id="0387a-427">`PrincipalPermission` Atribut funguje stejně dobře s rolemi.</span><span class="sxs-lookup"><span data-stu-id="0387a-427">The `PrincipalPermission` attribute works just as well with roles.</span></span>

<span data-ttu-id="0387a-428">Pojďme demonstruje použití `PrincipalPermission` atribut v prvku GridView `RowUpdating` a `RowDeleting` obslužné rutiny událostí zakázat spouštění pro uživatele bez oprávnění.</span><span class="sxs-lookup"><span data-stu-id="0387a-428">Let's demonstrate using the `PrincipalPermission` attribute on the GridView's `RowUpdating` and `RowDeleting` event handlers to prohibit execution for non-authorized users.</span></span> <span data-ttu-id="0387a-429">Je budeme muset udělat přidat atribut příslušné na každý definice funkce:</span><span class="sxs-lookup"><span data-stu-id="0387a-429">All we need to do is add the appropriate attribute atop each function definition:</span></span>

[!code-csharp[Main](role-based-authorization-cs/samples/sample13.cs)]

<span data-ttu-id="0387a-430">Atribut pro `RowUpdating` obslužné rutiny události stanoví, můžete jenom uživatelé v rolích správci nebo vedoucí spouštění obslužné rutiny události, kde je jako atribut na `RowDeleting` obslužné rutiny události omezuje na uživatele ve Správci provedení role.</span><span class="sxs-lookup"><span data-stu-id="0387a-430">The attribute for the `RowUpdating` event handler dictates that only users in the Administrators or Supervisors roles can execute the event handler, where as the attribute on the `RowDeleting` event handler limits the execution to users in the Administrators role.</span></span>


> [!NOTE]
> <span data-ttu-id="0387a-431">`PrincipalPermission` Atribut je reprezentována jako třída v `System.Security.Permissions` oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="0387a-431">The `PrincipalPermission` attribute is represented as a class in the `System.Security.Permissions` namespace.</span></span> <span data-ttu-id="0387a-432">Nezapomeňte přidat `using System.Security.Permissions` příkaz v horní části souboru kódu třídy pro import tento obor názvů.</span><span class="sxs-lookup"><span data-stu-id="0387a-432">Be sure to add a `using System.Security.Permissions` statement at the top of your code-behind class file to import this namespace.</span></span>


<span data-ttu-id="0387a-433">Pokud nějakým způsobem, který není správcem pokusí spustit `RowDeleting` obslužné rutiny události nebo pokud není nadřízeného nebo bez oprávnění správce pokusí spustit `RowUpdating` obslužné rutiny události, bude vyvolána modul runtime rozhraní .NET `SecurityException`.</span><span class="sxs-lookup"><span data-stu-id="0387a-433">If, somehow, a non-Administrator attempts to execute the `RowDeleting` event handler or if a non-Supervisor or non-Administrator attempts to execute the `RowUpdating` event handler, the .NET runtime will raise a `SecurityException`.</span></span>


<span data-ttu-id="0387a-434">[![Pokud kontext zabezpečení nemá oprávnění k provedení metody, je vyvolána výjimka zabezpečení](role-based-authorization-cs/_static/image47.png)](role-based-authorization-cs/_static/image46.png)</span><span class="sxs-lookup"><span data-stu-id="0387a-434">[![If the Security Context is not Authorized to Execute the Method, a SecurityException is Thrown](role-based-authorization-cs/_static/image47.png)](role-based-authorization-cs/_static/image46.png)</span></span>

<span data-ttu-id="0387a-435">**Obrázek 16**: Pokud kontext zabezpečení nemá oprávnění k provedení metody, `SecurityException` je vyvolána ([Kliknutím zobrazit obrázek v plné velikosti](role-based-authorization-cs/_static/image48.png))</span><span class="sxs-lookup"><span data-stu-id="0387a-435">**Figure 16**: If the Security Context is not Authorized to Execute the Method, a `SecurityException` is Thrown  ([Click to view full-size image](role-based-authorization-cs/_static/image48.png))</span></span>


<span data-ttu-id="0387a-436">Kromě stránek ASP.NET mnoho aplikací mít také architekturu, která zahrnuje různé vrstvy, jako je například obchodní logiku a Data přístup vrstvy.</span><span class="sxs-lookup"><span data-stu-id="0387a-436">In addition to ASP.NET pages, many applications also have an architecture that includes various layers, such as Business Logic and Data Access Layers.</span></span> <span data-ttu-id="0387a-437">Tyto vrstvy jsou obvykle implementovány jako knihovny tříd a nabízet třídy a metody pro provádění obchodní logiku a data související funkce.</span><span class="sxs-lookup"><span data-stu-id="0387a-437">These layers are typically implemented as Class Libraries and offer classes and methods for performing business logic- and data-related functionality.</span></span> <span data-ttu-id="0387a-438">`PrincipalPermission` Atribut je vhodná pro použití autorizačních pravidel na také tyto vrstvy.</span><span class="sxs-lookup"><span data-stu-id="0387a-438">The `PrincipalPermission` attribute is useful for applying authorization rules to these layers as well.</span></span>

<span data-ttu-id="0387a-439">Další informace o používání `PrincipalPermission` atribut definovat pravidla ověřování na třídy a metody, získáte na [Scott Guthrie](https://weblogs.asp.net/scottgu/)na položce blogu [přidáním autorizačních pravidel pro obchodní a datové vrstvy pomocí `PrincipalPermissionAttributes` ](https://weblogs.asp.net/scottgu/archive/2006/10/04/Tip_2F00_Trick_3A00_-Adding-Authorization-Rules-to-Business-and-Data-Layers-using-PrincipalPermissionAttributes.aspx).</span><span class="sxs-lookup"><span data-stu-id="0387a-439">For more information on using the `PrincipalPermission` attribute to define authorization rules on classes and methods, refer to [Scott Guthrie](https://weblogs.asp.net/scottgu/)'s blog entry [Adding Authorization Rules to Business and Data Layers Using `PrincipalPermissionAttributes`](https://weblogs.asp.net/scottgu/archive/2006/10/04/Tip_2F00_Trick_3A00_-Adding-Authorization-Rules-to-Business-and-Data-Layers-using-PrincipalPermissionAttributes.aspx).</span></span>

## <a name="summary"></a><span data-ttu-id="0387a-440">Souhrn</span><span class="sxs-lookup"><span data-stu-id="0387a-440">Summary</span></span>

<span data-ttu-id="0387a-441">V tomto kurzu jsme se podívali na tom, jak zadat hrubým a spočívá v jemné autorizační pravidla založená na role uživatele.</span><span class="sxs-lookup"><span data-stu-id="0387a-441">In this tutorial we looked at how to specify coarse and fine grain authorization rules based on the user's roles.</span></span> <span data-ttu-id="0387a-442">SOUBOR ASP. Funkce autorizace adresy URL pro NET umožňuje vývojáři stránky zadejte co identity se povoluje nebo odepírá přístup k jaké stránky.</span><span class="sxs-lookup"><span data-stu-id="0387a-442">ASP.NET's URL authorization feature allows a page developer to specify what identities are allowed or denied access to what pages.</span></span> <span data-ttu-id="0387a-443">Jak jsme viděli zpět v <a id="_msoanchor_10"> </a> [ *autorizace na základě uživatele* ](../membership/user-based-authorization-cs.md) kurz, autorizace adres URL pravidla lze použít na základě uživatele uživatelů.</span><span class="sxs-lookup"><span data-stu-id="0387a-443">As we saw back in the <a id="_msoanchor_10"></a>[*User-Based Authorization*](../membership/user-based-authorization-cs.md) tutorial, URL authorization rules can be applied on a user-by-user basis.</span></span> <span data-ttu-id="0387a-444">Je možné je použít na základě rolí role jako jsme viděli v kroku 1 tohoto kurzu.</span><span class="sxs-lookup"><span data-stu-id="0387a-444">They can also be applied on a role-by-role basis, as we saw in Step 1 of this tutorial.</span></span>

<span data-ttu-id="0387a-445">Spočívá v jemné autorizační pravidla mohou být použity deklarativně nebo prostřednictvím kódu programu.</span><span class="sxs-lookup"><span data-stu-id="0387a-445">Fine grain authorization rules may be applied declaratively or programmatically.</span></span> <span data-ttu-id="0387a-446">V kroku 2 jsme se podívali na použití funkce kolekci RoleGroups ovládací prvek LoginView k vykreslení odlišný výstup na základě rolí hostujícími uživatelů.</span><span class="sxs-lookup"><span data-stu-id="0387a-446">In Step 2 we looked at using the LoginView control's RoleGroups feature to render different output based on the visiting user's roles.</span></span> <span data-ttu-id="0387a-447">Také jsme se podívali na způsoby, jak určit prostřednictvím kódu programu, pokud uživatel patří do určité role a postup odpovídajícím způsobem upravit na stránce funkce.</span><span class="sxs-lookup"><span data-stu-id="0387a-447">We also looked at ways to programmatically determine if a user belongs to a specific role and how to adjust the page's functionality accordingly.</span></span>

<span data-ttu-id="0387a-448">Radostí programování!</span><span class="sxs-lookup"><span data-stu-id="0387a-448">Happy Programming!</span></span>

### <a name="further-reading"></a><span data-ttu-id="0387a-449">Další čtení</span><span class="sxs-lookup"><span data-stu-id="0387a-449">Further Reading</span></span>

<span data-ttu-id="0387a-450">Další informace o tématech popsané v tomto kurzu najdete v následujících zdrojích informací:</span><span class="sxs-lookup"><span data-stu-id="0387a-450">For more information on the topics discussed in this tutorial, refer to the following resources:</span></span>

- [<span data-ttu-id="0387a-451">Přidávání do obchodní a datové vrstvy pomocí autorizačních pravidel `PrincipalPermissionAttributes`</span><span class="sxs-lookup"><span data-stu-id="0387a-451">Adding Authorization Rules to Business and Data Layers Using `PrincipalPermissionAttributes`</span></span>](https://weblogs.asp.net/scottgu/archive/2006/10/04/Tip_2F00_Trick_3A00_-Adding-Authorization-Rules-to-Business-and-Data-Layers-using-PrincipalPermissionAttributes.aspx)
- [<span data-ttu-id="0387a-452">Zkoumání ASP.NET 2.0 je členství, role a profil: práce s rolemi</span><span class="sxs-lookup"><span data-stu-id="0387a-452">Examining ASP.NET 2.0's Membership, Roles, and Profile: Working with Roles</span></span>](http://aspnet.4guysfromrolla.com/articles/121405-1.aspx)
- [<span data-ttu-id="0387a-453">Seznam bezpečnostní otázku pro technologii ASP.NET 2.0</span><span class="sxs-lookup"><span data-stu-id="0387a-453">Security Question List for ASP.NET 2.0</span></span>](https://msdn.microsoft.com/library/ms998375.aspx)
- [<span data-ttu-id="0387a-454">Technická dokumentace k `<roleManager>` – Element</span><span class="sxs-lookup"><span data-stu-id="0387a-454">Technical documentation for the `<roleManager>` Element</span></span>](https://msdn.microsoft.com/library/ms164660.aspx)

### <a name="about-the-author"></a><span data-ttu-id="0387a-455">O autorovi</span><span class="sxs-lookup"><span data-stu-id="0387a-455">About the Author</span></span>

<span data-ttu-id="0387a-456">Scott Meisnerová, vytvořit více knih ASP/ASP.NET a zakladatele 4GuysFromRolla.com, má byla od 1998 práce s technologií Microsoft Web.</span><span class="sxs-lookup"><span data-stu-id="0387a-456">Scott Mitchell, author of multiple ASP/ASP.NET books and founder of 4GuysFromRolla.com, has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="0387a-457">Scott funguje jako nezávislé poradce, trainer a zapisovače.</span><span class="sxs-lookup"><span data-stu-id="0387a-457">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="0387a-458">Jeho nejnovější seznam k  *[Edice nakladatelství Sams naučit sami technologii ASP.NET 2.0 za 24 hodin](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco)*.</span><span class="sxs-lookup"><span data-stu-id="0387a-458">His latest book is *[Sams Teach Yourself ASP.NET 2.0 in 24 Hours](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco)*.</span></span> <span data-ttu-id="0387a-459">Scott lze dosáhnout za [ mitchell@4guysfromrolla.com ](mailto:mitchell@4guysfromrolla.com) nebo prostřednictvím svého blogu v [ http://ScottOnWriting.NET ](http://scottonwriting.net/).</span><span class="sxs-lookup"><span data-stu-id="0387a-459">Scott can be reached at [mitchell@4guysfromrolla.com](mailto:mitchell@4guysfromrolla.com) or via his blog at [http://ScottOnWriting.NET](http://scottonwriting.net/).</span></span>

### <a name="special-thanks-to"></a><span data-ttu-id="0387a-460">Zvláštní poděkování...</span><span class="sxs-lookup"><span data-stu-id="0387a-460">Special Thanks To…</span></span>

<span data-ttu-id="0387a-461">Tento kurz řady byla zkontrolovány uživatelem mnoho užitečné kontrolorů.</span><span class="sxs-lookup"><span data-stu-id="0387a-461">This tutorial series was reviewed by many helpful reviewers.</span></span> <span data-ttu-id="0387a-462">Vést kontroloři pro účely tohoto kurzu zahrnují Suchi Banerjee a Teresy Murphy.</span><span class="sxs-lookup"><span data-stu-id="0387a-462">Lead reviewers for this tutorial include Suchi Banerjee and Teresa Murphy.</span></span> <span data-ttu-id="0387a-463">Kontrola Moje nadcházející články MSDN máte zájem?</span><span class="sxs-lookup"><span data-stu-id="0387a-463">Interested in reviewing my upcoming MSDN articles?</span></span> <span data-ttu-id="0387a-464">Pokud ano, vyřaďte mi řádek v [mitchell@4GuysFromRolla.com](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="0387a-464">If so, drop me a line at [mitchell@4GuysFromRolla.com](mailto:mitchell@4GuysFromRolla.com)</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="0387a-465">[Předchozí](assigning-roles-to-users-cs.md)
> [další](creating-and-managing-roles-vb.md)</span><span class="sxs-lookup"><span data-stu-id="0387a-465">[Previous](assigning-roles-to-users-cs.md)
[Next](creating-and-managing-roles-vb.md)</span></span>
