---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: Představení rozhraní ASP.NET Web Pages – zobrazení dat | Dokumentace Microsoftu
author: tfitzmac
description: V tomto kurzu se dozvíte, jak vytvořit databázi v nástroji WebMatrix a způsob zobrazení dat z databáze na stránce při použití webových stránek ASP.NET (Razor). Předpokládá y...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 864b9f7826763e307368706116458678abf50d3b
ms.sourcegitcommit: 45ac74e400f9f2b7dbded66297730f6f14a4eb25
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/16/2018
ms.locfileid: "41753332"
---
<a name="introducing-aspnet-web-pages---displaying-data"></a><span data-ttu-id="51a56-104">Úvod do webových stránek ASP.NET – zobrazení dat</span><span class="sxs-lookup"><span data-stu-id="51a56-104">Introducing ASP.NET Web Pages - Displaying Data</span></span>
====================
<span data-ttu-id="51a56-105">podle [Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="51a56-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="51a56-106">V tomto kurzu se dozvíte, jak vytvořit databázi v nástroji WebMatrix a způsob zobrazení dat z databáze na stránce při použití webových stránek ASP.NET (Razor).</span><span class="sxs-lookup"><span data-stu-id="51a56-106">This tutorial shows you how to create a database in WebMatrix and how to display database data in a page when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="51a56-107">Předpokládá, že jste dokončili řady prostřednictvím [Úvod do programování webových stránek ASP.NET](../introducing-razor-syntax-c.md).</span><span class="sxs-lookup"><span data-stu-id="51a56-107">It assumes you have completed the series through [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md).</span></span>
> 
> <span data-ttu-id="51a56-108">Co se dozvíte:</span><span class="sxs-lookup"><span data-stu-id="51a56-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="51a56-109">Jak vytvořit databázi a databázových tabulek pomocí nástroje WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="51a56-109">How to use WebMatrix tools to create a database and database tables.</span></span>
> - <span data-ttu-id="51a56-110">Jak přidat data do databáze pomocí nástroje WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="51a56-110">How to use WebMatrix tools to add data to a database.</span></span>
> - <span data-ttu-id="51a56-111">Jak zobrazit data z databáze na stránce.</span><span class="sxs-lookup"><span data-stu-id="51a56-111">How to display data from the database on a page.</span></span>
> - <span data-ttu-id="51a56-112">Jak spouštět SQL příkazy v rozhraní ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="51a56-112">How to run SQL commands in ASP.NET Web Pages.</span></span>
> - <span data-ttu-id="51a56-113">Jak přizpůsobit `WebGrid` pomocná Změna zobrazených dat a přidání stránkování a řazení.</span><span class="sxs-lookup"><span data-stu-id="51a56-113">How to customize the `WebGrid` helper to change the data display and to add paging and sorting.</span></span>
>   
> 
> <span data-ttu-id="51a56-114">Popsané funkce a technologie:</span><span class="sxs-lookup"><span data-stu-id="51a56-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="51a56-115">Databázové nástroje WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="51a56-115">WebMatrix database tools.</span></span>
> - <span data-ttu-id="51a56-116">`WebGrid` pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="51a56-116">`WebGrid` helper.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="51a56-117">Co budete vytvářet</span><span class="sxs-lookup"><span data-stu-id="51a56-117">What You'll Build</span></span>

<span data-ttu-id="51a56-118">V předchozím kurzu jste se seznámili s pro webové stránky ASP.NET (*.cshtml* soubory), základní informace o syntaxi Razor a pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="51a56-118">In the previous tutorial, you were introduced to ASP.NET Web Pages (*.cshtml* files), to the basics of Razor syntax, and to helpers.</span></span> <span data-ttu-id="51a56-119">V tomto kurzu budete začít vytvářet skutečné webové aplikace, které budete používat pro zbývající části této série.</span><span class="sxs-lookup"><span data-stu-id="51a56-119">In this tutorial, you'll begin creating the actual web application that you'll use for the rest of the series.</span></span> <span data-ttu-id="51a56-120">Aplikace je jednoduché filmové aplikace, která umožňuje zobrazit, přidat, změnit nebo odstranit informace o filmech.</span><span class="sxs-lookup"><span data-stu-id="51a56-120">The app is a simple movie application that lets you view, add, change, and delete information about movies.</span></span>

<span data-ttu-id="51a56-121">Až budete hotovi s tímto kurzem, budete moci zobrazit výpis film, který vypadá jako na této stránce:</span><span class="sxs-lookup"><span data-stu-id="51a56-121">When you're done with this tutorial, you'll be able to view a movie listing that looks like this page:</span></span>

![WebGrid zobrazení, která obsahuje parametry nastavena na názvy tříd šablon stylů CSS](displaying-data/_static/image1.png)

<span data-ttu-id="51a56-123">Ale pokud chcete začít, je nutné vytvořit databázi.</span><span class="sxs-lookup"><span data-stu-id="51a56-123">But to begin, you have to create a database.</span></span>

## <a name="a-very-brief-introduction-to-databases"></a><span data-ttu-id="51a56-124">Velmi stručný úvod do databáze</span><span class="sxs-lookup"><span data-stu-id="51a56-124">A Very Brief Introduction to Databases</span></span>

<span data-ttu-id="51a56-125">V tomto kurzu budou poskytovat jenom briefest Úvod do databáze.</span><span class="sxs-lookup"><span data-stu-id="51a56-125">This tutorial will provide only the briefest introduction to databases.</span></span> <span data-ttu-id="51a56-126">Pokud máte zkušenosti s databáze, můžete přeskočit Tento krátký oddíl.</span><span class="sxs-lookup"><span data-stu-id="51a56-126">If you have database experience, you can skip this short section.</span></span>

<span data-ttu-id="51a56-127">Databáze obsahuje jednu nebo více tabulek, které obsahují informace &mdash; například tabulky pro zákazníky, objednávky a dodavatele, nebo pro studenty, učitelé, třídy a třídy.</span><span class="sxs-lookup"><span data-stu-id="51a56-127">A database contains one or more tables that contain information &mdash; for example, tables for customers, orders, and vendors, or for students, teachers, classes, and grades.</span></span> <span data-ttu-id="51a56-128">Databázové tabulky strukturálně, je jako tabulku.</span><span class="sxs-lookup"><span data-stu-id="51a56-128">Structurally, a database table is like a spreadsheet.</span></span> <span data-ttu-id="51a56-129">Představte typické adresáře.</span><span class="sxs-lookup"><span data-stu-id="51a56-129">Imagine a typical address book.</span></span> <span data-ttu-id="51a56-130">Pro každou položku v adresáři (to znamená, že pro každou osobu) mají různé druhy informace, jako je jméno, příjmení, adresu, e-mailovou adresu a telefonní číslo.</span><span class="sxs-lookup"><span data-stu-id="51a56-130">For each entry in the address book (that is, for each person) you have several pieces of information such as first name, last name, address, email address, and phone number.</span></span>

![Ukázkové tabulky databáze jako jednoduchý mřížky](displaying-data/_static/image2.png)

<span data-ttu-id="51a56-132">(Řádky jsou někdy označovány jako *záznamy*, a sloupce jsou někdy označovány jako *pole*.)</span><span class="sxs-lookup"><span data-stu-id="51a56-132">(Rows are sometimes referred to as *records*, and columns are sometimes referred to as *fields*.)</span></span>

<span data-ttu-id="51a56-133">Pro většinu tabulky databáze v tabulce musí mít sloupec, který obsahuje jedinečná hodnota, například zákaznické číslo, číslo účtu a tak dále.</span><span class="sxs-lookup"><span data-stu-id="51a56-133">For most database tables, the table has to have a column that contains a unique value, like a customer number, account number, and so on.</span></span> <span data-ttu-id="51a56-134">Tato hodnota se označuje jako v tabulce *primární klíč*, a můžete ji použít k identifikaci každý řádek v tabulce.</span><span class="sxs-lookup"><span data-stu-id="51a56-134">This value is known as the table's *primary key*, and you use it to identify each row in the table.</span></span> <span data-ttu-id="51a56-135">V tomto příkladu je ID sloupce primární klíč pro adresář je znázorněno v předchozím příkladu.</span><span class="sxs-lookup"><span data-stu-id="51a56-135">In the example, the ID column is the primary key for the address book shown in the previous example.</span></span>

<span data-ttu-id="51a56-136">Velká část práce, kterou provedete v webových aplikací se skládá z čtení informací z databáze a jeho zobrazení na stránce.</span><span class="sxs-lookup"><span data-stu-id="51a56-136">Much of the work you do in web applications consists of reading information out of the database and displaying it on a page.</span></span> <span data-ttu-id="51a56-137">Kromě toho často budete shromažďovat informace od uživatelů a přidejte ho do databáze, nebo upravíte záznamy, které jsou již v databázi.</span><span class="sxs-lookup"><span data-stu-id="51a56-137">You'll also often gather information from users and add it to a database, or you'll modify records that are already in the database.</span></span> <span data-ttu-id="51a56-138">(Probereme všechny tyto operace v této sérii kurzů.)</span><span class="sxs-lookup"><span data-stu-id="51a56-138">(We'll cover all of these operations in the course of this tutorial set.)</span></span>

<span data-ttu-id="51a56-139">Pracovní databáze může být enormously složité a můžou vyžadovat, aby specializované znalosti.</span><span class="sxs-lookup"><span data-stu-id="51a56-139">Database work can be enormously complex and can require specialized knowledge.</span></span> <span data-ttu-id="51a56-140">Pro tuto sérii kurzů, musíte pochopit pouze základní koncepty, které budou všechny vysvětlena průběžně.</span><span class="sxs-lookup"><span data-stu-id="51a56-140">For this tutorial set, though, you have to understand only basic concepts, which will all be explained as you go.</span></span>

## <a name="creating-a-database"></a><span data-ttu-id="51a56-141">Vytvoření databáze</span><span class="sxs-lookup"><span data-stu-id="51a56-141">Creating a Database</span></span>

<span data-ttu-id="51a56-142">Služba WebMatrix obsahuje nástroje, které umožňují snadno vytvořit databázi a vytvoření tabulky v databázi.</span><span class="sxs-lookup"><span data-stu-id="51a56-142">WebMatrix includes tools that make it easy to create a database and to create tables in the database.</span></span> <span data-ttu-id="51a56-143">(Struktura databáze se označuje jako databáze *schématu*.) Pro tuto sérii kurzů, vytvoříte databázi, která obsahuje pouze jednu tabulku v něm &mdash; videa.</span><span class="sxs-lookup"><span data-stu-id="51a56-143">(The structure of a database is referred to as the database's *schema*.) For this tutorial set, you'll create a database that has only one table in it &mdash; Movies.</span></span>

<span data-ttu-id="51a56-144">Otevřete nástroj WebMatrix, pokud jste tak již neučinili a otevřete web WebPagesMovies, kterou jste vytvořili v předchozím kurzu.</span><span class="sxs-lookup"><span data-stu-id="51a56-144">Open WebMatrix if you haven't already done so, and open the WebPagesMovies site that you created in the previous tutorial.</span></span>

<span data-ttu-id="51a56-145">V levém podokně klikněte **databáze** pracovního prostoru.</span><span class="sxs-lookup"><span data-stu-id="51a56-145">In the left pane, click the **Database** workspace.</span></span>

![Karta WebMatrix databáze pracovního prostoru](displaying-data/_static/image3.png)

<span data-ttu-id="51a56-147">Pásu karet se změní a objeví úloh souvisejících s databází.</span><span class="sxs-lookup"><span data-stu-id="51a56-147">The ribbon changes to show database-related tasks.</span></span> <span data-ttu-id="51a56-148">Na pásu karet klikněte na tlačítko **novou databázi**.</span><span class="sxs-lookup"><span data-stu-id="51a56-148">In the ribbon, click **New Database**.</span></span>

!['Nové databáze' tlačítko na pásu karet nástroje WebMatrix](displaying-data/_static/image4.png)

<span data-ttu-id="51a56-150">Služba WebMatrix vytvoří databázi SQL Server CE ( *SDF* soubor), který má stejný název jako lokalitu pro &mdash; *WebPagesMovies.sdf*.</span><span class="sxs-lookup"><span data-stu-id="51a56-150">WebMatrix creates a SQL Server CE database (an *.sdf* file) that has the same name as your site &mdash; *WebPagesMovies.sdf*.</span></span> <span data-ttu-id="51a56-151">(Není to zde, ale můžete přejmenovat soubor na cokoli, co vám vyhovuje, za předpokladu, má *SDF* rozšíření.)</span><span class="sxs-lookup"><span data-stu-id="51a56-151">(You won't do this here, but you can rename the file to anything you like, as long as it has an *.sdf* extension.)</span></span>

## <a name="creating-a-table"></a><span data-ttu-id="51a56-152">Vytvoření tabulky</span><span class="sxs-lookup"><span data-stu-id="51a56-152">Creating a Table</span></span>

<span data-ttu-id="51a56-153">Na pásu karet klikněte na tlačítko **nová tabulka**.</span><span class="sxs-lookup"><span data-stu-id="51a56-153">In the ribbon, click **New Table**.</span></span> <span data-ttu-id="51a56-154">Služba WebMatrix otevře na nové kartě návrháře tabulky. (Pokud není k dispozici možnost nové tabulky, ujistěte se, že se nová databáze je vybraný ve stromovém zobrazení na levé straně.)</span><span class="sxs-lookup"><span data-stu-id="51a56-154">WebMatrix opens the table designer in a new tab. (If the New Table option isn't available, make sure that the new database is selected in the tree view on the left.)</span></span>

!['Nové tabulky' tlačítko na pásu karet nástroje WebMatrix](displaying-data/_static/image5.png)

<span data-ttu-id="51a56-156">Do textového pole v horní části stránky (kde vodoznak říká "Zadejte název tabulky") zadejte "Filmy".</span><span class="sxs-lookup"><span data-stu-id="51a56-156">In the text box at the top (where the watermark says "Enter table name"), enter "Movies".</span></span>

![Zadání názvu tabulky v Návrháři databáze pro službu WebMatrix](displaying-data/_static/image6.png)

<span data-ttu-id="51a56-158">V podokně pod název tabulky je tady můžete definovat jednotlivých sloupců.</span><span class="sxs-lookup"><span data-stu-id="51a56-158">The pane underneath the table name is where you define individual columns.</span></span> <span data-ttu-id="51a56-159">Pro *filmy* tabulky v tomto kurzu vytvoříte pouze několik sloupců: *ID*, *Title*, *žánr*, a *rok*.</span><span class="sxs-lookup"><span data-stu-id="51a56-159">For the *Movies* table in this tutorial, you'll create only a few columns: *ID*, *Title*, *Genre*, and *Year*.</span></span>

<span data-ttu-id="51a56-160">V **název** zadejte "ID".</span><span class="sxs-lookup"><span data-stu-id="51a56-160">In the **Name** box, enter "ID".</span></span> <span data-ttu-id="51a56-161">Zadáním hodnoty tady aktivuje všechny ovládací prvky pro nový sloupec.</span><span class="sxs-lookup"><span data-stu-id="51a56-161">Entering a value here activates all the controls for the new column.</span></span>

<span data-ttu-id="51a56-162">Záložku **datový typ** seznam a zvolte **int**. Tato hodnota určuje, že sloupec ID bude obsahovat data o celé číslo (číslo).</span><span class="sxs-lookup"><span data-stu-id="51a56-162">Tab to the **Data Type** list and choose **int**. This value specifies that the ID column will contain integer (number) data.</span></span>

> [!NOTE]
> <span data-ttu-id="51a56-163">Nebude voláme ho žádné najdete tady (mnohem), ale můžete použít standardní klávesnice gesta Windows pro navigaci v této mřížce.</span><span class="sxs-lookup"><span data-stu-id="51a56-163">We won't call it out any more here (much), but you can use standard Windows keyboard gestures to navigate in this grid.</span></span> <span data-ttu-id="51a56-164">Například můžete vytvořit kartu mezi poli, stačí začít psát, abyste mohli vybrat položku v seznamu a tak dále.</span><span class="sxs-lookup"><span data-stu-id="51a56-164">For example, you can tab between fields, you can just start typing in order to select an item in a list, and so on.</span></span>


<span data-ttu-id="51a56-165">Karta minulé **výchozí hodnota** pole (to znamená, že necháte prázdné).</span><span class="sxs-lookup"><span data-stu-id="51a56-165">Tab past the **Default Value** box (that is, leave it blank).</span></span> <span data-ttu-id="51a56-166">Záložku **je primární klíč** zaškrtněte políčko a vyberte ji.</span><span class="sxs-lookup"><span data-stu-id="51a56-166">Tab to the **Is Primary Key** check box and select it.</span></span> <span data-ttu-id="51a56-167">Tato možnost informuje databázi, která *ID* sloupec bude obsahovat data, která identifikuje jednotlivé řádky.</span><span class="sxs-lookup"><span data-stu-id="51a56-167">This option tells the database that the *ID* column will contain the data that identifies individual rows.</span></span> <span data-ttu-id="51a56-168">(To znamená, že každý řádek bude mít jedinečnou hodnotu ve sloupci ID, který vám pomůže najít tento řádek.)</span><span class="sxs-lookup"><span data-stu-id="51a56-168">(That is, each row will have a unique value in the ID column that you can use to find that row.)</span></span>

<span data-ttu-id="51a56-169">Zvolte **je identita** možnost.</span><span class="sxs-lookup"><span data-stu-id="51a56-169">Choose the **Is Identity** option.</span></span> <span data-ttu-id="51a56-170">Tato možnost informuje databázi, má automaticky generovat další pořadové číslo pro každý nový řádek.</span><span class="sxs-lookup"><span data-stu-id="51a56-170">This option tells the database that it should automatically generate the next sequential number for each new row.</span></span> <span data-ttu-id="51a56-171">( **Je identita** možnost funguje pouze v případě, že jste vybrali **je primární klíč** možnost.)</span><span class="sxs-lookup"><span data-stu-id="51a56-171">(The **Is Identity** option works only if you've also selected the **Is Primary Key** option.)</span></span>

<span data-ttu-id="51a56-172">Klikněte na další řádek mřížky nebo stisknutím klávesy Tab dvakrát na dokončení aktuální řádek.</span><span class="sxs-lookup"><span data-stu-id="51a56-172">Click in the next grid row, or press Tab twice to finish the current row.</span></span> <span data-ttu-id="51a56-173">Buď gesta uloží aktuální řádek a spustí další příkaz.</span><span class="sxs-lookup"><span data-stu-id="51a56-173">Either gesture saves the current row and starts the next one.</span></span> <span data-ttu-id="51a56-174">Všimněte si, že **výchozí hodnota** sloupce teď uvádí, že **Null**.</span><span class="sxs-lookup"><span data-stu-id="51a56-174">Notice that the **Default Value** column now says **Null**.</span></span> <span data-ttu-id="51a56-175">(Null je výchozí hodnota pro výchozí hodnotu, takže na speak.)</span><span class="sxs-lookup"><span data-stu-id="51a56-175">(Null is the default value for the default value, so to speak.)</span></span>

<span data-ttu-id="51a56-176">Až dokončíte definování nového **ID** sloupec, Návrhář bude vypadat jako na tomto obrázku:</span><span class="sxs-lookup"><span data-stu-id="51a56-176">When you've finished defining the new **ID** column, the designer will look like this illustration:</span></span>

![Služba WebMatrix návrháře databáze po definování ID sloupce pro tabulku filmy](displaying-data/_static/image7.png)

<span data-ttu-id="51a56-178">Chcete-li vytvořit další sloupce, klikněte do pole ve **název** sloupce.</span><span class="sxs-lookup"><span data-stu-id="51a56-178">To create the next column, click in the box in the **Name** column.</span></span> <span data-ttu-id="51a56-179">Zadejte "Title" pro sloupec a pak vyberte **nvarchar** pro **datový typ** hodnotu.</span><span class="sxs-lookup"><span data-stu-id="51a56-179">Enter "Title" for the column and then select **nvarchar** for the **Data Type** value.</span></span> <span data-ttu-id="51a56-180">Část "var" **nvarchar** říká databáze, že data pro tento sloupec bude řetězec, jehož velikost může lišit od záznamu.</span><span class="sxs-lookup"><span data-stu-id="51a56-180">The "var" part of **nvarchar** tells the database that the data for this column will be a string whose size might vary from record to record.</span></span> <span data-ttu-id="51a56-181">(Předpona "n" představuje "national", což znamená, že pole může obsahovat znak data pro jakékoli abecedy nebo systému zápisu – to znamená, že pole obsahuje data ve formátu Unicode.)</span><span class="sxs-lookup"><span data-stu-id="51a56-181">(The "n" prefix represents "national," which indicates that the field can hold character data for any alphabet or writing system — that is, the field holds Unicode data.)</span></span>

<span data-ttu-id="51a56-182">Pokud zvolíte **nvarchar**, zobrazí se jiné pole, ve kterém můžete zadat maximální počet znaků pro pole.</span><span class="sxs-lookup"><span data-stu-id="51a56-182">When you choose **nvarchar**, another box appears where you can enter the maximum length for the field.</span></span> <span data-ttu-id="51a56-183">Zadejte hodnotu 50, za předpokladu, že žádný název filmu, budete pracovat v tomto kurzu bude delší než 50 znaků.</span><span class="sxs-lookup"><span data-stu-id="51a56-183">Enter 50, on the assumption that no movie title that you'll work with in this tutorial will be longer than 50 characters.</span></span>

<span data-ttu-id="51a56-184">Přeskočit **výchozí hodnota** a zrušte **Allow Nulls** možnost.</span><span class="sxs-lookup"><span data-stu-id="51a56-184">Skip **Default Value** and clear the **Allow Nulls** option.</span></span> <span data-ttu-id="51a56-185">Nechcete databáze, kterou chcete povolit všechny filmy se zapisují do databáze, které nemají název.</span><span class="sxs-lookup"><span data-stu-id="51a56-185">You don't want the database to allow any movies to be entered into the database that don't have a title.</span></span>

<span data-ttu-id="51a56-186">Když jste hotovi a přechod na další řádek, Návrhář bude vypadat jako na tomto obrázku:</span><span class="sxs-lookup"><span data-stu-id="51a56-186">When you're done and move to the next row, the designer looks like this illustration:</span></span>

![Služba WebMatrix návrháře databáze po definování název sloupce pro tabulku filmy](displaying-data/_static/image8.png)

<span data-ttu-id="51a56-188">Opakováním těchto kroků můžete vytvořit sloupec s názvem "Žánr", s výjimkou délky, nastavte ho na právě 30.</span><span class="sxs-lookup"><span data-stu-id="51a56-188">Repeat these steps to create a column named "Genre", except for the length, set it to just 30.</span></span>

<span data-ttu-id="51a56-189">Vytvořit jiný sloupec s názvem "Rok."</span><span class="sxs-lookup"><span data-stu-id="51a56-189">Create another column named "Year."</span></span> <span data-ttu-id="51a56-190">Datový typ, zvolte **nchar** (ne **nvarchar**) a nastavit délku 4.</span><span class="sxs-lookup"><span data-stu-id="51a56-190">For the data type, choose **nchar** (not **nvarchar**) and set the length to 4.</span></span> <span data-ttu-id="51a56-191">Za rok budete používat 4 číslice jako "1995" nebo "2010", takže nevyžadují proměnlivé velikosti sloupce.</span><span class="sxs-lookup"><span data-stu-id="51a56-191">For the year, you're going to use a 4-digit number like "1995" or "2010", so you don't require a variable-sized column.</span></span>

<span data-ttu-id="51a56-192">Zde je, jak vypadá dokončení návrhu:</span><span class="sxs-lookup"><span data-stu-id="51a56-192">Here's what the finished design looks like:</span></span>

![Služba WebMatrix návrháře databáze po všechna pole jsou definována pro tabulku filmy](displaying-data/_static/image9.png)

<span data-ttu-id="51a56-194">Stisknutím klávesy Ctrl + S nebo kliknutím **Uložit** tlačítko na panelu nástrojů Rychlý přístup.</span><span class="sxs-lookup"><span data-stu-id="51a56-194">Press Ctrl+S or click the **Save** button in the Quick Access toolbar.</span></span> <span data-ttu-id="51a56-195">Když zavřete kartu zavřete návrháře databáze.</span><span class="sxs-lookup"><span data-stu-id="51a56-195">Close the database designer by closing the tab.</span></span>

## <a name="adding-some-example-data"></a><span data-ttu-id="51a56-196">Přidání některých ukázková Data</span><span class="sxs-lookup"><span data-stu-id="51a56-196">Adding Some Example Data</span></span>

<span data-ttu-id="51a56-197">Dále v této sérii kurzů vytvoříte stránky, kde můžete zadat nové filmy ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="51a56-197">Later in this tutorial series you'll create a page where you can enter new movies in a form.</span></span> <span data-ttu-id="51a56-198">Prozatím se však můžete přidat některé ukázková data, která pak můžete zobrazit na stránce.</span><span class="sxs-lookup"><span data-stu-id="51a56-198">For now, however, you can add some example data that you can then display on a page.</span></span>

<span data-ttu-id="51a56-199">V **databáze** pracovního prostoru v nástroji WebMatrix, Všimněte si, že je strom, který ukazuje *SDF* souborů, které jste vytvořili dříve.</span><span class="sxs-lookup"><span data-stu-id="51a56-199">In the **Database** workspace in WebMatrix, notice that there's a tree that shows you the *.sdf* file you created earlier.</span></span> <span data-ttu-id="51a56-200">Otevřete uzel nové *SDF* souboru a pak otevřete **tabulky** uzlu.</span><span class="sxs-lookup"><span data-stu-id="51a56-200">Open the node for your new *.sdf* file, and then open the **Tables** node.</span></span>

![Služba WebMatrix databáze pracovního prostoru s stromu otevřít na filmy tabulku](displaying-data/_static/image10.png)

<span data-ttu-id="51a56-202">Klikněte pravým tlačítkem myši **filmy** uzlu a pak zvolte možnost **Data**.</span><span class="sxs-lookup"><span data-stu-id="51a56-202">Right-click the **Movies** node and then choose **Data**.</span></span> <span data-ttu-id="51a56-203">Služba WebMatrix se otevře do mřížky, kde můžete zadat data pro *filmy* tabulky:</span><span class="sxs-lookup"><span data-stu-id="51a56-203">WebMatrix opens a grid where you can enter data for the *Movies* table:</span></span>

![Databáze položky mřížky ve službě WebMatrix (prázdné)](displaying-data/_static/image11.png)

<span data-ttu-id="51a56-205">Klikněte na tlačítko **název** sloupci a zadejte "Při Harry splněny Sally".</span><span class="sxs-lookup"><span data-stu-id="51a56-205">Click the **Title** column and enter "When Harry Met Sally".</span></span> <span data-ttu-id="51a56-206">Přesunout **žánr** sloupce (můžete použít klávesu Tab) a zadejte "Milostné komedie".</span><span class="sxs-lookup"><span data-stu-id="51a56-206">Move to the **Genre** column (you can use the Tab key) and enter "Romantic Comedy".</span></span> <span data-ttu-id="51a56-207">Přesunout **rok** sloupci a zadejte "1989":</span><span class="sxs-lookup"><span data-stu-id="51a56-207">Move to the **Year** column and enter "1989":</span></span>

![Položky mřížky databáze v nástroji WebMatrix s jedním záznamem](displaying-data/_static/image12.png)

<span data-ttu-id="51a56-209">Stisknutím klávesy Enter a službě WebMatrix uloží nový film.</span><span class="sxs-lookup"><span data-stu-id="51a56-209">Press Enter, and WebMatrix saves the new movie.</span></span> <span data-ttu-id="51a56-210">Všimněte si, **ID** bylo vyplněno sloupce.</span><span class="sxs-lookup"><span data-stu-id="51a56-210">Notice that the **ID** column has been filled in.</span></span>

![Položky mřížky databáze v nástroji WebMatrix s jedním záznamem a automaticky generovaný kód](displaying-data/_static/image13.png)

<span data-ttu-id="51a56-212">Zadejte jiný film (například "pryč s the větru", "Drama", "1939").</span><span class="sxs-lookup"><span data-stu-id="51a56-212">Enter another movie (for example, "Gone with the Wind", "Drama", "1939").</span></span> <span data-ttu-id="51a56-213">Sloupec ID vyplněno znovu:</span><span class="sxs-lookup"><span data-stu-id="51a56-213">The ID column is filled in again:</span></span>

![Položky mřížky databáze v nástroji WebMatrix se dva záznamy a automaticky generované ID](displaying-data/_static/image14.png)

<span data-ttu-id="51a56-215">Zadejte třetí movie (například "Ghostbusters", "Komedie").</span><span class="sxs-lookup"><span data-stu-id="51a56-215">Enter a third movie (for example, "Ghostbusters", "Comedy").</span></span> <span data-ttu-id="51a56-216">Jako experiment, nechat **rok** sloupce prázdné a potom stiskněte klávesu Enter.</span><span class="sxs-lookup"><span data-stu-id="51a56-216">As an experiment, leave the **Year** column blank and then press Enter.</span></span> <span data-ttu-id="51a56-217">Protože jste zrušení výběru **Allow Nulls** možnost, ukazuje chybu, databáze:</span><span class="sxs-lookup"><span data-stu-id="51a56-217">Because you unselected the **Allow Nulls** option, the database shows an error:</span></span>

![Pokud požadovaný sloupec hodnota je prázdné, zobrazí se chyba 'neplatná data.](displaying-data/_static/image15.png)

<span data-ttu-id="51a56-219">Klikněte na tlačítko **OK** přejděte zpět a opravte položka (pole year pro "Ghostbusters" je 1984) a stiskněte klávesu Enter.</span><span class="sxs-lookup"><span data-stu-id="51a56-219">Click **OK** to go back and fix the entry (the year for "Ghostbusters" is 1984), and then press Enter.</span></span>

<span data-ttu-id="51a56-220">Vyplňte několik filmy, dokud nebudete mít 8 nebo tak.</span><span class="sxs-lookup"><span data-stu-id="51a56-220">Fill in several movies until you have 8 or so.</span></span> <span data-ttu-id="51a56-221">(Zadání 8 usnadňuje práci s stránkování později.</span><span class="sxs-lookup"><span data-stu-id="51a56-221">(Entering 8 makes it easier to work with paging later.</span></span> <span data-ttu-id="51a56-222">Ale pokud se jedná o příliš mnoho, zadejte jenom některé zatím.) Skutečná data nezáleží.</span><span class="sxs-lookup"><span data-stu-id="51a56-222">But if that's too many, enter just a few for now.) The actual data doesn't matter.</span></span>

![Položky mřížky databáze v nástroji WebMatrix se buď záznamy](displaying-data/_static/image16.png)

<span data-ttu-id="51a56-224">Pokud jste zadali všechny filmy bez chyb, jsou hodnoty ID sekvenční.</span><span class="sxs-lookup"><span data-stu-id="51a56-224">If you entered all the movies without any errors, the ID values are sequential.</span></span> <span data-ttu-id="51a56-225">Pokud jste se pokusili uložit záznam neúplné film, nemusí být čísla ID sekvenční.</span><span class="sxs-lookup"><span data-stu-id="51a56-225">If you tried to save an incomplete movie record, the ID numbers might not be sequential.</span></span> <span data-ttu-id="51a56-226">Pokud ano, který je v pořádku.</span><span class="sxs-lookup"><span data-stu-id="51a56-226">If so, that's okay.</span></span> <span data-ttu-id="51a56-227">Tato čísla nemají žádné nepsaný význam a jediné, co je důležité je, že jsou jedinečné *filmy* tabulky.</span><span class="sxs-lookup"><span data-stu-id="51a56-227">The numbers don't have any inherent meaning, and the only thing that's important is that they're unique in the *Movies* table.</span></span>

<span data-ttu-id="51a56-228">Zavřete kartu, která obsahuje návrháře databáze.</span><span class="sxs-lookup"><span data-stu-id="51a56-228">Close the tab that contains the database designer.</span></span>

<span data-ttu-id="51a56-229">Teď můžete zapnout pro zobrazení těchto dat na webové stránce.</span><span class="sxs-lookup"><span data-stu-id="51a56-229">Now you can turn to displaying this data on a web page.</span></span>

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a><span data-ttu-id="51a56-230">Zobrazení dat na stránce s využitím pomocné rutiny WebGrid</span><span class="sxs-lookup"><span data-stu-id="51a56-230">Displaying Data in a Page by Using the WebGrid Helper</span></span>

<span data-ttu-id="51a56-231">K zobrazení dat na stránce, se chystáte použít `WebGrid` pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="51a56-231">To display data in a page, you're going to use the `WebGrid` helper.</span></span> <span data-ttu-id="51a56-232">Tato pomocná vytvoří zobrazení v mřížce nebo tabulce (řádků a sloupců).</span><span class="sxs-lookup"><span data-stu-id="51a56-232">This helper produces a display in a grid or table (rows and columns).</span></span> <span data-ttu-id="51a56-233">Jak uvidíte, bude možné upravit mřížku formátování a další funkce.</span><span class="sxs-lookup"><span data-stu-id="51a56-233">As you'll see, you'll be able refine the grid with formatting and other features.</span></span>

<span data-ttu-id="51a56-234">Ke spuštění mřížky, budete muset napsat pár řádků kódu.</span><span class="sxs-lookup"><span data-stu-id="51a56-234">To run the grid, you'll have to write a few lines of code.</span></span> <span data-ttu-id="51a56-235">Těchto několik řádků bude sloužit jako druh vzor pro téměř všechny přístup k datům, které provedete v tomto kurzu.</span><span class="sxs-lookup"><span data-stu-id="51a56-235">These few lines will serve as a kind of pattern for almost all of the data access that you do in this tutorial.</span></span>

> [!NOTE]
> <span data-ttu-id="51a56-236">Ve skutečnosti máte celou řadu možností pro zobrazení dat na stránce. `WebGrid` pomocné rutiny je jen jednou.</span><span class="sxs-lookup"><span data-stu-id="51a56-236">You actually have many options for displaying data on a page; the `WebGrid` helper is just one.</span></span> <span data-ttu-id="51a56-237">Rozhodli jsme se pro účely tohoto kurzu vzhledem k tomu, že je nejjednodušší způsob, jak zobrazit data, a proto je poměrně flexibilní.</span><span class="sxs-lookup"><span data-stu-id="51a56-237">We chose it for this tutorial because it's the easiest way to display data and because it's reasonably flexible.</span></span> <span data-ttu-id="51a56-238">V následující kurz uvidíte, jak používat další "Ruční" způsob, jak pracovat s daty na stránce, která uděluje více přímou kontrolu nad způsob zobrazení dat.</span><span class="sxs-lookup"><span data-stu-id="51a56-238">In the next tutorial set, you'll see how to use a more "manual" way to work with data in the page, which gives you more direct control over how to display the data.</span></span>


<span data-ttu-id="51a56-239">V levém podokně v nástroji WebMatrix, klikněte na tlačítko **soubory** pracovního prostoru.</span><span class="sxs-lookup"><span data-stu-id="51a56-239">In the left pane in WebMatrix, click the **Files** workspace.</span></span>

<span data-ttu-id="51a56-240">Je nový vytvořená databáze zobrazí v *aplikace\_Data* složky.</span><span class="sxs-lookup"><span data-stu-id="51a56-240">The new database you created is in the *App\_Data* folder.</span></span> <span data-ttu-id="51a56-241">Pokud složka ještě neexistuje, služba WebMatrix vytvoření nové databáze.</span><span class="sxs-lookup"><span data-stu-id="51a56-241">If the folder didn't already exist, WebMatrix created it for your new database.</span></span> <span data-ttu-id="51a56-242">(Složka může mít existoval Pokud jste dříve nainstalovali pomocné rutiny.)</span><span class="sxs-lookup"><span data-stu-id="51a56-242">(The folder might have existed if you'd previously installed helpers.)</span></span>

<span data-ttu-id="51a56-243">Ve stromovém zobrazení vyberte kořenový Web.</span><span class="sxs-lookup"><span data-stu-id="51a56-243">In the tree view, select the root of the website.</span></span> <span data-ttu-id="51a56-244">Musíte vybrat kořenový web; v opačném případě může do aplikace přidá nový soubor\_složku Data.</span><span class="sxs-lookup"><span data-stu-id="51a56-244">You must select the root of the website; otherwise, the new file might be added to the App\_Data folder.</span></span>

<span data-ttu-id="51a56-245">Na pásu karet klikněte na tlačítko **nový**.</span><span class="sxs-lookup"><span data-stu-id="51a56-245">In the ribbon, click **New**.</span></span> <span data-ttu-id="51a56-246">V **vyberte typ souboru** zvolte **CSHTML**.</span><span class="sxs-lookup"><span data-stu-id="51a56-246">In the **Choose a File Type** box, choose **CSHTML**.</span></span>

<span data-ttu-id="51a56-247">V **název** pole, zadejte název nové stránky "Movies.cshtml":</span><span class="sxs-lookup"><span data-stu-id="51a56-247">In the **Name** box, name the new page "Movies.cshtml":</span></span>

!["Vyberte typ souboru" dialogové okno stránkou "videa.](displaying-data/_static/image17.png)

<span data-ttu-id="51a56-249">Klikněte na tlačítko **OK** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="51a56-249">Click the **OK** button.</span></span> <span data-ttu-id="51a56-250">Služba WebMatrix otevře nový soubor s některé kostru prvky v ní.</span><span class="sxs-lookup"><span data-stu-id="51a56-250">WebMatrix opens a new file with some skeleton elements in it.</span></span> <span data-ttu-id="51a56-251">Nejprve napíšete kód získá data z databáze.</span><span class="sxs-lookup"><span data-stu-id="51a56-251">First you'll write some code to go get the data from the database.</span></span> <span data-ttu-id="51a56-252">Potom přidáte kód na stránku ve skutečnosti zobrazit data.</span><span class="sxs-lookup"><span data-stu-id="51a56-252">Then you'll add markup to the page to actually display the data.</span></span>

### <a name="writing-the-data-query-code"></a><span data-ttu-id="51a56-253">Psaní kódu dotazování dat</span><span class="sxs-lookup"><span data-stu-id="51a56-253">Writing the Data Query Code</span></span>

<span data-ttu-id="51a56-254">V horní části stránky mezi `@{` a `}` znaky, zadejte následující kód.</span><span class="sxs-lookup"><span data-stu-id="51a56-254">At the top of the page, between the `@{` and `}` characters, enter the following code.</span></span> <span data-ttu-id="51a56-255">(Ujistěte se, že zadáte tento kód mezi otevírací a uzavírací závorky.)</span><span class="sxs-lookup"><span data-stu-id="51a56-255">(Make sure that you enter this code between the opening and closing braces.)</span></span>

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

<span data-ttu-id="51a56-256">První řádek se otevře databáze, kterou jste vytvořili dříve, což je vždy první krok před provedením něco s databází.</span><span class="sxs-lookup"><span data-stu-id="51a56-256">The first line opens the database that you created earlier, which is always the first step before doing something with the database.</span></span> <span data-ttu-id="51a56-257">Dáte `Database.Open` metoda název databáze, kterou chcete otevřít.</span><span class="sxs-lookup"><span data-stu-id="51a56-257">You tell the `Database.Open` method name of the database to open.</span></span> <span data-ttu-id="51a56-258">Všimněte si, že nechcete zahrnout *SDF* v názvu.</span><span class="sxs-lookup"><span data-stu-id="51a56-258">Notice that you don't include *.sdf* in the name.</span></span> <span data-ttu-id="51a56-259">`Open` Metoda předpokládá, že hledá *SDF* souboru (to znamená, *WebPagesMovies.sdf*) a že *SDF* soubor *aplikace\_ Data* složky.</span><span class="sxs-lookup"><span data-stu-id="51a56-259">The `Open` method assumes that it's looking for an *.sdf* file (that is, *WebPagesMovies.sdf*) and that the *.sdf* file is in the *App\_Data* folder.</span></span> <span data-ttu-id="51a56-260">(Dříve jsme už uvedli, který *aplikace\_Data* vyhrazené složky; Tento scénář je jeden z míst, kde technologie ASP.NET je předpoklady o tímto názvem.)</span><span class="sxs-lookup"><span data-stu-id="51a56-260">(Earlier we noted that the *App\_Data* folder is reserved; this scenario is one of the places where ASP.NET makes assumptions about that name.)</span></span>

<span data-ttu-id="51a56-261">Při otevření databáze na ni odkaz přejde do proměnné s názvem `db`.</span><span class="sxs-lookup"><span data-stu-id="51a56-261">When the database is opened, a reference to it is put into the variable named `db`.</span></span> <span data-ttu-id="51a56-262">(Která by mohla mít název nic.) `db` Proměnná je, jak budete mít nakonec interakce s databází.</span><span class="sxs-lookup"><span data-stu-id="51a56-262">(Which could be named anything.) The `db` variable is how you'll end up interacting with the database.</span></span>

<span data-ttu-id="51a56-263">Druhý řádek ve skutečnosti načte data databáze s použitím `Query` metody.</span><span class="sxs-lookup"><span data-stu-id="51a56-263">The second line actually fetches the database data by using the `Query` method.</span></span> <span data-ttu-id="51a56-264">Všimněte si, jak funguje takto: `db` proměnná obsahuje odkaz na databázi otevřený a je zapotřebí vyvolat `Query` metoda pomocí `db` proměnné (`db.Query`).</span><span class="sxs-lookup"><span data-stu-id="51a56-264">Notice how this code works: the `db` variable has a reference to the opened database, and you invoke the `Query` method by using the `db` variable (`db.Query`).</span></span>

<span data-ttu-id="51a56-265">Je samotný dotaz SQL `Select` příkazu.</span><span class="sxs-lookup"><span data-stu-id="51a56-265">The query itself is a SQL `Select` statement.</span></span> <span data-ttu-id="51a56-266">(Trochu související informace o SQL, viz vysvětlení později.) V příkazu `Movies` identifikuje tabulku pro dotaz.</span><span class="sxs-lookup"><span data-stu-id="51a56-266">(For a little background about SQL, see the explanation later.) In the statement, `Movies` identifies the table to query.</span></span> <span data-ttu-id="51a56-267">`*` Znaků určuje, že dotaz by měl vrátit všechny sloupce z tabulky.</span><span class="sxs-lookup"><span data-stu-id="51a56-267">The `*` character specifies that the query should return all the columns from the table.</span></span> <span data-ttu-id="51a56-268">(Může být také seznam sloupců jednotlivě, oddělené čárkami.)</span><span class="sxs-lookup"><span data-stu-id="51a56-268">(You could also list columns individually, separated by commas.)</span></span>

<span data-ttu-id="51a56-269">Výsledky dotazu, pokud existuje, jsou vráceny a k dispozici v `selectedData` proměnné.</span><span class="sxs-lookup"><span data-stu-id="51a56-269">The results of the query, if any, are returned and made available in the `selectedData` variable.</span></span> <span data-ttu-id="51a56-270">Může mít znovu, název proměnné, cokoli.</span><span class="sxs-lookup"><span data-stu-id="51a56-270">Again, the variable could be named anything.</span></span>

<span data-ttu-id="51a56-271">Nakonec třetí řádek říká technologie ASP.NET, že chcete použít instanci `WebGrid` pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="51a56-271">Finally, the third line tells ASP.NET that you want to use an instance of the `WebGrid` helper.</span></span> <span data-ttu-id="51a56-272">Vytvoříte (*vytvořit instanci*) pomocný objekt s použitím `new` – klíčové slovo a předat ji přes výsledky dotazu `selectedData` proměnné.</span><span class="sxs-lookup"><span data-stu-id="51a56-272">You create (*instantiate*) the helper object by using the `new` keyword and pass it the query results via the `selectedData` variable.</span></span> <span data-ttu-id="51a56-273">Nové `WebGrid` jsou k dispozici v objektu, spolu s výsledky dotazu databáze `grid` proměnné.</span><span class="sxs-lookup"><span data-stu-id="51a56-273">The new `WebGrid` object, along with the results of the database query, are made available in the `grid` variable.</span></span> <span data-ttu-id="51a56-274">Bude nutné tento výsledek v tuto chvíli ve skutečnosti zobrazit data na stránce.</span><span class="sxs-lookup"><span data-stu-id="51a56-274">You'll need that result in a moment to actually display the data in the page.</span></span>

<span data-ttu-id="51a56-275">V této fázi databázi otevřít, zobrazila data mají být, a jste připravili `WebGrid` pomocné rutiny s daty.</span><span class="sxs-lookup"><span data-stu-id="51a56-275">At this stage, the database has been opened, you've gotten the data you want, and you've prepared the `WebGrid` helper with that data.</span></span> <span data-ttu-id="51a56-276">Dále je vytvoření značky na stránce.</span><span class="sxs-lookup"><span data-stu-id="51a56-276">Next is to create the markup in the page.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="51a56-277">**Structured Query Language (SQL)**</span><span class="sxs-lookup"><span data-stu-id="51a56-277">**Structured Query Language (SQL)**</span></span>
> 
> <span data-ttu-id="51a56-278">SQL je jazyk, který se používá v Většina relačních databází pro správu dat v databázi.</span><span class="sxs-lookup"><span data-stu-id="51a56-278">SQL is a language that's used in most relational databases for managing data in a database.</span></span> <span data-ttu-id="51a56-279">Obsahuje příkazy, které umožňují načtení dat a aktualizovat je a, které umožňují vytvářet, upravovat a spravovat data v tabulkách databáze.</span><span class="sxs-lookup"><span data-stu-id="51a56-279">It includes commands that let you retrieve data and update it, and that let you create, modify, and manage data in database tables.</span></span> <span data-ttu-id="51a56-280">SQL se liší od programovací jazyk (jako je C#).</span><span class="sxs-lookup"><span data-stu-id="51a56-280">SQL is different than a programming language (like C#).</span></span> <span data-ttu-id="51a56-281">Pomocí jazyka SQL dáte databáze, co má a je databáze úlohu zjistit, jak získat data nebo provést úlohu.</span><span class="sxs-lookup"><span data-stu-id="51a56-281">With SQL, you tell the database what you want, and it's the database's job to figure out how to get the data or perform the task.</span></span> <span data-ttu-id="51a56-282">Tady jsou příklady některých příkazů SQL a jejich význam:</span><span class="sxs-lookup"><span data-stu-id="51a56-282">Here are examples of some SQL commands and what they do:</span></span>
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> <span data-ttu-id="51a56-283">První `Select` příkaz načte všechny sloupce (určená `*`) z *filmy* tabulky.</span><span class="sxs-lookup"><span data-stu-id="51a56-283">The first `Select` statement gets all the columns (specified by `*`) from the *Movies* table.</span></span>
> 
> <span data-ttu-id="51a56-284">Druhá `Select` příkaz načte ID, název a cena sloupce ze záznamů ve *produktu* tabulku, jejíž hodnota sloupce cena je více než 10.</span><span class="sxs-lookup"><span data-stu-id="51a56-284">The second `Select` statement fetches the ID, Name, and Price columns from records in the *Product* table whose Price column value is more than 10.</span></span> <span data-ttu-id="51a56-285">Příkaz vrátí výsledky v abecedním pořadí podle hodnot ve sloupci Název.</span><span class="sxs-lookup"><span data-stu-id="51a56-285">The command returns the results in alphabetical order based on the values of the Name column.</span></span> <span data-ttu-id="51a56-286">Pokud cenu kritériím neodpovídají žádné záznamy, příkaz vrátí prázdnou sadou.</span><span class="sxs-lookup"><span data-stu-id="51a56-286">If no records match the price criteria, the command returns an empty set.</span></span>
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> <span data-ttu-id="51a56-287">Tento příkaz vloží nový záznam do *produktu* tabulku nastavení sloupec název "Croissant", ve sloupci Popis na "Nespolehlivé naprostou spokojenost A" a cena na 1,99.</span><span class="sxs-lookup"><span data-stu-id="51a56-287">This command inserts a new record into the *Product* table, setting the Name column to "Croissant", the Description column to "A flaky delight", and the price to 1.99.</span></span>
> 
> <span data-ttu-id="51a56-288">Všimněte si, že když zadáváte nečíselná hodnota, hodnota se udává v jednoduchých uvozovkách (ne dvojitých uvozovek, stejně jako v jazyce C#).</span><span class="sxs-lookup"><span data-stu-id="51a56-288">Notice that when you're specifying a non-numeric value, the value is enclosed in single quotation marks (not double quotation marks, as in C#).</span></span> <span data-ttu-id="51a56-289">Můžete použít tyto uvozovky kolem hodnot textu nebo datum, ale nikoli celého čísla.</span><span class="sxs-lookup"><span data-stu-id="51a56-289">You use these quotation marks around text or date values, but not around numbers.</span></span>
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> <span data-ttu-id="51a56-290">Tento příkaz odstraní záznamy *produktu* tabulky, jehož sloupec Datum vypršení platnosti je starší než 1. ledna 2008.</span><span class="sxs-lookup"><span data-stu-id="51a56-290">This command deletes records in the *Product* table whose expiration date column is earlier than January 1, 2008.</span></span> <span data-ttu-id="51a56-291">(Příkaz předpokládá, že *produktu* tabulka obsahuje sloupce, samozřejmě.) Datum je ve formátu MM/DD/RRRR tady zadáte, ale by měly být zadány ve formátu, který se používá pro vaše národní prostředí.</span><span class="sxs-lookup"><span data-stu-id="51a56-291">(The command assumes that the *Product* table has such a column, of course.) The date is entered here in MM/DD/YYYY format, but it should be entered in the format that's used for your locale.</span></span>
> 
> <span data-ttu-id="51a56-292">`Insert` a `Delete` příkazy nevracejte sad výsledků dotazu.</span><span class="sxs-lookup"><span data-stu-id="51a56-292">The `Insert` and `Delete` commands don't return result sets.</span></span> <span data-ttu-id="51a56-293">Místo toho vrátí číslo, které se říká, kolik záznamů byly ovlivněny příkazu.</span><span class="sxs-lookup"><span data-stu-id="51a56-293">Instead, they return a number that tells you how many records were affected by the command.</span></span>
> 
> <span data-ttu-id="51a56-294">Pro některé z těchto operací (např. vkládání a odstranění záznamů) proces, který žádá o operaci musí mít příslušná oprávnění v databázi.</span><span class="sxs-lookup"><span data-stu-id="51a56-294">For some of these operations (like inserting and deleting records), the process that's requesting the operation has to have appropriate permissions in the database.</span></span> <span data-ttu-id="51a56-295">To je důvod, proč pro produkční databáze je často nutné zadat uživatelské jméno a heslo při připojování k databázi.</span><span class="sxs-lookup"><span data-stu-id="51a56-295">That's why for production databases you often have to supply a user name and password when you connect to the database.</span></span>
> 
> <span data-ttu-id="51a56-296">Existují desítek příkazů SQL, ale všechny se řídí vzorem stejně jako příkazy, kterou tady vidíte.</span><span class="sxs-lookup"><span data-stu-id="51a56-296">There are dozens of SQL commands, but they all follow a pattern like the commands you see here.</span></span> <span data-ttu-id="51a56-297">Příkazy SQL můžete použít k vytvoření databázových tabulek, počet záznamů v tabulce, výpočtu ceny a provádět mnoho dalších operacích.</span><span class="sxs-lookup"><span data-stu-id="51a56-297">You can use SQL commands to create database tables, count the number of records in a table, calculate prices, and perform many more operations.</span></span>


### <a name="adding-markup-to-display-the-data"></a><span data-ttu-id="51a56-298">Přidání značek k zobrazení dat</span><span class="sxs-lookup"><span data-stu-id="51a56-298">Adding Markup to Display the Data</span></span>

<span data-ttu-id="51a56-299">Uvnitř `<head>` elementu, obsah sady `<title>` element na "Filmy":</span><span class="sxs-lookup"><span data-stu-id="51a56-299">Inside the `<head>` element, set contents of the `<title>` element to "Movies":</span></span>

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

<span data-ttu-id="51a56-300">Uvnitř `<body>` element na stránce, přidejte následující:</span><span class="sxs-lookup"><span data-stu-id="51a56-300">Inside the `<body>` element of the page, add the following:</span></span>

[!code-html[Main](displaying-data/samples/sample3.html)]

<span data-ttu-id="51a56-301">Je to.</span><span class="sxs-lookup"><span data-stu-id="51a56-301">That's it.</span></span> <span data-ttu-id="51a56-302">`grid` Proměnné je hodnota jste vytvořili při vytváření `WebGrid` objekt v kódu výše.</span><span class="sxs-lookup"><span data-stu-id="51a56-302">The `grid` variable is the value you created when you created the `WebGrid` object in code earlier.</span></span>

<span data-ttu-id="51a56-303">Služba WebMatrix stromové zobrazení, klikněte pravým tlačítkem na stránku a vyberte **spustit v prohlížeči**.</span><span class="sxs-lookup"><span data-stu-id="51a56-303">In the WebMatrix tree view, right-click the page and select **Launch in browser**.</span></span> <span data-ttu-id="51a56-304">Zobrazí se stránka podobná této:</span><span class="sxs-lookup"><span data-stu-id="51a56-304">You'll see something like this page:</span></span>

![Výchozí WebGrid pomocné rutiny výstup z tabulky filmy](displaying-data/_static/image18.png)

<span data-ttu-id="51a56-306">Kliknutím na odkaz záhlaví sloupců řadit podle sloupce.</span><span class="sxs-lookup"><span data-stu-id="51a56-306">Click a column heading link to sort by that column.</span></span> <span data-ttu-id="51a56-307">Schopnost seřadit klepnutím na záhlaví je funkce, která je integrovaná **WebGrid** pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="51a56-307">Being able to sort by clicking a heading is a feature that's built into the **WebGrid** helper.</span></span>

<span data-ttu-id="51a56-308">`GetHtml` Způsob, jak její název napovídá, generuje kód, který se zobrazí data.</span><span class="sxs-lookup"><span data-stu-id="51a56-308">The `GetHtml` method, as its name suggests, generates markup that displays the data.</span></span> <span data-ttu-id="51a56-309">Ve výchozím nastavení `GetHtml` metoda generuje HTML `<table>` element.</span><span class="sxs-lookup"><span data-stu-id="51a56-309">By default, the `GetHtml` method generates an HTML `<table>` element.</span></span> <span data-ttu-id="51a56-310">(Pokud chcete, můžete ověřit vykreslování zobrazením zdroje stránky v prohlížeči.)</span><span class="sxs-lookup"><span data-stu-id="51a56-310">(If you want, you can verify the rendering by looking at the source of the page in the browser.)</span></span>

## <a name="modifying-the-look-of-the-grid"></a><span data-ttu-id="51a56-311">Změna vzhledu mřížky</span><span class="sxs-lookup"><span data-stu-id="51a56-311">Modifying the Look of the Grid</span></span>

<span data-ttu-id="51a56-312">Použití `WebGrid` pomocné rutiny, jako je právě předvedl je jednoduché, ale výsledné zobrazení je jednoduché.</span><span class="sxs-lookup"><span data-stu-id="51a56-312">Using the `WebGrid` helper like you just did is easy, but the resulting display is plain.</span></span> <span data-ttu-id="51a56-313">`WebGrid` Pomocné rutiny má celou řadu možností, které umožňují řídit, jak se data zobrazí.</span><span class="sxs-lookup"><span data-stu-id="51a56-313">The `WebGrid` helper has all sorts of options that let you control how the data is displayed.</span></span> <span data-ttu-id="51a56-314">Existuje příliš mnoho chcete data prozkoumat v tomto kurzu, ale tato část vám poskytne vám představu o některé z těchto možností.</span><span class="sxs-lookup"><span data-stu-id="51a56-314">There are far too many to explore in this tutorial, but this section will give you an idea of some of those options.</span></span> <span data-ttu-id="51a56-315">V dalších kurzech v tomto seriálu se budeme několik dalších možností.</span><span class="sxs-lookup"><span data-stu-id="51a56-315">A few additional options will be covered in later tutorials in this series.</span></span>

### <a name="specifying-individual-columns-to-display"></a><span data-ttu-id="51a56-316">Určení jednotlivé sloupce zobrazení</span><span class="sxs-lookup"><span data-stu-id="51a56-316">Specifying Individual Columns to Display</span></span>

<span data-ttu-id="51a56-317">Pokud chcete začít, můžete určit, že chcete zobrazit jenom určité sloupce.</span><span class="sxs-lookup"><span data-stu-id="51a56-317">To start, you can specify that you want to display only certain columns.</span></span> <span data-ttu-id="51a56-318">Ve výchozím nastavení, jak už víte, mřížky se zobrazí všechny čtyři sloupce *filmy* tabulky.</span><span class="sxs-lookup"><span data-stu-id="51a56-318">By default, as you've seen, the grid shows all four of the columns from the *Movies* table.</span></span>

<span data-ttu-id="51a56-319">V *Movies.cshtml* souboru, nahradí `@grid.GetHtml()` kód, který jste právě přidali následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="51a56-319">In the *Movies.cshtml* file, replace the `@grid.GetHtml()` markup that you just added with the following:</span></span>

[!code-css[Main](displaying-data/samples/sample4.css)]

<span data-ttu-id="51a56-320">Zjistit pomocné rutiny, které sloupce se zobrazí, můžete zahrnout `columns` parametr pro `GetHtml` a předáte v kolekci sloupců.</span><span class="sxs-lookup"><span data-stu-id="51a56-320">To tell the helper which columns to display, you include a `columns` parameter for the `GetHtml` method and pass in a collection of columns.</span></span> <span data-ttu-id="51a56-321">V kolekci zadejte jednotlivé sloupce, které chcete zahrnout.</span><span class="sxs-lookup"><span data-stu-id="51a56-321">In the collection, you specify each column to include.</span></span> <span data-ttu-id="51a56-322">Zadejte jednotlivé sloupce pro zobrazení zahrnutím `grid.Column` objektu a předejte název sloupce dat, který chcete.</span><span class="sxs-lookup"><span data-stu-id="51a56-322">You specify an individual column to display by including a `grid.Column` object, and pass in the name of the data column you want.</span></span> <span data-ttu-id="51a56-323">(Tyto sloupce musí být součástí výsledků dotazu SQL – `WebGrid` pomocné rutiny nemůže zobrazit sloupce, které nebyly vrácených dotazem.)</span><span class="sxs-lookup"><span data-stu-id="51a56-323">(These columns must be included in the SQL query results — the `WebGrid` helper cannot display columns that were not returned by the query.)</span></span>

<span data-ttu-id="51a56-324">Spusťte *Movies.cshtml* stránku v prohlížeči znovu a tentokrát získat zobrazení jako na následující (Všimněte si, že je zobrazen žádný sloupec ID):</span><span class="sxs-lookup"><span data-stu-id="51a56-324">Launch the *Movies.cshtml* page in the browser again, and this time you get a display like the following one (notice that no ID column is displayed):</span></span>

![Zobrazení WebGrid zobrazující pouze vybrané sloupce](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a><span data-ttu-id="51a56-326">Změna vzhledu mřížky</span><span class="sxs-lookup"><span data-stu-id="51a56-326">Changing the Look of the Grid</span></span>

<span data-ttu-id="51a56-327">Existuje mnoho další možnosti pro zobrazení sloupce, z nichž některé budeme se zabývat v dalších kurzech v této sadě.</span><span class="sxs-lookup"><span data-stu-id="51a56-327">There are quite a few more options for displaying columns, some of which will be explored in later tutorials in this set.</span></span> <span data-ttu-id="51a56-328">Prozatím se v této části vás seznámí s možnosti, ve kterém můžete styl mřížky jako celek.</span><span class="sxs-lookup"><span data-stu-id="51a56-328">For now, this section will introduce you to ways in which you can style the grid as a whole.</span></span>

<span data-ttu-id="51a56-329">Uvnitř `<head>` části stránky těsně před uzavírací `</head>` značky, přidejte následující `<style>` element:</span><span class="sxs-lookup"><span data-stu-id="51a56-329">Inside the `<head>` section of the page, just before the closing `</head>` tag, add the following `<style>` element:</span></span>

[!code-css[Main](displaying-data/samples/sample5.css)]

<span data-ttu-id="51a56-330">Tento kód šablony stylů CSS definuje třídy s názvem `grid`, `head`, a tak dále.</span><span class="sxs-lookup"><span data-stu-id="51a56-330">This CSS markup defines classes named `grid`, `head`, and so on.</span></span> <span data-ttu-id="51a56-331">Můžete také umístit tyto definice stylů v samostatném *.css* souboru a odkaz na stránku.</span><span class="sxs-lookup"><span data-stu-id="51a56-331">You could also put these style definitions in a separate *.css* file and link that to the page.</span></span> <span data-ttu-id="51a56-332">(Ve skutečnosti, můžete to udělat později v této sérii kurzů.) Ale aby to bylo snadné pro účely tohoto kurzu, jsou na stejné stránce, která zobrazuje data.</span><span class="sxs-lookup"><span data-stu-id="51a56-332">(In fact, you'll do that later in this tutorial set.) But to make things easy for this tutorial, they're inside the same page that displays the data.</span></span>

<span data-ttu-id="51a56-333">Teď si můžete nechat `WebGrid` pomocné rutiny k použití těchto tříd style.</span><span class="sxs-lookup"><span data-stu-id="51a56-333">Now you can get the `WebGrid` helper to use these style classes.</span></span> <span data-ttu-id="51a56-334">Pomocná rutina má několik vlastností (například `tableStyle`) pro právě tento účel – k nim přiřadíte název třídy stylu CSS, a tento název třídy je vykreslen jako součást kód, který je vykreslen metodou pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="51a56-334">The helper has a number of properties (for example, `tableStyle`) for just this purpose — you assign a CSS style class name to them, and that class name is rendered as part of the markup that's rendered by the helper.</span></span>

<span data-ttu-id="51a56-335">Změnit `grid.GetHtml` kód tak, že teď vypadá nějak takto:</span><span class="sxs-lookup"><span data-stu-id="51a56-335">Change the `grid.GetHtml` markup so that it now looks like this code:</span></span>

[!code-css[Main](displaying-data/samples/sample6.css)]

<span data-ttu-id="51a56-336">Co je nového v tomto poli je, že jste přidali `tableStyle`, `headerStyle`, a `alternatingRowStyle` parametrů `GetHtml` metody.</span><span class="sxs-lookup"><span data-stu-id="51a56-336">What's new here is that you've added `tableStyle`, `headerStyle`, and `alternatingRowStyle` parameters to the `GetHtml` method.</span></span> <span data-ttu-id="51a56-337">Tyto parametry jsou nastavené na názvy, které jste přidali před chvílí styly CSS.</span><span class="sxs-lookup"><span data-stu-id="51a56-337">These parameters have been set to the names of the CSS styles that you added a moment ago.</span></span>

<span data-ttu-id="51a56-338">Spuštění stránky a tentokrát se zobrazí v mřížce, která vypadá mnohem méně prostý než před:</span><span class="sxs-lookup"><span data-stu-id="51a56-338">Run the page, and this time you see a grid that looks much less plain than before:</span></span>

![WebGrid zobrazení, která obsahuje parametry nastavena na názvy tříd šablon stylů CSS](displaying-data/_static/image20.png)

<span data-ttu-id="51a56-340">Abyste zjistili, co `GetHtml` metodou, vygenerované, můžete se podívat na zdroji stránku v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="51a56-340">To see what the `GetHtml` method generated, you can look at the source of the page in the browser.</span></span> <span data-ttu-id="51a56-341">Nebude přejdeme do podrobností tady ale důležité je, že tak, že zadáte parametry jako `tableStyle`, způsobil mřížky mají vygenerovat značky HTML vypadat asi takto:</span><span class="sxs-lookup"><span data-stu-id="51a56-341">We won't go into detail here, but the important point is that by specifying parameters like `tableStyle`, you caused the grid to generate HTML tags like the following:</span></span>

`<table class="grid">`

<span data-ttu-id="51a56-342">`<table>` Měl značky `class` do ní přidat atribut, který odkazuje na jeden z pravidel šablon stylů CSS, které jste přidali dříve.</span><span class="sxs-lookup"><span data-stu-id="51a56-342">The `<table>` tag has had a `class` attribute added to it that references one of the CSS rules that you added earlier.</span></span> <span data-ttu-id="51a56-343">Tento kód vám zobrazuje základní vzor &mdash; různé parametry pro `GetHtml` metoda umožňují vám odkaz šablon stylů CSS třídy, poté vygeneruje metodu spolu s značky.</span><span class="sxs-lookup"><span data-stu-id="51a56-343">This code shows you the basic pattern &mdash; different parameters for the `GetHtml` method let you reference CSS classes that the method then generates along with the markup.</span></span> <span data-ttu-id="51a56-344">Co dělat s třídami šablony stylů CSS je na vás.</span><span class="sxs-lookup"><span data-stu-id="51a56-344">What you do with the CSS classes is up to you.</span></span>

## <a name="adding-paging"></a><span data-ttu-id="51a56-345">Přidání stránkování</span><span class="sxs-lookup"><span data-stu-id="51a56-345">Adding Paging</span></span>

<span data-ttu-id="51a56-346">Jako poslední úlohy pro účely tohoto kurzu přidáte stránkování mřížky.</span><span class="sxs-lookup"><span data-stu-id="51a56-346">As the last task for this tutorial, you'll add paging to the grid.</span></span> <span data-ttu-id="51a56-347">V tuto chvíli bez obav zobrazíte všechny filmů najednou.</span><span class="sxs-lookup"><span data-stu-id="51a56-347">Right now it's no problem to display all your movies at once.</span></span> <span data-ttu-id="51a56-348">Ale pokud jste přidali stovky filmy, zobrazení stránky byste získali dlouho.</span><span class="sxs-lookup"><span data-stu-id="51a56-348">But if you added hundreds of movies, the page display would get long.</span></span>

<span data-ttu-id="51a56-349">V kódu stránky, změňte řádek, který vytvoří `WebGrid` objektu v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="51a56-349">In the page code, change the line that creates the `WebGrid` object to the following code:</span></span>

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

<span data-ttu-id="51a56-350">Jediný rozdíl oproti před je, že jste přidali `rowsPerPage` parametr, který je nastaven na 3.</span><span class="sxs-lookup"><span data-stu-id="51a56-350">The only difference from before is that you've added a `rowsPerPage` parameter that's set to 3.</span></span>

<span data-ttu-id="51a56-351">Spuštění stránky.</span><span class="sxs-lookup"><span data-stu-id="51a56-351">Run the page.</span></span> <span data-ttu-id="51a56-352">V mřížce zobrazené na čas prodloužený navigační odkazy, které umožní procházet videa ve vaší databázi 3 řádky:</span><span class="sxs-lookup"><span data-stu-id="51a56-352">The grid displays 3 rows at a time, plus navigation links that let you page through the movies in your database:</span></span>

![Zobrazení WebGrid pomocí stránkování](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a><span data-ttu-id="51a56-354">Chystá se další</span><span class="sxs-lookup"><span data-stu-id="51a56-354">Coming Up Next</span></span>

<span data-ttu-id="51a56-355">V dalším kurzu se dozvíte, jak používat kód Razor a C# k získání vstupu uživatele ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="51a56-355">In the next tutorial, you'll learn how to use Razor and C# code to get user input in a form.</span></span> <span data-ttu-id="51a56-356">Přidáte vyhledávacího pole na stránce videa, abyste našli filmy podle názvu nebo rozšířením podle tematických.</span><span class="sxs-lookup"><span data-stu-id="51a56-356">You'll add a search box to the Movies page so that you can find movies by title or genre.</span></span>

## <a name="complete-listing-for-movies-page"></a><span data-ttu-id="51a56-357">Úplný seznam pro stránku filmy</span><span class="sxs-lookup"><span data-stu-id="51a56-357">Complete Listing for Movies Page</span></span>

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="51a56-358">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="51a56-358">Additional Resources</span></span>

- [<span data-ttu-id="51a56-359">Úvod k programování v prostředí ASP.NET pomocí syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="51a56-359">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> <span data-ttu-id="51a56-360">[Předchozí](intro-to-web-pages-programming.md)
> [další](form-basics.md)</span><span class="sxs-lookup"><span data-stu-id="51a56-360">[Previous](intro-to-web-pages-programming.md)
[Next](form-basics.md)</span></span>
