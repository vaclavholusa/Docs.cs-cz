---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: Představení technologie ASP.NET Web Pages – základy formuláře HTML | Microsoft Docs
author: tfitzmac
description: Tento kurz ukazuje základy toho, jak vytvořit vstupní formulář a jak se zpracovat vstup uživatele při použití technologie ASP.NET Web Pages (Razor). A teď kterou jste...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: 6f44f74774c2fa6338524987779e15f3940d1830
ms.sourcegitcommit: 9a35906446af7ffd4ccfc18daec38874b5abbef7
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 06/20/2018
ms.locfileid: "30898918"
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="cf580-104">Představení technologie ASP.NET Web Pages – základy formuláře HTML</span><span class="sxs-lookup"><span data-stu-id="cf580-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="cf580-105">podle [tní FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="cf580-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="cf580-106">Tento kurz ukazuje základy toho, jak vytvořit vstupní formulář a jak se zpracovat vstup uživatele při použití technologie ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="cf580-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="cf580-107">A teď, když máte k dispozici databáze, budete používat svoje dovednosti formuláře umožníte uživatelům najít konkrétní filmy v databázi.</span><span class="sxs-lookup"><span data-stu-id="cf580-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="cf580-108">Předpokládá, že jste dokončili řady prostřednictvím [Úvod k zobrazení dat pomocí rozhraní ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span><span class="sxs-lookup"><span data-stu-id="cf580-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="cf580-109">Získáte informace:</span><span class="sxs-lookup"><span data-stu-id="cf580-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="cf580-110">Jak vytvořit formulář pomocí standardní elementů HTML.</span><span class="sxs-lookup"><span data-stu-id="cf580-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="cf580-111">Jak si uživatel vstupního ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="cf580-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="cf580-112">Jak vytvořit dotaz SQL, selektivně získá data pomocí hledání termín, uživatel poskytuje.</span><span class="sxs-lookup"><span data-stu-id="cf580-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="cf580-113">Jak obsahovat pole na stránce "nezapomeňte" co uživatel zadal.</span><span class="sxs-lookup"><span data-stu-id="cf580-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="cf580-114">Funkce nebo technologie popsané:</span><span class="sxs-lookup"><span data-stu-id="cf580-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="cf580-115">`Request` Objektu.</span><span class="sxs-lookup"><span data-stu-id="cf580-115">The `Request` object.</span></span>
> - <span data-ttu-id="cf580-116">SQL `Where` klauzule.</span><span class="sxs-lookup"><span data-stu-id="cf580-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="cf580-117">Co budete sestavení</span><span class="sxs-lookup"><span data-stu-id="cf580-117">What You'll Build</span></span>

<span data-ttu-id="cf580-118">V předchozích kurzu vytvořili databázi, do ní přidat data a pak použít `WebGrid` pomocné rutiny pro zobrazení dat.</span><span class="sxs-lookup"><span data-stu-id="cf580-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="cf580-119">V tomto kurzu přidáte vyhledávací pole, která umožňuje najít filmy konkrétní genre nebo jejichž název obsahuje ať slovo, které zadáte.</span><span class="sxs-lookup"><span data-stu-id="cf580-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="cf580-120">(Například budete moci najít všechny filmy, jehož genre je "Action" nebo jejichž název obsahuje "Harry Sverdlove" nebo "Adventure.")</span><span class="sxs-lookup"><span data-stu-id="cf580-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="cf580-121">Po dokončení tohoto kurzu, budete mít stránky podobné následujícímu:</span><span class="sxs-lookup"><span data-stu-id="cf580-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![Stránka filmy s Genre a název vyhledávání](form-basics/_static/image1.png)

<span data-ttu-id="cf580-123">Seznam součástí stránky je stejný jako poslední kurzu &mdash; mřížka.</span><span class="sxs-lookup"><span data-stu-id="cf580-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="cf580-124">Rozdíl bude, že mřížky zobrazí pouze filmy, že jste hledali.</span><span class="sxs-lookup"><span data-stu-id="cf580-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="cf580-125">O formuláře HTML</span><span class="sxs-lookup"><span data-stu-id="cf580-125">About HTML Forms</span></span>

<span data-ttu-id="cf580-126">(Pokud máte zkušenosti s vytvářením formuláře HTML a s rozdíl mezi `GET` a `POST`, můžete tuto část přeskočit.)</span><span class="sxs-lookup"><span data-stu-id="cf580-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="cf580-127">Formulář má elementy vstupu uživatele &mdash; textová pole, tlačítka, přepínače, zaškrtněte políčka, rozevírací seznamy a tak dále.</span><span class="sxs-lookup"><span data-stu-id="cf580-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="cf580-128">Uživatelé vyplnit tyto ovládací prvky nebo možnosti a pak kliknutím na tlačítko odeslání formuláře.</span><span class="sxs-lookup"><span data-stu-id="cf580-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="cf580-129">Tento příklad je zobrazená základní syntaxe HTML ve tvaru:</span><span class="sxs-lookup"><span data-stu-id="cf580-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="cf580-130">Když tento kód spustí na stránce, vytvoří jednoduchý formulář, který vypadá jako na obrázku:</span><span class="sxs-lookup"><span data-stu-id="cf580-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![Základní formuláře HTML jako vykreslené v prohlížeči](form-basics/_static/image2.png)

<span data-ttu-id="cf580-132">`<form>` Element uzavře elementů HTML k odeslání.</span><span class="sxs-lookup"><span data-stu-id="cf580-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="cf580-133">(Snadno chybou, aby je přidat elementy na stránce, ale pak zapomněli jejich uvnitř umístění `<form>` elementu.</span><span class="sxs-lookup"><span data-stu-id="cf580-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="cf580-134">V takovém případě nic je odeslat.) `method` Atribut sděluje prohlížeči, jak odeslat vstup uživatele.</span><span class="sxs-lookup"><span data-stu-id="cf580-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="cf580-135">Nastavíte `post` při provádění aktualizace na serveru nebo na `get` Pokud jste právě načítání dat ze serveru.</span><span class="sxs-lookup"><span data-stu-id="cf580-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="cf580-136">**GET, POST a akce HTTP zabezpečení**</span><span class="sxs-lookup"><span data-stu-id="cf580-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="cf580-137">Protokolu HTTP, protokol, který prohlížeče a servery používají k výměně informací o, je pozoruhodně jednoduchý v jeho základní operace.</span><span class="sxs-lookup"><span data-stu-id="cf580-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="cf580-138">V prohlížečích používá jenom pár operace provádět požadavky na servery.</span><span class="sxs-lookup"><span data-stu-id="cf580-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="cf580-139">Při psaní kódu pro web, je vhodné se seznámit s tyto příkazy a jak je používat prohlížečem a serverem.</span><span class="sxs-lookup"><span data-stu-id="cf580-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="cf580-140">Na nejčastěji používané příkazy a daleko jsou tyto:</span><span class="sxs-lookup"><span data-stu-id="cf580-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="cf580-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="cf580-141">`GET`.</span></span> <span data-ttu-id="cf580-142">Prohlížeč používá tento příkaz se něco načíst ze serveru.</span><span class="sxs-lookup"><span data-stu-id="cf580-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="cf580-143">Například když zadáte adresu URL do prohlížeče, prohlížeč provede `GET` operaci žádosti požadovanou stránku.</span><span class="sxs-lookup"><span data-stu-id="cf580-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="cf580-144">Pokud stránka obsahuje grafiky, prohlížeč provede další `GET` operations získat bitové kopie.</span><span class="sxs-lookup"><span data-stu-id="cf580-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="cf580-145">Pokud `GET` operace obsahuje předávat informace na server, se předá informace jako část adresy URL v řetězci dotazu.</span><span class="sxs-lookup"><span data-stu-id="cf580-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="cf580-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="cf580-146">`POST`.</span></span> <span data-ttu-id="cf580-147">Odešle do prohlížeče `POST` požadavek, aby bylo možné odeslat data přidat nebo změnit na serveru.</span><span class="sxs-lookup"><span data-stu-id="cf580-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="cf580-148">Například `POST` příkaz slouží k vytváření záznamů v databázi nebo změnit stávající.</span><span class="sxs-lookup"><span data-stu-id="cf580-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="cf580-149">Ve většině případů, kdy vyplňte formulář a klikněte na tlačítko pro odeslání, Prohlížeč provádí `POST` operaci.</span><span class="sxs-lookup"><span data-stu-id="cf580-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="cf580-150">V `POST` operace, data předávány na server je v těle stránky.</span><span class="sxs-lookup"><span data-stu-id="cf580-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="cf580-151">Je zásadní rozdíl mezi tyto příkazy, které `GET` operace nemá změnit nic na serveru – a umístí jej něco abstraktnějšího způsobem, `GET` operaci nedojde ke změně stavu na serveru.</span><span class="sxs-lookup"><span data-stu-id="cf580-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="cf580-152">Můžete provádět `GET` operace na stejné prostředky jako tolikrát, kolikrát se vám líbí a tyto prostředky se nezmění.</span><span class="sxs-lookup"><span data-stu-id="cf580-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="cf580-153">(A `GET` operaci často říká, že je jako "bezpečnou", nebo použít technický termín, je *idempotent*.) Naproti tomu samozřejmě `POST` žádost o něco na serveru změní pokaždé, když provádíte operaci.</span><span class="sxs-lookup"><span data-stu-id="cf580-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="cf580-154">Pomůže dva příklady ilustrují tento rozdíl.</span><span class="sxs-lookup"><span data-stu-id="cf580-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="cf580-155">Při hledání pomocí modul jako Bing nebo Google, abyste vyplnili formulář, který se skládá z jednoho textového pole a klikněte na tlačítko Hledat.</span><span class="sxs-lookup"><span data-stu-id="cf580-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="cf580-156">Provede prohlížeče `GET` operace s hodnotou zadanou do pole předanou v rámci adresy URL.</span><span class="sxs-lookup"><span data-stu-id="cf580-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="cf580-157">Použití `GET` operace pro tento typ formuláře je vhodná, protože operace vyhledávání nezmění žádné prostředky na serveru, stačí načte informace.</span><span class="sxs-lookup"><span data-stu-id="cf580-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="cf580-158">Nyní v úvahu proces řazení něco online.</span><span class="sxs-lookup"><span data-stu-id="cf580-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="cf580-159">Zadejte podrobnosti pořadí a pak klikněte na tlačítko pro odeslání.</span><span class="sxs-lookup"><span data-stu-id="cf580-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="cf580-160">Tato operace bude `POST` požadavek, protože operace bude mít za následek změny na serveru, jako je například nový záznam pořadí, změnu informací o vašem účtu a případně mnoho dalších změn.</span><span class="sxs-lookup"><span data-stu-id="cf580-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="cf580-161">Na rozdíl od `GET` operaci, nelze opakovat vaší `POST` žádost – Pokud jste to udělali, pokaždé, když byly opětovně odeslány žádost, by vytvořit novou objednávku na serveru.</span><span class="sxs-lookup"><span data-stu-id="cf580-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="cf580-162">(V takových případech weby často upozornění, není k více než jednou klikněte na tlačítko pro odeslání, nebo bude zakázat tlačítko pro odeslání, takže je nemusíte formulář znovu odešlete omylem.)</span><span class="sxs-lookup"><span data-stu-id="cf580-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="cf580-163">V průběhu tohoto kurzu budete používat i `GET` operace a `POST` operace pro práci s formuláře HTML.</span><span class="sxs-lookup"><span data-stu-id="cf580-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="cf580-164">Vysvětlíme v každém případu důvod, proč je operace, které můžete použít příslušné.</span><span class="sxs-lookup"><span data-stu-id="cf580-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="cf580-165">(Další informace o příkazy HTTP, najdete v článku [metoda definice](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) článku na webu W3C.)</span><span class="sxs-lookup"><span data-stu-id="cf580-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="cf580-166">Většina elementy vstupu uživatele jsou HTML `<input>` elementy.</span><span class="sxs-lookup"><span data-stu-id="cf580-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="cf580-167">Zobrazují se jako `<input type="type" name="name">,` kde *typ* určuje druh uživatele vstupního ovládacího prvku chcete.</span><span class="sxs-lookup"><span data-stu-id="cf580-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="cf580-168">Tyto prvky jsou běžné těm, které jsou:</span><span class="sxs-lookup"><span data-stu-id="cf580-168">These elements are the common ones:</span></span>

- <span data-ttu-id="cf580-169">Textové pole: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="cf580-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="cf580-170">Zaškrtněte políčko: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="cf580-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="cf580-171">Přepínač: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="cf580-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="cf580-172">tlačítko: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="cf580-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="cf580-173">Tlačítko odešlete: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="cf580-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="cf580-174">Můžete také `<textarea>` elementu, který chcete vytvořit víceřádkové textové pole a `<select>` elementu, který chcete vytvořit rozevíracím seznamu nebo posuvný seznamu.</span><span class="sxs-lookup"><span data-stu-id="cf580-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="cf580-175">(Další informace o HTML formuláři elementy najdete v části [formuláře HTML a vstup](http://www.w3schools.com/html/html_forms.asp) na webu W3Schools.)</span><span class="sxs-lookup"><span data-stu-id="cf580-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="cf580-176">`name` Atribut je velmi důležité, protože název je, jak získáte hodnota elementu později, jak se krátce zobrazí.</span><span class="sxs-lookup"><span data-stu-id="cf580-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="cf580-177">Zajímavé část je, vývojář stránky provést se vstupem uživatele.</span><span class="sxs-lookup"><span data-stu-id="cf580-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="cf580-178">Není k dispozici žádné předdefinované chování spojené s těmito elementy.</span><span class="sxs-lookup"><span data-stu-id="cf580-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="cf580-179">Místo toho budete muset získat hodnoty, které má uživatel zadaný nebo vybrané a dělat něco s nimi.</span><span class="sxs-lookup"><span data-stu-id="cf580-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="cf580-180">To je, získáte informace v tomto kurzu.</span><span class="sxs-lookup"><span data-stu-id="cf580-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="cf580-181">**HTML5 a vstupních formulářů**</span><span class="sxs-lookup"><span data-stu-id="cf580-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="cf580-182">Možná jste zjistili, HTML je v přechodném stavu a na nejnovější verzi (HTML5) zahrnuje podporu pro více intuitivní způsoby pro uživatele k zadání informací.</span><span class="sxs-lookup"><span data-stu-id="cf580-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="cf580-183">Například v jazyce HTML5, (stránka vývojáře) zjistíte stránky chcete, aby uživatel zadal datum.</span><span class="sxs-lookup"><span data-stu-id="cf580-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="cf580-184">V prohlížeči můžete pak automaticky zobrazí kalendář místo nutnosti uživatelům ručně zadejte datum.</span><span class="sxs-lookup"><span data-stu-id="cf580-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="cf580-185">Však HTML5 je nová a zatím nepodporuje v všechny prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="cf580-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="cf580-186">ASP.NET – webové stránky podporuje HTML5 vstup, pokud nemá prohlížeče uživatele.</span><span class="sxs-lookup"><span data-stu-id="cf580-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="cf580-187">Pro představu o nové atributy `<input>` element v jazyce HTML5, najdete v části [HTML &lt;vstupní&gt; atribut typu](http://www.w3schools.com/html/html_form_input_types.asp) na webu W3Schools.</span><span class="sxs-lookup"><span data-stu-id="cf580-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="cf580-188">Vytvoření formuláře</span><span class="sxs-lookup"><span data-stu-id="cf580-188">Creating the Form</span></span>

<span data-ttu-id="cf580-189">Ve službě WebMatrix v **soubory** pracovního prostoru, otevřete *Movies.cshtml* stránky.</span><span class="sxs-lookup"><span data-stu-id="cf580-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="cf580-190">Po zavření `</h1>` značky a před otevření `<div>` značky `grid.GetHtml` volání, přidejte následující kód:</span><span class="sxs-lookup"><span data-stu-id="cf580-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="cf580-191">Tento kód vytvoří formulář obsahující textové pole s názvem `searchGenre` a tlačítko pro odeslání.</span><span class="sxs-lookup"><span data-stu-id="cf580-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="cf580-192">Text pole a tlačítko jsou uzavřené v odeslání `<form>` element jejichž `method` je nastavena na hodnotu `get`.</span><span class="sxs-lookup"><span data-stu-id="cf580-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="cf580-193">(Nezapomeňte, že pokud nemáte put textového pole a tlačítko uvnitř odeslat `<form>` elementu, nic se po kliknutí na tlačítko Odeslat.) Můžete použít `GET` příkaz zde vzhledem k tomu, že vytváříte formulář, nebyly provedeny žádné změny na serveru – je právě výsledkem vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="cf580-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="cf580-194">(V tomto kurzu předchozí jste použili `post` metoda, která je, jak odeslat změny na server.</span><span class="sxs-lookup"><span data-stu-id="cf580-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="cf580-195">Uvidíte, v dalším kurzu znovu.)</span><span class="sxs-lookup"><span data-stu-id="cf580-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="cf580-196">Spuštění stránky.</span><span class="sxs-lookup"><span data-stu-id="cf580-196">Run the page.</span></span> <span data-ttu-id="cf580-197">I když nebyly definovány žádné chování pro daný formulář, najdete v článku bude vypadat takto:</span><span class="sxs-lookup"><span data-stu-id="cf580-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![Stránka filmy s vyhledávacího pole pro Genre](form-basics/_static/image3.png)

<span data-ttu-id="cf580-199">Zadejte hodnotu do textového pole, jako je "Komedie."</span><span class="sxs-lookup"><span data-stu-id="cf580-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="cf580-200">Pak klikněte na tlačítko **vyhledávání Genre**.</span><span class="sxs-lookup"><span data-stu-id="cf580-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="cf580-201">Poznamenejte si adresu URL stránky.</span><span class="sxs-lookup"><span data-stu-id="cf580-201">Take note of the URL of the page.</span></span> <span data-ttu-id="cf580-202">Vzhledem k tomu, že nastavíte `<form>` elementu `method` atribut `get`, je zadaná hodnota je teď součástí řetězce dotazu v adrese URL, například takto:</span><span class="sxs-lookup"><span data-stu-id="cf580-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="cf580-203">Čtení hodnot formuláře</span><span class="sxs-lookup"><span data-stu-id="cf580-203">Reading Form Values</span></span>

<span data-ttu-id="cf580-204">Tato stránka již obsahuje některé kód, který získá data databáze a zobrazí výsledky v mřížce.</span><span class="sxs-lookup"><span data-stu-id="cf580-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="cf580-205">Nyní máte přidat nějaký kód, který čte hodnota textového pole, takže je možné spustit dotaz SQL, který obsahuje hledaný termín.</span><span class="sxs-lookup"><span data-stu-id="cf580-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="cf580-206">Protože nastavena metoda formuláře na `get`, si můžete přečíst hodnotu, která byla zadán do textového pole pomocí kódu takto:</span><span class="sxs-lookup"><span data-stu-id="cf580-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="cf580-207">`Request.QueryString` Objekt ( `QueryString` vlastnost `Request` objektu) obsahuje hodnoty elementů, které byly odeslány jako součást `GET` operaci.</span><span class="sxs-lookup"><span data-stu-id="cf580-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="cf580-208">`Request.QueryString` Vlastnost obsahuje *kolekce* (list) hodnoty, které jsou odeslány ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="cf580-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="cf580-209">Chcete-li získat všechny jednotlivé hodnoty, zadáte název elementu, který chcete.</span><span class="sxs-lookup"><span data-stu-id="cf580-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="cf580-210">Proto musíte mít `name` atributu u `<input>` – element (`searchTerm`) vytvářející textového pole.</span><span class="sxs-lookup"><span data-stu-id="cf580-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="cf580-211">(Další informace o tom `Request` objektu, najdete v článku [bočním panelu](#BKMK_TheRequestObject) později.)</span><span class="sxs-lookup"><span data-stu-id="cf580-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="cf580-212">Je dostatečně jednoduchá načíst hodnotu textového pole.</span><span class="sxs-lookup"><span data-stu-id="cf580-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="cf580-213">Ale pokud uživatel nebyl zadejte nic vůbec do textového pole, ale kliknutí na **vyhledávání** i přesto, můžete ignorovat kliknutím, vzhledem k tomu, že žádná data pro vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="cf580-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="cf580-214">Následující kód je příklad, který ukazuje, jak implementovat tyto podmínky.</span><span class="sxs-lookup"><span data-stu-id="cf580-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="cf580-215">(Přidat tento kód ještě nemáte, můžete to udělat za chvíli.)</span><span class="sxs-lookup"><span data-stu-id="cf580-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="cf580-216">Test rozpis tímto způsobem:</span><span class="sxs-lookup"><span data-stu-id="cf580-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="cf580-217">Získá hodnotu `Request.QueryString["searchGenre"]`, konkrétně hodnotu, která byla zadán do `<input>` element s názvem `searchGenre`.</span><span class="sxs-lookup"><span data-stu-id="cf580-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="cf580-218">Zjistěte, jestli je prázdný pomocí `IsEmpty` metoda.</span><span class="sxs-lookup"><span data-stu-id="cf580-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="cf580-219">Tato metoda je standardní způsob jak určit, zda něco (například element form) obsahuje hodnotu.</span><span class="sxs-lookup"><span data-stu-id="cf580-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="cf580-220">Ale skutečně, které jsou pro vás pouze v případě, že má *není* prázdný, proto...</span><span class="sxs-lookup"><span data-stu-id="cf580-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="cf580-221">Přidat `!` operátor před `IsEmpty` otestovat.</span><span class="sxs-lookup"><span data-stu-id="cf580-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="cf580-222">( `!` Operátor znamená logická NEGACE).</span><span class="sxs-lookup"><span data-stu-id="cf580-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="cf580-223">Zjednodušeně celý `if` podmínku překládá do následující: *Pokud formuláře searchGenre element není prázdný, pak...*</span><span class="sxs-lookup"><span data-stu-id="cf580-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="cf580-224">Tento blok nastaví fázi pro vytvoření dotazu, který používá hledaný termín.</span><span class="sxs-lookup"><span data-stu-id="cf580-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="cf580-225">Můžete to udělat v další části.</span><span class="sxs-lookup"><span data-stu-id="cf580-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="cf580-226">**Objekt požadavku**</span><span class="sxs-lookup"><span data-stu-id="cf580-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="cf580-227">`Request` Objekt obsahuje všechny informace, které prohlížeč odesílá do vaší aplikace po požadovaný nebo odeslání stránky.</span><span class="sxs-lookup"><span data-stu-id="cf580-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="cf580-228">Tento objekt obsahuje všechny informace, které uživatel nezadá jako textové pole hodnoty nebo soubor k odeslání.</span><span class="sxs-lookup"><span data-stu-id="cf580-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="cf580-229">Zahrnuje také nejrůznějším Další informace, jako jsou soubory cookie, hodnot v řetězci dotazu adresy URL (pokud existuje), cesta k souboru stránky, která je spuštěná, typu prohlížeče, které uživatel používá, seznam jazyků, které jsou nastaveny v prohlížeči a mnohem víc.</span><span class="sxs-lookup"><span data-stu-id="cf580-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="cf580-230">`Request` Objekt *kolekce* (list) hodnot.</span><span class="sxs-lookup"><span data-stu-id="cf580-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="cf580-231">Jednotlivé hodnoty z kolekce získáte zadáním názvu:</span><span class="sxs-lookup"><span data-stu-id="cf580-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="cf580-232">`Request` Objekt ve skutečnosti poskytuje několik podmnožin.</span><span class="sxs-lookup"><span data-stu-id="cf580-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="cf580-233">Příklad:</span><span class="sxs-lookup"><span data-stu-id="cf580-233">For example:</span></span>
> 
> - <span data-ttu-id="cf580-234">`Request.Form` poskytuje hodnoty z elementů uvnitř odeslané `<form>` element, je-li žádost `POST` požadavku.</span><span class="sxs-lookup"><span data-stu-id="cf580-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="cf580-235">`Request.QueryString` vám právě hodnot v řetězci dotazu adresu URL.</span><span class="sxs-lookup"><span data-stu-id="cf580-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="cf580-236">(V adrese URL jako `http://mysite/myapp/page?searchGenre=action&page=2`, `?searchGenre=action&page=2` část adresy URL je řetězec dotazu.)</span><span class="sxs-lookup"><span data-stu-id="cf580-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="cf580-237">`Request.Cookies` kolekce umožňuje přístup k soubory cookie, které prohlížeč odeslal.</span><span class="sxs-lookup"><span data-stu-id="cf580-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="cf580-238">Chcete-li získat hodnotu, která víte, je v odeslané podobě, můžete použít `Request["name"]`.</span><span class="sxs-lookup"><span data-stu-id="cf580-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="cf580-239">Alternativně můžete použít více konkrétních verzí `Request.Form["name"]` (pro `POST` požadavky) nebo `Request.QueryString["name"]` (pro `GET` požadavky).</span><span class="sxs-lookup"><span data-stu-id="cf580-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="cf580-240">Samozřejmě *název* je název položky získat.</span><span class="sxs-lookup"><span data-stu-id="cf580-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="cf580-241">Název položky, které chcete získat musí být jedinečný v rámci kolekce, kterou používáte.</span><span class="sxs-lookup"><span data-stu-id="cf580-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="cf580-242">Proto `Request` objekt poskytuje, jako jsou podmnožiny `Request.Form` a `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="cf580-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="cf580-243">Předpokládejme, že stránka obsahuje element form s názvem `userName` a *také* obsahuje soubor cookie s názvem `userName`.</span><span class="sxs-lookup"><span data-stu-id="cf580-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="cf580-244">Pokud se zobrazí `Request["userName"]`, je nejednoznačné, jestli chcete hodnot formulářů nebo soubor cookie.</span><span class="sxs-lookup"><span data-stu-id="cf580-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="cf580-245">Ale pokud získáte `Request.Form["userName"]` nebo `Request.Cookie["userName"]`, jste právě explicitní o hodnotu, která potřebujete.</span><span class="sxs-lookup"><span data-stu-id="cf580-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="cf580-246">Je dobrým zvykem konkrétní a použít podmnožinu `Request` , co vás zajímá, jako je `Request.Form` nebo `Request.QueryString`.</span><span class="sxs-lookup"><span data-stu-id="cf580-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="cf580-247">Jednoduché stránek, které vytváříte v tomto kurzu je pravděpodobně nemá je skutečně žádný rozdíl.</span><span class="sxs-lookup"><span data-stu-id="cf580-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="cf580-248">Ale při vytváření složitějších stránky, pomocí explicitní verze `Request.Form` nebo `Request.QueryString` vám může pomoct vyhnout problémům, které mohou nastat, když se tato stránka obsahuje formuláře (nebo více formulářů), soubory cookie, hodnoty řetězců dotazu a tak dále.</span><span class="sxs-lookup"><span data-stu-id="cf580-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="cf580-249">Vytvoření dotazu pomocí hledaný termín</span><span class="sxs-lookup"><span data-stu-id="cf580-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="cf580-250">Teď, když víte, jak získat hledaný termín, který uživatel zadal, můžete vytvořit dotaz, který se používá.</span><span class="sxs-lookup"><span data-stu-id="cf580-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="cf580-251">Mějte na paměti, že všechny položky film z databáze nástroje získáte používáte dotaz SQL, který vypadá jako tento příkaz:</span><span class="sxs-lookup"><span data-stu-id="cf580-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="cf580-252">Potřebujete jenom určité filmy, budete muset použít dotaz, který zahrnuje `Where` klauzule.</span><span class="sxs-lookup"><span data-stu-id="cf580-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="cf580-253">Tuto klauzuli umožňuje nastavit podmínky, na kterém jsou řádky vrácené dotazem.</span><span class="sxs-lookup"><span data-stu-id="cf580-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="cf580-254">Tady je příklad:</span><span class="sxs-lookup"><span data-stu-id="cf580-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="cf580-255">Základní formát je `WHERE column = value`.</span><span class="sxs-lookup"><span data-stu-id="cf580-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="cf580-256">Můžete použít různé operátory kromě právě `=`, například `>` (větší než), `<` (méně než), `<>` (nerovná se), `<=` (je menší než nebo rovno), atd., v závislosti na tom, co hledáte.</span><span class="sxs-lookup"><span data-stu-id="cf580-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="cf580-257">V případě, že přemýšlíte, příkazy SQL nejsou velká a malá písmena &mdash; `SELECT` je stejný jako `Select` (nebo i `select`).</span><span class="sxs-lookup"><span data-stu-id="cf580-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="cf580-258">Ale osoby často velká počáteční klíčová slova v příkazu jazyka SQL, jako je třeba `SELECT` a `WHERE`, aby bylo snazší ke čtení.</span><span class="sxs-lookup"><span data-stu-id="cf580-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="cf580-259">Předání jako parametr hledaný termín</span><span class="sxs-lookup"><span data-stu-id="cf580-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="cf580-260">Hledání pro konkrétní genre je dostatečně snadno (`WHERE Genre = 'Action'`), ale chcete mít možnost vyhledat všechny genre, který uživatel zadá.</span><span class="sxs-lookup"><span data-stu-id="cf580-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="cf580-261">K tomu, je vytvořit jako dotaz SQL, který obsahuje zástupný symbol pro hodnotu k vyhledání.</span><span class="sxs-lookup"><span data-stu-id="cf580-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="cf580-262">Ho bude vypadat jako tento příkaz:</span><span class="sxs-lookup"><span data-stu-id="cf580-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="cf580-263">Je zástupný symbol `@` následovaný nula.</span><span class="sxs-lookup"><span data-stu-id="cf580-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="cf580-264">Jak můžou snadno uhodnout, dotaz může obsahovat více zástupných symbolů a by se jmenovala `@0`, `@1`, `@2`atd.</span><span class="sxs-lookup"><span data-stu-id="cf580-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="cf580-265">Nastavit dotaz a ve skutečnosti je předat hodnotu, je použít kód takto:</span><span class="sxs-lookup"><span data-stu-id="cf580-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="cf580-266">Tento kód je podobná co již bylo provedeno k zobrazení dat v mřížce.</span><span class="sxs-lookup"><span data-stu-id="cf580-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="cf580-267">Pouze rozdíly jsou:</span><span class="sxs-lookup"><span data-stu-id="cf580-267">The only differences are:</span></span>

- <span data-ttu-id="cf580-268">Dotaz obsahuje zástupný znak (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="cf580-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="cf580-269">Dotaz je uvést do proměnné (`selectCommand`); před přímo na předán dotaz `db.Query` metoda.</span><span class="sxs-lookup"><span data-stu-id="cf580-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="cf580-270">Při volání `db.Query` metoda, předáte dotaz a hodnota, která má použít pro zástupný symbol.</span><span class="sxs-lookup"><span data-stu-id="cf580-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="cf580-271">(Pokud dotaz měli více zástupných symbolů, by je předat všechny jako samostatné hodnoty metodě.)</span><span class="sxs-lookup"><span data-stu-id="cf580-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="cf580-272">Pokud jste v kostce řečeno tyto prvky, získáte následující kód:</span><span class="sxs-lookup"><span data-stu-id="cf580-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="cf580-273">**Důležité!**</span><span class="sxs-lookup"><span data-stu-id="cf580-273">**Important!**</span></span> <span data-ttu-id="cf580-274">Pomocí zástupné symboly (například `@0`) mají být předány hodnoty příkaz SQL je *velmi důležité* pro zabezpečení.</span><span class="sxs-lookup"><span data-stu-id="cf580-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="cf580-275">Způsob, jakým jste ji tady vidíte, se zástupnými symboly pro různá data, je jediným způsobem, jak je potřeba vytvořit příkazy SQL.</span><span class="sxs-lookup"><span data-stu-id="cf580-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="cf580-276">Nikdy vytvoření příkazu SQL umístěním společně (spojováním) literálu text a hodnoty, které můžete získat z uživatele.</span><span class="sxs-lookup"><span data-stu-id="cf580-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="cf580-277">Zřetězení uživatelský vstup do příkazu SQL otevře váš web, *útok prostřednictvím injektáže SQL* kde uživatel se zlými úmysly odešle hodnoty na stránku, které zabezpečení vaší databáze.</span><span class="sxs-lookup"><span data-stu-id="cf580-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="cf580-278">(Další informace v článku [Injektáž SQL](https://msdn.microsoft.com/library/ms161953.aspx) webu MSDN.)</span><span class="sxs-lookup"><span data-stu-id="cf580-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="cf580-279">Aktualizací stránky filmy vyhledávání kódu</span><span class="sxs-lookup"><span data-stu-id="cf580-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="cf580-280">Teď můžete aktualizovat kód *Movies.cshtml* souboru.</span><span class="sxs-lookup"><span data-stu-id="cf580-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="cf580-281">Pokud chcete začít, nahraďte kód v bloku kódu v horní části stránky s tímto kódem:</span><span class="sxs-lookup"><span data-stu-id="cf580-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="cf580-282">Rozdíl je, že jste vložte dotaz do `selectCommand` proměnné, které budete předat `db.Query` později.</span><span class="sxs-lookup"><span data-stu-id="cf580-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="cf580-283">Vložení příkaz jazyka SQL do proměnné umožňuje změnit příkazu, které se to udělat provést hledání.</span><span class="sxs-lookup"><span data-stu-id="cf580-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="cf580-284">Rovněž jsme odstranili tyto dva řádky, které jste budete vrátit zpět k později:</span><span class="sxs-lookup"><span data-stu-id="cf580-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="cf580-285">Nechcete spusťte dotaz ještě (to znamená, volání `db.Query`) a nechcete, aby k chybě při inicializaci `WebGrid` pomocné rutiny, ale buď.</span><span class="sxs-lookup"><span data-stu-id="cf580-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="cf580-286">Poté, co jste započítáno se má spuštěný, které příkaz jazyka SQL, můžete to udělat všechny tyto věci.</span><span class="sxs-lookup"><span data-stu-id="cf580-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="cf580-287">Po tento rewritten blok můžete přidat nové logiku pro zpracování vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="cf580-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="cf580-288">Dokončený kód bude vypadat následovně.</span><span class="sxs-lookup"><span data-stu-id="cf580-288">The completed code will look like the following.</span></span> <span data-ttu-id="cf580-289">Aktualizujte kód na stránku tak, aby odpovídala tento příklad:</span><span class="sxs-lookup"><span data-stu-id="cf580-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="cf580-290">Stránka teď funguje takto.</span><span class="sxs-lookup"><span data-stu-id="cf580-290">The page now works like this.</span></span> <span data-ttu-id="cf580-291">Při každém spuštění stránky kód otevře databáze a `selectCommand` proměnná je nastavená na příkazu SQL, který získá všechny záznamy z `Movies` tabulky.</span><span class="sxs-lookup"><span data-stu-id="cf580-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="cf580-292">Kód také inicializuje `searchTerm` proměnné.</span><span class="sxs-lookup"><span data-stu-id="cf580-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="cf580-293">Ale pokud aktuální požadavek obsahuje hodnotu pro `searchGenre` element, nastaví kód `selectCommand` na jiný dotaz –, konkrétně k, které obsahuje `Where` klauzule k vyhledání genre.</span><span class="sxs-lookup"><span data-stu-id="cf580-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="cf580-294">Nastaví taky `searchTerm` ať byl předán pro do vyhledávacího pole (který může být nic).</span><span class="sxs-lookup"><span data-stu-id="cf580-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="cf580-295">Bez ohledu na to, které SQL příkazu je v `selectCommand`, kód pak zavolá `db.Query` spusťte dotaz, předání ať je v `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="cf580-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="cf580-296">Pokud není nic `searchTerm`, nezáleží, protože v takovém případě není žádný parametr předat hodnotu pro `selectCommand` přesto.</span><span class="sxs-lookup"><span data-stu-id="cf580-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="cf580-297">Nakonec kód inicializuje `WebGrid` pomocná pomocí výsledky dotazu, stejně jako předtím.</span><span class="sxs-lookup"><span data-stu-id="cf580-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="cf580-298">Můžete uvidíte, že umístěním příkaz jazyka SQL a hledaný termín do proměnné, kterou jste přidali flexibilitu do kód.</span><span class="sxs-lookup"><span data-stu-id="cf580-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="cf580-299">Jak uvidíte později v tomto kurzu, můžete použít tento základní architekturu a pokračujte v přidávání logiku pro různé typy vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="cf580-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="cf580-300">Testování funkce vyhledávání Genre</span><span class="sxs-lookup"><span data-stu-id="cf580-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="cf580-301">Ve službě WebMatrix, spusťte *Movies.cshtml* stránky.</span><span class="sxs-lookup"><span data-stu-id="cf580-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="cf580-302">Zobrazí se stránka s textového pole pro genre.</span><span class="sxs-lookup"><span data-stu-id="cf580-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="cf580-303">Zadejte genre, který jste zadali pro jednu ze svých záznamech testu a pak klikněte na **vyhledávání**.</span><span class="sxs-lookup"><span data-stu-id="cf580-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="cf580-304">Tentokrát uvidíte seznam právě filmy, které odpovídají této genre:</span><span class="sxs-lookup"><span data-stu-id="cf580-304">This time you see a listing of just the movies that match that genre:</span></span>

![Stránka filmy výpis po hledání genre Comedies](form-basics/_static/image4.png)

<span data-ttu-id="cf580-306">Zadejte jiný genre a hledat znovu.</span><span class="sxs-lookup"><span data-stu-id="cf580-306">Enter a different genre and search again.</span></span> <span data-ttu-id="cf580-307">Zkuste zadat genre prostřednictvím všechna všechna písmena velká nebo malá písmena, takže uvidíte, že vyhledávání není velká a malá písmena.</span><span class="sxs-lookup"><span data-stu-id="cf580-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="cf580-308">"Nezapomeňte" co uživatel zadal</span><span class="sxs-lookup"><span data-stu-id="cf580-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="cf580-309">Možná jste si všimli, poté, co jste zadali genre a kliknutí na **vyhledávání Genre**, jste viděli v seznamu, pro nějž.</span><span class="sxs-lookup"><span data-stu-id="cf580-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="cf580-310">Ale textového pole pro vyhledávání byla prázdná &mdash; jinými slovy, nebyla pamatovat, co jste nezadali.</span><span class="sxs-lookup"><span data-stu-id="cf580-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="cf580-311">Je důležité pochopit, proč k tomuto chování dochází.</span><span class="sxs-lookup"><span data-stu-id="cf580-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="cf580-312">Při odesílání stránky prohlížeč odešle požadavek na webový server.</span><span class="sxs-lookup"><span data-stu-id="cf580-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="cf580-313">Když ASP.NET získá požadavek, vytvoří instanci zcela nové stránky, spuštěním kódu v ní a pak znovu vykreslí stránku v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="cf580-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="cf580-314">Platit ale stránky neví, že jste právě pracovali s předchozí verzí sám sebe.</span><span class="sxs-lookup"><span data-stu-id="cf580-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="cf580-315">Všechny že bude vědět, že je to byla přijata žádost, která měla některé data formuláře v ní.</span><span class="sxs-lookup"><span data-stu-id="cf580-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="cf580-316">Pokaždé, když požadavek stránku &mdash; zda poprvé nebo odesláním ji &mdash; vám nová stránka.</span><span class="sxs-lookup"><span data-stu-id="cf580-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="cf580-317">Webový server má nedostatek paměti u vaší poslední žádosti.</span><span class="sxs-lookup"><span data-stu-id="cf580-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="cf580-318">Ani nepodporuje technologii ASP.NET a ani se v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="cf580-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="cf580-319">Pouze připojení mezi tyto samostatné instance stránky je žádná data, která přenášet mezi nimi.</span><span class="sxs-lookup"><span data-stu-id="cf580-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="cf580-320">Pokud odešlete stránku, například novou instanci stránky mohou získat data formuláře, který vám byl zaslán starší instance.</span><span class="sxs-lookup"><span data-stu-id="cf580-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="cf580-321">(Další způsob předávání dat mezi stránkami je používat soubory cookie.)</span><span class="sxs-lookup"><span data-stu-id="cf580-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="cf580-322">Formální způsob, jak popisuje tato situace je to, že webové stránky jsou *bezstavové*.</span><span class="sxs-lookup"><span data-stu-id="cf580-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="cf580-323">Webové servery a vlastních stránek a elementy na stránce neudržují žádné informace o stavu předchozí stránky.</span><span class="sxs-lookup"><span data-stu-id="cf580-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="cf580-324">Web byl navržen tímto způsobem, protože zachování stavu pro jednotlivé požadavky by rychle vyčerpat prostředky webových serverů, které často zpracování tisíc, možná i stovky tisíc požadavků za sekundu.</span><span class="sxs-lookup"><span data-stu-id="cf580-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="cf580-325">Tak, aby se důvod, proč byla textového pole prázdná.</span><span class="sxs-lookup"><span data-stu-id="cf580-325">So that's why the text box was empty.</span></span> <span data-ttu-id="cf580-326">Po odeslání stránky ASP.NET vytvoří novou instanci třídy stránky a spustili prostřednictvím kódu a kódu.</span><span class="sxs-lookup"><span data-stu-id="cf580-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="cf580-327">V tomto kód, který vás vyzval technologii ASP.NET pro vložení hodnoty do textového pole nic došlo.</span><span class="sxs-lookup"><span data-stu-id="cf580-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="cf580-328">Proto ASP.NET nereaguje, a bez hodnoty v něm byl vykreslen textového pole.</span><span class="sxs-lookup"><span data-stu-id="cf580-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="cf580-329">Je ve skutečnosti snadný způsob, jak získat chcete tento problém vyřešit.</span><span class="sxs-lookup"><span data-stu-id="cf580-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="cf580-330">Genre, kterou jste zadali do textového pole *je* k dispozici v kódu &mdash; je v `Request.QueryString["searchGenre"]`.</span><span class="sxs-lookup"><span data-stu-id="cf580-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="cf580-331">Aktualizujte kód pro textové pole tak, aby `value` atribut získá svou hodnotu z `searchTerm`, jako jsou v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="cf580-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="cf580-332">Na této stránce můžete mít také nastavit `value` atribut `searchTerm` proměnné, protože tuto proměnnou také obsahuje genre jste zadali.</span><span class="sxs-lookup"><span data-stu-id="cf580-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="cf580-333">Ale pomocí `Request` objekt, který chcete nastavit `value` atributu, jak je vidět tady je standardní způsob k provedení této úlohy.</span><span class="sxs-lookup"><span data-stu-id="cf580-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="cf580-334">(Za předpokladu, že chcete i tomu &mdash; v některých případech můžete chtít vykreslení stránky *bez* hodnoty v polích.</span><span class="sxs-lookup"><span data-stu-id="cf580-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="cf580-335">Všechny závisí na co se děje s vaší aplikací.)</span><span class="sxs-lookup"><span data-stu-id="cf580-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="cf580-336">"Nepamatujete" hodnota textové pole, které se používá pro hesla.</span><span class="sxs-lookup"><span data-stu-id="cf580-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="cf580-337">Je bezpečnostní riziko umožnit uživatelům vyplnit pole pro heslo pomocí kódu.</span><span class="sxs-lookup"><span data-stu-id="cf580-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="cf580-338">Znovu spusťte stránky, zadejte genre a klikněte na **vyhledávání Genre**.</span><span class="sxs-lookup"><span data-stu-id="cf580-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="cf580-339">Tentokrát nejen zobrazí výsledky hledání, ale textového pole pamatuje, co jste naposledy zadali:</span><span class="sxs-lookup"><span data-stu-id="cf580-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![Stránky zobrazující, že textového pole má 'uloží' předchozí položce](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="cf580-341">Hledání libovolného slova v názvu</span><span class="sxs-lookup"><span data-stu-id="cf580-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="cf580-342">Nyní můžete vyhledat všechny genre, ale můžete také vyhledat název.</span><span class="sxs-lookup"><span data-stu-id="cf580-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="cf580-343">Je obtížné získat název zcela správně při hledání, takže místo toho že můžete vyhledat slovo, které se zobrazí kdekoli uvnitř název.</span><span class="sxs-lookup"><span data-stu-id="cf580-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="cf580-344">To lze provést v systému SQL, můžete použít `LIKE` operátor a syntaxe, podobně jako následující:</span><span class="sxs-lookup"><span data-stu-id="cf580-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="cf580-345">Tento příkaz načte všechny filmy, jejichž názvy obsahovat "adventure".</span><span class="sxs-lookup"><span data-stu-id="cf580-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="cf580-346">Při použití `LIKE` operátor, zahrnují zástupného znaku `%` jako součást hledaný termín.</span><span class="sxs-lookup"><span data-stu-id="cf580-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="cf580-347">Hledání `LIKE 'adventure%'` znamená "od 'adventure'".</span><span class="sxs-lookup"><span data-stu-id="cf580-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="cf580-348">(Technicky znamená "Řetězec, který následuje nic adventure.") Podobně hledaný termín `LIKE '%adventure'` znamená "jakýkoli následuje řetězec 'adventure'", což je jiný způsob, jak můžete "ukončuje s 'adventure'".</span><span class="sxs-lookup"><span data-stu-id="cf580-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="cf580-349">Hledaný termín `LIKE '%adventure%'` proto znamená "s"adventure' kdekoli v názvu."</span><span class="sxs-lookup"><span data-stu-id="cf580-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="cf580-350">(Technicky "nic v názvu, za nímž následuje 'adventure', za nímž následuje nic.")</span><span class="sxs-lookup"><span data-stu-id="cf580-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="cf580-351">Uvnitř `<form>` elementu, přidejte následující značky vpravo pod ukončovací `</div>` značky pro hledání genre (těsně před uzavírací `</form>` element):</span><span class="sxs-lookup"><span data-stu-id="cf580-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="cf580-352">Kód pro zpracování tohoto hledání je podobný kódu pro hledání genre, s tím rozdílem, že máte ke kompilaci `LIKE` vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="cf580-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="cf580-353">Uvnitř bloku kódu v horní části stránky, přidejte tuto `if` blokovat jenom po `if` blok pro hledání genre:</span><span class="sxs-lookup"><span data-stu-id="cf580-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="cf580-354">Tento kód používá stejné logiky, která jste viděli dříve, s tím rozdílem, že používá hledání `LIKE` operátor a PUT kód "`%`" před a po hledaný termín.</span><span class="sxs-lookup"><span data-stu-id="cf580-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="cf580-355">Všimněte si, jak se snadno přidat další hledání na stránku.</span><span class="sxs-lookup"><span data-stu-id="cf580-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="cf580-356">Všechny, které jste museli provést byla:</span><span class="sxs-lookup"><span data-stu-id="cf580-356">All you had to do was:</span></span>

- <span data-ttu-id="cf580-357">Vytvoření `if` blok, který testován, zda měl relevantní vyhledávacího pole zadejte hodnotu.</span><span class="sxs-lookup"><span data-stu-id="cf580-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="cf580-358">Nastavte `selectCommand` proměnnou nový příkaz SQL.</span><span class="sxs-lookup"><span data-stu-id="cf580-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="cf580-359">Nastavte `searchTerm` proměnné na hodnotu k předání do dotazu.</span><span class="sxs-lookup"><span data-stu-id="cf580-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="cf580-360">Tady je blok dokončení kódu, který obsahuje nové logiku pro hledání názvu:</span><span class="sxs-lookup"><span data-stu-id="cf580-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="cf580-361">Zde je souhrn jaké tento kód:</span><span class="sxs-lookup"><span data-stu-id="cf580-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="cf580-362">Proměnné `searchTerm` a `selectCommand` jsou inicializovány v horní části.</span><span class="sxs-lookup"><span data-stu-id="cf580-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="cf580-363">Chcete-li nastavit tyto proměnné na příslušné hledaný termín (pokud existuje) a příslušný příkaz SQL založený na uživatele nemá na stránce.</span><span class="sxs-lookup"><span data-stu-id="cf580-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="cf580-364">Výchozí nastavení hledání je jednoduchý případ všechny filmy získat z databáze.</span><span class="sxs-lookup"><span data-stu-id="cf580-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="cf580-365">V testech pro `searchGenre` a `searchTitle`, nastaví kód `searchTerm` na hodnotu, kterou chcete vyhledat.</span><span class="sxs-lookup"><span data-stu-id="cf580-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="cf580-366">Tyto bloky kódu nastavit také `selectCommand` příslušný příkaz SQL pro toto hledání.</span><span class="sxs-lookup"><span data-stu-id="cf580-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="cf580-367">`db.Query` Metoda je volána pouze jednou, pomocí ať příkaz SQL je v `selectedCommand` a ať hodnota `searchTerm`.</span><span class="sxs-lookup"><span data-stu-id="cf580-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="cf580-368">Pokud neexistuje žádný hledaný termín (žádné genre a žádné slovo title), hodnota `searchTerm` je prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="cf580-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="cf580-369">Ale, který není důležité, protože v takovém případě dotaz nevyžaduje parametr.</span><span class="sxs-lookup"><span data-stu-id="cf580-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="cf580-370">Testování funkce hledání názvu</span><span class="sxs-lookup"><span data-stu-id="cf580-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="cf580-371">Nyní můžete otestovat stránku dokončené vyhledávání.</span><span class="sxs-lookup"><span data-stu-id="cf580-371">Now you can test your completed search page.</span></span> <span data-ttu-id="cf580-372">Spustit *Movies.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="cf580-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="cf580-373">Zadejte genre a klikněte na **vyhledávání Genre**.</span><span class="sxs-lookup"><span data-stu-id="cf580-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="cf580-374">V mřížce zobrazené filmy genre, jako je před.</span><span class="sxs-lookup"><span data-stu-id="cf580-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="cf580-375">Zadejte název word a klikněte na **vyhledávání název**.</span><span class="sxs-lookup"><span data-stu-id="cf580-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="cf580-376">V mřížce zobrazené filmy, které mají v názvu aplikace word.</span><span class="sxs-lookup"><span data-stu-id="cf580-376">The grid displays movies that have that word in the title.</span></span>

![Stránka filmy výpis po hledání pro "Na" v názvu](form-basics/_static/image6.png)

<span data-ttu-id="cf580-378">Ponechat prázdné obou polí a buď tlačítko.</span><span class="sxs-lookup"><span data-stu-id="cf580-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="cf580-379">V mřížce zobrazené všechny filmy.</span><span class="sxs-lookup"><span data-stu-id="cf580-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="cf580-380">Kombinování dotazy</span><span class="sxs-lookup"><span data-stu-id="cf580-380">Combining the Queries</span></span>

<span data-ttu-id="cf580-381">Můžete si všimnout, že se hledání, které můžete provádět nevylučují.</span><span class="sxs-lookup"><span data-stu-id="cf580-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="cf580-382">Název a genre nelze hledat ve stejnou dobu, i když obě pole hledání obsahují hodnoty.</span><span class="sxs-lookup"><span data-stu-id="cf580-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="cf580-383">Například nelze vyhledat všechny akce filmy, jejíž název obsahuje "Adventure".</span><span class="sxs-lookup"><span data-stu-id="cf580-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="cf580-384">(Jako stránky je zakódovaný nyní, pokud zadejte hodnoty pro genre a název, získá název vyhledávání prioritu.) Pokud chcete vytvořit hledání, které kombinuje podmínky, je nutné vytvořit dotaz SQL, který má následující syntaxi:</span><span class="sxs-lookup"><span data-stu-id="cf580-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="cf580-385">A budete muset spustit dotaz pomocí příkazu následujícím způsobem (přibližně hovořícího):</span><span class="sxs-lookup"><span data-stu-id="cf580-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="cf580-386">Vytváření logiku, která umožní velký počet permutací kritéria vyhledávání můžete zapojení trochu, jak můžete vidět.</span><span class="sxs-lookup"><span data-stu-id="cf580-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="cf580-387">Proto jsme už nebudou sem.</span><span class="sxs-lookup"><span data-stu-id="cf580-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="cf580-388">Objevuje další</span><span class="sxs-lookup"><span data-stu-id="cf580-388">Coming Up Next</span></span>

<span data-ttu-id="cf580-389">V dalším kurzu vytvoříte stránky, která používá formuláře tak, aby uživatelé přidat filmy do databáze.</span><span class="sxs-lookup"><span data-stu-id="cf580-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="cf580-390">Úplný seznam pro stránku Movie (aktualizováno vyhledávání)</span><span class="sxs-lookup"><span data-stu-id="cf580-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="cf580-391">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="cf580-391">Additional Resources</span></span>

- [<span data-ttu-id="cf580-392">Úvod do rozhraní ASP.NET Web programování pomocí syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="cf580-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="cf580-393">[Klauzule WHERE SQL](http://www.w3schools.com/sql/sql_where.asp) na webu W3Schools</span><span class="sxs-lookup"><span data-stu-id="cf580-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="cf580-394">[Metoda definice](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) článku na webu W3C</span><span class="sxs-lookup"><span data-stu-id="cf580-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="cf580-395">[Předchozí](displaying-data.md)
> [další](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="cf580-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
