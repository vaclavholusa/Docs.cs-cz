---
uid: web-pages/overview/getting-started/introducing-razor-syntax-c
title: "Úvod do rozhraní ASP.NET Web programování pomocí syntaxe Razor (C#) | Microsoft Docs"
author: tfitzmac
description: "Tato kapitola obsahuje přehled programování pomocí webových stránek ASP.NET pomocí syntaxe Razor. ASP.NET je technologie společnosti Microsoft pro spuštění dynamického webového pa..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: aa67d304-583b-4bf8-a231-195656cfb587
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-c
msc.type: authoredcontent
ms.openlocfilehash: 32cdd8d524d783d7ccc3ab076de636ce4a868132
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-c"></a><span data-ttu-id="0a5f4-104">Úvod do rozhraní ASP.NET Web programování pomocí syntaxe Razor (C#)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-104">Introduction to ASP.NET Web Programming Using the Razor Syntax (C#)</span></span>
====================
<span data-ttu-id="0a5f4-105">podle [tní FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="0a5f4-106">Tento článek poskytuje přehled programování pomocí webových stránek ASP.NET pomocí syntaxe Razor.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-106">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax.</span></span> <span data-ttu-id="0a5f4-107">ASP.NET je technologie společnosti Microsoft pro spouštění dynamické webové stránky na webové servery.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-107">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span> <span data-ttu-id="0a5f4-108">Toto se článek zaměřuje na pomocí programovacího jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-108">This articles focuses on using the C# programming language.</span></span>
> 
> <span data-ttu-id="0a5f4-109">**Co se dozvíte**:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-109">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="0a5f4-110">Horní 8 Programovací tipy pro Začínáme s programováním pomocí syntaxe Razor rozhraní ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-110">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="0a5f4-111">Základní koncepty programování, které budete potřebovat.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-111">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="0a5f4-112">Jaké kódu serveru ASP.NET a syntaxe Razor je všechno o.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-112">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="0a5f4-113">Verze softwaru</span><span class="sxs-lookup"><span data-stu-id="0a5f4-113">Software versions</span></span>
> 
> 
> - <span data-ttu-id="0a5f4-114">Rozhraní ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="0a5f4-114">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="0a5f4-115">V tomto kurzu taky spolupracuje se službou ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-115">This tutorial also works with ASP.NET Web Pages 2.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="0a5f4-116">Horní 8 Tipy pro programování</span><span class="sxs-lookup"><span data-stu-id="0a5f4-116">The Top 8 Programming Tips</span></span>

<span data-ttu-id="0a5f4-117">Tato část obsahuje několik tipy, které je nezbytně nutné znát jako počáteční psaní kódu serveru ASP.NET pomocí syntaxe Razor.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-117">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

> [!NOTE]
> <span data-ttu-id="0a5f4-118">Syntaxe Razor je založena na programovací jazyk C# a který je jazyk, který se používá nejčastěji s ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-118">The Razor syntax is based on the C# programming language, and that's the language that's used most often with ASP.NET Web Pages.</span></span> <span data-ttu-id="0a5f4-119">Syntaxe Razor však také podporuje jazyka Visual Basic a vše, co vidíte, že můžete provést také v jazyce Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-119">However, the Razor syntax also supports the Visual Basic language, and everything you see you can also do in Visual Basic.</span></span> <span data-ttu-id="0a5f4-120">Podrobnosti najdete v tématu příloze [jazyka Visual Basic a syntaxe](https://go.microsoft.com/fwlink/?LinkId=202908).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-120">For details, see the appendix [Visual Basic Language and Syntax](https://go.microsoft.com/fwlink/?LinkId=202908).</span></span>


<span data-ttu-id="0a5f4-121">Další informace o většinu těchto programátorských technik, naleznete dále v článku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-121">You can find more details about most of these programming techniques later in the article.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="0a5f4-122">1. Přidejte kód na stránku pomocí @ – znak</span><span class="sxs-lookup"><span data-stu-id="0a5f4-122">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="0a5f4-123">`@` Začne znak vložené výrazy, jediný příkaz bloky a bloky vícepříkazové:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-123">The `@` character starts inline expressions, single statement blocks, and multi-statement blocks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample1.html)]

<span data-ttu-id="0a5f4-124">Toto je, jak tyto příkazy vypadat při spuštění stránky v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-124">This is what these statements look like when the page runs in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-c/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="0a5f4-126">**HTML Encoding**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-126">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="0a5f4-127">Při zobrazení obsahu stránce pomocí `@` znak, jako v předchozích příkladech ASP.NET umístí kódování HTML výstup.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-127">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="0a5f4-128">Tím se nahradí vyhrazené znaky HTML (například `<` a `>` a `&`) s kódy, které umožňují znaků, který má být zobrazen jako znaků na webové stránce nebude interpretován jako značky HTML nebo entity.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-128">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="0a5f4-129">Bez kódování HTML výstup z vašeho kódu serveru se nemusí zobrazit správně a mohla vystavit stránky na bezpečnostní rizika.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-129">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="0a5f4-130">Pokud vaším cílem je výstup jazyka HTML, který vykreslí značky jako značky (například `<p></p>` pro odstavec nebo `<em></em>` pro zvýraznění textu), najdete v části [kombinování Text, značek a kódu v bloky kódu](#BM_CombiningTextMarkupAndCode) dále v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-130">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="0a5f4-131">Další informace o kódování HTML v [práci s formuláři](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-131">You can read more about HTML encoding in [Working with Forms](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-in-braces"></a><span data-ttu-id="0a5f4-132">2. Bloky kódu je uzavřít do složených závorek</span><span class="sxs-lookup"><span data-stu-id="0a5f4-132">2. You enclose code blocks in braces</span></span>

<span data-ttu-id="0a5f4-133">A *blok kódu* obsahuje jeden nebo více příkazů kódu a je uzavřené do složených závorek.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-133">A *code block* includes one or more code statements and is enclosed in braces.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample2.html)]

<span data-ttu-id="0a5f4-134">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-134">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-c/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-semicolon"></a><span data-ttu-id="0a5f4-136">3. Uvnitř bloku vám stát, že každý příkaz kódu středníkem</span><span class="sxs-lookup"><span data-stu-id="0a5f4-136">3. Inside a block, you end each code statement with a semicolon</span></span>

<span data-ttu-id="0a5f4-137">Každý příkaz kompletní kód uvnitř bloku kódu musí končit středníkem.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-137">Inside a code block, each complete code statement must end with a semicolon.</span></span> <span data-ttu-id="0a5f4-138">Vložené výrazy nejsou končit středníkem.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-138">Inline expressions don't end with a semicolon.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample3.html)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="0a5f4-139">4. Proměnné se používají k ukládání hodnot</span><span class="sxs-lookup"><span data-stu-id="0a5f4-139">4. You use variables to store values</span></span>

<span data-ttu-id="0a5f4-140">Hodnoty v lze ukládat *proměnná*, včetně řetězce, čísla a kalendářní data, atd. Vytvoření nové proměnné pomocí `var` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-140">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `var` keyword.</span></span> <span data-ttu-id="0a5f4-141">Hodnoty proměnných můžete vložit přímo do stránky pomocí `@`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-141">You can insert variable values directly in a page using `@`.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample4.html)]

<span data-ttu-id="0a5f4-142">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-142">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-c/_static/image3.jpg)

<a id="ID_StringLiterals"></a>
### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="0a5f4-144">5. Uzavřete řetězcového literálu hodnoty v uvozovkách</span><span class="sxs-lookup"><span data-stu-id="0a5f4-144">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="0a5f4-145">A *řetězec* je posloupnost znaků, které jsou považovány za text.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-145">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="0a5f4-146">Pokud chcete zadat řetězec, vložte jej do dvojitých uvozovek:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-146">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample5.cshtml)]

<span data-ttu-id="0a5f4-147">Pokud řetězec, který chcete zobrazit obsahuje znak zpětného lomítka ( `\` ) nebo dvojité uvozovky ( `"` ), použijte *typu verbatim řetězcový literál* , je s předponou `@` operátor.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-147">If the string that you want to display contains a backslash character ( `\` ) or double quotation marks ( `"` ), use a *verbatim string literal* that's prefixed with the `@` operator.</span></span> <span data-ttu-id="0a5f4-148">(V jazyku C# \ znak má zvláštní význam, pokud nechcete použít typu verbatim řetězcový literál.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-148">(In C#, the \ character has special meaning unless you use a verbatim string literal.)</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample6.html)]

<span data-ttu-id="0a5f4-149">Pro vložení dvojitých uvozovek nahoře, použijte literálu typu verbatim řetězec a opakujte uvozovek:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-149">To embed double quotation marks, use a verbatim string literal and repeat the quotation marks:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample7.html)]

<span data-ttu-id="0a5f4-150">Zde je výsledkem použití obě tyto příklady na stránce:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-150">Here's the result of using both of these examples in a page:</span></span>

![Razor Img4](introducing-razor-syntax-c/_static/image4.jpg)

> [!NOTE]
> <span data-ttu-id="0a5f4-152">Všimněte si, že `@` znak se používá k označení typu verbatim textové literály v jazyce C# a označit kódu na stránkách ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-152">Notice that the `@` character is used both to mark verbatim string literals in C# and to mark code in ASP.NET pages.</span></span>


### <a name="6-code-is-case-sensitive"></a><span data-ttu-id="0a5f4-153">6. Kód je malá a velká písmena</span><span class="sxs-lookup"><span data-stu-id="0a5f4-153">6. Code is case sensitive</span></span>

<span data-ttu-id="0a5f4-154">V jazyce C#, klíčová slova (jako je `var`, `true`, a `if`) a názvy proměnných jsou velká a malá písmena.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-154">In C#, keywords (like `var`, `true`, and `if`) and variable names are case sensitive.</span></span> <span data-ttu-id="0a5f4-155">Následující řádky kódu vytvoření dvou různých proměnných, `lastName` a`LastName.`</span><span class="sxs-lookup"><span data-stu-id="0a5f4-155">The following lines of code create two different variables, `lastName` and `LastName.`</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample8.cshtml)]

<span data-ttu-id="0a5f4-156">Pokud je deklarovat proměnnou jako `var lastName = "Smith";` a pokud se pokusíte odkazovat na tuto proměnnou na stránce jako `@LastName`, dochází k chybě, protože `LastName` nebude rozpoznána.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-156">If you declare a variable as `var lastName = "Smith";` and if you try to reference that variable in your page as `@LastName`, an error results because `LastName` won't be recognized.</span></span>

> [!NOTE]
> <span data-ttu-id="0a5f4-157">V jazyce Visual Basic, klíčová slova a proměnné jsou *není* velká a malá písmena.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-157">In Visual Basic, keywords and variables are *not* case sensitive.</span></span>


### <a name="7-much-of-your-coding-involves-objects"></a><span data-ttu-id="0a5f4-158">7. Velká část kódování zahrnuje objekty</span><span class="sxs-lookup"><span data-stu-id="0a5f4-158">7. Much of your coding involves objects</span></span>

<span data-ttu-id="0a5f4-159">*Objekt* představuje věcí, kterou můžete programu s &#8212; na stránce textové pole, soubor, bitovou kopii, webové žádosti, e-mailovou zprávu, záznam zákazníka (řádku databáze), atd. Objekty mají vlastnosti, které popisují jejich vlastnosti a, abyste si přečetli nebo změnu &#8212; má objekt textové pole `Text` vlastnost (mimo jiné), má objekt žádosti `Url` vlastnost, má e-mailovou zprávu `From` vlastnost a objekt zákazník má `FirstName` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-159">An *object* represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics and that you can read or change &#8212; a text box object has a `Text` property (among others), a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="0a5f4-160">Objekty mají metody, které jsou také &quot;příkazy&quot; mohou provádět.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-160">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="0a5f4-161">Mezi příklady patří objektu soubor `Save` metoda, objekt image `Rotate` metoda a objekt e-mailu `Send` metoda.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-161">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="0a5f4-162">Často budete pracovat `Request` objektu, který nabízí informace jako hodnoty textová pole (polí formuláře) na stránce, jaký typ prohlížeče požadavek, adresu URL stránky, identita uživatele, atd. Následující příklad ukazuje, jak pro přístup k vlastnostem `Request` objekt a jak volat `MapPath` metodu `Request` objekt, který vám dává absolutní cesta ke stránce na serveru:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-162">You'll often work with the `Request` object, which gives you information like the values of text boxes (form fields) on the page, what type of browser made the request, the URL of the page, the user identity, etc. The following example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample9.html)]

<span data-ttu-id="0a5f4-163">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-163">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-c/_static/image5.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="0a5f4-165">8. Můžete napsat kód, který uskutečňuje rozhodnutí</span><span class="sxs-lookup"><span data-stu-id="0a5f4-165">8. You can write code that makes decisions</span></span>

<span data-ttu-id="0a5f4-166">Klíčovou funkcí dynamické webové stránky je, že můžete určit co dělat na základě podmínky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-166">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="0a5f4-167">Nejběžnější způsob k tomu je s `if` – příkaz (a volitelné `else` příkaz).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-167">The most common way to do this is with the `if` statement (and optional `else` statement).</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample10.cshtml)]

<span data-ttu-id="0a5f4-168">Příkaz `if(IsPost)` je zjednodušený způsob zápis `if(IsPost == true)`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-168">The statement `if(IsPost)` is a shorthand way of writing `if(IsPost == true)`.</span></span> <span data-ttu-id="0a5f4-169">Spolu s `if` příkazy, existuje mnoho různých způsobů, jak testovacích podmínkách, opakování bloky kódu, a tak dále, které jsou popsány dále v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-169">Along with `if` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="0a5f4-170">Výsledek zobrazí v prohlížeči (po kliknutí na **odeslání**):</span><span class="sxs-lookup"><span data-stu-id="0a5f4-170">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor Img6](introducing-razor-syntax-c/_static/image6.jpg)

> [!TIP] 
> 
> <a id="SB_HttpGetPost"></a>
> ### <a name="http-get-and-post-methods-and-the-ispost-property"></a><span data-ttu-id="0a5f4-172">HTTP GET a POST metody a vlastnosti IsPost</span><span class="sxs-lookup"><span data-stu-id="0a5f4-172">HTTP GET and POST Methods and the IsPost Property</span></span>
> 
> <span data-ttu-id="0a5f4-173">Protokol použitý pro webové stránky (HTTP) podporuje velmi omezený počet metody (operace), které se používají k provádění požadavků na server.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-173">The protocol used for web pages (HTTP) supports a very limited number of methods (verbs) that are used to make requests to the server.</span></span> <span data-ttu-id="0a5f4-174">Dva nejběžnější ty, které jsou GET, které slouží k načtení stránky, a POST, který se používá k odeslání stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-174">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="0a5f4-175">Obecně platí při prvním požadavku na stránce požadavku na stránku pomocí GET.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-175">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="0a5f4-176">Pokud uživatel doplní do formuláře a potom kliknutím na tlačítko pro odeslání, prohlížeč provede požadavek POST na server.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-176">If the user fills in a form and then clicks a submit button, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="0a5f4-177">Ve webové programování je často užitečné vědět, jestli stránky je vyžadován jako GET nebo POST, abyste věděli, jak pro zpracování stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-177">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="0a5f4-178">Na webových stránkách ASP.NET, můžete použít `IsPost` vlastnosti chcete zobrazit, zda je požadavek GET nebo POST.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-178">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="0a5f4-179">Pokud je požadavek POST, `IsPost` vlastnost vrátí hodnotu true, a můžete provést třeba číst hodnoty textová pole ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-179">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="0a5f4-180">Uvidíte mnoho příklady ukazují, jak zpracovat stránce odlišně v závislosti na hodnotě `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-180">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="0a5f4-181">Jednoduchým příkladem kódu</span><span class="sxs-lookup"><span data-stu-id="0a5f4-181">A Simple Code Example</span></span>

<span data-ttu-id="0a5f4-182">Tento postup ukazuje, jak vytvořit stránku, která ukazuje základní programovací techniky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-182">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="0a5f4-183">V příkladu vytvoříte stránky, který umožňuje uživatelům zadejte dvě čísla, potom se přidají a zobrazuje výsledek.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-183">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="0a5f4-184">V editoru, vytvořte nový soubor s názvem *AddNumbers.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-184">In your editor, create a new file and name it *AddNumbers.cshtml*.</span></span>
2. <span data-ttu-id="0a5f4-185">Zkopírujte následující kód a kód na stránku nahraďte nic již na stránce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-185">Copy the following code and markup into the page, replacing anything already in the page.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample11.cshtml)]

    <span data-ttu-id="0a5f4-186">Zde jsou některé kroky, abyste Všimněte si:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-186">Here are some things for you to note:</span></span>

    - <span data-ttu-id="0a5f4-187">`@` Znak spustí první blok kódu na stránce, a předchází `totalMessage` proměnné, která se vloží v dolní části stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-187">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable that's embedded near the bottom of the page.</span></span>
    - <span data-ttu-id="0a5f4-188">Blok v horní části stránky je uzavřené do složených závorek.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-188">The block at the top of the page is enclosed in braces.</span></span>
    - <span data-ttu-id="0a5f4-189">Všechny řádky v bloku v horní části končit středníkem.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-189">In the block at the top, all lines end with a semicolon.</span></span>
    - <span data-ttu-id="0a5f4-190">Proměnné `total`, `num1`, `num2`, a `totalMessage` uložit několik čísla a řetězec.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="0a5f4-191">Řetězcový literál hodnota přiřazená `totalMessage` proměnná je v uvozovkách.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="0a5f4-192">Protože kód je malá a velká písmena, když `totalMessage` proměnná se používá v dolní části stránky, jeho název musí přesně shodovat proměnnou v horní části.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-192">Because the code is case-sensitive, when the `totalMessage` variable is used near the bottom of the page, its name must match the variable at the top exactly.</span></span>
    - <span data-ttu-id="0a5f4-193">Výraz `num1.AsInt() + num2.AsInt()` ukazuje, jak pracovat s objekty a metody.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-193">The expression `num1.AsInt() + num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="0a5f4-194">`AsInt` Metodu na každou proměnnou převede řetězec zadaný uživatelem na číslo (integer) tak, aby aritmetické operace můžete provádět na něm.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-194">The `AsInt` method on each variable converts the string entered by a user to a number (an integer) so that you can perform arithmetic on it.</span></span>
    - <span data-ttu-id="0a5f4-195">`<form>` Zahrnuje značky `method="post"` atribut.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-195">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="0a5f4-196">To určuje, že když uživatel klikne **přidat**, stránky odešle na server pomocí metody POST protokolu HTTP.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-196">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="0a5f4-197">Při odeslání stránky `if(IsPost)` testovací vyhodnocena jako true a podmínku kód běží, zobrazení výsledkem přidání čísla.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-197">When the page is submitted, the `if(IsPost)` test evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="0a5f4-198">Uložit stránky a spusťte ji v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-198">Save the page and run it in a browser.</span></span> <span data-ttu-id="0a5f4-199">(Ujistěte se, že je vybraný stránky v **soubory** pracovního prostoru, než ji spustit.) Zadejte dvě celá čísla a poté klikněte **přidat** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-199">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span> 

    ![Razor-Img7](introducing-razor-syntax-c/_static/image7.jpg)

## <a name="basic-programming-concepts"></a><span data-ttu-id="0a5f4-201">Základní koncepty programování</span><span class="sxs-lookup"><span data-stu-id="0a5f4-201">Basic Programming Concepts</span></span>

<span data-ttu-id="0a5f4-202">Tento článek vám poskytne přehled webové programování technologie ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-202">This article provides you with an overview of ASP.NET web programming.</span></span> <span data-ttu-id="0a5f4-203">Není vyčerpávající zkoumání se právě stručný přehled prostřednictvím koncepty programování, který budete používat nejčastěji.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-203">It isn't an exhaustive examination, just a quick tour through the programming concepts you'll use most often.</span></span> <span data-ttu-id="0a5f4-204">I tak pokrývá téměř všechny objekty, které budete muset Začínáme s ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-204">Even so, it covers almost everything you'll need to get started with ASP.NET Web Pages.</span></span>

<span data-ttu-id="0a5f4-205">Ale první, málo technické pozadí.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-205">But first, a little technical background.</span></span>

### <a name="the-razor-syntax-server-code-and-aspnet"></a><span data-ttu-id="0a5f4-206">Syntaxe Razor, kódu serveru a technologie ASP.NET</span><span class="sxs-lookup"><span data-stu-id="0a5f4-206">The Razor Syntax, Server Code, and ASP.NET</span></span>

<span data-ttu-id="0a5f4-207">Syntaxe Razor je jednoduchý programovací syntaxi pro vkládání serverový kód na webové stránce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-207">Razor syntax is a simple programming syntax for embedding server-based code in a web page.</span></span> <span data-ttu-id="0a5f4-208">Na webové stránce, která používá syntaxi Razor, existují dva typy obsahu: obsah a server kódu klienta.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-208">In a web page that uses the Razor syntax, there are two kinds of content: client content and server code.</span></span> <span data-ttu-id="0a5f4-209">Obsah klienta je vy jste zvyklí ve webových stránkách: Značka jazyka HTML (prvky), styl informace, jako je šablon stylů CSS, možná některé klientského skriptu, jako je JavaScript a prostý text.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-209">Client content is the stuff you're used to in web pages: HTML markup (elements), style information such as CSS, maybe some client script such as JavaScript, and plain text.</span></span>

<span data-ttu-id="0a5f4-210">Syntaxe Razor můžete přidat serverový kód k tomuto obsahu klienta.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-210">Razor syntax lets you add server code to this client content.</span></span> <span data-ttu-id="0a5f4-211">Pokud je na stránce kód serveru, na serveru běží nejprve tento kód tak, než odešle stránku v prohlížeči uvedené.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-211">If there's server code in the page, the server runs that code first, before it sends the page to the browser.</span></span> <span data-ttu-id="0a5f4-212">Spuštěním na serveru, kód můžete provádět úlohy, které mohou být mnohem složitější provedete použitím klienta obsahu samostatně, jako je přístup na serveru databáze.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-212">By running on the server, the code can perform tasks that can be a lot more complex to do using client content alone, like accessing server-based databases.</span></span> <span data-ttu-id="0a5f4-213">Co je nejdůležitější kódu serveru můžete vytvořit dynamicky klienta obsah &#8212; může generovat kód HTML nebo jiného obsahu za chodu a potom ji odešlete do prohlížeče spolu s všechny statické HTML, které může obsahovat stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-213">Most importantly, server code can dynamically create client content &#8212; it can generate HTML markup or other content on the fly and then send it to the browser along with any static HTML that the page might contain.</span></span> <span data-ttu-id="0a5f4-214">Z hlediska prohlížeče je obsah klienta, který je generovaný kód serveru nejsou jiné než žádný jiný obsah klienta.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-214">From the browser's perspective, client content that's generated by your server code is no different than any other client content.</span></span> <span data-ttu-id="0a5f4-215">Jak už jste viděli, kód serveru, které je nutné je poměrně jednoduché.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-215">As you've already seen, the server code that's required is quite simple.</span></span>

<span data-ttu-id="0a5f4-216">Webové stránky ASP.NET, jež obsahují syntaxi Razor mají příponu speciální (*.cshtml* nebo *.vbhtml*).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-216">ASP.NET web pages that include the Razor syntax have a special file extension (*.cshtml* or *.vbhtml*).</span></span> <span data-ttu-id="0a5f4-217">Server rozpozná tato rozšíření, spustí kód, který je označen se syntaxí Razor a poté odešle stránce v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-217">The server recognizes these extensions, runs the code that's marked with Razor syntax, and then sends the page to the browser.</span></span>

### <a name="where-does-aspnet-fit-in"></a><span data-ttu-id="0a5f4-218">Kde se uplatní ASP.NET?</span><span class="sxs-lookup"><span data-stu-id="0a5f4-218">Where does ASP.NET fit in?</span></span>

<span data-ttu-id="0a5f4-219">Syntaxe Razor je založena na technologii Microsoft názvem ASP.NET, která zase je založena na rozhraní Microsoft .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-219">Razor syntax is based on a technology from Microsoft called ASP.NET, which in turn is based on the Microsoft .NET Framework.</span></span> <span data-ttu-id="0a5f4-220">Rozhraní.NET Framework je velký, komplexní programovací architektura od společnosti Microsoft pro vývoj prakticky jakéhokoli typu aplikace počítače.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-220">The.NET Framework is a big, comprehensive programming framework from Microsoft for developing virtually any type of computer application.</span></span> <span data-ttu-id="0a5f4-221">ASP.NET je součástí rozhraní .NET Framework, která je určená speciálně pro vytváření webových aplikací.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-221">ASP.NET is the part of the .NET Framework that's specifically designed for creating web applications.</span></span> <span data-ttu-id="0a5f4-222">Vývojáři mají ASP.NET použít k vytvoření řadu největší a nejvyšší provoz webů na světě.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-222">Developers have used ASP.NET to create many of the largest and highest-traffic websites in the world.</span></span> <span data-ttu-id="0a5f4-223">(Vždy, když se zobrazí příponu názvu souboru *.aspx* jako část adresy URL v lokalitě, budete vědět, zda webu, byla zapsána pomocí technologie ASP.NET.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-223">(Any time you see the file-name extension *.aspx* as part of the URL in a site, you'll know that the site was written using ASP.NET.)</span></span>

<span data-ttu-id="0a5f4-224">Syntaxe Razor vám dává všechny ASP.NET, ale pomocí zjednodušenou syntaxi, která je jednodušší zjistit, zda jste začátečník a který zpřístupňuje můžete zvýšit produktivitu Pokud jste odborník.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-224">The Razor syntax gives you all the power of ASP.NET, but using a simplified syntax that's easier to learn if you're a beginner and that makes you more productive if you're an expert.</span></span> <span data-ttu-id="0a5f4-225">I když tato syntaxe je snadno použitelný, jeho rodiny relaci ASP.NET a rozhraní .NET Framework znamená, že vaše weby jsou složitější, nutné power větší rozhraní, které jsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-225">Even though this syntax is simple to use, its family relationship to ASP.NET and the .NET Framework means that as your websites become more sophisticated, you have the power of the larger frameworks available to you.</span></span>

![Razor-Img8](introducing-razor-syntax-c/_static/image8.jpg)

> [!TIP] 
> 
> <span data-ttu-id="0a5f4-227">**Třídy a instance**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-227">**Classes and Instances**</span></span>
> 
> <span data-ttu-id="0a5f4-228">Kód serveru ASP.NET používá objekty, které jsou naopak postavené na nápad tříd.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-228">ASP.NET server code uses objects, which are in turn built on the idea of classes.</span></span> <span data-ttu-id="0a5f4-229">Třída je definice nebo šablony objektu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-229">The class is the definition or template for an object.</span></span> <span data-ttu-id="0a5f4-230">Například může aplikace obsahovat `Customer` třídu, která definuje vlastnosti a metody, které potřebuje libovolného objektu zákazníka.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-230">For example, an application might contain a `Customer` class that defines the properties and methods that any customer object needs.</span></span>
> 
> <span data-ttu-id="0a5f4-231">Když aplikace potřebuje pracovat s informacemi o skutečnou zákazníků, vytvoří instanci (nebo *vytvoří*) objekt zákazníka.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-231">When the application needs to work with actual customer information, it creates an instance of (or *instantiates*) a customer object.</span></span> <span data-ttu-id="0a5f4-232">Každého zákazníka se samostatnou instanci `Customer` třídy.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-232">Each individual customer is a separate instance of the `Customer` class.</span></span> <span data-ttu-id="0a5f4-233">Všechny instance podporuje stejné vlastnosti a metody, ale hodnoty vlastností pro každou instanci se obvykle liší, protože každý objekt zákazníka je jedinečný.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-233">Every instance supports the same properties and methods, but the property values for each instance are typically different, because each customer object is unique.</span></span> <span data-ttu-id="0a5f4-234">V objektu jednoho zákazníka `LastName` vlastnost může být "Smith"; v jiném objektu zákazníka `LastName` vlastnost může být "Petr."</span><span class="sxs-lookup"><span data-stu-id="0a5f4-234">In one customer object, the `LastName` property might be "Smith"; in another customer object, the `LastName` property might be "Jones."</span></span>
> 
> <span data-ttu-id="0a5f4-235">Podobně je všechny jednotlivé webové stránky ve vaší lokalitě `Page` objekt, který představuje instanci `Page` – třída.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-235">Similarly, any individual web page in your site is a `Page` object that's an instance of the `Page` class.</span></span> <span data-ttu-id="0a5f4-236">Tlačítko na stránce je `Button` objekt, který představuje instanci `Button` třídy a tak dále.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-236">A button on the page is a `Button` object that is an instance of the `Button` class, and so on.</span></span> <span data-ttu-id="0a5f4-237">Každá instance má svou vlastní vlastnosti, ale všechny jsou založené na zadaných v definici třídy objektu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-237">Each instance has its own characteristics, but they all are based on what's specified in the object's class definition.</span></span>


## <a name="basic-syntax"></a><span data-ttu-id="0a5f4-238">Základní syntaxe</span><span class="sxs-lookup"><span data-stu-id="0a5f4-238">Basic Syntax</span></span>

<span data-ttu-id="0a5f4-239">Dříve jste viděli základních příkladů jak vytvořit stránku ASP.NET Web Pages a přidání serverový kód pro kód HTML.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-239">Earlier you saw a basic example of how to create an ASP.NET Web Pages page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="0a5f4-240">Zde se dozvíte základní informace o psaní kódu serveru ASP.NET pomocí syntaxe Razor &#8212; To znamená, programovací jazyk pravidla.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-240">Here you'll learn the basics of writing ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="0a5f4-241">Pokud máte zkušenosti s programováním (obzvláště pokud jste použili C, C++, C#, Visual Basic nebo JavaScript), většinu si přečíst zde bude seznámit.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-241">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="0a5f4-242">Pravděpodobně budete muset Seznamte se jenom s jak kód serveru je přidán do značek v *.cshtml* soubory.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-242">You'll probably need to familiarize yourself only with how server code is added to markup in *.cshtml* files.</span></span>

<a id="BM_CombiningTextMarkupAndCode"></a>
### <a name="combining-text-markup-and-code-in-code-blocks"></a><span data-ttu-id="0a5f4-243">Kombinování Text, značek a kódu v bloky kódu</span><span class="sxs-lookup"><span data-stu-id="0a5f4-243">Combining Text, Markup, and Code in Code Blocks</span></span>

<span data-ttu-id="0a5f4-244">V serveru bloky kódu často chcete výstup textu nebo značek (nebo obě) na stránku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-244">In server code blocks, you often want to output text or markup (or both) to the page.</span></span> <span data-ttu-id="0a5f4-245">Pokud blok serverového kódu obsahuje text, který není kódu a který místo toho má být vykreslen jako je, musí být schopen rozlišit tento text z kódu ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-245">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="0a5f4-246">Chcete-li to provést několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-246">There are several ways to do this.</span></span>

- <span data-ttu-id="0a5f4-247">Uzavřete text v elementu HTML jako `<p></p>` nebo `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-247">Enclose the text in an HTML element like `<p></p>` or `<em></em>`:</span></span>   

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample12.cshtml)]

    <span data-ttu-id="0a5f4-248">Prvek HTML mohou zahrnovat text, další prvky HTML a výrazy kódu serveru.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-248">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="0a5f4-249">Když ASP.NET uvidí otevírání značky HTML (například `<p>`), vykreslí vše, včetně elementu a jeho obsah, jako je do prohlížeče, řešení výrazy kód serveru, protože probíhá.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-249">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything including the element and its content as is to the browser, resolving server-code expressions as it goes.</span></span>
- <span data-ttu-id="0a5f4-250">Použití `@:` operátor nebo `<text>` elementu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-250">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="0a5f4-251">`@:` Výstupy jeden řádek obsahující prostý text nebo neodpovídající značky HTML; obsahu `<text>` element uzavře více řádků na výstup.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-251">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="0a5f4-252">Tyto možnosti jsou užitečné, když nechcete, aby k vykreslení elementu HTML v rámci výstupu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-252">These options are useful when you don't want to render an HTML element as part of the output.</span></span>  

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample13.cshtml)]

    <span data-ttu-id="0a5f4-253">Pokud chcete výstup více řádků textu nebo neodpovídající značky HTML, je před každý řádek s `@:`, nebo můžete uzavřete na řádku `<text>` elementu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-253">If you want to output multiple lines of text or unmatched HTML tags, you can precede each line with `@:`, or you can enclose the line in a `<text>` element.</span></span> <span data-ttu-id="0a5f4-254">Podobně jako `@:` operátor,`<text>` značky jsou použitý technologií ASP.NET k identifikaci textového obsahu a nikdy se zobrazují v výstup stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-254">Like the `@:` operator,`<text>` tags are used by ASP.NET to identify text content and are never rendered in the page output.</span></span>

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample14.cshtml)]

    <span data-ttu-id="0a5f4-255">V prvním příkladu opakuje předchozí příklad, ale používá jednu dvojici `<text>` značek k uzavřete text k vykreslení.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-255">The first example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span> <span data-ttu-id="0a5f4-256">V druhém příkladu `<text>` a `</text>` značky uzavřete tři řádky, které mají všechny některé uncontained text a neodpovídající značky HTML (`<br />`), spolu s kódu serveru a odpovídající značky HTML.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-256">In the second example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="0a5f4-257">Znovu, může také předcházet každý řádek jednotlivě s `@:` operátor; buď způsob, jak funguje.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-257">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    > [!NOTE]
    > <span data-ttu-id="0a5f4-258">Pokud výstupní text jak je znázorněno v této části &#8212; pomocí prvku HTML `@:` operátor, nebo `<text>` element &#8212; Technologie ASP.NET není kódování HTML výstup.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-258">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="0a5f4-259">(Jak je uvedeno výše, ASP.NET kódování výstup výrazy kódu serveru a bloky kódu serveru, které jsou označeny `@`, s výjimkou ve zvláštních případech uvedených v této části.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-259">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="0a5f4-260">Whitespace</span><span class="sxs-lookup"><span data-stu-id="0a5f4-260">Whitespace</span></span>

<span data-ttu-id="0a5f4-261">Mezery v příkazu (a mimo řetězcový literál) není ovlivňují příkaz:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-261">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample15.cshtml)]

<span data-ttu-id="0a5f4-262">Konec řádku v příkazu nemá žádný vliv na příkaz a může obtékat příkazy čitelnější.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-262">A line break in a statement has no effect on the statement, and you can wrap statements for readability.</span></span> <span data-ttu-id="0a5f4-263">Následující příkazy jsou stejné:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-263">The following statements are the same:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample16.cshtml)]

<span data-ttu-id="0a5f4-264">Však nelze obalit řádku uprostřed řetězcový literál.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-264">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="0a5f4-265">Předchozí postup nebude fungovat v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-265">The following example doesn't work:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample17.cshtml)]

<span data-ttu-id="0a5f4-266">Kombinování dlouhý řetězec, který zabalí do více řádků jako výše uvedený kód, existují dvě možnosti.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-266">To combine a long string that wraps to multiple lines like the above code, there are two options.</span></span> <span data-ttu-id="0a5f4-267">Můžete použít operátor řetězení (`+`), který se zobrazí později v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-267">You can use the concatenation operator (`+`), which you'll see later in this article.</span></span> <span data-ttu-id="0a5f4-268">Můžete také `@` znak k vytvoření typu verbatim řetězec literálu, protože jste viděli dříve v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-268">You can also use the `@` character to create a verbatim string literal, as you saw earlier in this article.</span></span> <span data-ttu-id="0a5f4-269">Literály typu verbatim řetězec lze rozdělit na řádcích:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-269">You can break verbatim string literals across lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample18.cshtml)]

### <a name="code-and-markup-comments"></a><span data-ttu-id="0a5f4-270">Kód (a značky) komentáře</span><span class="sxs-lookup"><span data-stu-id="0a5f4-270">Code (and Markup) Comments</span></span>

<span data-ttu-id="0a5f4-271">Komentáře umožňují nechte poznámky pro sebe nebo ostatní.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-271">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="0a5f4-272">Také umožňují vám zakázat (*komentář*) oddílu kód nebo značky, které nechcete spustit ale chcete zachovat v stránku prozatím.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-272">They also allow you to disable (*comment out*) a section of code or markup that you don't want to run but want to keep in your page for the time being.</span></span>

<span data-ttu-id="0a5f4-273">Neexistuje jiný komentářů syntaxe Razor kódu a kódu HTML.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-273">There's different commenting syntax for Razor code and for HTML markup.</span></span> <span data-ttu-id="0a5f4-274">Stejně jako u všech kódu Razor, komentáře syntaxe Razor jsou zpracovány (a pak odebral) na serveru, než se stránka odešle do prohlížeče.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-274">As with all Razor code, Razor comments are processed (and then removed) on the server before the page is sent to the browser.</span></span> <span data-ttu-id="0a5f4-275">Proto komentovaný syntaxi Razor umožňuje umístit komentáře v kódu (nebo i do kódu), které se zobrazí, pokud chcete soubor upravit, ale není zobrazen uživatelům, i ve zdroji stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-275">Therefore, the Razor commenting syntax lets you put comments into the code (or even into the markup) that you can see when you edit the file, but that users don't see, even in the page source.</span></span>

<span data-ttu-id="0a5f4-276">Pro komentáře syntaxe Razor rozhraní ASP.NET, spusťte komentář s `@*` a ukončete její `*@`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-276">For ASP.NET Razor comments, you start the comment with `@*` and end it with `*@`.</span></span> <span data-ttu-id="0a5f4-277">Komentář může být na jeden nebo více řádků:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-277">The comment can be on one line or multiple lines:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample19.cshtml)]

<span data-ttu-id="0a5f4-278">Tady je komentář v rámci bloku kódu:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-278">Here is a comment within a code block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample20.cshtml)]

<span data-ttu-id="0a5f4-279">Zde je stejný blok kódu, s řádkem kódu komentované tak, aby se nespustí:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-279">Here is the same block of code, with the line of code commented out so that it won't run:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample21.cshtml)]

<span data-ttu-id="0a5f4-280">Uvnitř bloku kódu jako alternativu k použití komentáře syntaxe Razor, můžete použít komentovaný syntaxe programovací jazyk, který používáte, například C#:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-280">Inside a code block, as an alternative to using Razor comment syntax, you can use the commenting syntax of the programming language you're using, such as C#:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample22.cshtml)]

<span data-ttu-id="0a5f4-281">V jazyce C#, jeden řádek komentáře předchází `//` znaky a komentáře Víceřádkový začínat `/*` a končit `*/`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-281">In C#, single-line comments are preceded by the `//` characters, and multi-line comments begin with `/*` and end with `*/`.</span></span> <span data-ttu-id="0a5f4-282">(Stejně jako u komentáře syntaxe Razor, se nevykreslí komentáře jazyka C# do prohlížeče.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-282">(As with Razor comments, C# comments are not rendered to the browser.)</span></span>

<span data-ttu-id="0a5f4-283">Pro značky jak pravděpodobně víte, můžete vytvořit komentář HTML:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-283">For markup, as you probably know, you can create an HTML comment:</span></span>

[!code-xml[Main](introducing-razor-syntax-c/samples/sample23.xml)]

<span data-ttu-id="0a5f4-284">Komentáře HTML začínat `<!--` znaků a konec s `-->`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-284">HTML comments start with `<!--` characters and end with `-->`.</span></span> <span data-ttu-id="0a5f4-285">Komentáře HTML můžete obklopit pouze text, ale také všechny jazyka HTML, který budete chtít zachovat na stránce, ale nechcete, aby k vykreslení.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-285">You can use HTML comments to surround not only text, but also any HTML markup that you may want to keep in the page but don't want to render.</span></span> <span data-ttu-id="0a5f4-286">Tento komentář HTML skryje celý obsah značky a text, který obsahují:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-286">This HTML comment will hide the entire content of the tags and the text they contain:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample24.html)]

<span data-ttu-id="0a5f4-287">Na rozdíl od komentáře syntaxe Razor, HTML komentáře *jsou* vykresluje na stránku a uživatel je uvidí zobrazením zdroji stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-287">Unlike Razor comments, HTML comments *are* rendered to the page and the user can see them by viewing the page source.</span></span>

<span data-ttu-id="0a5f4-288">Syntaxe Razor má omezení na vnořené bloky jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-288">Razor has limitations on nested blocks of C#.</span></span> <span data-ttu-id="0a5f4-289">Další informace najdete v článku [s názvem C# proměnné a vnořené bloky generovat přerušený kód](http://aspnetwebstack.codeplex.com/workitem/1914)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-289">For more information see [Named C# Variables and Nested Blocks Generate Broken Code](http://aspnetwebstack.codeplex.com/workitem/1914)</span></span>

## <a name="variables"></a><span data-ttu-id="0a5f4-290">Proměnné</span><span class="sxs-lookup"><span data-stu-id="0a5f4-290">Variables</span></span>

<span data-ttu-id="0a5f4-291">Proměnné je objekt s názvem, který použijete k uložení dat.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-291">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="0a5f4-292">Zadáte název proměnné nic, ale název musí začínat znakem abecedy a nesmí obsahovat prázdné znaky nebo vyhrazené znaky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-292">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="0a5f4-293">Datové typy a proměnné</span><span class="sxs-lookup"><span data-stu-id="0a5f4-293">Variables and Data Types</span></span>

<span data-ttu-id="0a5f4-294">Proměnná může mít specifické datového typu, který určuje, jaký typ dat je uložené v proměnné.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-294">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="0a5f4-295">Můžete mít proměnné řetězce, které ukládají řetězcové hodnoty (například &quot;Hello, world&quot;), celé číslo proměnné, které ukládají celé číslo hodnoty (například 3 nebo 79) a proměnné datum, které ukládání hodnot data v různých formátech (např. 4/12/2012 nebo březnem 2009 ).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-295">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="0a5f4-296">A existuje mnoho dalších typů dat, které můžete použít.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-296">And there are many other data types you can use.</span></span>

<span data-ttu-id="0a5f4-297">Ale obecně nemusíte zadejte typ pro proměnnou.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-297">However, you generally don't have to specify a type for a variable.</span></span> <span data-ttu-id="0a5f4-298">Ve většině případů, ASP.NET můžete zjistit typ závislosti na tom, jak se používá data v proměnné.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-298">Most of the time, ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="0a5f4-299">(Někdy nutné zadat typ, uvidíte příklady, pokud je to pravda.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-299">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="0a5f4-300">Deklarace proměnné pomocí `var` – klíčové slovo (Pokud nechcete zadat typ) nebo pomocí název typu:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-300">You declare a variable using the `var` keyword (if you don't want to specify a type) or by using the name of the type:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample25.cshtml)]

<span data-ttu-id="0a5f4-301">Následující příklad ukazuje některé typické použití proměnných na webové stránce:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-301">The following example shows some typical uses of variables in a web page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample26.cshtml)]

<span data-ttu-id="0a5f4-302">Pokud kombinujete v předchozích příkladech na stránce, zobrazí tento zobrazený v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-302">If you combine the previous examples in a page, you see this displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-c/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="0a5f4-304">Převod a testování datové typy</span><span class="sxs-lookup"><span data-stu-id="0a5f4-304">Converting and Testing Data Types</span></span>

<span data-ttu-id="0a5f4-305">I když ASP.NET lze obvykle zjistit datový typ automaticky, někdy ji nelze.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-305">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="0a5f4-306">Proto můžete chtít Pomozte ASP.NET tím, že provádění explicitní převod.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-306">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="0a5f4-307">I když nemáte pro převod typů, někdy je užitečné zjistěte, jaký typ dat je možné, že pracujete s.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-307">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="0a5f4-308">Nejběžnější případem je, že budete muset převést řetězec na jiný typ, například na celé číslo nebo datum.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-308">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="0a5f4-309">Následující příklad ukazuje obvyklý případ, kdy je nutné převést řetězec na číslo.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-309">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample27.cshtml)]

<span data-ttu-id="0a5f4-310">Uživatelský vstup se dodává jako pravidlo, pro vás jako řetězce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-310">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="0a5f4-311">I když jste výzva k uživatelům zadejte číslo, a i v případě číslici, že jste zadali při odeslání vstup uživatele a můžete číst v kódu, se data ve formátu řetězce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-311">Even if you've prompted users to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="0a5f4-312">Proto je nutné převést řetězec na číslo.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-312">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="0a5f4-313">V příkladu Pokud se pokusíte provést aritmetické na hodnoty bez převodu, k následující chybě výsledky, protože ASP.NET nelze přidat dva řetězce:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-313">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

<span data-ttu-id="0a5f4-314">*Typ 'řetězec' pro 'int, nelze implicitně převést.*</span><span class="sxs-lookup"><span data-stu-id="0a5f4-314">*Cannot implicitly convert type 'string' to 'int'.*</span></span>

<span data-ttu-id="0a5f4-315">K převodu hodnoty na celá čísla, zavoláte `AsInt` metoda.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-315">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="0a5f4-316">Pokud převod úspěšný, můžete pak přidat čísla.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-316">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="0a5f4-317">Následující tabulka uvádí některé běžné převod a testování metody pro proměnné.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-317">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="0a5f4-318">**– Metoda**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-318">**Method**</span></span> | <span data-ttu-id="0a5f4-319">**Popis**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-319">**Description**</span></span> | <span data-ttu-id="0a5f4-320">**Příklad**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-320">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="0a5f4-321">Převede řetězec, který představuje celé číslo (např. "593") na celé číslo.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-321">Converts a string that represents a whole number (like "593") to an integer.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample28.cs)] |
| `AsBool(), IsBool()` | <span data-ttu-id="0a5f4-322">Převede řetězec jako &quot;true&quot; nebo &quot;false&quot; typu Boolean.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-322">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample29.cs)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="0a5f4-323">Převede řetězec, který má hodnotu decimal jako &quot;1.3&quot; nebo &quot;7.439&quot; na číslo s plovoucí desetinnou čárkou.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-323">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample30.cs)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="0a5f4-324">Převede řetězec, který má hodnotu decimal jako &quot;1.3&quot; nebo &quot;7.439&quot; na desetinné číslo.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-324">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="0a5f4-325">(V technologii ASP.NET, je přesnější než číslo s plovoucí desetinnou čárkou desetinné číslo.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-325">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample31.cs)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="0a5f4-326">Převede řetězec představující hodnotu data a času ASP.NET `DateTime` typu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-326">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample32.cs)] |
| `ToString()` | <span data-ttu-id="0a5f4-327">Převede ostatních typů dat na řetězec.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-327">Converts any other data type to a string.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample33.js)] |

## <a name="operators"></a><span data-ttu-id="0a5f4-328">Operátory</span><span class="sxs-lookup"><span data-stu-id="0a5f4-328">Operators</span></span>

<span data-ttu-id="0a5f4-329">Operátor je – klíčové slovo nebo znak, který sděluje ASP.NET, jaký druh příkaz k provedení ve výrazu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-329">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="0a5f4-330">Jazyk C# (a syntaxe Razor, který je založený na něm) podporuje mnoho operátory, ale potřebujete rozpoznat pár začít pracovat.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-330">The C# language (and the Razor syntax that's based on it) supports many operators, but you only need to recognize a few to get started.</span></span> <span data-ttu-id="0a5f4-331">Následující tabulka shrnuje nejběžnější operátory.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-331">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="0a5f4-332">**Operátor**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-332">**Operator**</span></span> | <span data-ttu-id="0a5f4-333">**Popis**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-333">**Description**</span></span> | <span data-ttu-id="0a5f4-334">**Příklady**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-334">**Examples**</span></span> |
| --- | --- | --- |
| <span data-ttu-id="0a5f4-335">`+` `-` `*` `/`</span><span class="sxs-lookup"><span data-stu-id="0a5f4-335">`+` `-` `*` `/`</span></span> | <span data-ttu-id="0a5f4-336">Matematické operátory použít ve výrazech číselná.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-336">Math operators used in numerical expressions.</span></span> | [!code-css[Main](introducing-razor-syntax-c/samples/sample34.css)] |
| `=` | <span data-ttu-id="0a5f4-337">Přiřazení.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-337">Assignment.</span></span> <span data-ttu-id="0a5f4-338">Hodnota na pravé straně příkazu přiřadí objekt na levé straně.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-338">Assigns the value on the right side of a statement to the object on the left side.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample35.cs)] |
| `==` | <span data-ttu-id="0a5f4-339">Rovnosti.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-339">Equality.</span></span> <span data-ttu-id="0a5f4-340">Vrátí `true` Pokud jsou hodnoty stejné.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-340">Returns `true` if the values are equal.</span></span> <span data-ttu-id="0a5f4-341">(Všimněte si rozdíl mezi `=` operátor a `==` operátor.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-341">(Notice the distinction between the `=` operator and the `==` operator.)</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample36.cs)] |
| `!=` | <span data-ttu-id="0a5f4-342">Nerovnosti.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-342">Inequality.</span></span> <span data-ttu-id="0a5f4-343">Vrátí `true` Pokud hodnoty nejsou shodné.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-343">Returns `true` if the values are not equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample37.cs)] |
| `< > <= >=` | <span data-ttu-id="0a5f4-344">Menší – než, více – než menší než nebo rovno a větší než nebo rovno.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-344">Less-than, greater-than, less-than-or-equal, and greater-than-or-equal.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample38.cs)] |
| `+` | <span data-ttu-id="0a5f4-345">Zřetězení, který se používá pro připojení řetězce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-345">Concatenation, which is used to join strings.</span></span> <span data-ttu-id="0a5f4-346">ASP.NET zná rozdíl mezi tento operátor a operátor sčítání na základě typu dat, z výrazu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-346">ASP.NET knows the difference between this operator and the addition operator based on the data type of the expression.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample39.cs)] |
| <span data-ttu-id="0a5f4-347">`+=``-=`</span><span class="sxs-lookup"><span data-stu-id="0a5f4-347">`+=` `-=`</span></span> | <span data-ttu-id="0a5f4-348">Přírůstek a snížení operátory, které sčítání a odečítání 1 (v uvedeném pořadí) z proměnné.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-348">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample40.cs)] |
| `.` | <span data-ttu-id="0a5f4-349">Tečkou.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-349">Dot.</span></span> <span data-ttu-id="0a5f4-350">Používá k rozlišení objekty a jejich vlastnosti a metody.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-350">Used to distinguish objects and their properties and methods.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample41.cs)] |
| `()` | <span data-ttu-id="0a5f4-351">Závorky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-351">Parentheses.</span></span> <span data-ttu-id="0a5f4-352">Použít na výrazy skupiny a předat parametry metody.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-352">Used to group expressions and to pass parameters to methods.</span></span> | [!code-javascript[Main](introducing-razor-syntax-c/samples/sample42.js)] |
| `[]` | <span data-ttu-id="0a5f4-353">Závorky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-353">Brackets.</span></span> <span data-ttu-id="0a5f4-354">Použít pro přístup k hodnot v polích nebo kolekce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-354">Used for accessing values in arrays or collections.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample43.cs)] |
| `!` | <span data-ttu-id="0a5f4-355">Není.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-355">Not.</span></span> <span data-ttu-id="0a5f4-356">Obrátí `true` hodnotu `false` a naopak.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-356">Reverses a `true` value to `false` and vice versa.</span></span> <span data-ttu-id="0a5f4-357">Obvykle se používá jako sdružená způsob, jak otestovat `false` (který je pro není `true`).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-357">Typically used as a shorthand way to test for `false` (that is, for not `true`).</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample44.cs)] |
| <span data-ttu-id="0a5f4-358">`&&`<code>&#124;&#124;</code></span><span class="sxs-lookup"><span data-stu-id="0a5f4-358">`&&` <code>&#124;&#124;</code></span></span> | <span data-ttu-id="0a5f4-359">Logické a a, nebo které se používají k propojení podmínky společně.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-359">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-csharp[Main](introducing-razor-syntax-c/samples/sample45.cs)] |

<a id="ID_WorkingWithFileAndFolderPaths"></a>
## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="0a5f4-360">Práce s souboru a cesty ke složce zadat v kódu</span><span class="sxs-lookup"><span data-stu-id="0a5f4-360">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="0a5f4-361">Cesty k souborům a složkám budete často pracovat v kódu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-361">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="0a5f4-362">Tady je příklad struktury fyzická složka pro web, jak se může objevit ve svém vývojovém počítači:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-362">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="0a5f4-363">Zde jsou některé důležité podrobnosti o adresy URL a cesty:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-363">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="0a5f4-364">Adresa URL začíná, buď název domény (`http://www.example.com`) nebo název serveru (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-364">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="0a5f4-365">Adresa URL odpovídá na fyzickou cestu na hostitelském počítači.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-365">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="0a5f4-366">Například `http://myserver` může odpovídat do složky *C:\websites\mywebsite* na serveru.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-366">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="0a5f4-367">Virtuální cesta je sdružená představují cesty v kódu bez nutnosti zadávat úplnou cestu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-367">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="0a5f4-368">Obsahuje části adresy URL, která následuje název domény nebo serveru.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-368">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="0a5f4-369">Pokud používáte virtuální cesty, můžete přesunout kódu do jiné domény nebo serveru bez nutnosti aktualizovat cesty.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-369">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="0a5f4-370">Tady je příklad, které vám pomohou pochopit rozdíly:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-370">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="0a5f4-371">Úplnou adresu URL</span><span class="sxs-lookup"><span data-stu-id="0a5f4-371">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="0a5f4-372">název serveru</span><span class="sxs-lookup"><span data-stu-id="0a5f4-372">Server name</span></span> | <span data-ttu-id="0a5f4-373">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="0a5f4-373">*mycompanyserver*</span></span> |
| <span data-ttu-id="0a5f4-374">Virtuální cesta</span><span class="sxs-lookup"><span data-stu-id="0a5f4-374">Virtual path</span></span> | <span data-ttu-id="0a5f4-375">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="0a5f4-375">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="0a5f4-376">Fyzická cesta</span><span class="sxs-lookup"><span data-stu-id="0a5f4-376">Physical path</span></span> | <span data-ttu-id="0a5f4-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="0a5f4-377">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="0a5f4-378">Je virtuální kořenový adresář, /, stejně jako kořenové jednotce C:, jednotka je \.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-378">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="0a5f4-379">(Virtuální složky cesty vždy použít lomítka.) Virtuální cesta ke složce nemusí mít stejný název jako fyzická složka; může být alias.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-379">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="0a5f4-380">(Na provozních serverech virtuální cestu zřídka odpovídá přesnou fyzickou cestu.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-380">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="0a5f4-381">Při práci se soubory a složky v kódu, někdy budete muset odkazovat na fyzickou cestu a někdy virtuální cesty, v závislosti na tom, jaké objekty, kterými pracujete.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-381">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="0a5f4-382">Technologie ASP.NET poskytuje tyto nástroje pro práci s cest k souborům a složkám v kódu: `Server.MapPath` metody a `~` operátor a `Href` metoda.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-382">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="0a5f4-383">Převod virtuálních a fyzických cest: Metoda Server.MapPath</span><span class="sxs-lookup"><span data-stu-id="0a5f4-383">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="0a5f4-384">`Server.MapPath` Metoda převede virtuální cestu (například */default.cshtml*) na absolutní fyzická cesta (jako *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-384">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="0a5f4-385">Tuto metodu použijte vždy, když potřebujete úplnou fyzickou cestu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-385">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="0a5f4-386">Typickým příkladem je při čtení nebo zápis textový soubor nebo soubor bitové kopie na webovém serveru.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-386">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="0a5f4-387">Obvykle neznáte absolutní fyzická cesta vašeho webu na serveru pro hostování lokality, takže této metody můžete převést cestu si jisti – virtuální cestu – odpovídající cestu na serveru pro vás.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-387">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="0a5f4-388">Předat virtuální cestu k souboru nebo složce metodu a vrátí fyzickou cestu:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-388">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample46.cshtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="0a5f4-389">Odkazování na virtuální kořenový adresář: ~ operátor a metoda Href</span><span class="sxs-lookup"><span data-stu-id="0a5f4-389">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="0a5f4-390">V *.cshtml* nebo *.vbhtml* souboru, můžete odkazovat pomocí cesty virtuální kořenový adresář `~` operátor.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-390">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="0a5f4-391">To je velmi užitečný, protože můžete přesouváte stránky v lokalitě a všechny odkazy, které obsahují k ostatním stránkám nebudou přerušená.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-391">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="0a5f4-392">Je taky užitečný v případě, že přesunete někdy webu do jiného umístění.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-392">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="0a5f4-393">Následuje několik příkladů:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-393">Here are some examples:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample47.cshtml)]

<span data-ttu-id="0a5f4-394">Pokud je web `http://myserver/myapp`, zde je, jak bude ASP.NET považovat tyto cesty při spuštění stránky:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-394">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="0a5f4-395">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="0a5f4-395">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="0a5f4-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="0a5f4-396">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="0a5f4-397">(Ve skutečnosti neuvidíte tyto cesty jako hodnoty proměnné, ale technologie ASP.NET bude považovat cesty, jako kdyby se, jak byly.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-397">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="0a5f4-398">Můžete použít `~` operátor v serverovém kódu (jak je uvedeno výše) a ve značkách takto:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-398">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample48.html)]

<span data-ttu-id="0a5f4-399">V kódu, můžete použít `~` operátor vytvoření cesty k prostředkům, jako jsou soubory obrázků, jiné webové stránky a souborů CSS.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-399">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="0a5f4-400">Při spuštění stránky ASP.NET vypadá prostřednictvím stránky (kód a kód) a všechny přeloží `~` odkazy na příslušné cestě.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-400">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="0a5f4-401">Podmíněnou logiku a smyčky</span><span class="sxs-lookup"><span data-stu-id="0a5f4-401">Conditional Logic and Loops</span></span>

<span data-ttu-id="0a5f4-402">Kód serveru technologie ASP.NET umožňuje provádět úlohy na základě podmínek a napsat kód, který provede příkazy konkrétní počet (to znamená, kód, který běží smyčku).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-402">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times (that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="0a5f4-403">Testování podmínky</span><span class="sxs-lookup"><span data-stu-id="0a5f4-403">Testing Conditions</span></span>

<span data-ttu-id="0a5f4-404">K otestování jednoduché podmínku můžete použít `if` příkaz, který vrátí hodnotu PRAVDA nebo NEPRAVDA podle testu zadáte:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-404">To test a simple condition you use the `if` statement, which returns true or false based on a test you specify:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample49.cshtml)]

<span data-ttu-id="0a5f4-405">`if` – Klíčové slovo spustí blok.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-405">The `if` keyword starts a block.</span></span> <span data-ttu-id="0a5f4-406">Skutečné test (podmínka) je v závorkách a vrátí hodnotu true nebo false.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-406">The actual test (condition) is in parentheses and returns true or false.</span></span> <span data-ttu-id="0a5f4-407">Příkazy, které spustit, pokud platí test jsou uzavřené do složených závorek.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-407">The statements that run if the test is true are enclosed in braces.</span></span> <span data-ttu-id="0a5f4-408">`if` Může zahrnovat příkaz `else` blok, který určuje příkazy ke spouštění, pokud je podmínka vyhodnocena jako false:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-408">An `if` statement can include an `else` block that specifies statements to run if the condition is false:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample50.cshtml)]

<span data-ttu-id="0a5f4-409">Můžete přidat více podmínek použití `else if` bloku:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-409">You can add multiple conditions using an `else if` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample51.cshtml)]

<span data-ttu-id="0a5f4-410">V tomto příkladu, pokud je první podmínka vyhodnocena jako v oblasti, pokud blok není nastavena hodnota true, `else if` se kontroluje podmínku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-410">In this example, if the first condition in the if block is not true, the `else if` condition is checked.</span></span> <span data-ttu-id="0a5f4-411">Při splnění této podmínky je, příkazy v `else if` provedení bloku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-411">If that condition is met, the statements in the `else if` block are executed.</span></span> <span data-ttu-id="0a5f4-412">Pokud žádná z podmínek jsou splněny, příkazy v `else` provedení bloku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-412">If none of the conditions are met, the statements in the `else` block are executed.</span></span> <span data-ttu-id="0a5f4-413">Můžete přidat libovolný počet else if blokuje a pak zavřete s `else` blokovat jako &quot;všem ostatním&quot; podmínku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-413">You can add any number of else if blocks, and then close with an `else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="0a5f4-414">Chcete-li otestovat velký počet podmínky, použijte `switch` bloku:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-414">To test a large number of conditions, use a `switch` block:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample52.cshtml)]

<span data-ttu-id="0a5f4-415">Hodnota k testování je v závorkách (v příkladu `weekday` proměnné).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-415">The value to test is in parentheses (in the example, the `weekday` variable).</span></span> <span data-ttu-id="0a5f4-416">Používá každé jednotlivé testy `case` příkaz, který končí dvojtečkou (:).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-416">Each individual test uses a `case` statement that ends with a colon (:).</span></span> <span data-ttu-id="0a5f4-417">Pokud hodnota `case` příkaz odpovídá hodnotě testu, spuštění kódu v tomto případu bloku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-417">If the value of a `case` statement matches the test value, the code in that case block is executed.</span></span> <span data-ttu-id="0a5f4-418">Zavřete každou case – příkaz s `break` příkaz.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-418">You close each case statement with a `break` statement.</span></span> <span data-ttu-id="0a5f4-419">(Pokud zapomenete zahrnout rozdělení v každém `case` blok kódu z další `case` příkaz spustí také.) A `switch` bloku často má `default` příkaz jako poslední případ &quot;všem ostatním&quot; možnost, která spustí, pokud žádné z ostatních případech platí.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-419">(If you forget to include break in each `case` block, the code from the next `case` statement will run also.) A `switch` block often has a `default` statement as the last case for an &quot;everything else&quot; option that runs if none of the other cases are true.</span></span>

<span data-ttu-id="0a5f4-420">Výsledek posledních dvou podmíněné bloky zobrazený v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-420">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-c/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="0a5f4-422">Ve smyčce kódu</span><span class="sxs-lookup"><span data-stu-id="0a5f4-422">Looping Code</span></span>

<span data-ttu-id="0a5f4-423">Potřebujete často opakovaně spouštět stejné příkazy.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-423">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="0a5f4-424">To provedete opakování ve smyčce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-424">You do this by looping.</span></span> <span data-ttu-id="0a5f4-425">Například můžete často spustit stejné příkazy pro každou položku v kolekci dat.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-425">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="0a5f4-426">Pokud znáte přesně kolikrát chcete cykly, můžete použít `for` smyčky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-426">If you know exactly how many times you want to loop, you can use a `for` loop.</span></span> <span data-ttu-id="0a5f4-427">Tento druh smyčky je zvláště užitečná pro počítání nebo počítání:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-427">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample53.html)]

<span data-ttu-id="0a5f4-428">Smyčky začíná `for` – klíčové slovo, za nímž následuje tři příkazy v závorkách, každý ukončit středníkem.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-428">The loop begins with the `for` keyword, followed by three statements in parentheses, each terminated with a semicolon.</span></span>

- <span data-ttu-id="0a5f4-429">V závorkách, první příkaz (`var i=10;`) vytvoří čítače a inicializuje do 10.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-429">Inside the parentheses, the first statement (`var i=10;`) creates a counter and initializes it to 10.</span></span> <span data-ttu-id="0a5f4-430">Nemáte název čítače `i` &#8212; můžete použít všechny proměnné.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-430">You don't have to name the counter `i` &#8212; you can use any variable.</span></span> <span data-ttu-id="0a5f4-431">Když `for` spustí smyčky, hodnota čítače se zvýší automaticky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-431">When the `for` loop runs, the counter is automatically incremented.</span></span>
- <span data-ttu-id="0a5f4-432">Druhý příkaz (`i < 21;`) nastaví podmínky pro jak daleko chcete spočítat.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-432">The second statement (`i < 21;`) sets the condition for how far you want to count.</span></span> <span data-ttu-id="0a5f4-433">V takovém případě se má přejít na maximálně 20 (to znamená, pokračujte při čítač je menší než 21).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-433">In this case, you want it to go to a maximum of 20 (that is, keep going while the counter is less than 21).</span></span>
- <span data-ttu-id="0a5f4-434">Příkaz třetí (`i++` ) používá operátor přírůstku, která jednoduše Určuje, že čítač by měl mít 1 přidat k němu pokaždé, když běží smyčky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-434">The third statement (`i++` ) uses an increment operator, which simply specifies that the counter should have 1 added to it each time the loop runs.</span></span>

<span data-ttu-id="0a5f4-435">Uvnitř závorek je kód, který se spustí pro každou iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-435">Inside the braces is the code that will run for each iteration of the loop.</span></span> <span data-ttu-id="0a5f4-436">Kód vytvoří nový odstavec (`<p>` element) každou čas a přidá řádek do výstupu zobrazení hodnotu `i` (Čítač).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-436">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of `i` (the counter).</span></span> <span data-ttu-id="0a5f4-437">Při spuštění této stránce v příkladu se vytváří 11 řádky zobrazení výstupu s textem do každého řádku, která určuje počet položek.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-437">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-c/_static/image11.jpg)

<span data-ttu-id="0a5f4-439">Pokud pracujete s kolekce nebo pole, často používají `foreach` smyčky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-439">If you're working with a collection or array, you often use a `foreach` loop.</span></span> <span data-ttu-id="0a5f4-440">Kolekce je skupina podobné objekty a `foreach` cykly umožňuje provádět úlohy na každou položku v kolekci.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-440">A collection is a group of similar objects, and the `foreach` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="0a5f4-441">Tento typ smyčky je vhodné pro kolekce, protože na rozdíl od `for` smyčky, nemusíte zvýší čítače nebo nastavit limit.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-441">This type of loop is convenient for collections, because unlike a `for` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="0a5f4-442">Místo toho `foreach` smyčky kód jednoduše pokračuje prostřednictvím kolekce, dokud se nedokončí jeho.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-442">Instead, the `foreach` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="0a5f4-443">Například následující kód vrátí položky v `Request.ServerVariables` kolekce, která je objekt, který obsahuje informace o vašem webovém serveru.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-443">For example, the following code returns the items in the `Request.ServerVariables` collection, which is an object that contains information about your web server.</span></span> <span data-ttu-id="0a5f4-444">Použije `foreac` h smyčky, zobrazí se název každé položky tak, že vytvoříte novou `<li>` element v seznamu s odrážkami HTML.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-444">It uses a `foreac` h loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-html[Main](introducing-razor-syntax-c/samples/sample54.html)]

<span data-ttu-id="0a5f4-445">`foreach` – Klíčové slovo následuje závorkách kde deklarace proměnné, která představuje jednu položku v kolekci (v příkladu `var item`), za nímž následují `in` – klíčové slovo, za nímž následuje kolekce, kterou chcete projít.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-445">The `foreach` keyword is followed by parentheses where you declare a variable that represents a single item in the collection (in the example, `var item`), followed by the `in` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="0a5f4-446">V textu `foreach` smyčky, dostanete s aktuální položkou použitím proměnné, kterou jste předtím deklarován.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-446">In the body of the `foreach` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-c/_static/image12.jpg)

<span data-ttu-id="0a5f4-448">Chcete-li vytvořit více pro obecné účely smyčku, použijte `while` příkaz:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-448">To create a more general-purpose loop, use the `while` statement:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample55.cshtml)]

<span data-ttu-id="0a5f4-449">A `while` smyčky začíná `while` – klíčové slovo, následuje závorky, kde můžete určit, jak dlouho bude pokračovat smyčky (sem pro tak dlouho, dokud `countNum` je menší než 50), pak bloku opakování.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-449">A `while` loop begins with the `while` keyword, followed by parentheses where you specify how long the loop continues (here, for as long as `countNum` is less than 50), then the block to repeat.</span></span> <span data-ttu-id="0a5f4-450">Obvykle zvýšit smyčky (přidejte) nebo snížení (odečíst od) proměnnou nebo objekt použitý pro počítání.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-450">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="0a5f4-451">V příkladu `+=` operátor přidá 1 `countNum` pokaždé, když běží smyčky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-451">In the example, the `+=` operator adds 1 to `countNum` each time the loop runs.</span></span> <span data-ttu-id="0a5f4-452">(Chcete-li snížení proměnné ve smyčce počty dolů, použijte operátor snížení `-=`).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-452">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`).</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="0a5f4-453">Objekty a kolekce</span><span class="sxs-lookup"><span data-stu-id="0a5f4-453">Objects and Collections</span></span>

<span data-ttu-id="0a5f4-454">Téměř všechno ve webové stránky ASP.NET je objekt, včetně webové stránky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-454">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="0a5f4-455">Tato část popisuje některé důležité objekty, se kterými budete pracovat často v kódu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-455">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="0a5f4-456">Objekty stránky</span><span class="sxs-lookup"><span data-stu-id="0a5f4-456">Page Objects</span></span>

<span data-ttu-id="0a5f4-457">Objekt nejzákladnější technologie ASP.NET je stránka.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-457">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="0a5f4-458">Můžete přistoupit k vlastnostem objektu page přímo bez žádné opravňující objektu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-458">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="0a5f4-459">Následující kód získá cesta k souboru stránky, pomocí `Request` objekt stránky:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-459">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample56.cshtml)]

<span data-ttu-id="0a5f4-460">Aby bylo jasné jste odkazování vlastnosti a metody pro aktuální objekt stránky, můžete volitelně použít klíčové slovo `this` k reprezentaci objektu stránky ve vašem kódu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-460">To make it clear that you're referencing properties and methods on the current page object, you can optionally use the keyword `this` to represent the page object in your code.</span></span> <span data-ttu-id="0a5f4-461">Tady je předchozí příklad kódu s `this` přidány do představují stránky:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-461">Here is the previous code example, with `this` added to represent the page:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample57.cshtml)]

<span data-ttu-id="0a5f4-462">Můžete použít vlastnosti `Page` objekt, který chcete získat velké množství informací, jako například:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-462">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="0a5f4-463">`Request`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-463">`Request`.</span></span> <span data-ttu-id="0a5f4-464">Jak už jsme viděli, to je kolekce informací o aktuálním požadavku, včetně, jaký typ prohlížeče požadavek, adresu URL stránky, identita uživatele, atd.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-464">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="0a5f4-465">`Response`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-465">`Response`.</span></span> <span data-ttu-id="0a5f4-466">Toto je kolekce informací o odpovědi (stránky), která se odešle do prohlížeče při kódu serveru bylo dokončeno.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-466">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="0a5f4-467">Například můžete tuto vlastnost při zápisu informací do odpovědi.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-467">For example, you can use this property to write information into the response.</span></span> 

    [!code-cshtml[Main](introducing-razor-syntax-c/samples/sample58.cshtml)]

<a id="ID_CollectionsAndObjects"></a>
### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="0a5f4-468">Kolekce objektů (pole a slovník)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-468">Collection Objects (Arrays and Dictionaries)</span></span>

<span data-ttu-id="0a5f4-469">A *kolekce* je skupina objekty stejného typu, například v kolekci `Customer` objekty z databáze.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-469">A *collection* is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="0a5f4-470">ASP.NET obsahuje mnoho předdefinovaných kolekcí, jako je třeba `Request.Files` kolekce.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-470">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="0a5f4-471">Často budete pracovat dat v kolekcích.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-471">You'll often work with data in collections.</span></span> <span data-ttu-id="0a5f4-472">Jsou dvě běžné typy kolekcí *pole* a *slovník*.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-472">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="0a5f4-473">Pole je užitečné, když chcete ukládat kolekce podobné položky, ale nechcete vytvořit samostatnou proměnnou pro každou položku:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-473">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample59.cshtml)]

<span data-ttu-id="0a5f4-474">S poli, je třeba deklarovat určité datového typu, `string`, `int`, nebo `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-474">With arrays, you declare a specific data type, such as `string`, `int`, or `DateTime`.</span></span> <span data-ttu-id="0a5f4-475">K označení, proměnná může obsahovat pole, přidat závorky deklarace (například `string[]` nebo `int[]`).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-475">To indicate that the variable can contain an array, you add brackets to the declaration (such as `string[]` or `int[]`).</span></span> <span data-ttu-id="0a5f4-476">Dostanete položky v poli pomocí jejich polohu (index) nebo pomocí `foreach` příkaz.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-476">You can access items in an array using their position (index) or by using the `foreach` statement.</span></span> <span data-ttu-id="0a5f4-477">Pole indexy jsou od nuly &#8212; To znamená první položka je v umístění 0, druhý položka je na pozici 1 a tak dále.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-477">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample60.cshtml)]

<span data-ttu-id="0a5f4-478">Můžete určit počet položek v matici získáním jeho `Length` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-478">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="0a5f4-479">Pozice konkrétní položku v poli (pro vyhledávání pole), použijte `Array.IndexOf` metoda.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-479">To get the position of a specific item in the array (to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="0a5f4-480">Můžete také provést třeba zpětného obsah pole ( `Array.Reverse` metoda) nebo řazení obsahu ( `Array.Sort` metoda).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-480">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="0a5f4-481">Výstupní kód pole řetězec, který je zobrazený v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-481">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-c/_static/image13.jpg)

<span data-ttu-id="0a5f4-483">Slovník je kolekce dvojic klíč/hodnota, kde zadáte klíč (nebo název) nastavit nebo načíst s odpovídající hodnotou:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-483">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample61.cshtml)]

<span data-ttu-id="0a5f4-484">Chcete-li vytvořit slovník, je použít `new` – klíčové slovo indikující, že vytváříte nový objekt slovníku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-484">To create a dictionary, you use the `new` keyword to indicate that you're creating a new dictionary object.</span></span> <span data-ttu-id="0a5f4-485">Slovník můžete přiřadit k proměnné pomocí `var` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-485">You can assign a dictionary to a variable using the `var` keyword.</span></span> <span data-ttu-id="0a5f4-486">Určují typy položek ve slovníku pomocí lomené závorky ( `< >` ).</span><span class="sxs-lookup"><span data-stu-id="0a5f4-486">You indicate the data types of the items in the dictionary using angle brackets ( `< >` ).</span></span> <span data-ttu-id="0a5f4-487">Na konci tohoto prohlášení je nutné přidat pár závorky, protože to je ve skutečnosti metoda, která vytvoří do nového slovníku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-487">At the end of the declaration, you must add a pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="0a5f4-488">Chcete-li přidat položky do slovníku, můžete zavolat `Add` metoda slovník proměnné (`myScores` v tomto případě) a pak zadejte klíč a hodnotu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-488">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="0a5f4-489">Alternativně můžete hranaté závorky znamenat klíč a provádět jednoduché přiřazení, jako v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-489">Alternatively, you can use square brackets to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample62.cs)]

<span data-ttu-id="0a5f4-490">Získá hodnotu ze slovníku, zadejte klíč do hranatých závorek:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-490">To get a value from the dictionary, you specify the key in brackets:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample63.cs)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="0a5f4-491">Volání metody s parametry</span><span class="sxs-lookup"><span data-stu-id="0a5f4-491">Calling Methods with Parameters</span></span>

<span data-ttu-id="0a5f4-492">Při čtení dříve v tomto článku, může mít objekty, které můžete naprogramovat s metody.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-492">As you read earlier in this article, the objects that you program with can have methods.</span></span> <span data-ttu-id="0a5f4-493">Například `Database` objekt může mít `Database.Connect` metoda.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-493">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="0a5f4-494">Mnoho způsobů také mít jeden nebo více parametrů.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-494">Many methods also have one or more parameters.</span></span> <span data-ttu-id="0a5f4-495">A *parametr* je hodnota, kterou předáte metodě povolení metody a dokončení úkolu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-495">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="0a5f4-496">Například, podívejte se na deklaraci pro `Request.MapPath` metodu, která přijímá tři parametry:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-496">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-csharp[Main](introducing-razor-syntax-c/samples/sample64.cs)]

<span data-ttu-id="0a5f4-497">(Na řádku byl zalomen aby byl čitelnější.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-497">(The line has been wrapped to make it more readable.</span></span> <span data-ttu-id="0a5f4-498">Mějte na paměti, které můžete vložit konce řádků téměř žádné umístit s výjimkou uvnitř řetězců, které jsou uzavřené v uvozovkách.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-498">Remember that you can put line breaks almost any place except inside strings that are enclosed in quotation marks.)</span></span>

<span data-ttu-id="0a5f4-499">Tato metoda vrátí fyzickou cestu na serveru, který odpovídá do zadané virtuální cesty.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-499">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="0a5f4-500">Jsou tři parametry pro metodu `virtualPath`, `baseVirtualDir`, a `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-500">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="0a5f4-501">(Všimněte si, že v deklaraci, jsou parametry uvedené s datovými typy dat, která budete přijmou.) Když zavoláte tuto metodu, je nutné zadat hodnoty pro všechny tři parametry.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-501">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="0a5f4-502">Syntaxe Razor nabízí dvě možnosti pro předávání parametrů pro metodu: *poziční parametry* a *pojmenované parametry*.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-502">The Razor syntax gives you two options for passing parameters to a method: *positional parameters* and *named parameters*.</span></span> <span data-ttu-id="0a5f4-503">Pro volání metody pomocí poziční parametry, předáte parametry striktní pořadí, které je zadaný v deklaraci metoda.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-503">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="0a5f4-504">(By obvykle víte, toto pořadí načtením dokumentace pro metodu.) Je třeba postupovat podle pořadí, a přeskočíte nelze některý z parametrů &#8212; Pokud potřeby předáte prázdný řetězec (`""`) nebo `null` pro poziční nemáte hodnotu pro parametr.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-504">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or `null` for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="0a5f4-505">Následující příklad předpokládá, že máte složku s názvem *skripty* na vašem webu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-505">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="0a5f4-506">Volání kódu `Request.MapPath` metoda a předává hodnoty pro tři parametry ve správném pořadí.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-506">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="0a5f4-507">Pak zobrazí výsledná namapované cesta.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-507">It then displays the resulting mapped path.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample65.cshtml)]

<span data-ttu-id="0a5f4-508">Když metoda má mnoho parametrů, můžete ponechat kódu srozumitelnější pomocí pojmenované parametry.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-508">When a method has many parameters, you can keep your code more readable by using named parameters.</span></span> <span data-ttu-id="0a5f4-509">Chcete-li zavolat metodu pomocí pojmenované parametry, zadáte název parametru následovaný dvojtečkou (:) a hodnotu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-509">To call a method using named parameters, you specify the parameter name followed by a colon (:), and then the value.</span></span> <span data-ttu-id="0a5f4-510">Výhodou pojmenované parametry je, že můžete je předat v libovolném pořadí, které chcete.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-510">The advantage of named parameters is that you can pass them in any order you want.</span></span> <span data-ttu-id="0a5f4-511">(Nevýhodou je, že volání metody není jako compact.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-511">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="0a5f4-512">V následujícím příkladu volání stejnou metodu, jak je uvedeno výše, ale používá pojmenované parametry k zadání hodnoty:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-512">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample66.cshtml)]

<span data-ttu-id="0a5f4-513">Jak je vidět parametry se jí předávají v jiném pořadí.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-513">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="0a5f4-514">Pokud spustíte předchozí příklad a v tomto příkladu, budete však vrátit stejnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-514">However, if you run the previous example and this example, they'll return the same value.</span></span>

<a id="ID_HandlingErrors"></a>
## <a name="handling-errors"></a><span data-ttu-id="0a5f4-515">Zpracování chyb</span><span class="sxs-lookup"><span data-stu-id="0a5f4-515">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="0a5f4-516">Try-Catch – příkazy</span><span class="sxs-lookup"><span data-stu-id="0a5f4-516">Try-Catch Statements</span></span>

<span data-ttu-id="0a5f4-517">Příkazy často budete mít ve svém kódu, který může selhat z důvodů mimo vlastního ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-517">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="0a5f4-518">Příklad:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-518">For example:</span></span>

- <span data-ttu-id="0a5f4-519">Pokud váš kód se pokusí vytvořit nebo získat přístup souboru, může dojít k nejrůznějším chyb.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-519">If your code tries to create or access a file, all sorts of errors might occur.</span></span> <span data-ttu-id="0a5f4-520">Požadovaný soubor pravděpodobně neexistuje, může být zablokován, kód nemusí mít oprávnění a tak dále.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-520">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="0a5f4-521">Podobně pokud kód pokusí k aktualizaci záznamů v databázi, může být problémy s oprávněními, může dojít ke ztrátě připojení k databázi, data uložit může být neplatný a tak dále.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-521">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="0a5f4-522">Programovací podmínky, se nazývají těchto situacích *výjimky*.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-522">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="0a5f4-523">Pokud váš kód zjistí výjimku, generuje (vyvolává) chybovou zprávu to je v nejlépe obtěžování uživatelům:</span><span class="sxs-lookup"><span data-stu-id="0a5f4-523">If your code encounters an exception, it generates (throws) an error message that's, at best, annoying to users:</span></span>

![Razor-Img14](introducing-razor-syntax-c/_static/image14.jpg)

<span data-ttu-id="0a5f4-525">V situacích, kde kódu setkat výjimky a aby se zabránilo chybové zprávy tohoto typu, můžete použít `try/catch` příkazy.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-525">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `try/catch` statements.</span></span> <span data-ttu-id="0a5f4-526">V `try` prohlášení, můžete spustit kód, který při kontrole.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-526">In the `try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="0a5f4-527">V jedné nebo více `catch` příkazy, můžete vyhledat konkrétní chyby (konkrétní typy výjimek), jež mohly nastat.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-527">In one or more `catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="0a5f4-528">Můžete zahrnout tolik `catch` příkazy, jako je třeba vyhledejte chyby, které se očekává.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-528">You can include as many `catch` statements as you need to look for errors that you are anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="0a5f4-529">Doporučujeme, abyste neměli používat `Response.Redirect` metoda v `try/catch` příkazy, protože ho může způsobit výjimku v stránku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-529">We recommend that you avoid using the `Response.Redirect` method in `try/catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="0a5f4-530">Následující příklad ukazuje stránky, který vytvoří textový soubor na první požadavek a potom zobrazí tlačítko, které umožňuje uživateli otevřít soubor.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-530">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="0a5f4-531">V příkladu úmyslně použije chybný název souboru, takže způsobí výjimku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-531">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="0a5f4-532">Tento kód obsahuje `catch` příkazy pro dvě možné výjimky: `FileNotFoundException`, která nastane, pokud je špatná, název souboru a `DirectoryNotFoundException`, který v případě ASP.NET i nelze najít složku.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-532">The code includes `catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="0a5f4-533">(Chcete-li zobrazit, jak se spustí při všechno funguje správně můžete Odkomentujte příkaz v příkladu.)</span><span class="sxs-lookup"><span data-stu-id="0a5f4-533">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="0a5f4-534">Pokud váš kód nebyl zpracovat výjimku, zobrazí se chybová stránka jako předchozí snímek obrazovky.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-534">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="0a5f4-535">Ale `try/catch` části pomáhá zabránit uživateli v zobrazení tyto typy chyb.</span><span class="sxs-lookup"><span data-stu-id="0a5f4-535">However, the `try/catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-c/samples/sample67.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="0a5f4-536">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="0a5f4-536">Additional Resources</span></span>

<span data-ttu-id="0a5f4-537">**Programování v jazyce Visual Basic**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-537">**Programming with Visual Basic**</span></span>


[<span data-ttu-id="0a5f4-538">Dodatek: jazyk Visual Basic a syntaxe</span><span class="sxs-lookup"><span data-stu-id="0a5f4-538">Appendix: Visual Basic Language and Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkId=202908)


<span data-ttu-id="0a5f4-539">**Referenční dokumentace**</span><span class="sxs-lookup"><span data-stu-id="0a5f4-539">**Reference Documentation**</span></span>


[<span data-ttu-id="0a5f4-540">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="0a5f4-540">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)

[<span data-ttu-id="0a5f4-541">Jazyk C#</span><span class="sxs-lookup"><span data-stu-id="0a5f4-541">C# Language</span></span>](https://msdn.microsoft.com/library/kx37x362.aspx)
