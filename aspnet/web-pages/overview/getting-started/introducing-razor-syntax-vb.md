---
uid: web-pages/overview/getting-started/introducing-razor-syntax-vb
title: "Úvod do rozhraní ASP.NET Web programování pomocí syntaxe Razor (Visual Basic) | Microsoft Docs"
author: tfitzmac
description: "Tento dodatek poskytuje přehled programování s webovými stránkami ASP.NET v jazyce Visual Basic pomocí syntaxe Razor."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/07/2014
ms.topic: article
ms.assetid: 5da59646-e973-41cd-88a9-c6b2c0594027
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-razor-syntax-vb
msc.type: authoredcontent
ms.openlocfilehash: 8f5d223a5944d8adb9fe65c89e87829d18d1c7ee
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/24/2018
---
<a name="introduction-to-aspnet-web-programming-using-the-razor-syntax-visual-basic"></a><span data-ttu-id="01725-103">Úvod do rozhraní ASP.NET Web programování pomocí syntaxe Razor (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="01725-103">Introduction to ASP.NET Web Programming Using the Razor Syntax (Visual Basic)</span></span>
====================
<span data-ttu-id="01725-104">podle [tní FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="01725-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="01725-105">Tento článek poskytuje přehled programování pomocí webových stránek ASP.NET pomocí syntaxe Razor a Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="01725-105">This article gives you an overview of programming with ASP.NET Web Pages using the Razor syntax and Visual Basic.</span></span> <span data-ttu-id="01725-106">ASP.NET je technologie společnosti Microsoft pro spouštění dynamické webové stránky na webové servery.</span><span class="sxs-lookup"><span data-stu-id="01725-106">ASP.NET is Microsoft's technology for running dynamic web pages on web servers.</span></span>
> 
> <span data-ttu-id="01725-107">**Co se dozvíte**:</span><span class="sxs-lookup"><span data-stu-id="01725-107">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="01725-108">Horní 8 Programovací tipy pro Začínáme s programováním pomocí syntaxe Razor rozhraní ASP.NET Web Pages.</span><span class="sxs-lookup"><span data-stu-id="01725-108">The top 8 programming tips for getting started with programming ASP.NET Web Pages using Razor syntax.</span></span>
> - <span data-ttu-id="01725-109">Základní koncepty programování, které budete potřebovat.</span><span class="sxs-lookup"><span data-stu-id="01725-109">Basic programming concepts you'll need.</span></span>
> - <span data-ttu-id="01725-110">Jaké kódu serveru ASP.NET a syntaxe Razor je všechno o.</span><span class="sxs-lookup"><span data-stu-id="01725-110">What ASP.NET server code and the Razor syntax is all about.</span></span>
>   
> 
> ## <a name="software-versions"></a><span data-ttu-id="01725-111">Verze softwaru</span><span class="sxs-lookup"><span data-stu-id="01725-111">Software versions</span></span>
> 
> 
> - <span data-ttu-id="01725-112">Rozhraní ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="01725-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="01725-113">V tomto kurzu taky spolupracuje se službou ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="01725-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>


<span data-ttu-id="01725-114">Většina příkladů použití technologie ASP.NET Web Pages se syntaxí Razor pomocí jazyka C#.</span><span class="sxs-lookup"><span data-stu-id="01725-114">Most examples of using ASP.NET Web Pages with Razor syntax use C#.</span></span> <span data-ttu-id="01725-115">Ale syntaxi Razor podporuje také jazyka Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="01725-115">But the Razor syntax also supports Visual Basic.</span></span> <span data-ttu-id="01725-116">Chcete-li program webovou stránku ASP.NET v jazyce Visual Basic, vytvoříte webovou stránku pomocí *.vbhtml* příponu názvu souboru a poté přidejte kód jazyka Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="01725-116">To program an ASP.NET web page in Visual Basic, you create a web page with a *.vbhtml* filename extension, and then add Visual Basic code.</span></span> <span data-ttu-id="01725-117">Tento článek poskytuje přehled o práci s jazykem Visual Basic a syntaxe k vytvoření webových stránek ASP.NET ve službě.</span><span class="sxs-lookup"><span data-stu-id="01725-117">This article gives you an overview of working with the Visual Basic language and syntax to create ASP.NET Webpages.</span></span>

> [!NOTE]
> <span data-ttu-id="01725-118">Výchozí šablony webové stránky pro nástroj Microsoft WebMatrix (**Pekárna**, **Fotogalerie**, a **Starter Site**atd) jsou k dispozici v C# a Visual Basic verze.</span><span class="sxs-lookup"><span data-stu-id="01725-118">The default website templates for Microsoft WebMatrix (**Bakery**, **Photo Gallery**, and **Starter Site**, etc.) are available in C# and Visual Basic versions.</span></span> <span data-ttu-id="01725-119">Visual Basic šablony podle můžete nainstalovat jako balíčky NuGet.</span><span class="sxs-lookup"><span data-stu-id="01725-119">You can install the Visual Basic templates by as NuGet packages.</span></span> <span data-ttu-id="01725-120">Šablony webu jsou nainstalovány v kořenové složce webu do složky s názvem *Microsoft Templates*.</span><span class="sxs-lookup"><span data-stu-id="01725-120">Website templates are installed in the root folder of your site in a folder named *Microsoft Templates*.</span></span>


## <a name="the-top-8-programming-tips"></a><span data-ttu-id="01725-121">Horní 8 Tipy pro programování</span><span class="sxs-lookup"><span data-stu-id="01725-121">The Top 8 Programming Tips</span></span>

<span data-ttu-id="01725-122">Tato část obsahuje několik tipy, které je nezbytně nutné znát jako počáteční psaní kódu serveru ASP.NET pomocí syntaxe Razor.</span><span class="sxs-lookup"><span data-stu-id="01725-122">This section lists a few tips that you absolutely need to know as you start writing ASP.NET server code using the Razor syntax.</span></span>

### <a name="1-you-add-code-to-a-page-using-the--character"></a><span data-ttu-id="01725-123">1. Přidejte kód na stránku pomocí @ – znak</span><span class="sxs-lookup"><span data-stu-id="01725-123">1. You add code to a page using the @ character</span></span>

<span data-ttu-id="01725-124">`@` Začne znak vložené výrazy, jedním příkazem bloky a bloky vícepříkazové:</span><span class="sxs-lookup"><span data-stu-id="01725-124">The `@` character starts inline expressions, single-statement blocks, and multi-statement blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample1.vbhtml)]

<span data-ttu-id="01725-125">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-125">The result displayed in a browser:</span></span>

![Razor-Img1](introducing-razor-syntax-vb/_static/image1.jpg)

> [!TIP] 
> 
> <span data-ttu-id="01725-127">**HTML Encoding**</span><span class="sxs-lookup"><span data-stu-id="01725-127">**HTML Encoding**</span></span>
> 
> <span data-ttu-id="01725-128">Při zobrazení obsahu stránce pomocí `@` znak, jako v předchozích příkladech ASP.NET umístí kódování HTML výstup.</span><span class="sxs-lookup"><span data-stu-id="01725-128">When you display content in a page using the `@` character, as in the preceding examples, ASP.NET HTML-encodes the output.</span></span> <span data-ttu-id="01725-129">Tím se nahradí vyhrazené znaky HTML (například `<` a `>` a `&`) s kódy, které umožňují znaků, který má být zobrazen jako znaků na webové stránce nebude interpretován jako značky HTML nebo entity.</span><span class="sxs-lookup"><span data-stu-id="01725-129">This replaces reserved HTML characters (such as `<` and `>` and `&`) with codes that enable the characters to be displayed as characters in a web page instead of being interpreted as HTML tags or entities.</span></span> <span data-ttu-id="01725-130">Bez kódování HTML výstup z vašeho kódu serveru se nemusí zobrazit správně a mohla vystavit stránky na bezpečnostní rizika.</span><span class="sxs-lookup"><span data-stu-id="01725-130">Without HTML encoding, the output from your server code might not display correctly, and could expose a page to security risks.</span></span>
> 
> <span data-ttu-id="01725-131">Pokud vaším cílem je výstup jazyka HTML, který vykreslí značky jako značky (například `<p></p>` pro odstavec nebo `<em></em>` pro zvýraznění textu), najdete v části [kombinování Text, značek a kódu v bloky kódu](#BM_CombiningTextMarkupAndCode) dále v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="01725-131">If your goal is to output HTML markup that renders tags as markup (for example `<p></p>` for a paragraph or `<em></em>` to emphasize text), see the section [Combining Text, Markup, and Code in Code Blocks](#BM_CombiningTextMarkupAndCode) later in this article.</span></span>
> 
> <span data-ttu-id="01725-132">Další informace o kódování HTML v [práci s formuláři HTML v lokalitách webové stránky ASP.NET](https://go.microsoft.com/fwlink/?LinkId=202892).</span><span class="sxs-lookup"><span data-stu-id="01725-132">You can read more about HTML encoding in [Working with HTML Forms in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202892).</span></span>


### <a name="2-you-enclose-code-blocks-with-codeend-code"></a><span data-ttu-id="01725-133">2. Je-li uzavřít bloky kódu s kódem... End kódu</span><span class="sxs-lookup"><span data-stu-id="01725-133">2. You enclose code blocks with Code...End Code</span></span>

<span data-ttu-id="01725-134">Blok kódu obsahuje jeden nebo více příkazů kódu a je spolu s klíčovými slovy `Code` a `End Code`.</span><span class="sxs-lookup"><span data-stu-id="01725-134">A code block includes one or more code statements and is enclosed with the keywords `Code` and `End Code`.</span></span> <span data-ttu-id="01725-135">Umístěte otevření `Code` – klíčové slovo ihned po `@` znak &#8212; mezi nimi nemůže být prázdné.</span><span class="sxs-lookup"><span data-stu-id="01725-135">Place the opening `Code` keyword immediately after the `@` character &#8212; there can't be whitespace between them.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample2.vbhtml)]

<span data-ttu-id="01725-136">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-136">The result displayed in a browser:</span></span>

![Razor-Img2](introducing-razor-syntax-vb/_static/image2.jpg)

### <a name="3-inside-a-block-you-end-each-code-statement-with-a-line-break"></a><span data-ttu-id="01725-138">3. Uvnitř bloku vám stát, že každý příkaz kódu s konec řádku</span><span class="sxs-lookup"><span data-stu-id="01725-138">3. Inside a block, you end each code statement with a line break</span></span>

<span data-ttu-id="01725-139">V bloku kódu jazyka Visual Basic každý příkaz končí konec řádku.</span><span class="sxs-lookup"><span data-stu-id="01725-139">In a Visual Basic code block, each statement ends with a line break.</span></span> <span data-ttu-id="01725-140">(Dále v článku uvidíte způsob, jak zabalit příkaz dlouho kódu do více řádků v případě potřeby.)</span><span class="sxs-lookup"><span data-stu-id="01725-140">(Later in the article you'll see a way to wrap a long code statement into multiple lines if needed.)</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample3.vbhtml)]

### <a name="4-you-use-variables-to-store-values"></a><span data-ttu-id="01725-141">4. Proměnné se používají k ukládání hodnot</span><span class="sxs-lookup"><span data-stu-id="01725-141">4. You use variables to store values</span></span>

<span data-ttu-id="01725-142">Hodnoty v lze ukládat *proměnná*, včetně řetězce, čísla a kalendářní data, atd. Vytvoření nové proměnné pomocí `Dim` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="01725-142">You can store values in a *variable*, including strings, numbers, and dates, etc. You create a new variable using the `Dim` keyword.</span></span> <span data-ttu-id="01725-143">Hodnoty proměnných můžete vložit přímo do stránky pomocí `@`.</span><span class="sxs-lookup"><span data-stu-id="01725-143">You can insert variable values directly in a page using `@`.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample4.vbhtml)]

<span data-ttu-id="01725-144">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-144">The result displayed in a browser:</span></span>

![Razor-Img3](introducing-razor-syntax-vb/_static/image3.jpg)

### <a name="5-you-enclose-literal-string-values-in-double-quotation-marks"></a><span data-ttu-id="01725-146">5. Uzavřete řetězcového literálu hodnoty v uvozovkách</span><span class="sxs-lookup"><span data-stu-id="01725-146">5. You enclose literal string values in double quotation marks</span></span>

<span data-ttu-id="01725-147">A *řetězec* je posloupnost znaků, které jsou považovány za text.</span><span class="sxs-lookup"><span data-stu-id="01725-147">A *string* is a sequence of characters that are treated as text.</span></span> <span data-ttu-id="01725-148">Pokud chcete zadat řetězec, vložte jej do dvojitých uvozovek:</span><span class="sxs-lookup"><span data-stu-id="01725-148">To specify a string, you enclose it in double quotation marks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample5.vbhtml)]

<span data-ttu-id="01725-149">Pro vložení dvojitých uvozovek nahoře v rámci řetězcovou hodnotu, vložte dva znaky uvozovky.</span><span class="sxs-lookup"><span data-stu-id="01725-149">To embed double quotation marks within a string value, insert two double quotation mark characters.</span></span> <span data-ttu-id="01725-150">Pokud chcete znak dvojitých uvozovek jednou zobrazí ve výstupu stránky, zadejte je jako `""` v rámci uvozovkách řetězec a pokud chcete zobrazit dvakrát, zadejte je jako `""""` v rámci řetězec v uvozovkách.</span><span class="sxs-lookup"><span data-stu-id="01725-150">If you want the double quotation character to appear once in the page output, enter it as `""` within the quoted string, and if you want it to appear twice, enter it as `""""` within the quoted string.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample6.vbhtml)]

<span data-ttu-id="01725-151">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-151">The result displayed in a browser:</span></span>

![Razor Img4](introducing-razor-syntax-vb/_static/image4.jpg)

### <a name="6-visual-basic-code-is-not-case-sensitive"></a><span data-ttu-id="01725-153">6. Kód jazyka Visual Basic není malá a velká písmena</span><span class="sxs-lookup"><span data-stu-id="01725-153">6. Visual Basic code is not case sensitive</span></span>

<span data-ttu-id="01725-154">Jazyk Visual Basic není velká a malá písmena.</span><span class="sxs-lookup"><span data-stu-id="01725-154">The Visual Basic language is not case sensitive.</span></span> <span data-ttu-id="01725-155">Klíčová slova programování (jako je `Dim`, `If`, a `True`) a názvy proměnných (jako je `myString`, nebo `subTotal`) může být napsán v každém případě.</span><span class="sxs-lookup"><span data-stu-id="01725-155">Programming keywords (like `Dim`, `If`, and `True`) and variable names (like `myString`, or `subTotal`) can be written in any case.</span></span>

<span data-ttu-id="01725-156">Následující řádky kódu přiřadit hodnotu proměnné `lastname` pomocí jedno malé písmeno název a hodnotu proměnné na stránku pomocí velká název ve výstupu.</span><span class="sxs-lookup"><span data-stu-id="01725-156">The following lines of code assign a value to the variable `lastname` using a lowercase name, and then output the variable value to the page using an uppercase name.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample7.vbhtml)]

<span data-ttu-id="01725-157">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-157">The result displayed in a browser:</span></span>

![vb-syntax-5](introducing-razor-syntax-vb/_static/image5.jpg)

### <a name="7-much-of-your-coding-involves-working-with-objects"></a><span data-ttu-id="01725-159">7. Velká část kódování zahrnuje pracovat s objekty</span><span class="sxs-lookup"><span data-stu-id="01725-159">7. Much of your coding involves working with objects</span></span>

<span data-ttu-id="01725-160">Představuje objekt věcí, kterou můžete programu s &#8212; na stránce textové pole, soubor, bitovou kopii, webové žádosti, e-mailovou zprávu, záznam zákazníka (řádku databáze), atd. Objekty mají vlastnosti, které popisují jejich charakteristikami &#8212; má objekt textové pole `Text` vlastnost, má objekt žádosti `Url` vlastnost, má e-mailovou zprávu `From` vlastnost a objekt zákazník má `FirstName` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="01725-160">An object represents a thing that you can program with &#8212; a page, a text box, a file, an image, a web request, an email message, a customer record (database row), etc. Objects have properties that describe their characteristics &#8212; a text box object has a `Text` property, a request object has a `Url` property, an email message has a `From` property, and a customer object has a `FirstName` property.</span></span> <span data-ttu-id="01725-161">Objekty mají metody, které jsou také &quot;příkazy&quot; mohou provádět.</span><span class="sxs-lookup"><span data-stu-id="01725-161">Objects also have methods that are the &quot;verbs&quot; they can perform.</span></span> <span data-ttu-id="01725-162">Mezi příklady patří objektu soubor `Save` metoda, objekt image `Rotate` metoda a objekt e-mailu `Send` metoda.</span><span class="sxs-lookup"><span data-stu-id="01725-162">Examples include a file object's `Save` method, an image object's `Rotate` method, and an email object's `Send` method.</span></span>

<span data-ttu-id="01725-163">Často budete pracovat `Request` objekt, který nabízí informace jako hodnoty formuláře polí na stránce (textová pole, atd.), jaký typ prohlížeče požadavek, adresu URL stránky, identita uživatele, atd. Tento příklad ukazuje, jak pro přístup k vlastnostem `Request` objekt a jak volat `MapPath` metodu `Request` objekt, který vám dává absolutní cesta ke stránce na serveru:</span><span class="sxs-lookup"><span data-stu-id="01725-163">You'll often work with the `Request` object, which gives you information like the values of form fields on the page (text boxes, etc.), what type of browser made the request, the URL of the page, the user identity, etc. This example shows how to access properties of the `Request` object and how to call the `MapPath` method of the `Request` object, which gives you the absolute path of the page on the server:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample8.html)]

<span data-ttu-id="01725-164">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-164">The result displayed in a browser:</span></span>

![Razor-Img5](introducing-razor-syntax-vb/_static/image6.jpg)

### <a name="8-you-can-write-code-that-makes-decisions"></a><span data-ttu-id="01725-166">8. Můžete napsat kód, který uskutečňuje rozhodnutí</span><span class="sxs-lookup"><span data-stu-id="01725-166">8. You can write code that makes decisions</span></span>

<span data-ttu-id="01725-167">Klíčovou funkcí dynamické webové stránky je, že můžete určit co dělat na základě podmínky.</span><span class="sxs-lookup"><span data-stu-id="01725-167">A key feature of dynamic web pages is that you can determine what to do based on conditions.</span></span> <span data-ttu-id="01725-168">Nejběžnější způsob k tomu je s `If` – příkaz (a volitelné `Else` příkaz).</span><span class="sxs-lookup"><span data-stu-id="01725-168">The most common way to do this is with the `If` statement (and optional `Else` statement).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample9.vbhtml)]

<span data-ttu-id="01725-169">Příkaz `If IsPost` je zjednodušený způsob zápis `If IsPost = True`.</span><span class="sxs-lookup"><span data-stu-id="01725-169">The statement `If IsPost` is a shorthand way of writing `If IsPost = True`.</span></span> <span data-ttu-id="01725-170">Spolu s `If` příkazy, existuje mnoho různých způsobů, jak testovacích podmínkách, opakování bloky kódu, a tak dále, které jsou popsány dále v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="01725-170">Along with `If` statements, there are a variety of ways to test conditions, repeat blocks of code, and so on, which are described later in this article.</span></span>

<span data-ttu-id="01725-171">Výsledek zobrazí v prohlížeči (po kliknutí na **odeslání**):</span><span class="sxs-lookup"><span data-stu-id="01725-171">The result displayed in a browser (after clicking **Submit**):</span></span>

![Razor Img6](introducing-razor-syntax-vb/_static/image7.jpg)

> [!TIP] 
> 
> <span data-ttu-id="01725-173">**HTTP GET a POST metody a vlastnosti IsPost**</span><span class="sxs-lookup"><span data-stu-id="01725-173">**HTTP GET and POST Methods and the IsPost Property**</span></span>
> 
> <span data-ttu-id="01725-174">Protokol použitý pro webové stránky (HTTP) podporuje velmi omezený počet metod (&quot;příkazy&quot;) používané provádět požadavky na server.</span><span class="sxs-lookup"><span data-stu-id="01725-174">The protocol used for web pages (HTTP) supports a very limited number of methods (&quot;verbs&quot;) that are used to make requests to the server.</span></span> <span data-ttu-id="01725-175">Dva nejběžnější ty, které jsou GET, které slouží k načtení stránky, a POST, který se používá k odeslání stránky.</span><span class="sxs-lookup"><span data-stu-id="01725-175">The two most common ones are GET, which is used to read a page, and POST, which is used to submit a page.</span></span> <span data-ttu-id="01725-176">Obecně platí při prvním požadavku na stránce požadavku na stránku pomocí GET.</span><span class="sxs-lookup"><span data-stu-id="01725-176">In general, the first time a user requests a page, the page is requested using GET.</span></span> <span data-ttu-id="01725-177">Pokud uživatel doplní do formuláře a poté klikne na tlačítko **odeslání**, prohlížeč provede požadavek POST na server.</span><span class="sxs-lookup"><span data-stu-id="01725-177">If the user fills in a form and then clicks **Submit**, the browser makes a POST request to the server.</span></span>
> 
> <span data-ttu-id="01725-178">Ve webové programování je často užitečné vědět, jestli stránky je vyžadován jako GET nebo POST, abyste věděli, jak pro zpracování stránky.</span><span class="sxs-lookup"><span data-stu-id="01725-178">In web programming, it's often useful to know whether a page is being requested as a GET or as a POST so that you know how to process the page.</span></span> <span data-ttu-id="01725-179">Na webových stránkách ASP.NET, můžete použít `IsPost` vlastnosti chcete zobrazit, zda je požadavek GET nebo POST.</span><span class="sxs-lookup"><span data-stu-id="01725-179">In ASP.NET Web Pages, you can use the `IsPost` property to see whether a request is a GET or a POST.</span></span> <span data-ttu-id="01725-180">Pokud je požadavek POST, `IsPost` vlastnost vrátí hodnotu true, a můžete provést třeba číst hodnoty textová pole ve formuláři.</span><span class="sxs-lookup"><span data-stu-id="01725-180">If the request is a POST, the `IsPost` property will return true, and you can do things like read the values of text boxes on a form.</span></span> <span data-ttu-id="01725-181">Uvidíte mnoho příklady ukazují, jak zpracovat stránce odlišně v závislosti na hodnotě `IsPost`.</span><span class="sxs-lookup"><span data-stu-id="01725-181">Many examples you'll see show you how to process the page differently depending on the value of `IsPost`.</span></span>


## <a name="a-simple-code-example"></a><span data-ttu-id="01725-182">Jednoduchým příkladem kódu</span><span class="sxs-lookup"><span data-stu-id="01725-182">A Simple Code Example</span></span>

<span data-ttu-id="01725-183">Tento postup ukazuje, jak vytvořit stránku, která ukazuje základní programovací techniky.</span><span class="sxs-lookup"><span data-stu-id="01725-183">This procedure shows you how to create a page that illustrates basic programming techniques.</span></span> <span data-ttu-id="01725-184">V příkladu vytvoříte stránky, který umožňuje uživatelům zadejte dvě čísla, potom se přidají a zobrazuje výsledek.</span><span class="sxs-lookup"><span data-stu-id="01725-184">In the example, you create a page that lets users enter two numbers, then it adds them and displays the result.</span></span>

1. <span data-ttu-id="01725-185">V editoru, vytvořte nový soubor s názvem *AddNumbers.vbhtml*.</span><span class="sxs-lookup"><span data-stu-id="01725-185">In your editor, create a new file and name it *AddNumbers.vbhtml*.</span></span>
2. <span data-ttu-id="01725-186">Zkopírujte následující kód a kód na stránku nahraďte nic již na stránce.</span><span class="sxs-lookup"><span data-stu-id="01725-186">Copy the following code and markup into the page, replacing anything already in the page.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample10.vbhtml)]

    <span data-ttu-id="01725-187">Zde jsou některé kroky, abyste Všimněte si:</span><span class="sxs-lookup"><span data-stu-id="01725-187">Here are some things for you to note:</span></span>

    - <span data-ttu-id="01725-188">`@` Znak spustí první blok kódu na stránce, a předchází `totalMessage` proměnná vložených v dolní.</span><span class="sxs-lookup"><span data-stu-id="01725-188">The `@` character starts the first block of code in the page, and it precedes the `totalMessage` variable embedded near the bottom.</span></span>
    - <span data-ttu-id="01725-189">Blok v horní části stránky je uzavřen v `Code...End Code`.</span><span class="sxs-lookup"><span data-stu-id="01725-189">The block at the top of the page is enclosed in `Code...End Code`.</span></span>
    - <span data-ttu-id="01725-190">Proměnné `total`, `num1`, `num2`, a `totalMessage` uložit několik čísla a řetězec.</span><span class="sxs-lookup"><span data-stu-id="01725-190">The variables `total`, `num1`, `num2`, and `totalMessage` store several numbers and a string.</span></span>
    - <span data-ttu-id="01725-191">Řetězcový literál hodnota přiřazená `totalMessage` proměnná je v uvozovkách.</span><span class="sxs-lookup"><span data-stu-id="01725-191">The literal string value assigned to the `totalMessage` variable is in double quotation marks.</span></span>
    - <span data-ttu-id="01725-192">Protože kód jazyka Visual Basic není velká a malá písmena, když `totalMessage` proměnná se používá v dolní části stránky, jeho název pouze musí odpovídat pravopis deklarace proměnné v horní části stránky.</span><span class="sxs-lookup"><span data-stu-id="01725-192">Because Visual Basic code is not case sensitive, when the `totalMessage` variable is used near the bottom of the page, its name only needs to match the spelling of the variable declaration at the top of the page.</span></span> <span data-ttu-id="01725-193">Jedno, malá a velká písmena.</span><span class="sxs-lookup"><span data-stu-id="01725-193">The casing doesn't matter.</span></span>
    - <span data-ttu-id="01725-194">Výraz `num1.AsInt()`  +  `num2.AsInt()` ukazuje, jak pracovat s objekty a metody.</span><span class="sxs-lookup"><span data-stu-id="01725-194">The expression `num1.AsInt()` + `num2.AsInt()` shows how to work with objects and methods.</span></span> <span data-ttu-id="01725-195">`AsInt` Metodu na každou proměnnou převede řetězec zadaný uživatelem na celé číslo (integer), který lze přidat.</span><span class="sxs-lookup"><span data-stu-id="01725-195">The `AsInt` method on each variable converts the string entered by a user to a whole number (an integer) that can be added.</span></span>
    - <span data-ttu-id="01725-196">`<form>` Zahrnuje značky `method="post"` atribut.</span><span class="sxs-lookup"><span data-stu-id="01725-196">The `<form>` tag includes a `method="post"` attribute.</span></span> <span data-ttu-id="01725-197">To určuje, že když uživatel klikne **přidat**, stránky odešle na server pomocí metody POST protokolu HTTP.</span><span class="sxs-lookup"><span data-stu-id="01725-197">This specifies that when the user clicks **Add**, the page will be sent to the server using the HTTP POST method.</span></span> <span data-ttu-id="01725-198">Při odeslání stránky, kód `If IsPost` vyhodnocena jako true a podmínku kód běží, zobrazení výsledkem přidání čísla.</span><span class="sxs-lookup"><span data-stu-id="01725-198">When the page is submitted, the code `If IsPost` evaluates to true and the conditional code runs, displaying the result of adding the numbers.</span></span>
3. <span data-ttu-id="01725-199">Uložit stránky a spusťte ji v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="01725-199">Save the page and run it in a browser.</span></span> <span data-ttu-id="01725-200">(Ujistěte se, že je vybraný stránky v **soubory** pracovního prostoru, než ji spustit.) Zadejte dvě celá čísla a poté klikněte **přidat** tlačítko.</span><span class="sxs-lookup"><span data-stu-id="01725-200">(Make sure the page is selected in the **Files** workspace before you run it.) Enter two whole numbers and then click the **Add** button.</span></span>

    ![Razor-Img7](introducing-razor-syntax-vb/_static/image8.jpg)

## <a name="visual-basic-language-and-syntax"></a><span data-ttu-id="01725-202">Jazyk Visual Basic a syntaxe</span><span class="sxs-lookup"><span data-stu-id="01725-202">Visual Basic Language and Syntax</span></span>

<span data-ttu-id="01725-203">Dříve jste viděli základních příkladů jak vytvořit webovou stránku ASP.NET a přidání serverový kód pro kód HTML.</span><span class="sxs-lookup"><span data-stu-id="01725-203">Earlier you saw a basic example of how to create an ASP.NET web page, and how you can add server code to HTML markup.</span></span> <span data-ttu-id="01725-204">Zde se dozvíte základní informace o použití jazyka Visual Basic pro psaní kódu serveru ASP.NET pomocí syntaxe Razor &#8212; To znamená, programovací jazyk pravidla.</span><span class="sxs-lookup"><span data-stu-id="01725-204">Here you'll learn the basics of using Visual Basic to write ASP.NET server code using the Razor syntax &#8212; that is, the programming language rules.</span></span>

<span data-ttu-id="01725-205">Pokud máte zkušenosti s programováním (obzvláště pokud jste použili C, C++, C#, Visual Basic nebo JavaScript), většinu si přečíst zde bude seznámit.</span><span class="sxs-lookup"><span data-stu-id="01725-205">If you're experienced with programming (especially if you've used C, C++, C#, Visual Basic, or JavaScript), much of what you read here will be familiar.</span></span> <span data-ttu-id="01725-206">Pravděpodobně budete muset Seznamte se jenom s jak služba WebMatrix kód je přidán do značek v *.vbhtml* soubory.</span><span class="sxs-lookup"><span data-stu-id="01725-206">You'll probably need to familiarize yourself only with how WebMatrix code is added to markup in *.vbhtml* files.</span></span>

### <a id="BM_CombiningTextMarkupAndCode"></a><span data-ttu-id="01725-207">Kombinování text, značek a kódu v bloky kódu</span><span class="sxs-lookup"><span data-stu-id="01725-207">Combining text, markup, and code in code blocks</span></span>

<span data-ttu-id="01725-208">V blocích kód serveru budete často chtít výstup text a značku na stránku.</span><span class="sxs-lookup"><span data-stu-id="01725-208">In server code blocks, you'll often want to output text and markup to the page.</span></span> <span data-ttu-id="01725-209">Pokud blok serverového kódu obsahuje text, který není kódu a který místo toho má být vykreslen jako je, musí být schopen rozlišit tento text z kódu ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="01725-209">If a server code block contains text that's not code and that instead should be rendered as is, ASP.NET needs to be able to distinguish that text from code.</span></span> <span data-ttu-id="01725-210">Chcete-li to provést několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="01725-210">There are several ways to do this.</span></span>

- <span data-ttu-id="01725-211">Uzavřete text v elementu HTML bloku jako `<p></p>` nebo `<em></em>`:</span><span class="sxs-lookup"><span data-stu-id="01725-211">Enclose the text in an HTML block element like `<p></p>` or `<em></em>`:</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample11.vbhtml)]

    <span data-ttu-id="01725-212">Prvek HTML mohou zahrnovat text, další prvky HTML a výrazy kódu serveru.</span><span class="sxs-lookup"><span data-stu-id="01725-212">The HTML element can include text, additional HTML elements, and server-code expressions.</span></span> <span data-ttu-id="01725-213">Když ASP.NET uvidí otevírání značky HTML (například `<p>`), vykreslí všechno elementu a jeho obsahu, jako je prohlížeče (a překládá výrazy kódu serveru).</span><span class="sxs-lookup"><span data-stu-id="01725-213">When ASP.NET sees the opening HTML tag (for example, `<p>`), it renders everything the element and its content as is to the browser (and resolves the server-code expressions).</span></span>

- <span data-ttu-id="01725-214">Použití `@:` operátor nebo `<text>` elementu.</span><span class="sxs-lookup"><span data-stu-id="01725-214">Use the `@:` operator or the `<text>` element.</span></span> <span data-ttu-id="01725-215">`@:` Výstupy jeden řádek obsahující prostý text nebo neodpovídající značky HTML; obsahu `<text>` element uzavře více řádků na výstup.</span><span class="sxs-lookup"><span data-stu-id="01725-215">The `@:` outputs a single line of content containing plain text or unmatched HTML tags; the `<text>` element encloses multiple lines to output.</span></span> <span data-ttu-id="01725-216">Tyto možnosti jsou užitečné, když nechcete, aby k vykreslení elementu HTML v rámci výstupu.</span><span class="sxs-lookup"><span data-stu-id="01725-216">These options are useful when you don't want to render an HTML element as part of the output.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample12.vbhtml)]

    <span data-ttu-id="01725-217">Následující příklad opakuje předchozí příklad, ale používá jednu dvojici `<text>` značek k uzavřete text k vykreslení.</span><span class="sxs-lookup"><span data-stu-id="01725-217">The following example repeats the previous example but uses a single pair of `<text>` tags to enclose the text to render.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample13.vbhtml)]

    <span data-ttu-id="01725-218">V následujícím příkladu `<text>` a `</text>` značky uzavřete tři řádky, které mají všechny některé uncontained text a neodpovídající značky HTML (`<br />`), spolu s kódu serveru a odpovídající značky HTML.</span><span class="sxs-lookup"><span data-stu-id="01725-218">In the following example, the `<text>` and `</text>` tags enclose three lines, all of which have some uncontained text and unmatched HTML tags (`<br />`), along with server code and matched HTML tags.</span></span> <span data-ttu-id="01725-219">Znovu, může také předcházet každý řádek jednotlivě s `@:` operátor; buď způsob, jak funguje.</span><span class="sxs-lookup"><span data-stu-id="01725-219">Again, you could also precede each line individually with the `@:` operator; either way works.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample14.vbhtml)]

    > [!NOTE]
    > <span data-ttu-id="01725-220">Pokud výstupní text jak je znázorněno v této části &#8212; pomocí prvku HTML `@:` operátor, nebo `<text>` element &#8212; Technologie ASP.NET není kódování HTML výstup.</span><span class="sxs-lookup"><span data-stu-id="01725-220">When you output text as shown in this section &#8212; using an HTML element, the `@:` operator, or the `<text>` element &#8212; ASP.NET doesn't HTML-encode the output.</span></span> <span data-ttu-id="01725-221">(Jak je uvedeno výše, ASP.NET kódování výstup výrazy kódu serveru a bloky kódu serveru, které jsou označeny `@`, s výjimkou ve zvláštních případech uvedených v této části.)</span><span class="sxs-lookup"><span data-stu-id="01725-221">(As noted earlier, ASP.NET does encode the output of server code expressions and server code blocks that are preceded by `@`, except in the special cases noted in this section.)</span></span>

### <a name="whitespace"></a><span data-ttu-id="01725-222">Whitespace</span><span class="sxs-lookup"><span data-stu-id="01725-222">Whitespace</span></span>

<span data-ttu-id="01725-223">Mezery v příkazu (a mimo řetězcový literál) není ovlivňují příkaz:</span><span class="sxs-lookup"><span data-stu-id="01725-223">Extra spaces in a statement (and outside of a string literal) don't affect the statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample15.vbhtml)]

### <a name="breaking-long-statements-into-multiple-lines"></a><span data-ttu-id="01725-224">Rozdělení dlouhých příkazů do více řádků</span><span class="sxs-lookup"><span data-stu-id="01725-224">Breaking long statements into multiple lines</span></span>

<span data-ttu-id="01725-225">Příkaz dlouho kódu můžete rozdělit na více řádků pomocí znak podtržítka `_` (v jazyce Visual Basic tzv. *znak pro pokračování*) po každém řádku kódu.</span><span class="sxs-lookup"><span data-stu-id="01725-225">You can break a long code statement into multiple lines by using the underscore character `_` (which in Visual Basic is called the *continuation character*) after each line of code.</span></span> <span data-ttu-id="01725-226">Pokud chcete rozdělit příkaz na další řádek, na konci řádku přidejte mezeru a poté znak pro pokračování.</span><span class="sxs-lookup"><span data-stu-id="01725-226">To break a statement onto the next line, at the end of the line add a space and then the continuation character.</span></span> <span data-ttu-id="01725-227">Příkaz pokračujte na další řádek.</span><span class="sxs-lookup"><span data-stu-id="01725-227">Continue the statement on the next line.</span></span> <span data-ttu-id="01725-228">Může obtékat příkazy na počet řádků, je potřeba pro zlepšení čitelnosti.</span><span class="sxs-lookup"><span data-stu-id="01725-228">You can wrap statements onto as many lines as you need to improve readability.</span></span> <span data-ttu-id="01725-229">Následující příkazy jsou stejné:</span><span class="sxs-lookup"><span data-stu-id="01725-229">The following statements are the same:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample16.vbhtml)]

<span data-ttu-id="01725-230">Však nelze obalit řádku uprostřed řetězcový literál.</span><span class="sxs-lookup"><span data-stu-id="01725-230">However, you can't wrap a line in the middle of a string literal.</span></span> <span data-ttu-id="01725-231">Předchozí postup nebude fungovat v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="01725-231">The following example doesn't work:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample17.vbhtml)]

<span data-ttu-id="01725-232">Kombinování dlouhý řetězec, který zabalí do více řádků jako výše uvedený kód, museli byste používat *operátor řetězení* (`&`), který se zobrazí později v tomto článku.</span><span class="sxs-lookup"><span data-stu-id="01725-232">To combine a long string that wraps to multiple lines like the above code, you would need to use the *concatenation operator* (`&`), which you'll see later in this article.</span></span>

### <a name="code-comments"></a><span data-ttu-id="01725-233">Komentáře kódu</span><span class="sxs-lookup"><span data-stu-id="01725-233">Code comments</span></span>

<span data-ttu-id="01725-234">Komentáře umožňují nechte poznámky pro sebe nebo ostatní.</span><span class="sxs-lookup"><span data-stu-id="01725-234">Comments let you leave notes for yourself or others.</span></span> <span data-ttu-id="01725-235">Komentáře syntaxe Razor mají předponu `@*` a končit `*@`.</span><span class="sxs-lookup"><span data-stu-id="01725-235">Razor syntax comments are prefixed with `@*` and end with `*@`.</span></span>

[!code-cshtml[Main](introducing-razor-syntax-vb/samples/sample18.cshtml)]

<span data-ttu-id="01725-236">V rámci bloků kódu komentáře syntaxe Razor můžete použít, nebo můžete použít obyčejnou jazyka Visual Basic komentář znaku, který je v jednoduchých uvozovkách (`'`) předponu na každém řádku.</span><span class="sxs-lookup"><span data-stu-id="01725-236">Within code blocks you can use the Razor syntax comments, or you can use ordinary Visual Basic comment character, which is a single quote (`'`) prefixed to each line.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample19.vbhtml)]

## <a name="variables"></a><span data-ttu-id="01725-237">Proměnné</span><span class="sxs-lookup"><span data-stu-id="01725-237">Variables</span></span>

<span data-ttu-id="01725-238">Proměnné je objekt s názvem, který použijete k uložení dat.</span><span class="sxs-lookup"><span data-stu-id="01725-238">A variable is a named object that you use to store data.</span></span> <span data-ttu-id="01725-239">Zadáte název proměnné nic, ale název musí začínat znakem abecedy a nesmí obsahovat prázdné znaky nebo vyhrazené znaky.</span><span class="sxs-lookup"><span data-stu-id="01725-239">You can name variables anything, but the name must begin with an alphabetic character and it cannot contain whitespace or reserved characters.</span></span> <span data-ttu-id="01725-240">V jazyce Visual Basic jak jste viděli dříve, malá a velká písmena v názvu proměnné jedno.</span><span class="sxs-lookup"><span data-stu-id="01725-240">In Visual Basic, as you saw earlier, the case of the letters in a variable name doesn't matter.</span></span>

### <a name="variables-and-data-types"></a><span data-ttu-id="01725-241">Proměnné a datové typy</span><span class="sxs-lookup"><span data-stu-id="01725-241">Variables and data types</span></span>

<span data-ttu-id="01725-242">Proměnná může mít specifické datového typu, který určuje, jaký typ dat je uložené v proměnné.</span><span class="sxs-lookup"><span data-stu-id="01725-242">A variable can have a specific data type, which indicates what kind of data is stored in the variable.</span></span> <span data-ttu-id="01725-243">Můžete mít proměnné řetězce, které ukládají řetězcové hodnoty (například &quot;Hello, world&quot;), celé číslo proměnné, které ukládají celé číslo hodnoty (například 3 nebo 79) a proměnné datum, které ukládání hodnot data v různých formátech (např. 4/12/2012 nebo březnem 2009 ).</span><span class="sxs-lookup"><span data-stu-id="01725-243">You can have string variables that store string values (like &quot;Hello world&quot;), integer variables that store whole-number values (like 3 or 79), and date variables that store date values in a variety of formats (like 4/12/2012 or March 2009).</span></span> <span data-ttu-id="01725-244">A existuje mnoho dalších typů dat, které můžete použít.</span><span class="sxs-lookup"><span data-stu-id="01725-244">And there are many other data types you can use.</span></span>

<span data-ttu-id="01725-245">Nemáte však zadejte typ pro proměnnou.</span><span class="sxs-lookup"><span data-stu-id="01725-245">However, you don't have to specify a type for a variable.</span></span> <span data-ttu-id="01725-246">Ve většině případů ASP.NET můžete zjistit typ závislosti na tom, jak se používá data v proměnné.</span><span class="sxs-lookup"><span data-stu-id="01725-246">In most cases ASP.NET can figure out the type based on how the data in the variable is being used.</span></span> <span data-ttu-id="01725-247">(Někdy nutné zadat typ, uvidíte příklady, pokud je to pravda.)</span><span class="sxs-lookup"><span data-stu-id="01725-247">(Occasionally you must specify a type; you'll see examples where this is true.)</span></span>

<span data-ttu-id="01725-248">Chcete-li deklarovat proměnnou bez zadání typu, použijte `Dim` plus název proměnné (například `Dim myVar`).</span><span class="sxs-lookup"><span data-stu-id="01725-248">To declare a variable without specifying a type, use `Dim` plus the variable name (for instance, `Dim myVar`).</span></span> <span data-ttu-id="01725-249">Chcete-li proměnnou deklarovat s typem, použijte `Dim` plus název proměnné, za nímž následuje `As` a potom název typu (například `Dim myVar As String`).</span><span class="sxs-lookup"><span data-stu-id="01725-249">To declare a variable with a type, use `Dim` plus the variable name, followed by `As` and then the type name (for instance, `Dim myVar As String`).</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample20.vbhtml)]

<span data-ttu-id="01725-250">Následující příklad ukazuje některé vložené výrazy, které používají proměnné na webové stránce.</span><span class="sxs-lookup"><span data-stu-id="01725-250">The following example shows some inline expressions that use the variables in a web page.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample21.vbhtml)]

<span data-ttu-id="01725-251">Výsledek zobrazí v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-251">The result displayed in a browser:</span></span>

![Razor-Img9](introducing-razor-syntax-vb/_static/image9.jpg)

### <a name="converting-and-testing-data-types"></a><span data-ttu-id="01725-253">Převod a testování datové typy</span><span class="sxs-lookup"><span data-stu-id="01725-253">Converting and testing data types</span></span>

<span data-ttu-id="01725-254">I když ASP.NET lze obvykle zjistit datový typ automaticky, někdy ji nelze.</span><span class="sxs-lookup"><span data-stu-id="01725-254">Although ASP.NET can usually determine a data type automatically, sometimes it can't.</span></span> <span data-ttu-id="01725-255">Proto můžete chtít Pomozte ASP.NET tím, že provádění explicitní převod.</span><span class="sxs-lookup"><span data-stu-id="01725-255">Therefore, you might need to help ASP.NET out by performing an explicit conversion.</span></span> <span data-ttu-id="01725-256">I když nemáte pro převod typů, někdy je užitečné zjistěte, jaký typ dat je možné, že pracujete s.</span><span class="sxs-lookup"><span data-stu-id="01725-256">Even if you don't have to convert types, sometimes it's helpful to test to see what type of data you might be working with.</span></span>

<span data-ttu-id="01725-257">Nejběžnější případem je, že budete muset převést řetězec na jiný typ, například na celé číslo nebo datum.</span><span class="sxs-lookup"><span data-stu-id="01725-257">The most common case is that you have to convert a string to another type, such as to an integer or date.</span></span> <span data-ttu-id="01725-258">Následující příklad ukazuje obvyklý případ, kdy je nutné převést řetězec na číslo.</span><span class="sxs-lookup"><span data-stu-id="01725-258">The following example shows a typical case where you must convert a string to a number.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample22.vbhtml)]

<span data-ttu-id="01725-259">Uživatelský vstup se dodává jako pravidlo, pro vás jako řetězce.</span><span class="sxs-lookup"><span data-stu-id="01725-259">As a rule, user input comes to you as strings.</span></span> <span data-ttu-id="01725-260">I když jste zobrazí výzva, aby uživatel zadal číslo a i v případě číslici, že jste zadali při odeslání vstup uživatele a můžete číst v kódu, se data ve formátu řetězce.</span><span class="sxs-lookup"><span data-stu-id="01725-260">Even if you've prompted the user to enter a number, and even if they've entered a digit, when user input is submitted and you read it in code, the data is in string format.</span></span> <span data-ttu-id="01725-261">Proto je nutné převést řetězec na číslo.</span><span class="sxs-lookup"><span data-stu-id="01725-261">Therefore, you must convert the string to a number.</span></span> <span data-ttu-id="01725-262">V příkladu Pokud se pokusíte provést aritmetické na hodnoty bez převodu, k následující chybě výsledky, protože ASP.NET nelze přidat dva řetězce:</span><span class="sxs-lookup"><span data-stu-id="01725-262">In the example, if you try to perform arithmetic on the values without converting them, the following error results, because ASP.NET cannot add two strings:</span></span>

`Cannot implicitly convert type 'string' to 'int'.`

<span data-ttu-id="01725-263">K převodu hodnoty na celá čísla, zavoláte `AsInt` metoda.</span><span class="sxs-lookup"><span data-stu-id="01725-263">To convert the values to integers, you call the `AsInt` method.</span></span> <span data-ttu-id="01725-264">Pokud převod úspěšný, můžete pak přidat čísla.</span><span class="sxs-lookup"><span data-stu-id="01725-264">If the conversion is successful, you can then add the numbers.</span></span>

<span data-ttu-id="01725-265">Následující tabulka uvádí některé běžné převod a testování metody pro proměnné.</span><span class="sxs-lookup"><span data-stu-id="01725-265">The following table lists some common conversion and test methods for variables.</span></span>

| <span data-ttu-id="01725-266">**– Metoda**</span><span class="sxs-lookup"><span data-stu-id="01725-266">**Method**</span></span> | <span data-ttu-id="01725-267">**Popis**</span><span class="sxs-lookup"><span data-stu-id="01725-267">**Description**</span></span> | <span data-ttu-id="01725-268">**Příklad**</span><span class="sxs-lookup"><span data-stu-id="01725-268">**Example**</span></span> |
| --- | --- | --- |
| `AsInt(), IsInt()` | <span data-ttu-id="01725-269">Převede řetězec, který představuje celé číslo (například &quot;593&quot;) na celé číslo.</span><span class="sxs-lookup"><span data-stu-id="01725-269">Converts a string that represents a whole number (like &quot;593&quot;) to an integer.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample23.vb)] |
| `AsBool(), IsBool()` | <span data-ttu-id="01725-270">Převede řetězec jako &quot;true&quot; nebo &quot;false&quot; typu Boolean.</span><span class="sxs-lookup"><span data-stu-id="01725-270">Converts a string like &quot;true&quot; or &quot;false&quot; to a Boolean type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample24.vb)] |
| `AsFloat(), IsFloat()` | <span data-ttu-id="01725-271">Převede řetězec, který má hodnotu decimal jako &quot;1.3&quot; nebo &quot;7.439&quot; na číslo s plovoucí desetinnou čárkou.</span><span class="sxs-lookup"><span data-stu-id="01725-271">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a floating-point number.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample25.vb)] |
| `AsDecimal(), IsDecimal()` | <span data-ttu-id="01725-272">Převede řetězec, který má hodnotu decimal jako &quot;1.3&quot; nebo &quot;7.439&quot; na desetinné číslo.</span><span class="sxs-lookup"><span data-stu-id="01725-272">Converts a string that has a decimal value like &quot;1.3&quot; or &quot;7.439&quot; to a decimal number.</span></span> <span data-ttu-id="01725-273">(V technologii ASP.NET, je přesnější než číslo s plovoucí desetinnou čárkou desetinné číslo.)</span><span class="sxs-lookup"><span data-stu-id="01725-273">(In ASP.NET, a decimal number is more precise than a floating-point number.)</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample26.vb)] |
| `AsDateTime(), IsDateTime()` | <span data-ttu-id="01725-274">Převede řetězec představující hodnotu data a času ASP.NET `DateTime` typu.</span><span class="sxs-lookup"><span data-stu-id="01725-274">Converts a string that represents a date and time value to the ASP.NET `DateTime` type.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample27.vb)] |
| `ToString()` | <span data-ttu-id="01725-275">Převede ostatních typů dat na řetězec.</span><span class="sxs-lookup"><span data-stu-id="01725-275">Converts any other data type to a string.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample28.vb)] |

## <a name="operators"></a><span data-ttu-id="01725-276">Operátory</span><span class="sxs-lookup"><span data-stu-id="01725-276">Operators</span></span>

<span data-ttu-id="01725-277">Operátor je – klíčové slovo nebo znak, který sděluje ASP.NET, jaký druh příkaz k provedení ve výrazu.</span><span class="sxs-lookup"><span data-stu-id="01725-277">An operator is a keyword or character that tells ASP.NET what kind of command to perform in an expression.</span></span> <span data-ttu-id="01725-278">Podporuje mnoho operátory jazyka Visual Basic, ale potřebujete rozpoznat mají-li začít s vývojem webových stránek ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="01725-278">Visual Basic supports many operators, but you only need to recognize a few to get started developing ASP.NET web pages.</span></span> <span data-ttu-id="01725-279">Následující tabulka shrnuje nejběžnější operátory.</span><span class="sxs-lookup"><span data-stu-id="01725-279">The following table summarizes the most common operators.</span></span>

| <span data-ttu-id="01725-280">**Operátor**</span><span class="sxs-lookup"><span data-stu-id="01725-280">**Operator**</span></span> | <span data-ttu-id="01725-281">**Popis**</span><span class="sxs-lookup"><span data-stu-id="01725-281">**Description**</span></span> | <span data-ttu-id="01725-282">**Příklady**</span><span class="sxs-lookup"><span data-stu-id="01725-282">**Examples**</span></span> |
| --- | --- | --- |
| `+ - * /` | <span data-ttu-id="01725-283">Matematické operátory použít ve výrazech číselná.</span><span class="sxs-lookup"><span data-stu-id="01725-283">Math operators used in numerical expressions.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample29.vb)] |
| `=` | <span data-ttu-id="01725-284">Přiřazení a rovnosti.</span><span class="sxs-lookup"><span data-stu-id="01725-284">Assignment and equality.</span></span> <span data-ttu-id="01725-285">V závislosti na kontextu buď hodnota na pravé straně příkazu přiřadí objekt na levé straně nebo kontroluje hodnoty rovnosti.</span><span class="sxs-lookup"><span data-stu-id="01725-285">Depending on context, either assigns the value on the right side of a statement to the object on the left side, or checks the values for equality.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample30.vb)] |
| `<>` | <span data-ttu-id="01725-286">Nerovnosti.</span><span class="sxs-lookup"><span data-stu-id="01725-286">Inequality.</span></span> <span data-ttu-id="01725-287">Vrátí `True` Pokud hodnoty nejsou shodné.</span><span class="sxs-lookup"><span data-stu-id="01725-287">Returns `True` if the values are not equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample31.vb)] |
| `< > <= >=` | <span data-ttu-id="01725-288">Menší než větší, menší nebo rovna a větší než nebo rovno.</span><span class="sxs-lookup"><span data-stu-id="01725-288">Less than, greater than, less than or equal, and greater than or equal.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample32.vb)] |
| `&` | <span data-ttu-id="01725-289">Zřetězení, který se používá pro připojení řetězce.</span><span class="sxs-lookup"><span data-stu-id="01725-289">Concatenation, which is used to join strings.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample33.vbhtml)] |
| `+= -=` | <span data-ttu-id="01725-290">Přírůstek a snížení operátory, které sčítání a odečítání 1 (v uvedeném pořadí) z proměnné.</span><span class="sxs-lookup"><span data-stu-id="01725-290">The increment and decrement operators, which add and subtract 1 (respectively) from a variable.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample34.vb)] |
| `.` | <span data-ttu-id="01725-291">Tečkou.</span><span class="sxs-lookup"><span data-stu-id="01725-291">Dot.</span></span> <span data-ttu-id="01725-292">Používá k rozlišení objekty a jejich vlastnosti a metody.</span><span class="sxs-lookup"><span data-stu-id="01725-292">Used to distinguish objects and their properties and methods.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample35.vb)] |
| `()` | <span data-ttu-id="01725-293">Závorky.</span><span class="sxs-lookup"><span data-stu-id="01725-293">Parentheses.</span></span> <span data-ttu-id="01725-294">Použít na výrazy skupiny předat parametry metody a členy přístup pole a kolekcí.</span><span class="sxs-lookup"><span data-stu-id="01725-294">Used to group expressions, to pass parameters to methods, and to access members of arrays and collections.</span></span> | [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample36.vbhtml)] |
| `Not` | <span data-ttu-id="01725-295">Není.</span><span class="sxs-lookup"><span data-stu-id="01725-295">Not.</span></span> <span data-ttu-id="01725-296">Obrátí hodnotu true na false a naopak.</span><span class="sxs-lookup"><span data-stu-id="01725-296">Reverses a true value to false and vice versa.</span></span> <span data-ttu-id="01725-297">Obvykle se používá jako sdružená způsob, jak otestovat `False` (který je pro není `True`).</span><span class="sxs-lookup"><span data-stu-id="01725-297">Typically used as a shorthand way to test for `False` (that is, for not `True`).</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample37.vb)] |
| `AndAlso OrElse` | <span data-ttu-id="01725-298">Logické a a, nebo které se používají k propojení podmínky společně.</span><span class="sxs-lookup"><span data-stu-id="01725-298">Logical AND and OR, which are used to link conditions together.</span></span> | [!code-vb[Main](introducing-razor-syntax-vb/samples/sample38.vb)] |

## <a name="working-with-file-and-folder-paths-in-code"></a><span data-ttu-id="01725-299">Práce s souboru a cesty ke složce zadat v kódu</span><span class="sxs-lookup"><span data-stu-id="01725-299">Working with File and Folder Paths in Code</span></span>

<span data-ttu-id="01725-300">Cesty k souborům a složkám budete často pracovat v kódu.</span><span class="sxs-lookup"><span data-stu-id="01725-300">You'll often work with file and folder paths in your code.</span></span> <span data-ttu-id="01725-301">Tady je příklad struktury fyzická složka pro web, jak se může objevit ve svém vývojovém počítači:</span><span class="sxs-lookup"><span data-stu-id="01725-301">Here is an example of physical folder structure for a website as it might appear on your development computer:</span></span>

`C:\WebSites\MyWebSite default.cshtml datafile.txt \images Logo.jpg \styles Styles.css`

<span data-ttu-id="01725-302">Zde jsou některé důležité podrobnosti o adresy URL a cesty:</span><span class="sxs-lookup"><span data-stu-id="01725-302">Here are some essential details about URLs and paths:</span></span>

- <span data-ttu-id="01725-303">Adresa URL začíná, buď název domény (`http://www.example.com`) nebo název serveru (`http://localhost`, `http://mycomputer`).</span><span class="sxs-lookup"><span data-stu-id="01725-303">A URL begins with either a domain name (`http://www.example.com`) or a server name (`http://localhost`, `http://mycomputer`).</span></span>
- <span data-ttu-id="01725-304">Adresa URL odpovídá na fyzickou cestu na hostitelském počítači.</span><span class="sxs-lookup"><span data-stu-id="01725-304">A URL corresponds to a physical path on a host computer.</span></span> <span data-ttu-id="01725-305">Například `http://myserver` může odpovídat do složky *C:\websites\mywebsite* na serveru.</span><span class="sxs-lookup"><span data-stu-id="01725-305">For example, `http://myserver` might correspond to the folder *C:\websites\mywebsite* on the server.</span></span>
- <span data-ttu-id="01725-306">Virtuální cesta je sdružená představují cesty v kódu bez nutnosti zadávat úplnou cestu.</span><span class="sxs-lookup"><span data-stu-id="01725-306">A virtual path is shorthand to represent paths in code without having to specify the full path.</span></span> <span data-ttu-id="01725-307">Obsahuje části adresy URL, která následuje název domény nebo serveru.</span><span class="sxs-lookup"><span data-stu-id="01725-307">It includes the portion of a URL that follows the domain or server name.</span></span> <span data-ttu-id="01725-308">Pokud používáte virtuální cesty, můžete přesunout kódu do jiné domény nebo serveru bez nutnosti aktualizovat cesty.</span><span class="sxs-lookup"><span data-stu-id="01725-308">When you use virtual paths, you can move your code to a different domain or server without having to update the paths.</span></span>

<span data-ttu-id="01725-309">Tady je příklad, které vám pomohou pochopit rozdíly:</span><span class="sxs-lookup"><span data-stu-id="01725-309">Here's an example to help you understand the differences:</span></span>

| <span data-ttu-id="01725-310">Úplnou adresu URL</span><span class="sxs-lookup"><span data-stu-id="01725-310">Complete URL</span></span> | `http://mycompanyserver/humanresources/CompanyPolicy.htm` |
| --- | --- |
| <span data-ttu-id="01725-311">název serveru</span><span class="sxs-lookup"><span data-stu-id="01725-311">Server name</span></span> | <span data-ttu-id="01725-312">*mycompanyserver*</span><span class="sxs-lookup"><span data-stu-id="01725-312">*mycompanyserver*</span></span> |
| <span data-ttu-id="01725-313">Virtuální cesta</span><span class="sxs-lookup"><span data-stu-id="01725-313">Virtual path</span></span> | <span data-ttu-id="01725-314">*/humanresources/CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="01725-314">*/humanresources/CompanyPolicy.htm*</span></span> |
| <span data-ttu-id="01725-315">Fyzická cesta</span><span class="sxs-lookup"><span data-stu-id="01725-315">Physical path</span></span> | <span data-ttu-id="01725-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span><span class="sxs-lookup"><span data-stu-id="01725-316">*C:\mywebsites\humanresources\CompanyPolicy.htm*</span></span> |

<span data-ttu-id="01725-317">Je virtuální kořenový adresář, /, stejně jako kořenové jednotce C:, jednotka je \.</span><span class="sxs-lookup"><span data-stu-id="01725-317">The virtual root is /, just like the root of your C: drive is \.</span></span> <span data-ttu-id="01725-318">(Virtuální složky cesty vždy použít lomítka.) Virtuální cesta ke složce nemusí mít stejný název jako fyzická složka; může být alias.</span><span class="sxs-lookup"><span data-stu-id="01725-318">(Virtual folder paths always use forward slashes.) The virtual path of a folder doesn't have to have the same name as the physical folder; it can be an alias.</span></span> <span data-ttu-id="01725-319">(Na provozních serverech virtuální cestu zřídka odpovídá přesnou fyzickou cestu.)</span><span class="sxs-lookup"><span data-stu-id="01725-319">(On production servers, the virtual path rarely matches an exact physical path.)</span></span>

<span data-ttu-id="01725-320">Při práci se soubory a složky v kódu, někdy budete muset odkazovat na fyzickou cestu a někdy virtuální cesty, v závislosti na tom, jaké objekty, kterými pracujete.</span><span class="sxs-lookup"><span data-stu-id="01725-320">When you work with files and folders in code, sometimes you need to reference the physical path and sometimes a virtual path, depending on what objects you're working with.</span></span> <span data-ttu-id="01725-321">Technologie ASP.NET poskytuje tyto nástroje pro práci s cest k souborům a složkám v kódu: `Server.MapPath` metody a `~` operátor a `Href` metoda.</span><span class="sxs-lookup"><span data-stu-id="01725-321">ASP.NET gives you these tools for working with file and folder paths in code: the `Server.MapPath` method, and the `~` operator and `Href` method.</span></span>

### <a name="converting-virtual-to-physical-paths-the-servermappath-method"></a><span data-ttu-id="01725-322">Převod virtuálních a fyzických cest: Metoda Server.MapPath</span><span class="sxs-lookup"><span data-stu-id="01725-322">Converting virtual to physical paths: the Server.MapPath method</span></span>

<span data-ttu-id="01725-323">`Server.MapPath` Metoda převede virtuální cestu (například */default.cshtml*) na absolutní fyzická cesta (jako *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span><span class="sxs-lookup"><span data-stu-id="01725-323">The `Server.MapPath` method converts a virtual path (like */default.cshtml*) to an absolute physical path (like *C:\WebSites\MyWebSiteFolder\default.cshtml*).</span></span> <span data-ttu-id="01725-324">Tuto metodu použijte vždy, když potřebujete úplnou fyzickou cestu.</span><span class="sxs-lookup"><span data-stu-id="01725-324">You use this method any time you need a complete physical path.</span></span> <span data-ttu-id="01725-325">Typickým příkladem je při čtení nebo zápis textový soubor nebo soubor bitové kopie na webovém serveru.</span><span class="sxs-lookup"><span data-stu-id="01725-325">A typical example is when you're reading or writing a text file or image file on the web server.</span></span>

<span data-ttu-id="01725-326">Obvykle neznáte absolutní fyzická cesta vašeho webu na serveru pro hostování lokality, takže této metody můžete převést cestu si jisti – virtuální cestu – odpovídající cestu na serveru pro vás.</span><span class="sxs-lookup"><span data-stu-id="01725-326">You typically don't know the absolute physical path of your site on a hosting site's server, so this method can convert the path you do know — the virtual path — to the corresponding path on the server for you.</span></span> <span data-ttu-id="01725-327">Předat virtuální cestu k souboru nebo složce metodu a vrátí fyzickou cestu:</span><span class="sxs-lookup"><span data-stu-id="01725-327">You pass the virtual path to a file or folder to the method, and it returns the physical path:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample39.vbhtml)]

### <a name="referencing-the-virtual-root-the--operator-and-href-method"></a><span data-ttu-id="01725-328">Odkazování na virtuální kořenový adresář: ~ operátor a metoda Href</span><span class="sxs-lookup"><span data-stu-id="01725-328">Referencing the virtual root: the ~ operator and Href method</span></span>

<span data-ttu-id="01725-329">V *.cshtml* nebo *.vbhtml* souboru, můžete odkazovat pomocí cesty virtuální kořenový adresář `~` operátor.</span><span class="sxs-lookup"><span data-stu-id="01725-329">In a *.cshtml* or *.vbhtml* file, you can reference the virtual root path using the `~` operator.</span></span> <span data-ttu-id="01725-330">To je velmi užitečný, protože můžete přesouváte stránky v lokalitě a všechny odkazy, které obsahují k ostatním stránkám nebudou přerušená.</span><span class="sxs-lookup"><span data-stu-id="01725-330">This is very handy because you can move pages around in a site, and any links they contain to other pages won't be broken.</span></span> <span data-ttu-id="01725-331">Je taky užitečný v případě, že přesunete někdy webu do jiného umístění.</span><span class="sxs-lookup"><span data-stu-id="01725-331">It's also handy in case you ever move your website to a different location.</span></span> <span data-ttu-id="01725-332">Následuje několik příkladů:</span><span class="sxs-lookup"><span data-stu-id="01725-332">Here are some examples:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample40.vbhtml)]

<span data-ttu-id="01725-333">Pokud je web `http://myserver/myapp`, zde je, jak bude ASP.NET považovat tyto cesty při spuštění stránky:</span><span class="sxs-lookup"><span data-stu-id="01725-333">If the website is `http://myserver/myapp`, here's how ASP.NET will treat these paths when the page runs:</span></span>

- <span data-ttu-id="01725-334">`myImagesFolder`: `http://myserver/myapp/images`</span><span class="sxs-lookup"><span data-stu-id="01725-334">`myImagesFolder`: `http://myserver/myapp/images`</span></span>
- <span data-ttu-id="01725-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span><span class="sxs-lookup"><span data-stu-id="01725-335">`myStyleSheet` : `http://myserver/myapp/styles/Stylesheet.css`</span></span>

<span data-ttu-id="01725-336">(Ve skutečnosti neuvidíte tyto cesty jako hodnoty proměnné, ale technologie ASP.NET bude považovat cesty, jako kdyby se, jak byly.)</span><span class="sxs-lookup"><span data-stu-id="01725-336">(You won't actually see these paths as the values of the variable, but ASP.NET will treat the paths as if that's what they were.)</span></span>

<span data-ttu-id="01725-337">Můžete použít `~` operátor v serverovém kódu (jak je uvedeno výše) a ve značkách takto:</span><span class="sxs-lookup"><span data-stu-id="01725-337">You can use the `~` operator both in server code (as above) and in markup, like this:</span></span>

[!code-html[Main](introducing-razor-syntax-vb/samples/sample41.html)]

<span data-ttu-id="01725-338">V kódu, můžete použít `~` operátor vytvoření cesty k prostředkům, jako jsou soubory obrázků, jiné webové stránky a souborů CSS.</span><span class="sxs-lookup"><span data-stu-id="01725-338">In markup, you use the `~` operator to create paths to resources like image files, other web pages, and CSS files.</span></span> <span data-ttu-id="01725-339">Při spuštění stránky ASP.NET vypadá prostřednictvím stránky (kód a kód) a všechny přeloží `~` odkazy na příslušné cestě.</span><span class="sxs-lookup"><span data-stu-id="01725-339">When the page runs, ASP.NET looks through the page (both code and markup) and resolves all the `~` references to the appropriate path.</span></span>

## <a name="conditional-logic-and-loops"></a><span data-ttu-id="01725-340">Podmíněnou logiku a smyčky</span><span class="sxs-lookup"><span data-stu-id="01725-340">Conditional Logic and Loops</span></span>

<span data-ttu-id="01725-341">Kód serveru technologie ASP.NET umožňuje provádět úlohy na základě ujednání a zápisu kód opakující se příkazy konkrétní počet tedy kód, který běží smyčku).</span><span class="sxs-lookup"><span data-stu-id="01725-341">ASP.NET server code lets you perform tasks based on conditions and write code that repeats statements a specific number of times that is, code that runs a loop).</span></span>

### <a name="testing-conditions"></a><span data-ttu-id="01725-342">Testování podmínky</span><span class="sxs-lookup"><span data-stu-id="01725-342">Testing conditions</span></span>

<span data-ttu-id="01725-343">K otestování jednoduché podmínku můžete použít `If...Then` příkaz, který vrací `True` nebo `False` podle testu zadáte:</span><span class="sxs-lookup"><span data-stu-id="01725-343">To test a simple condition you use the `If...Then` statement, which returns `True` or `False` based on a test you specify:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample42.vbhtml)]

<span data-ttu-id="01725-344">`If` – Klíčové slovo spustí blok.</span><span class="sxs-lookup"><span data-stu-id="01725-344">The `If` keyword starts a block.</span></span> <span data-ttu-id="01725-345">Dodržuje skutečný test (podmínka) `If` – klíčové slovo a vrátí hodnotu true nebo false.</span><span class="sxs-lookup"><span data-stu-id="01725-345">The actual test (condition) follows the `If` keyword and returns true or false.</span></span> <span data-ttu-id="01725-346">`If` Příkaz končí `Then`.</span><span class="sxs-lookup"><span data-stu-id="01725-346">The `If` statement ends with `Then`.</span></span> <span data-ttu-id="01725-347">Příkazy, které se spustí, pokud je testu hodnota true, jsou uvedeny v `If` a `End If`.</span><span class="sxs-lookup"><span data-stu-id="01725-347">The statements that will run if the test is true are enclosed by `If` and `End If`.</span></span> <span data-ttu-id="01725-348">`If` Může zahrnovat příkaz `Else` blok, který určuje příkazy ke spouštění, pokud je podmínka vyhodnocena jako false:</span><span class="sxs-lookup"><span data-stu-id="01725-348">An `If` statement can include an `Else` block that specifies statements to run if the condition is false:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample43.vbhtml)]

<span data-ttu-id="01725-349">Pokud `If` příkaz spustí blok kódu, nemusíte používat normální `Code...End Code` příkazy k zahrnutí bloků.</span><span class="sxs-lookup"><span data-stu-id="01725-349">If an `If` statement starts a code block, you don't have to use the normal `Code...End Code` statements to include the blocks.</span></span> <span data-ttu-id="01725-350">Můžete přidat pouze `@` blok, a bude fungovat.</span><span class="sxs-lookup"><span data-stu-id="01725-350">You can just add `@` to the block, and it will work.</span></span> <span data-ttu-id="01725-351">Tento postup funguje s `If` a také další jazyka Visual Basic programování klíčová slova, která následuje za bloky kódu, včetně `For`, `For Each`, `Do While`atd.</span><span class="sxs-lookup"><span data-stu-id="01725-351">This approach works with `If` as well as other Visual Basic programming keywords that are followed by code blocks, including `For`, `For Each`, `Do While`, etc.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample44.vbhtml)]

<span data-ttu-id="01725-352">Můžete přidat více podmínek použití jednoho nebo více `ElseIf` bloky:</span><span class="sxs-lookup"><span data-stu-id="01725-352">You can add multiple conditions using one or more `ElseIf` blocks:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample45.vbhtml)]

<span data-ttu-id="01725-353">V tomto příkladu, pokud je první podmínka vyhodnocena jako v `If` blok není nastavena hodnota true, `ElseIf` se kontroluje podmínku.</span><span class="sxs-lookup"><span data-stu-id="01725-353">In this example, if the first condition in the `If` block is not true, the `ElseIf` condition is checked.</span></span> <span data-ttu-id="01725-354">Při splnění této podmínky je, příkazy v `ElseIf` provedení bloku.</span><span class="sxs-lookup"><span data-stu-id="01725-354">If that condition is met, the statements in the `ElseIf` block are executed.</span></span> <span data-ttu-id="01725-355">Pokud žádná z podmínek jsou splněny, příkazy v `Else` provedení bloku.</span><span class="sxs-lookup"><span data-stu-id="01725-355">If none of the conditions are met, the statements in the `Else` block are executed.</span></span> <span data-ttu-id="01725-356">Můžete přidat libovolný počet `ElseIf` blokuje a pak zavřete s `Else` blokovat jako &quot;všem ostatním&quot; podmínku.</span><span class="sxs-lookup"><span data-stu-id="01725-356">You can add any number of `ElseIf` blocks, and then close with an `Else` block as the &quot;everything else&quot; condition.</span></span>

<span data-ttu-id="01725-357">Chcete-li otestovat velký počet podmínky, použijte `Select Case` bloku:</span><span class="sxs-lookup"><span data-stu-id="01725-357">To test a large number of conditions, use a `Select Case` block:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample46.vbhtml)]

<span data-ttu-id="01725-358">Hodnota k testování je v závorkách (v příkladu proměnnou den v týdnu).</span><span class="sxs-lookup"><span data-stu-id="01725-358">The value to test is in parentheses (in the example, the weekday variable).</span></span> <span data-ttu-id="01725-359">Používá každé jednotlivé testy `Case` příkaz, který uvádí hodnotu.</span><span class="sxs-lookup"><span data-stu-id="01725-359">Each individual test uses a `Case` statement that lists a value.</span></span> <span data-ttu-id="01725-360">Pokud hodnota `Case` příkaz odpovídá hodnotě testovací kód v tom, že `Case` bloku se spustí.</span><span class="sxs-lookup"><span data-stu-id="01725-360">If the value of a `Case` statement matches the test value, the code in that `Case` block is executed.</span></span>

<span data-ttu-id="01725-361">Výsledek posledních dvou podmíněné bloky zobrazený v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-361">The result of the last two conditional blocks displayed in a browser:</span></span>

![Razor-Img10](introducing-razor-syntax-vb/_static/image10.jpg)

### <a name="looping-code"></a><span data-ttu-id="01725-363">Ve smyčce kódu</span><span class="sxs-lookup"><span data-stu-id="01725-363">Looping code</span></span>

<span data-ttu-id="01725-364">Potřebujete často opakovaně spouštět stejné příkazy.</span><span class="sxs-lookup"><span data-stu-id="01725-364">You often need to run the same statements repeatedly.</span></span> <span data-ttu-id="01725-365">To provedete opakování ve smyčce.</span><span class="sxs-lookup"><span data-stu-id="01725-365">You do this by looping.</span></span> <span data-ttu-id="01725-366">Například můžete často spustit stejné příkazy pro každou položku v kolekci dat.</span><span class="sxs-lookup"><span data-stu-id="01725-366">For example, you often run the same statements for each item in a collection of data.</span></span> <span data-ttu-id="01725-367">Pokud znáte přesně kolikrát chcete cykly, můžete použít `For` smyčky.</span><span class="sxs-lookup"><span data-stu-id="01725-367">If you know exactly how many times you want to loop, you can use a `For` loop.</span></span> <span data-ttu-id="01725-368">Tento druh smyčky je zvláště užitečná pro počítání nebo počítání:</span><span class="sxs-lookup"><span data-stu-id="01725-368">This kind of loop is especially useful for counting up or counting down:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample47.vbhtml)]

<span data-ttu-id="01725-369">Smyčky začíná `For` – klíčové slovo, za nímž následuje tři prvky:</span><span class="sxs-lookup"><span data-stu-id="01725-369">The loop begins with the `For` keyword, followed by three elements:</span></span>

- <span data-ttu-id="01725-370">Ihned po `For` příkaz deklarace proměnné čítače (nemusíte používat `Dim`) a potom rozsahu, jako v `i = 10 to 20`.</span><span class="sxs-lookup"><span data-stu-id="01725-370">Immediately after the `For` statement, you declare a counter variable (you don't have to use `Dim`) and then indicate the range, as in `i = 10 to 20`.</span></span> <span data-ttu-id="01725-371">To znamená proměnnou `i` bude spustit počítání na 10 a bude pokračovat, dokud nebude dosaženo 20 (včetně).</span><span class="sxs-lookup"><span data-stu-id="01725-371">This means the variable `i` will start counting at 10 and continue until it reaches 20 (inclusive).</span></span>
- <span data-ttu-id="01725-372">Mezi `For` a `Next` příkazy je obsah bloku.</span><span class="sxs-lookup"><span data-stu-id="01725-372">Between the `For` and `Next` statements is the content of the block.</span></span> <span data-ttu-id="01725-373">To může obsahovat jeden nebo více kód příkazů, které provést s každou smyčku.</span><span class="sxs-lookup"><span data-stu-id="01725-373">This can contain one or more code statements that execute with each loop.</span></span>
- <span data-ttu-id="01725-374">`Next i` Příkaz ukončení smyčky.</span><span class="sxs-lookup"><span data-stu-id="01725-374">The `Next i` statement ends the loop.</span></span> <span data-ttu-id="01725-375">To zvýší čítače a spustí další iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="01725-375">It increments the counter and starts the next iteration of the loop.</span></span>

<span data-ttu-id="01725-376">Řádek kódu mezi `For` a `Next` řádky obsahuje kód, který spouští pro každou iteraci smyčky.</span><span class="sxs-lookup"><span data-stu-id="01725-376">The line of code between the `For` and `Next` lines contains the code that runs for each iteration of the loop.</span></span> <span data-ttu-id="01725-377">Kód vytvoří nový odstavec (`<p>` element) každou čas a přidá řádek do výstupu zobrazení hodnotu i (Čítač).</span><span class="sxs-lookup"><span data-stu-id="01725-377">The markup creates a new paragraph (`<p>` element) each time and adds a line to the output, displaying the value of i (the counter).</span></span> <span data-ttu-id="01725-378">Při spuštění této stránce v příkladu se vytváří 11 řádky zobrazení výstupu s textem do každého řádku, která určuje počet položek.</span><span class="sxs-lookup"><span data-stu-id="01725-378">When you run this page, the example creates 11 lines displaying the output, with the text in each line indicating the item number.</span></span>

![Razor-Img11](introducing-razor-syntax-vb/_static/image11.jpg)

<span data-ttu-id="01725-380">Pokud pracujete s kolekce nebo pole, často používají `For Each` smyčky.</span><span class="sxs-lookup"><span data-stu-id="01725-380">If you're working with a collection or array, you often use a `For Each` loop.</span></span> <span data-ttu-id="01725-381">Kolekce je skupina podobné objekty a `For Each` cykly umožňuje provádět úlohy na každou položku v kolekci.</span><span class="sxs-lookup"><span data-stu-id="01725-381">A collection is a group of similar objects, and the `For Each` loop lets you carry out a task on each item in the collection.</span></span> <span data-ttu-id="01725-382">Tento typ smyčky je vhodné pro kolekce, protože na rozdíl od `For` smyčky, nemusíte zvýší čítače nebo nastavit limit.</span><span class="sxs-lookup"><span data-stu-id="01725-382">This type of loop is convenient for collections, because unlike a `For` loop, you don't have to increment the counter or set a limit.</span></span> <span data-ttu-id="01725-383">Místo toho `For Each` smyčky kód jednoduše pokračuje prostřednictvím kolekce, dokud se nedokončí jeho.</span><span class="sxs-lookup"><span data-stu-id="01725-383">Instead, the `For Each` loop code simply proceeds through the collection until it's finished.</span></span>

<span data-ttu-id="01725-384">Vrátí položky v tomto příkladu `Request.ServerVariables` kolekce (která obsahuje informace o vašem webovém serveru).</span><span class="sxs-lookup"><span data-stu-id="01725-384">This example returns the items in the `Request.ServerVariables` collection (which contains information about your web server).</span></span> <span data-ttu-id="01725-385">Použije `For Each` smyčky a zobrazí se název každé položky tak, že vytvoříte novou `<li>` element v seznamu s odrážkami HTML.</span><span class="sxs-lookup"><span data-stu-id="01725-385">It uses a `For Each` loop to display the name of each item by creating a new `<li>` element in an HTML bulleted list.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample48.vbhtml)]

<span data-ttu-id="01725-386">`For Each` – Klíčové slovo následuje proměnné, která představuje jednu položku v kolekci (v příkladu `myItem`), za nímž následují `In` – klíčové slovo, za nímž následuje kolekce, kterou chcete projít.</span><span class="sxs-lookup"><span data-stu-id="01725-386">The `For Each` keyword is followed by a variable that represents a single item in the collection (in the example, `myItem`), followed by the `In` keyword, followed by the collection you want to loop through.</span></span> <span data-ttu-id="01725-387">V textu `For Each` smyčky, dostanete s aktuální položkou použitím proměnné, kterou jste předtím deklarován.</span><span class="sxs-lookup"><span data-stu-id="01725-387">In the body of the `For Each` loop, you can access the current item using the variable that you declared earlier.</span></span>

![Razor-Img12](introducing-razor-syntax-vb/_static/image12.jpg)

<span data-ttu-id="01725-389">Chcete-li vytvořit více pro obecné účely smyčku, použijte `Do While` příkaz:</span><span class="sxs-lookup"><span data-stu-id="01725-389">To create a more general-purpose loop, use the `Do While` statement:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample49.vbhtml)]

<span data-ttu-id="01725-390">Tato smyčka začíná `Do While` – klíčové slovo, za nímž následuje podmínku, za nímž následuje bloku opakování.</span><span class="sxs-lookup"><span data-stu-id="01725-390">This loop begins with the `Do While` keyword, followed by a condition, followed by the block to repeat.</span></span> <span data-ttu-id="01725-391">Obvykle zvýšit smyčky (přidejte) nebo snížení (odečíst od) proměnnou nebo objekt použitý pro počítání.</span><span class="sxs-lookup"><span data-stu-id="01725-391">Loops typically increment (add to) or decrement (subtract from) a variable or object used for counting.</span></span> <span data-ttu-id="01725-392">V příkladu `+=` operátor přidá 1 na hodnotu proměnné pokaždé, když běží smyčky.</span><span class="sxs-lookup"><span data-stu-id="01725-392">In the example, the `+=` operator adds 1 to the value of a variable each time the loop runs.</span></span> <span data-ttu-id="01725-393">(Chcete-li snížení proměnné ve smyčce počty dolů, použijte operátor snížení `-=`.)</span><span class="sxs-lookup"><span data-stu-id="01725-393">(To decrement a variable in a loop that counts down, you would use the decrement operator `-=`.)</span></span>

## <a name="objects-and-collections"></a><span data-ttu-id="01725-394">Objekty a kolekce</span><span class="sxs-lookup"><span data-stu-id="01725-394">Objects and Collections</span></span>

<span data-ttu-id="01725-395">Téměř všechno ve webové stránky ASP.NET je objekt, včetně webové stránky.</span><span class="sxs-lookup"><span data-stu-id="01725-395">Nearly everything in an ASP.NET website is an object, including the web page itself.</span></span> <span data-ttu-id="01725-396">Tato část popisuje některé důležité objekty, se kterými budete pracovat často v kódu.</span><span class="sxs-lookup"><span data-stu-id="01725-396">This section discusses some important objects you'll work with frequently in your code.</span></span>

### <a name="page-objects"></a><span data-ttu-id="01725-397">Objekty stránky</span><span class="sxs-lookup"><span data-stu-id="01725-397">Page objects</span></span>

<span data-ttu-id="01725-398">Objekt nejzákladnější technologie ASP.NET je stránka.</span><span class="sxs-lookup"><span data-stu-id="01725-398">The most basic object in ASP.NET is the page.</span></span> <span data-ttu-id="01725-399">Můžete přistoupit k vlastnostem objektu page přímo bez žádné opravňující objektu.</span><span class="sxs-lookup"><span data-stu-id="01725-399">You can access properties of the page object directly without any qualifying object.</span></span> <span data-ttu-id="01725-400">Následující kód získá cesta k souboru stránky, pomocí `Request` objekt stránky:</span><span class="sxs-lookup"><span data-stu-id="01725-400">The following code gets the page's file path, using the `Request` object of the page:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample50.vbhtml)]

<span data-ttu-id="01725-401">Můžete použít vlastnosti `Page` objekt, který chcete získat velké množství informací, jako například:</span><span class="sxs-lookup"><span data-stu-id="01725-401">You can use properties of the `Page` object to get a lot of information, such as:</span></span>

- <span data-ttu-id="01725-402">`Request`.</span><span class="sxs-lookup"><span data-stu-id="01725-402">`Request`.</span></span> <span data-ttu-id="01725-403">Jak už jsme viděli, to je kolekce informací o aktuálním požadavku, včetně, jaký typ prohlížeče požadavek, adresu URL stránky, identita uživatele, atd.</span><span class="sxs-lookup"><span data-stu-id="01725-403">As you've already seen, this is a collection of information about the current request, including what type of browser made the request, the URL of the page, the user identity, etc.</span></span>
- <span data-ttu-id="01725-404">`Response`.</span><span class="sxs-lookup"><span data-stu-id="01725-404">`Response`.</span></span> <span data-ttu-id="01725-405">Toto je kolekce informací o odpovědi (stránky), která se odešle do prohlížeče při kódu serveru bylo dokončeno.</span><span class="sxs-lookup"><span data-stu-id="01725-405">This is a collection of information about the response (page) that will be sent to the browser when the server code has finished running.</span></span> <span data-ttu-id="01725-406">Například můžete tuto vlastnost při zápisu informací do odpovědi.</span><span class="sxs-lookup"><span data-stu-id="01725-406">For example, you can use this property to write information into the response.</span></span>

    [!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample51.vbhtml)]

### <a name="collection-objects-arrays-and-dictionaries"></a><span data-ttu-id="01725-407">Kolekce objektů (pole a slovník)</span><span class="sxs-lookup"><span data-stu-id="01725-407">Collection objects (arrays and dictionaries)</span></span>

<span data-ttu-id="01725-408">Kolekce je pro skupinu objektů stejného typu, například v kolekci `Customer` objekty z databáze.</span><span class="sxs-lookup"><span data-stu-id="01725-408">A collection is a group of objects of the same type, such as a collection of `Customer` objects from a database.</span></span> <span data-ttu-id="01725-409">ASP.NET obsahuje mnoho předdefinovaných kolekcí, jako je třeba `Request.Files` kolekce.</span><span class="sxs-lookup"><span data-stu-id="01725-409">ASP.NET contains many built-in collections, like the `Request.Files` collection.</span></span>

<span data-ttu-id="01725-410">Často budete pracovat dat v kolekcích.</span><span class="sxs-lookup"><span data-stu-id="01725-410">You'll often work with data in collections.</span></span> <span data-ttu-id="01725-411">Jsou dvě běžné typy kolekcí *pole* a *slovník*.</span><span class="sxs-lookup"><span data-stu-id="01725-411">Two common collection types are the *array* and the *dictionary*.</span></span> <span data-ttu-id="01725-412">Pole je užitečné, když chcete ukládat kolekce podobné položky, ale nechcete vytvořit samostatnou proměnnou pro každou položku:</span><span class="sxs-lookup"><span data-stu-id="01725-412">An array is useful when you want to store a collection of similar items but don't want to create a separate variable to hold each item:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample52.vbhtml)]

<span data-ttu-id="01725-413">S poli, je třeba deklarovat určité datového typu, `String`, `Integer`, nebo `DateTime`.</span><span class="sxs-lookup"><span data-stu-id="01725-413">With arrays, you declare a specific data type, such as `String`, `Integer`, or `DateTime`.</span></span> <span data-ttu-id="01725-414">K označení, proměnná může obsahovat pole, je závorkách přidat k názvu proměnné v deklaraci (například `Dim myVar() As String`).</span><span class="sxs-lookup"><span data-stu-id="01725-414">To indicate that the variable can contain an array, you add parentheses to the variable name in the declaration (such as `Dim myVar() As String`).</span></span> <span data-ttu-id="01725-415">Dostanete položky v poli pomocí jejich polohu (index) nebo pomocí `For Each` příkaz.</span><span class="sxs-lookup"><span data-stu-id="01725-415">You can access items in an array using their position (index) or by using the `For Each` statement.</span></span> <span data-ttu-id="01725-416">Pole indexy jsou od nuly &#8212; To znamená první položka je v umístění 0, druhý položka je na pozici 1 a tak dále.</span><span class="sxs-lookup"><span data-stu-id="01725-416">Array indexes are zero-based &#8212; that is, the first item is at position 0, the second item is at position 1, and so on.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample53.vbhtml)]

<span data-ttu-id="01725-417">Můžete určit počet položek v matici získáním jeho `Length` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="01725-417">You can determine the number of items in an array by getting its `Length` property.</span></span> <span data-ttu-id="01725-418">Získat pozice konkrétní položku v poli (tedy prohledávání pole), použijte `Array.IndexOf` metoda.</span><span class="sxs-lookup"><span data-stu-id="01725-418">To get the position of a specific item in the array (that is, to search the array), use the `Array.IndexOf` method.</span></span> <span data-ttu-id="01725-419">Můžete také provést třeba zpětného obsah pole ( `Array.Reverse` metoda) nebo řazení obsahu ( `Array.Sort` metoda).</span><span class="sxs-lookup"><span data-stu-id="01725-419">You can also do things like reverse the contents of an array (the `Array.Reverse` method) or sort the contents (the `Array.Sort` method).</span></span>

<span data-ttu-id="01725-420">Výstupní kód pole řetězec, který je zobrazený v prohlížeči:</span><span class="sxs-lookup"><span data-stu-id="01725-420">The output of the string array code displayed in a browser:</span></span>

![Razor-Img13](introducing-razor-syntax-vb/_static/image13.jpg)

<span data-ttu-id="01725-422">Slovník je kolekce dvojic klíč/hodnota, kde zadáte klíč (nebo název) nastavit nebo načíst s odpovídající hodnotou:</span><span class="sxs-lookup"><span data-stu-id="01725-422">A dictionary is a collection of key/value pairs, where you provide the key (or name) to set or retrieve the corresponding value:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample54.vbhtml)]

<span data-ttu-id="01725-423">Chcete-li vytvořit slovník, je použít `New` – klíčové slovo indikující, že vytváříte novou `Dictionary` objektu.</span><span class="sxs-lookup"><span data-stu-id="01725-423">To create a dictionary, you use the `New` keyword to indicate that you're creating a new `Dictionary` object.</span></span> <span data-ttu-id="01725-424">Slovník můžete přiřadit k proměnné pomocí `Dim` – klíčové slovo.</span><span class="sxs-lookup"><span data-stu-id="01725-424">You can assign a dictionary to a variable using the `Dim` keyword.</span></span> <span data-ttu-id="01725-425">Určují typy položek ve slovníku pomocí závorek ( `( )` ).</span><span class="sxs-lookup"><span data-stu-id="01725-425">You indicate the data types of the items in the dictionary using parentheses ( `( )` ).</span></span> <span data-ttu-id="01725-426">Na konci tohoto prohlášení je nutné přidat jinou dvojici závorek, protože to je ve skutečnosti metoda, která vytvoří do nového slovníku.</span><span class="sxs-lookup"><span data-stu-id="01725-426">At the end of the declaration, you must add another pair of parentheses, because this is actually a method that creates a new dictionary.</span></span>

<span data-ttu-id="01725-427">Chcete-li přidat položky do slovníku, můžete zavolat `Add` metoda slovník proměnné (`myScores` v tomto případě) a pak zadejte klíč a hodnotu.</span><span class="sxs-lookup"><span data-stu-id="01725-427">To add items to the dictionary, you can call the `Add` method of the dictionary variable (`myScores` in this case), and then specify a key and a value.</span></span> <span data-ttu-id="01725-428">Alternativně můžete závorkách označuje klíč a provádět jednoduché přiřazení, jako v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="01725-428">Alternatively, you can use parentheses to indicate the key and do a simple assignment, as in the following example:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample55.vbhtml)]

<span data-ttu-id="01725-429">Získá hodnotu ze slovníku, zadejte klíč v závorkách:</span><span class="sxs-lookup"><span data-stu-id="01725-429">To get a value from the dictionary, you specify the key in parentheses:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample56.vbhtml)]

## <a name="calling-methods-with-parameters"></a><span data-ttu-id="01725-430">Volání metody s parametry</span><span class="sxs-lookup"><span data-stu-id="01725-430">Calling Methods with Parameters</span></span>

<span data-ttu-id="01725-431">Jak už jste viděli dříve v tomto článku, objekty, které můžete naprogramovat s mají metody.</span><span class="sxs-lookup"><span data-stu-id="01725-431">As you saw earlier in this article, the objects that you program with have methods.</span></span> <span data-ttu-id="01725-432">Například `Database` objekt může mít `Database.Connect` metoda.</span><span class="sxs-lookup"><span data-stu-id="01725-432">For example, a `Database` object might have a `Database.Connect` method.</span></span> <span data-ttu-id="01725-433">Mnoho způsobů také mít jeden nebo více parametrů.</span><span class="sxs-lookup"><span data-stu-id="01725-433">Many methods also have one or more parameters.</span></span> <span data-ttu-id="01725-434">A *parametr* je hodnota, kterou předáte metodě povolení metody a dokončení úkolu.</span><span class="sxs-lookup"><span data-stu-id="01725-434">A *parameter* is a value that you pass to a method to enable the method to complete its task.</span></span> <span data-ttu-id="01725-435">Například, podívejte se na deklaraci pro `Request.MapPath` metodu, která přijímá tři parametry:</span><span class="sxs-lookup"><span data-stu-id="01725-435">For example, look at a declaration for the `Request.MapPath` method, which takes three parameters:</span></span>

[!code-vb[Main](introducing-razor-syntax-vb/samples/sample57.vb)]

<span data-ttu-id="01725-436">Tato metoda vrátí fyzickou cestu na serveru, který odpovídá do zadané virtuální cesty.</span><span class="sxs-lookup"><span data-stu-id="01725-436">This method returns the physical path on the server that corresponds to a specified virtual path.</span></span> <span data-ttu-id="01725-437">Jsou tři parametry pro metodu `virtualPath`, `baseVirtualDir`, a `allowCrossAppMapping`.</span><span class="sxs-lookup"><span data-stu-id="01725-437">The three parameters for the method are `virtualPath`, `baseVirtualDir`, and `allowCrossAppMapping`.</span></span> <span data-ttu-id="01725-438">(Všimněte si, že v deklaraci, jsou parametry uvedené s datovými typy dat, která budete přijmou.) Když zavoláte tuto metodu, je nutné zadat hodnoty pro všechny tři parametry.</span><span class="sxs-lookup"><span data-stu-id="01725-438">(Notice that in the declaration, the parameters are listed with the data types of the data that they'll accept.) When you call this method, you must supply values for all three parameters.</span></span>

<span data-ttu-id="01725-439">Pokud používáte Visual Basic se syntaxí Razor, máte dvě možnosti pro předávání parametrů pro metodu: *poziční parametry* nebo *pojmenované parametry*.</span><span class="sxs-lookup"><span data-stu-id="01725-439">When you're using Visual Basic with the Razor syntax, you have two options for passing parameters to a method: *positional parameters* or *named parameters*.</span></span> <span data-ttu-id="01725-440">Pro volání metody pomocí poziční parametry, předáte parametry striktní pořadí, které je zadaný v deklaraci metoda.</span><span class="sxs-lookup"><span data-stu-id="01725-440">To call a method using positional parameters, you pass the parameters in a strict order that's specified in the method declaration.</span></span> <span data-ttu-id="01725-441">(By obvykle víte, toto pořadí načtením dokumentace pro metodu.) Je třeba postupovat podle pořadí, a přeskočíte nelze některý z parametrů &#8212; Pokud potřeby předáte prázdný řetězec (`""`) nebo pro poziční parametr, který nemáte hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="01725-441">(You would typically know this order by reading documentation for the method.) You must follow the order, and you can't skip any of the parameters &#8212; if necessary, you pass an empty string (`""`) or null for a positional parameter that you don't have a value for.</span></span>

<span data-ttu-id="01725-442">Následující příklad předpokládá, že máte složku s názvem *skripty* na vašem webu.</span><span class="sxs-lookup"><span data-stu-id="01725-442">The following example assumes you have a folder named *scripts* on your website.</span></span> <span data-ttu-id="01725-443">Volání kódu `Request.MapPath` metoda a předává hodnoty pro tři parametry ve správném pořadí.</span><span class="sxs-lookup"><span data-stu-id="01725-443">The code calls the `Request.MapPath` method and passes values for the three parameters in the correct order.</span></span> <span data-ttu-id="01725-444">Pak zobrazí výsledná namapované cesta.</span><span class="sxs-lookup"><span data-stu-id="01725-444">It then displays the resulting mapped path.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample58.vbhtml)]

<span data-ttu-id="01725-445">Pokud je mnoho parametrů pro metodu, můžete ponechat kódu čisticí a srozumitelnější pomocí pojmenované parametry.</span><span class="sxs-lookup"><span data-stu-id="01725-445">When there are many parameters for a method, you can keep your code cleaner and more readable by using named parameters.</span></span> <span data-ttu-id="01725-446">Chcete-li zavolat metodu pomocí pojmenované parametry, zadejte název parametru, za nímž následuje `:=` a pak zadejte hodnotu.</span><span class="sxs-lookup"><span data-stu-id="01725-446">To call a method using named parameters, specify the parameter name followed by `:=` and then provide the value.</span></span> <span data-ttu-id="01725-447">Výhodou pojmenované parametry je, že budete moct přidat v libovolném pořadí, které chcete.</span><span class="sxs-lookup"><span data-stu-id="01725-447">An advantage of named parameters is that you can add them in any order you want.</span></span> <span data-ttu-id="01725-448">(Nevýhodou je, že volání metody není jako compact.)</span><span class="sxs-lookup"><span data-stu-id="01725-448">(A disadvantage is that the method call is not as compact.)</span></span>

<span data-ttu-id="01725-449">V následujícím příkladu volání stejnou metodu, jak je uvedeno výše, ale používá pojmenované parametry k zadání hodnoty:</span><span class="sxs-lookup"><span data-stu-id="01725-449">The following example calls the same method as above, but uses named parameters to supply the values:</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample59.vbhtml)]

<span data-ttu-id="01725-450">Jak je vidět parametry se jí předávají v jiném pořadí.</span><span class="sxs-lookup"><span data-stu-id="01725-450">As you can see, the parameters are passed in a different order.</span></span> <span data-ttu-id="01725-451">Pokud spustíte předchozí příklad a v tomto příkladu, budete však vrátit stejnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="01725-451">However, if you run the previous example and this example, they'll return the same value.</span></span>

## <a name="handling-errors"></a><span data-ttu-id="01725-452">Zpracování chyb</span><span class="sxs-lookup"><span data-stu-id="01725-452">Handling Errors</span></span>

### <a name="try-catch-statements"></a><span data-ttu-id="01725-453">Try-Catch – příkazy</span><span class="sxs-lookup"><span data-stu-id="01725-453">Try-Catch statements</span></span>

<span data-ttu-id="01725-454">Příkazy často budete mít ve svém kódu, který může selhat z důvodů mimo vlastního ovládacího prvku.</span><span class="sxs-lookup"><span data-stu-id="01725-454">You'll often have statements in your code that might fail for reasons outside your control.</span></span> <span data-ttu-id="01725-455">Příklad:</span><span class="sxs-lookup"><span data-stu-id="01725-455">For example:</span></span>

- <span data-ttu-id="01725-456">Pokud váš kód se pokusí otevřít, vytváření, čtení a zápis do souboru, může dojít k nejrůznějším chyb.</span><span class="sxs-lookup"><span data-stu-id="01725-456">If your code tries to open, create, read, or write a file, all sorts of errors might occur.</span></span> <span data-ttu-id="01725-457">Požadovaný soubor pravděpodobně neexistuje, může být zablokován, kód nemusí mít oprávnění a tak dále.</span><span class="sxs-lookup"><span data-stu-id="01725-457">The file you want might not exist, it might be locked, the code might not have permissions, and so on.</span></span>
- <span data-ttu-id="01725-458">Podobně pokud kód pokusí k aktualizaci záznamů v databázi, může být problémy s oprávněními, může dojít ke ztrátě připojení k databázi, data uložit může být neplatný a tak dále.</span><span class="sxs-lookup"><span data-stu-id="01725-458">Similarly, if your code tries to update records in a database, there can be permissions issues, the connection to the database might be dropped, the data to save might be invalid, and so on.</span></span>

<span data-ttu-id="01725-459">Programovací podmínky, se nazývají těchto situacích *výjimky*.</span><span class="sxs-lookup"><span data-stu-id="01725-459">In programming terms, these situations are called *exceptions*.</span></span> <span data-ttu-id="01725-460">Pokud váš kód zjistí výjimku, generuje (vyvolává) chybové zprávy na nejvyšší, který je obtěžování uživatelům.</span><span class="sxs-lookup"><span data-stu-id="01725-460">If your code encounters an exception, it generates (throws) an error message that is, at best, annoying to users.</span></span>

![Razor-Img14](introducing-razor-syntax-vb/_static/image14.jpg)

<span data-ttu-id="01725-462">V situacích, kde kódu setkat výjimky a aby se zabránilo chybové zprávy tohoto typu, můžete použít `Try/Catch` příkazy.</span><span class="sxs-lookup"><span data-stu-id="01725-462">In situations where your code might encounter exceptions, and in order to avoid error messages of this type, you can use `Try/Catch` statements.</span></span> <span data-ttu-id="01725-463">V `Try` prohlášení, můžete spustit kód, který při kontrole.</span><span class="sxs-lookup"><span data-stu-id="01725-463">In the `Try` statement, you run the code that you're checking.</span></span> <span data-ttu-id="01725-464">V jedné nebo více `Catch` příkazy, můžete vyhledat konkrétní chyby (konkrétní typy výjimek), jež mohly nastat.</span><span class="sxs-lookup"><span data-stu-id="01725-464">In one or more `Catch` statements, you can look for specific errors (specific types of exceptions) that might have occurred.</span></span> <span data-ttu-id="01725-465">Můžete zahrnout tolik `Catch` příkazy, jako je třeba vyhledejte chyby, které se očekává.</span><span class="sxs-lookup"><span data-stu-id="01725-465">You can include as many `Catch` statements as you need to look for errors that you're anticipating.</span></span>

> [!NOTE]
> <span data-ttu-id="01725-466">Doporučujeme, abyste neměli používat `Response.Redirect` metoda v `Try/Catch` příkazy, protože ho může způsobit výjimku v stránku.</span><span class="sxs-lookup"><span data-stu-id="01725-466">We recommend that you avoid using the `Response.Redirect` method in `Try/Catch` statements, because it can cause an exception in your page.</span></span>


<span data-ttu-id="01725-467">Následující příklad ukazuje stránky, který vytvoří textový soubor na první požadavek a potom zobrazí tlačítko, které umožňuje uživateli otevřít soubor.</span><span class="sxs-lookup"><span data-stu-id="01725-467">The following example shows a page that creates a text file on the first request and then displays a button that lets the user open the file.</span></span> <span data-ttu-id="01725-468">V příkladu úmyslně použije chybný název souboru, takže způsobí výjimku.</span><span class="sxs-lookup"><span data-stu-id="01725-468">The example deliberately uses a bad file name so that it will cause an exception.</span></span> <span data-ttu-id="01725-469">Tento kód obsahuje `Catch` příkazy pro dvě možné výjimky: `FileNotFoundException`, která nastane, pokud je špatná, název souboru a `DirectoryNotFoundException`, který v případě ASP.NET i nelze najít složku.</span><span class="sxs-lookup"><span data-stu-id="01725-469">The code includes `Catch` statements for two possible exceptions: `FileNotFoundException`, which occurs if the file name is bad, and `DirectoryNotFoundException`, which occurs if ASP.NET can't even find the folder.</span></span> <span data-ttu-id="01725-470">(Chcete-li zobrazit, jak se spustí při všechno funguje správně můžete Odkomentujte příkaz v příkladu.)</span><span class="sxs-lookup"><span data-stu-id="01725-470">(You can uncomment a statement in the example in order to see how it runs when everything works properly.)</span></span>

<span data-ttu-id="01725-471">Pokud váš kód nebyl zpracovat výjimku, zobrazí se chybová stránka jako předchozí snímek obrazovky.</span><span class="sxs-lookup"><span data-stu-id="01725-471">If your code didn't handle the exception, you would see an error page like the previous screen shot.</span></span> <span data-ttu-id="01725-472">Ale `Try/Catch` části pomáhá zabránit uživateli v zobrazení tyto typy chyb.</span><span class="sxs-lookup"><span data-stu-id="01725-472">However, the `Try/Catch` section helps prevent the user from seeing these types of errors.</span></span>

[!code-vbhtml[Main](introducing-razor-syntax-vb/samples/sample60.vbhtml)]

## <a name="additional-resources"></a><span data-ttu-id="01725-473">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="01725-473">Additional Resources</span></span>

### <a name="reference-documentation"></a><span data-ttu-id="01725-474">Referenční dokumentace</span><span class="sxs-lookup"><span data-stu-id="01725-474">Reference Documentation</span></span>

- [<span data-ttu-id="01725-475">ASP.NET</span><span class="sxs-lookup"><span data-stu-id="01725-475">ASP.NET</span></span>](https://msdn.microsoft.com/library/ee532866.aspx)
- [<span data-ttu-id="01725-476">Jazyk Visual Basic</span><span class="sxs-lookup"><span data-stu-id="01725-476">Visual Basic Language</span></span>](https://msdn.microsoft.com/library/2x7h1hfk.aspx)
