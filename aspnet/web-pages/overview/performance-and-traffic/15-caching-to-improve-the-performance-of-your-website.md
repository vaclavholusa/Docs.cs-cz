---
uid: web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
title: "Ukládání dat v rozhraní ASP.NET Web Pages (Razor) lokality pro dosažení vyššího výkonu | Microsoft Docs"
author: tfitzmac
description: "Můžete urychlit web tak, že ho uložit – to znamená, mezipaměti - výsledky data, která by obvykle trvat poměrně dlouho načíst nebo zpracovat..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/14/2014
ms.topic: article
ms.assetid: 961e525b-7700-469e-8a68-d7010b6fb68c
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/performance-and-traffic/15-caching-to-improve-the-performance-of-your-website
msc.type: authoredcontent
ms.openlocfilehash: 742409219bd3b05f8ddf2c0d5034919fc9bf1d26
ms.sourcegitcommit: 060879fcf3f73d2366b5c811986f8695fff65db8
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/24/2018
---
<a name="caching-data-in-an-aspnet-web-pages-razor-site-for-better-performance"></a><span data-ttu-id="874bc-103">Ukládání dat v Web Pages (Razor) technologie ASP.NET do mezipaměti pro dosažení vyššího výkonu</span><span class="sxs-lookup"><span data-stu-id="874bc-103">Caching Data in an ASP.NET Web Pages (Razor) Site for Better Performance</span></span>
====================
<span data-ttu-id="874bc-104">podle [tní FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="874bc-104">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="874bc-105">Tento článek vysvětluje, jak používat pomocná rutina pro informace o mezipaměti pro dosažení vyššího výkonu na webu technologie ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="874bc-105">This article explains how to use a helper to cache information for faster performance in an ASP.NET Web Pages (Razor) website.</span></span> <span data-ttu-id="874bc-106">Můžete zrychlení webu tak, že se úložiště &#8212; To znamená, mezipaměti &#8212; výsledky data, která normálně by trvat velmi dlouho načíst nebo zpracování a který se nemění často.</span><span class="sxs-lookup"><span data-stu-id="874bc-106">You can speed up your website by having it store &#8212; that is, cache &#8212; the results of data that ordinarily would take considerable time to retrieve or process and that does not change often.</span></span>
> 
> <span data-ttu-id="874bc-107">**Získáte informace:**</span><span class="sxs-lookup"><span data-stu-id="874bc-107">**What you'll learn:**</span></span> 
> 
> - <span data-ttu-id="874bc-108">Jak používat ukládání do mezipaměti zlepšit odezvu vašeho webu.</span><span class="sxs-lookup"><span data-stu-id="874bc-108">How to use caching to improve the responsiveness of your website.</span></span>
> 
> <span data-ttu-id="874bc-109">Toto jsou nové v článku funkce ASP.NET:</span><span class="sxs-lookup"><span data-stu-id="874bc-109">These are the ASP.NET features introduced in the article:</span></span>
> 
> - <span data-ttu-id="874bc-110">`WebCache` Pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="874bc-110">The `WebCache` helper.</span></span>
>   
> 
> ## <a name="software-versions-used-in-the-tutorial"></a><span data-ttu-id="874bc-111">V tomto kurzu použili verze softwaru</span><span class="sxs-lookup"><span data-stu-id="874bc-111">Software versions used in the tutorial</span></span>
> 
> 
> - <span data-ttu-id="874bc-112">Rozhraní ASP.NET Web Pages (Razor) 3</span><span class="sxs-lookup"><span data-stu-id="874bc-112">ASP.NET Web Pages (Razor) 3</span></span>
>   
> 
> <span data-ttu-id="874bc-113">V tomto kurzu taky spolupracuje se službou ASP.NET Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="874bc-113">This tutorial also works with ASP.NET Web Pages 2.</span></span>


<span data-ttu-id="874bc-114">Pokaždé, když se někdo požádá o stránku z vaší lokality, webový server má nějakou práci za účelem splnění požadavku.</span><span class="sxs-lookup"><span data-stu-id="874bc-114">Every time someone requests a page from your site, the web server has to do some work in order to fulfill the request.</span></span> <span data-ttu-id="874bc-115">Některé z vašich stránek server může být potřeba provádět úlohy, které trvat (poměrně) dlouhou dobu, jako je například načítání dat z databáze.</span><span class="sxs-lookup"><span data-stu-id="874bc-115">For some of your pages, the server might have to perform tasks that take a (comparatively) long time, such as retrieving data from a database.</span></span> <span data-ttu-id="874bc-116">I v případě, že tyto úlohy nejsou trvat dlouho v absolutních číslech, pokud váš web vyskytne velký objem provozu, celou řadu jednotlivých požadavků, které způsobí webový server k provedení úlohy složitý nebo pomalé můžete přidat do značné úsilí.</span><span class="sxs-lookup"><span data-stu-id="874bc-116">Even if these tasks don't take long in absolute terms, if your site experiences a lot of traffic, a whole series of individual requests that cause the web server to perform the complicated or slow task can add up to a lot of work.</span></span> <span data-ttu-id="874bc-117">Nakonec to může mít vliv na výkon webu.</span><span class="sxs-lookup"><span data-stu-id="874bc-117">This can ultimately affect the performance of the site.</span></span>

<span data-ttu-id="874bc-118">Jeden způsob, jak zlepšit výkon vašeho webu v případech, jako je to je ukládat data do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-118">One way to improve the performance of your website in circumstances like this is to cache data.</span></span> <span data-ttu-id="874bc-119">Pokud váš web získá opakované žádosti pro stejné informace a není potřeba upravit pro každou osobu, informace a není čas citlivé místo znovu načítání nebo přepočítání, můžete po načtení dat a potom uložte výsledky.</span><span class="sxs-lookup"><span data-stu-id="874bc-119">If your site gets repeated requests for the same information, and the information does not need to be modified for each person, and it's not time sensitive, instead of re-fetching or recalculating it, you can fetch the data once and then store the results.</span></span> <span data-ttu-id="874bc-120">Při příštím přijde žádost pro tento informace, jenom získáte ji z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-120">The next time a request comes in for that information, you just get it out of the cache.</span></span>

<span data-ttu-id="874bc-121">Obecně platí mezipaměti informace, které nemění příliš často.</span><span class="sxs-lookup"><span data-stu-id="874bc-121">In general, you cache information that doesn't change frequently.</span></span> <span data-ttu-id="874bc-122">Když vložíte informace v mezipaměti, uloží se do paměti na webovém serveru.</span><span class="sxs-lookup"><span data-stu-id="874bc-122">When you put information in the cache, it's stored in memory on the web server.</span></span> <span data-ttu-id="874bc-123">Můžete určit, jak dlouho se do mezipaměti, z sekund dnů.</span><span class="sxs-lookup"><span data-stu-id="874bc-123">You can specify how long it should be cached, from seconds to days.</span></span> <span data-ttu-id="874bc-124">Při ukládání do mezipaměti období vyprší, informace automaticky odebrány z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-124">When the caching period expires, the information is automatically removed from the cache.</span></span>

> [!NOTE]
> <span data-ttu-id="874bc-125">Položky v mezipaměti může odebrat z důvodů než jejich platnost vyprší.</span><span class="sxs-lookup"><span data-stu-id="874bc-125">Entries in the cache might be removed for reasons other than that they've expired.</span></span> <span data-ttu-id="874bc-126">Například webový server může dočasně nedostatek paměti a jedním ze způsobů, můžete ho získat paměť je aktivována položky z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-126">For example, the web server might temporarily run low on memory, and one way it can reclaim memory is by throwing entries out of the cache.</span></span> <span data-ttu-id="874bc-127">Jak zjistíte, i když jste vložíte informace do mezipaměti, budete muset Přesvědčte se, zda je stále existuje v případě potřeby.</span><span class="sxs-lookup"><span data-stu-id="874bc-127">As you'll see, even if you've put information into the cache, you have to check to be sure it's still there when you need it.</span></span>


<span data-ttu-id="874bc-128">Představte si, že má daný web stránku, která zobrazuje aktuální teploty a Prognóza počasí.</span><span class="sxs-lookup"><span data-stu-id="874bc-128">Imagine your website has a page that displays the current temperature and weather forecast.</span></span> <span data-ttu-id="874bc-129">Chcete-li získat tento typ informací, může odeslat požadavek na externí služby.</span><span class="sxs-lookup"><span data-stu-id="874bc-129">To get this type of information, you might send a request to an external service.</span></span> <span data-ttu-id="874bc-130">Vzhledem k tomu, že tyto informace nemění příliš hodně (v rámci dvou hodin časové období, např.) a vzhledem k tomu, že externí volání vyžadovat čas a šířku pásma, je vhodným kandidátem pro ukládání do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-130">Since this information doesn't change much (within a two-hour time period, for example) and since external calls require time and bandwidth, it's a good candidate for caching.</span></span>

## <a name="adding-caching-to-a-page"></a><span data-ttu-id="874bc-131">Přidání ukládání do mezipaměti na stránku</span><span class="sxs-lookup"><span data-stu-id="874bc-131">Adding Caching to a Page</span></span>

<span data-ttu-id="874bc-132">Technologie ASP.NET obsahuje `WebCache` pomocné rutiny, která umožňuje snadno přidat ukládání do mezipaměti na váš web a přidejte data do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-132">ASP.NET includes a `WebCache` helper that makes it easy to add caching to your site and add data to the cache.</span></span> <span data-ttu-id="874bc-133">V tomto postupu vytvoříte stránky, který ukládá do mezipaměti aktuální čas.</span><span class="sxs-lookup"><span data-stu-id="874bc-133">In this procedure, you'll create a page that caches the current time.</span></span> <span data-ttu-id="874bc-134">Tato akce není v příkladu reálného vzhledem k tomu, že aktuální čas je něco, často mění a která kromě toho není komplexní k výpočtu.</span><span class="sxs-lookup"><span data-stu-id="874bc-134">This isn't a real-world example, since the current time is something that does change often, and that moreover isn't complex to calculate.</span></span> <span data-ttu-id="874bc-135">Je však vhodné pro ilustraci ukládání do mezipaměti v akci.</span><span class="sxs-lookup"><span data-stu-id="874bc-135">However, it's a good way to illustrate caching in action.</span></span>

1. <span data-ttu-id="874bc-136">Přidat novou stránku s názvem *WebCache.cshtml* na web.</span><span class="sxs-lookup"><span data-stu-id="874bc-136">Add a new page named *WebCache.cshtml* to the website.</span></span>
2. <span data-ttu-id="874bc-137">Přidejte následující kód a kód na stránku:</span><span class="sxs-lookup"><span data-stu-id="874bc-137">Add the following code and markup to the page:</span></span>

    [!code-cshtml[Main](15-caching-to-improve-the-performance-of-your-website/samples/sample1.cshtml)]

    <span data-ttu-id="874bc-138">Když budete ukládat data do mezipaměti, které jste ji vložili do mezipaměti pomocí názvu to je v rámci webu jedinečný.</span><span class="sxs-lookup"><span data-stu-id="874bc-138">When you cache data, you put it into the cache using a name this is unique across the website.</span></span> <span data-ttu-id="874bc-139">V takovém případě použijete položky mezipaměti s názvem `CachedTime`.</span><span class="sxs-lookup"><span data-stu-id="874bc-139">In this case, you'll use a cache entry named `CachedTime`.</span></span> <span data-ttu-id="874bc-140">Toto je `cacheItemKey` uvedeno v příkladu kódu.</span><span class="sxs-lookup"><span data-stu-id="874bc-140">This is the `cacheItemKey` shown in the code example.</span></span>

    <span data-ttu-id="874bc-141">Nejprve čte kód `CachedTime` položky mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-141">The code first reads the `CachedTime` cache entry.</span></span> <span data-ttu-id="874bc-142">Pokud je vrácena hodnota (Pokud položka mezipaměti není null), kód právě nastaví hodnotu proměnné čas na data v mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-142">If a value is returned (that is, if the cache entry isn't null), the code just sets the value of the time variable to the cache data.</span></span>

    <span data-ttu-id="874bc-143">Ale pokud neexistuje položky mezipaměti (to znamená, je null), kód nastaví hodnotu čas, přidá do mezipaměti a nastaví hodnotu vypršení platnosti na jednu minutu.</span><span class="sxs-lookup"><span data-stu-id="874bc-143">However, if the cache entry doesn't exist (that is, it's null), the code sets the time value, adds it to the cache, and sets an expiration value to one minute.</span></span> <span data-ttu-id="874bc-144">Za minutu budou zahozeny položky mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-144">After one minute, the cache entry is discarded.</span></span> <span data-ttu-id="874bc-145">(Výchozí hodnota pro vypršení platnosti položky v mezipaměti je 20 minut). Příkaz `WebCache.Set(cacheItemKey, time, 1, false)` ukazuje, jak přidat aktuální hodnota času do mezipaměti a jeho vypršení platnosti nastavené na 1 minutu.</span><span class="sxs-lookup"><span data-stu-id="874bc-145">(The default expiration value for an item in the cache is 20 minutes.) The command `WebCache.Set(cacheItemKey, time, 1, false)` shows how to add the current time value to the cache and set its expiration to 1 minute.</span></span> <span data-ttu-id="874bc-146">Nastavení *parametr slidingExpiration* parametru `false` znamená čas vypršení platnosti neobnovíte pokaždé, když se požaduje.</span><span class="sxs-lookup"><span data-stu-id="874bc-146">Setting the *slidingExpiration* parameter to `false` means the expiration time is not renewed each time it is requested.</span></span> <span data-ttu-id="874bc-147">Vypršení platnosti přesně 1 minutu po původně byl přidán do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-147">It will expire exactly 1 minute after it was originally added to the cache.</span></span> <span data-ttu-id="874bc-148">Pokud nastavíte tuto hodnotu na `true` čas vypršení platnosti 1 minuta se vynuluje pokaždé, když je požadována hodnota z mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-148">If you set this value to `true` the 1 minute expiration time is reset each time the value is requested from the cache.</span></span>

    <span data-ttu-id="874bc-149">Tento kód ukazuje vzor, který byste měli vždycky používat při mezipaměti data.</span><span class="sxs-lookup"><span data-stu-id="874bc-149">This code illustrates the pattern you should always use when you cache data.</span></span> <span data-ttu-id="874bc-150">Než získáte něco z mezipaměti, vždy proveďte nejprve kontrolu jestli `WebCache.Get` Metoda vrátila hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="874bc-150">Before you get something out of the cache, always check first whether the `WebCache.Get` method has returned null.</span></span> <span data-ttu-id="874bc-151">Mějte na paměti, že položky mezipaměti vypršela nebo mohl být odebrán z jiného důvodu, takže všechny položky daného nikdy zaručena bezpečnost pro přístup do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-151">Remember that the cache entry might have expired or might have been removed for some other reason, so any given entry is never guaranteed to be in the cache.</span></span>
3. <span data-ttu-id="874bc-152">Spustit *WebCache.cshtml* v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="874bc-152">Run *WebCache.cshtml* in a browser.</span></span> <span data-ttu-id="874bc-153">(Ujistěte se, že je vybraný stránky v **soubory** pracovního prostoru, než ji spustit.) Při prvním požadavku na stránce časových dat není v mezipaměti a kód má pro přidání hodnoty time do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-153">(Make sure the page is selected in the **Files** workspace before you run it.) The first time you request the page, the time data isn't in the cache, and the code has to add the time value to the cache.</span></span>

    ![cache-1](15-caching-to-improve-the-performance-of-your-website/_static/image1.jpg)
4. <span data-ttu-id="874bc-155">Aktualizujte *WebCache.cshtml* v prohlížeči.</span><span class="sxs-lookup"><span data-stu-id="874bc-155">Refresh *WebCache.cshtml* in the browser.</span></span> <span data-ttu-id="874bc-156">Tentokrát časových dat je v mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-156">This time, the time data is in the cache.</span></span> <span data-ttu-id="874bc-157">Všimněte si, že se od posledního zobrazit stránku nezměnil čas.</span><span class="sxs-lookup"><span data-stu-id="874bc-157">Notice that the time hasn't changed since the last time you viewed the page.</span></span>

    ![cache-2](15-caching-to-improve-the-performance-of-your-website/_static/image2.jpg)
5. <span data-ttu-id="874bc-159">Počkejte minutu mezipaměti vyprázdnit a pak aktualizujte stránku.</span><span class="sxs-lookup"><span data-stu-id="874bc-159">Wait one minute for the cache to be emptied, and then refresh the page.</span></span> <span data-ttu-id="874bc-160">Stránky znovu označuje, že časových dat nebyl nalezen v mezipaměti, a čas poslední aktualizace je přidán do mezipaměti.</span><span class="sxs-lookup"><span data-stu-id="874bc-160">The page again indicates that the time data wasn't found in the cache, and the updated time is added to the cache.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="874bc-161">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="874bc-161">Additional Resources</span></span>


- [<span data-ttu-id="874bc-162">Zobrazení dat v grafu</span><span class="sxs-lookup"><span data-stu-id="874bc-162">Displaying Data in a Chart</span></span>](https://go.microsoft.com/fwlink/?LinkId=202895)
- <span data-ttu-id="874bc-163">[Referenční dokumentace rozhraní API WebCache](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span><span class="sxs-lookup"><span data-stu-id="874bc-163">[WebCache API reference](https://msdn.microsoft.com/library/system.web.helpers.webcache(v=vs.99).aspx) (MSDN)</span></span>
