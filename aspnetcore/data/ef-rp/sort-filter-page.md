---
title: Stránky Razor s EF Core v ASP.NET Core – řazení, filtrování, stránkování – 3 z 8
author: rick-anderson
description: V tomto kurzu přidáte řazení, filtrování a stránkování funkce pro stránky ASP.NET Core a Entity Framework Core.
ms.author: riande
ms.date: 6/31/2017
uid: data/ef-rp/sort-filter-page
ms.openlocfilehash: 668d6e54a64022dda948170f72538f6d4cd9a49f
ms.sourcegitcommit: b2723654af4969a24545f09ebe32004cb5e84a96
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/18/2018
ms.locfileid: "46011635"
---
# <a name="razor-pages-with-ef-core-in-aspnet-core---sort-filter-paging---3-of-8"></a><span data-ttu-id="179cf-103">Stránky Razor s EF Core v ASP.NET Core – řazení, filtrování, stránkování – 3 z 8</span><span class="sxs-lookup"><span data-stu-id="179cf-103">Razor Pages with EF Core in ASP.NET Core - Sort, Filter, Paging - 3 of 8</span></span>

[!INCLUDE[2.0 version](~/includes/RP-EF/20-pdf.md)]

::: moniker range=">= aspnetcore-2.1"

<span data-ttu-id="179cf-104">Podle [Petr Dykstra](https://github.com/tdykstra), [Rick Anderson](https://twitter.com/RickAndMSFT), a [Jan Macek P](https://twitter.com/thereformedprog)</span><span class="sxs-lookup"><span data-stu-id="179cf-104">By [Tom Dykstra](https://github.com/tdykstra), [Rick Anderson](https://twitter.com/RickAndMSFT), and [Jon P Smith](https://twitter.com/thereformedprog)</span></span>

[!INCLUDE [about the series](~/includes/RP-EF/intro.md)]

<span data-ttu-id="179cf-105">V tomto kurzu, řazení, filtrování, seskupování a stránkování se přidá funkce.</span><span class="sxs-lookup"><span data-stu-id="179cf-105">In this tutorial, sorting, filtering, grouping, and paging, functionality is added.</span></span>

<span data-ttu-id="179cf-106">Následující obrázek ukazuje stránku dokončené.</span><span class="sxs-lookup"><span data-stu-id="179cf-106">The following illustration shows a completed page.</span></span> <span data-ttu-id="179cf-107">Záhlaví sloupců jsou odkazy, můžete kliknout na seřadí hodnoty ve sloupci.</span><span class="sxs-lookup"><span data-stu-id="179cf-107">The column headings are clickable links to sort the column.</span></span> <span data-ttu-id="179cf-108">Kliknutím na záhlaví opakovaně sloupce Přepne mezi vzestupným a sestupným řazením.</span><span class="sxs-lookup"><span data-stu-id="179cf-108">Clicking a column heading repeatedly switches between ascending and descending sort order.</span></span>

![Studenti indexová stránka](sort-filter-page/_static/paging.png)

<span data-ttu-id="179cf-110">Pokud narazíte na potíže nelze vyřešit, stáhněte si [dokončené aplikace](https://github.com/aspnet/Docs/tree/master/aspnetcore/data/ef-rp/intro/samples).</span><span class="sxs-lookup"><span data-stu-id="179cf-110">If you run into problems you can't solve, download the [completed app](https://github.com/aspnet/Docs/tree/master/aspnetcore/data/ef-rp/intro/samples).</span></span>

## <a name="add-sorting-to-the-index-page"></a><span data-ttu-id="179cf-111">Přidání řazení na indexovou stránku</span><span class="sxs-lookup"><span data-stu-id="179cf-111">Add sorting to the Index page</span></span>

<span data-ttu-id="179cf-112">Přidejte do řetězce *Students/Index.cshtml.cs* `PageModel` tak, aby obsahovala řazení parametry:</span><span class="sxs-lookup"><span data-stu-id="179cf-112">Add strings to the *Students/Index.cshtml.cs* `PageModel` to contain the sorting parameters:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet1&highlight=10-13)]

<span data-ttu-id="179cf-113">Aktualizace *Students/Index.cshtml.cs* `OnGetAsync` následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="179cf-113">Update the *Students/Index.cshtml.cs* `OnGetAsync` with the following code:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortOnly)]

<span data-ttu-id="179cf-114">Předchozí kód přijme `sortOrder` parametr z řetězce dotazu v adrese URL.</span><span class="sxs-lookup"><span data-stu-id="179cf-114">The preceding code receives a `sortOrder` parameter from the query string in the URL.</span></span> <span data-ttu-id="179cf-115">Generuje adresu URL (včetně řetězce dotazu) [ukotvení pomocné rutiny značky](xref:mvc/views/tag-helpers/builtin-th/anchor-tag-helper
)</span><span class="sxs-lookup"><span data-stu-id="179cf-115">The URL (including the query string) is generated by the [Anchor Tag Helper](xref:mvc/views/tag-helpers/builtin-th/anchor-tag-helper
)</span></span>

<span data-ttu-id="179cf-116">`sortOrder` Je parametr "Name" nebo "Data".</span><span class="sxs-lookup"><span data-stu-id="179cf-116">The `sortOrder` parameter is either "Name" or "Date."</span></span> <span data-ttu-id="179cf-117">`sortOrder` Parametr může volitelně následovat "_desc" Zadejte sestupném pořadí.</span><span class="sxs-lookup"><span data-stu-id="179cf-117">The `sortOrder` parameter is optionally followed by "_desc" to specify descending order.</span></span> <span data-ttu-id="179cf-118">Je výchozí pořadí řazení vzestupně.</span><span class="sxs-lookup"><span data-stu-id="179cf-118">The default sort order is ascending.</span></span>

<span data-ttu-id="179cf-119">Když vyžádané indexovou stránku **studenty** propojení, neexistuje žádný řetězec dotazu.</span><span class="sxs-lookup"><span data-stu-id="179cf-119">When the Index page is requested from the **Students** link, there's no query string.</span></span> <span data-ttu-id="179cf-120">Studenty se zobrazují ve vzestupném pořadí podle jména.</span><span class="sxs-lookup"><span data-stu-id="179cf-120">The students are displayed in ascending order by last name.</span></span> <span data-ttu-id="179cf-121">V vzestupném pořadí podle příjmení je výchozí nastavení (případ propuštěním) `switch` příkazu.</span><span class="sxs-lookup"><span data-stu-id="179cf-121">Ascending order by last name is the default (fall-through case) in the `switch` statement.</span></span> <span data-ttu-id="179cf-122">Když uživatel klepne sloupce záhlaví odkaz, odpovídající `sortOrder` v hodnotě řetězce dotazu je zadaná hodnota.</span><span class="sxs-lookup"><span data-stu-id="179cf-122">When the user clicks a column heading link, the appropriate `sortOrder` value is provided in the query string value.</span></span>

<span data-ttu-id="179cf-123">`NameSort` a `DateSort` se použít ke konfiguraci hypertextové odkazy záhlaví sloupce s hodnotami řetězec dotazu odpovídající stránky Razor:</span><span class="sxs-lookup"><span data-stu-id="179cf-123">`NameSort` and `DateSort` are used by the Razor Page to configure the column heading hyperlinks with the appropriate query string values:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortOnly&highlight=3-4)]

<span data-ttu-id="179cf-124">Následující kód obsahuje podmíněné C# [?: – operátor](https://docs.microsoft.com/dotnet/csharp/language-reference/operators/conditional-operator):</span><span class="sxs-lookup"><span data-stu-id="179cf-124">The following code contains the C# conditional [?: operator](https://docs.microsoft.com/dotnet/csharp/language-reference/operators/conditional-operator):</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_Ternary)]

<span data-ttu-id="179cf-125">Určuje, že první řádek `sortOrder` má hodnotu null nebo je prázdný, `NameSort` je nastavena na "name_desc."</span><span class="sxs-lookup"><span data-stu-id="179cf-125">The first line specifies that when `sortOrder` is null or empty, `NameSort` is set to "name_desc."</span></span> <span data-ttu-id="179cf-126">Pokud `sortOrder` je **není** hodnotu null nebo je prázdný, `NameSort` je nastaven na prázdný řetězec.</span><span class="sxs-lookup"><span data-stu-id="179cf-126">If `sortOrder` is **not** null or empty, `NameSort` is set to an empty string.</span></span>

<span data-ttu-id="179cf-127">`?: operator` Se taky říká tříhodnotový operátor.</span><span class="sxs-lookup"><span data-stu-id="179cf-127">The `?: operator` is also known as the ternary operator.</span></span>

<span data-ttu-id="179cf-128">Tyto dva příkazy povolit na stránce nastavení sloupce záhlaví hypertextové odkazy následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="179cf-128">These two statements enable the page to set the column heading hyperlinks as follows:</span></span>

| <span data-ttu-id="179cf-129">Aktuální pořadí řazení</span><span class="sxs-lookup"><span data-stu-id="179cf-129">Current sort order</span></span> | <span data-ttu-id="179cf-130">Poslední název hypertextový odkaz</span><span class="sxs-lookup"><span data-stu-id="179cf-130">Last Name Hyperlink</span></span> | <span data-ttu-id="179cf-131">Datum hypertextový odkaz</span><span class="sxs-lookup"><span data-stu-id="179cf-131">Date Hyperlink</span></span> |
|:--------------------:|:-------------------:|:--------------:|
| <span data-ttu-id="179cf-132">Poslední název vzestupně</span><span class="sxs-lookup"><span data-stu-id="179cf-132">Last Name ascending</span></span> | <span data-ttu-id="179cf-133">descending</span><span class="sxs-lookup"><span data-stu-id="179cf-133">descending</span></span>        | <span data-ttu-id="179cf-134">ascending</span><span class="sxs-lookup"><span data-stu-id="179cf-134">ascending</span></span>      |
| <span data-ttu-id="179cf-135">Poslední název sestupně</span><span class="sxs-lookup"><span data-stu-id="179cf-135">Last Name descending</span></span> | <span data-ttu-id="179cf-136">ascending</span><span class="sxs-lookup"><span data-stu-id="179cf-136">ascending</span></span>           | <span data-ttu-id="179cf-137">ascending</span><span class="sxs-lookup"><span data-stu-id="179cf-137">ascending</span></span>      |
| <span data-ttu-id="179cf-138">Datum vzestupně</span><span class="sxs-lookup"><span data-stu-id="179cf-138">Date ascending</span></span>       | <span data-ttu-id="179cf-139">ascending</span><span class="sxs-lookup"><span data-stu-id="179cf-139">ascending</span></span>           | <span data-ttu-id="179cf-140">descending</span><span class="sxs-lookup"><span data-stu-id="179cf-140">descending</span></span>     |
| <span data-ttu-id="179cf-141">Datum sestupně</span><span class="sxs-lookup"><span data-stu-id="179cf-141">Date descending</span></span>      | <span data-ttu-id="179cf-142">ascending</span><span class="sxs-lookup"><span data-stu-id="179cf-142">ascending</span></span>           | <span data-ttu-id="179cf-143">ascending</span><span class="sxs-lookup"><span data-stu-id="179cf-143">ascending</span></span>      |

<span data-ttu-id="179cf-144">Metoda používá k určení tento sloupec seřadit podle technologie LINQ to Entities.</span><span class="sxs-lookup"><span data-stu-id="179cf-144">The method uses LINQ to Entities to specify the column to sort by.</span></span> <span data-ttu-id="179cf-145">Inicializuje kód `IQueryable<Student>` před příkazem switch a změní v příkazu switch:</span><span class="sxs-lookup"><span data-stu-id="179cf-145">The code initializes an `IQueryable<Student>` before the switch statement, and modifies it in the switch statement:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortOnly&highlight=6-999)]

 <span data-ttu-id="179cf-146">Když`IQueryable` vytvoření nebo úpravě, žádný dotaz odeslán do databáze.</span><span class="sxs-lookup"><span data-stu-id="179cf-146">When an`IQueryable` is created or modified, no query is sent to the database.</span></span> <span data-ttu-id="179cf-147">Dotaz není spuštěn až `IQueryable` objekt je převést do kolekce.</span><span class="sxs-lookup"><span data-stu-id="179cf-147">The query isn't executed until the `IQueryable` object is converted into a collection.</span></span> <span data-ttu-id="179cf-148">`IQueryable` převedou do kolekce voláním metody `ToListAsync`.</span><span class="sxs-lookup"><span data-stu-id="179cf-148">`IQueryable` are converted to a collection by calling a method such as `ToListAsync`.</span></span> <span data-ttu-id="179cf-149">Proto `IQueryable` kódu výsledky v jediném dotazu, který se spustí až do následujícího příkazu:</span><span class="sxs-lookup"><span data-stu-id="179cf-149">Therefore, the `IQueryable` code results in a single query that's not executed until the following statement:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortOnlyRtn)]

<span data-ttu-id="179cf-150">`OnGetAsync` může získat podrobné s velkým množstvím řazení sloupců.</span><span class="sxs-lookup"><span data-stu-id="179cf-150">`OnGetAsync` could get verbose with a large number of sortable columns.</span></span>

### <a name="add-column-heading-hyperlinks-to-the-student-index-page"></a><span data-ttu-id="179cf-151">Přidat záhlaví sloupce hypertextové odkazy na studenta indexovou stránku</span><span class="sxs-lookup"><span data-stu-id="179cf-151">Add column heading hyperlinks to the Student Index page</span></span>

<span data-ttu-id="179cf-152">Nahraďte kód v *Students/Index.cshtml*, s následující zvýrazněný kód:</span><span class="sxs-lookup"><span data-stu-id="179cf-152">Replace the code in *Students/Index.cshtml*, with the following highlighted code:</span></span>

[!code-html[](intro/samples/cu21/Pages/Students/Index2.cshtml?highlight=17-19,25-27)]

<span data-ttu-id="179cf-153">Předchozí kód:</span><span class="sxs-lookup"><span data-stu-id="179cf-153">The preceding code:</span></span>

* <span data-ttu-id="179cf-154">Přidá odkazy `LastName` a `EnrollmentDate` záhlaví sloupců.</span><span class="sxs-lookup"><span data-stu-id="179cf-154">Adds hyperlinks to the `LastName` and `EnrollmentDate` column headings.</span></span>
* <span data-ttu-id="179cf-155">Pomocí informací v `NameSort` a `DateSort` nastavit hypertextové odkazy pomocí aktuálních hodnot pořadí řazení.</span><span class="sxs-lookup"><span data-stu-id="179cf-155">Uses the information in `NameSort` and `DateSort` to set up hyperlinks with the current sort order values.</span></span>

<span data-ttu-id="179cf-156">K ověření funkčnosti řazení:</span><span class="sxs-lookup"><span data-stu-id="179cf-156">To verify that sorting works:</span></span>

* <span data-ttu-id="179cf-157">Spusťte aplikaci a vyberte **studenty** kartu.</span><span class="sxs-lookup"><span data-stu-id="179cf-157">Run the app and select the **Students** tab.</span></span>
* <span data-ttu-id="179cf-158">Klikněte na tlačítko **příjmení**.</span><span class="sxs-lookup"><span data-stu-id="179cf-158">Click **Last Name**.</span></span>
* <span data-ttu-id="179cf-159">Klikněte na tlačítko **datum registrace**.</span><span class="sxs-lookup"><span data-stu-id="179cf-159">Click **Enrollment Date**.</span></span>

<span data-ttu-id="179cf-160">Pokud chcete získat lepší přehled kódu:</span><span class="sxs-lookup"><span data-stu-id="179cf-160">To get a better understanding of the code:</span></span>

* <span data-ttu-id="179cf-161">V *Student/Index.cshtml.cs*, nastavit zarážku na `switch (sortOrder)`.</span><span class="sxs-lookup"><span data-stu-id="179cf-161">In *Student/Index.cshtml.cs*, set a breakpoint on `switch (sortOrder)`.</span></span>
* <span data-ttu-id="179cf-162">Přidat kukátko pro `NameSort` a `DateSort`.</span><span class="sxs-lookup"><span data-stu-id="179cf-162">Add a watch for `NameSort` and `DateSort`.</span></span>
* <span data-ttu-id="179cf-163">V *Student/Index.cshtml*, nastavit zarážku na `@Html.DisplayNameFor(model => model.Student[0].LastName)`.</span><span class="sxs-lookup"><span data-stu-id="179cf-163">In *Student/Index.cshtml*, set a breakpoint on `@Html.DisplayNameFor(model => model.Student[0].LastName)`.</span></span>

<span data-ttu-id="179cf-164">Projděte skrze ladicí program.</span><span class="sxs-lookup"><span data-stu-id="179cf-164">Step through the debugger.</span></span>

## <a name="add-a-search-box-to-the-students-index-page"></a><span data-ttu-id="179cf-165">Přidání vyhledávacího pole na studenty indexovou stránku</span><span class="sxs-lookup"><span data-stu-id="179cf-165">Add a Search Box to the Students Index page</span></span>

<span data-ttu-id="179cf-166">Chcete-li přidat filtrování pro studenty indexovou stránku:</span><span class="sxs-lookup"><span data-stu-id="179cf-166">To add filtering to the Students Index page:</span></span>

* <span data-ttu-id="179cf-167">Textové pole a tlačítko pro odeslání se přidá do stránky Razor.</span><span class="sxs-lookup"><span data-stu-id="179cf-167">A text box and a submit button is added to the Razor Page.</span></span> <span data-ttu-id="179cf-168">Do textového pole poskytuje hledaný řetězec v první nebo poslední název.</span><span class="sxs-lookup"><span data-stu-id="179cf-168">The text box supplies a search string on the first or last name.</span></span>
* <span data-ttu-id="179cf-169">Model stránky v aktualizaci hodnotu textového pole.</span><span class="sxs-lookup"><span data-stu-id="179cf-169">The page model is updated to use the text box value.</span></span>

### <a name="add-filtering-functionality-to-the-index-method"></a><span data-ttu-id="179cf-170">Filtrování doplňují Index – metoda</span><span class="sxs-lookup"><span data-stu-id="179cf-170">Add filtering functionality to the Index method</span></span>

<span data-ttu-id="179cf-171">Aktualizace *Students/Index.cshtml.cs* `OnGetAsync` následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="179cf-171">Update the *Students/Index.cshtml.cs* `OnGetAsync` with the following code:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortFilter&highlight=1,5,9-13)]

<span data-ttu-id="179cf-172">Předchozí kód:</span><span class="sxs-lookup"><span data-stu-id="179cf-172">The preceding code:</span></span>

* <span data-ttu-id="179cf-173">Přidá `searchString` parametr `OnGetAsync` metody.</span><span class="sxs-lookup"><span data-stu-id="179cf-173">Adds the `searchString` parameter to the `OnGetAsync` method.</span></span> <span data-ttu-id="179cf-174">Přijetí hledání řetězcovou hodnotu z textového pole, který je přidán v další části.</span><span class="sxs-lookup"><span data-stu-id="179cf-174">The search string value is received from a text box that's added in the next section.</span></span>
* <span data-ttu-id="179cf-175">Přidání příkazu LINQ `Where` klauzuli.</span><span class="sxs-lookup"><span data-stu-id="179cf-175">Added to the LINQ statement a `Where` clause.</span></span> <span data-ttu-id="179cf-176">`Where` Klauzule vybere pouze studenti, jejichž křestní jméno nebo příjmení vyskytuje hledaný řetězec.</span><span class="sxs-lookup"><span data-stu-id="179cf-176">The `Where` clause selects only students whose first name or last name contains the search string.</span></span> <span data-ttu-id="179cf-177">Pouze v případě, že je hodnota k vyhledání, je proveden příkaz LINQ.</span><span class="sxs-lookup"><span data-stu-id="179cf-177">The LINQ statement is executed only if there's a value to search for.</span></span>

<span data-ttu-id="179cf-178">Poznámka: Předchozí kód volá `Where` metodu `IQueryable` objekt a filtr zpracování na serveru.</span><span class="sxs-lookup"><span data-stu-id="179cf-178">Note: The preceding code calls the `Where` method on an `IQueryable` object, and the filter is processed on the server.</span></span> <span data-ttu-id="179cf-179">V některých případech může volat aplikaci `Where` metody jako metody rozšíření v kolekci v paměti.</span><span class="sxs-lookup"><span data-stu-id="179cf-179">In some scenarios, the app might be calling the `Where` method as an extension method on an in-memory collection.</span></span> <span data-ttu-id="179cf-180">Předpokládejme například, že `_context.Students` změní z EF Core `DbSet` úložiště metody, která se vrátí `IEnumerable` kolekce.</span><span class="sxs-lookup"><span data-stu-id="179cf-180">For example, suppose `_context.Students` changes from EF Core `DbSet` to a repository method that returns an `IEnumerable` collection.</span></span> <span data-ttu-id="179cf-181">Výsledek by obvykle stejný, ale v některých případech může lišit.</span><span class="sxs-lookup"><span data-stu-id="179cf-181">The result would normally be the same but in some cases may be different.</span></span>

<span data-ttu-id="179cf-182">Například rozhraní .NET Framework provádění `Contains` ve výchozím nastavení provádí porovnání velká a malá písmena.</span><span class="sxs-lookup"><span data-stu-id="179cf-182">For example, the .NET Framework implementation of `Contains` performs a case-sensitive comparison by default.</span></span> <span data-ttu-id="179cf-183">V systému SQL Server `Contains` rozlišování je určeno nastavení kolace instance systému SQL Server.</span><span class="sxs-lookup"><span data-stu-id="179cf-183">In SQL Server, `Contains` case-sensitivity is determined by the collation setting of the SQL Server instance.</span></span> <span data-ttu-id="179cf-184">SQL Server výchozí hodnoty na velká a malá písmena.</span><span class="sxs-lookup"><span data-stu-id="179cf-184">SQL Server defaults to case-insensitive.</span></span> <span data-ttu-id="179cf-185">`ToUpper` může volat provést test explicitně velká a malá písmena:</span><span class="sxs-lookup"><span data-stu-id="179cf-185">`ToUpper` could be called to make the test explicitly case-insensitive:</span></span>

`Where(s => s.LastName.ToUpper().Contains(searchString.ToUpper())`

<span data-ttu-id="179cf-186">Předchozí kód zajistí, že výsledky jsou malá a velká písmena Pokud se změní kód určený `IEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="179cf-186">The preceding code would ensure that results are case-insensitive if the code changes to use `IEnumerable`.</span></span> <span data-ttu-id="179cf-187">Když `Contains` je volán na `IEnumerable` kolekce, .NET Core, který se použije implementace.</span><span class="sxs-lookup"><span data-stu-id="179cf-187">When `Contains` is called on an `IEnumerable` collection, the .NET Core implementation is used.</span></span> <span data-ttu-id="179cf-188">Když `Contains` je volán na `IQueryable` objektu se použije implementace databáze.</span><span class="sxs-lookup"><span data-stu-id="179cf-188">When `Contains` is called on an `IQueryable` object, the database implementation is used.</span></span> <span data-ttu-id="179cf-189">Vrácení `IEnumerable` z úložiště může mít penality výkonu:</span><span class="sxs-lookup"><span data-stu-id="179cf-189">Returning an `IEnumerable` from a repository can have a significant performance penality:</span></span>

1. <span data-ttu-id="179cf-190">Všechny řádky jsou vráceny z Databázového serveru.</span><span class="sxs-lookup"><span data-stu-id="179cf-190">All the rows are returned from the DB server.</span></span>
1. <span data-ttu-id="179cf-191">Filtr platí pro všechny řádky vrácené v aplikaci.</span><span class="sxs-lookup"><span data-stu-id="179cf-191">The filter is applied to all the returned rows in the application.</span></span>

<span data-ttu-id="179cf-192">Je snížení výkonu pro volání `ToUpper`.</span><span class="sxs-lookup"><span data-stu-id="179cf-192">There's a performance penalty for calling `ToUpper`.</span></span> <span data-ttu-id="179cf-193">`ToUpper` Kód přidá funkce v klauzuli WHERE příkazu TSQL SELECT.</span><span class="sxs-lookup"><span data-stu-id="179cf-193">The `ToUpper` code adds a function in the WHERE clause of the TSQL SELECT statement.</span></span> <span data-ttu-id="179cf-194">Přidání funkce zabraňuje Optimalizátor pomocí indexu.</span><span class="sxs-lookup"><span data-stu-id="179cf-194">The added function prevents the optimizer from using an index.</span></span> <span data-ttu-id="179cf-195">Vzhledem k tomu, že SQL je nainstalován jako velkých a malých písmen, je nejlepší je vyhnout `ToUpper` volat, pokud není potřeba.</span><span class="sxs-lookup"><span data-stu-id="179cf-195">Given that SQL is installed as case-insensitive, it's best to avoid the `ToUpper` call when it's not needed.</span></span>

### <a name="add-a-search-box-to-the-student-index-page"></a><span data-ttu-id="179cf-196">Přidání vyhledávacího pole na studenta indexovou stránku</span><span class="sxs-lookup"><span data-stu-id="179cf-196">Add a Search Box to the Student Index page</span></span>

<span data-ttu-id="179cf-197">V *Pages/Students/Index.cshtml*, přidejte následující zvýrazněný kód k vytvoření **hledání** tlačítko a různé chrome.</span><span class="sxs-lookup"><span data-stu-id="179cf-197">In *Pages/Students/Index.cshtml*, add the following highlighted code to create a **Search** button and assorted chrome.</span></span>

[!code-html[](intro/samples/cu21/Pages/Students/Index3.cshtml?highlight=14-23&range=1-25)]

<span data-ttu-id="179cf-198">Předchozí kód používá `<form>` [pomocné rutiny značky](xref:mvc/views/tag-helpers/intro) přidáte textové pole hledání a tlačítko.</span><span class="sxs-lookup"><span data-stu-id="179cf-198">The preceding code uses the `<form>` [tag helper](xref:mvc/views/tag-helpers/intro) to add the search text box and button.</span></span> <span data-ttu-id="179cf-199">Ve výchozím nastavení `<form>` pomocné rutiny značky odešle formulář dat pomocí příspěvek.</span><span class="sxs-lookup"><span data-stu-id="179cf-199">By default, the `<form>` tag helper submits form data with a POST.</span></span> <span data-ttu-id="179cf-200">Příspěvku parametry jsou předány v textu zprávy HTTP a ne v adrese URL.</span><span class="sxs-lookup"><span data-stu-id="179cf-200">With POST, the parameters are passed in the HTTP message body and not in the URL.</span></span> <span data-ttu-id="179cf-201">Při použití HTTP GET data formuláře je předána v adrese URL jako řetězce dotazu.</span><span class="sxs-lookup"><span data-stu-id="179cf-201">When HTTP GET is used, the form data is passed in the URL as query strings.</span></span> <span data-ttu-id="179cf-202">Předávání dat s řetězci dotazu umožňuje uživatelům adresa URL záložky.</span><span class="sxs-lookup"><span data-stu-id="179cf-202">Passing the data with query strings enables users to bookmark the URL.</span></span> <span data-ttu-id="179cf-203">[W3C pokyny](https://www.w3.org/2001/tag/doc/whenToUseGet.html) doporučujeme, aby GET má být použit při akci nevede k aktualizaci.</span><span class="sxs-lookup"><span data-stu-id="179cf-203">The [W3C guidelines](https://www.w3.org/2001/tag/doc/whenToUseGet.html) recommend that GET should be used when the action doesn't result in an update.</span></span>

<span data-ttu-id="179cf-204">Testování aplikace:</span><span class="sxs-lookup"><span data-stu-id="179cf-204">Test the app:</span></span>

* <span data-ttu-id="179cf-205">Vyberte **studenty** kartě a zadejte hledaný řetězec.</span><span class="sxs-lookup"><span data-stu-id="179cf-205">Select the **Students** tab and enter a search string.</span></span>
* <span data-ttu-id="179cf-206">Vyberte **hledání**.</span><span class="sxs-lookup"><span data-stu-id="179cf-206">Select **Search**.</span></span>

<span data-ttu-id="179cf-207">Všimněte si, že adresa URL vyskytuje hledaný řetězec.</span><span class="sxs-lookup"><span data-stu-id="179cf-207">Notice that the URL contains the search string.</span></span>

```html
http://localhost:5000/Students?SearchString=an
```

<span data-ttu-id="179cf-208">Pokud je vytvořili záložku na stránce, na záložku obsahuje adresu URL stránky a `SearchString` řetězec dotazu.</span><span class="sxs-lookup"><span data-stu-id="179cf-208">If the page is bookmarked, the bookmark contains the URL to the page and the `SearchString` query string.</span></span> <span data-ttu-id="179cf-209">`method="get"` v `form` značka je, co způsobilo řetězec dotazu, který nevygeneruje.</span><span class="sxs-lookup"><span data-stu-id="179cf-209">The `method="get"` in the `form` tag is what caused the query string to be generated.</span></span>

<span data-ttu-id="179cf-210">V současné době vyberete řazení odkaz záhlaví sloupce filtru hodnotu **hledání** pole se ztratí.</span><span class="sxs-lookup"><span data-stu-id="179cf-210">Currently, when a column heading sort link is selected, the filter value from the **Search** box is lost.</span></span> <span data-ttu-id="179cf-211">V další části má pevnou hodnotu ztráty filtru.</span><span class="sxs-lookup"><span data-stu-id="179cf-211">The lost filter value is fixed in the next section.</span></span>

## <a name="add-paging-functionality-to-the-students-index-page"></a><span data-ttu-id="179cf-212">Přidání funkce stránkování na studenty indexovou stránku</span><span class="sxs-lookup"><span data-stu-id="179cf-212">Add paging functionality to the Students Index page</span></span>

<span data-ttu-id="179cf-213">V této části `PaginatedList` je vytvořená třída pro podporu stránkování.</span><span class="sxs-lookup"><span data-stu-id="179cf-213">In this section, a `PaginatedList` class is created to support paging.</span></span> <span data-ttu-id="179cf-214">`PaginatedList` Třídy používá `Skip` a `Take` příkazy k filtrování dat na serveru, místo získávání všech řádků v tabulce.</span><span class="sxs-lookup"><span data-stu-id="179cf-214">The `PaginatedList` class uses `Skip` and `Take` statements to filter data on the server instead of retrieving all rows of the table.</span></span> <span data-ttu-id="179cf-215">Následující obrázek znázorňuje tlačítka stránkování.</span><span class="sxs-lookup"><span data-stu-id="179cf-215">The following illustration shows the paging buttons.</span></span>

![Studenti index stránky s odkazy stránkování](sort-filter-page/_static/paging.png)

<span data-ttu-id="179cf-217">Ve složce projektu vytvořit `PaginatedList.cs` následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="179cf-217">In the project folder, create `PaginatedList.cs` with the following code:</span></span>

[!code-csharp[](intro/samples/cu21/PaginatedList.cs)]

<span data-ttu-id="179cf-218">`CreateAsync` Metoda v předchozím kódu má velikost stránky a číslo stránky a použije příslušné `Skip` a `Take` příkazy `IQueryable`.</span><span class="sxs-lookup"><span data-stu-id="179cf-218">The `CreateAsync` method in the preceding code takes page size and page number and applies the appropriate `Skip` and `Take` statements to the `IQueryable`.</span></span> <span data-ttu-id="179cf-219">Když `ToListAsync` je volán na `IQueryable`, vrací seznam obsahující pouze požadované stránky.</span><span class="sxs-lookup"><span data-stu-id="179cf-219">When `ToListAsync` is called on the `IQueryable`, it returns a List containing only the requested page.</span></span> <span data-ttu-id="179cf-220">Vlastnosti `HasPreviousPage` a `HasNextPage` slouží k povolení nebo zakázání **předchozí** a **Další** tlačítka stránkování.</span><span class="sxs-lookup"><span data-stu-id="179cf-220">The properties `HasPreviousPage` and `HasNextPage` are used to enable or disable **Previous** and **Next** paging buttons.</span></span>

<span data-ttu-id="179cf-221">`CreateAsync` Metoda se používá k vytvoření `PaginatedList<T>`.</span><span class="sxs-lookup"><span data-stu-id="179cf-221">The `CreateAsync` method is used to create the `PaginatedList<T>`.</span></span> <span data-ttu-id="179cf-222">Nelze vytvořit konstruktor `PaginatedList<T>` objektu, konstruktory nelze spustit asynchronního kódu.</span><span class="sxs-lookup"><span data-stu-id="179cf-222">A constructor can't create the `PaginatedList<T>` object, constructors can't run asynchronous code.</span></span>

## <a name="add-paging-functionality-to-the-index-method"></a><span data-ttu-id="179cf-223">Přidání funkce stránkování Index – metoda</span><span class="sxs-lookup"><span data-stu-id="179cf-223">Add paging functionality to the Index method</span></span>

<span data-ttu-id="179cf-224">V *Students/Index.cshtml.cs*, aktualizujte typ `Student` z `IList<Student>` k `PaginatedList<Student>`:</span><span class="sxs-lookup"><span data-stu-id="179cf-224">In *Students/Index.cshtml.cs*, update the type of `Student` from `IList<Student>` to `PaginatedList<Student>`:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortFilterPageType)]

<span data-ttu-id="179cf-225">Aktualizace *Students/Index.cshtml.cs* `OnGetAsync` následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="179cf-225">Update the *Students/Index.cshtml.cs* `OnGetAsync` with the following code:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortFilterPage&highlight=1-4,7-14,41-999)]

<span data-ttu-id="179cf-226">Předchozí kód přidá index stránky, aktuální `sortOrder`a `currentFilter` do podpisu metody.</span><span class="sxs-lookup"><span data-stu-id="179cf-226">The preceding code adds the page index, the current `sortOrder`, and the `currentFilter` to the method signature.</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortFilterPage2)]

<span data-ttu-id="179cf-227">Všechny parametry mají hodnotu null při:</span><span class="sxs-lookup"><span data-stu-id="179cf-227">All the parameters are null when:</span></span>

* <span data-ttu-id="179cf-228">Na stránce funkce je volána z **studenty** odkaz.</span><span class="sxs-lookup"><span data-stu-id="179cf-228">The page is called from the **Students** link.</span></span>
* <span data-ttu-id="179cf-229">Uživatel nebyl kliknul stránkování a řazení odkaz.</span><span class="sxs-lookup"><span data-stu-id="179cf-229">The user hasn't clicked a paging or sorting link.</span></span>

<span data-ttu-id="179cf-230">Po kliknutí na odkaz stránkování indexovaná proměnná stránky obsahuje číslo stránky k zobrazení.</span><span class="sxs-lookup"><span data-stu-id="179cf-230">When a paging link is clicked, the page index variable contains the page number to display.</span></span>

<span data-ttu-id="179cf-231">`CurrentSort` Stránka Razor poskytuje aktuální pořadí řazení.</span><span class="sxs-lookup"><span data-stu-id="179cf-231">`CurrentSort` provides the Razor Page with the current sort order.</span></span> <span data-ttu-id="179cf-232">Aktuální pořadí řazení, musí být součástí odkazy stránkování zachovat pořadí řazení a stránkování.</span><span class="sxs-lookup"><span data-stu-id="179cf-232">The current sort order must be included in the paging links to keep the sort order while paging.</span></span>

<span data-ttu-id="179cf-233">`CurrentFilter` Stránka Razor poskytuje aktuální řetězec filtru.</span><span class="sxs-lookup"><span data-stu-id="179cf-233">`CurrentFilter` provides the Razor Page with the current filter string.</span></span> <span data-ttu-id="179cf-234">`CurrentFilter` Hodnotu:</span><span class="sxs-lookup"><span data-stu-id="179cf-234">The `CurrentFilter` value:</span></span>

* <span data-ttu-id="179cf-235">Musí být součástí odkazy stránkování pro zachování nastavení filtru během stránkování.</span><span class="sxs-lookup"><span data-stu-id="179cf-235">Must be included in the paging links in order to maintain the filter settings during paging.</span></span>
* <span data-ttu-id="179cf-236">Musíte do textového pole. až se obnoví na stránce se zobrazí znovu.</span><span class="sxs-lookup"><span data-stu-id="179cf-236">Must be restored to the text box when the page is redisplayed.</span></span>

<span data-ttu-id="179cf-237">Pokud hledaný řetězec se změní při stránkování, stránky se resetuje na hodnotu 1.</span><span class="sxs-lookup"><span data-stu-id="179cf-237">If the search string is changed while paging, the page is reset to 1.</span></span> <span data-ttu-id="179cf-238">Na stránce musí obnovit na hodnotu 1, protože nový filtr může vést k jiné data se mají zobrazit.</span><span class="sxs-lookup"><span data-stu-id="179cf-238">The page has to be reset to 1 because the new filter can result in different data to display.</span></span> <span data-ttu-id="179cf-239">Pokud je zadána hodnota vyhledávání a **odeslat** je vybrán:</span><span class="sxs-lookup"><span data-stu-id="179cf-239">When a search value is entered and **Submit** is selected:</span></span>

* <span data-ttu-id="179cf-240">Hledaný řetězec se změní.</span><span class="sxs-lookup"><span data-stu-id="179cf-240">The search string is changed.</span></span>
* <span data-ttu-id="179cf-241">`searchString` Parametr není null.</span><span class="sxs-lookup"><span data-stu-id="179cf-241">The `searchString` parameter isn't null.</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortFilterPage3)]

<span data-ttu-id="179cf-242">`PaginatedList.CreateAsync` Metoda převede student dotazu na jednu stránku studentů v typu kolekce, který podporuje stránkování.</span><span class="sxs-lookup"><span data-stu-id="179cf-242">The `PaginatedList.CreateAsync` method converts the student query to a single page of students in a collection type that supports paging.</span></span> <span data-ttu-id="179cf-243">Této stránce studentů je předán stránky Razor.</span><span class="sxs-lookup"><span data-stu-id="179cf-243">That single page of students is passed to the Razor Page.</span></span>

[!code-csharp[](intro/samples/cu21/Pages/Students/Index.cshtml.cs?name=snippet_SortFilterPage4)]

<span data-ttu-id="179cf-244">Dvě otazníky v `PaginatedList.CreateAsync` představují [operátoru nulového sjednocení](/dotnet/csharp/language-reference/operators/null-conditional-operator).</span><span class="sxs-lookup"><span data-stu-id="179cf-244">The two question marks in `PaginatedList.CreateAsync` represent the [null-coalescing operator](/dotnet/csharp/language-reference/operators/null-conditional-operator).</span></span> <span data-ttu-id="179cf-245">Operátoru nulového sjednocení definuje výchozí hodnotu pro typ připouštějící hodnotu Null.</span><span class="sxs-lookup"><span data-stu-id="179cf-245">The null-coalescing operator defines a default value for a nullable type.</span></span> <span data-ttu-id="179cf-246">Výraz `(pageIndex ?? 1)` znamená, že návratová hodnota z `pageIndex` Pokud má hodnotu.</span><span class="sxs-lookup"><span data-stu-id="179cf-246">The expression `(pageIndex ?? 1)` means return the value of `pageIndex` if it has a value.</span></span> <span data-ttu-id="179cf-247">Pokud `pageIndex` nebude mít hodnotu, vrátí se 1.</span><span class="sxs-lookup"><span data-stu-id="179cf-247">If `pageIndex` doesn't have a value, return 1.</span></span>

## <a name="add-paging-links-to-the-student-razor-page"></a><span data-ttu-id="179cf-248">Přidat odkazy stránkování na studenta stránky Razor</span><span class="sxs-lookup"><span data-stu-id="179cf-248">Add paging links to the student Razor Page</span></span>

<span data-ttu-id="179cf-249">Aktualizovat kód v *Students/Index.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="179cf-249">Update the markup in *Students/Index.cshtml*.</span></span> <span data-ttu-id="179cf-250">Změny jsou zvýrazněny:</span><span class="sxs-lookup"><span data-stu-id="179cf-250">The changes are highlighted:</span></span>

[!code-html[](intro/samples/cu21/Pages/Students/Index.cshtml?highlight=28-31,37-40,68-999)]

<span data-ttu-id="179cf-251">Odkazy záhlaví sloupce použijte řetězec dotazu k předání do aktuálního vyhledávaného řetězce `OnGetAsync` metodu tak, aby uživatel mohl třídit v rámci filtr výsledků:</span><span class="sxs-lookup"><span data-stu-id="179cf-251">The column header links use the query string to pass the current search string to the `OnGetAsync` method so that the user can sort within filter results:</span></span>

[!code-html[](intro/samples/cu21/Pages/Students/Index.cshtml?range=28-31)]

<span data-ttu-id="179cf-252">Mají tlačítka stránkování podle pomocných rutin značek:</span><span class="sxs-lookup"><span data-stu-id="179cf-252">The paging buttons are displayed by tag helpers:</span></span>

[!code-html[](intro/samples/cu21/Pages/Students/Index.cshtml?range=72-)]

<span data-ttu-id="179cf-253">Spusťte aplikaci a přejděte na stránku pro studenty.</span><span class="sxs-lookup"><span data-stu-id="179cf-253">Run the app and navigate to the students page.</span></span>

* <span data-ttu-id="179cf-254">Ujistěte se, že funguje stránkování, klikněte na odkazy stránkování v jiné pořadí řazení.</span><span class="sxs-lookup"><span data-stu-id="179cf-254">To make sure paging works, click the paging links in different sort orders.</span></span>
* <span data-ttu-id="179cf-255">K ověření, zda stránkování správně funguje s řazením a filtrováním, zadejte hledaný řetězec a stránkování.</span><span class="sxs-lookup"><span data-stu-id="179cf-255">To verify that paging works correctly with sorting and filtering, enter a search string and try paging.</span></span>

![Studenti index stránky s odkazy stránkování](sort-filter-page/_static/paging.png)

<span data-ttu-id="179cf-257">Pokud chcete získat lepší přehled kódu:</span><span class="sxs-lookup"><span data-stu-id="179cf-257">To get a better understanding of the code:</span></span>

* <span data-ttu-id="179cf-258">V *Student/Index.cshtml.cs*, nastavit zarážku na `switch (sortOrder)`.</span><span class="sxs-lookup"><span data-stu-id="179cf-258">In *Student/Index.cshtml.cs*, set a breakpoint on `switch (sortOrder)`.</span></span>
* <span data-ttu-id="179cf-259">Přidat kukátko pro `NameSort`, `DateSort`, `CurrentSort`, a `Model.Student.PageIndex`.</span><span class="sxs-lookup"><span data-stu-id="179cf-259">Add a watch for `NameSort`, `DateSort`, `CurrentSort`, and `Model.Student.PageIndex`.</span></span>
* <span data-ttu-id="179cf-260">V *Student/Index.cshtml*, nastavit zarážku na `@Html.DisplayNameFor(model => model.Student[0].LastName)`.</span><span class="sxs-lookup"><span data-stu-id="179cf-260">In *Student/Index.cshtml*, set a breakpoint on `@Html.DisplayNameFor(model => model.Student[0].LastName)`.</span></span>

<span data-ttu-id="179cf-261">Projděte skrze ladicí program.</span><span class="sxs-lookup"><span data-stu-id="179cf-261">Step through the debugger.</span></span>

## <a name="update-the-about-page-to-show-student-statistics"></a><span data-ttu-id="179cf-262">Aktualizovat o stránky zobrazit statistiku studenta</span><span class="sxs-lookup"><span data-stu-id="179cf-262">Update the About page to show student statistics</span></span>

<span data-ttu-id="179cf-263">V tomto kroku *Pages/About.cshtml* se aktualizuje a zobrazí, kolik studenty zaregistrovali pro každé datum registrace.</span><span class="sxs-lookup"><span data-stu-id="179cf-263">In this step, *Pages/About.cshtml* is updated to display how many students have enrolled for each enrollment date.</span></span> <span data-ttu-id="179cf-264">Aktualizace používá seskupování a zahrnuje následující kroky:</span><span class="sxs-lookup"><span data-stu-id="179cf-264">The update uses grouping and includes the following steps:</span></span>

* <span data-ttu-id="179cf-265">Vytvoření modelu zobrazení pro data používaná **o** stránky.</span><span class="sxs-lookup"><span data-stu-id="179cf-265">Create a view model for the data used by the **About** Page.</span></span>
* <span data-ttu-id="179cf-266">Aktualizujte stránku o použití modelu zobrazení.</span><span class="sxs-lookup"><span data-stu-id="179cf-266">Update the About page to use the view model.</span></span>

### <a name="create-the-view-model"></a><span data-ttu-id="179cf-267">Vytvoření zobrazení modelu</span><span class="sxs-lookup"><span data-stu-id="179cf-267">Create the view model</span></span>

<span data-ttu-id="179cf-268">Vytvoření *SchoolViewModels* složky *modely* složky.</span><span class="sxs-lookup"><span data-stu-id="179cf-268">Create a *SchoolViewModels* folder in the *Models* folder.</span></span>

<span data-ttu-id="179cf-269">V *SchoolViewModels* složky, přidejte *EnrollmentDateGroup.cs* následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="179cf-269">In the *SchoolViewModels* folder, add a *EnrollmentDateGroup.cs* with the following code:</span></span>

[!code-csharp[](intro/samples/cu21/Models/SchoolViewModels/EnrollmentDateGroup.cs)]

### <a name="update-the-about-page-model"></a><span data-ttu-id="179cf-270">Aktualizace modelu o stránce</span><span class="sxs-lookup"><span data-stu-id="179cf-270">Update the About page model</span></span>

<span data-ttu-id="179cf-271">Aktualizace *Pages/About.cshtml.cs* souboru následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="179cf-271">Update the *Pages/About.cshtml.cs* file with the following code:</span></span>

[!code-csharp[](intro/samples/cu21/Pages/About.cshtml.cs)]

<span data-ttu-id="179cf-272">Příkaz LINQ skupiny studentů entity podle data registrace, vypočítá počet entit v každé skupině a ukládá výsledky v kolekci `EnrollmentDateGroup` zobrazit objekty modelu.</span><span class="sxs-lookup"><span data-stu-id="179cf-272">The LINQ statement groups the student entities by enrollment date, calculates the number of entities in each group, and stores the results in a collection of `EnrollmentDateGroup` view model objects.</span></span>

### <a name="modify-the-about-razor-page"></a><span data-ttu-id="179cf-273">Upravit o stránky Razor</span><span class="sxs-lookup"><span data-stu-id="179cf-273">Modify the About Razor Page</span></span>

<span data-ttu-id="179cf-274">Nahraďte kód v *Pages/About.cshtml* souboru následujícím kódem:</span><span class="sxs-lookup"><span data-stu-id="179cf-274">Replace the code in the *Pages/About.cshtml* file with the following code:</span></span>

[!code-html[](intro/samples/cu21/Pages/About.cshtml)]

<span data-ttu-id="179cf-275">Spusťte aplikaci a přejděte na stránku o.</span><span class="sxs-lookup"><span data-stu-id="179cf-275">Run the app and navigate to the About page.</span></span> <span data-ttu-id="179cf-276">Počet studentů pro každé datum registrace se zobrazí v tabulce.</span><span class="sxs-lookup"><span data-stu-id="179cf-276">The count of students for each enrollment date is displayed in a table.</span></span>

<span data-ttu-id="179cf-277">Pokud narazíte na potíže nelze vyřešit, stáhněte si [dokončené aplikace pro tuto fázi](https://github.com/aspnet/Docs/tree/master/aspnetcore/data/ef-rp/intro/samples/StageSnapShots/cu-part3-sorting).</span><span class="sxs-lookup"><span data-stu-id="179cf-277">If you run into problems you can't solve, download the [completed app for this stage](https://github.com/aspnet/Docs/tree/master/aspnetcore/data/ef-rp/intro/samples/StageSnapShots/cu-part3-sorting).</span></span>

![O stránku](sort-filter-page/_static/about.png)

## <a name="additional-resources"></a><span data-ttu-id="179cf-279">Další zdroje</span><span class="sxs-lookup"><span data-stu-id="179cf-279">Additional resources</span></span>

* [<span data-ttu-id="179cf-280">Ladění ASP.NET Core 2.x zdroje</span><span class="sxs-lookup"><span data-stu-id="179cf-280">Debugging ASP.NET Core 2.x source</span></span>](https://github.com/aspnet/Docs/issues/4155)

<span data-ttu-id="179cf-281">V dalším kurzu se aplikace používá k aktualizaci modelu dat migrace.</span><span class="sxs-lookup"><span data-stu-id="179cf-281">In the next tutorial, the app uses migrations to update the data model.</span></span>

::: moniker-end

> [!div class="step-by-step"]
> <span data-ttu-id="179cf-282">[Předchozí](xref:data/ef-rp/crud)
> [další](xref:data/ef-rp/migrations)</span><span class="sxs-lookup"><span data-stu-id="179cf-282">[Previous](xref:data/ef-rp/crud)
[Next](xref:data/ef-rp/migrations)</span></span>
