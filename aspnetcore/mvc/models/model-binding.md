---
title: Vazby modelu v ASP.NET Core
author: rachelappel
description: Zjistěte, jak mapuje vazby modelu v aplikaci ASP.NET MVC základní data z požadavků HTTP pro parametry metody akce.
manager: wpickett
ms.assetid: 0be164aa-1d72-4192-bd6b-192c9c301164
ms.author: rachelap
ms.date: 01/22/2018
ms.prod: asp.net-core
ms.technology: aspnet
ms.topic: article
uid: mvc/models/model-binding
ms.openlocfilehash: f416bda1d7bccdfa922ba598a411ef1d150e3111
ms.sourcegitcommit: 493a215355576cfa481773365de021bcf04bb9c7
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 03/15/2018
ms.locfileid: "29904557"
---
# <a name="model-binding-in-aspnet-core"></a><span data-ttu-id="ee0bf-103">Vazby modelu v ASP.NET Core</span><span class="sxs-lookup"><span data-stu-id="ee0bf-103">Model Binding in ASP.NET Core</span></span>

<span data-ttu-id="ee0bf-104">Podle [Rachel Appel](https://github.com/rachelappel)</span><span class="sxs-lookup"><span data-stu-id="ee0bf-104">By [Rachel Appel](https://github.com/rachelappel)</span></span>

## <a name="introduction-to-model-binding"></a><span data-ttu-id="ee0bf-105">Úvod do modelu vazby</span><span class="sxs-lookup"><span data-stu-id="ee0bf-105">Introduction to model binding</span></span>

<span data-ttu-id="ee0bf-106">Vazby modelu v aplikaci ASP.NET MVC základní mapuje data z požadavků HTTP pro parametry metody akce.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-106">Model binding in ASP.NET Core MVC maps data from HTTP requests to action method parameters.</span></span> <span data-ttu-id="ee0bf-107">Parametry může být jednoduché typy, jako je například řetězce, celá čísla nebo obtékaných objektů, nebo mohou být komplexní typy.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-107">The parameters may be simple types such as strings, integers, or floats, or they may be complex types.</span></span> <span data-ttu-id="ee0bf-108">Toto je skvělé funkce MVC, protože mapování příchozích dat na protějšek je často opakovaných scénář, bez ohledu na velikost a složitost dat.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-108">This is a great feature of MVC because mapping incoming data to a counterpart is an often repeated scenario, regardless of size or complexity of the data.</span></span> <span data-ttu-id="ee0bf-109">MVC řeší tento problém tím, že poskytuje rychle abstrakci vazby, takže vývojáři nemusí zachovat přepisování trochu odlišná verze tohoto stejný kód v každé aplikace.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-109">MVC solves this problem by abstracting binding away so developers don't have to keep rewriting a slightly different version of that same code in every app.</span></span> <span data-ttu-id="ee0bf-110">Zápis vlastního textu na typ převaděče kód je zdlouhavé a tím zvyšuje i pravděpodobnost chyby.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-110">Writing your own text to type converter code is tedious, and error prone.</span></span>

## <a name="how-model-binding-works"></a><span data-ttu-id="ee0bf-111">Jak funguje vazby modelu</span><span class="sxs-lookup"><span data-stu-id="ee0bf-111">How model binding works</span></span>

<span data-ttu-id="ee0bf-112">Když MVC obdrží požadavek HTTP, nasměruje ho do určité akce metodu řadiče.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-112">When MVC receives an HTTP request, it routes it to a specific action method of a controller.</span></span> <span data-ttu-id="ee0bf-113">Určuje, která metoda akce ke spuštění na základě toho, co je v datech trasy a hodnot z požadavku HTTP se váže k této metodě akce parametry.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-113">It determines which action method to run based on what is in the route data, then it binds values from the HTTP request to that action method's parameters.</span></span> <span data-ttu-id="ee0bf-114">Zvažte například následující adresu URL:</span><span class="sxs-lookup"><span data-stu-id="ee0bf-114">For example, consider the following URL:</span></span>

`http://contoso.com/movies/edit/2`

<span data-ttu-id="ee0bf-115">Vzhledem k tomu, že šablona trasy vypadá to, `{controller=Home}/{action=Index}/{id?}`, `movies/edit/2` směruje `Movies` řadiče a jeho `Edit` metody akce.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-115">Since the route template looks like this, `{controller=Home}/{action=Index}/{id?}`, `movies/edit/2` routes to the `Movies` controller, and its `Edit` action method.</span></span> <span data-ttu-id="ee0bf-116">Přijme také volitelný parametr názvem `id`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-116">It also accepts an optional parameter called `id`.</span></span> <span data-ttu-id="ee0bf-117">Kód pro metodu akce by měl vypadat přibližně takto:</span><span class="sxs-lookup"><span data-stu-id="ee0bf-117">The code for the action method should look something like this:</span></span>

```csharp
public IActionResult Edit(int? id)
   ```

<span data-ttu-id="ee0bf-118">Poznámka: Řetězců v trasu adresy URL nejsou velká a malá písmena.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-118">Note: The strings in the URL route are not case sensitive.</span></span>

<span data-ttu-id="ee0bf-119">MVC se pokusí vytvořit vazbu data žádosti k parametrům akce podle názvu.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-119">MVC will try to bind request data to the action parameters by name.</span></span> <span data-ttu-id="ee0bf-120">MVC bude vyhledávat hodnoty pro každý parametr pomocí názvu parametru a názvy jeho veřejné nastavit vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-120">MVC will look for values for each parameter using the parameter name and the names of its public settable properties.</span></span> <span data-ttu-id="ee0bf-121">V předchozím příkladu je jedinou akcí parametr s názvem `id`, který MVC váže hodnotu s tímto názvem v hodnoty trasy.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-121">In the above example, the only action parameter is named `id`, which MVC binds to the value with the same name in the route values.</span></span> <span data-ttu-id="ee0bf-122">Kromě hodnoty trasy MVC vytvoří vazbu dat z různých částí žádosti a dělá to tak, aby sada.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-122">In addition to route values MVC will bind data from various parts of the request and it does so in a set order.</span></span> <span data-ttu-id="ee0bf-123">Níže je seznam zdrojů dat v pořadí, ve kterém vazby modelu vypadá prostřednictvím je:</span><span class="sxs-lookup"><span data-stu-id="ee0bf-123">Below is a list of the data sources in the order that model binding looks through them:</span></span>

1. <span data-ttu-id="ee0bf-124">`Form values`: Toto jsou hodnot formuláře, které přejděte v požadavku HTTP využívající metodu POST.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-124">`Form values`: These are form values that go in the HTTP request using the POST method.</span></span> <span data-ttu-id="ee0bf-125">(včetně požadavků jQuery POST).</span><span class="sxs-lookup"><span data-stu-id="ee0bf-125">(including jQuery POST requests).</span></span>

2. <span data-ttu-id="ee0bf-126">`Route values`: Sada hodnot trasy poskytované [směrování](xref:fundamentals/routing)</span><span class="sxs-lookup"><span data-stu-id="ee0bf-126">`Route values`: The set of route values provided by [Routing](xref:fundamentals/routing)</span></span>

3. <span data-ttu-id="ee0bf-127">`Query strings`: Část řetězec dotazu identifikátoru URI.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-127">`Query strings`: The query string part of the URI.</span></span>

<!-- DocFX BUG
The link works but generates an error when building with DocFX
@fundamentals/routing
[Routing](xref:fundamentals/routing)
-->

<span data-ttu-id="ee0bf-128">Poznámka: Formuláře hodnoty, data trasy a dotazů řetězce se ukládají jako dvojice název hodnota.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-128">Note: Form values, route data, and query strings are all stored as name-value pairs.</span></span>

<span data-ttu-id="ee0bf-129">Vzhledem k tomu, že vazby modelu žádali klíč s názvem `id` a není nic s názvem `id` v hodnot formuláře, přesune do vyhledávání pro tento klíč hodnoty trasy.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-129">Since model binding asked for a key named `id` and there's nothing named `id` in the form values, it moved on to the route values looking for that key.</span></span> <span data-ttu-id="ee0bf-130">V našem příkladu je nalezena shoda.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-130">In our example, it's a match.</span></span> <span data-ttu-id="ee0bf-131">Vázání a hodnota je převést na celé číslo 2.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-131">Binding happens, and the value is converted to the integer 2.</span></span> <span data-ttu-id="ee0bf-132">Stejné žádosti pomocí úpravy (řetězec id) by převést na řetězec "2".</span><span class="sxs-lookup"><span data-stu-id="ee0bf-132">The same request using Edit(string id) would convert to the string "2".</span></span>

<span data-ttu-id="ee0bf-133">Pokud v příkladu jednoduché typy.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-133">So far the example uses simple types.</span></span> <span data-ttu-id="ee0bf-134">V MVC jednoduché typy jsou všechny .NET primitivní typ nebo typ pomocí převaděče typu řetězec.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-134">In MVC simple types are any .NET primitive type or type with a string type converter.</span></span> <span data-ttu-id="ee0bf-135">Kdyby parametru metody akce třídy, jako `Movie` typu, který obsahuje jednoduché a komplexní typy jako vhodně vlastností MVC model vazby budou nadále jej zpracovat.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-135">If the action method's parameter were a class such as the `Movie` type, which contains both simple and complex types as properties, MVC's model binding will still handle it nicely.</span></span> <span data-ttu-id="ee0bf-136">Reflexe a rekurze používá procházení vlastnosti komplexních typů hledání shody.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-136">It uses reflection and recursion to traverse the properties of complex types looking for matches.</span></span> <span data-ttu-id="ee0bf-137">Vazby modelu hledá vzoru *parameter_name.property_name* k vytvoření vazby na vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-137">Model binding looks for the pattern *parameter_name.property_name* to bind values to properties.</span></span> <span data-ttu-id="ee0bf-138">Pokud se nenajde odpovídající hodnoty tohoto formuláře, pokusí se vytvořit vazbu pomocí název vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-138">If it doesn't find matching values of this form, it will attempt to bind using just the property name.</span></span> <span data-ttu-id="ee0bf-139">Pro tyto typy jako `Collection` typy, vazby modelu hledá shod, který se *parameter_name [index]* nebo právě *[index]*.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-139">For those types such as `Collection` types, model binding looks for matches to *parameter_name[index]* or just *[index]*.</span></span> <span data-ttu-id="ee0bf-140">Model vazby vyhodnotí `Dictionary` podobně typy žádostí o *parameter_name [klíč]* nebo právě *[klíč]*, pokud jsou klíče jednoduché typy.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-140">Model binding treats  `Dictionary` types similarly, asking for *parameter_name[key]* or just *[key]*, as long as the keys are simple types.</span></span> <span data-ttu-id="ee0bf-141">Klíče, které jsou podporovány odpovídat názvům pole HTML a značky Pomocníci vygenerované stejný typ modelu.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-141">Keys that are supported match the field names HTML and tag helpers generated for the same model type.</span></span> <span data-ttu-id="ee0bf-142">To umožňuje odezvy hodnoty tak, aby pole formuláře zůstaly vyplněný se vstupem uživatele pro jejich pohodlí, například když vázaných dat z vytvořit nebo upravit neproběhla úspěšně ověření.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-142">This enables round-tripping values so that the form fields remain filled with the user's input for their convenience, for example, when bound data from a create or edit didn't pass validation.</span></span>

<span data-ttu-id="ee0bf-143">V pořadí pro vazbu provést třídu musí mít veřejný výchozí konstruktor a člen vázat musí být veřejné vlastnosti s možností zápisu.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-143">In order for binding to happen the class must have a public default constructor and member to be bound must be public writable properties.</span></span> <span data-ttu-id="ee0bf-144">Pokud vazba modelu se stane, že třída bude vytvořena pouze pomocí výchozí veřejný konstruktor, můžete nastavit vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-144">When model binding happens the class will only be instantiated using the public default constructor, then the properties can be set.</span></span>

<span data-ttu-id="ee0bf-145">Když vazbu parametru vazby modelu zastaví hledá hodnoty s tímto názvem a jeho přejde k další parametr vazby.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-145">When a parameter is bound, model binding stops looking for values with that name and it moves on to bind the next parameter.</span></span> <span data-ttu-id="ee0bf-146">Výchozí chování vazby modelu, jinak hodnota nastaví parametrů na výchozí hodnoty v závislosti na jejich typu:</span><span class="sxs-lookup"><span data-stu-id="ee0bf-146">Otherwise, the default model binding behavior sets parameters to their default values depending on their type:</span></span>

* <span data-ttu-id="ee0bf-147">`T[]`: S výjimkou produktů pole typu `byte[]`, vazba nastaví parametry typu `T[]` k `Array.Empty<T>()`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-147">`T[]`: With the exception of arrays of type `byte[]`, binding sets parameters of type `T[]` to `Array.Empty<T>()`.</span></span> <span data-ttu-id="ee0bf-148">Pole typu `byte[]` jsou nastaveny na `null`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-148">Arrays of type `byte[]` are set to `null`.</span></span>

* <span data-ttu-id="ee0bf-149">Odkazové typy: Vazba vytvoří instanci třídy s výchozím konstruktorem bez nastavení vlastností.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-149">Reference Types: Binding creates an instance of a class with the default constructor without setting properties.</span></span> <span data-ttu-id="ee0bf-150">Však model vazby sady `string` parametry `null`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-150">However, model binding sets `string` parameters to `null`.</span></span>

* <span data-ttu-id="ee0bf-151">Typy s možnou hodnotou NULL: Typy s možnou hodnotou Null jsou nastaveny na `null`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-151">Nullable Types: Nullable types are set to `null`.</span></span> <span data-ttu-id="ee0bf-152">V předchozím příkladu model vazby sady `id` k `null` vzhledem k tomu, že je typu `int?`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-152">In the above example, model binding sets `id` to `null` since it's of type `int?`.</span></span>

* <span data-ttu-id="ee0bf-153">Typy hodnot: Použití hodnot Null typy hodnot typu `T` jsou nastaveny na `default(T)`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-153">Value Types: Non-nullable value types of type `T` are set to `default(T)`.</span></span> <span data-ttu-id="ee0bf-154">Vazby modelu bude třeba nastavit parametr `int id` na hodnotu 0.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-154">For example, model binding will set a parameter `int id` to 0.</span></span> <span data-ttu-id="ee0bf-155">Zvažte použití ověření modelu nebo typy s možnou hodnotou Null, namísto spoléhání na výchozí hodnoty.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-155">Consider using model validation or nullable types rather than relying on default values.</span></span>

<span data-ttu-id="ee0bf-156">Pokud vazba se nezdaří, nebude MVC vyvolána chyba.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-156">If binding fails, MVC doesn't throw an error.</span></span> <span data-ttu-id="ee0bf-157">Všechny akce, která přijímá vstup uživatele by se mělo zjistit `ModelState.IsValid` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-157">Every action which accepts user input should check the `ModelState.IsValid` property.</span></span>

<span data-ttu-id="ee0bf-158">Poznámka: Každý záznam v kontroleru `ModelState` vlastnost je `ModelStateEntry` obsahující `Errors` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-158">Note: Each entry in the controller's `ModelState` property is a `ModelStateEntry` containing an `Errors` property.</span></span> <span data-ttu-id="ee0bf-159">Je málokdy potřeba dotazování tuto kolekci sami.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-159">It's rarely necessary to query this collection yourself.</span></span> <span data-ttu-id="ee0bf-160">Místo nich se používá `ModelState.IsValid`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-160">Use `ModelState.IsValid` instead.</span></span>

<span data-ttu-id="ee0bf-161">Kromě toho existují některé speciální datové typy, které MVC nutné vzít v úvahu při provádění vazby modelu:</span><span class="sxs-lookup"><span data-stu-id="ee0bf-161">Additionally, there are some special data types that MVC must consider when performing model binding:</span></span>

* <span data-ttu-id="ee0bf-162">`IFormFile`, `IEnumerable<IFormFile>`: Jeden nebo více odeslané soubory, které jsou součástí požadavku HTTP.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-162">`IFormFile`, `IEnumerable<IFormFile>`: One or more uploaded files that are part of the HTTP request.</span></span>

* <span data-ttu-id="ee0bf-163">`CancellationToken`: Sloužící ke zrušení aktivity v asynchronní řadiče.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-163">`CancellationToken`: Used to cancel activity in asynchronous controllers.</span></span>

<span data-ttu-id="ee0bf-164">Tyto typy mohou být vázány na parametry akce nebo vlastnosti na typ třídy.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-164">These types can be bound to action parameters or to properties on a class type.</span></span>

<span data-ttu-id="ee0bf-165">Po dokončení vazby modelu [ověření](validation.md) dojde.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-165">Once model binding is complete, [Validation](validation.md) occurs.</span></span> <span data-ttu-id="ee0bf-166">Vazby modelu výchozí dobře funguje pro velká většina vývojové scénáře.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-166">Default model binding works great for the vast majority of development scenarios.</span></span> <span data-ttu-id="ee0bf-167">Je také rozšiřitelný, pokud máte jedinečné potřeby předdefinované chování můžete přizpůsobit.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-167">It's also extensible so if you have unique needs you can customize the built-in behavior.</span></span>

## <a name="customize-model-binding-behavior-with-attributes"></a><span data-ttu-id="ee0bf-168">Přizpůsobení chování vazby modelu s atributy</span><span class="sxs-lookup"><span data-stu-id="ee0bf-168">Customize model binding behavior with attributes</span></span>

<span data-ttu-id="ee0bf-169">MVC obsahuje několik atributů, které můžete použít k přímé jeho výchozí chování vazby modelu do jiného zdroje.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-169">MVC contains several attributes that you can use to direct its default model binding behavior to a different source.</span></span> <span data-ttu-id="ee0bf-170">Například můžete určit, zda vazby je vyžadováno pro vlastnost, nebo pokud ji by se nikdy nemělo stát vůbec pomocí `[BindRequired]` nebo `[BindNever]` atributy.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-170">For example, you can specify whether binding is required for a property, or if it should never happen at all by using the `[BindRequired]` or `[BindNever]` attributes.</span></span> <span data-ttu-id="ee0bf-171">Alternativně můžete přepsat výchozí zdroj dat a zadejte zdroj dat vazač modelu.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-171">Alternatively, you can override the default data source, and specify the model binder's data source.</span></span> <span data-ttu-id="ee0bf-172">Níže je seznam atributů vazby modelu:</span><span class="sxs-lookup"><span data-stu-id="ee0bf-172">Below is a list of model binding attributes:</span></span>

* <span data-ttu-id="ee0bf-173">`[BindRequired]`: Tento atribut přidá chybu stavu modelu, pokud vazba nebyla vytvořena.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-173">`[BindRequired]`: This attribute adds a model state error if binding cannot occur.</span></span>

* <span data-ttu-id="ee0bf-174">`[BindNever]`: Informuje vazač modelu pro nikdy vazbu pro tento parametr.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-174">`[BindNever]`: Tells the model binder to never bind to this parameter.</span></span>

* <span data-ttu-id="ee0bf-175">`[FromHeader]`, `[FromQuery]`, `[FromRoute]`, `[FromForm]`: Použijte tyto zadat zdroj přesný vazby, kterou chcete použít.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-175">`[FromHeader]`, `[FromQuery]`, `[FromRoute]`, `[FromForm]`: Use these to specify the exact binding source you want to apply.</span></span>

* <span data-ttu-id="ee0bf-176">`[FromServices]`: Tento atribut používá [vkládání závislostí](../../fundamentals/dependency-injection.md) pro svázání parametrů ze služeb.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-176">`[FromServices]`: This attribute uses [dependency injection](../../fundamentals/dependency-injection.md) to bind parameters from services.</span></span>

* <span data-ttu-id="ee0bf-177">`[FromBody]`: Pomocí nakonfigurované formátovací moduly vázat data z textu požadavku.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-177">`[FromBody]`: Use the configured formatters to bind data from the request body.</span></span> <span data-ttu-id="ee0bf-178">Formátovací modul je vybrána, založené na typ obsahu požadavku.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-178">The formatter is selected based on content type of the request.</span></span>

* <span data-ttu-id="ee0bf-179">`[ModelBinder]`: Používaná k přepsání výchozí vazač modelu, vazba zdroje a název.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-179">`[ModelBinder]`: Used to override the default model binder, binding source and name.</span></span>

<span data-ttu-id="ee0bf-180">Atributy jsou velmi užitečné nástroje, když potřebujete přepsat výchozí chování vazby modelu.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-180">Attributes are very helpful tools when you need to override the default behavior of model binding.</span></span>

## <a name="bind-formatted-data-from-the-request-body"></a><span data-ttu-id="ee0bf-181">Vytvoření vazby formátovaných dat z textu žádosti</span><span class="sxs-lookup"><span data-stu-id="ee0bf-181">Bind formatted data from the request body</span></span>

<span data-ttu-id="ee0bf-182">Žádost o data mohou pocházet v různých formátech, včetně JSON, XML a mnohé další.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-182">Request data can come in a variety of formats including JSON, XML and many others.</span></span> <span data-ttu-id="ee0bf-183">Při použití atributu [FromBody] k označení, že chcete vazby parametru na data v textu požadavku, aplikace MVC používá nakonfigurované sadu formátovacích modulů pro zpracování dat požadavku na základě jeho obsahu typu.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-183">When you use the [FromBody] attribute to indicate that you want to bind a parameter to data in the request body, MVC uses a configured set of formatters to handle the request data based on its content type.</span></span> <span data-ttu-id="ee0bf-184">Ve výchozím nastavení aplikace MVC zahrnuje `JsonInputFormatter` třídy pro zpracování dat JSON, ale můžete přidat další formátovacích modulů pro zpracování XML a dalších vlastních formátů.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-184">By default MVC includes a `JsonInputFormatter` class for handling JSON data, but you can add additional formatters for handling XML and other custom formats.</span></span>

> [!NOTE]
> <span data-ttu-id="ee0bf-185">Může existovat maximálně jeden parametr na každou akci označených pomocí `[FromBody]`.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-185">There can be at most one parameter per action decorated with `[FromBody]`.</span></span> <span data-ttu-id="ee0bf-186">ASP.NET Core MVC runtime deleguje je zodpovědností čtení datový proud požadavku k formátování.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-186">The ASP.NET Core MVC run-time delegates the responsibility of reading the request stream to the formatter.</span></span> <span data-ttu-id="ee0bf-187">Jakmile datový proud požadavku je pro čtení pro parametr, není obecně možné číst datový proud požadavku znovu pro vazby dalších `[FromBody]` parametry.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-187">Once the request stream is read for a parameter, it's generally not possible to read the request stream again for binding other `[FromBody]` parameters.</span></span>

> [!NOTE]
> <span data-ttu-id="ee0bf-188">`JsonInputFormatter` Je výchozí formátování a je založena na [Json.NET](https://www.newtonsoft.com/json).</span><span class="sxs-lookup"><span data-stu-id="ee0bf-188">The `JsonInputFormatter` is the default formatter and is based on [Json.NET](https://www.newtonsoft.com/json).</span></span>

<span data-ttu-id="ee0bf-189">Vybere vstupní formátování na základě technologie ASP.NET [Content-Type](https://www.w3.org/Protocols/rfc1341/4_Content-Type.html) záhlaví a typ parametru, pokud je atribut u ní zadáno jinak.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-189">ASP.NET selects input formatters based on the [Content-Type](https://www.w3.org/Protocols/rfc1341/4_Content-Type.html) header and the type of the parameter, unless there's an attribute applied to it specifying otherwise.</span></span> <span data-ttu-id="ee0bf-190">Pokud chcete použít XML nebo jiného formátu je nutné ho nakonfigurovat v *Startup.cs* soubor, ale budete chtít nejdřív mají získat odkaz na `Microsoft.AspNetCore.Mvc.Formatters.Xml` pomocí nástroje NuGet.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-190">If you'd like to use XML or another format you must configure it in the *Startup.cs* file, but you may first have to obtain a reference to `Microsoft.AspNetCore.Mvc.Formatters.Xml` using NuGet.</span></span> <span data-ttu-id="ee0bf-191">Spuštění kódu by měl vypadat přibližně takto:</span><span class="sxs-lookup"><span data-stu-id="ee0bf-191">Your startup code should look something like this:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddMvc()
        .AddXmlSerializerFormatters();
   }
```

<span data-ttu-id="ee0bf-192">Kód na *Startup.cs* soubor obsahuje `ConfigureServices` metoda s `services` argument můžete použít k vytvoření služeb pro aplikace ASP.NET.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-192">Code in the *Startup.cs* file contains a `ConfigureServices` method with a `services` argument you can use to build up services for your ASP.NET app.</span></span> <span data-ttu-id="ee0bf-193">V ukázce přidáváme formátovací modul XML jako služba, která bude poskytovat MVC pro tuto aplikaci.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-193">In the sample, we are adding an XML formatter as a service that MVC will provide for this app.</span></span> <span data-ttu-id="ee0bf-194">`options` Argument předaný `AddMvc` metoda umožňuje přidávat a spravovat filtry, formátování a další možnosti systému z MVC po spuštění aplikace.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-194">The `options` argument passed into the `AddMvc` method allows you to add and manage filters, formatters, and other system options from MVC upon app startup.</span></span> <span data-ttu-id="ee0bf-195">Následně použít `Consumes` atribut třídy kontroleru nebo metod akce pro práci s formátem chcete.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-195">Then apply the `Consumes` attribute to controller classes or action methods to work with the format you want.</span></span>

### <a name="custom-model-binding"></a><span data-ttu-id="ee0bf-196">Vazby vlastní modelu</span><span class="sxs-lookup"><span data-stu-id="ee0bf-196">Custom Model Binding</span></span>

<span data-ttu-id="ee0bf-197">Vazby modelu můžete rozšířit vytvořením vlastní vazače modelů vlastní.</span><span class="sxs-lookup"><span data-stu-id="ee0bf-197">You can extend model binding by writing your own custom model binders.</span></span> <span data-ttu-id="ee0bf-198">Další informace o [vazby modelu vlastní](../advanced/custom-model-binding.md).</span><span class="sxs-lookup"><span data-stu-id="ee0bf-198">Learn more about [custom model binding](../advanced/custom-model-binding.md).</span></span>
