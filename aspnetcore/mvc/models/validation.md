---
title: Ověření modelu v ASP.NET Core MVC
author: tdykstra
description: Další informace o ověření modelu v ASP.NET Core MVC.
ms.author: riande
ms.date: 07/31/2018
uid: mvc/models/validation
ms.openlocfilehash: f407903577e40b6501737ef5b78d90e1e3e60c06
ms.sourcegitcommit: e955a722c05ce2e5e21b4219f7d94fb878e255a6
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 07/31/2018
ms.locfileid: "39378664"
---
# <a name="model-validation-in-aspnet-core-mvc"></a><span data-ttu-id="99ece-103">Ověření modelu v ASP.NET Core MVC</span><span class="sxs-lookup"><span data-stu-id="99ece-103">Model validation in ASP.NET Core MVC</span></span>

<span data-ttu-id="99ece-104">Podle [Rachel Appel](https://github.com/rachelappel)</span><span class="sxs-lookup"><span data-stu-id="99ece-104">By [Rachel Appel](https://github.com/rachelappel)</span></span>

## <a name="introduction-to-model-validation"></a><span data-ttu-id="99ece-105">Úvod k ověření modelu</span><span class="sxs-lookup"><span data-stu-id="99ece-105">Introduction to model validation</span></span>

<span data-ttu-id="99ece-106">Předtím, než aplikace ukládá data do databáze, aplikace musí ověřit data.</span><span class="sxs-lookup"><span data-stu-id="99ece-106">Before an app stores data in a database, the app must validate the data.</span></span> <span data-ttu-id="99ece-107">Data musí být kontrolované potenciálních bezpečnostních hrozeb, ověřit, že je správně naformátovaný podle typu a velikosti a musí odpovídat pravidla.</span><span class="sxs-lookup"><span data-stu-id="99ece-107">Data must be checked for potential security threats, verified that it's appropriately formatted by type and size, and it must conform to your rules.</span></span> <span data-ttu-id="99ece-108">Ověření je nezbytné, i když může být redundantní chybám a únavná implementovat.</span><span class="sxs-lookup"><span data-stu-id="99ece-108">Validation is necessary although it can be redundant and tedious to implement.</span></span> <span data-ttu-id="99ece-109">V aplikaci MVC se stane ověření klienta i serveru.</span><span class="sxs-lookup"><span data-stu-id="99ece-109">In MVC, validation happens on both the client and server.</span></span>

<span data-ttu-id="99ece-110">Naštěstí má .NET abstrahovaná ověření do atributů ověření.</span><span class="sxs-lookup"><span data-stu-id="99ece-110">Fortunately, .NET has abstracted validation into validation attributes.</span></span> <span data-ttu-id="99ece-111">Tyto atributy obsahují kód pro ověření, a tím snižuje množství kódu, který musíte napsat.</span><span class="sxs-lookup"><span data-stu-id="99ece-111">These attributes contain validation code, thereby reducing the amount of code you must write.</span></span>

<span data-ttu-id="99ece-112">[Zobrazit nebo stáhnout ukázky z Githubu](https://github.com/aspnet/Docs/tree/master/aspnetcore/mvc/models/validation/sample).</span><span class="sxs-lookup"><span data-stu-id="99ece-112">[View or download sample from GitHub](https://github.com/aspnet/Docs/tree/master/aspnetcore/mvc/models/validation/sample).</span></span>

## <a name="validation-attributes"></a><span data-ttu-id="99ece-113">Ověřování atributů</span><span class="sxs-lookup"><span data-stu-id="99ece-113">Validation Attributes</span></span>

<span data-ttu-id="99ece-114">Ověřování atributů představují způsob, jak nakonfigurovat ověřování modelu, tak, aby se koncepčně podobá ověření na pole v databázové tabulky.</span><span class="sxs-lookup"><span data-stu-id="99ece-114">Validation attributes are a way to configure model validation so it's similar conceptually to validation on fields in database tables.</span></span> <span data-ttu-id="99ece-115">To zahrnuje omezení, jako je například přiřazení datových typů nebo povinná pole.</span><span class="sxs-lookup"><span data-stu-id="99ece-115">This includes constraints such as assigning data types or required fields.</span></span> <span data-ttu-id="99ece-116">Jiné typy ověření, které zahrnuje použití vzorců k datům a vynucování obchodních pravidel, jako je například platební kartu, telefonní číslo nebo e-mailovou adresu.</span><span class="sxs-lookup"><span data-stu-id="99ece-116">Other types of validation include applying patterns to data to enforce business rules, such as a credit card, phone number, or email address.</span></span> <span data-ttu-id="99ece-117">Atributy ověření provést vynucují požadavky pro tyto mnohem jednodušší a usnadňuje používání.</span><span class="sxs-lookup"><span data-stu-id="99ece-117">Validation attributes make enforcing these requirements much simpler and easier to use.</span></span>

<span data-ttu-id="99ece-118">Níže je s poznámkami `Movie` modelů z aplikace, která uchovává informace o filmů a televizních pořadů.</span><span class="sxs-lookup"><span data-stu-id="99ece-118">Below is an annotated `Movie` model from an app that stores information about movies and TV shows.</span></span> <span data-ttu-id="99ece-119">Většina vlastností jsou povinné a několik vlastností řetězce mají požadavky na délku.</span><span class="sxs-lookup"><span data-stu-id="99ece-119">Most of the properties are required and several string properties have length requirements.</span></span> <span data-ttu-id="99ece-120">Kromě toho je omezení číselného rozsahu v místě `Price` vlastnost od 0 do $999,99, spolu s vlastní ověřovací atribut.</span><span class="sxs-lookup"><span data-stu-id="99ece-120">Additionally, there's a numeric range restriction in place for the `Price` property from 0 to $999.99, along with a custom validation attribute.</span></span>

[!code-csharp[](validation/sample/Movie.cs?range=6-29)]

<span data-ttu-id="99ece-121">Pouze pro čtení přes model odhalí pravidla týkající se data pro tuto aplikaci, což usnadňuje údržbu kódu.</span><span class="sxs-lookup"><span data-stu-id="99ece-121">Simply reading through the model reveals the rules about data for this app, making it easier to maintain the code.</span></span> <span data-ttu-id="99ece-122">Níže jsou uvedeny několik oblíbených ověření integrované atributy:</span><span class="sxs-lookup"><span data-stu-id="99ece-122">Below are several popular built-in validation attributes:</span></span>

* <span data-ttu-id="99ece-123">`[CreditCard]`: Ověří vlastnost má formát platební karty.</span><span class="sxs-lookup"><span data-stu-id="99ece-123">`[CreditCard]`: Validates the property has a credit card format.</span></span>

* <span data-ttu-id="99ece-124">`[Compare]`: Ověří dvě vlastnosti v modelu shoda.</span><span class="sxs-lookup"><span data-stu-id="99ece-124">`[Compare]`: Validates two properties in a model match.</span></span>

* <span data-ttu-id="99ece-125">`[EmailAddress]`: Ověří vlastnost má formát e-mailu.</span><span class="sxs-lookup"><span data-stu-id="99ece-125">`[EmailAddress]`: Validates the property has an email format.</span></span>

* <span data-ttu-id="99ece-126">`[Phone]`: Ověří vlastnost má formát telefonu.</span><span class="sxs-lookup"><span data-stu-id="99ece-126">`[Phone]`: Validates the property has a telephone format.</span></span>

* <span data-ttu-id="99ece-127">`[Range]`: Ověří spadá hodnotu vlastnosti v daném rozsahu.</span><span class="sxs-lookup"><span data-stu-id="99ece-127">`[Range]`: Validates the property value falls within the given range.</span></span>

* <span data-ttu-id="99ece-128">`[RegularExpression]`: Ověří, že data odpovídá zadanému regulárnímu výrazu.</span><span class="sxs-lookup"><span data-stu-id="99ece-128">`[RegularExpression]`: Validates that the data matches the specified regular expression.</span></span>

* <span data-ttu-id="99ece-129">`[Required]`: Vytvoří požadované vlastnosti.</span><span class="sxs-lookup"><span data-stu-id="99ece-129">`[Required]`: Makes a property required.</span></span>

* <span data-ttu-id="99ece-130">`[StringLength]`: Ověří, že řetězcovou vlastnost má nejvíce dané maximální délky.</span><span class="sxs-lookup"><span data-stu-id="99ece-130">`[StringLength]`: Validates that a string property has at most the given maximum length.</span></span>

* <span data-ttu-id="99ece-131">`[Url]`: Ověří vlastnost má formát adresy URL.</span><span class="sxs-lookup"><span data-stu-id="99ece-131">`[Url]`: Validates the property has a URL format.</span></span>

<span data-ttu-id="99ece-132">MVC podporuje všechny atributy, které je odvozena z `ValidationAttribute` pro účely ověření.</span><span class="sxs-lookup"><span data-stu-id="99ece-132">MVC supports any attribute that derives from `ValidationAttribute` for validation purposes.</span></span> <span data-ttu-id="99ece-133">Mnoho užitečných ověřování atributů najdete v [System.ComponentModel.DataAnnotations](/dotnet/api/system.componentmodel.dataannotations) oboru názvů.</span><span class="sxs-lookup"><span data-stu-id="99ece-133">Many useful validation attributes can be found in the [System.ComponentModel.DataAnnotations](/dotnet/api/system.componentmodel.dataannotations) namespace.</span></span>

<span data-ttu-id="99ece-134">Může existovat instance, kdy potřebujete víc funkcí než vestavěné atributy.</span><span class="sxs-lookup"><span data-stu-id="99ece-134">There may be instances where you need more features than built-in attributes provide.</span></span> <span data-ttu-id="99ece-135">Pro situace, můžete vytvořit vlastní ověřovací atributy odvozený od `ValidationAttribute` nebo změna modelu k implementaci `IValidatableObject`.</span><span class="sxs-lookup"><span data-stu-id="99ece-135">For those times, you can create custom validation attributes by deriving from `ValidationAttribute` or changing your model to implement `IValidatableObject`.</span></span>

## <a name="notes-on-the-use-of-the-required-attribute"></a><span data-ttu-id="99ece-136">Poznámky k použití požadovaný atribut</span><span class="sxs-lookup"><span data-stu-id="99ece-136">Notes on the use of the Required attribute</span></span>

<span data-ttu-id="99ece-137">Neumožňující [typů hodnot](/dotnet/csharp/language-reference/keywords/value-types) (například `decimal`, `int`, `float`, a `DateTime`) jsou ze své podstaty povinné a nemusíte `Required` atribut.</span><span class="sxs-lookup"><span data-stu-id="99ece-137">Non-nullable [value types](/dotnet/csharp/language-reference/keywords/value-types) (such as `decimal`, `int`, `float`, and `DateTime`) are inherently required and don't need the `Required` attribute.</span></span> <span data-ttu-id="99ece-138">Aplikace provádí žádné kontroly ověřování na straně serveru pro typy neumožňující, které jsou označeny `Required`.</span><span class="sxs-lookup"><span data-stu-id="99ece-138">The app performs no server-side validation checks for non-nullable types that are marked `Required`.</span></span>

<span data-ttu-id="99ece-139">Vazby modelu MVC, která se týká ověření a atributů ověření, zamítne formuláře pole obsahující chybí hodnota nebo prázdné znaky Null typu.</span><span class="sxs-lookup"><span data-stu-id="99ece-139">MVC model binding, which isn't concerned with validation and validation attributes, rejects a form field submission containing a missing value or whitespace for a non-nullable type.</span></span> <span data-ttu-id="99ece-140">Chybí `BindRequired` atribut na cílovou vlastnost vazby modelu přeskočí chybějící data pro typy neumožňující, ve kterém chybí pole formuláře z příchozí data formuláře.</span><span class="sxs-lookup"><span data-stu-id="99ece-140">In the absence of a `BindRequired` attribute on the target property, model binding ignores missing data for non-nullable types, where the form field is absent from the incoming form data.</span></span>

<span data-ttu-id="99ece-141">[BindRequired atribut](/dotnet/api/microsoft.aspnetcore.mvc.modelbinding.bindrequiredattribute) (viz také [přizpůsobit chování vazby modelu s atributy](xref:mvc/models/model-binding#customize-model-binding-behavior-with-attributes)) je užitečný k zajištění toho, dokončení data formuláře.</span><span class="sxs-lookup"><span data-stu-id="99ece-141">The [BindRequired attribute](/dotnet/api/microsoft.aspnetcore.mvc.modelbinding.bindrequiredattribute) (also see [Customize model binding behavior with attributes](xref:mvc/models/model-binding#customize-model-binding-behavior-with-attributes)) is useful to ensure form data is complete.</span></span> <span data-ttu-id="99ece-142">Při použití na vlastnost, systém vazby modelu vyžaduje hodnotu pro tuto vlastnost.</span><span class="sxs-lookup"><span data-stu-id="99ece-142">When applied to a property, the model binding system requires a value for that property.</span></span> <span data-ttu-id="99ece-143">Při použití na typ, systém vazby modelu vyžaduje hodnoty pro všechny vlastnosti tohoto typu.</span><span class="sxs-lookup"><span data-stu-id="99ece-143">When applied to a type, the model binding system requires values for all of the properties of that type.</span></span>

<span data-ttu-id="99ece-144">Při použití [Nullable\<T > typ](/dotnet/csharp/programming-guide/nullable-types/) (například `decimal?` nebo `System.Nullable<decimal>`) a označte ji `Required`, se provádí kontroly ověřování na straně serveru, jako kdyby byly standardní typ připouštějící hodnotu Null (pro vlastnost například `string`).</span><span class="sxs-lookup"><span data-stu-id="99ece-144">When you use a [Nullable\<T> type](/dotnet/csharp/programming-guide/nullable-types/) (for example, `decimal?` or `System.Nullable<decimal>`) and mark it `Required`, a server-side validation check is performed as if the property were a standard nullable type (for example, a `string`).</span></span>

<span data-ttu-id="99ece-145">Ověřování na straně klienta vyžaduje hodnotu pro pole formuláře, který odpovídá vlastnosti modelu, který jste označili `Required` a Null typu vlastnosti, která nejsou označena `Required`.</span><span class="sxs-lookup"><span data-stu-id="99ece-145">Client-side validation requires a value for a form field that corresponds to a model property that you've marked `Required` and for a non-nullable type property that you haven't marked `Required`.</span></span> <span data-ttu-id="99ece-146">`Required` můžete použít k řízení chybovou zprávu ověření na straně klienta.</span><span class="sxs-lookup"><span data-stu-id="99ece-146">`Required` can be used to control the client-side validation error message.</span></span>

## <a name="model-state"></a><span data-ttu-id="99ece-147">Stav modelu</span><span class="sxs-lookup"><span data-stu-id="99ece-147">Model State</span></span>

<span data-ttu-id="99ece-148">Stav modelu představuje chyb při ověřování v zadané hodnoty formuláře HTML.</span><span class="sxs-lookup"><span data-stu-id="99ece-148">Model state represents validation errors in submitted HTML form values.</span></span>

<span data-ttu-id="99ece-149">MVC bude pokračovat až do dosáhne ověřování polí maximální počet chyb (200 ve výchozím nastavení).</span><span class="sxs-lookup"><span data-stu-id="99ece-149">MVC will continue validating fields until reaches the maximum number of errors (200 by default).</span></span> <span data-ttu-id="99ece-150">Toto číslo můžete nakonfigurovat tak, že vloží následující kód do `ConfigureServices` metodu *Startup.cs* souboru:</span><span class="sxs-lookup"><span data-stu-id="99ece-150">You can configure this number by inserting the following code into the `ConfigureServices` method in the *Startup.cs* file:</span></span>

[!code-csharp[](validation/sample/Startup.cs?range=27)]

## <a name="handling-model-state-errors"></a><span data-ttu-id="99ece-151">Chyby stavu modelu zpracování</span><span class="sxs-lookup"><span data-stu-id="99ece-151">Handling Model State Errors</span></span>

<span data-ttu-id="99ece-152">Ověření modelu nastává před každou volaná akce kontroleru a zodpovídá za metodu akce ke kontrole `ModelState.IsValid` a reagují odpovídajícím způsobem.</span><span class="sxs-lookup"><span data-stu-id="99ece-152">Model validation occurs prior to each controller action being invoked, and it's the action method's responsibility to inspect `ModelState.IsValid` and react appropriately.</span></span> <span data-ttu-id="99ece-153">V mnoha případech odpovídající reakce je reakce na chybu, ideálně s podrobnostmi o příčině selhání ověření modelu.</span><span class="sxs-lookup"><span data-stu-id="99ece-153">In many cases, the appropriate reaction is to return an error response, ideally detailing the reason why model validation failed.</span></span>

<span data-ttu-id="99ece-154">Některé aplikace zvolí dodržovat standardní konvence týkající se chyby ověření modelu, ve kterých může být případ filtr příslušné místo k implementaci těchto zásad.</span><span class="sxs-lookup"><span data-stu-id="99ece-154">Some apps will choose to follow a standard convention for dealing with model validation errors, in which case a filter may be an appropriate place to implement such a policy.</span></span> <span data-ttu-id="99ece-155">Měli byste otestovat chování vaše akce se stavy model platný nebo neplatný.</span><span class="sxs-lookup"><span data-stu-id="99ece-155">You should test how your actions behave with valid and invalid model states.</span></span>

## <a name="manual-validation"></a><span data-ttu-id="99ece-156">Ruční ověření</span><span class="sxs-lookup"><span data-stu-id="99ece-156">Manual validation</span></span>

<span data-ttu-id="99ece-157">Po dokončení vazby modelu a ověření můžete opakovat jeho části.</span><span class="sxs-lookup"><span data-stu-id="99ece-157">After model binding and validation are complete, you may want to repeat parts of it.</span></span> <span data-ttu-id="99ece-158">Například uživatel může zadat text v poli, očekává se celé číslo, nebo možná budete muset výpočtu hodnoty pro vlastnosti modelu.</span><span class="sxs-lookup"><span data-stu-id="99ece-158">For example, a user may have entered text in a field expecting an integer, or you may need to compute a value for a model's property.</span></span>

<span data-ttu-id="99ece-159">Budete muset ručně spusťte ověření.</span><span class="sxs-lookup"><span data-stu-id="99ece-159">You may need to run validation manually.</span></span> <span data-ttu-id="99ece-160">Chcete-li tak učinit, zavolejte `TryValidateModel` způsob, jak je znázorněno zde:</span><span class="sxs-lookup"><span data-stu-id="99ece-160">To do so, call the `TryValidateModel` method, as shown here:</span></span>

[!code-csharp[](validation/sample/MoviesController.cs?range=52)]

## <a name="custom-validation"></a><span data-ttu-id="99ece-161">Vlastní ověřování</span><span class="sxs-lookup"><span data-stu-id="99ece-161">Custom validation</span></span>

<span data-ttu-id="99ece-162">Atributy ověření fungovat pro většinu ověření potřeb.</span><span class="sxs-lookup"><span data-stu-id="99ece-162">Validation attributes work for most validation needs.</span></span> <span data-ttu-id="99ece-163">Některá pravidla ověřování jsou však specifické pro vaše podnikání.</span><span class="sxs-lookup"><span data-stu-id="99ece-163">However, some validation rules are specific to your business.</span></span> <span data-ttu-id="99ece-164">Pravidla nemusí být běžná technika ověření dat, jako je zajištění pole je povinné nebo, který odpovídá rozsahu hodnot.</span><span class="sxs-lookup"><span data-stu-id="99ece-164">Your rules might not be common data validation techniques such as ensuring a field is required or that it conforms to a range of values.</span></span> <span data-ttu-id="99ece-165">Vlastní ověřovací atributy pro tyto scénáře jsou vynikající řešení.</span><span class="sxs-lookup"><span data-stu-id="99ece-165">For these scenarios, custom validation attributes are a great solution.</span></span> <span data-ttu-id="99ece-166">Vytvoření vlastní ověřovací atributy v aplikaci MVC je jednoduché.</span><span class="sxs-lookup"><span data-stu-id="99ece-166">Creating your own custom validation attributes in MVC is easy.</span></span> <span data-ttu-id="99ece-167">Dědit pouze z `ValidationAttribute`a přepsat `IsValid` metody.</span><span class="sxs-lookup"><span data-stu-id="99ece-167">Just inherit from the `ValidationAttribute`, and override the `IsValid` method.</span></span> <span data-ttu-id="99ece-168">`IsValid` Metoda přijímá dva parametry, první je objekt s názvem *hodnotu* a druhá `ValidationContext` objekt s názvem *parametr validationContext*.</span><span class="sxs-lookup"><span data-stu-id="99ece-168">The `IsValid` method accepts two parameters, the first is an object named *value* and the second is a `ValidationContext` object named *validationContext*.</span></span> <span data-ttu-id="99ece-169">*Hodnota* odkazuje na skutečnou hodnotu z pole, která ověřuje svůj vlastní validátor.</span><span class="sxs-lookup"><span data-stu-id="99ece-169">*Value* refers to the actual value from the field that your custom validator is validating.</span></span>

<span data-ttu-id="99ece-170">V následujícím příkladu obchodní pravidlo uvádí, zda uživatelé nemusí nastaveno žánr *Classic* filmu vydanou po 1960.</span><span class="sxs-lookup"><span data-stu-id="99ece-170">In the following sample, a business rule states that users may not set the genre to *Classic* for a movie released after 1960.</span></span> <span data-ttu-id="99ece-171">`[ClassicMovie]` Atribut žánr nejprve zkontroluje, a pokud jde o klasický, pak zkontroluje, že je novější než 1960 datum vydání.</span><span class="sxs-lookup"><span data-stu-id="99ece-171">The `[ClassicMovie]` attribute checks the genre first, and if it's a classic, then it checks the release date to see that it's later than 1960.</span></span> <span data-ttu-id="99ece-172">Pokud se uvolní po 1960, ověření se nezdaří.</span><span class="sxs-lookup"><span data-stu-id="99ece-172">If it's released after 1960, validation fails.</span></span> <span data-ttu-id="99ece-173">Atribut přijímá jako parametr celé číslo představující rok, který vám pomůže ověřit data.</span><span class="sxs-lookup"><span data-stu-id="99ece-173">The attribute accepts an integer parameter representing the year that you can use to validate data.</span></span> <span data-ttu-id="99ece-174">Hodnota parametru v konstruktoru atributu, můžete zachytit, jak je znázorněno zde:</span><span class="sxs-lookup"><span data-stu-id="99ece-174">You can capture the value of the parameter in the attribute's constructor, as shown here:</span></span>

[!code-csharp[](validation/sample/ClassicMovieAttribute.cs?range=9-28)]

<span data-ttu-id="99ece-175">`movie` Proměnné výše představuje `Movie` objekt, který obsahuje data z odeslání formuláře k ověření.</span><span class="sxs-lookup"><span data-stu-id="99ece-175">The `movie` variable above represents a `Movie` object that contains the data from the form submission to validate.</span></span> <span data-ttu-id="99ece-176">V takovém případě ověřovací kód kontroluje data a rozšířením podle tematických v `IsValid` metodu `ClassicMovieAttribute` třídy podle pravidla.</span><span class="sxs-lookup"><span data-stu-id="99ece-176">In this case, the validation code checks the date and genre in the `IsValid` method of the `ClassicMovieAttribute` class as per the rules.</span></span> <span data-ttu-id="99ece-177">Po úspěšném ověření`IsValid` vrátí `ValidationResult.Success` kódu.</span><span class="sxs-lookup"><span data-stu-id="99ece-177">Upon successful validation ,`IsValid` returns a `ValidationResult.Success` code.</span></span> <span data-ttu-id="99ece-178">Když se ověřování nezdaří, `ValidationResult` s chybou je vrácená zpráva:</span><span class="sxs-lookup"><span data-stu-id="99ece-178">When validation fails, a `ValidationResult` with an error message is returned:</span></span>

[!code-csharp[](validation/sample/ClassicMovieAttribute.cs?range=55-58)]

<span data-ttu-id="99ece-179">Když uživatel změní `Genre` pole a odešle formulář, `IsValid` metodu `ClassicMovieAttribute` ověří, zda je tento film klasický.</span><span class="sxs-lookup"><span data-stu-id="99ece-179">When a user modifies the `Genre` field and submits the form, the `IsValid` method of the `ClassicMovieAttribute` will verify whether the movie is a classic.</span></span> <span data-ttu-id="99ece-180">Stejně jako všechny vestavěné atributy, použije `ClassicMovieAttribute` na vlastnost, jako `ReleaseDate` k zajištění ověření dojde, jak je znázorněno v předchozí ukázce kódu.</span><span class="sxs-lookup"><span data-stu-id="99ece-180">Like any built-in attribute, apply the `ClassicMovieAttribute` to a property such as `ReleaseDate` to ensure validation happens, as shown in the previous code sample.</span></span> <span data-ttu-id="99ece-181">Protože příklad funguje pouze s `Movie` typy, je vhodnější použít `IValidatableObject` jak je znázorněno v následujícím odstavci.</span><span class="sxs-lookup"><span data-stu-id="99ece-181">Since the example works only with `Movie` types, a better option is to use `IValidatableObject` as shown in the following paragraph.</span></span>

<span data-ttu-id="99ece-182">Alternativně může umístit tento stejný kód v modelu implementací `Validate` metodu na `IValidatableObject` rozhraní.</span><span class="sxs-lookup"><span data-stu-id="99ece-182">Alternatively, this same code could be placed in the model by implementing the `Validate` method on the `IValidatableObject` interface.</span></span> <span data-ttu-id="99ece-183">Při vlastní ověřovací atributy fungovat dobře pro jednotlivé vlastnosti ověřování, implementace `IValidatableObject` je možné provádět ověřování na úrovni třídy, jak je vidět tady.</span><span class="sxs-lookup"><span data-stu-id="99ece-183">While custom validation attributes work well for validating individual properties, implementing `IValidatableObject` can be used to implement class-level validation as seen here.</span></span>

[!code-csharp[](validation/sample/MovieIValidatable.cs?range=32-40)]

## <a name="client-side-validation"></a><span data-ttu-id="99ece-184">Ověřování na straně klienta</span><span class="sxs-lookup"><span data-stu-id="99ece-184">Client side validation</span></span>

<span data-ttu-id="99ece-185">Ověřování na straně klienta je skvělé usnadnění práce pro uživatele.</span><span class="sxs-lookup"><span data-stu-id="99ece-185">Client side validation is a great convenience for users.</span></span> <span data-ttu-id="99ece-186">To šetří čas, který byste jinak strávili čekání na odezvu na server.</span><span class="sxs-lookup"><span data-stu-id="99ece-186">It saves time they would otherwise spend waiting for a round trip to the server.</span></span> <span data-ttu-id="99ece-187">V obchodní termíny dokonce i pár zlomky sekund vynásobené opakovaném každý den přidá až hodně času a výdajů, frustrace.</span><span class="sxs-lookup"><span data-stu-id="99ece-187">In business terms, even a few fractions of seconds multiplied hundreds of times each day adds up to be a lot of time, expense, and frustration.</span></span> <span data-ttu-id="99ece-188">Jednoduché a okamžité ověřování umožňuje uživatelům vyšší efektivity práce a vytvářet lepší kvalitu vstupu a výstupu.</span><span class="sxs-lookup"><span data-stu-id="99ece-188">Straightforward and immediate validation enables users to work more efficiently and produce better quality input and output.</span></span>

<span data-ttu-id="99ece-189">Zobrazení s správné odkazy skriptu JavaScript musí mít nastavené pro ověřování na straně klienta fungovat jako tady vidíte.</span><span class="sxs-lookup"><span data-stu-id="99ece-189">You must have a view with the proper JavaScript script references in place for client side validation to work as you see here.</span></span>

[!code-cshtml[](validation/sample/Views/Shared/_Layout.cshtml?range=37)]

[!code-cshtml[](validation/sample/Views/Shared/_ValidationScriptsPartial.cshtml)]

<span data-ttu-id="99ece-190">[JQuery Nerušivý ověření](https://github.com/aspnet/jquery-validation-unobtrusive) skript je vlastní Microsoft front-endu knihovnu, která staví na oblíbené [jQuery ověřit](https://jqueryvalidation.org/) modulu plug-in.</span><span class="sxs-lookup"><span data-stu-id="99ece-190">The [jQuery Unobtrusive Validation](https://github.com/aspnet/jquery-validation-unobtrusive) script is a custom Microsoft front-end library that builds on the popular [jQuery Validate](https://jqueryvalidation.org/) plugin.</span></span> <span data-ttu-id="99ece-191">Bez jQuery Nerušivý ověřování, bude muset kód stejnou logiku ověřování na dvou místech: jednou v atributů ověření na straně serveru na vlastnosti projektu a poté znovu v skripty na straně klienta (příklady pro architekturu jQuery ověřením vaší [ `validate()` ](https://jqueryvalidation.org/validate/) metoda ukazuje, jak komplexní to může být).</span><span class="sxs-lookup"><span data-stu-id="99ece-191">Without jQuery Unobtrusive Validation, you would have to code the same validation logic in two places: once in the server side validation attributes on model properties, and then again in client side scripts (the examples for jQuery Validate's [`validate()`](https://jqueryvalidation.org/validate/) method shows how complex this could become).</span></span> <span data-ttu-id="99ece-192">Místo toho MVC [pomocných rutin značek](xref:mvc/views/tag-helpers/intro) a [pomocných rutin HTML](xref:mvc/views/overview) budou moct používat atributy ověření a metadata z vlastnosti modelu k vykreslení HTML 5 typu [datové atributy](http://w3c.github.io/html/dom.html#embedding-custom-non-visible-data-with-the-data-attributes) v elementy formuláře, které vyžadují ověřování.</span><span class="sxs-lookup"><span data-stu-id="99ece-192">Instead, MVC's [Tag Helpers](xref:mvc/views/tag-helpers/intro) and [HTML helpers](xref:mvc/views/overview) are able to use the validation attributes and type metadata from model properties to render HTML 5 [data- attributes](http://w3c.github.io/html/dom.html#embedding-custom-non-visible-data-with-the-data-attributes) in the form elements that need validation.</span></span> <span data-ttu-id="99ece-193">MVC vygeneruje `data-` atributy pro předdefinované a vlastní atributy.</span><span class="sxs-lookup"><span data-stu-id="99ece-193">MVC generates the `data-` attributes for both built-in and custom attributes.</span></span> <span data-ttu-id="99ece-194">pak analyzuje jQuery Nerušivý ověření `data-` atributy a předá logiku jQuery ověřit efektivně "kopírování" logiku ověřování na straně serveru do klienta.</span><span class="sxs-lookup"><span data-stu-id="99ece-194">jQuery Unobtrusive Validation then parses the `data-` attributes and passes the logic to jQuery Validate, effectively "copying" the server side validation logic to the client.</span></span> <span data-ttu-id="99ece-195">Zobrazení chyb ověřování na straně klienta, použití pomocných rutin značek relevantní, jak je znázorněno zde:</span><span class="sxs-lookup"><span data-stu-id="99ece-195">You can display validation errors on the client using the relevant tag helpers as shown here:</span></span>

[!code-cshtml[](validation/sample/Views/Movies/Create.cshtml?highlight=4,5&range=19-25)]

<span data-ttu-id="99ece-196">Výše uvedené pomocných rutin značek vykreslení HTML níže.</span><span class="sxs-lookup"><span data-stu-id="99ece-196">The tag helpers above render the HTML below.</span></span> <span data-ttu-id="99ece-197">Všimněte si, že `data-` atributů v kódu HTML výstup odpovídají atributů ověření pro `ReleaseDate` vlastnost.</span><span class="sxs-lookup"><span data-stu-id="99ece-197">Notice that the `data-` attributes in the HTML output correspond to the validation attributes for the `ReleaseDate` property.</span></span> <span data-ttu-id="99ece-198">`data-val-required` Atribut obsahuje chybovou zprávu se zobrazí, pokud uživatel nemá vyplnit pole Datum vydání verze.</span><span class="sxs-lookup"><span data-stu-id="99ece-198">The `data-val-required` attribute below contains an error message to display if the user doesn't fill in the release date field.</span></span> <span data-ttu-id="99ece-199">jQuery Nerušivý ověření tuto hodnotu předá jQuery ověřit [ `required()` ](https://jqueryvalidation.org/required-method/) metodu, která se zobrazí zpráva v souvisejícím  **\<span >** elementu.</span><span class="sxs-lookup"><span data-stu-id="99ece-199">jQuery Unobtrusive Validation passes this value to the jQuery Validate [`required()`](https://jqueryvalidation.org/required-method/) method, which then displays that message in the accompanying **\<span>** element.</span></span>

```html
<form action="/Movies/Create" method="post">
    <div class="form-horizontal">
        <h4>Movie</h4>
        <div class="text-danger"></div>
        <div class="form-group">
            <label class="col-md-2 control-label" for="ReleaseDate">ReleaseDate</label>
            <div class="col-md-10">
                <input class="form-control" type="datetime"
                data-val="true" data-val-required="The ReleaseDate field is required."
                id="ReleaseDate" name="ReleaseDate" value="" />
                <span class="text-danger field-validation-valid"
                data-valmsg-for="ReleaseDate" data-valmsg-replace="true"></span>
            </div>
        </div>
    </div>
</form>
```

<span data-ttu-id="99ece-200">Ověřování na straně klienta zabrání odeslání dokud formulář není platný.</span><span class="sxs-lookup"><span data-stu-id="99ece-200">Client-side validation prevents submission until the form is valid.</span></span> <span data-ttu-id="99ece-201">Tlačítka pro odeslání spustí jazyka JavaScript, který se odešle formulář nebo zobrazí chybové zprávy.</span><span class="sxs-lookup"><span data-stu-id="99ece-201">The Submit button runs JavaScript that either submits the form or displays error messages.</span></span>

<span data-ttu-id="99ece-202">Určuje typ hodnoty atributů na základě .NET datového typu vlastnosti, případně přepsat pomocí MVC `[DataType]` atributy.</span><span class="sxs-lookup"><span data-stu-id="99ece-202">MVC determines type attribute values based on the .NET data type of a property, possibly overridden using `[DataType]` attributes.</span></span> <span data-ttu-id="99ece-203">Základní `[DataType]` atribut nemá žádné reálné serverové ověření.</span><span class="sxs-lookup"><span data-stu-id="99ece-203">The base `[DataType]` attribute does no real server-side validation.</span></span> <span data-ttu-id="99ece-204">Prohlížeče zvolte vlastní chybové zprávy a zobrazit opravu těchto chyb, jak si přejí, ale můžete balíček ověření Nerušivého jQuery přepsat zprávy a zobrazí je konzistentní s ostatními.</span><span class="sxs-lookup"><span data-stu-id="99ece-204">Browsers choose their own error messages and display those errors as they wish, however the jQuery Validation Unobtrusive package can override the messages and display them consistently with others.</span></span> <span data-ttu-id="99ece-205">To se stane, když uživatelé nejvíce samozřejmě používat `[DataType]` podtřídy jako `[EmailAddress]`.</span><span class="sxs-lookup"><span data-stu-id="99ece-205">This happens most obviously when users apply `[DataType]` subclasses such as `[EmailAddress]`.</span></span>

### <a name="add-validation-to-dynamic-forms"></a><span data-ttu-id="99ece-206">Přidání ověřování pro dynamické formuláře</span><span class="sxs-lookup"><span data-stu-id="99ece-206">Add Validation to Dynamic Forms</span></span>

<span data-ttu-id="99ece-207">Protože jQuery Nerušivý ověření předá logiku ověřování a parametry jQuery ověřit při prvním načtení stránky, dynamicky generované formulářů neprojevuje automaticky ověření.</span><span class="sxs-lookup"><span data-stu-id="99ece-207">Because jQuery Unobtrusive Validation passes validation logic and parameters to jQuery Validate when the page first loads, dynamically generated forms won't automatically exhibit validation.</span></span> <span data-ttu-id="99ece-208">Místo toho je zapotřebí sdělit jQuery Nerušivý ověření analyzovat dynamická podoba ihned po jeho vytvoření.</span><span class="sxs-lookup"><span data-stu-id="99ece-208">Instead, you must tell jQuery Unobtrusive Validation to parse the dynamic form immediately after creating it.</span></span> <span data-ttu-id="99ece-209">Například následující kód ukazuje, jak můžete nastavit ověřování na straně klienta ve formuláři přidány prostřednictvím AJAX.</span><span class="sxs-lookup"><span data-stu-id="99ece-209">For example, the code below shows how you might set up client side validation on a form added via AJAX.</span></span>

```js
$.get({
    url: "https://url/that/returns/a/form",
    dataType: "html",
    error: function(jqXHR, textStatus, errorThrown) {
        alert(textStatus + ": Couldn't add form. " + errorThrown);
    },
    success: function(newFormHTML) {
        var container = document.getElementById("form-container");
        container.insertAdjacentHTML("beforeend", newFormHTML);
        var forms = container.getElementsByTagName("form");
        var newForm = forms[forms.length - 1];
        $.validator.unobtrusive.parse(newForm);
    }
})
```

<span data-ttu-id="99ece-210">`$.validator.unobtrusive.parse()` Metoda přijímá selektor jQuery pro svůj argument.</span><span class="sxs-lookup"><span data-stu-id="99ece-210">The `$.validator.unobtrusive.parse()` method accepts a jQuery selector for its one argument.</span></span> <span data-ttu-id="99ece-211">Tato metoda říká jQuery Nerušivý ověření analyzovat `data-` atributy formulářů v rámci tohoto selektoru.</span><span class="sxs-lookup"><span data-stu-id="99ece-211">This method tells jQuery Unobtrusive Validation to parse the `data-` attributes of forms within that selector.</span></span> <span data-ttu-id="99ece-212">Hodnoty tyto atributy jsou potom předány do modulu plug-in jQuery ověřit tak, aby formuláře je třeba požadovanou na straně ověřovacích pravidel klienta atributu.</span><span class="sxs-lookup"><span data-stu-id="99ece-212">The values of those attributes are then passed to the jQuery Validate plugin so that the form exhibits the desired client side validation rules.</span></span>

### <a name="add-validation-to-dynamic-controls"></a><span data-ttu-id="99ece-213">Přidání ověřování pro dynamické ovládací prvky</span><span class="sxs-lookup"><span data-stu-id="99ece-213">Add Validation to Dynamic Controls</span></span>

<span data-ttu-id="99ece-214">Můžete také aktualizovat ověřovací pravidla ve formuláři, pokud jednotlivé ovládací prvky, jako `<input/>`s a `<select/>`s, generuje dynamicky.</span><span class="sxs-lookup"><span data-stu-id="99ece-214">You can also update the validation rules on a form when individual controls, such as `<input/>`s and `<select/>`s, are dynamically generated.</span></span> <span data-ttu-id="99ece-215">Nelze předat selektory pro tyto prvky, aby `parse()` metoda přímo protože okolní formulář již byl analyzován a nebude aktualizovat.</span><span class="sxs-lookup"><span data-stu-id="99ece-215">You cannot pass selectors for these elements to the `parse()` method directly because the surrounding form has already been parsed and won't update.</span></span> <span data-ttu-id="99ece-216">Místo toho je nejprve odeberte stávající ověřovací data, potom rozboru celý formulář, jak je znázorněno níže:</span><span class="sxs-lookup"><span data-stu-id="99ece-216">Instead, you first remove the existing validation data, then reparse the entire form, as shown below:</span></span>

```js
$.get({
    url: "https://url/that/returns/a/control",
    dataType: "html",
    error: function(jqXHR, textStatus, errorThrown) {
        alert(textStatus + ": Couldn't add control. " + errorThrown);
    },
    success: function(newInputHTML) {
        var form = document.getElementById("my-form");
        form.insertAdjacentHTML("beforeend", newInputHTML);
        $(form).removeData("validator")    // Added by jQuery Validate
               .removeData("unobtrusiveValidation");   // Added by jQuery Unobtrusive Validation
        $.validator.unobtrusive.parse(form);
    }
})
```

## <a name="iclientmodelvalidator"></a><span data-ttu-id="99ece-217">IClientModelValidator</span><span class="sxs-lookup"><span data-stu-id="99ece-217">IClientModelValidator</span></span>

<span data-ttu-id="99ece-218">Můžete vytvořit logiku na straně klienta pro vaše vlastní atribut a [nerušivý ověření](http://bradwilson.typepad.com/blog/2010/10/mvc3-unobtrusive-validation.html) která vytvoří adaptér pro [k ověřování jquery](http://jqueryvalidation.org/documentation/) se spustí na straně klienta za vás automaticky jako součást ověření.</span><span class="sxs-lookup"><span data-stu-id="99ece-218">You may create client side logic for your custom attribute, and [unobtrusive validation](http://bradwilson.typepad.com/blog/2010/10/mvc3-unobtrusive-validation.html) which creates an adapter to [jquery validation](http://jqueryvalidation.org/documentation/) will execute it on the client for you automatically as part of validation.</span></span> <span data-ttu-id="99ece-219">Prvním krokem je řídit, jaké atributy dat. přidá implementace `IClientModelValidator` rozhraní, jak je znázorněno zde:</span><span class="sxs-lookup"><span data-stu-id="99ece-219">The first step is to control what data- attributes are added by implementing the `IClientModelValidator` interface as shown here:</span></span>

[!code-csharp[](validation/sample/ClassicMovieAttribute.cs?range=30-42)]

<span data-ttu-id="99ece-220">Atributy, které toto rozhraní implementují můžete přidat do generované pole atributů HTML.</span><span class="sxs-lookup"><span data-stu-id="99ece-220">Attributes that implement this interface can add HTML attributes to generated fields.</span></span> <span data-ttu-id="99ece-221">Zkoumání výstup `ReleaseDate` element odhalí kód HTML, který je podobný jako předchozí příklad, s tím rozdílem, nyní je `data-val-classicmovie` atribut, který byl definován v `AddValidation` metodu `IClientModelValidator`.</span><span class="sxs-lookup"><span data-stu-id="99ece-221">Examining the output for the `ReleaseDate` element reveals HTML that's similar to the previous example, except now there's a `data-val-classicmovie` attribute that was defined in the `AddValidation` method of `IClientModelValidator`.</span></span>

```html
<input class="form-control" type="datetime"
    data-val="true"
    data-val-classicmovie="Classic movies must have a release year earlier than 1960."
    data-val-classicmovie-year="1960"
    data-val-required="The ReleaseDate field is required."
    id="ReleaseDate" name="ReleaseDate" value="" />
```

<span data-ttu-id="99ece-222">Ověření nerušivého používá data v `data-` atributy se mají zobrazit chybové zprávy.</span><span class="sxs-lookup"><span data-stu-id="99ece-222">Unobtrusive validation uses the data in the `data-` attributes to display error messages.</span></span> <span data-ttu-id="99ece-223">Nicméně jQuery nebude vědět o pravidlech nebo zprávy, dokud je nepřidáte do prvku jQuery `validator` objektu.</span><span class="sxs-lookup"><span data-stu-id="99ece-223">However, jQuery doesn't know about rules or messages until you add them to jQuery's `validator` object.</span></span> <span data-ttu-id="99ece-224">To je ukázáno v následujícím příkladu, který přidá vlastní `classicmovie` způsob ověření klienta jQuery `validator` objektu.</span><span class="sxs-lookup"><span data-stu-id="99ece-224">This is shown in the following example, which adds a custom `classicmovie` client validation method to the jQuery `validator` object.</span></span> <span data-ttu-id="99ece-225">Vysvětlení `unobtrusive.adapters.add` metodu, najdete v článku [Nerušivý ověření klienta v architektuře ASP.NET MVC](http://bradwilson.typepad.com/blog/2010/10/mvc3-unobtrusive-validation.html).</span><span class="sxs-lookup"><span data-stu-id="99ece-225">For an explanation of the `unobtrusive.adapters.add` method, see [Unobtrusive Client Validation in ASP.NET MVC](http://bradwilson.typepad.com/blog/2010/10/mvc3-unobtrusive-validation.html).</span></span>

[!code-javascript[](validation/sample/Views/Movies/Create.cshtml?name=snippet_UnobtrusiveValidation)]

<span data-ttu-id="99ece-226">S předchozím kódu `classicmovie` metoda provádí ověřování na straně klienta na film datum vydání.</span><span class="sxs-lookup"><span data-stu-id="99ece-226">With the preceding code, the `classicmovie` method performs client-side validation on the movie release date.</span></span> <span data-ttu-id="99ece-227">Chybová zpráva se zobrazí, pokud metoda vrátí `false`.</span><span class="sxs-lookup"><span data-stu-id="99ece-227">The error message displays if the method returns `false`.</span></span>

## <a name="remote-validation"></a><span data-ttu-id="99ece-228">Vzdálené ověření</span><span class="sxs-lookup"><span data-stu-id="99ece-228">Remote validation</span></span>

<span data-ttu-id="99ece-229">Vzdálené ověření je skvělé funkce, která použijte, pokud je potřeba ověřit data na straně klienta s daty na serveru.</span><span class="sxs-lookup"><span data-stu-id="99ece-229">Remote validation is a great feature to use when you need to validate data on the client against data on the server.</span></span> <span data-ttu-id="99ece-230">Vaše aplikace může například potřebovat a ověřte, jestli e-mail nebo uživatelské jméno je již používán a dotáže velké množství dat k tomu.</span><span class="sxs-lookup"><span data-stu-id="99ece-230">For example, your app may need to verify whether an email or user name is already in use, and it must query a large amount of data to do so.</span></span> <span data-ttu-id="99ece-231">Stahování velkých sad dat pro ověření jednu nebo několik polí spotřebovává příliš mnoho prostředků.</span><span class="sxs-lookup"><span data-stu-id="99ece-231">Downloading large sets of data for validating one or a few fields consumes too many resources.</span></span> <span data-ttu-id="99ece-232">Také to může zveřejnit citlivé informace.</span><span class="sxs-lookup"><span data-stu-id="99ece-232">It may also expose sensitive information.</span></span> <span data-ttu-id="99ece-233">Další možností je vytvořit round-trip žádost o ověření pole.</span><span class="sxs-lookup"><span data-stu-id="99ece-233">An alternative is to make a round-trip request to validate a field.</span></span>

<span data-ttu-id="99ece-234">Implementace vzdáleného ověřování ve dvou krocích.</span><span class="sxs-lookup"><span data-stu-id="99ece-234">You can implement remote validation in a two step process.</span></span> <span data-ttu-id="99ece-235">Nejprve musíte označit modelu pomocí `[Remote]` atribut.</span><span class="sxs-lookup"><span data-stu-id="99ece-235">First, you must annotate your model with the `[Remote]` attribute.</span></span> <span data-ttu-id="99ece-236">`[Remote]` Atribut je možné zadat více přetížení, můžete použít ke směrování JavaScript na straně klienta na odpovídající kód pro volání.</span><span class="sxs-lookup"><span data-stu-id="99ece-236">The `[Remote]` attribute accepts multiple overloads you can use to direct client side JavaScript to the appropriate code to call.</span></span> <span data-ttu-id="99ece-237">Následující příklad ukazuje na `VerifyEmail` metody akce `Users` kontroleru.</span><span class="sxs-lookup"><span data-stu-id="99ece-237">The example below points to the `VerifyEmail` action method of the `Users` controller.</span></span>

[!code-csharp[](validation/sample/User.cs?range=7-8)]

<span data-ttu-id="99ece-238">Druhý krok je uvedení kód pro ověření v odpovídající metody akce, jak jsou definovány v `[Remote]` atribut.</span><span class="sxs-lookup"><span data-stu-id="99ece-238">The second step is putting the validation code in the corresponding action method as defined in the `[Remote]` attribute.</span></span> <span data-ttu-id="99ece-239">Podle jQuery ověřit [vzdálené](https://jqueryvalidation.org/remote-method/) metoda dokumentaci odpovědi serveru musí být řetězec formátu JSON, který je buď:</span><span class="sxs-lookup"><span data-stu-id="99ece-239">According to the jQuery Validate [remote](https://jqueryvalidation.org/remote-method/) method documentation, the server response must be a JSON string that's either:</span></span>

* <span data-ttu-id="99ece-240">`"true"` pro platné prvky.</span><span class="sxs-lookup"><span data-stu-id="99ece-240">`"true"` for valid elements.</span></span>
* <span data-ttu-id="99ece-241">`"false"`, `undefined`, nebo `null` neplatné elementy pomocí výchozí chybovou zprávu.</span><span class="sxs-lookup"><span data-stu-id="99ece-241">`"false"`, `undefined`, or `null` for invalid elements, using the default error message.</span></span>

<span data-ttu-id="99ece-242">Pokud odpověď serveru je řetězec (například `"That name is already taken, try peter123 instead"`), řetězec se zobrazí jako vlastní chybovou zprávu místo výchozí řetězec.</span><span class="sxs-lookup"><span data-stu-id="99ece-242">If the server response is a string (for example, `"That name is already taken, try peter123 instead"`), the string is displayed as a custom error message in place of the default string.</span></span>

<span data-ttu-id="99ece-243">Definice `VerifyEmail` metoda řídí následujícími pravidly, jak je znázorněno níže.</span><span class="sxs-lookup"><span data-stu-id="99ece-243">The definition of the `VerifyEmail` method follows these rules, as shown below.</span></span> <span data-ttu-id="99ece-244">Vrátí Chyba ověřování zpráv, pokud se používá e-mailu, nebo `true` Pokud e-mailu je zdarma a zabalí výsledek `JsonResult` objektu.</span><span class="sxs-lookup"><span data-stu-id="99ece-244">It returns a validation error message if the email is taken, or `true` if the email is free, and wraps the result in a `JsonResult` object.</span></span> <span data-ttu-id="99ece-245">Na straně klienta můžete použít vrácené hodnoty pak pokračujte v případě potřeby zobrazí chybu.</span><span class="sxs-lookup"><span data-stu-id="99ece-245">The client side can then use the returned value to proceed or display the error if needed.</span></span>

[!code-csharp[](validation/sample/UsersController.cs?range=19-28)]

<span data-ttu-id="99ece-246">Nyní když uživatelé zadají e-mailu, JavaScript v zobrazení zavolá vzdálené zobrazíte-li tuto e-mailu je už zabraný a pokud ano, zobrazí se chybová zpráva.</span><span class="sxs-lookup"><span data-stu-id="99ece-246">Now when users enter an email, JavaScript in the view makes a remote call to see if that email has been taken and, if so, displays the error message.</span></span> <span data-ttu-id="99ece-247">V opačném případě uživatel odešle formulář jako obvykle.</span><span class="sxs-lookup"><span data-stu-id="99ece-247">Otherwise, the user can submit the form as usual.</span></span>

<span data-ttu-id="99ece-248">`AdditionalFields` Vlastnost `[Remote]` atribut je užitečné pro ověření kombinace různých typů polí s daty na serveru.</span><span class="sxs-lookup"><span data-stu-id="99ece-248">The `AdditionalFields` property of the `[Remote]` attribute is useful for validating combinations of fields against data on the server.</span></span> <span data-ttu-id="99ece-249">Například pokud `User` modelu výše má dvě další vlastnosti volá `FirstName` a `LastName`, můžete chtít ověřit, že stávající uživatelé už nemají tohoto páru názvy.</span><span class="sxs-lookup"><span data-stu-id="99ece-249">For example, if the `User` model from above had two additional properties called `FirstName` and `LastName`, you might want to verify that no existing users already have that pair of names.</span></span> <span data-ttu-id="99ece-250">Můžete definovat nové vlastnosti, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="99ece-250">You define the new properties as shown in the following code:</span></span>

[!code-csharp[](validation/sample/User.cs?range=10-13)]

<span data-ttu-id="99ece-251">`AdditionalFields` mohli jste explicitně nastavena na řetězce `"FirstName"` a `"LastName"`, ale pomocí [ `nameof` ](https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/nameof) operátor takto zjednodušuje později refaktoring.</span><span class="sxs-lookup"><span data-stu-id="99ece-251">`AdditionalFields` could've been set explicitly to the strings `"FirstName"` and `"LastName"`, but using the [`nameof`](https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/nameof) operator like this simplifies later refactoring.</span></span> <span data-ttu-id="99ece-252">Metody akce k provedení ověření pak musí přijmout dva argumenty, jeden pro hodnotu vlastnosti `FirstName` a jeden pro hodnotu `LastName`.</span><span class="sxs-lookup"><span data-stu-id="99ece-252">The action method to perform the validation must then accept two arguments, one for the value of `FirstName` and one for the value of `LastName`.</span></span>

[!code-csharp[](validation/sample/UsersController.cs?range=30-39)]

<span data-ttu-id="99ece-253">Nyní když uživatelé zadat křestní jméno a příjmení, JavaScript:</span><span class="sxs-lookup"><span data-stu-id="99ece-253">Now when users enter a first and last name, JavaScript:</span></span>

* <span data-ttu-id="99ece-254">Díky vzdálené volání zobrazíte, pokud je už zabraný tohoto dvojice názvů.</span><span class="sxs-lookup"><span data-stu-id="99ece-254">Makes a remote call to see if that pair of names has been taken.</span></span>
* <span data-ttu-id="99ece-255">Pokud je už zabraný dvojici, zobrazí se chybová zpráva.</span><span class="sxs-lookup"><span data-stu-id="99ece-255">If the pair has been taken, an error message is displayed.</span></span>
* <span data-ttu-id="99ece-256">Pokud ne, uživatel odešle formulář.</span><span class="sxs-lookup"><span data-stu-id="99ece-256">If not taken, the user can submit the form.</span></span>

<span data-ttu-id="99ece-257">Pokud je potřeba ověřit dvě nebo více polí s `[Remote]` atribut, je uvádíte jako seznam oddělený čárkami.</span><span class="sxs-lookup"><span data-stu-id="99ece-257">If you need to validate two or more additional fields with the `[Remote]` attribute, you provide them as a comma-delimited list.</span></span> <span data-ttu-id="99ece-258">Například, chcete-li přidat `MiddleName` nastavena vlastnost modelu, `[Remote]` atributu, jak je znázorněno v následujícím kódu:</span><span class="sxs-lookup"><span data-stu-id="99ece-258">For example, to add a `MiddleName` property to the model, set the `[Remote]` attribute as shown in the following code:</span></span>

```cs
[Remote(action: "VerifyName", controller: "Users", AdditionalFields = nameof(FirstName) + "," + nameof(LastName))]
public string MiddleName { get; set; }
```

<span data-ttu-id="99ece-259">`AdditionalFields`, jako jsou všechny argumenty atributu musí být konstantní výraz.</span><span class="sxs-lookup"><span data-stu-id="99ece-259">`AdditionalFields`, like all attribute arguments, must be a constant expression.</span></span> <span data-ttu-id="99ece-260">Proto se nesmí používat [interpolovaný řetězec](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/interpolated-strings) nebo volání [ `string.Join()` ](https://msdn.microsoft.com/library/system.string.join(v=vs.110).aspx) inicializovat `AdditionalFields`.</span><span class="sxs-lookup"><span data-stu-id="99ece-260">Therefore, you must not use an [interpolated string](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/interpolated-strings) or call [`string.Join()`](https://msdn.microsoft.com/library/system.string.join(v=vs.110).aspx) to initialize `AdditionalFields`.</span></span> <span data-ttu-id="99ece-261">Pro každé další pole, které přidáte do `[Remote]` atribut, je nutné přidat další argument na odpovídající metodu akce kontroleru.</span><span class="sxs-lookup"><span data-stu-id="99ece-261">For every additional field that you add to the `[Remote]` attribute, you must add another argument to the corresponding controller action method.</span></span>
