---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
title: "Představení technologie ASP.NET Web Pages – aktualizace dat databáze | Microsoft Docs"
author: tfitzmac
description: "V tomto kurzu se dozvíte, jak aktualizovat položku (změnit) existující databáze při použití technologie ASP.NET Web Pages (Razor). Předpokládá, že jste dokončili řady tý..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 01/02/2018
ms.topic: article
ms.assetid: ac86ec9c-6b69-485b-b9e0-8b9127b13e6b
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/updating-data
msc.type: authoredcontent
ms.openlocfilehash: 6fc8631c323dbb82f4f33b78128ba8de8bc9bfdd
ms.sourcegitcommit: 2d23ea501e0213bbacf65298acf1c8bd17209540
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 01/09/2018
---
<a name="introducing-aspnet-web-pages---updating-database-data"></a><span data-ttu-id="83882-104">Představení technologie ASP.NET Web Pages – aktualizace dat databáze</span><span class="sxs-lookup"><span data-stu-id="83882-104">Introducing ASP.NET Web Pages - Updating Database Data</span></span>
====================
<span data-ttu-id="83882-105">podle [tní FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="83882-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="83882-106">V tomto kurzu se dozvíte, jak aktualizovat položku (změnit) existující databáze při použití technologie ASP.NET Web Pages (Razor).</span><span class="sxs-lookup"><span data-stu-id="83882-106">This tutorial shows you how to update (change) an existing database entry when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="83882-107">Předpokládá, že jste dokončili řady prostřednictvím [zadávání dat pomocí pomocí formulářů pomocí rozhraní ASP.NET Web Pages](entering-data.md).</span><span class="sxs-lookup"><span data-stu-id="83882-107">It assumes you have completed the series through [Entering Data by Using Forms Using ASP.NET Web Pages](entering-data.md).</span></span>
> 
> <span data-ttu-id="83882-108">Získáte informace:</span><span class="sxs-lookup"><span data-stu-id="83882-108">What you'll learn:</span></span>
> 
> - <span data-ttu-id="83882-109">Jak vybrat jednotlivé záznamy v `WebGrid` pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="83882-109">How to select an individual record in the `WebGrid` helper.</span></span>
> - <span data-ttu-id="83882-110">Jak si jeden záznam z databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-110">How to read a single record from a database.</span></span>
> - <span data-ttu-id="83882-111">Postup předběžné načtení formuláře s hodnotami ze záznamů databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-111">How to preload a form with values from the database record.</span></span>
> - <span data-ttu-id="83882-112">Postup aktualizace stávajícího záznamu v databázi.</span><span class="sxs-lookup"><span data-stu-id="83882-112">How to update an existing record in a database.</span></span>
> - <span data-ttu-id="83882-113">Tom, jak ukládat informace na stránce bez jeho zobrazení.</span><span class="sxs-lookup"><span data-stu-id="83882-113">How to store information in the page without displaying it.</span></span>
> - <span data-ttu-id="83882-114">Jak používat k ukládání informací skryté pole.</span><span class="sxs-lookup"><span data-stu-id="83882-114">How to use a hidden field to store information.</span></span>
>   
> 
> <span data-ttu-id="83882-115">Funkce nebo technologie popsané:</span><span class="sxs-lookup"><span data-stu-id="83882-115">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="83882-116">`WebGrid` Pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="83882-116">The `WebGrid` helper.</span></span>
> - <span data-ttu-id="83882-117">SQL `Update` příkaz.</span><span class="sxs-lookup"><span data-stu-id="83882-117">The SQL `Update` command.</span></span>
> - <span data-ttu-id="83882-118">`Database.Execute` Metoda.</span><span class="sxs-lookup"><span data-stu-id="83882-118">The `Database.Execute` method.</span></span>
> - <span data-ttu-id="83882-119">Skryté pole (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="83882-119">Hidden fields (`<input type="hidden">`).</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="83882-120">Co budete sestavení</span><span class="sxs-lookup"><span data-stu-id="83882-120">What You'll Build</span></span>

<span data-ttu-id="83882-121">V tomto kurzu předchozí jste zjistili, jak přidat záznam do databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-121">In the previous tutorial, you learned how to add a record to a database.</span></span> <span data-ttu-id="83882-122">Zde dozvíte jak zobrazit záznam pro úpravy.</span><span class="sxs-lookup"><span data-stu-id="83882-122">Here, you'll learn how to display a record for editing.</span></span> <span data-ttu-id="83882-123">V *filmy* stránky, budete aktualizovat `WebGrid` pomocné rutiny, které se zobrazí **upravit** odkaz vedle jednotlivých film:</span><span class="sxs-lookup"><span data-stu-id="83882-123">In the *Movies* page, you'll update the `WebGrid` helper so that it displays an **Edit** link next to each movie:</span></span>

![WebGrid zobrazení, včetně odkazu na "Upravit" pro každý film](updating-data/_static/image1.png)

<span data-ttu-id="83882-125">Když kliknete **upravit** odkaz, tím přejdete na jinou stránku, kde informace film je již ve formuláři:</span><span class="sxs-lookup"><span data-stu-id="83882-125">When you click the **Edit** link, it takes you to a different page, where the movie information is already in a form:</span></span>

![Upravit film stránky zobrazující film bude upravena](updating-data/_static/image2.png)

<span data-ttu-id="83882-127">Můžete změnit žádnou hodnotu.</span><span class="sxs-lookup"><span data-stu-id="83882-127">You can change any of the values.</span></span> <span data-ttu-id="83882-128">Při odesílání změn kódu na stránce aktualizuje databázi a přejde zpět na film výpis.</span><span class="sxs-lookup"><span data-stu-id="83882-128">When you submit the changes, the code in the page updates the database and takes you back to the movie listing.</span></span>

<span data-ttu-id="83882-129">Tato část procesu funguje skoro stejně jako *AddMovie.cshtml* stránky, které jste vytvořili v předchozí kurz, takže většinu tohoto kurzu bude známé.</span><span class="sxs-lookup"><span data-stu-id="83882-129">This part of the process works almost exactly like the *AddMovie.cshtml* page you created in the previous tutorial, so much of this tutorial will be familiar.</span></span>

<span data-ttu-id="83882-130">Způsob, jak upravit jednotlivé film může implementovat několika způsoby.</span><span class="sxs-lookup"><span data-stu-id="83882-130">There are several ways you could implement a way to edit an individual movie.</span></span> <span data-ttu-id="83882-131">Přístup zobrazí jste vybrali, protože je snadno implementovat a pochopitelnější.</span><span class="sxs-lookup"><span data-stu-id="83882-131">The approach shown was chosen because it's easy to implement and easy to understand.</span></span>

## <a name="adding-an-edit-link-to-the-movie-listing"></a><span data-ttu-id="83882-132">Přidávání do seznamu film odkaz pro úpravy</span><span class="sxs-lookup"><span data-stu-id="83882-132">Adding an Edit Link to the Movie Listing</span></span>

<span data-ttu-id="83882-133">Pokud chcete začít, budete aktualizovat *filmy* stránky tak, aby každý film výpis také obsahuje **upravit** odkaz.</span><span class="sxs-lookup"><span data-stu-id="83882-133">To begin, you'll update the *Movies* page so that each movie listing also contains an **Edit** link.</span></span>

<span data-ttu-id="83882-134">Otevřete *Movies.cshtml* souboru.</span><span class="sxs-lookup"><span data-stu-id="83882-134">Open the *Movies.cshtml* file.</span></span>

<span data-ttu-id="83882-135">V těle stránky změnit `WebGrid` značek přidáním sloupce.</span><span class="sxs-lookup"><span data-stu-id="83882-135">In the body of the page, change the `WebGrid` markup by adding a column.</span></span> <span data-ttu-id="83882-136">Tady je upravených značek:</span><span class="sxs-lookup"><span data-stu-id="83882-136">Here's the modified markup:</span></span>

[!code-html[Main](updating-data/samples/sample1.html?highlight=6)]

<span data-ttu-id="83882-137">Nový sloupec, je tato:</span><span class="sxs-lookup"><span data-stu-id="83882-137">The new column is this one:</span></span>

[!code-html[Main](updating-data/samples/sample2.html)]

<span data-ttu-id="83882-138">Bod v tomto sloupci se zobrazí odkaz (`<a>` element) jejíž text říká "Upravit".</span><span class="sxs-lookup"><span data-stu-id="83882-138">The point of this column is to show a link (`<a>` element) whose text says "Edit".</span></span> <span data-ttu-id="83882-139">Máme po je vytvoření odkaz, který vypadá takto při spuštění stránky s `id` hodnotu pro každý film jiné:</span><span class="sxs-lookup"><span data-stu-id="83882-139">What we're after is to create a link that looks like the following when the page runs, with the `id` value different for each movie:</span></span>

[!code-css[Main](updating-data/samples/sample3.css)]

<span data-ttu-id="83882-140">Tento odkaz bude vyvolat stránku s názvem *EditMovie*, a předá řetězec dotazu `?id=7` na této stránce.</span><span class="sxs-lookup"><span data-stu-id="83882-140">This link will invoke a page named *EditMovie*, and it will pass the query string `?id=7` to that page.</span></span>

<span data-ttu-id="83882-141">Syntaxe pro nový sloupec, může vypadat poněkud složité, je to ale jenom protože umístí společně několik elementy.</span><span class="sxs-lookup"><span data-stu-id="83882-141">The syntax for the new column might look a bit complex, but that's only because it puts together several elements.</span></span> <span data-ttu-id="83882-142">Každý jednotlivý prvek je jednoduchá.</span><span class="sxs-lookup"><span data-stu-id="83882-142">Each individual element is straightforward.</span></span> <span data-ttu-id="83882-143">Pokud vám soustředit se jenom na právě na `<a>` elementu, zobrazí tento kód:</span><span class="sxs-lookup"><span data-stu-id="83882-143">If you concentrate on just the `<a>` element, you see this markup:</span></span>

[!code-html[Main](updating-data/samples/sample4.html)]

<span data-ttu-id="83882-144">Některé informace o tom, jak funguje mřížky: v mřížce zobrazené řádky, jeden pro každý záznam v databázi, a zobrazuje sloupce pro každé pole v záznamu v databázi.</span><span class="sxs-lookup"><span data-stu-id="83882-144">Some background about how the grid works: the grid displays rows, one for each database record, and it displays columns for each field in the database record.</span></span> <span data-ttu-id="83882-145">Když je vytvářen každý řádek mřížky, `item` objekt obsahuje záznam databáze (položek) pro tento řádek.</span><span class="sxs-lookup"><span data-stu-id="83882-145">While each grid row is being constructed, the `item` object contains the database record (item) for that row.</span></span> <span data-ttu-id="83882-146">Toto uspořádání poskytuje způsob, jak můžete získat na data pro tento řádek v kódu.</span><span class="sxs-lookup"><span data-stu-id="83882-146">This arrangement gives you a way in code to get at the data for that row.</span></span> <span data-ttu-id="83882-147">To je, co vidíte zde: výraz `item.ID` dochází hodnota ID položky pro aktuální databázi.</span><span class="sxs-lookup"><span data-stu-id="83882-147">That's what you see here: the expression `item.ID` is getting the ID value of the current database item.</span></span> <span data-ttu-id="83882-148">Můžete získat žádné z hodnot v databázi (název, genre nebo rok) stejným způsobem jako pomocí `item.Title`, `item.Genre`, nebo `item.Year`.</span><span class="sxs-lookup"><span data-stu-id="83882-148">You could get any of the database values (title, genre, or year) the same way by using `item.Title`, `item.Genre`, or `item.Year`.</span></span>

<span data-ttu-id="83882-149">Výraz `"~/EditMovie?id=@item.ID` kombinuje součástí pevně cílová adresa URL (`~/EditMovie?id=`) s číslem ID této dynamicky odvozené.</span><span class="sxs-lookup"><span data-stu-id="83882-149">The expression `"~/EditMovie?id=@item.ID` combines the hard-coded part of the target URL (`~/EditMovie?id=`) with this dynamically derived ID.</span></span> <span data-ttu-id="83882-150">(Jste viděli `~` operátor v předchozí kurzu; je operátor ASP.NET, který představuje aktuální kořenový adresář webu.)</span><span class="sxs-lookup"><span data-stu-id="83882-150">(You saw the `~` operator in the previous tutorial; it's an ASP.NET operator that represents the current website root.)</span></span>

<span data-ttu-id="83882-151">Výsledkem je, že tato součást kód ve sloupci jednoduše vytvoří něco jako následující kód v době běhu:</span><span class="sxs-lookup"><span data-stu-id="83882-151">The result is that this part of the markup in the column simply produces something like the following markup at run time:</span></span>

[!code-xml[Main](updating-data/samples/sample5.xml)]

<span data-ttu-id="83882-152">Samozřejmě se skutečnou hodnotou `id` se bude lišit pro každý řádek.</span><span class="sxs-lookup"><span data-stu-id="83882-152">Naturally, the actual value of `id` will be different for each row.</span></span>

## <a name="creating-a-custom-display-for-a-grid-column"></a><span data-ttu-id="83882-153">Vytváření vlastních zobrazení sloupce mřížky</span><span class="sxs-lookup"><span data-stu-id="83882-153">Creating a Custom Display for a Grid Column</span></span>

<span data-ttu-id="83882-154">Nyní zpět na sloupci mřížky.</span><span class="sxs-lookup"><span data-stu-id="83882-154">Now back to the grid column.</span></span> <span data-ttu-id="83882-155">Tři sloupce původně bylo v mřížce zobrazí pouze data hodnoty (název, genre a roku).</span><span class="sxs-lookup"><span data-stu-id="83882-155">The three columns you originally had in the grid displayed only data values (title, genre, and year).</span></span> <span data-ttu-id="83882-156">Toto zobrazení určeného předávání název sloupce databáze &mdash; například `grid.Column("Title")`.</span><span class="sxs-lookup"><span data-stu-id="83882-156">You specified this display by passing the name of the database column &mdash; for example, `grid.Column("Title")`.</span></span>

<span data-ttu-id="83882-157">Tento nový **upravit** odkaz sloupce se liší.</span><span class="sxs-lookup"><span data-stu-id="83882-157">This new **Edit** link column is different.</span></span> <span data-ttu-id="83882-158">Místo zadání název sloupce, které předávání `format` parametr.</span><span class="sxs-lookup"><span data-stu-id="83882-158">Instead of specifying a column name, you're passing a `format` parameter.</span></span> <span data-ttu-id="83882-159">Tento parametr umožňuje definovat značek, `WebGrid` pomocná učiní spolu s `item` hodnotu pro zobrazení data ve sloupci jako tučné a zelenou nebo ve formátu, které chcete.</span><span class="sxs-lookup"><span data-stu-id="83882-159">This parameter lets you define markup that the `WebGrid` helper will render along with the `item` value to display the column data as bold or green or in whatever format that you want.</span></span> <span data-ttu-id="83882-160">Například pokud byste chtěli nadpis zobrazuje tučným písmem, můžete vytvořit sloupec následujícím způsobem:</span><span class="sxs-lookup"><span data-stu-id="83882-160">For example, if you wanted the title to appear bold, you could create a column like this example:</span></span>

[!code-html[Main](updating-data/samples/sample6.html)]

<span data-ttu-id="83882-161">(Různé `@` znaky, které se zobrazí v `format` vlastnost označit přechod mezi značek a kódu hodnotu.)</span><span class="sxs-lookup"><span data-stu-id="83882-161">(The various `@` characters you see in the `format` property mark the transition between markup and a code value.)</span></span>

<span data-ttu-id="83882-162">Jakmile víte o `format` vlastnost, je jednodušší zjistit, jak nové **upravit** připravili odkaz sloupce:</span><span class="sxs-lookup"><span data-stu-id="83882-162">Once you know about the `format` property, it's easier to understand how the new **Edit** link column is put together:</span></span>

[!code-html[Main](updating-data/samples/sample7.html)]

<span data-ttu-id="83882-163">Sloupec obsahuje *pouze* z kódu, který vykreslí odkaz, plus některé informace (ID), je extrahována ze záznamů databáze pro řádek.</span><span class="sxs-lookup"><span data-stu-id="83882-163">The column consists *only* of the markup that renders the link, plus some information (the ID) that's extracted from the database record for the row.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="83882-164">**Poziční parametry pro metodu a pojmenované parametry**</span><span class="sxs-lookup"><span data-stu-id="83882-164">**Named Parameters and Positional Parameters for a Method**</span></span>
> 
> <span data-ttu-id="83882-165">Tolikrát, kolikrát jste volal metodu a do ní předán parametry, jednoduše uvedené v seznamu hodnot parametrů oddělených čárkami.</span><span class="sxs-lookup"><span data-stu-id="83882-165">Many times when you've called a method and passed parameters to it, you've simply listed the parameter values separated by commas.</span></span> <span data-ttu-id="83882-166">Tady je několik příkladů:</span><span class="sxs-lookup"><span data-stu-id="83882-166">Here are a couple of examples:</span></span>
> 
> `db.Execute(insertCommand, title, genre, year)`
> 
> `Validation.RequireField("title", "You must enter a title")`
> 
> <span data-ttu-id="83882-167">Jsme nepomohly problém zmínili, když znáte tento kód, ale v každém případě se předávání do metod v určitém pořadí parametrů &mdash; konkrétně, pořadí, ve kterém jsou definovány parametry v dané metody.</span><span class="sxs-lookup"><span data-stu-id="83882-167">We didn't mention the issue when you first saw this code, but in each case, you're passing parameters to the methods in a specific order &mdash; namely, the order in which the parameters are defined in that method.</span></span> <span data-ttu-id="83882-168">Pro `db.Execute` a `Validation.RequireFields`, pokud se promíchají pořadí hodnot předáte, by získat chybová zpráva při spuštění stránky nebo alespoň některé neobvyklé výsledky.</span><span class="sxs-lookup"><span data-stu-id="83882-168">For `db.Execute` and `Validation.RequireFields`, if you mixed up the order of the values you pass, you'd get an error message when the page runs, or at least some strange results.</span></span> <span data-ttu-id="83882-169">Je zřejmé budete muset vědět předat parametry v pořadí.</span><span class="sxs-lookup"><span data-stu-id="83882-169">Clearly, you have to know the order to pass the parameters in.</span></span> <span data-ttu-id="83882-170">(Ve službě WebMatrix, IntelliSense vám může pomoci další obrázek na název, typ a pořadí parametrů.)</span><span class="sxs-lookup"><span data-stu-id="83882-170">(In WebMatrix, IntelliSense can help you learn figure out the name, type, and order of the parameters.)</span></span>
> 
> <span data-ttu-id="83882-171">Jako alternativu k předávání hodnot v pořadí, můžete použít *pojmenované parametry*.</span><span class="sxs-lookup"><span data-stu-id="83882-171">As an alternative to passing values in order, you can use *named parameters*.</span></span> <span data-ttu-id="83882-172">(Předávání parametrů v pořadí se označuje jako pomocí *poziční parametry*.) Pojmenované parametry je explicitně zahrnout název parametru při předávání jeho hodnotu.</span><span class="sxs-lookup"><span data-stu-id="83882-172">(Passing parameters in order is known as using *positional parameters*.) For named parameters, you explicitly include the name of the parameter when passing its value.</span></span> <span data-ttu-id="83882-173">Jste použili pojmenované parametry již několikrát v těchto kurzech.</span><span class="sxs-lookup"><span data-stu-id="83882-173">You've used named parameters already a number of times in these tutorials.</span></span> <span data-ttu-id="83882-174">Příklad:</span><span class="sxs-lookup"><span data-stu-id="83882-174">For example:</span></span>
> 
> [!code-csharp[Main](updating-data/samples/sample8.cs)]
> 
> <span data-ttu-id="83882-175">and</span><span class="sxs-lookup"><span data-stu-id="83882-175">and</span></span>
> 
> [!code-css[Main](updating-data/samples/sample9.css)]
> 
> <span data-ttu-id="83882-176">Pojmenované parametry jsou užitečné několik situací, zejména v případě, že metoda přebírá mnoho parametrů.</span><span class="sxs-lookup"><span data-stu-id="83882-176">Named parameters are handy for a couple of situations, especially when a method takes many parameters.</span></span> <span data-ttu-id="83882-177">Jedna je, když chcete předat pouze jeden nebo dva parametry, ale hodnoty, které chcete předat nejsou mezi první pozice v seznamu parametrů.</span><span class="sxs-lookup"><span data-stu-id="83882-177">One is when you want to pass only one or two parameters, but the values you want to pass are not among the first positions in the parameter list.</span></span> <span data-ttu-id="83882-178">Jiné situaci je, pokud chcete zvýšit vašeho kódu předáním parametry v pořadí, ve kterém je pro vás nejvhodnější.</span><span class="sxs-lookup"><span data-stu-id="83882-178">Another situation is when you want to make your code more readable by passing the parameters in the order that makes the most sense to you.</span></span>
> 
> <span data-ttu-id="83882-179">Samozřejmě používat pojmenované parametry, budete muset znát názvy parametrů.</span><span class="sxs-lookup"><span data-stu-id="83882-179">Obviously, to use named parameters, you have to know the names of the parameters.</span></span> <span data-ttu-id="83882-180">Služba WebMatrix IntelliSense můžete *zobrazit* názvy, ale nemůžete vyplnit aktuálně je pro vás.</span><span class="sxs-lookup"><span data-stu-id="83882-180">WebMatrix IntelliSense can *show* you the names, but it cannot currently fill them in for you.</span></span>


## <a name="creating-the-edit-page"></a><span data-ttu-id="83882-181">Vytvoření stránky úpravy</span><span class="sxs-lookup"><span data-stu-id="83882-181">Creating the Edit Page</span></span>

<span data-ttu-id="83882-182">Nyní můžete vytvořit *EditMovie* stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-182">Now you can create the *EditMovie* page.</span></span> <span data-ttu-id="83882-183">Když uživatelé kliknou na **upravit** odkazu, který se nakonec se na této stránce.</span><span class="sxs-lookup"><span data-stu-id="83882-183">When users click the **Edit** link, they'll end up on this page.</span></span>

<span data-ttu-id="83882-184">Vytvoření stránky s názvem *EditMovie.cshtml* a nahraďte, co je v souboru s následující kód:</span><span class="sxs-lookup"><span data-stu-id="83882-184">Create a page named *EditMovie.cshtml* and replace what's in the file with the following markup:</span></span>

[!code-cshtml[Main](updating-data/samples/sample10.cshtml)]

<span data-ttu-id="83882-185">Tento kód a kód je podobná máte ve *AddMovie* stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-185">This markup and code is similar to what you have in the *AddMovie* page.</span></span> <span data-ttu-id="83882-186">Je malý rozdíl v text pro tlačítko pro odeslání.</span><span class="sxs-lookup"><span data-stu-id="83882-186">There's a small difference in the text for the submit button.</span></span> <span data-ttu-id="83882-187">Stejně jako u *AddMovie* stránky, dojde `Html.ValidationSummary` volání, které se zobrazí chyby ověření, pokud jsou k dispozici.</span><span class="sxs-lookup"><span data-stu-id="83882-187">As with the *AddMovie* page, there's an `Html.ValidationSummary` call that will display validation errors if there are any.</span></span> <span data-ttu-id="83882-188">Tentokrát jsme se vynechala volání `Validation.Message`, protože se zobrazí chyby v souhrnu ověření.</span><span class="sxs-lookup"><span data-stu-id="83882-188">This time we're leaving out calls to `Validation.Message`, since errors will be displayed in the validation summary.</span></span> <span data-ttu-id="83882-189">Jak jsme uvedli v předchozím kurzu, můžete použít v různých kombinacích souhrnu ověření a jednotlivé chybové zprávy.</span><span class="sxs-lookup"><span data-stu-id="83882-189">As noted in the previous tutorial, you can use the validation summary and the individual error messages in various combinations.</span></span>

<span data-ttu-id="83882-190">Všimněte si znovu, že `method` atribut `<form>` element je nastaven na hodnotu `post`.</span><span class="sxs-lookup"><span data-stu-id="83882-190">Notice again that the `method` attribute of the `<form>` element is set to `post`.</span></span> <span data-ttu-id="83882-191">Stejně jako u *AddMovie.cshtml* stránky, tato stránka umožňuje změny do databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-191">As with the *AddMovie.cshtml* page, this page makes changes to the database.</span></span> <span data-ttu-id="83882-192">Proto by měla provést tento formulář `POST` operaci.</span><span class="sxs-lookup"><span data-stu-id="83882-192">Therefore, this form should perform a `POST` operation.</span></span> <span data-ttu-id="83882-193">(Další informace o rozdílu mezi `GET` a `POST` operací, najdete v článku [GET, POST a zabezpečení protokolu HTTP příkaz](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) bočním panelu v tomto kurzu ve formulářích HTML.)</span><span class="sxs-lookup"><span data-stu-id="83882-193">(For more about the difference between `GET` and `POST` operations, see the [GET, POST, and HTTP Verb Safety](form-basics.md#GET,_POST,_and_HTTP_Verb_Safety) sidebar in the tutorial on HTML forms.)</span></span>

<span data-ttu-id="83882-194">Jak už jste viděli v dřívější kurzu, `value` atributy do textových polí se se nastavují s kódu Razor, aby bylo možné přednačtení je.</span><span class="sxs-lookup"><span data-stu-id="83882-194">As you saw in an earlier tutorial, the `value` attributes of the text boxes are being set with Razor code in order to preload them.</span></span> <span data-ttu-id="83882-195">Tentokrát, když používáte proměnné jako `title` a `genre` pro tuto úlohu místo `Request.Form["title"]`:</span><span class="sxs-lookup"><span data-stu-id="83882-195">This time, though, you're using variables like `title` and `genre` for that task instead of `Request.Form["title"]`:</span></span>

`<input type="text" name="title" value="@title" />`

<span data-ttu-id="83882-196">Jako dříve, tento kód bude přednačtení textové hodnoty pole s hodnotami film.</span><span class="sxs-lookup"><span data-stu-id="83882-196">As before, this markup will preload the text box values with the movie values.</span></span> <span data-ttu-id="83882-197">Se zobrazí ve chvíli, proč je užitečné používat proměnné tentokrát místo použití `Request` objektu.</span><span class="sxs-lookup"><span data-stu-id="83882-197">You'll see in a moment why it's handy to use variables this time instead of using the `Request` object.</span></span>

<span data-ttu-id="83882-198">K dispozici je také `<input type="hidden">` element na této stránce.</span><span class="sxs-lookup"><span data-stu-id="83882-198">There's also a `<input type="hidden">` element on this page.</span></span> <span data-ttu-id="83882-199">Tento element ukládá ID film bez viditelnosti na stránce.</span><span class="sxs-lookup"><span data-stu-id="83882-199">This element stores the movie ID without making it visible on the page.</span></span> <span data-ttu-id="83882-200">ID je původně předána na stránku pomocí pomocí hodnoty řetězce dotazu (`?id=7` nebo podobné v adrese URL).</span><span class="sxs-lookup"><span data-stu-id="83882-200">The ID is initially passed to the page by using a query string value (`?id=7` or similar in the URL).</span></span> <span data-ttu-id="83882-201">Hodnota ID vložíte do skryté pole, můžete zkontrolovat, zda je k dispozici při odeslání formuláře, i když už máte přístup k původní adresu URL, který stránka byla vyvolána s.</span><span class="sxs-lookup"><span data-stu-id="83882-201">By putting the ID value into a hidden field, you can make sure that it's available when the form is submitted, even if you no longer have access to the original URL that the page was invoked with.</span></span>

<span data-ttu-id="83882-202">Na rozdíl od *AddMovie* stránky kód pro *EditMovie* stránky má dvě odlišné funkce.</span><span class="sxs-lookup"><span data-stu-id="83882-202">Unlike the *AddMovie* page, the code for the *EditMovie* page has two distinct functions.</span></span> <span data-ttu-id="83882-203">První funkce je, že pokud stránka se zobrazí při prvním (a *pouze* pak), kód získá ID film z řetězce dotazu.</span><span class="sxs-lookup"><span data-stu-id="83882-203">The first function is that when the page is displayed for the first time (and *only* then), the code gets the movie ID from the query string.</span></span> <span data-ttu-id="83882-204">Kód pak používá ID, které ke čtení odpovídající film z databáze nástroje a zobrazení (přednačtení) ji do textových polí.</span><span class="sxs-lookup"><span data-stu-id="83882-204">The code then uses the ID to read the corresponding movie out of the database and display (preload) it in the text boxes.</span></span>

<span data-ttu-id="83882-205">Funkce second je, že když uživatel klikne **odeslání změn** tlačítko kód má číst hodnoty do textových polí a jejich ověření.</span><span class="sxs-lookup"><span data-stu-id="83882-205">The second function is that when the user clicks the **Submit Changes** button, the code has to read the values of the text boxes and validate them.</span></span> <span data-ttu-id="83882-206">Kód má také k aktualizaci položky databáze s novými hodnotami.</span><span class="sxs-lookup"><span data-stu-id="83882-206">The code also has to update the database item with the new values.</span></span> <span data-ttu-id="83882-207">Tento postup je podobný jako při přidávání záznam, jako jste viděli v *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="83882-207">This technique is similar to adding a record, as you saw in *AddMovie*.</span></span>

## <a name="adding-code-to-read-a-single-movie"></a><span data-ttu-id="83882-208">Přidání kódu ke čtení jeden film</span><span class="sxs-lookup"><span data-stu-id="83882-208">Adding Code to Read a Single Movie</span></span>

<span data-ttu-id="83882-209">K provedení první funkci, přidejte do horní části stránky tento kód:</span><span class="sxs-lookup"><span data-stu-id="83882-209">To perform the first function, add this code to the top of the page:</span></span>

[!code-cshtml[Main](updating-data/samples/sample11.cshtml)]

<span data-ttu-id="83882-210">Většina tohoto kódu je uvnitř bloku, která se spouští `if(!IsPost)`.</span><span class="sxs-lookup"><span data-stu-id="83882-210">Most of this code is inside a block that starts `if(!IsPost)`.</span></span> <span data-ttu-id="83882-211">`!` Operátor znamená "Ne", takže znamená výraz *Pokud tento požadavek není post odeslání*, což je nepřímým způsobem o tom, že *Pokud tento požadavek je při prvním spuštění této stránce*.</span><span class="sxs-lookup"><span data-stu-id="83882-211">The `!` operator means "not," so the expression means *if this request is not a post submission*, which is an indirect way of saying *if this request is the first time that this page has been run*.</span></span> <span data-ttu-id="83882-212">Jak již bylo uvedeno dříve, tento kód by měl spustit *pouze* při prvním spuštění stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-212">As noted earlier, this code should run *only* the first time the page runs.</span></span> <span data-ttu-id="83882-213">Pokud nebylo uzavřete kód v `if(!IsPost)`, by spustit při každém stránka je vyvolána, jestli poprvé nebo v reakci na tlačítko klikněte na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="83882-213">If you didn't enclose the code in `if(!IsPost)`, it would run every time the page is invoked, whether the first time or in response to a button click.</span></span>

<span data-ttu-id="83882-214">Všimněte si, že tento kód obsahuje `else` blokovat této doby.</span><span class="sxs-lookup"><span data-stu-id="83882-214">Notice that the code includes an `else` block this time.</span></span> <span data-ttu-id="83882-215">Jak jsme uvedli, když zavedli jsme `if` bloky, někdy chcete spustit alternativní kódu, pokud není podmínka testujete hodnotu true.</span><span class="sxs-lookup"><span data-stu-id="83882-215">As we said when we introduced `if` blocks, sometimes you want to run alternative code if the condition you're testing isn't true.</span></span> <span data-ttu-id="83882-216">Je to tento případ sem.</span><span class="sxs-lookup"><span data-stu-id="83882-216">That's the case here.</span></span> <span data-ttu-id="83882-217">Pokud podmínka předá (to znamená, pokud je třeba ID předána na stránku ok), přečíst řádek z databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-217">If the condition passes (that is, if the ID passed to the page is ok), you read a row from the database.</span></span> <span data-ttu-id="83882-218">Ale pokud podmínka neprojde, `else` bloku běží a kód nastaví chybovou zprávu.</span><span class="sxs-lookup"><span data-stu-id="83882-218">However, if the condition doesn't pass, the `else` block runs and the code sets an error message.</span></span>

## <a name="validating-a-value-passed-to-the-page"></a><span data-ttu-id="83882-219">Ověřování hodnotu předána na stránku</span><span class="sxs-lookup"><span data-stu-id="83882-219">Validating a Value Passed to the Page</span></span>

<span data-ttu-id="83882-220">Kód používá `Request.QueryString["id"]` získat ID, která je předána na stránku.</span><span class="sxs-lookup"><span data-stu-id="83882-220">The code uses `Request.QueryString["id"]` to get the ID that's passed to the page.</span></span> <span data-ttu-id="83882-221">Kód zajišťuje, že hodnota byla zadána ve skutečnosti ID.</span><span class="sxs-lookup"><span data-stu-id="83882-221">The code makes sure that a value was actually passed for the ID.</span></span> <span data-ttu-id="83882-222">Pokud byla předána žádná hodnota, nastaví kód chyby ověřování.</span><span class="sxs-lookup"><span data-stu-id="83882-222">If no value was passed, the code sets a validation error.</span></span>

<span data-ttu-id="83882-223">Tento kód ukazuje jiný způsob, jak ověřit informace.</span><span class="sxs-lookup"><span data-stu-id="83882-223">This code shows a different way to validate information.</span></span> <span data-ttu-id="83882-224">V tomto kurzu předchozí pracoval s `Validation` pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="83882-224">In the previous tutorial, you worked with the `Validation` helper.</span></span> <span data-ttu-id="83882-225">Registrovaných pole k ověření a automaticky se ověřování ASP.NET a zobrazí chyby pomocí `Html.ValidationMessage` a `Html.ValidationSummary`.</span><span class="sxs-lookup"><span data-stu-id="83882-225">You registered fields to validate, and ASP.NET automatically did the validation and displayed errors by using `Html.ValidationMessage` and `Html.ValidationSummary`.</span></span> <span data-ttu-id="83882-226">V takovém případě však můžete jste ověřování není skutečně uživatelského vstupu.</span><span class="sxs-lookup"><span data-stu-id="83882-226">In this case, however, you're not really validating user input.</span></span> <span data-ttu-id="83882-227">Místo toho že ověřování hodnotu, která byla předána na stránku z jinde.</span><span class="sxs-lookup"><span data-stu-id="83882-227">Instead, you're validating a value that was passed to the page from elsewhere.</span></span> <span data-ttu-id="83882-228">`Validation` Pomocné rutiny, která není to pro vás.</span><span class="sxs-lookup"><span data-stu-id="83882-228">The `Validation` helper doesn't do that for you.</span></span>

<span data-ttu-id="83882-229">Proto byste zkontrolovat hodnotu sami, vyzkoušejte s `if(!Request.QueryString["ID"].IsEmpty()`).</span><span class="sxs-lookup"><span data-stu-id="83882-229">Therefore, you check the value yourself, by testing it with `if(!Request.QueryString["ID"].IsEmpty()`).</span></span> <span data-ttu-id="83882-230">Pokud dojde k potížím, můžete zobrazit chyba pomocí `Html.ValidationSummary`, stejně jako `Validation` pomocné rutiny.</span><span class="sxs-lookup"><span data-stu-id="83882-230">If there's a problem, you can display the error by using `Html.ValidationSummary`, as you did with the `Validation` helper.</span></span> <span data-ttu-id="83882-231">Chcete-li to udělat, zavolejte `Validation.AddFormError` a předejte ji zprávu zobrazíte.</span><span class="sxs-lookup"><span data-stu-id="83882-231">To do that, you call `Validation.AddFormError` and pass it a message to display.</span></span> <span data-ttu-id="83882-232">`Validation.AddFormError`je integrované metoda, která umožňuje definovat vlastní zprávy, které se pomocí ověřování systému, které jste již obeznámeni s svázání.</span><span class="sxs-lookup"><span data-stu-id="83882-232">`Validation.AddFormError` is a built-in method that lets you define custom messages that tie in with the validation system you're already familiar with.</span></span> <span data-ttu-id="83882-233">(Později v tomto kurzu budeme mluvit o tom, aby tento proces ověření trochu robustnější.)</span><span class="sxs-lookup"><span data-stu-id="83882-233">(Later in this tutorial we'll talk about how to make this validation process a little more robust.)</span></span>

<span data-ttu-id="83882-234">Po provedení se, že je ID videa, kód čte databázi, hledá pouze položku jedné databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-234">After making sure that there's an ID for the movie, the code reads the database, looking for only a single database item.</span></span> <span data-ttu-id="83882-235">(Pravděpodobně jste si všimli obecné vzor pro databázové operace: Otevřete databázi, definovat příkazu jazyka SQL a spusťte příkaz.) Tentokrát SQL `Select` příkaz obsahuje `WHERE ID = @0`.</span><span class="sxs-lookup"><span data-stu-id="83882-235">(You probably have noticed the general pattern for database operations: open the database, define a SQL statement, and run the statement.) This time, the SQL `Select` statement includes `WHERE ID = @0`.</span></span> <span data-ttu-id="83882-236">Protože ID je jedinečný, se může vracet jenom jeden záznam.</span><span class="sxs-lookup"><span data-stu-id="83882-236">Because the ID is unique, only one record can be returned.</span></span>

<span data-ttu-id="83882-237">Dotaz se provádí pomocí `db.QuerySingle` (není `db.Query`, jako jste použili movie seznam), a vloží kód výsledku do `row` proměnné.</span><span class="sxs-lookup"><span data-stu-id="83882-237">The query is performed by using `db.QuerySingle` (not `db.Query`, as you used for the movie listing), and the code puts the result into the `row` variable.</span></span> <span data-ttu-id="83882-238">Název `row` libovolný; zadáte název proměnné jakkoli chcete.</span><span class="sxs-lookup"><span data-stu-id="83882-238">The name `row` is arbitrary; you can name the variables anything you like.</span></span> <span data-ttu-id="83882-239">Proměnné inicializovat v horní části pak hodnotu podrobnosti o filmu tak, aby tyto hodnoty lze zobrazit v textových polích.</span><span class="sxs-lookup"><span data-stu-id="83882-239">The variables initialized at the top are then filled with the movie details so that these values can be displayed in the text boxes.</span></span>

## <a name="testing-the-edit-page-so-far"></a><span data-ttu-id="83882-240">Testování stránce pro úpravy (dosavadní)</span><span class="sxs-lookup"><span data-stu-id="83882-240">Testing the Edit Page (So Far)</span></span>

<span data-ttu-id="83882-241">Pokud chcete otestovat stránku, spusťte *filmy* nyní a klikněte na tlačítko **upravit** odkaz vedle žádné film.</span><span class="sxs-lookup"><span data-stu-id="83882-241">If you'd like to test your page, run the *Movies* page now and click an **Edit** link next to any movie.</span></span> <span data-ttu-id="83882-242">Zobrazí se *EditMovie* stránky s podrobnostmi předvyplněné film jste vybrali:</span><span class="sxs-lookup"><span data-stu-id="83882-242">You'll see the *EditMovie* page with the details filled in for the movie you selected:</span></span>

![Upravit film stránky zobrazující film bude upravena](updating-data/_static/image3.png)

<span data-ttu-id="83882-244">Všimněte si, že adresa URL stránky obsahuje něco podobného jako `?id=10` (nebo jiné telefonní číslo).</span><span class="sxs-lookup"><span data-stu-id="83882-244">Notice that the URL of the page includes something like `?id=10` (or some other number).</span></span> <span data-ttu-id="83882-245">Dosavadní který otestujete **upravit** odkazů v *film* stránky práce, stránku je přečtení ID z řetězce dotazu, a že databázi dotazy s cílem získat jeden film záznam pracuje.</span><span class="sxs-lookup"><span data-stu-id="83882-245">So far you've tested that **Edit** links in the *Movie* page work, that your page is reading the ID from the query string, and that the database query to get a single movie record is working.</span></span>

<span data-ttu-id="83882-246">Můžete změnit informace film, ale nic se stane, když kliknete na tlačítko **odeslání změn**.</span><span class="sxs-lookup"><span data-stu-id="83882-246">You can change the movie information, but nothing happens when you click **Submit Changes**.</span></span>

## <a name="adding-code-to-update-the-movie-with-the-users-changes"></a><span data-ttu-id="83882-247">Přidání kódu k aktualizaci film uživatele změn</span><span class="sxs-lookup"><span data-stu-id="83882-247">Adding Code to Update the Movie with the User's Changes</span></span>

<span data-ttu-id="83882-248">V *EditMovie.cshtml* souboru, pokud chcete implementovat funkce second (ukládají se změny), přidejte následující kód pouze uvnitř složená závorka `@` bloku.</span><span class="sxs-lookup"><span data-stu-id="83882-248">In the *EditMovie.cshtml* file, to implement the second function (saving changes), add the following code just inside the closing brace of the `@` block.</span></span> <span data-ttu-id="83882-249">(Pokud si nejste jistí, přesně umístění kód, můžete se podívat na [úplný výpis pro stránku upravit film kódu](#Complete_Page_Listing_for_EditMovie) , zobrazí se na konci tohoto kurzu.)</span><span class="sxs-lookup"><span data-stu-id="83882-249">(If you're not sure exactly where to put the code, you can look at the [complete code listing for the Edit Movie page](#Complete_Page_Listing_for_EditMovie) that appears at the end of this tutorial.)</span></span>

[!code-csharp[Main](updating-data/samples/sample12.cs)]

<span data-ttu-id="83882-250">Znovu, tento kód a kód je podobný kódu v *AddMovie*.</span><span class="sxs-lookup"><span data-stu-id="83882-250">Again, this markup and code is similar to the code in *AddMovie*.</span></span> <span data-ttu-id="83882-251">Kód je v `if(IsPost)` blokovat, protože tento kód spustí pouze v případě, že uživatel klikne **odeslání změn** tlačítko &mdash; , kdy (a pouze tehdy, když) formulář odeslal.</span><span class="sxs-lookup"><span data-stu-id="83882-251">The code is in an `if(IsPost)` block, because this code runs only when the user clicks the **Submit Changes** button &mdash; that is, when (and only when) the form has been posted.</span></span> <span data-ttu-id="83882-252">V takovém případě nepoužíváte testu jako `if(IsPost && Validation.IsValid())`– to znamená, nejsou kombinace obou testů pomocí AND.</span><span class="sxs-lookup"><span data-stu-id="83882-252">In this case, you're not using a test like `if(IsPost && Validation.IsValid())`— that is, you're not combining both tests by using AND.</span></span> <span data-ttu-id="83882-253">Na této stránce můžete nejprve určit, jestli je odeslání formuláře (`if(IsPost)`) a pak zaregistrujte pole pro ověření.</span><span class="sxs-lookup"><span data-stu-id="83882-253">In this page, you first determine whether there's a form submission (`if(IsPost)`), and only then register the fields for validation.</span></span> <span data-ttu-id="83882-254">Potom můžete testovat výsledky ověření (`if(Validation.IsValid()`).</span><span class="sxs-lookup"><span data-stu-id="83882-254">Then you can test the validation results (`if(Validation.IsValid()`).</span></span> <span data-ttu-id="83882-255">Tok se poněkud liší od v *AddMovie.cshtml* stránky, ale účinek je stejný.</span><span class="sxs-lookup"><span data-stu-id="83882-255">The flow is slightly different than in the *AddMovie.cshtml* page, but the effect is the same.</span></span>

<span data-ttu-id="83882-256">Získat hodnoty do textových polí pomocí `Request.Form["title"]` a podobný kód pro druhý `<input>` elementy.</span><span class="sxs-lookup"><span data-stu-id="83882-256">You get the values of the text boxes by using `Request.Form["title"]` and similar code for the other `<input>` elements.</span></span> <span data-ttu-id="83882-257">Všimněte si, že tuto chvíli kód získá film ID mimo skryté pole (`<input type="hidden">`).</span><span class="sxs-lookup"><span data-stu-id="83882-257">Notice that this time, the code gets the movie ID out of the hidden field (`<input type="hidden">`).</span></span> <span data-ttu-id="83882-258">Pokud stránky spustili poprvé, kód tu ID z řetězce dotazu.</span><span class="sxs-lookup"><span data-stu-id="83882-258">When the page ran the first time, the code got the ID out of the query string.</span></span> <span data-ttu-id="83882-259">Získat hodnotu z pole skrytá a ujistěte se, že vám ID videa, která byla původně zobrazena, v případě, že od té doby se nějakým způsobem změnit řetězec dotazu.</span><span class="sxs-lookup"><span data-stu-id="83882-259">You get the value from the hidden field to make sure that you're getting the ID of the movie that was originally displayed, in case the query string was somehow altered since then.</span></span>

<span data-ttu-id="83882-260">Opravdu důležité rozdíl mezi *AddMovie* kód a tento kód je, že v tomto kódu použít SQL `Update` příkaz místo `Insert Into` příkaz.</span><span class="sxs-lookup"><span data-stu-id="83882-260">The really important difference between the *AddMovie* code and this code is that in this code you use the SQL `Update` statement instead of the `Insert Into` statement.</span></span> <span data-ttu-id="83882-261">Následující příklad ukazuje syntaxi SQL `Update` příkaz:</span><span class="sxs-lookup"><span data-stu-id="83882-261">The following example shows the syntax of the SQL `Update` statement:</span></span>

`UPDATE table SET col1="value", col2="value", col3="value" ... WHERE ID = value`

<span data-ttu-id="83882-262">Můžete zadat všechny sloupce v libovolném pořadí a nemáte nutně aktualizovat každý sloupec během `Update` operaci.</span><span class="sxs-lookup"><span data-stu-id="83882-262">You can specify any columns in any order, and you don't necessarily have to update every column during an `Update` operation.</span></span> <span data-ttu-id="83882-263">(ID samostatně, nelze aktualizovat, protože záznam, výsledkem by uložit jako nový záznam a který není povolen pro `Update` operace.)</span><span class="sxs-lookup"><span data-stu-id="83882-263">(You cannot update the ID itself, because that would in effect save the record as a new record, and that's not allowed for an `Update` operation.)</span></span>

> [!NOTE] 
> 
> <span data-ttu-id="83882-264">**Důležité** `Where` klauzule s ID je velmi důležité, protože se jedná o tom, jak databázi zná databázi, kterou záznamů, které chcete aktualizovat.</span><span class="sxs-lookup"><span data-stu-id="83882-264">**Important** The `Where` clause with the ID is very important, because that's how the database knows which database record you want to update.</span></span> <span data-ttu-id="83882-265">Pokud jste ji přerušili `Where` klauzuli by aktualizoval databázi *každých* záznamů v databázi.</span><span class="sxs-lookup"><span data-stu-id="83882-265">If you left off the `Where` clause, the database would update *every* record in the database.</span></span> <span data-ttu-id="83882-266">Ve většině případů, které by se havárie.</span><span class="sxs-lookup"><span data-stu-id="83882-266">In most cases, that would be a disaster.</span></span>


<span data-ttu-id="83882-267">V kódu jsou hodnoty tak, aby aktualizace předán příkaz jazyka SQL s použitím zástupné symboly.</span><span class="sxs-lookup"><span data-stu-id="83882-267">In the code, the values to update are passed to the SQL statement by using placeholders.</span></span> <span data-ttu-id="83882-268">Opakování, co jsme uvedli jste před: z bezpečnostních důvodů *pouze* použít zástupné symboly a předat hodnoty do příkazu SQL.</span><span class="sxs-lookup"><span data-stu-id="83882-268">To repeat what we've said before: for security reasons, *only* use placeholders to pass values to a SQL statement.</span></span>

<span data-ttu-id="83882-269">Po kód používá `db.Execute` ke spuštění `Update` prohlášení, je přesměrován zpět na stránku výpis, kde můžete sledovat změny.</span><span class="sxs-lookup"><span data-stu-id="83882-269">After the code uses `db.Execute` to run the `Update` statement, it redirects back to the listing page, where you can see the changes.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="83882-270">**Jiný SQL příkazy, různé metody**</span><span class="sxs-lookup"><span data-stu-id="83882-270">**Different SQL Statements, Different Methods**</span></span>
> 
> <span data-ttu-id="83882-271">Možná jste si všimli, používáte mírně různé metody pro spouštění různých příkazů SQL.</span><span class="sxs-lookup"><span data-stu-id="83882-271">You might have noticed that you use slightly different methods to run different SQL statements.</span></span> <span data-ttu-id="83882-272">Ke spuštění `Select` dotaz to potenciálně vrací více záznamů, můžete použít `Query` metoda.</span><span class="sxs-lookup"><span data-stu-id="83882-272">To run a `Select` query that potentially returns multiple records, you use the `Query` method.</span></span> <span data-ttu-id="83882-273">Ke spuštění `Select` dotaz, který víte, bude vrátit jen jednu položku databáze, můžete použít `QuerySingle` metoda.</span><span class="sxs-lookup"><span data-stu-id="83882-273">To run a `Select` query that you know will return only one database item, you use the `QuerySingle` method.</span></span> <span data-ttu-id="83882-274">Ke spuštění příkazů, které usnadňují změny, ale které nevrací položky databáze, můžete použít `Execute` metoda.</span><span class="sxs-lookup"><span data-stu-id="83882-274">To run commands that make changes but that don't return database items, you use the `Execute` method.</span></span>
> 
> <span data-ttu-id="83882-275">Budete muset mít různé metody, protože každý z nich vrátí odlišné výsledky, protože jste viděli již rozdíl mezi `Query` a `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="83882-275">You have to have different methods because each of them returns different results, as you saw already in the difference between `Query` and `QuerySingle`.</span></span> <span data-ttu-id="83882-276">( `Execute` Ve skutečnosti vrátí metoda hodnotu také &mdash; konkrétně, počet řádky databáze, které byly ovlivněny příkaz &mdash; ale můžete jste byla ignorována, dosavadní.)</span><span class="sxs-lookup"><span data-stu-id="83882-276">(The `Execute` method actually returns a value also &mdash; namely, the number of database rows that were affected by the command &mdash; but you've been ignoring that so far.)</span></span>
> 
> <span data-ttu-id="83882-277">Samozřejmě `Query` metoda může vrátit pouze jeden řádek databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-277">Of course, the `Query` method might return only one database row.</span></span> <span data-ttu-id="83882-278">Ale technologie ASP.NET vždy zpracuje výsledky `Query` metoda jako kolekce.</span><span class="sxs-lookup"><span data-stu-id="83882-278">However, ASP.NET always treats the results of the `Query` method as a collection.</span></span> <span data-ttu-id="83882-279">I v případě, že metoda vrátí pouze jeden řádek, budete muset tento jeden řádek extrahovat z kolekce.</span><span class="sxs-lookup"><span data-stu-id="83882-279">Even if the method returns just one row, you have to extract that single row from the collection.</span></span> <span data-ttu-id="83882-280">Proto v situacích, kde jste *vědět* získáte zpět pouze jeden řádek, je bit pohodlnější použití `QuerySingle`.</span><span class="sxs-lookup"><span data-stu-id="83882-280">Therefore, in situations where you *know* you'll get back only one row, it's a bit more convenient to use `QuerySingle`.</span></span>
> 
> <span data-ttu-id="83882-281">Existuje několik metod, které provádějí konkrétní typy databázových operací.</span><span class="sxs-lookup"><span data-stu-id="83882-281">There are a few other methods that perform specific types of database operations.</span></span> <span data-ttu-id="83882-282">Můžete najít v seznamu metod databáze v [Stručná referenční příručka aplikace ASP.NET Web Pages rozhraní API](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span><span class="sxs-lookup"><span data-stu-id="83882-282">You can find a listing of database methods in the [ASP.NET Web Pages API Quick Reference](../../api-reference/asp-net-web-pages-api-reference.md#Data).</span></span>


## <a name="making-validation-for-the-id-more-robust"></a><span data-ttu-id="83882-283">Provedení ověření informace ID robustní</span><span class="sxs-lookup"><span data-stu-id="83882-283">Making Validation for the ID More Robust</span></span>

<span data-ttu-id="83882-284">Při prvním spuštění stránky, můžete získat ID film z řetězce dotazu, takže můžete přejít, že film získat z databáze.</span><span class="sxs-lookup"><span data-stu-id="83882-284">The first time that the page runs, you get the movie ID from the query string so that you can go get that movie from the database.</span></span> <span data-ttu-id="83882-285">Můžete provedeny jistotu, že ve skutečnosti došlo hodnotu na vyhledání, která jste udělali s využitím tento kód:</span><span class="sxs-lookup"><span data-stu-id="83882-285">You made sure that there actually was a value to go look for, which you did by using this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample13.cs)]

<span data-ttu-id="83882-286">Použít tento kód a ujistěte se, pokud uživatel získá k *EditMovies* stránky bez první výběru video v *filmy* stránky, na stránce se zobrazí uživatelsky přívětivý chybová zpráva.</span><span class="sxs-lookup"><span data-stu-id="83882-286">You used this code to make sure that if a user gets to the *EditMovies* page without first selecting a movie in the *Movies* page, the page would display a user-friendly error message.</span></span> <span data-ttu-id="83882-287">(Jinak, uživatelé by viz chybu, která je pravděpodobně pouze matou.)</span><span class="sxs-lookup"><span data-stu-id="83882-287">(Otherwise, users would see an error that would probably just confuse them.)</span></span>

<span data-ttu-id="83882-288">Toto ověření není však nepříliš robustní.</span><span class="sxs-lookup"><span data-stu-id="83882-288">However, this validation isn't very robust.</span></span> <span data-ttu-id="83882-289">Stránka může být vyvolána také s tyto chyby:</span><span class="sxs-lookup"><span data-stu-id="83882-289">The page might also be invoked with these errors:</span></span>

- <span data-ttu-id="83882-290">ID není číslo.</span><span class="sxs-lookup"><span data-stu-id="83882-290">The ID isn't a number.</span></span> <span data-ttu-id="83882-291">Například může vyvolat stránku s adresou URL jako `http://localhost:nnnnn/EditMovie?id=abc`.</span><span class="sxs-lookup"><span data-stu-id="83882-291">For example, the page could be invoked with a URL like `http://localhost:nnnnn/EditMovie?id=abc`.</span></span>
- <span data-ttu-id="83882-292">ID je číslo, ale odkazuje na film, který neexistuje (například `http://localhost:nnnnn/EditMovie?id=100934`).</span><span class="sxs-lookup"><span data-stu-id="83882-292">The ID is a number, but it references a movie that doesn't exist (for example, `http://localhost:nnnnn/EditMovie?id=100934`).</span></span>

<span data-ttu-id="83882-293">Pokud jste zvědaví, pokud chcete zobrazit chyby, které jsou výsledkem tyto adresy URL, spusťte *filmy* stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-293">If you're curious to see the errors that result from these URLs, run the *Movies* page.</span></span> <span data-ttu-id="83882-294">Vyberte video upravit a poté změňte adresu URL *EditMovie* stránky na adresu URL, která obsahuje abecedním ID nebo ID film neexistující.</span><span class="sxs-lookup"><span data-stu-id="83882-294">Select a movie to edit, and then change the URL of the *EditMovie* page to a URL that contains an alphabetic ID or the ID of a non-existent movie.</span></span>

<span data-ttu-id="83882-295">Proto co můžete dělat?</span><span class="sxs-lookup"><span data-stu-id="83882-295">So what should you do?</span></span> <span data-ttu-id="83882-296">První je zajistit, že ne jenom je ID předána na stránku, ale že ID je celé číslo.</span><span class="sxs-lookup"><span data-stu-id="83882-296">The first fix is to make sure that not only is an ID passed to the page, but that the ID is an integer.</span></span> <span data-ttu-id="83882-297">Změnit kód `!IsPost` test, aby vypadala jako v tomto příkladu:</span><span class="sxs-lookup"><span data-stu-id="83882-297">Change the code for the `!IsPost` test to look like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample14.cs)]

<span data-ttu-id="83882-298">Na druhou podmínku, která jste přidali `IsEmpty` test, propojený s `&&` (logické a):</span><span class="sxs-lookup"><span data-stu-id="83882-298">You've added a second condition to the `IsEmpty` test, linked with `&&` (logical AND):</span></span>

[!code-csharp[Main](updating-data/samples/sample15.cs)]

<span data-ttu-id="83882-299">Může pamatovat z [Úvod do programování webových stránek ASP.NET](../introducing-razor-syntax-c.md) kurz, který jako metody `AsBool` `AsInt` převést na jiný datový typ řetězec znaků.</span><span class="sxs-lookup"><span data-stu-id="83882-299">You might remember from the [Introduction to ASP.NET Web Pages Programming](../introducing-razor-syntax-c.md) tutorial that methods like `AsBool` an `AsInt` convert a character string to some other data type.</span></span> <span data-ttu-id="83882-300">`IsInt` – Metoda (a jiné, jako jsou `IsBool` a `IsDateTime`) jsou podobné.</span><span class="sxs-lookup"><span data-stu-id="83882-300">The `IsInt` method (and others, like `IsBool` and `IsDateTime`) are similar.</span></span> <span data-ttu-id="83882-301">Ale testují pouze zda je *můžete* převést řetězec bez ve skutečnosti převod.</span><span class="sxs-lookup"><span data-stu-id="83882-301">However, they test only whether you *can* convert the string, without actually performing the conversion.</span></span> <span data-ttu-id="83882-302">Proto zde jste se v podstatě oznámením *Pokud hodnotu řetězce dotazu lze převést na celé číslo...* .</span><span class="sxs-lookup"><span data-stu-id="83882-302">So here you're essentially saying *If the query string value can be converted to an integer ...*.</span></span>

<span data-ttu-id="83882-303">Potenciální problém hledá film, který neexistuje.</span><span class="sxs-lookup"><span data-stu-id="83882-303">The other potential problem is looking for a movie that doesn't exist.</span></span> <span data-ttu-id="83882-304">Kód slouží k získání film vypadá tento kód:</span><span class="sxs-lookup"><span data-stu-id="83882-304">The code to get a movie looks like this code:</span></span>

[!code-csharp[Main](updating-data/samples/sample16.cs)]

<span data-ttu-id="83882-305">Pokud předáte `movieId` hodnotu `QuerySingle` metoda, která neodpovídá skutečné film, nic nevrátí a příkazy, které následují (například `title=row.Title`) vést k chybám.</span><span class="sxs-lookup"><span data-stu-id="83882-305">If you pass a `movieId` value to the `QuerySingle` method that doesn't correspond to an actual movie, nothing is returned and the statements that follow (for example, `title=row.Title`) result in errors.</span></span>

<span data-ttu-id="83882-306">Je opět snadno opravu.</span><span class="sxs-lookup"><span data-stu-id="83882-306">Again there's an easy fix.</span></span> <span data-ttu-id="83882-307">Pokud `db.QuerySingle` metoda vrátí žádné výsledky `row` proměnná bude mít hodnotu null.</span><span class="sxs-lookup"><span data-stu-id="83882-307">If the `db.QuerySingle` method returns no results, the `row` variable will be null.</span></span> <span data-ttu-id="83882-308">Proto můžete zkontrolovat, zda `row` proměnná má hodnotu null, před pokusem o získání hodnoty z něj.</span><span class="sxs-lookup"><span data-stu-id="83882-308">So you can check whether the `row` variable is null before you try to get values from it.</span></span> <span data-ttu-id="83882-309">Následující kód přidá `if` bloku kolem příkazy, které získávají hodnoty z `row` objektu:</span><span class="sxs-lookup"><span data-stu-id="83882-309">The following code adds an `if` block around the statements that get the values out of the `row` object:</span></span>

[!code-csharp[Main](updating-data/samples/sample17.cs)]

<span data-ttu-id="83882-310">Stránku s tyto dva další ověřovací testy, stane odrážka ověření.</span><span class="sxs-lookup"><span data-stu-id="83882-310">With these two additional validation tests, the page becomes more bullet-proof.</span></span> <span data-ttu-id="83882-311">Kompletní kód `!IsPost` větve bude vypadat jako tento ukázkový:</span><span class="sxs-lookup"><span data-stu-id="83882-311">The complete code for the `!IsPost` branch now looks like this example:</span></span>

[!code-csharp[Main](updating-data/samples/sample18.cs)]

<span data-ttu-id="83882-312">Jsme budete jednou Upozorňujeme, že tato úloha je vhodné využít pro `else` bloku.</span><span class="sxs-lookup"><span data-stu-id="83882-312">We'll note once more that this task is a good use for an `else` block.</span></span> <span data-ttu-id="83882-313">Pokud nemáte jsou testy úspěšné, `else` bloky nastavit chybové zprávy.</span><span class="sxs-lookup"><span data-stu-id="83882-313">If the tests don't pass, the `else` blocks set error messages.</span></span>

## <a name="adding-a-link-to-return-to-the-movies-page"></a><span data-ttu-id="83882-314">Přidání odkazu na návrat na stránku filmy</span><span class="sxs-lookup"><span data-stu-id="83882-314">Adding a Link to Return to the Movies Page</span></span>

<span data-ttu-id="83882-315">Konečné a užitečné podrobností je přidáte propojení zpět *filmy* stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-315">A final and helpful detail is to add a link back to the *Movies* page.</span></span> <span data-ttu-id="83882-316">Obyčejnou toku události, uživatelé spustí ve *filmy* a klikněte na tlačítko **upravit** odkaz.</span><span class="sxs-lookup"><span data-stu-id="83882-316">In the ordinary flow of events, users will start at the *Movies* page and click an **Edit** link.</span></span> <span data-ttu-id="83882-317">Která integruje do *EditMovie* stránky, kde můžete upravit film a klikněte na tlačítko.</span><span class="sxs-lookup"><span data-stu-id="83882-317">That brings them to the *EditMovie* page, where they can edit the movie and click the button.</span></span> <span data-ttu-id="83882-318">Po kód zpracovává změnu, je přesměrován zpět *filmy* stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-318">After the code processes the change, it redirects back to the *Movies* page.</span></span>

<span data-ttu-id="83882-319">Ale:</span><span class="sxs-lookup"><span data-stu-id="83882-319">However:</span></span>

- <span data-ttu-id="83882-320">Uživatel může rozhodnout nic nezmění.</span><span class="sxs-lookup"><span data-stu-id="83882-320">The user might decide not to change anything.</span></span>
- <span data-ttu-id="83882-321">Uživatel může být, že podmínky na tuto stránku bez první kliknutí na **upravit** na odkaz v *filmy* stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-321">The user might have gotten to this page without first clicking an **Edit** link in the *Movies* page.</span></span>

<span data-ttu-id="83882-322">V obou případech chcete mohli snadno vrátit do hlavní výpis.</span><span class="sxs-lookup"><span data-stu-id="83882-322">Either way, you want to make it easy for them to return to the main listing.</span></span> <span data-ttu-id="83882-323">Je snadno opravu &mdash; přidejte následující kód bezprostředně za ukončovací `</form>` značky v kódu:</span><span class="sxs-lookup"><span data-stu-id="83882-323">It's an easy fix &mdash; add the following markup just after the closing `</form>` tag in the markup:</span></span>

[!code-html[Main](updating-data/samples/sample19.html)]

<span data-ttu-id="83882-324">Tyto značky používají stejnou syntaxi pro `<a>` element, který jste se seznámili s jinde.</span><span class="sxs-lookup"><span data-stu-id="83882-324">This markup uses the same syntax for an `<a>` element that you've seen elsewhere.</span></span> <span data-ttu-id="83882-325">Adresa URL obsahuje `~` rozumí "root webu."</span><span class="sxs-lookup"><span data-stu-id="83882-325">The URL includes `~` to mean "root of the website."</span></span>

## <a name="testing-the-movie-update-process"></a><span data-ttu-id="83882-326">Testování procesu aktualizace filmu</span><span class="sxs-lookup"><span data-stu-id="83882-326">Testing the Movie Update Process</span></span>

<span data-ttu-id="83882-327">Nyní můžete otestovat.</span><span class="sxs-lookup"><span data-stu-id="83882-327">Now you can test.</span></span> <span data-ttu-id="83882-328">Spustit *filmy* a klikněte na tlačítko **upravit** vedle film.</span><span class="sxs-lookup"><span data-stu-id="83882-328">Run the *Movies* page, and click **Edit** next to a movie.</span></span> <span data-ttu-id="83882-329">Když *EditMovie* se zobrazí stránka provádět změny film a klikněte na tlačítko **odeslání změn**.</span><span class="sxs-lookup"><span data-stu-id="83882-329">When the *EditMovie* page appears, make changes to the movie and click **Submit Changes**.</span></span> <span data-ttu-id="83882-330">Jakmile se zobrazí seznam film, ujistěte se, že se změny zobrazují.</span><span class="sxs-lookup"><span data-stu-id="83882-330">When the movie listing appears, make sure that your changes are shown.</span></span>

<span data-ttu-id="83882-331">Chcete-li mít jistotu, že ověřování pracuje, klikněte na tlačítko **upravit** pro jiné film.</span><span class="sxs-lookup"><span data-stu-id="83882-331">To make sure that validation is working, click **Edit** for another movie.</span></span> <span data-ttu-id="83882-332">Když dojde k *EditMovie* zrušte zaškrtnutí políčka **Genre** pole (nebo **roku** pole, nebo obě) a zkuste odeslat změny.</span><span class="sxs-lookup"><span data-stu-id="83882-332">When you get to the *EditMovie* page, clear the **Genre** field (or **Year** field, or both) and try to submit your changes.</span></span> <span data-ttu-id="83882-333">Se zobrazí chyba, jak jste zvyklí:</span><span class="sxs-lookup"><span data-stu-id="83882-333">You'll see an error, as you'd expect:</span></span>

![Upravit film stránky zobrazující chyby ověřování](updating-data/_static/image4.png)

<span data-ttu-id="83882-335">Klikněte na tlačítko **vrátit do seznamu film** odkaz na zrušte změny a vrátit se *filmy* stránky.</span><span class="sxs-lookup"><span data-stu-id="83882-335">Click the **Return to movie listing** link to abandon your changes and return to the *Movies* page.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="83882-336">Objevuje další</span><span class="sxs-lookup"><span data-stu-id="83882-336">Coming Up Next</span></span>

<span data-ttu-id="83882-337">V dalším kurzu se zobrazí, jak odstranit záznam film.</span><span class="sxs-lookup"><span data-stu-id="83882-337">In the next tutorial, you'll see how to delete a movie record.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-edit-links"></a><span data-ttu-id="83882-338">Úplný seznam pro stránku Movie (aktualizovat pomocí odkazů pro úpravy)</span><span class="sxs-lookup"><span data-stu-id="83882-338">Complete Listing for Movie Page (Updated with Edit Links)</span></span>

[!code-cshtml[Main](updating-data/samples/sample20.cshtml)]

<a id="Complete_Page_Listing_for_EditMovie"></a>
## <a name="complete-page-listing-for-edit-movie-page"></a><span data-ttu-id="83882-339">Dokončení výpis stránky pro stránku film úpravy</span><span class="sxs-lookup"><span data-stu-id="83882-339">Complete Page Listing for Edit Movie Page</span></span>

[!code-cshtml[Main](updating-data/samples/sample21.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="83882-340">Další prostředky</span><span class="sxs-lookup"><span data-stu-id="83882-340">Additional Resources</span></span>

- [<span data-ttu-id="83882-341">Úvod do rozhraní ASP.NET Web programování pomocí syntaxe Razor</span><span class="sxs-lookup"><span data-stu-id="83882-341">Introduction to ASP.NET Web Programming by Using the Razor Syntax</span></span>](../introducing-razor-syntax-c.md)
- <span data-ttu-id="83882-342">[Prohlášení aktualizace SQL](http://www.w3schools.com/sql/sql_update.asp) na webu W3Schools</span><span class="sxs-lookup"><span data-stu-id="83882-342">[SQL UPDATE Statement](http://www.w3schools.com/sql/sql_update.asp) on the W3Schools site</span></span>

>[!div class="step-by-step"]
<span data-ttu-id="83882-343">[Předchozí](entering-data.md)
[další](deleting-data.md)</span><span class="sxs-lookup"><span data-stu-id="83882-343">[Previous](entering-data.md)
[Next](deleting-data.md)</span></span>
